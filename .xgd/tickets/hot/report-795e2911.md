---
uid: report-795e2911
id: REPORT-1154
type: report
title: 'Code Review: bundle-4ff83a8b'
created_by: xgd
created_at: '2026-08-03T05:33:36.569374+00:00'
updated_at: '2026-08-03T05:33:36.569374+00:00'
completed_at: null
last_field_updated: created_at
result: fail
fields:
  report_kind: code_review
  subject_uid: bundle-4ff83a8b
  anchor_uid: bundle-4ff83a8b
---

# Code Review

**Result**: FAIL
**Anchor**: bundle-4ff83a8b (BUNDLE-10) · **Mode**: commits
**Scope**: 23 free-coded commits + 1 fix commit; 36 non-test production files (6,854 insertions / 719 deletions)
**Pass**: second — re-review after `fix_review_free_coded` (report-afa81f3e)

## Summary

The previous review's **critical** issue is genuinely fixed and I verified it independently:
the 33 stale `geometry.anchor` nodes in `storage/sites/gigabytealchemy/draft/pages/home.json`
are migrated exactly and mechanically to the `{x, width}` term model, nothing else in the
document changed, and `1c render gigabytealchemy` now exits 0. The guard test the review
asked for exists, is attached to a real AC (AC-765), and is non-vacuous. The code itself
remains high quality — typed, bounded, validated, no dead modules, no debug residue.

However, the **full suite is red**: `npx vitest run` at HEAD is **868 passed / 1 failed**.
`tests/bug17-fold-padding.test.ts` — a file **added by this bundle** — reads the gitignored
`storage/references/…/multistate.json` **without the `existsSync` skip guard that every other
real-capture UAT in this repo uses**, so it hard-fails on any clean checkout. The scoped
quality run passes only because that test is one of the 751 deselected. This is the same
class of defect as the last pass (a green scoped run masking a red artifact), it is a
regression relative to `main`, and the fix is two lines with an in-repo precedent.

## Quality Gates

| Gate | Report (report-45906fe4) | Independent verification |
|------|--------------------------|--------------------------|
| Lint | success, 0 errors / 0 warnings | **No-op** — 0.0001s; no `lint` script in `package.json` |
| Build | success | **No-op** — 0.0s; root `build` is `pnpm -r build`, not run by the gate |
| Tests (scoped) | 118 passed, 0 failed, 751 deselected | matches |
| Tests (**full**) | not run by the gate | **`npx vitest run` → 1 failed / 868 passed (126 files)** — **FAIL** |
| Typecheck | not a gate | `tsc --noEmit` on site-schema, framework, tools/generate — **all exit 0, clean** |

Because lint/build are unwired no-ops, compilation was verified manually; it is clean.

## External Interface Accessibility

New entry points wired in: **yes** — no dead modules, unchanged from the last pass and
re-spot-checked.

| New/changed surface | Wired at |
|---|---|
| `tools/generate/src/l1/forms.ts` | `fold.ts:55-60`, `fold.ts:2033`; types consumed `capture.ts:9`, `bundle.ts:20`; re-exported `l1/index.ts:27-32` |
| `tools/generate/src/l1/assets.ts` (`localizeAssets`) | called `cli/repro.ts:131`; imported `repro.ts:23`; exported `l1/index.ts:34` |
| `packages/site-schema/src/l1/slots.ts` | `schema.ts:3` (page validator binding check), `l1/index.ts` |
| `latestModuleVersion` | `registry.ts:33` → `modules/index.ts:1` → `framework/src/index.ts:26` |
| `renderL1Document(doc, {mounts})` | `tools/generate/src/render/render.ts:119-125` |
| `needsAstro` widened for L1+module pages | `render/render.ts:207` |

`1c --help` (exit 0) documents `repro` and `l1-gate`; both routed in the parser.

## Code Quality

| File | Finding | Severity |
|------|---------|----------|
| `tests/bug17-fold-padding.test.ts:114-116` | Real-capture UAT reads gitignored `storage/references/gigabytealchemy.ai/index/multistate.json` with **no `existsSync` guard**; hard-fails `ENOENT` on a clean checkout. Every sibling real-capture UAT guards (`req91:316`, `req92`, `req90`) and `req91:312-314` documents the convention verbatim. It is the **only** new test in the bundle that touches that path unguarded. | **Critical** |
| `tools/generate/src/cli/capture/playwright-driver.ts:21-32` | The BUG-16 doc comment that documented `FONT_BARRIER` is now orphaned above `FONT_LOAD_BUDGET_MS` — the fix inserted the new constants between the comment and the const it describes. Cosmetic; move the comment back down to `FONT_BARRIER`. | Nit |
| quality config | `lint` / `build` gates remain unwired no-ops and no typecheck runs in the gate. Deliberately deferred by the fix pass as an operator decision (config is main-only); recorded again, not held against this bundle. | Warning (open) |

Positives verified this pass: **zero** `console.log`/`console.debug`, `TODO`, `FIXME`,
`@ts-ignore`, `@ts-expect-error`, `eslint-disable`, `: any`/`as any`, or commented-out code
in the added production lines; magic values are named constants with measured rationale in
their doc comments (`PADDING_MAX`, `BAND_TAIL_PAD`, `CLUSTER_GAP_FACTOR`,
`FONT_LOAD_BUDGET_MS`, `FONTS_READY_BUDGET_MS`); reuse-first holds — everything lands in
existing files/axes, no `_v2` parallel implementations; the deleted `tests/bug11-fold-surface-fill.test.ts`
is properly superseded by `bug14`/`reconciliation-l1-fold-surface-reconstruction` coverage,
not merely dropped.

### Verification of the previous pass's fixes

| Prior finding | Verified |
|---|---|
| Critical — stale `home.json` anchors | **Fixed.** 33 anchors, paths identical pre/post, every one exactly `startPx→x.px` / `startFraction→x.fraction` / `widthPx→width.px` / `widthFraction→width.fraction`, 0 mismatches; all non-anchor content byte-identical. `1c render gigabytealchemy` **exit 0**. Zero retired keys remain anywhere under `storage/`. Semantics confirmed equivalent against `schema.ts:210-228` and `render.ts:339-367` (`width = width.px + width.fraction * extent`). |
| Warning — no guard on committed L1 docs | **Fixed.** `tests/reconciliation-l1-responsive-and-column-axes.test.ts:964` `test_UAT_AC765_committed_site_documents_satisfy_the_l1_envelope` walks all of `storage/sites`, validates every doc carrying `l1`, reports file + first 5 `path :: message`, and asserts the doc set is non-empty so it cannot go vacuous. AC-765 exists and is the correct owner (column/anchor axes). |
| Nit — duplicated validator traversal | **Fixed.** Pre-pass folded into `walk()` via `hasColumn` (`validate.ts:298-310, 363-365, 422`); one `children` recursion remains. |
| Nit — inlined font budgets | **Fixed** (see cosmetic comment-placement note above). |
| Nit — untyped omit | **Fixed.** `forms.ts:181` rest-destructure, no cast. |

## Security Review (DOC-2 — structured-only, validated by construction)

No security checklist ticket exists; this is my own pass against the policy. **No violation found.**

- The one new raw-HTML sink is the slot mount (`packages/framework/src/l1/render.ts:657-670`,
  `${mounted}`). It is fed exclusively from `tools/generate/src/render/render.ts:119-125`, where
  the string is the framework's own Astro module render — the vetted behavior-module seam
  DOC-2 §3 sanctions. `L1RenderOptions.mounts` is not reachable from site JSON; instance
  values inside already passed the module's escaping/URL sinks. The slot's own `name`/`behavior`
  attributes are `escapeHtml`'d.
- Envelope extended coherently, not loosened: every new object is `.strict()`; `paddingPx`
  bounded 0–10,000; scalar tracks range-checked and pinned to declared widths; text chip axes
  bounded exactly like box axes.
- Dangling references rejected, not defaulted: `anchor` without `column` is an error
  (`validate.ts:304-309`) — and now on the single traversal, so a new child-bearing kind
  cannot leak past it.
- Slot binding is validated, not best-effort: unbound module, unknown slot, duplicate slot
  name, and `slot` on a page with no L1 tree are each a pathed error (`schema.ts:564-616`).
- Captured endpoints gated: `forms.ts:263` admits `form.action` only via `isSafeUrl`; the
  module re-checks with `assertSafeUrl` (`contact-form/index.astro:67`). Image `src` gated
  at `render.ts:108` and `fold.ts:1818-1823`.

## Checklist Compliance

No `architecture_checklist`, `security_checklist` or `design_checklist` report tickets exist
for this anchor — all three sections skipped per instruction.

## Smoke Test

| Entry point | Result |
|---|---|
| `node tools/generate/bin/1c.mjs --help` | **PASS** — exit 0 |
| `1c list` | **PASS** — exit 0, 3 sites |
| `1c render gigabytealchemy` (the bundle's reproduction) | **PASS** — exit 0 (was exit 1 last pass) |
| `tsc --noEmit` × site-schema, framework, tools/generate | **PASS** — all exit 0 |
| `npx vitest run` (full) | **FAIL** — 1 failed / 868 passed |
| `1c render 1stcontact` / `harbor-cafe` | exit 1: `Module not found in catalog: 'header' v2. Known modules: contact-form@3, carousel@2.` — **pre-existing**, confirmed: `main`'s registry already carries only those two modules and neither site's storage is touched by this bundle. Out of scope, reported for completeness. |

Worktree left clean (`git status` empty; `storage/dist/` is gitignored).

## Issues Found

**Critical (must fix)**:

- `tests/bug17-fold-padding.test.ts` fails the full suite on a clean checkout. The file is
  new in this bundle (commit `7d918ec42`), and `storage/references/` is gitignored
  (`.gitignore:146`) so it exists only after a local `1c capture page`. Merging this to
  `main` ships a permanently-red `pnpm test`.

**Warnings (should fix)**:

- Orphaned doc comment at `playwright-driver.ts:21-32` (see table).
- `lint` / `build` quality gates are unwired no-ops and no typecheck runs in the gate — this
  is why a scoped-green run keeps masking full-suite and artifact defects. Genuinely open;
  needs an operator decision (config is main-only, not editable from a branch worktree).

**Informational (not held against this bundle)**:

- All 14 `tests/bug1[2-9]*` / `bug2[0-5]*` files are still `test_UAT_FC_BUG-nn_*`-named.
  `fc_orphan_check` and `reconciliation_test_naming_check` both passed, so this is the
  reconciliation loop's concern, not this review's.

## Fix-It Prompt

**One change.** Add the missing skip guard to the real-capture UAT, matching the convention
already established in this repo.

File: `tests/bug17-fold-padding.test.ts`.

1. Add `existsSync` to the `node:fs` import on line 17:

```ts
import { existsSync, readFileSync } from 'node:fs'
```

2. In `test_UAT_FC_BUG-17_fold_gigabytealchemy_badge_padding` (line 114), guard the bundle
   read exactly as `tests/req91-l1-pixel-mover-axes.test.ts:312-316` does — copy that
   comment's reasoning so the convention stays visible:

```ts
const bundle = path.resolve(__dirname, '../storage/references/gigabytealchemy.ai/index/multistate.json')
// `storage/references` is gitignored — the bundle only exists after a local
// `1c capture page`. Skip cleanly when it is absent (the same convention the
// REQ-90/REQ-91/REQ-92 real-capture UATs use) rather than failing on a clean checkout.
if (!existsSync(bundle)) return
const multiState = JSON.parse(readFileSync(bundle, 'utf8')) as MultiStateCapture
```

Leave the rest of the test body unchanged — the assertions are correct and valuable when the
bundle *is* present.

Do **not** "fix" this by deleting the test, by committing the capture bundle (it is
deliberately gitignored), or by weakening its assertions.

Optional, while in the file: move the BUG-16 block comment at
`tools/generate/src/cli/capture/playwright-driver.ts:21-32` back down so it sits directly
above `const FONT_BARRIER` (line 47) rather than above `FONT_LOAD_BUDGET_MS`.

Verify — all four must hold:

```
npx vitest run                                          # must be 0 failed
node tools/generate/bin/1c.mjs render gigabytealchemy   # must exit 0
node tools/generate/bin/1c.mjs --help                   # must exit 0
npx tsc -p tools/generate/tsconfig.json --noEmit        # must exit 0
```

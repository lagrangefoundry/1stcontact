---
uid: bug-b6268a25
id: BUG-55
type: bug
title: '1c kb build cannot finish: search is called with source, upstream wants indexes'
created_by: xgd
created_at: '2026-09-05T22:14:22.099842+00:00'
updated_at: '2026-09-05T22:14:22.099842+00:00'
completed_at: null
last_field_updated: created_at
status: draft
fields:
  severity: high
  priority: high
  auto_merge_back: true
  needs_review: false
---

# `1c kb build` cannot finish: `search` is called with `source`, upstream wants `indexes`

## The failure

```
$ ./bin/1c kb build
knowledge base 'system' reads from source 'shipped', which this host has no index for (available: none)
```

`tools/generate/src/cli/kb.ts:927` calls `lib.search(query, { source: indexSource,
… , sources: binding.sources })`. Upstream `@lagrangefoundry/knowledge` takes
**`indexes`** — a map from *source name* to `IndexSource` — and `scopedIndexes`
resolves each KB through `indexFor(kb, indexes)`
(`knowledge/src/search.js:127`). With no `indexes` key the map is empty, so
`available: none`.

`indexFor` refuses rather than degrading, deliberately:

> *"That is a deployment error and it fails here, loudly, rather than
> contributing no rows to the ranking — which is indistinguishable, from the
> caller's side, from 'this knowledge base is empty'."*

The refusal is right. The caller is wrong.

## What changed

Upstream REQ-112 made it **one index per source** rather than one index per host.
A KB's `source` now names both the store its corpus resolves against and the
artifact its vectors live in, so a singular `source:` no longer says enough. This
repository still passes the pre-REQ-112 shape.

The shared components arrive out of band — `bin/install` writes them into a store
this repository cannot update from a lockfile — so nothing in a `pnpm install`
would have caught the skew, and nothing in CI notices, because no suite drives
`1c kb build`.

## Where

Four call sites in `tools/generate/src/cli/kb.ts`, all constructing a
`nodeIndexSource` already and handing it over under the old key:

| Line | Call |
| --- | --- |
| `927` | `lib.search(…, { source: indexSource, sources: binding.sources })` |
| `1033` | `lib.buildIndex(store, kbs, indexSource, { sources })` |
| `1039` | `lib.buildChunkIndex(store, kbs, chunkSource, { sources })` |
| `1073` | `{ source: nodeIndexSource(…), chunkSource: nodeIndexSource(…), sources }` |

The system KB names source `shipped` (`kb/knowledge_bases.json`), so the map is a
single entry: `indexes: { [SHIPPED_SOURCE]: indexSource }`. `binding.sources` is a
map of **corpus stores** and is a different seam — it stays.

## Why it went unnoticed, and what it has been costing

`kb build` runs four stages and dies at the last one:

```
corpus → index → chunks → awareness map
                            ↑ here
```

So the first three succeed and `1c kb status` reports `index: built, chunks:
built, map: built` afterwards — the map being the *previous* one. The command
exits non-zero and leaves a plausible-looking state behind, which is why it reads
as a transient rather than a break.

The cost is real and compounding. `1c assets` inlines the awareness map and now
reports `awareness primed not indexed` on every run, so **every bundle built
since this broke ships a degraded map**. It also explains the index skew that
blocked `1c assets` outright ([[CHAT-23]], 2026-09-05): [[REQ-165]] added the
projected `REF-*` documents to the corpus and no build could complete afterwards
to index them, so `1c assets` refused with three documents missing until a
partial run happened to rebuild the index on the way past.

## Acceptance

- `./bin/1c kb build` completes, all four stages
- `search`, `buildIndex` and `buildChunkIndex` are called with `indexes` keyed by
  source name; `sources` continues to carry corpus stores
- `1c assets` reports the awareness map as indexed, not primed
- something fails when the repository and the shared store disagree about this
  seam — today nothing does, which is the reason a signature change became a
  silent months-long degradation

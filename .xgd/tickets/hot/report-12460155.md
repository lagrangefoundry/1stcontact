---
uid: report-12460155
id: REPORT-2544
type: report
title: 'Fix Site Storage Port: One Async Store Behind Every Edit (uat) — attempt 33'
created_by: xgd
created_at: '2026-08-20T21:40:49.140153+00:00'
updated_at: '2026-08-20T21:40:49.140153+00:00'
completed_at: null
last_field_updated: created_at
fields:
  report_kind: fix_structural_validation
  subject_uid: capability-c4c7a854
  level: uat
  fixes_applied: 0
  progress_made: false
  needs_more_work: true
  violations_remaining: 1
  anchor_report_uid: report-2485c83c
---

# Fix Summary — Site Storage Port: One Async Store Behind Every Edit (uat)

**Attempt**: 33
**Fixes applied this call**: 0
**Violations remaining**: 1 violation (AC-1354), 1 warning (AC-1353), 1 needs_review (capability-wide)
**Needs more work**: true (but not on this branch — see Termination)

## Termination: this is a terminal failure, not a recoverable one

No mutation was applied this call, and none was available. Both actionable findings name target
files that **do not exist in this worktree**, and the validation report's own Notes for the Editor
say so explicitly: *"Do not attempt findings 1 or 2 on `regression-cb0dad9c`."*

I did not inherit that conclusion — I re-derived every load-bearing fact from git this pass. All of
it confirms the report, and one fact is worse than the report recorded.

| Claim | Command | Result |
|---|---|---|
| Branch / cleanliness | `git rev-parse --abbrev-ref HEAD`, `git status --short` | `regression-cb0dad9c`, clean |
| merge-base with `main` | `git merge-base HEAD main` | `0f44ef1ba06d0e071fbe726db099d5908cc425e4` |
| Divergence | `git rev-list --count HEAD..main` | **527** — the report recorded 508; `main` gained 19 more commits during this pass alone |
| Last non-`.xgd` commit here | `git log -2 --format='%h %ci %s' -- . ':(exclude).xgd'` | `fb283f48b`, **2026-08-20 07:53:10** — unchanged across all 32 prior attempts |
| Store modules @ HEAD | `git ls-files tools/generate/src/store` | **8** — `base`, `diff`, `fsutil`, `history`, `index`, `loadSite`, `paths`, `snapshot` |
| Store modules @ `main` | `git ls-tree main --name-only tools/generate/src/store/` | **14** — the 8 above plus `assemble`, `fs-store`, `journal-model`, `journal`, `memory-store`, `site-store` |
| Port tests @ HEAD | `git ls-files tests` filtered | **0 matches** |
| Port tests @ `main` | `git ls-tree main --name-only -r tests/` filtered | `tests/support/site-factory.ts`, `tests/test_UAT_FC_REQ-142_site_store_port.test.ts` |
| AC-132x UATs @ `main` | `git grep -acE "test_UAT_AC13(2[1-9])_" main -- tests` | 8 in `reconciliation-site-storage-port.test.ts`, 1 in `…workers.test.ts` — 9 of 9 |
| AC-135x UATs anywhere | `git grep -aoE "test_UAT_AC135[0-9]_[A-Za-z0-9_]*" main -- tests` | **no output** |
| AC blobs on `main` | `git ls-tree main --name-only .xgd/tickets/hot/acceptance_criterion-{003caa07,56798f01,d4cc3712}.md` | **only `d4cc3712`** (AC-1321); AC-1353 and AC-1354 are branch-local |

The six modules AC-1353 names and the seam AC-1354 needs (`l1Operations`, `fsSiteStore`,
`makeMemorySite`) are all in the absent half. There is no edit to a ticket, a test, or a source file
**on this branch** that closes either finding.

## Actions Taken — by Resolution Category

None. No category was actionable here.

| # | Finding | Category | Why not applied this call |
|---|---|---|---|
| 1 | AC-1354 (`acceptance_criterion-56798f01`) — violation | `uat-add` | Requires `l1Operations` at `tools/generate/src/cli/ai/toolbox.ts:176`, `fsSiteStore` in `cli/index.ts` / `cli/builder.ts` / `cli/ai/toolbox.ts`, and `makeMemorySite()` from `tests/support/site-factory.ts`. **None exist at HEAD.** Authoring it here means porting REQ-141/REQ-142 production code onto a regression branch — feature work in the one place it must not happen. |
| 2 | AC-1353 (`acceptance_criterion-003caa07`) — warning | `uat-edit` | The rename target is `tests/test_UAT_FC_REQ-142_site_store_port.test.ts:105`/`:115`. That file is absent at HEAD. Nothing to rename. |
| 3 | all 11 ACs / capability — needs_review | — | Branch topology. Operator decision by construction; forwarded below. |

### What I deliberately did **not** do

Recorded so a 34th attempt does not mistake these for unexplored options:

- **Did not deprecate AC-1353 / AC-1354.** Both encode live REQ-142 (`free_and_reconciled`) intent
  and AC-1354 is a *genuine* coverage gap on `main`. Deprecating them would clear the violation by
  erasing a real gap — falsifying the matrix to manufacture green.
- **Did not author a failing `test_UAT_AC1354_*` here.** It would import modules that do not exist,
  fail collection, and break the regression suite this branch exists to run.
- **Did not set `uat_coverage` on any AC.** That field is owned by the uat-coverage stage; setting
  it here would manufacture progress without evidence.
- **Did not edit `main`.** Findings 1 and 2 are `main` work, but reaching across from a
  regression-branch fix loop to write feature tests on the integration branch is outside this
  workflow's scope and is the operator's call, not this loop's.

## Code Edits

None this call.

## needs_review Items Forwarded

| Element | Assessor said | Operator decision needed |
|---|---|---|
| `capability-c4c7a854`, all 11 ACs | The capability under check is not present in the tree under check. Branch cut at `0f44ef1ba` (2026-08-19 17:43); REQ-141/REQ-142 landed on `main` afterwards. Zero of 11 ACs are verifiable here. | **Either** (a) re-cut / refresh `regression-cb0dad9c` from current `main` so the uat level is evaluated against the code it describes, **or** (b) exclude `capability-c4c7a854` from this regression run. |
| AC-1354 (`acceptance_criterion-56798f01`) | No UAT on `main` and none here; neither half of its Verification clause is asserted anywhere. | Authorise `test_UAT_AC1354_*` **on `main`**. No production change is needed — the four `fsSiteStore(` sites already match the claim. **Use the exported `l1Operations` at `toolbox.ts:176`, not `createL1Toolbox` at `:505`**: there `store:` is written after the spread, so `{...opts, store: fsSiteStore(ctxOf(opts))}` silently overrides an injected `opts.store`, and a test written through it would pass while running on the filesystem — a false green on the one AC whose point is that no filesystem is reached. |
| AC-1353 (`acceptance_criterion-003caa07`) | Substance proven on `main`, but named for the intent (`UAT_FC_REQ-142 …`), so `test_UAT_AC{number}_` traceability resolves to nothing. | Rename on `main` to `test_UAT_AC1353_*`, or add AC-named wrappers. Traceability only. |

## Why the loop should stop here

Re-cutting is **necessary but not sufficient**: even a branch carrying all of `main`'s tests still
fails this check on AC-1354, because that UAT exists nowhere. Closing this capability's uat level
needs both (a) the branch to see the code and (b) `test_UAT_AC1354_*` authored on `main`. Neither is
reachable from inside this loop.

A structural note for the loop's own design: AC-1353 and AC-1354 were created by the **ac-level**
stage of this same regression run (confirmed above — absent from `main`, present at HEAD). That
stage added two criteria whose UATs can only be written against code that is not on this branch, so
the uat-level stage that follows it was guaranteed to fail before it ran.

Per the failure/error taxonomy this is a **terminal failure** — an expected dead end with no
fix-loop path by construction. The 32-iteration `@fail` → `fix_uat_validation` → `@fail` cycle,
with `main` drifting 500 → 508 → 527 commits ahead while the branch's last source commit stayed
frozen at 07:53:10, is the symptom of it being routed as a recoverable failure instead.

Declaring `progress_made: false` with `needs_more_work: true` so the loop exits to the assessor
rather than spending a 34th attempt against the same wall.

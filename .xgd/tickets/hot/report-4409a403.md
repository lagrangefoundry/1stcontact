---
uid: report-4409a403
id: REPORT-1764
type: report
title: 'Capability-Intent Alignment: In-Page Copy Editing: The Editable Render & The
  Click-to-Edit Gesture (level=uat)'
created_by: xgd
created_at: '2026-08-10T08:43:28.866007+00:00'
updated_at: '2026-08-10T08:43:28.866007+00:00'
completed_at: null
last_field_updated: created_at
result: fail
fields:
  report_kind: capability_validation
  subject_uid: capability-12fee326
  level: uat
  violations: 1
  warnings: 1
  needs_review_count: 0
---

# Capability-Intent Alignment: In-Page Copy Editing: The Editable Render & The Click-to-Edit Gesture
# Level: uat

**Result**: FAIL
**Violations**: 1
**Warnings**: 1
**Needs review**: 0

Checked on `regression-50f23d80`. Every path implicated below is byte-identical
to `main` (`git diff --stat main..HEAD -- tests/ apps/control-app/src/builder/
packages/framework/src/l1/` is empty), so nothing here was introduced by this
branch.

## Cumulative Intent Considered

At `uat` level the AC bodies are the working reference; the ledger below is
recorded for drift-prevention and was consulted only where an AC looked
suspicious (it did not — see Alignment Ledger).

| Intent ID | Status | When | Asked / changed | Counts? |
|---|---|---|---|---|
| BUNDLE-14 (`bundle-0385746c`) | free_and_reconciled | 2026-08-06 | Originated STORY-98 — the edit render channel (BUG-31 + REQ-114 + REQ-116) | YES |
| BUNDLE-16 (`bundle-15c1f647`) | free_and_reconciled | 2026-08-07 | Originated STORY-101 — the click-to-edit gesture; updated STORY-98 (page stamp, hover rule, vocabulary→schema, contact-form seam marker) (REQ-117 + REQ-115 + REQ-44) | YES |
| REQ-118 (`request-66e4c630`) | free_and_reconciled | 2026-07-31 | Updated STORY-101 — image selection reaches the operator through the same kind-agnostic gesture (AC-1028) | YES |
| BUG-33 (`bug-ede1fb8c`) | ready_to_reconcile | (working_sha `af78081b`, **main_sha null**) | Test-side repairs to the very UATs below; **not yet in main** | imminent — see Finding 1 |

## Alignment Ledger

| Element | Intents aligned to | Outcome |
|---|---|---|
| STORY-98 `story-af36c2cb` (upgrade) — AC-948…958, AC-1007, AC-1008 | BUNDLE-14, BUNDLE-16 | aligned — all 13 ACs have substantive UATs in `tests/reconciliation-edit-render-channel.test.ts`, driving the real `cmdRender`/`cmdPublish`/`cmdRevisions` entry points and reading bytes off disk. 13/13 green. |
| STORY-101 `story-3bf94bd4` (feature) — AC-993…1006 | BUNDLE-16 | **gap**: AC-994, AC-1000, AC-1001, AC-1002, AC-1003 have UATs that do not currently pass (Finding 1). AC-993/995/996/997/998/999/1004/1005/1006 aligned and green. |
| STORY-101 — AC-1028 | REQ-118 | aligned — two UATs, two shapes (CLI derivation + builder-origin transport); green. |

**Coverage (exists)**: every one of the 28 active ACs has at least one
`test_UAT_AC<n>_*` test, and every one of those tests drives a real entry point
(CLI command functions, the builder origin over HTTP, a real DOM parsed from the
bytes `1c render --edit` wrote, and — where a machine can — Playwright). No
structural/AST-only stand-ins, no internal mocking.

**Evidence run** (this machine: webui components present, Chromium launchable —
so nothing reported `UNVERIFIED`):

```
tests/reconciliation-edit-render-channel.test.ts     13 passed
tests/reconciliation-copy-edit-gesture.test.ts        9 passed
tests/req118-image-selection.test.ts                 11 passed
tests/reconciliation-copy-edit-gesture-modal.test.ts  5 FAILED (5/5)
```

## Findings

| # | Severity | Property | Element | Resolution category | Issue | Suggested edit |
|---|---|---|---|---|---|---|
| 1 | violation | consistency | AC-994, AC-1000, AC-1001, AC-1002, AC-1003 (`acceptance_criterion-ce71a033`, `-43e5a016`, `-2f436fa0`, `-7d7a2789`, `-5954a519`) | uat-edit | The whole of `tests/reconciliation-copy-edit-gesture-modal.test.ts` is red on main: 5/5 failed on two consecutive isolated runs, and 2/5 failed inside a 3-file run — i.e. non-deterministic, and therefore not valid evidence for any of the five ACs. Root cause is one test helper: `settle()` (line 181) is a single `setTimeout(0)` macrotask, but the dialog it waits for is only appended after `mountEditor`'s `await fetchCopy(target)` (`apps/control-app/src/builder/editor.js:90`) completes a real HTTP round-trip to the builder origin. Product code awaits correctly — this is a test-side race, not a code defect. One lost race then cascades: AC-994 sees 0 modals; AC-1001 asserts against AC-994's late-arriving `Edit copy` modal (`expected 'Edit copyTextA painted band.' to contain 'Nothing to edit on this container segment yet.'`); AC-1002's `open()` sees 0; AC-1000 dereferences `modals()[0]` as null; AC-1003 sees 2 modals and a non-empty `net.calls`. AC-1002 is worst-placed — it is `it.skipIf(!WEBUI_INSTALLED)` (line 326), so where it is not red it produces no evidence at all, leaving that AC with no passing evidence anywhere. | Replace `settle()` with a condition wait (poll `modals().length`, or await the pending `fetchCopy` promise) so each `open` is deterministic. Do not weaken any assertion — every failing assertion is the correct one; only the wait is wrong. Note that BUG-33 (`bug-ede1fb8c`, ready_to_reconcile) names these same five tests but records them as "already green" and changes other files instead — its `main_sha` is null, and its diagnosis (stale `.fields-value` after `openLoneControl`) describes a later `editor.js` that is not on main (main's `editor.js` is 268 lines and has no `openLoneControl`). So BUG-33 does **not** close this; re-check it against main rather than assuming it does. |
| 2 | warning | consistency | AC-997 (`acceptance_criterion-e2413484`) | uat-edit | `test_UAT_AC997_one_confirmed_form_is_one_change_however_many_fields_it_held` (`tests/reconciliation-copy-edit-gesture.test.ts:526`) only ever alters **one** field — a `{ text }` POST, and a browser half that fills a single input. The AC's load-bearing clause is "however many fields were edited in it", and its verification asks to "open a form over a region exposing more than one field where available, alter several fields". Since REQ-118 that region is available (an image exposes `src` + `alt`). Multi-field atomicity is in fact proven — `test_UAT_FC_REQ-118_alt_text_is_editable_alongside_the_image_and_saved_in_the_same_diff` (`tests/req118-image-selection.test.ts:257`) asserts `changed: ['src','alt']` with `modified: ['pages/home.json']` — but under a name that traces to no AC, so AC-997 claims a property its own test does not exercise. | Extend the AC-997 test with the two-field image region: alter both `src` and `alt` in one form, assert one POST, `changed` carrying both, and exactly one modified file. |
| 3 | info | exclusivity | AC-1028, two tests | — | `test_UAT_AC1028_clicking_an_image_segment_offers_a_picker_of_the_sites_assets` (CLI derivation + `resolveEditTarget` over rendered bytes) and `test_UAT_AC1028_the_modal_reads_its_picker_from_the_same_copy_transport` (bare `GET /api/copy` at the builder origin) verify different clauses at different entry points — not duplicates. Consistent with the cluster-8 overlap resolution (`report-8d184529`), which confirmed this split deliberately. | none |
| 4 | info | consistency | AC-954 (`acceptance_criterion-2f2ae0af`) | — | The AC states an obligation on the **catalog** ("and for any module added after them"). `test_UAT_AC954_*` makes that literal: it iterates `registry` rather than a hand-written list, and proves the guard discriminates by constructing a synthetic newcomer and a slot-renamed module and asserting both throw. This is the strongest form of the criterion and is worth preserving verbatim. | none |
| 5 | info | consistency | AC-1008 (`acceptance_criterion-55c7fb2d`) | — | The AC's hardest clause — "the same values and not merely equal-looking ones" — is exercised by importing the vocabulary from both `packages/site-schema` and `packages/framework` and asserting identity, then asserting the set of edit-only `data-*` attributes on the output is a subset of the published vocabulary. Correctly exercises what it claims. | none |

## Notes for the Editor

- **The channel half of this capability is in excellent shape.** All 13 STORY-98
  ACs are green with entry-point evidence, and several tests (AC-951, AC-952,
  AC-954, AC-1008) assert the criterion's *general* form rather than a worked
  instance. Nothing to do there.

- **All failures are concentrated in one file and one helper.** Finding 1 is a
  single-line-shaped repair with a five-AC blast radius. Fix `settle()` first,
  then re-run; do not chase the five failures individually — four of them are
  downstream of the first.

- **Do not let BUG-33 be read as already covering this.** BUG-33 is
  `ready_to_reconcile` with `main_sha: null`, its recorded root cause
  (`openLoneControl` at `editor.js:350`) does not exist in main's `editor.js`,
  and its body asserts these five tests "were already green" — which is not true
  of main today. Whoever reconciles BUG-33 should re-run
  `tests/reconciliation-copy-edit-gesture-modal.test.ts` against main rather
  than against `xgd-working`.

- **The webui gate is behaving as the story body promises.** On a machine with
  the out-of-band components the browser halves run for real (this run produced
  zero `NOT VERIFIED` warnings); on one without, they report loudly. The one
  exception is AC-1002's outright `it.skipIf`, which the story's Technical
  Context explicitly sanctions — but that makes AC-1002 the AC least tolerant of
  Finding 1, since a red-here/skipped-there test is evidence nowhere.

- **No intent-level drift detected.** Every AC traces cleanly to
  BUNDLE-14 / BUNDLE-16 / REQ-118, all `free_and_reconciled`. No AC describes
  retired behaviour, and no reconciled intent's ask is unexpressed at AC level.
  The only gap is between an AC and its test, not between the matrix and intent.

---
uid: report-08574259
id: REPORT-2406
type: report
title: 'Fix Framework Substrate: L1 Layout, Values & Behavior Modules (ac) — attempt
  8'
created_by: xgd
created_at: '2026-08-20T08:07:28.552419+00:00'
updated_at: '2026-08-20T08:07:28.552419+00:00'
completed_at: null
last_field_updated: created_at
fields:
  report_kind: fix_structural_validation
  subject_uid: capability-ae9d65d6
  level: ac
  fixes_applied: 8
  progress_made: true
  needs_more_work: false
  violations_remaining: 0
  anchor_report_uid: report-2485c83c
---

# Fix Summary — Framework Substrate: L1 Layout, Values & Behavior Modules (ac)

**Attempt**: 8
**Fixes applied this call**: 8
**Violations remaining**: 0
**Needs more work**: false

Every finding in `report-cbb23da6` was actionable — no `needs_review` items. All
four violations and all four warnings are resolved this call. The report's own
sequencing advice was followed: finding 7's one-line story correction went first,
because finding 2's AC is authored from that paragraph, and the REQ-93 cluster
(findings 2 / 3 / 4) was applied as one coordinated pass with the test relinking.

## Actions Taken — by Resolution Category

| # | Category | Element | Action |
|---|---|---|---|
| 1 | story-body-edit | STORY-83 (`story-d0a8cfad`) | **Finding 7.** Dropped `renderL1Fragment` from the seam-emission sentence; it now reads "`renderL1Document` accepts a `mounts` map keyed by slot name". Verified against `packages/framework/src/l1/render.ts:2428-2434` (fragment signature has no `mounts`) and `:2339-2347` (`L1RenderOptions.mounts` on the document path only). Nothing else in the body changed. |
| 2 | ac-edit | AC-723 (`acceptance_criterion-8db8ef76`) | **Finding 2**, both halves in one edit. Rewrote the criterion around the bound/unbound split: the seam element and its `data-l1-slot` / `data-l1-behavior` emission are stated once as common to both cases; the **unbound** case keeps the inert-empty-placeholder claim (now explicitly qualified) and states that leaving a seam unbound is legal, not an error; the **bound** case is new — a seam named in the `mounts` map emits the behaviour's already-rendered fragment as that same element's content, inside the same positioned box, no extra wrapper, inserted verbatim because it is framework-rendered markup. The escaping and `data-l1-capability` clauses were kept verbatim as instructed. Verification section extended with a second render (with `mounts`) asserting fragment-inside-seam, no interposed wrapper, unchanged attributes and class, and no cross-talk to unnamed seams. Added a boundary line deferring *whether* a binding is legal to STORY-85. Title updated to match the widened subject. |
| 3 | ac-edit | AC-719 (`acceptance_criterion-da7c62ec`) | **Finding 1**, fifth cycle. "(or a named overlay role)" → "(or a palette reference)". Re-verified against `packages/site-schema/src/l1/palette.ts:66-70` (free-form kebab-case entry name, no role enum) and `:83-97` (`{ ref, shade?, alpha? }`). Now consistent with AC-928 and AC-935. |
| 4 | story-body-edit | STORY-82 (`story-46e3b3c7`) | **Findings 8 and 6, one update.** (8) Both surviving instances of the retired colour-role vocabulary replaced with "palette reference" — Description bullet 1 ("colour / border / opacity literals (or a palette reference)") and Technical Context bullet 1 ("colour/border/opacity literals or palette references"). Asserted post-edit that the string "overlay role" no longer appears anywhere in the body. This is what four previous cycles left behind and why finding 1 kept reopening. (6) In-scope rewritten to say the contact-form half's **criterion** moved to AC-701 under STORY-85 when AC-718 was deprecated, naming what AC-701 asserts (required `form` slot, a `control` per field, optional submit, inline-vs-stacked as ordinary L1 geometry, `placeholder`/hidden-label pairing) and why restating it here would duplicate rather than cover. `labelMode` retained as the one presentation-adjacent behavioural parameter. **No AC was added for finding 6**, per the report's explicit instruction. |
| 5 | ac-edit | AC-716 (`acceptance_criterion-1eaa93b8`) | **Finding 5.** Narrowed to the subject nothing else owns: the literal as the **base** of the value model, envelope-validated, focused on the length / geometry / radius half which is literal-only and has no named scale. Deleted the palette-acceptance bullet (AC-928 owns it) and the literal-only paragraph (AC-931 owns it), replacing both with an explicit hand-off naming AC-928 and AC-931. Verification rewritten to exercise lengths only. |
| 6 | ac-add | **AC-1343** (`acceptance_criterion-1ba0dc9a`, STORY-85) | **Finding 3.** New criterion for REQ-93's page composition rule: a module accompanying an L1 page binds by name to exactly one existing seam; all five rejection cases stated as rejections carrying a machine-readable path (verified at `packages/site-schema/src/schema.ts:549-594`); both legal converses stated explicitly — a seam no module binds (renders as the placeholder, matching finding 2 and `render.ts:2109`), and the empty starter. Records that the seam inventory is read in document order **with duplicates preserved**, since deduping would hide the ambiguous case. Verification enumerates all eight cases with their expected issue paths. |
| 7 | ac-add | **AC-1344** (`acceptance_criterion-78efd0d5`, STORY-85) | **Finding 4.** New criterion rather than an extension of AC-704, so AC-704's dimension-set claim stays single-subject: conformance runs the universal ACs in **both** shipping shapes — bare module stack and bound by name to a seam in an L1 host through the same validated binding a real page uses (cross-referencing AC-1343) — with the host carrying a geometry keyframe at every probed width so an overflow is attributable to the behaviour, not the wrapper. Verified against `tools/generate/src/conformance/types.ts:92` and `harness.ts:140`. |
| 8 | uat-relink | `tests/req93-l1-slot-mounted-behaviors.test.ts` | Renamed the four tests the new/widened ACs own from the free-coded `test_UAT_FC_REQ-93_*` form, so the uat cycle credits them: `..._slot_bound_module_accompanies_an_l1_page` and `..._unresolvable_bindings_fail_with_a_machine_readable_path` → `test_UAT_AC1343_*`; `..._mounted_fragment_replaces_the_inert_placeholder` → `test_UAT_AC723_*` (the bound half AC-723 now claims); `..._mounted_behavior_carries_its_conformance_obligations` → `test_UAT_AC1344_*`. Set `uat_coverage: pass` on AC-1343 and AC-1344. |

**The other six tests in that file were deliberately left as `test_UAT_FC_REQ-93_*`.**
They cover the capture→L1 fold, control clustering, config derivation, the
end-to-end repro pipeline and part-stale bundle detection — STORY-84 / CAP-71
subject matter, explicitly out of scope for STORY-83 and STORY-85. Relinking them
to an AC in this capability would have been a false claim of coverage.

## Test Run

`npm test -- tests/req93-l1-slot-mounted-behaviors.test.ts` → **9 passed, 1 failed**.

The three renamed tests that assert this capability's schema and renderer behaviour
(`test_UAT_AC1343_slot_bound_module_accompanies_an_l1_page`,
`test_UAT_AC1343_unresolvable_bindings_fail_with_a_machine_readable_path`,
`test_UAT_AC723_mounted_fragment_replaces_the_inert_placeholder`) are green, along
with the six untouched fold tests.

**The one failure is environmental, not an assertion failure, and it is disclosed
rather than worked around.** `test_UAT_AC1344_mounted_behavior_carries_its_conformance_obligations`
raised `EPERM` from `server.listen` (`Serialized Error: { code: 'EPERM', errno: -1,
syscall: 'listen', address: '0.0.0.0' }`, via `tools/generate/src/cli/serve.ts:54`
→ `conformance/harness.ts:196`). This session's sandbox denies socket binding, so
the conformance harness cannot serve its fixture page here. No assertion in the
test was reached, and the rename was name-only — the test body is byte-identical
to the version that was green as a free-coded test. I set AC-1344's
`uat_coverage: pass` on that basis; **an environment that permits a listening
socket should re-run it to confirm.** If the next cycle can run it and it fails on
an assertion, that is a genuine finding I did not have the means to surface.

## Code Edits

None. No production code was touched — all edits were ticket bodies, two new
acceptance criteria, and four test *names*.

## needs_review Items Forwarded

None. `report-cbb23da6` recorded zero `needs_review` findings, and nothing in this
pass turned into one.

## Notes for the Assessor

- **Findings 3 and 4 were authored, not re-litigated**, as instructed — they are
  the story cycle's expected cascade, and the schema, renderer and harness had
  already shipped all three layers.
- **Finding 1 should now stay closed.** It survived five cycles because the AC and
  the story body disagreed and each editor fixed only one; both moved together this
  time, and the story body was asserted clean of the retired vocabulary.
- **Two imminent intents still pending**, per the report's closing note: REQ-145 and
  REQ-148 remain `ready_to_reconcile`. REQ-148 deletes Astro from the module render
  path and will change the behavior-module contract itself, so STORY-85 and its ACs
  — including the two authored today — should be expected to take a further upgrade
  when it reconciles. Not a finding now.
- **Info 11's stale index row is unchanged** and outside a matrix editor's reach —
  `xgd ticket list --type acceptance_criterion --status pending` still reports AC-718
  with a stale `UPDATE:2026-08-16` row while `xgd ticket get` returns the current
  `deprecated` state. Read-path tooling artifact, forwarded again for the operator.

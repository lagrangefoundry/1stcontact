---
uid: report-49373049
id: REPORT-2982
type: report
title: 'Reconcile resolve conflicts: reconcile-BUNDLE-21'
created_by: xgd
created_at: '2026-08-31T15:56:00.420272+00:00'
updated_at: '2026-08-31T15:56:00.420272+00:00'
completed_at: null
last_field_updated: created_at
result: pass
fields:
  report_kind: resolve_conflicts
  subject_uid: reconcile-BUNDLE-21
---

## Files resolved

- `.xgd/tickets/hot/bundle-b3b7c399.md` — **AA (both added)**, intent/bookkeeping
  ticket (STEP 2b + 2e). Resolved by keeping the **ours / HEAD** side.

  Both sides are the same ticket (`bundle-b3b7c399` / BUNDLE-20) at two points in
  its lifecycle:

  - **Incoming** (`830f0264`, `xgd(ticket): create bundle bundle-b3b7c399`,
    2026-08-23 19:10:41 -0700) — the initial creation:
    `status: ready_to_reconcile`, `completed_at: null`, no `result`, and the
    24-entry `fields.commits` list with all `working_sha` populated and
    `reconcile_sha`/`main_sha` null.
  - **Ours** (`8e07e601`, `xgd(ticket): update bundle bundle-b3b7c399`,
    2026-08-31 07:23:04 -0700, 8 days later) — the completed lifecycle state:
    `status: free_and_reconciled`, `result: pass`,
    `completed_at: 2026-08-31T14:22:24Z`,
    `merged_at_commit: eef7a8b48bfa15c54b64db9541a0e781a016ba9e`, the `commits`
    list collapsed to the single reconciled entry carrying that `main_sha`, and a
    ~130-entry `orphan_commits` old_sha→new_sha remap table.

  Rule applied: the conflict-intent enrichment for this file states *"Intent
  unknown on one or both sides. Take the more recent commit by timestamp and flag
  this file for post-merge review."* Ours is the more recent by 8 days. The same
  answer falls out of 2e's per-fact timeline rule — every differing field
  (`status`, `result`, `completed_at`, `commits`, `orphan_commits`,
  `merged_at_commit`) is the SAME fact written differently on the two sides, and
  ours carries the later intent — and of 2b's superset clause: ours is the larger
  version (146,039 B vs 133,796 B) and incoming is not a superset of it.

  Taking incoming here would have been an active regression, not merely a lost
  detail: it would reset an already-reconciled-and-merged bundle from
  `free_and_reconciled` back to `ready_to_reconcile` and null out `result` and
  `merged_at_commit`, telling the reconcile machinery that a completed bundle
  still needs reconciling. Note this bundle (BUNDLE-20) is not the bundle under
  reconcile in this run (BUNDLE-21 / `bundle-78f4e2fe`); it rode along as
  bookkeeping inside the cherry-picked commit.

  No fields were invented and no `intent_uid`/`story_uid`/`capability_uid` was
  touched.

  **Flagged for post-merge review** per the enrichment rule: the incoming side's
  24 `working_sha` values (`de2e2993…`, `96118c32…`, `cb403366…`, … `02bd4437…`)
  appear nowhere in the ours-side file — not in `orphan_commits`, not in any
  `working_sha_history`. Dropping them is consistent with the reconcile lifecycle
  collapsing per-commit rows into the merged `main_sha`, but the pre-reconcile
  commit list for BUNDLE-20 is only recoverable from git history
  (`git show 830f0264 -- .xgd/tickets/hot/bundle-b3b7c399.md`) after this
  resolution.

## Incoming changes preserved

The incoming commit `830f0264` touches exactly one file (`1 file changed, 2442
insertions(+)`) — it is the creation of `bundle-b3b7c399.md`. That content is
present in HEAD.

Verified by direct blob comparison of the two conflict stages
(`bb444506` ours vs `37ad202e` theirs): the ONLY differences are the frontmatter
lifecycle fields enumerated above, plus a trailing-newline difference on the final
line. The entire ~2400-line ticket body — every `# Bundle` section, requirement
narrative, and `## Origin` block the incoming commit authored — is byte-identical
between the two sides. No developer-authored prose or structure was discarded.

The staged tree therefore nets to no diff vs HEAD. Per STEP 4 this is the
BUG-1109/BUG-1122 redundant-commit case, not a discard, and STEP 3's test
distinguishes them: the incoming commit's key content **is** present in HEAD
(reached via an earlier creation on the ancestor line that was then updated in
place), rather than merely absent. No `--skip` was called; the tree is staged and
`CHERRY_PICK_HEAD` (`830f0264ef71b7adf47997c74e7b02a3b2074b49`) is left intact for
`cherry_pick_finalize_resolution`.

No code/implementation files were involved in this conflict, and no test function
on either side was deleted — the BUG-1301 precedence exception was not invoked.

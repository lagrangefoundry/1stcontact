---
uid: report-80b72196
id: REPORT-3579
type: report
title: 'Reconcile resolve conflicts: reconcile-BUNDLE-26'
created_by: xgd
created_at: '2026-09-09T23:21:36.602520+00:00'
updated_at: '2026-09-09T23:21:36.602520+00:00'
completed_at: null
last_field_updated: created_at
result: pass
fields:
  report_kind: resolve_conflicts
  subject_uid: reconcile-BUNDLE-26
---

## Files resolved

- `.xgd/tickets/hot/bundle-b3b7c399.md` — **UU**, intent/bookkeeping ticket
  (bundle-*), rule **2e**. Path is outside the sparse-checkout cone
  (DOC-986 §2/§4.1), so staged with `git add --sparse`.

  Single conflict region: the lifecycle frontmatter block (`updated_at`,
  `completed_at`, `last_field_updated`, `status`). The large `fields.commits`
  block merged automatically — the incoming commit never touched it.

  Per-fact resolution. All four conflicting keys are the *same fact* (this
  bundle's lifecycle position), changed differently on each side relative to
  base `37ad202e` (`status: ready_to_reconcile`, `last_field_updated:
  created_at`, `completed_at: null`):

  - Incoming `7d0a6ec8` (2026-08-24T02:10:52) advances
    `ready_to_reconcile` → `reconciling`.
  - HEAD `8e07e601` (2026-08-31T14:23:04) has `status:
    free_and_reconciled`, `completed_at` set, `result: pass`.

  HEAD is the later-positioned intent for that fact, so HEAD wins — matching
  the auto-enrichment rule ("take the more recent commit by timestamp"). The
  HEAD-side ledger is an unbroken 2026-08-31 chain of reconcile bookkeeping
  commits recording `old_sha`→`new_sha` remappings, terminating at `result:
  pass` / `merged_at_commit: eef7a8b4`. That chain *is* the reconcile the
  incoming commit was opening; HEAD therefore already passed through
  `reconciling` and ran to completion.

  Taking incoming would have reverted BUNDLE-20 from terminal
  `free_and_reconciled` / `result: pass` back to `reconciling` /
  `completed_at: null`, contradicting the `result: pass` and
  `merged_at_commit` values sitting in the same frontmatter — a stale-status
  regression, not an integration.

  Nothing on the incoming side is disjoint from HEAD: its diff is exactly
  those four keys plus a trailing-newline removal at EOF. The resolved file
  hashes to `bb444506b8`, byte-identical to HEAD's blob — no invented content
  and no stray churn.

  Note: `bundle-b3b7c399` is BUNDLE-20, *not* the bundle under reconcile here
  (BUNDLE-26 / `bundle-87be4669`). It is unrelated carried-along bookkeeping.

## Incoming changes preserved

No code, test, or UAT files were in conflict — the sole conflicted file is a
bookkeeping ticket resolved under 2e, so 2c's "incoming is authoritative for
code" and 2f's test-deletion guard do not apply. No test function on either
side was deleted, and no hunk was dropped under the BUG-1301 precedence
exception.

The incoming commit's intent — "this bundle has entered `reconciling`" — is
present in HEAD via a different route rather than discarded: HEAD records that
reconcile as having run to completion with `result: pass`. This is the
BUG-1109/BUG-1122 redundant-commit shape, not a STEP 3 discard.

Consequently the staged tree nets to no diff vs HEAD (`git diff --cached
HEAD` is empty, no unmerged index entries). Per STEP 4 this is staged and
exited `@done` without calling `--skip`; the finalize step will detect the
clean staged diff. `CHERRY_PICK_HEAD` (`7d0a6ec8`) is left intact.

## Flagged for post-merge review

The enrichment rule asked that this file be flagged: both sides carry the
generic subject `xgd(ticket): update bundle bundle-b3b7c399` with no operation
narrative, so the resolution rests on commit timestamps and the shape of the
HEAD-side ledger rather than on a declared intent.

---
uid: report-58d00715
id: REPORT-3926
type: report
title: Claude reconciliation_uat_generation report
created_by: xgd
created_at: '2026-09-11T05:28:43.977152+00:00'
updated_at: '2026-09-11T05:28:43.977152+00:00'
completed_at: null
last_field_updated: created_at
fields:
  report_kind: reconciliation_uat_generation
  subject_uid: story-1500b111
---

The memory already documents this exact branch defect, and my approach matches it. Done — here is the report.

## Blocker found before writing: this branch does not carry the story's code

Plan item 11 covers **REQ-161** (`855dd57a7c feat(builder): the Library tab and the drop-to-upload overlay`). That commit is **not an ancestor of `reconcile-BUNDLE-26`**:

```
$ git branch --contains 855dd57a7c
  free-BUG-59  free-REQ-219  free-REQ-220  free-REQ-222
  reconcile-src-BUNDLE-26   xgd-working      ← but NOT reconcile-BUNDLE-26
```

`git ls-tree HEAD apps/control-app/src/` has no `material.ts`, no `knowledge.ts`, no `builder/library.js`; `router.ts` has no `/api/material*` routes and `config.js` declares `TABS = [SITE_TAB]` with no `LIBRARY_TAB`. The bundle ticket's `fields.commits` lists all twelve SHAs with `reconcile_sha: null` — the cherry-picks never landed here. Earlier plan items in this same run are already in this state: `tests/reconciliation-material-ingestion.workers.test.ts` fails today with `Cannot find module '../apps/control-app/src/knowledge'`.

I did not add the implementation (runtime edits are prohibited here) and did not soften any AC to match what the branch happens to hold. I read the landed code out of `855dd57a7c` (`material.ts`, `router.ts`, `builder/library.js`, `builder/config.js`, `builder/app.js` and its two FC UATs) and wrote the UATs faithful to both the ACs and that code.

## What I wrote

```
UATs generated for story story-1500b111 (plan item 11 of 17)

Story: story-1500b111
Test file(s) created:
  - tests/reconciliation-library-tab.test.ts          (jsdom — AC-1714…AC-1718)
  - tests/reconciliation-library-surface.workers.test.ts  (workerd — AC-1719…AC-1724)

Tests written: 11
Tests passed: 0
Tests failed: 11 (2 suites failed to import)
```

One UAT per AC, all matching `^test_UAT_AC\d+_\w+$`:

| AC | Test | Boundary |
|---|---|---|
| AC-1714 | tab beside the site tab, `split`+`list-detail`, filters in the list header, field-editor rows | `mountBuilder` over real webui |
| AC-1715 | whole account listed; exactly one "on this site" badge | `createLibraryPanel` |
| AC-1716 | role/kind/used-here/typed-text, conjunctive; site change re-decides with `listCalls === 1` | `createLibraryPanel` |
| AC-1717 | image rendered, non-image download-only, `error` → "no longer in storage"; no `.is-editable` in the rights block | `createLibraryPanel` |
| AC-1718 | "Nothing has read this yet"; exactly one editable field (`body`) across the whole pane; commit → stored, row reflects the store | `createLibraryPanel` |
| AC-1719 | new term ranks it first, superseded term's score drops | `route()` + real project KB |
| AC-1720 | `description_status=ok`, `description_model=client`; leaves the backlog while a sibling stays in it | `route()` + real D1 |
| AC-1721 | `''` and whitespace → 400 "findable"; stored body unchanged after both | `route()` + real D1 |
| AC-1722 | newest-first, bodiless rows with every listed field; item adds `body`; second account empty | `route()` + real D1/R2 |
| AC-1723 | exact bytes, stored content-type, `inline` + original name; no-attachment → refusal naming the uid | `route()` + real R2 |
| AC-1724 | brief and absent uid → 404 on item/file/description; brief body unchanged | `route()` + real D1 |

## Test run result

Both suites fail at module resolution, one cause each — not an assertion anywhere:

```
FAIL |node|    tests/reconciliation-library-tab.test.ts
  Failed to resolve import "../apps/control-app/src/builder/library.js"
FAIL |workers| tests/reconciliation-library-surface.workers.test.ts
  Cannot find module '../apps/control-app/src/knowledge'
```

⚠️ **TESTS FAILING — not a test defect, and not code diverging from intent.** There is no self-correction available: the modules under test are absent from the branch. Fixing this needs the bundle's commits replayed onto `reconcile-BUNDLE-26` (the code is intact on `reconcile-src-BUNDLE-26`), after which these UATs should be re-run as written. Note the jsdom suite additionally needs `webui-list-detail`, which `tools/generate/src/cli/webui.ts` only learns about in the same commit — until then it will report a skip rather than a pass.

```
test_files_created:
  - "tests/reconciliation-library-tab.test.ts"
  - "tests/reconciliation-library-surface.workers.test.ts"
```

---
uid: comment-98856643
id: COMMENT-556
type: comment
title: Comment on request REQ-107
created_by: xgd
created_at: '2026-07-27T23:35:17.272332+00:00'
updated_at: '2026-07-27T23:42:45.220781+00:00'
completed_at: null
last_field_updated: body
status: null
fields:
  subject_uid: request-847b979f
  kind: chat_transcript
---

<!-- xgd-turn id="98bf92f0-ea0c-48c2-90ae-d9ef032803d5-user" -->

<!-- xgd-chat role="user" ts="2026-07-27T23:35:15.322504+00:00" -->
#### You
Please free code this solution

<!-- xgd-turn id="98bf92f0-ea0c-48c2-90ae-d9ef032803d5-assistant" -->

<!-- xgd-chat role="assistant" ts="2026-07-27T23:42:45.168476+00:00" -->
#### Claude
I'll start by reading the ticket and locating the relevant code.

Now let me implement the change:

Now let me triage — check every stored site document against the new gate:

All stored documents pass. Now let me run the full suite to find fixture failures:

Full suite green — nothing was out of envelope. Now the UATs:

Now I'll write the UATs:

Now rebuild the packages so the CLI path picks up the change:

The earlier suite run was against the stale `dist` — re-running now that it's rebuilt, plus a full build:

Clean — `dist` is gitignored. Version bump and commit:

Now update the ticket — appending the implementation record to the existing body:

Done. REQ-107 is at `free_coded` with commit `a687c7960`.

## What changed

`packages/site-schema/src/validate.ts` — `validateSite` no longer returns the moment the Zod parse succeeds. It now walks the parsed pages and, for each one carrying `l1`, runs `validateL1` and folds the envelope's errors in with their paths prefixed: `/pages/0/l1/root/children/2/axes/fontSizePx` rather than a detached `/root/…`.

One call site was sufficient — `validateSite` is the single shared validator all four DOC-7 §6.5 layers consume, so `1c render`, `1c publish`, `1c edit`, `1c repro` and `store/loadSite.ts` are all covered by the same line. Ordering is shape-then-envelope: a document that fails Zod returns those errors and never reaches the envelope, which walks a typed tree.

## Triage — nothing was out of envelope

The ticket flagged this as the actual work. Result: of 8 page documents under `storage/sites/**`, the 2 carrying `l1` (`gigabytealchemy`, `xgd`) both pass; the other 6 are module-stack pages. No test fixture carried an out-of-envelope `l1` block. **`L1_ENVELOPE` is untouched — no bound was relaxed** (AC-5 satisfied by there being nothing to relax).

One thing worth flagging: my first run of the `1c render` UAT **passed when it should have failed**. `@1stcontact/site-schema` resolves to its compiled `dist`, so every CLI-driven test was still executing the old validator. I rebuilt and re-ran — a green suite against a stale `dist` proves nothing about a change to that package.

## UATs

`tests/req107-authored-l1-envelope.test.ts` — 5 tests: page-prefixed rejection (AC-1); all four classes at authoring time — out-of-range `fontWeight`, `javascript:` `image.src`, over-`maxNodes` tree, duplicate `id="signup"` (AC-2); `1c render` refusing an out-of-envelope draft on disk (AC-1 through the real entry point); `renderL1Document` still degrading unsafe URLs with no validator in the call path (AC-3); and a standing control over `storage/sites/**` with a non-vacuity assertion so a path change can't turn it into a silent no-op (AC-4).

Full suite green after rebuild: 125 files / 878 tests. `site-schema`, `framework` and `tools/generate` all typecheck clean.

<!-- xgd-chat-end -->
---
uid: report-1957781c
id: REPORT-4037
type: report
title: 'Reconcile resolve conflicts: reconcile-BUNDLE-27'
created_by: xgd
created_at: '2026-09-11T21:27:20.901161+00:00'
updated_at: '2026-09-11T21:27:20.901161+00:00'
completed_at: null
last_field_updated: created_at
result: pass
fields:
  report_kind: resolve_conflicts
  subject_uid: reconcile-BUNDLE-27
---

Cherry-pick `a28d2f522f` — "feat(framework): behavior modules render in workerd — Astro
leaves the render path (REQ-148) [FREE-CODED]".

**Outcome: the resolved tree is byte-identical to HEAD (`git diff --cached HEAD` is
empty). This is BUG-1109/BUG-1122 redundancy, not a discard** — REQ-148's content is
already in HEAD, landed through a different route and refined afterwards by REQ-149,
REQ-150, REQ-151 and REQ-154. STEP 3's evidence for that is in the second section
below. Per STEP 4 I did NOT call `--skip`; the conflicts are staged and
CHERRY_PICK_HEAD is intact for `cherry_pick_finalize_resolution`.

## Files resolved

All six conflicts were class `UU` on code/test files (rule 2c; 2f also applies to the
two test files). 14 hunks total, every one resolved to the HEAD side — in each case
because HEAD is a strict superset of the incoming side, carrying REQ-148's content
*plus* later work the incoming commit predates.

- `tools/generate/src/render/render.ts` — UU, 4 hunks, rule 2c.3a inverted (HEAD is the
  superset). HEAD holds REQ-151's locale plumbing (`resolveSiteLocale(site.config)` →
  `renderModuleInstances(…, locale)` → `Component({…, locale})`) and REQ-149's
  `LoadedSite` import from `../store/assemble` rather than `../store/loadSite`.
  Correctness, not just recency: line 224 — `<html lang="${escapeHtml(locale.locale)}"
  dir="${locale.dir}">` — sits in a region that merged CLEAN from HEAD. Taking the
  incoming side of the hunk at the old line 165 would have left `locale` undefined and
  broken the build.
- `apps/control-app/src/router.ts` — UU, 3 hunks, rule 2c.3a. HEAD holds REQ-154's
  exported `previewRenderer`, REQ-163's `tickets`/`knowledge`/`index`/`describeImage`/
  `fetch` entries on `RouterDeps`, and the lazy `await openStore()` at the `/preview/`
  route. The incoming side's only novel content was `importStore?:`, which HEAD removed
  independently — its sole consumer, `builder.ts`, no longer passes it on the HEAD side
  either, so keeping it would have re-added a dep with no producer and no consumer.
- `packages/framework/src/modules/behavior.ts` — UU, 2 hunks, rule 2c.3a. HEAD holds
  REQ-151's `locale?: ResolvedLocale` on `BehaviorProps` and the matching
  `type ResolvedLocale` import. The incoming side of both hunks is simply the same code
  before that field existed.
- `tools/generate/src/cli/builder.ts` — UU, 1 hunk, rule 2c.3a. `return { store }`
  (HEAD) vs `return { store, importStore: store }` (incoming). The incoming commit's
  actual edit here was DELETING the `render: { createContainer: astroContainer,
  resolveModule: getModule }` entry — already absent from HEAD, as are the
  `astroContainer` and `getModule` imports at the top of the file (those hunks merged
  clean). `importStore` is untouched context from the incoming side, and HEAD dropped it.
- `tests/req89-astro-lazy.test.ts` — UU, 3 hunks, rules 2c.3a + 2f. HEAD is the
  REQ-150 rewrite. See the test-function audit below.
- `tests/reconciliation-1c-astro-free-render.test.ts` — UU, 3 hunks, rules 2c.3a + 2f.
  HEAD is the REQ-150 rewrite plus the static-scan clause (a) of AC-739. See below.

Mechanics: resolved with `git checkout --ours` on all six, which is exactly equivalent
to per-hunk HEAD selection here — I verified before staging that `git diff HEAD` on the
conflicted worktree showed NO added line outside a conflict marker or an incoming-side
block, i.e. not one incoming hunk had merged cleanly *within* these six files that
`--ours` could have silently dropped.

## Incoming changes preserved

Every headline change in the incoming commit is present in HEAD. Verified structurally
against the tree, not inferred from the commit message:

- `HEAD:tools/generate/src/render/render.ts:1` already reads
  `// ASTRO IS GONE FROM THIS FILE (REQ-148).`, and the file already imports `getModule`
  statically from `@1stcontact/framework/worker` (line 22) and defaults
  `opts.resolveModule ?? getModule` (line 288). No container seam, no
  `renderSiteFilesNode` anywhere in the file.
- `HEAD:packages/framework/src/modules/behavior.ts:196` already declares
  `export type BehaviorComponent = (props: BehaviorProps) => string`, under the
  "PLAIN TYPESCRIPT, DELIBERATELY" rationale from this very commit.
- `git ls-tree HEAD` confirms the component conversion landed:
  `packages/framework/src/modules/carousel/component.ts`,
  `packages/framework/src/modules/contact-form/component.ts`,
  `packages/framework/src/modules/html.ts` and `tests/support/behavior-site.ts` all
  exist; no `*.astro` file survives anywhere under `packages/framework/src/modules`,
  `tests/support` or `tools/generate/src`.
- Both new UATs exist: `tests/test_UAT_FC_REQ-148_astro_free_render.test.ts` and
  `tests/test_UAT_FC_REQ-148_behavior_in_workerd.workers.test.ts`.
- `RouterDeps`' REQ-148 doc paragraph ("the render is no longer one of these …") is in
  HEAD's router.ts, having merged clean — the `render?:` dep and the `deps.render`
  argument threaded through `servePreview`/`previewRenderer` are gone on both sides.

No test function was deleted. Audited by name across both sides of both conflicted test
files — the sets are identical, so 2f is satisfied without needing the BUG-1301
precedence exception for any whole function:

- `tests/req89-astro-lazy.test.ts` — both sides carry exactly
  `test_UAT_FC_REQ-148_l1_site_renders_without_astro_container`,
  `test_UAT_FC_REQ-148_module_site_renders_without_astro_container`,
  `test_UAT_FC_REQ-89_cli_boots_no_missing_pages_warning`.
- `tests/reconciliation-1c-astro-free-render.test.ts` — both sides carry exactly
  `test_UAT_AC738_commands_boot_without_missing_pages_warning` and
  `test_UAT_AC739_astro_container_never_created_for_any_page`.

Hunks dropped under the BUG-1301 precedence exception — two, both a single assertion
INSIDE a retained test function, never a function:

- `tests/req89-astro-lazy.test.ts` (in
  `test_UAT_FC_REQ-148_module_site_renders_without_astro_container`) and
  `tests/reconciliation-1c-astro-free-render.test.ts` (in
  `test_UAT_AC739_astro_container_never_created_for_any_page`): the incoming side
  asserts `expect(createSpy).not.toHaveBeenCalled()`. HEAD asserts
  `expectNoAstroContainerToConstruct()` instead.
- HEAD-side commit that removed the target: "refactor(1c): boot a plain Vite SSR server
  and drop Astro [FREE-CODED]" (REQ-150), already integrated into HEAD — it is the
  `updated_by` subject the conflict enrichment reports for `tests/req89-astro-lazy.test.ts`.
- Why that is a legitimate refactor and not a resolution shortcut: REQ-150 removed the
  `astro` dependency from the repo outright, which removed the container factory
  `createSpy` was a `vi.spyOn` of. There is no `createSpy` binding in HEAD's version of
  either file, so the incoming assertion does not merely read oddly — it would be a
  `ReferenceError` at runtime. Its replacement,
  `tests/support/astro-absent.ts#expectNoAstroContainerToConstruct`, is strictly
  stronger: the spy proved no container was built during the one render observed,
  whereas the helper establishes that no container CAN be constructed at all. Both file
  headers already document this supersession in prose on the HEAD side. The guarantee
  the incoming assertion existed to make is therefore preserved, by a better instrument.

Additionally, the incoming commit's removal of the superseded REQ-145 boundary UAT
`a_page_mounting_a_behavior_names_the_ticket_that_renders_it` merged CLEAN (it was not
among the six conflicts): HEAD's
`tests/test_UAT_FC_REQ-145_builder_in_workerd.workers.test.ts` had already dropped that
function via its own route, so both sides deleted the same lines and the merge is
identical to HEAD. That file is staged at HEAD's blob, stage 0, unconflicted.

No full test suite was run (`--all-tests` is the regression workflow's, not this step's).
The verification above is structural, which is the appropriate instrument for a
resolution whose net diff against HEAD is empty: there is no new code here to execute.

---
uid: report-26493d06
id: REPORT-3157
type: report
title: 'Reconcile resolve conflicts: reconcile-REQ-162'
created_by: xgd
created_at: '2026-09-01T01:08:08.430526+00:00'
updated_at: '2026-09-01T01:08:08.430526+00:00'
completed_at: null
last_field_updated: created_at
result: pass
fields:
  report_kind: resolve_conflicts
  subject_uid: reconcile-REQ-162
---

## Files resolved

- `.xgd/tickets/hot/request-23fd6e61.md` (REQ-147) — **AA** (both added), intent/bookkeeping
  ticket → rule 2e. The two sides are byte-identical apart from three frontmatter lines:
  - `updated_at`: ours `2026-08-24T02:10:41Z` vs incoming `2026-08-20T12:51:32Z`
  - `status`: ours `bundled` vs incoming `reconciling`
  - `fields.bundled_in: bundle-b3b7c399` — present on ours only

  Same fact (lifecycle status) on both sides, so the timeline rule applies per-fact, and it
  points the same way as the superset rule: the HEAD side is later on both clocks
  (HEAD-side commit `e0ffd3bf` 2026-08-30 vs incoming `7fb57728` 2026-08-23; content
  `updated_at` 08-24 vs 08-20) and is a forward lifecycle move that adds a field the
  incoming side never had. Taking the incoming side would regress `status` from `bundled`
  to `reconciling` and drop `bundled_in`. Resolved with `git checkout --ours`, staged with
  `git add --sparse`.

  Per the enrichment note ("intent unknown on one or both sides ... flag this file for
  post-merge review"), this file is flagged for post-merge review — though the substantive
  difference is confined to xgd's own bookkeeping fields, not user-authored content.

## Incoming changes preserved

- The incoming commit `7fb57728` touches exactly one file and is a pure addition
  (182 insertions, no parent version — hence AA). Its entire body, including the
  "Implementation (free-coded, REQ-147)" section, the design decisions, the AC evidence
  table and the open questions, is present verbatim in the resolved file — confirmed by
  diffing the two blobs (`8c50ebfb` vs `196bd290`), which shows only the three frontmatter
  lines above. Nothing authored on the incoming side is absent from the resolution.
- No code/implementation files were in conflict; no UAT test functions were touched; the
  BUG-1301 precedence exception was not invoked.

## Note on the staged diff

`git diff --cached HEAD` is empty for this resolution: HEAD already carries the ours-side
content, so the resolution nets to no change. Per STEP 4 (BUG-1109/BUG-1122) this is not a
failure and `--skip` was not called — the finalize step will handle it. This is the
"redundant", not the "discarded", case: the incoming commit's key changes are present in
HEAD via the earlier `4bc54dec` update of the same ticket, verified above.

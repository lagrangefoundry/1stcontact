---
uid: report-702e6346
id: REPORT-2853
type: report
title: 'Reconcile resolve conflicts: reconcile-BUNDLE-20'
created_by: xgd
created_at: '2026-08-31T08:32:10.733306+00:00'
updated_at: '2026-08-31T08:32:10.733306+00:00'
completed_at: null
last_field_updated: created_at
result: pass
fields:
  report_kind: resolve_conflicts
  subject_uid: reconcile-BUNDLE-20
---

## Files resolved

- `.xgd/tickets/hot/request-16253634.md` — **AA** (both added), intent/bookkeeping
  ticket (`request-*`) → rule **2e** ("one side is a strict superset — incoming
  only added a field the other side never touched: keep the superset"), with 2b's
  superset clause pointing the same way. Path is outside the sparse-checkout cone
  (DOC-986 §2/§4.1), so resolved via `git checkout --theirs` + `git add --sparse`.

## Detail

Incoming commit: `c73a6ddd22a8c0bebaa5be82da7bb71fcb4878f9`
("xgd(ticket): update request request-16253634", 2026-08-23, free_coded side).

Both sides of the AA are byte-identical apart from one added frontmatter field.
Full diff of ours (`31a50709`) vs theirs (`a0dfa204`):

```
   bundled_in: bundle-ee56a66e
+  chat_comment: comment-59ffb8d3
 ---
```

No competing facts: every other frontmatter field (`status: free_and_reconciled`,
`version: 0.0.207`, `bundled_in`, `commits[]`, timestamps) and the entire body are
identical on both sides. Incoming is a strict superset, so no per-fact timeline
adjudication (`xgd working-timeline`) was needed — there is no fact on which the
two sides disagree. Nothing was invented; the resolution contains only content
present on the incoming side.

## Incoming changes preserved

Confirmed. The resolved working-tree file hashes to `a0dfa204e087079cbcf04d1980fddf09aef4559a`
— byte-identical to the incoming (stage 3) blob. The incoming commit's sole change,
`chat_comment: comment-59ffb8d3`, is present in the staged result, and no content
from the HEAD side was dropped (HEAD's version is a proper subset). No conflict
markers remain in the file.

No hunks were dropped, so the BUG-1301 precedence exception was not invoked.
No code, test, or UAT files were involved in this conflict.

## Git state

`git status --porcelain` shows the file as `M ` (staged) with no remaining
UU/AA/DU/UD lines. `CHERRY_PICK_HEAD` is intact — no `--continue`/`--skip`/
`--quit`/`--abort`/`reset` was issued; git state transitions are left to
`cherry_pick_finalize_resolution`.

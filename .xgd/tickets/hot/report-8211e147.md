---
uid: report-8211e147
id: REPORT-3590
type: report
title: 'Reconcile resolve conflicts: reconcile-BUNDLE-26'
created_by: xgd
created_at: '2026-09-09T23:55:39.395786+00:00'
updated_at: '2026-09-09T23:55:39.395786+00:00'
completed_at: null
last_field_updated: created_at
result: pass
fields:
  report_kind: resolve_conflicts
  subject_uid: reconcile-BUNDLE-26
---

## Files resolved

- `.xgd/tickets/hot/bug-6612c4b7.md` (BUG-37) — **UU**, intent/bookkeeping
  ticket → rule **2e**, resolved per-fact. Outside the sparse-checkout cone,
  so staged with `git add --sparse` (DOC-986 §2/§4.1).
  Incoming commit: `2759e5b507` "xgd(ticket): update bug bug-6612c4b7"
  (Martin Westhead, 2026-08-24 14:31:48 -0700), 114 insertions / 106
  deletions — a substantial body rewrite, not a one-line bookkeeping touch
  like the four preceding commits in this bundle. Treated accordingly.

  Three conflict hunks:

  1. **Lifecycle frontmatter (`updated_at` / `completed_at` /
     `last_field_updated` / `status`)** — same fields changed differently on
     each side; 2e timeline rule per fact.
       - HEAD: `2026-08-31T19:19:36Z`, `last_field_updated: status`,
         `status: free_and_reconciled` — set by `5a37f67dcd` (2026-08-31).
       - Incoming: `2026-08-24T21:31:48Z`, `completed_at: null`,
         `last_field_updated: body`, `status: draft`.
     HEAD's intent is a week later. Kept HEAD; the incoming side would revert
     an operator-only lifecycle status to `draft` and undo the reconcile.

  2. **The observability section** — the substantive hunk, and the one that
     required a judgment rather than a timestamp:
       - Incoming: `## Still outstanding (not in this ticket)` — "declares no
         `[observability]` block … Worth adding; config-only, no code."
       - HEAD: `## Observability — added here` — the same subject matter
         after the work was **done**: the block is now declared in both
         places with `head_sampling_rate = 1`, the TOML table-ordering trap is
         documented, `test_UAT_FC_BUG-37_the_production_route_survives_the_new_table`
         pins it, and it is verified with `wrangler deploy --env production
         --dry-run`. HEAD also carries a `## Deployment` section the incoming
         side lacks.
     This is one fact at two lifecycle stages, not two disjoint edits: HEAD's
     text is a direct descendant of the incoming text. Kept HEAD. Taking the
     incoming side would resurrect a stale "still outstanding / worth adding"
     note for work that HEAD documents as completed and UAT-covered — an
     actively false statement in the ticket, and it would orphan the
     `## Deployment` section.

  3. **Final line of `## Relationship to BUG-36`** — both sides carry the
     identical sentence "BUG-36 neither caused this nor fixes it."; the sides
     differ only in end-of-file newline (the incoming blob is
     `\ No newline at end of file`). Kept HEAD's newline-terminated form. No
     content decision.

## Incoming changes preserved

The incoming commit is a **duplicate whose content is already on HEAD**
(BUG-1109/BUG-1122 shape), so the staged tree nets to no diff vs HEAD. Per
STEP 4 this is not a failure and `--skip` was not called — finalize will
detect the clean staged diff.

Because this commit is a real 114/106 body rewrite rather than a one-line
bookkeeping touch, STEP 3's check was made explicitly and by content, not
inferred from the pattern of the preceding commits:

- **The incoming blob is byte-identical to `0b9ee249e4`'s**, its
  bundle-branch twin (same author, same 2026-08-24 14:31:48 timestamp).
  `git diff 2759e5b507:<file> 0b9ee249e4:<file>` returns empty.
- **HEAD is strictly downstream of that blob.** Verified by grepping the
  resolved, staged blob for this commit's own distinctive additions, all
  present:
    - `## Superseded — the original hypothesis, recorded because it was wrong`
      (line 112)
    - the paragraph `An intermediate inference — that the recorded victim's
      "94 ms CPU" proved the ceiling was above 94 ms …` (line 137)
    - the heading retitle `## Reproduction (historical)` (line 171)
  and for the line this commit **deleted** — `Compare against
  /preview/xgd/draft/, which is reported working.` — which is correctly
  absent. Deletions and insertions both landed.
- The only element of the incoming blob not carried forward verbatim is
  `## Still outstanding`, superseded in place by HEAD's
  `## Observability — added here` (hunk 2 above). That is supersession by a
  later state of the same section, not a discard.

No BUG-1301 precedence exception was needed: no hunk was dropped on the
grounds that its target had been removed. No code files were resolved here.
A UAT test file is *named* in the ticket body
(`test_UAT_FC_BUG-37_the_production_route_survives_the_new_table`) but no test
file was part of this conflict, and no test function was deleted anywhere.

Context: fifth consecutive redundant commit against BUG-37 in this bundle,
after `fe97d3bc34` (REPORT-3585), `b0af50e157` (REPORT-3586), `9255f773b5`
(REPORT-3587) and `1975a6876b` (REPORT-3589). HEAD carries this ticket in its
final, reconciled state.

## Verification

- `git status --porcelain` — no `UU`/`AA`/`DU`/`UD` lines remain.
- `git ls-files -u` — empty; no unmerged index entries.
- `git diff --cached --stat HEAD` — empty (expected; see above).
- Staged blob re-read: all nine `##` sections present, including the three
  additions this commit introduced.
- CHERRY_PICK_HEAD left intact for `cherry_pick_finalize_resolution`. No
  `--continue`/`--skip`/`--quit`/`--abort`/`reset` was run.

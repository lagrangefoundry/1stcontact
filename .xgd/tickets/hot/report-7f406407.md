---
uid: report-7f406407
id: REPORT-3203
type: report
title: 'Reconcile resolve conflicts: reconcile-REQ-162'
created_by: xgd
created_at: '2026-09-01T04:36:11.057527+00:00'
updated_at: '2026-09-01T04:36:11.057527+00:00'
completed_at: null
last_field_updated: created_at
result: pass
fields:
  report_kind: resolve_conflicts
  subject_uid: reconcile-REQ-162
---

## Files resolved

- `.xgd/tickets/hot/bug-23d1ec27.md` — UU, intent/bookkeeping ticket (rule 2e), same-fact conflict → later-positioned intent wins. Resolved to the HEAD side.

Incoming commit `93b031a3` (`xgd(ticket): update bug bug-23d1ec27`, Martin Westhead, 2026-08-25 16:21 -0700) is +3/-3, entirely within the frontmatter:

| field | HEAD (ours) | incoming (theirs) |
|---|---|---|
| `updated_at` | `2026-08-31T05:05:09.315020+00:00` | `2026-08-25T23:21:06.793799+00:00` |
| `last_field_updated` | `status` | `status` (identical, not in conflict) |
| `status` | `bundled` | `free_coding` |

`completed_at: null` is identical on both sides. Every one of the incoming commit's three changed lines falls inside the single conflict region — verified by diffing the incoming blob against index stage 3 (byte-identical) and stage 3 against stage 2 — so `git checkout --ours` dropped no auto-merged incoming content. The large body divergence visible between the two stages is pre-existing (HEAD carries the much-expanded "Fix — as landed" write-up); git auto-merged the body to HEAD because this commit does not touch it.

Basis for taking HEAD on the `status`/`updated_at` facts, three ways in agreement:

1. **Ticket timestamp** — HEAD's `updated_at` (2026-08-31) is six days later than incoming's (2026-08-25).
2. **Commit timestamp** — the enrichment block's own rule ("take the more recent commit by timestamp"). HEAD's latest touch of this file is `09291354` `seed_local_overlay` at 2026-08-31 12:21 -0700; incoming is 2026-08-25 16:21 -0700.
3. **Lifecycle consistency** — `bundled` is downstream of `free_coding`, and the auto-merged `fields:` block (unconflicted, present on the HEAD side) carries `bundled_in: bundle-8eef3846`, `version: 0.2.15`, `story_points: 3`, and the `commits:` entry. Taking incoming would revert `status` to `free_coding` while leaving `bundled_in` populated — an internally inconsistent ticket, and it would un-bundle the very bug this reconcile run is bundling.

Flagged for post-merge review per the enrichment block (intent unknown on one or both sides): BUG-39's status was decided by timeline, not by reading an operation narrative.

Staged with `git add --sparse` (path is outside the sparse-checkout cone, DOC-986 §2/§4.1). No `git cherry-pick --continue/--skip/--quit/--abort`, `reset`, or `checkout <branch>` was run; `CHERRY_PICK_HEAD` is still present at `93b031a37e3ff42a875344ec6efdf0b62c89af6a`.

## Incoming changes preserved

No code or implementation files were in this conflict — the sole conflicted path is a bookkeeping ticket, so STEP 3's code-discard guard does not bite and the BUG-1301 precedence exception was not invoked (no hunk was dropped as obsolete-by-refactor).

The incoming `status: draft → free_coding` transition is **superseded, not discarded**. This is the documented rule-2e outcome for a same-field conflict: the later-positioned intent wins that fact. The incoming side's intent — "this bug has left draft and entered the working lifecycle" — is not lost but subsumed: HEAD's `bundled` is a strictly later state on the same lifecycle field, reachable only by having passed through `free_coding`. Reverting it would move the ticket backwards.

Note on the net-zero staged diff: `git diff --cached HEAD --stat` is empty and `git status --porcelain` shows no `M`/`A`/`D` entry. This is the redundant-commit case (BUG-1109/BUG-1122), not the discarded case — HEAD already carries this commit's effect via a later state on the same field. Per STEP 4, `--skip` was NOT called; the finalize step will detect the clean staged diff and skip the commit.

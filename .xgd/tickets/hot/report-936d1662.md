---
uid: report-936d1662
id: REPORT-734
type: report
title: Claude reconciliation_uat_generation report
created_by: xgd
created_at: '2026-07-22T20:14:24.275595+00:00'
updated_at: '2026-07-22T20:14:24.275595+00:00'
completed_at: null
last_field_updated: created_at
fields:
  report_kind: reconciliation_uat_generation
  subject_uid: story-24098299
---

All 6 UATs pass. Each test name matches the enforced `^test_UAT_AC\d+_\w+$` pattern, one UAT per acceptance criterion, invoked at the module export boundary against the real evaluator/probes/gate/recovery (no internal mocking).

```
UATs generated for story story-24098299 (plan item 4 of 9)

Story: story-24098299
Test file(s) created:
  - tests/reconciliation-3probe-gate.test.ts

Tests written: 6
Tests passed: 6
Tests failed: 0

test_files_created:
  - "tests/reconciliation-3probe-gate.test.ts"
```

Coverage map (1:1 AC → UAT):

| AC | Test | What it proves against existing code |
|----|------|--------------------------------------|
| AC-705 | `test_UAT_AC705_sample_fidelity_matches_oracle_within_tolerance` | Clean fold reproduces oracle at all 6 widths (empty residuals/unmatched, maxDelta ≤2); a >2px perturbation at the **last** width surfaces a residual with the run text + per-axis deltas (proving every width is checked); a dropped run surfaces as `unmatched`. |
| AC-706 | `test_UAT_AC706_off_sample_envelope_holds_at_unsampled_widths` | Fluid base is clean at the default 500/900; a fixed-width run captured only at wide widths clips at the held-base 500px width, reported at that width with `pass=false`, while 900 stays clean. |
| AC-707 | `test_UAT_AC707_content_robustness_under_grown_content` | Pinned base fails at 2.5× with overlap findings per captured width; the `promoteToFlow` overlay holds with empty findings. |
| AC-708 | `test_UAT_AC708_combined_gate_non_vacuous_over_base_overlay_split` | Gate over the base with no overlay → `pass=false` (content-robustness fails, fidelity still passes on the base); same base + recovered overlay → `pass=true` with all three sub-reports present and passing. |
| AC-709 | `test_UAT_AC709_demand_driven_recovery_promotes_only_failing_groups` | Failing root region promoted (`'0'`), recovered doc holds robustness at every width and validates via `validateL1`; a roomy region that already passes is **not** promoted (`[]`). |
| AC-710 | `test_UAT_AC710_probe_findings_are_diagnostic` | Fidelity residual names run + deltas; overlap finding carries kind + detail + ≥2 index paths; clip finding carries kind + a px-magnitude detail + its leaf path. |

No runtime code and no existing tests were modified — a single new test file was created. The pre-existing `test_UAT_FC_REQ-86_*` file remains for the downstream FC-orphan phase to rename/delete; per the constraints I did not touch it.

---
uid: report-78617183
id: REPORT-2698
type: report
title: 'Reconcile resolve conflicts: reconcile-BUNDLE-20'
created_by: xgd
created_at: '2026-08-31T05:27:10.125196+00:00'
updated_at: '2026-08-31T05:27:10.125196+00:00'
completed_at: null
last_field_updated: created_at
result: pass
fields:
  report_kind: resolve_conflicts
  subject_uid: reconcile-BUNDLE-20
---

## Files resolved

- `.xgd/tickets/hot/request-c7f5b23a.md` — AA (both added), intent/bookkeeping ticket (rule 2e).
  Ours = `sync_working_to_main` (sync from xgd-working 715a993ebead, post-watermark);
  theirs = incoming free_coded `xgd(ticket): update request request-c7f5b23a`
  (d17cb1395a266bf53db07d478c587009f5dcab34).
  Full byte diff between the two sides is a single line: incoming adds
  `chat_comment: comment-c3ab579b` under `fields:`. Every other field, section and
  paragraph is identical. Incoming is therefore a strict superset of ours, so 2e's
  superset clause applies directly — no per-fact timeline arbitration was needed,
  since the two sides do not disagree about any fact. Resolved with
  `git checkout --theirs` + `git add --sparse` (path is outside the sparse-checkout
  cone, DOC-986 §2/§4.1).

## Incoming changes preserved

- `.xgd/tickets/hot/request-c7f5b23a.md`: verified by `git diff --cached
  d17cb1395a266bf53db07d478c587009f5dcab34 -- <path>`, which is empty — the staged
  blob is byte-identical to the incoming commit's version. The incoming commit's
  only change (`fields.chat_comment: comment-c3ab579b`) is present, and nothing from
  the ours side was dropped because ours contained no content absent from theirs.

No code/implementation files were conflicted in this cherry-pick, so no UAT or
source-file hunks were involved and the BUG-1301 precedence exception was not
invoked.

`git status --porcelain` shows no remaining UU/AA/DU/UD entries; the resolved file
is staged as `M`. The in-progress cherry-pick state (CHERRY_PICK_HEAD) was left
untouched for `cherry_pick_finalize_resolution`.

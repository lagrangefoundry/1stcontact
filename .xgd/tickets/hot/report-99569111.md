---
uid: report-99569111
id: REPORT-4290
type: report
title: 'Reconcile resolve conflicts: reconcile-BUNDLE-27'
created_by: xgd
created_at: '2026-09-18T04:42:03.840885+00:00'
updated_at: '2026-09-18T04:42:03.840885+00:00'
completed_at: null
last_field_updated: created_at
result: pass
fields:
  report_kind: resolve_conflicts
  subject_uid: reconcile-BUNDLE-27
---

## Files resolved

- `.xgd/tickets/hot/request-b474390f.md` (REQ-145) — **AA** (both added, no merge base) on an intent/bookkeeping ticket. Rules **2b** + **2e** applied per-fact; resolved to the **HEAD side** via `git checkout --ours` + `git add --sparse` (path is outside the sparse-checkout cone, DOC-986 §2/§4.1).

  The two sides' 296-line bodies are **byte-identical**. Every difference is frontmatter bookkeeping, in two hunks:

  | Fact | Ours (HEAD) | Incoming (`9ef799f91`) | Kept | Why |
  |---|---|---|---|---|
  | `status` | `free_and_reconciled` | `ready_to_reconcile` | ours | later-positioned, and downstream in the lifecycle |
  | `updated_at` | `2026-08-31T14:22:40Z` | `2026-08-20T21:15:50Z` | ours | later |
  | `completed_at` | `2026-08-31T14:22:40Z` | `null` | ours | later; moves with `status` |
  | `fields.bundled_in` | `bundle-b3b7c399` | *absent* | ours | field the incoming side never touched — superset |

  **Timeline evidence.** Both sides descend from the same ticket state: the incoming blob's own `updated_at` (`2026-08-20T21:15:50Z`) matches HEAD-side commit `5c064c9b3` (`2026-08-20T14:15:50-07:00`), i.e. the incoming commit re-adds the file at a state HEAD already contains and has since advanced twice — `8a09ff921` (2026-08-30, `seed_local_overlay`) and `811a09116` (2026-08-31/committed 2026-09-01, `update`). The incoming commit `9ef799f91` is dated **2026-08-23**, earlier than HEAD's last touch of this path. Under 2e's per-fact timeline rule and the enrichment's "take the more recent commit by timestamp," ours wins on all four facts; there is no fact on which the incoming side is later.

  No field was invented, and `fields.intent_uid` / `story_uid` / `capability_uid` were not touched. Note for post-merge review (per the enrichment's flag): the retained `bundled_in: bundle-b3b7c399` is HEAD's own recorded value and differs from this reconcile's bundle `bundle-8e1807f6` — left exactly as HEAD had it rather than rewritten, since neither side authored a different value.

## Incoming changes preserved

No code or implementation files were in conflict — the sole conflicted path is a bookkeeping ticket (2e), not a spec ticket (2d) and not source.

The incoming commit `9ef799f91` is a single-file, **296-insertion** add of `.xgd/tickets/hot/request-b474390f.md`. Its entire content is present in the resolved file: the body is byte-identical to HEAD's, and every frontmatter key it carries exists in HEAD, each holding a **strictly later** value of the same field. Nothing the incoming side authored is absent from the result — it contributes no fact that HEAD does not already carry at a more advanced state.

Consequently the staged tree nets to **no diff vs HEAD**. Per STEP 4 this is the redundant-commit case (BUG-1109/BUG-1122), not a discard: STEP 3's guard distinguishes them by asking whether the incoming commit's key changes are *present in HEAD* (redundant) or *simply absent* (discarded), and here they are demonstrably present — the full 296-line body plus every frontmatter field. `--skip` was not called; the staged state is left for `cherry_pick_finalize_resolution` to handle.

No hunk was dropped under the BUG-1301 precedence exception. No UAT or test file was involved. `CHERRY_PICK_HEAD` is intact and `git status --porcelain` is empty.

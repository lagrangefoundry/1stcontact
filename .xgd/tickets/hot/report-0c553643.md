---
uid: report-0c553643
id: REPORT-2078
type: report
title: 'Capability-Intent Alignment: Site Materials & Starting Point: Scaffold, Assets,
  Provenance & Palette (level=uat)'
created_by: xgd
created_at: '2026-08-16T06:09:08.025626+00:00'
updated_at: '2026-08-16T06:09:08.025626+00:00'
completed_at: null
last_field_updated: created_at
result: pass
fields:
  report_kind: capability_validation
  subject_uid: capability-b4ac88fc
  level: uat
  violations: 0
  warnings: 3
  needs_review_count: 0
---

# Capability-Intent Alignment: Site Materials & Starting Point: Scaffold, Assets, Provenance & Palette
# Level: uat

**Result**: PASS
**Violations**: 0
**Warnings**: 3
**Needs review**: 0

Anchor report: report-7ef6a9ea. Previous attempts: 4.

Level is `uat`, so **AC bodies are the working reference**. All 36 ACs across the
capability's four stories were read in full and compared against the body of the
test claiming them. The intent ledger below is recorded as the drift-prevention
artifact; no finding rests on intent-history interpretation.

**Execution limitation — stated plainly.** The five suites carrying these UATs
were **not executed this run**. This session's Bash permissions (don't-ask mode)
deny `npx`, `npm`, `pnpm` and `node <script>`; only `node --version` is
permitted, so no test runner could be started. The previous uat-level check
(report-8cf89ae5, 2026-08-10) did execute them: 35 of 36 passed, 1 skipped
(AC-871, browser-gated). To avoid resting on that alone, every assertion in
these tests that depends on **frozen repository state** was re-verified
statically against the current tree (see "Static corroboration" below), because
that is the class of assertion that silently rots between runs. All of them
still hold.

## Cumulative Intent Considered

| Intent ID | Status | When | Asked / changed | Counts? |
|---|---|---|---|---|
| REQ-102 (BUNDLE-11, `bundle-ee56a66e`) | free_and_reconciled | merged `f9a415a8` 2026-08-06 | `1c new` seeds a complete valid L1 document; renders/shots unedited; repro overwrites wholesale; no flag, no mode → STORY-93 | YES |
| REQ-101 (BUNDLE-11, `bundle-ee56a66e`) | free_and_reconciled | merged `f9a415a8` 2026-08-06 | Font provenance index; three-state redistribution; `distribution` marker; four violation kinds + on-disk scan; actions warn, redistribution blocks → STORY-92 | YES |
| REQ-114 (BUNDLE-14, `bundle-0385746c`) | free_and_reconciled | merged `cd8f98c8` 2026-08-06 | Palette colour model (model half → STORY-80, other capability); census; retrofit; §4 retired the theme colour group → STORY-97, `updated_by` on STORY-93 | YES |
| REQ-118 (`request-66e4c630`) | free_and_reconciled | merged `b2b9208c` 2026-08-07 | `listSiteAssets` — union of registry + `draft/assets/`, merged by handle, provenance flags, one handle vocabulary, derived `kind`, CLI + builder origin → STORY-102 | YES |
| REQ-128 (`request-de67e1a1`) | free_and_reconciled | 2026-08-08 | Background-image picker over the *same* listing; no new source | YES (no delta) |
| REQ-132 (`request-5946d045`) | free_and_reconciled | merged `6cb3942f` 2026-08-12 | Image picker becomes a thumbnail grid; client-side presentation only | YES (no delta) |
| REQ-137 (`request-d2980a95`, `bundle-d9226698`) | bundled | 2026-08-12 | Deletes palette `steps` for a continuous `shade`; supersedes byte-identity with a ≤8/255 bound | imminent — **re-verified NOT landed** on this branch (`packages/site-schema/src/l1/palette.ts:72` still declares `steps`, no `shade`) |
| REQ-133 / REQ-140 | ready_to_reconcile | 2026-08-12 / 2026-08-15 | Palette popup; page-editor colour from the palette | imminent (editor capability, no UAT here) |
| REQ-134 (`request-ba3e3fba`) | abandoned | 2026-08-12 | Image generation component | NO |

Story→intent bindings verified directly: STORY-93 `bundle-ee56a66e` (updated_by
`bundle-0385746c`), STORY-92 `bundle-ee56a66e`, STORY-97 `bundle-0385746c`,
STORY-102 `request-66e4c630`. All resolve to `free_and_reconciled`.

## Alignment Ledger

All 36 ACs are `status: active`, `kind: behavior`, `uat_coverage: pass`. Exactly
one test claims each AC — no AC number appears twice anywhere in the repo
(`test_UAT_AC(857–876|930–949|1018–1023)` swept across `*.ts`/`*.tsx`/`*.py`).
Every test drives real entry points against real on-disk trees; none is a
structural/AST-only check.

| Element | Test | Outcome |
|---|---|---|
| AC-869 | `test_UAT_AC869_created_page_carries_a_complete_valid_l1_document` (`tests/reconciliation-scaffold-starter-l1.test.ts:110`) | aligned — reads the artifact back off disk, asserts widths/background/root-container/slug run, validates the document alone **and** the assembled definition through `validateSite` (envelope, node cap, ids) |
| AC-870 | `…:145` | aligned — renders with no intervening edit; asserts the run paints in `<body>` (not the `<title>`), flow+centre CSS, and the background read back from the seeded document rather than a restated literal |
| AC-871 | `…:178` | aligned but **conditional** — `it.runIf(chromiumAvailable())`; asserts the PNG signature, not merely file existence. See warning 1 |
| AC-872 | `…:199` | aligned — element-wise equality against `RESPONSIVE_VIEWPORTS`, plus `STARTER_WIDTHS` derived-not-restated |
| AC-873 | `…:219` | aligned — both page colours hex, run inherits `doc.textColor`, no site palette, no theme palette, theme = exactly the six non-colour groups, and every seeded colour is one the document declares |
| AC-874 | `…:266` | aligned — flowed/centred root, `geometry` undefined on every node, and the rendered CSS carries no `position: absolute` |
| AC-875 | `…:304` | aligned — three slugs each yielding a valid document, plus the shipped `1c help` line asserted verbatim as `1c new <slug> [--sandbox]` and no `--l1`/`--empty` |
| AC-876 | `…:338` | aligned — repro into a virgin slug vs a created slug, byte-compared after slug normalisation, plus imported widths/background and absence of the placeholder node |
| AC-857…AC-868 | `tests/reconciliation-font-provenance.test.ts:205–662` | aligned — the shipped record validated entry-by-entry plus four damaged candidates with error paths (857); all four violation kinds (858, 859, 860, 861) with kind, message, remediation and CLI exit; the 2×2 distribution/redistribution matrix held constant except the two inputs (861); the marker as validated site contract incl. rejection of `oem` (862); advisory-with-blast-radius and its absence (863); all four record-integrity failures with non-zero CLI exit and no `PASS` (864); both site trees with per-tree attribution (865); six reference forms plus a query-bearing end-to-end pass (866); real-repo scan counts + rendered report + failing report (867); `--json` on a passing and a failing project with flag/exit agreement and nothing else on the stream (868) |
| AC-939 | `tests/reconciliation-colour-census-and-retrofit.test.ts:200` | aligned — header counts with RGB strictly collapsing, per-literal ordering and α annotation, the alpha-families section, the zero-colour site, and a before/after tree hash proving read-only |
| AC-940 | `…:265` | aligned — whole stdout parsed as one JSON value, every field's presence and type, `distinctRgb` cross-checked against the records, and the human form's header cross-checked |
| AC-941 | `…:331` | aligned — literal count measured by census (not baked in), entry count < half, palette on the site, per-entry report lines, files-written list compared against an actual changed-file diff, no literal left in any page, and `--assign --json` equal to the stored palette |
| AC-942 | `…:404` | aligned — the three-opacity family collapses to exactly one carrier entry, no entry carries alpha, and each authored translucent literal is recovered by resolving the written references |
| AC-943 | `…:452` | aligned — all four grouping claims (ramp→one entry with steps, vivid vs near-neutral split, isolate keeps its own entry, greys/near-white/near-black share `neutral`) plus determinism on an independent second run |
| AC-944 | `…:506` | aligned — renders every page before and after, compares bytes file-for-file, and independently resolves the converted definition back to the pre-conversion object incl. opacity |
| AC-945 | `…:560` | aligned — all three refusal causes (no draft, non-lossless via forced name collision, invalid definition), each with non-zero exit, a cause-identifying stderr diagnostic, and an unchanged tree hash |
| AC-946 | `…:615` | aligned — kebab-case + descriptive-vocabulary + uniqueness incl. the disambiguating suffix; rename via `--names` changes names only (values/steps/other entries identical, resolved colours identical); an unknown family leaves the palette intact |
| AC-947 | `…:710` | aligned — a reproduced site arrives with literals and no palette; census before ≡ census after; a second retrofit reproduces the first palette exactly |
| AC-932 | `test_UAT_AC932_retrofit_shrinks_the_palette_materially_and_paints_the_same_colours` (`tests/reconciliation-colour-palette-overlay.test.ts:467`) | **covers all three of the AC's claims** — materially-smaller entries, the painted-colour multiset preserved across the conversion, and the zero-colour no-op on `1stcontact`/`harbor-cafe`. Two problems, both warnings: frozen counts (warning 2) and duplication of AC-941/AC-944 (warning 3) |
| AC-1018…AC-1023 | `tests/reconciliation-site-asset-listing.test.ts:218–376` | aligned — the empty-registry/full-directory case every real site is in (1018); declared-and-present merged to one entry plus declared-but-absent (1019); bare and qualified naming merged to one handle, handle order stable across calls, the handle compared against what the page actually holds, and the off-site-URL boundary (1020); kind per file with the unfiltered listing intact (1021); full entry shape from the CLI plus the empty-site success (1022); the builder origin over real HTTP compared entry-for-entry with the CLI answer, the 400 on a missing slug, and the builder's own `fetchAssets` client (1023) |

## Static corroboration (in lieu of execution)

Every repo-state-dependent expectation in these suites re-verified against the
current tree:

| Assertion | Where | Current tree | Verdict |
|---|---|---|---|
| `xgd` → 16 distinct RGB, 6 palette entries | AC-932 test:473 | `storage/sites/xgd/draft/site.json` carries 6 entries over 16 distinct opaque values | holds |
| `gigabytealchemy` → 30 distinct RGB, 8 entries | AC-932 test:474 | 8 entries over 30 distinct opaque values | holds |
| `1stcontact`, `harbor-cafe` carry no palette and census to zero | AC-932 test:510, AC-939 test:251 | neither declares `palette`; neither's pages carry a colour literal or a palette reference | holds |
| `#2e86a3` used at α 1.00 / 0.65 / 0.33 (bytes 255/166/85) | AC-939 test:247, AC-940 test:317 | `xgd`'s 210 page references resolve to exactly one alpha family, `#2e86a3` at α {1.0, 0.65098, 0.33333} | holds |
| Palette schema still `steps`, not `shade` (REQ-137 unlanded) | AC-941/943/944 | `packages/site-schema/src/l1/palette.ts:72` | holds |
| `fonts/registry.yaml` present and non-empty | AC-857/867 | present, referenced by `REGISTRY_REL` | holds |

## Findings

| # | Severity | Property | Element | Resolution category | Issue | Suggested edit |
|---|---|---|---|---|---|---|
| 1 | warning | coverage | AC-871 (`acceptance_criterion-b17420aa`) / `test_UAT_AC871_fresh_site_shoots_without_hand_editing` | — | The UAT is registered with `it.runIf(await chromiumAvailable())` (`tests/reconciliation-scaffold-starter-l1.test.ts:47-48,177`), so on a host without a headless browser the screenshot criterion is evidenced by nothing and the suite still reports green. The prior uat check (report-8cf89ae5) recorded exactly this skip. Held at warning, not violation, because AC-871's own body sanctions the gate ("Requires a headless browser; the check is gated on browser availability") — the criterion and the test agree | None at the test level. If the capability wants unconditional evidence, that is an AC-871 body change (drop the gating clause) plus a browser guarantee in the regression environment — an `ac`-level decision, not a `uat` repair |
| 2 | warning | consistency | `test_UAT_AC932_…` (`tests/reconciliation-colour-palette-overlay.test.ts:471-486`) | uat-edit | The test hard-codes today's repository contents — `['xgd', {distinctRgb: 16, entries: 6}], ['gigabytealchemy', {distinctRgb: 30, entries: 8}]` — and asserts them with `toBe`. AC-932's criterion is *relative* ("materially smaller than the number of distinct colours the site used"); the absolute numbers are not part of it. Adding one page to either stored site fails a test whose criterion has not changed. Verified true today (see static corroboration), so this is brittleness rather than breakage. It is the same defect the `ac` level raised against AC-932's body (report-42025e18 finding 2), which records that this exact drift already happened once when REQ-114's AC7 froze 17/15 and the site moved to 18/16 | Keep the relative assertions (`entries < census.distinctRgb / 2`, `assigned.before > entries`), which already carry the criterion, and drop the two `toBe(expected.…)` equalities — or demote them to a logged observation. The loop over both stored sites is worth keeping |
| 3 | warning | exclusivity | `test_UAT_AC932_…` vs `test_UAT_AC941_…` (`tests/reconciliation-colour-census-and-retrofit.test.ts:331`) and `test_UAT_AC944_…` (`…:506`) | uat-edit | AC-932's test re-asserts two claims already owned by sibling tests: "a palette, not a colour list" (AC-941's test:353 asserts the same `entryCount < literalCount / 2` on the same `xgd` colours) and colour-losslessness (AC-944's test:549-553 asserts the strictly stronger property — byte-identical render plus per-reference resolution back to the original literal). Held at **warning**, not violation, because the two tests are not the same *shape*: AC-932 runs the in-process handlers over copies of the real stored sites and compares painted-colour multisets, where AC-941 drives the shipped `1c` launcher over a sandbox copy and AC-944 uses a synthetic reproduced site. The duplication is inherited from the AC bodies, and the `ac` level already owns it as a violation (report-42025e18 finding 1, still unrepaired) | Do not repair here first. When AC-932 is narrowed to its one unique criterion (the zero-colour retrofit no-op), re-point this test at that case: keep lines 508-518 (`1stcontact`/`harbor-cafe`), and drop the shrink and multiset blocks that AC-941 and AC-944 already prove. Repairing the test before the AC would leave AC-932's current body unevidenced |

## Notes for the Editor

- **Nothing at this level blocks.** Zero violations, zero needs_review. All
  three warnings land on evidence that is currently correct; two of them are
  contingent repairs that should follow the `ac`-level fix, not precede it.

- **Sequencing with the `ac` level.** report-42025e18 (level=ac, FAIL) requires
  AC-932 be narrowed to the zero-colour no-op. The moment that lands, warnings 2
  and 3 become a single mechanical edit to
  `tests/reconciliation-colour-palette-overlay.test.ts:449-519`: keep the
  zero-colour block, delete the shrink/multiset blocks and the frozen counts,
  and rename the test to match the narrowed criterion. Doing it in the other
  order leaves AC-932 momentarily unevidenced.

- **The AC-932 test lives in another story's file.** It sits in
  `reconciliation-colour-palette-overlay.test.ts` beside AC-930 and AC-931,
  which belong to STORY-80 in the framework-substrate capability — a residue of
  AC-932 having been authored under STORY-80 on 2026-08-06 and moved onto
  STORY-97 on 2026-08-09. Not a finding (file placement is not a matrix
  property, and the test does drive STORY-97's `cmdColors`/`cmdColorsAssign`),
  but an editor touching it should know the file is shared with another
  capability's evidence.

- **Story-level drift does not reach this level.** report-375af0aa (level=story,
  FAIL) holds two unrepaired Technical Context violations on STORY-93 and
  STORY-102. Both were checked for cascade: no test in this capability repeats
  the stale phrasing, and neither stale claim is something a UAT asserts. They
  remain blocking at their own level.

- **Execution gap, and what would close it.** These suites could not be run in
  this session — Bash permissions deny every available runner (`npx`, `npm`,
  `pnpm`, `node <script>`; `node --version` alone is allowed). The static
  corroboration table above covers the assertions that decay with repository
  state, which is the failure mode reading cannot catch. What it does not cover
  is a runtime regression in the production code these tests drive — that is the
  UAT-coverage report's job, not this one's. If this check is expected to
  execute, the session needs `Bash(npx vitest*)` or an equivalent runner on the
  allowlist.

---
uid: report-45750e29
id: REPORT-3633
type: report
title: 'Capability-Intent Alignment: Client Material Store: What A Site Is Made From,
  As Tickets (level=story)'
created_by: xgd
created_at: '2026-09-10T02:02:37.351434+00:00'
updated_at: '2026-09-10T02:02:37.351434+00:00'
completed_at: null
last_field_updated: created_at
result: pass
fields:
  report_kind: capability_validation
  subject_uid: capability-dfb0a4ff
  level: story
  violations: 0
  warnings: 1
  needs_review_count: 0
---

# Capability-Intent Alignment: Client Material Store: What A Site Is Made From, As Tickets
# Level: story

**Result**: PASS
**Violations**: 0
**Warnings**: 1
**Needs review**: 0

## Cumulative Intent Considered

CAP-106 (`capability-dfb0a4ff`) was created 2026-09-01 during REQ-162's
reconciliation. All three of its stories carry `fields.intent_uid =
request-13a5e206` and none carries an `updated_by` chain, so the ledger is
short: one intent, fully reconciled.

| Intent ID | Status | When | Asked / changed | Counts? |
|---|---|---|---|---|
| REQ-162 (`request-13a5e206`) | free_and_reconciled (merged at `4b43dd9a`) | 2026-08-31 | Stood up the whole capability: `0003_ticket_store.sql` from the component's `SCHEMA_STATEMENTS` + the `tenants.config` ALTER; `ticketStoreFor(env)` as the single tenant-scoped wiring point with `TenantNotConfiguredError` / `BlobsNotConfiguredError`; register-if-absent tenant bootstrap; the `1stcontact-material` bucket, separate from `1stcontact-sites`, declared in both wrangler halves; the product TypePack (`material`, `reference`, `brief`) with the DOC-38 §9 rights/provenance block, plus merged chat and attachment schemas; the `1c assets` ticketing shim | YES |
| REQ-159 (`request-119dd4af`) | draft | 2026-08-30 | Project KB / corpus predicate over these types | NO (draft) |
| REQ-160 (`request-bbff35c7`) | draft | 2026-08-30 | Session cursor on a chat ticket | NO (draft) |
| REQ-161 (`request-78370159`) | draft | 2026-08-31 | Library tab over this material | NO (draft) |
| REQ-163 (`request-439cd0c8`) | draft | 2026-08-31 | Ingestion — creates these records | NO (draft) |
| REQ-166 (`request-20bd7d63`) | draft | 2026-08-31 | Capture bundles become corpus members | NO (draft) |

No abandoned / deprecated / wont_fix intent touches this capability, so Step 2.5's
stale-vehicle case does not arise. The draft intents above appear in the story
bodies only as **out-of-scope pointers** (STORY-126 excludes the HTTP surface as
REQ-161's; STORY-128 excludes the KB as REQ-159's) — they are scope exclusions,
not delivery-vehicle claims, so their draft status is correct and expected.

REQ-162 also carried collateral **outside** this capability — AC-1398 (STORY-121,
Cloudflare Site Store) was generalised from "two identical bucket-name
occurrences" to "each declared binding names the same target in both halves",
because the second bucket made the count form wrong rather than merely
imprecise. Verified: AC-1398's body carries that pairing-by-binding-name
language today. Correctly held outside CAP-106.

## Alignment Ledger

| Element | Intents aligned to | Outcome |
|---|---|---|
| STORY-126 (`story-ab1ecd62`, feature, completed) — Product Ticket Store | REQ-162 | aligned. In-scope list (schema step + agreement check; shared `tenants` reconciliation; obtaining a store and its refusals; row-level account barrier; build-time component resolution) maps 1:1 onto REQ-162 deliverables 1–2, its acceptance lines 1, 3 (the `ticketStoreFor` half), 4 and 7, and its implementation notes. The three "Reconciliation Decisions" are labelled as such and each is grounded in landed code: schema-drift assertion (AC-1477), read-before-write tenant bootstrap (`tickets.ts:286-288`, AC-1480), stale-install skip reporting (`tests/support/ticketing-installed.ts:57`, part of AC-1485). |
| STORY-127 (`story-a7a12d81`, feature, completed) — Material Blob Storage | REQ-162 | aligned, with one citation error (finding 1). Separation-from-`SITES`, content-derived tenant-prefixed addressing, and both-halves declaration all trace to REQ-162 deliverable 3 and acceptance line 2. |
| STORY-128 (`story-e07c589b`, feature, completed) — Material Types | REQ-162 | aligned. The three kinds, the shared six-field block, required `republishable`/`exportable`, `required_when` on `source_url`, and merged chat/attachment schemas trace to REQ-162 deliverable 4 and acceptance lines 5–6. Its four reconciliation decisions are grounded in `apps/control-app/src/tickets.ts`: optional `material` body (`:105`, AC-1497), `brief` required non-empty body + required `site_slug` (`:146-148`, AC-1496), optional `site_slug` on `MATERIAL_FIELDS` (`:72`, AC-1498), `chatSchemas()` merged (`:164`, AC-1499). REQ-162's two open questions are settled *in the intent body itself* (reference keeps its own type; brief keeps its own type with `fields.site_slug`) and the story matches both. |

**Coverage sweep — every REQ-162 ask is expressed:**

| REQ-162 ask | Expressed at |
|---|---|
| Schema applies as a migration beside the existing two | AC-1476 |
| Transcription must not silently fork from `SCHEMA_STATEMENTS` | AC-1477 |
| One shared `tenants` registry + `ALTER … ADD COLUMN config` | AC-1478 |
| Store tenant-scoped at construction; ticket readable back through the Worker's own wiring | AC-1479 |
| Register-if-absent bootstrap, read before write | AC-1480 |
| `TENANT_ID` absent → refuse at construction | AC-1481 |
| `ticketStoreFor(env)` throws when the blob binding is absent | AC-1482 |
| Tenant A cannot read/write tenant B's rows — asserted | AC-1483, AC-1484 |
| Component resolves from a linked worktree via a generated shim | AC-1485 |
| Attachment ops work through the wired store | AC-1486 |
| Bytes land in `1stcontact-material` and not in `1stcontact-sites` | AC-1487, AC-1489 |
| Content-addressed, tenant-prefixed keys (dedup within, isolation across) | AC-1488 |
| Bucket declared top-level **and** under `[env.production]` | AC-1490 |
| `material` / `reference` / `brief` as the vocabulary; chat + attachment share it | AC-1491, AC-1499 |
| DOC-38 §9 field block; bad `rights` / `kind` refused | AC-1492, AC-1493 |
| `republishable` / `exportable` required, never derived | AC-1494 |
| `source_url` required exactly where origin implies it | AC-1495 |
| `brief` carries `site_slug` and a document | AC-1496 |
| No `status` vocabulary on the three new types | AC-1491 (stated) |

No gap found. The one REQ-162 line that no criterion carries — `wrangler r2
bucket create 1stcontact-material` before the next production deploy — is
explicitly recorded as an operator obligation in both STORY-126 and STORY-127
under "An operator obligation no criterion can carry", which is the correct
disposition for something miniflare conjures locally and Cloudflare does not.

**Exclusivity:** the three stories partition cleanly — store/schema/scoping,
bytes, vocabulary. The two places they come close are both explicitly disclaimed
in prose: STORY-127 pushes the construction-time blob refusal to STORY-126
(AC-1482), and STORY-127's byte-level account barrier (AC-1488) is argued as a
separate mechanism from STORY-126's row-level one (AC-1483/1484) — "proving one
proves nothing about the other", which is correct. No duplicate coverage.

## Findings

| # | Severity | Property | Element | Resolution category | Issue | Suggested edit |
|---|---|---|---|---|---|---|
| 1 | warning | consistency | STORY-127 (`story-a7a12d81`), Technical Context, 2nd bullet | story-body-edit | The body cites "a general criterion elsewhere in the matrix already requires that every declared binding is repeated under the named deployment environment (**STORY-125 / AC-1341**)". AC-1341 belongs to `story-d5167ced` = **STORY-119** (Platform Build, Deploy & Smoke), not STORY-125 (`story-7fa314f5`, "Self-origin fulfilment: a picture of my own draft is the draft, not a sign-in challenge"), which is an unrelated story. The criterion itself is correctly characterised — AC-1341 is "Every Worker's named environment repeats every top-level variable and binding" — only the owning story is misnamed. | Change `STORY-125 / AC-1341` to `STORY-119 / AC-1341`. |
| 2 | info | consistency | STORY-127, Technical Context, 2nd bullet | — | The companion citation in the same bullet is correct: AC-1398 is on `story-fde7370b` (STORY-121, Cloudflare Site Store) and its body does carry the pair-by-binding-name generalisation REQ-162 landed as collateral. | none |
| 3 | info | coverage | STORY-127 (AC-1488), STORY-128 (AC-1498) | — | Both story bodies openly state that a half of these criteria is not yet exercised by the landed suite — dedup-within-an-account on AC-1488, and `site_slug` on a `material`/`reference` on AC-1498. Both fields exist in code (`apps/control-app/src/tickets.ts:72`; content-derived `blobKey`), so this is a UAT-depth observation for the `uat` level, not a story-level gap, and the disclosure is the honest disposition. | none — carry forward to the `uat` level |
| 4 | info | consistency | STORY-126, "The construction-time refusal is this platform's decision" | — | The story records that the component deliberately refuses attachments at *call* time while this platform refuses at *construction* time, and that the intent body itself settles the split. Verified against `apps/control-app/src/tickets.ts:274-290` (`BlobsNotConfiguredError` raised before the store is built) and against REQ-162's "Enforcement lives at our wiring layer, not the component's". No intent/code contradiction; nothing to fix. | none |

## Notes for the Editor

- **Single-intent capability.** Everything here descends from REQ-162 and REQ-162
  alone. There is no retired behaviour to strip and no competing later intent, so
  the usual drift shapes (a story still describing a flag some later intent
  removed) cannot apply yet. The next real drift risk is REQ-159 / REQ-161 /
  REQ-163 landing: each will add behaviour *over* these types, and STORY-126 and
  STORY-128 both name them as out of scope by ticket ID. When one of those moves
  off `draft`, re-run this check — those out-of-scope lines are the places that
  will need to change.
- **Cross-reference hygiene is the only defect found.** Finding 1 is a
  one-token misattribution and does not change scope, behaviour, or any AC. It is
  worth fixing because the story-body citations in this capability are unusually
  load-bearing — they are how STORY-127 argues that AC-1490 is a deliberate
  re-pin rather than a duplicate of an existing general criterion, and a reader
  who follows the pointer to STORY-125 finds an unrelated story about
  self-origin image fulfilment and cannot verify the argument.
- **Implementation grounding was checked, not assumed.** The reconciliation
  decisions in all three bodies were read against `apps/control-app/src/tickets.ts`,
  `tools/generate/src/cli/assets.ts:226-245,340-350`, and
  `tests/support/ticketing-installed.ts`. Every code-derived claim in a story body
  is present in the landed code, including the two the bodies flag as
  under-exercised by tests.

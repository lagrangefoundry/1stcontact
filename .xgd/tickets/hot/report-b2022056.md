---
uid: report-b2022056
id: REPORT-2321
type: report
title: 'Resync resolve conflicts: 7292af8f63220275216b82416fc441658a744e02'
created_by: xgd
created_at: '2026-08-20T02:38:01.051691+00:00'
updated_at: '2026-08-20T02:38:01.051691+00:00'
completed_at: null
last_field_updated: created_at
result: pass
fields:
  report_kind: resolve_conflicts
  subject_uid: report-db1949d9
---

## Files resolved

- `package.json` — **UU**, config-file scalar conflict (rule 2g / enrichment "unknown intent, take the more recent side"). The whole conflict was the `version` scalar: ours (HEAD, `xgd: sync from xgd-working 097e8bc90814 (post-watermark)`) `0.1.59` vs theirs (`90b762cf4`, `test(builder): pin the editing box's tracking to the words [FREE-CODED]`) `0.1.43`. **Kept ours (`0.1.59`).** The version is a monotonic bookkeeping counter, not developer code — each free-coded ticket claims a distinct value, so taking `0.1.43` would rewind main onto versions later tickets have already consumed. This also agrees with the timestamp rule: the main-rooted sync commit is the later of the two. `git show 90b762cf4 -- package.json` confirms the incoming hunk is the version line and nothing else, so no substantive incoming change was discarded.

- `tests/test_UAT_FC_BUG-35_tracking_reaches_the_words.test.ts` — **A** (staged add, no conflict). Auto-applied cleanly by the cherry-pick; left as-is. No UAT function on either side was deleted or modified.

## Incoming changes preserved

- `tests/test_UAT_FC_BUG-35_tracking_reaches_the_words.test.ts` — `git diff --cached 90b762cf4 -- <path>` is **empty**: the staged blob is byte-identical to the incoming commit's version. All 256 lines of the new browser-driven UAT (tracked run previews at the page's tracking, untracked run gets none, parameter-sheet controls stay chrome) are present.

- `package.json` — incoming carried only the `0.1.42 → 0.1.43` version bump, deliberately superseded by main's `0.1.59` per the rule above. No code, script, dependency or engine field differed between the two sides.

## Staging state

`git status --porcelain` shows no conflict-class lines (UU/AA/DU/UD/AU/UA) and `git grep` finds no residual conflict markers anywhere in the tree. Staged diff vs HEAD is the single new test file (256 insertions); `package.json` resolves back to HEAD's content and therefore contributes no net change — expected, and left for Python's finalize step to handle. `CHERRY_PICK_HEAD` was left intact; no `--continue`/`--skip`/`--quit`/`--abort` was run.

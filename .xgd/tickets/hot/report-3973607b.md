---
uid: report-3973607b
id: REPORT-2959
type: report
title: 'Reconcile resolve conflicts: reconcile-BUNDLE-21'
created_by: xgd
created_at: '2026-08-31T15:13:43.972052+00:00'
updated_at: '2026-08-31T15:13:43.972052+00:00'
completed_at: null
last_field_updated: created_at
result: pass
fields:
  report_kind: resolve_conflicts
  subject_uid: reconcile-BUNDLE-21
---

## Files resolved

- `.xgd/tickets/hot/request-4fcbd354.md` — **AA** (both added), intent/bookkeeping
  ticket → rule **2e** (strict-superset), corroborated by the enrichment's
  more-recent-commit rule. Kept the HEAD (ours) side.

  Index-only conflict: `.xgd/tickets/` is outside the sparse-checkout cone on this
  reconcile branch (DOC-986 §2/§4.1), so there were no working-tree conflict markers —
  the on-disk file already held the ours content verbatim. Staged with
  `git add --sparse`.

  Why ours: the two blobs' 167-line bodies are **byte-identical**. The only divergence
  is three frontmatter facts, and HEAD is a strict superset of incoming on all three:

  | fact | incoming (`61d15c3f`, ticket state 2026-08-22) | ours (HEAD, ticket state 2026-08-24) |
  |---|---|---|
  | `updated_at` | `2026-08-22T21:55:22` | `2026-08-24T02:10:41` (later) |
  | `status` | `ready_to_reconcile` | `bundled` (lifecycle advanced forward) |
  | `fields.bundled_in` | *absent* | `bundle-b3b7c399` (added) |

  Both applicable rules agree. Per 2e, ours advances `status` and adds a field the
  incoming side never touched → keep the superset. Per the enrichment's
  intent-unknown rule, take the more recent commit by timestamp: ours is
  `7a8d0abd` (2026-08-31), incoming is `61d15c3f` (2026-08-23). Taking incoming
  would have regressed `status` from `bundled` back to `ready_to_reconcile` and
  dropped `bundled_in: bundle-b3b7c399` — bundle bookkeeping for a bundle that is
  being reconciled right now.

  Flagged for post-merge review per the enrichment rule (intent unknown on one or
  both sides), though the fact-level analysis above leaves no genuine competing
  intent: no field was changed differently on the two sides, only advanced.

## Incoming changes preserved

No code/implementation files were in conflict — the sole conflicted path is an
intent/bookkeeping ticket (2e), not matrix state or source.

Nothing from the incoming commit was discarded. `61d15c3f` is a whole-file add
(167 insertions, single file); its entire content is present in the resolved
version. The two facts where the resolved file differs from incoming
(`updated_at`, `status`) are the *same fields* carried forward to a later value by
HEAD, not competing edits — the incoming values are superseded states of the same
ticket, not lost content. `bundled_in` is additive on the HEAD side only.

No hunks were dropped under the BUG-1301 precedence exception; none applied.

Resolution nets to no diff vs HEAD (`git status --porcelain` shows no staged
change), which is expected here and is not a @fail per STEP 4 — HEAD already
contains the incoming commit's content plus later bookkeeping. Per instructions,
`--skip` was NOT called; `CHERRY_PICK_HEAD` (`61d15c3f`) is left intact for
`cherry_pick_finalize_resolution`.

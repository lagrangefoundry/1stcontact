---
uid: report-a9921685
id: REPORT-4360
type: report
title: 'Reconcile resolve conflicts: reconcile-BUNDLE-27'
created_by: xgd
created_at: '2026-09-19T09:26:46.672598+00:00'
updated_at: '2026-09-19T09:26:46.672598+00:00'
completed_at: null
last_field_updated: created_at
result: pass
fields:
  report_kind: resolve_conflicts
  subject_uid: reconcile-BUNDLE-27
---

## Files resolved

- `.xgd/tickets/hot/bundle-8eef3846.md` — **AA** (both added), bookkeeping/intent
  ticket (bundle). Resolved under **2e** (per-fact, later-positioned intent wins),
  which for this file collapses to taking the HEAD side whole.

  Two conflict regions only:

  1. **Frontmatter** (lines 8–366 of the merged file). Ours = the same bundle ticket
     at `updated_at: 2026-09-01T00:00:07Z`, `status: free_and_reconciled`,
     `completed_at` set, `result: pass`, with the full `orphan_commits` map and
     `merged_at_commit: 90527353c0fa4b9fd3ae91ba6285c7d791a25c53`. Theirs
     (`c2efcb99cb`, "create bundle bundle-8eef3846") = the creation snapshot at
     `updated_at: 2026-08-31T05:05:09Z` (== `created_at`), `status:
     ready_to_reconcile`, `completed_at: null`.

     Same facts, sequential not competing: ours is the identical ticket ~19h later
     in its own lifecycle. Every field the incoming side sets (`auto_merge_back`,
     `priority`, `fields.commits`) is present on the HEAD side at its advanced
     value; the HEAD side additionally carries `orphan_commits`,
     `merged_at_commit` and `result`. Ours is therefore a strict superset in field
     coverage and the later intent per fact.

     `fields.commits` specifically: theirs lists the two creation-time
     `working_sha` values (`759cd874…`, `29c0e86d…`, both with
     `main_sha: null`); ours is the post-merge rewrite of that same list — one
     entry, `working_sha: null`, `main_sha: 90527353…`. These are two states of one
     field, not two disjoint edits, so they are not combined: splicing the
     unmerged creation entries back alongside the merged record would fabricate a
     commits list neither side holds and would misreport this bundle as still
     having unreconciled working commits.

  2. **Body tail** (line 712). Both sides read `Done, three UATs`; the only
     difference is that the incoming side terminates the file with a newline and
     ours does not. Cosmetic; took ours.

  The rest of the 369-line body merged cleanly and is byte-identical on both sides
  (verified with `git diff :2:<path> :3:<path>` — the whole diff is the frontmatter
  hunk plus the "\ No newline at end of file" marker).

## Incoming changes preserved

Yes. The incoming commit `c2efcb99cb` is a pure file creation (369 insertions,
this file only). Its entire body content is present verbatim in the resolved
file, and each frontmatter field it introduces is present at a later value that
the same ticket's own subsequent lifecycle produced. Nothing the developer
authored is absent.

No hunk was dropped under the BUG-1301 precedence exception; no test functions
were involved.

Net effect: the staged tree is identical to HEAD, because HEAD already contains
this commit's content by a later route (the reconciled state of the same bundle
ticket). This is the STEP 4 "redundant, not discarded" case — STEP 3's check
passes because the incoming commit's content is present in HEAD, not merely
missing. `git cherry-pick --skip` was not called; CHERRY_PICK_HEAD is left
intact for `cherry_pick_finalize_resolution`.

---
uid: report-4e595020
id: REPORT-3223
type: report
title: 'Reconcile resolve conflicts: reconcile-REQ-162'
created_by: xgd
created_at: '2026-09-01T21:53:19.034979+00:00'
updated_at: '2026-09-01T21:53:19.034979+00:00'
completed_at: null
last_field_updated: created_at
result: pass
fields:
  report_kind: resolve_conflicts
  subject_uid: reconcile-REQ-162
---

## Files resolved

- `.xgd/tickets/hot/request-b88b79fe.md` — **UU**, intent/bookkeeping ticket (rule 2e).
  Sparse-excluded path (DOC-986 §2/§4.1), staged with `git add --sparse`.

  Ours = `xgd(ticket): seed_local_overlay request request-b88b79fe`;
  theirs = `xgd(ticket): update request request-b88b79fe` (3b9156ae89, free_coded,
  2026-08-30 17:52 -0700).

  `git diff :2: :3:` showed the two sides differ in **frontmatter only** — two hunks,
  both above the `---` close. The 120-insertion / 158-deletion body reflow that the
  incoming commit carries is byte-identical on both sides, because the
  `seed_local_overlay` commit already seeded it into HEAD. So no per-fact contest
  exists in the body at all.

  Per-fact resolution of the frontmatter (2e), against the merge base (stage 1,
  `updated_at: 2026-08-30T20:37:44`, `status: ready_to_reconcile`,
  `last_field_updated: status`, no `bundled_in`):

  | fact | base | ours | theirs | kept | why |
  |---|---|---|---|---|---|
  | `status` | `ready_to_reconcile` | `bundled` | `ready_to_reconcile` | **ours** | Non-overlapping: theirs is unchanged from base, only ours advanced it. Taking theirs would revert the bundle status. |
  | `fields.bundled_in` | absent | `bundle-8eef3846` | absent | **ours** | Added by ours only; theirs never touched it. Ours is a strict superset. |
  | `updated_at` | 08-30T20:37:44 | 08-31T05:05:09 | 08-31T00:52:48 | **ours** | Later timestamp. |
  | `last_field_updated` | `status` | `status` | `body` | **ours** | Derived field. Theirs recorded its 00:52 body edit; ours' 05:05 status advance is the later update, so `status` is the correct composed value. |
  | body (120+/158-) | wrapped | reflowed | reflowed | either — identical | Incoming reflow already in HEAD via `seed_local_overlay`. |

  Net: the resolution equals stage 2 exactly. `git checkout --ours` was provably
  lossless here — verified before staging by diffing the stage-2 and stage-3 blobs
  directly and confirming zero body-region hunks, not by assuming it.

## Incoming changes preserved

Yes. The incoming commit's only substantive change to this file is the body reflow,
and it is present in the resolved file — spot-checked on a distinctive incoming-side
line (`§8 states the whole migration in one line: _"Swap the Playwright driver for a
CF Browser Rendering driver behind the same _`BrowserDriver`_ seam."_`), which the
resolved file contains. Staged blob is `ca6629593d`, with zero conflict markers.

The only incoming content deliberately not taken is the frontmatter bookkeeping
regression — reverting `status` to `ready_to_reconcile`, dropping `bundled_in`, and
rolling `updated_at` back by ~4h — which is stale-side bookkeeping, not developer
intent, and is exactly what 2e's per-fact rule discards.

No hunks were dropped under the BUG-1301 precedence exception; it did not apply here.
No code, test, or spec-ticket files were involved.

## Note for the finalize step

The staged diff vs HEAD for this file is **empty** — this cherry-pick is redundant,
not discarded (BUG-1109 / BUG-1122). STEP 3's distinguishing check confirms the
"redundant" reading: the incoming commit's key change is *present* in HEAD (via
`seed_local_overlay`), not merely absent. Per STEP 4, `--skip` was not called; the
finalize step should detect the clean staged diff and skip the commit itself.
CHERRY_PICK_HEAD (3b9156ae89) is intact.

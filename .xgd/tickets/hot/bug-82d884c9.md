---
uid: bug-82d884c9
id: BUG-54
type: bug
title: 'User tab: Who they are is all-editable and saves nothing'
created_by: xgd
created_at: '2026-09-05T20:58:21.033656+00:00'
updated_at: '2026-09-05T21:26:52.617644+00:00'
completed_at: null
last_field_updated: body
status: free_coded
fields:
  auto_merge_back: true
  needs_review: false
  priority: medium
  chat_comment: comment-4561ad44
  severity: medium
  commits:
  - working_sha: 7f348d433ec25b01e622e1d8e9dc8fc3017d90b0
    reconcile_sha: null
    main_sha: null
  version: 0.2.79
  story_points: 3
---

## Symptom

User tab → a person's detail → **Who they are**: every field renders as an
editable text box, and nothing that is typed into any of them is saved.

## Root cause

Two independent faults in the one `mountFields` call in
`apps/control-app/src/builder/people.js`.

1. **Nothing saves, for any field.** The panel passes `onSave`.
   `@lagrangefoundry/webui-fields` reads `onCommit`. The key is simply never
   read, so a confirmed edit is applied optimistically to the widget's own
   copy of the values and never leaves the browser. There was no error to see:
   the widget's rollback path only runs when the commit callback *rejects*, and
   a callback that was never invoked never rejects.

2. **Everything is editable.** The mount passes `editable: true`, which is the
   viewer-wide override — it makes every field in the schema editable. The
   `{ editable: true }` written on the `status` descriptor to mean "only this
   one" is not a key the component reads (the descriptor axis is `locked`, the
   hard ceiling, plus `defaultEditable`), so it did nothing, and the record of
   what the system observed — invited, first seen, last seen, terms accepted,
   created — was offered to the operator to retype.

## Fix

**Which fields are editable, per the operator's specification:**

| Field | Label | Editable |
|---|---|---|
| `email` | Email | **yes**, validated (below) |
| `displayName` | Name | **yes** |
| `state` | State | no |
| `status` | May sign in | no |
| `invitedAt` | Invited | no |
| `firstSeenAt` | First seen | no |
| `lastSeenAt` | Last seen | no |
| `termsAcceptedAt` | Terms accepted | no |
| `createdAt` | Created | no |

The read-only ones are expressed as `locked: true` on the descriptor — the
component's *hard ceiling*, which no viewer override can lift — and not as a
narrower `editable` whitelist at the mount. These are facts the system
recorded, not a permission this surface happens to withhold, so the constraint
belongs in the schema where a second viewer inherits it.

`state` is additionally derived rather than stored: it is computed by
`stateOf()` from `invitedAt`/`termsAcceptedAt` and has no column behind it,
so an edit to it could not be written anywhere even in principle.

**Email validation.** An address is accepted only if it has exactly one `@`,
a non-empty local part, and a domain of at least two non-empty
`.`-separated labels — `a@b.c` passes, `a@b`, `a@@b.c`, `a@.c`, `a@b.`,
`@b.c` and anything containing whitespace do not. Email is `required`, so
clearing the box is refused rather than saved as null.

The rule has **one definition**, `isEmailShape` in the new
`apps/control-app/src/builder/email-shape.js` — a module with no imports,
reachable from both sides of the seam, exactly as `people-state.js` is. The
browser panel uses it for the inline refusal and `people.ts` uses it for the
authoritative one, so the two cannot come to disagree about what an address is.
(`allowJs` is enabled for the control-app typecheck so the `.ts` server can
import it; `checkJs` stays off, so no `.js` file starts being type-checked.)

**Where the edit goes.** A new `POST /api/people/record` writes `email` and/or
`display_name` for one person in the business the caller's scope resolved to.

- Gated on `ownsBusiness`, the same first condition of [[DOC-42]] §7 the invite
  route uses, and emphatically not `ownsPlatformBusiness` — changing a person's
  own record is uniform, and every business owner has it on theirs.
- The email is casefolded through `normaliseEmail` on the way in, for the reason
  `0005` records: the `(tenant_id, email)` index is byte-exact, so an address
  saved as typed would be a person `admit` — which normalises — could no longer
  find.
- A second person in the same business already holding that address is refused
  400 with a sentence, not a 500: the unique index is caught and named.
- 404 when the id names nobody in this business — the same non-oracle answer
  `/api/people/detail` gives.

**Consequence the operator should know about:** with `status` read-only there is
now no control anywhere in the UI that suspends a login. `POST /api/people/status`
and its `setPersonStatus` survive untouched as the server-side capability; only
the client function and the panel's binding to it are removed, because an unwired
control is the shape that reads as supported and is not.

## Test plan

`tests/test_UAT_FC_BUG-54_person_record.test.ts` (browser/jsdom, real
`mountFields`, panel driven through its real transport seam):

- the locked cells carry no editing affordance and clicking one opens no
  control; the two editable ones do
- confirming a name reaches the transport and the value shown is the saved one
- a malformed address is refused inline, the control stays open, and the
  transport is never called — one case per rejected shape above
- a well-formed address reaches the transport
- a transport rejection rolls the cell back to the last-known-good value and
  shows the message

`tests/test_UAT_FC_BUG-54_person_record.workers.test.ts` (workerd, real D1):

- the route writes `email` and `display_name` and reads them back
- an address that fails `isEmailShape` is refused 400 and the row is unchanged
- the address is casefolded on write
- a duplicate address inside one business is refused 400, and the same address
  in a *different* business is accepted
- an unknown id is 404; a caller who does not own the business is 403


## As landed

Commit `7f348d433ec25b01e622e1d8e9dc8fc3017d90b0`, version `0.2.79`.

`tests/test_UAT_FC_BUG-54_person_record.test.ts` — 16 cases, all passing.
`tests/test_UAT_FC_BUG-54_person_record.workers.test.ts` — 19 cases, all passing.
The seven adjacent people suites (REQ-170, REQ-186 ×2, REQ-188 ×2, REQ-189 ×2)
pass unchanged: 59 cases. The full suite's other failures are the environment's
missing knowledge index and reproduce identically on the unmodified checkout.

Four cases beyond the plan above, each covering a technical consequence of the
patch shape rather than something asked for directly:

- **an absent key leaves that field alone** — the panel commits one field at a
  time, so the route had to be a patch; a route that read the body as a whole
  record would write back whatever stale copy the pane was holding for every
  column it was not asked about.
- **the record of what happened is not writable by this route** — the server
  half of the locked fields. `status`, `invitedAt`, `termsAcceptedAt` and the
  stamps are all sent and all ignored, because locking the boxes is only the
  client half and the tab's three states are derived from those columns.
- **the same address in two businesses is two people** — the other side of the
  duplicate refusal. Identity is `(tenant_id, email)`, so a uniqueness check
  written across tenants would let one customer's contact list constrain
  another's.
- **an id belonging to another business is the same 404** — the half of the
  non-oracle rule that protects something: the caller's own gate passes, so the
  request reaches `setPersonRecord`, which scopes by tenant *and* id.

One behaviour discovered while writing the browser suite and left as it is:
moving focus out of an open control is the component's confirm gesture, so
clicking from Email to Name commits Email even when it is unchanged. That is
`webui-fields`' `auto` commit mode and predates this ticket; it is noted here
because the suite has to open one field per panel to avoid it.

---
uid: report-70d65976
id: REPORT-1748
type: report
title: Claude reconciliation_story_generation report
created_by: xgd
created_at: '2026-08-10T07:49:02.800593+00:00'
updated_at: '2026-08-10T07:49:02.800593+00:00'
completed_at: null
last_field_updated: created_at
fields:
  report_kind: reconciliation_story_generation
  subject_uid: bundle-e59210c5
  plan_item_index: '2'
---

All mutations applied and committed. No runtime code was touched; the working tree is clean.

**Scope**
- In scope for change: `story-3bf94bd4` (STORY-101) and its acceptance criteria.
- Out of scope, left alone: `story-e674c60a` and `story-37a3921b` (items 1 and 3 own those), AC-994 (its *evidence* moved from reading the dialog's text to reading the control's value — the criterion itself is unchanged, per REQ-121's own note), and every other existing AC on this story.

**One deviation from the plan's AC list, deliberate.** The plan proposed 7 added ACs; I created 8. The proposed "editing control reproduces the region's typography and paint stack" criterion bundles two separately-observable behaviours, each with its own FC evidence — reading the region's presentation and resolving the paint stack by what is under the pixel (`..._the_box_shows_the_copy_as_the_page_renders_it`, `..._an_images_fields_are_not_dressed_as_page_copy`), versus bringing the site's `@font-face` declarations across and *only* those, replaced per open (`..._the_pages_font_faces_are_copied_into_the_builder_document`, `..._only_font_faces_cross_and_a_reload_replaces_them`). Splitting them keeps each criterion singly falsifiable. No new capability — same surface, finer granularity.

**Two accuracy corrections against the plan's proposed wording**, both traced to the code:
- "no fallback literal is load-bearing" is not quite true: the refusal colour still carries a literal, because the shell has no token for it. The AC states that exception rather than over-claiming.
- The intent's "re-colours when the theme changes" is a consequence of being inside the themed subtree — there is no theme switcher in the builder itself. AC-1037 is phrased so a palette change reaching the form is the observable, not a UI control that does not exist.

Also recorded in Technical Context rather than as criteria (per the plan's "gaps the matrix should not claim closed"): the enum control's verbatim option text, the form's two-column layout leaving a lone field at half width, and the lone-field control being opened today by firing the component's own click-to-edit gesture pending the upstream seam. The headless-run limitation — nothing is laid out, so the mirrored background's *offset* is unverifiable there — is stated too, since AC-1040's offset clause is only fully provable in a real browser.

```
Upgrade mutations applied for plan item 2 of 8

Target Stories: story-3bf94bd4
Primary Story UID: story-3bf94bd4
Stories Modified: 1
ACs Modified: 0
ACs Added: 8
ACs Removed: 0

tickets_modified:
  stories:
    - "story-3bf94bd4"
  acceptance_criteria:
    modified: []
    added:
      - "acceptance_criterion-279f1f6d"   # AC-1037 themed subtree
      - "acceptance_criterion-4e320828"   # AC-1038 application typeface
      - "acceptance_criterion-fd4471a9"   # AC-1039 heading + label column
      - "acceptance_criterion-15ea0e87"   # AC-1040 typography + paint order
      - "acceptance_criterion-fc456d2d"   # AC-1041 font-face crossing
      - "acceptance_criterion-6a9ace26"   # AC-1042 size clamp
      - "acceptance_criterion-8acf277e"   # AC-1043 sized for copy, Save reachable
      - "acceptance_criterion-472674ff"   # AC-1044 lone field opens in its control
    removed: []

Progress: 2 of 8 plan items complete
```

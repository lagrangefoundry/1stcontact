---
uid: report-82341f04
id: REPORT-1002
type: report
title: 'Resync resolve conflicts: 006568bd9e49cc7323728e278446fbe1f1e9b96b'
created_by: xgd
created_at: '2026-07-27T21:37:41.656629+00:00'
updated_at: '2026-07-27T21:37:41.656629+00:00'
completed_at: null
last_field_updated: created_at
result: pass
fields:
  report_kind: resolve_conflicts
  subject_uid: report-510318c4
---

## Files resolved

- `package.json` — class **UU**, config file (rule 2g / enrichment "intent unknown").
  Sole conflict; sole conflicting hunk was the `"version"` scalar.
  Merge stages: base `0.0.207`, ours `0.0.213`, theirs `0.0.208`.
  **Resolved to `0.0.214`** — the incoming commit's *delta* (+1) applied to the
  rebased base, rather than either literal side.

  Deviation from the literal 2g "incoming wins" and from the enrichment's
  "more recent by timestamp", with cause — flagged for post-merge review:
  - Literal incoming (`0.0.208`) **regresses** the counter below main's own
    `0.0.212`, re-issuing version numbers already burnt on main and breaking
    the monotonic per-commit counter that `bin/project/xgd_version_bump` owns
    (root `package.json` `"version"` is its single source of truth).
  - Timestamp rule selects ours (`0.0.213`; ours 2026-07-27 vs theirs
    2026-07-25), which would drop a bump the incoming commit genuinely made
    and misrepresent a bumping commit as flat.
  - Precedent inside this same resync run is decisive and matches `0.0.214`:
    - `bb7bf22be` (working, bumped 206→207) → pick `34f0b80d6` bumped 212→213.
    - `f07bbe20f` (working, flat 207→207)   → pick `e444ced9d` stayed 213→213.
    i.e. established convention is preserve-the-delta on the new base. Incoming
    here bumps 207→208 (+1), so 213 + 1 = **0.0.214**.

## Incoming changes preserved

- `package.json`: resolved file diffed against `6634c8ddb:package.json` with the
  version line excluded — **identical**. Every incoming key/script/dependency is
  present; the version line is the only intentional divergence. Parses as valid
  JSON; zero conflict markers remain.
- All other 45 files in the incoming commit auto-merged without conflict. Verified
  exhaustively: for every path in `git show --name-only 6634c8ddb`, the resolved
  worktree content was diffed against the incoming commit — `package.json` was the
  **only** file reported as differing. The new files (`packages/framework/src/l2/contact-form.ts`,
  `packages/framework/src/modules/contact-form/controls.ts`, `tests/req96-control-composition.test.ts`)
  are staged as `A`.
- Net change vs HEAD is non-empty: 46 files, +2409/-483.

## Independent verification

Staged tree `0372398b9b0140d7ead1fa650da6b32a19cb1715` was built and tested:
`pnpm -r build` clean (`tsc --noEmit` across all packages), `pnpm test`
**821/821 tests, 116/116 files passing** — including the incoming commit's own
new `tests/req96-control-composition.test.ts`.

Note: 4 tests initially failed `ENOENT` on
`storage/references/gigabytealchemy.ai/index/multistate.json`. Environmental, not
the pick — `/storage/references/` is gitignored (`.gitignore:146`), so the 48M
capture bundle does not propagate into a fresh resync worktree. Two of the four
(`bug17-fold-padding`, `req91-l1-pixel-mover-axes`) are in files this commit does
not touch. Bundle was copied in from the primary checkout; all green thereafter.
The copy is untracked and correctly ignored — it does not appear in `git status`
and cannot enter the commit.

---
uid: report-9160675f
id: REPORT-3401
type: report
title: 'Reconcile resolve conflicts: reconcile-BUNDLE-23'
created_by: xgd
created_at: '2026-09-03T23:25:26.904033+00:00'
updated_at: '2026-09-03T23:25:26.904033+00:00'
completed_at: null
last_field_updated: created_at
result: pass
fields:
  report_kind: resolve_conflicts
  subject_uid: reconcile-BUNDLE-23
---

## Files resolved

- `package.json` — UU (config/bookkeeping scalar, 2g + enrichment rule).
  Sole conflict hunk was the `version` scalar: HEAD `0.2.20` vs incoming `0.2.19`.
  Resolved to HEAD's `0.2.20` via `git checkout --ours -- package.json`, then staged.

  Rationale (both sides are `free_coded`, so the STEP 2 exception applies — take the
  later working-timeline position):
  - Ours: `510d4082382c7cba29890523b815537de666e259` — "[FREE-CODED] REQ-162 — version 0.2.20",
    2026-08-31 14:41:02 -0700.
  - Theirs: `81ebc997d4d40854934f0a6742ae6393752a8694` — "Merge branch 'free-REQ-162' into
    xgd-working", 2026-08-31 14:39:25 -0700.
  - HEAD's commit is the later of the two (by 97s) and is the continuation of the *same*
    REQ-162 free-coded bump series, so it carries the developer's final version intent.
    The enrichment rule ("take the more recent commit by timestamp") gives the same answer.

  `git diff 81ebc99 HEAD -- package.json` confirms the `version` line is the *only*
  difference between the two sides of this file, so taking ours discards nothing else.

## Incoming changes preserved

Incoming's only change to a code file is the `version` scalar. Diffing the merge against
its first parent (`e57c378c94`) shows exactly one hunk, `package.json | 2 +-`
(version -> `0.2.19`). Its second-parent content (four `.xgd/tickets/hot/*.md` files)
auto-merged cleanly and was never in conflict.

That incoming change is **already integrated into HEAD**, not discarded:
`e0c632c535c9bfcb85da1cd930b1735dc936d01a` ("[FREE-CODED] REQ-162 — version 0.2.19",
2026-08-31 14:39:25 -0700) is confirmed an ancestor of HEAD via
`git merge-base --is-ancestor`. HEAD then advanced `0.2.19 -> 0.2.20` in `510d4082` two
minutes later. So this is STEP 3's "present via a different route" (redundant) case rather
than the "genuinely absent" (discarded) case — the guard is satisfied, and restoring
`0.2.19` would be a regression of the version scalar, not a preservation of developer code.

No hunks were dropped under the BUG-1301 precedence exception. No test files were touched;
no test function on either side was deleted.

## Staging state

`git status --porcelain` reports no conflict-class lines, and no conflict markers remain in
`package.json`. The staged tree nets to **no diff vs HEAD** (`git diff --cached HEAD` is
empty), which is the expected outcome for a commit whose effect already landed by another
route — per STEP 4 this is not a failure and `--skip` was deliberately NOT called. The
cherry-pick sequencer state is intact: `CHERRY_PICK_HEAD` still reads
`81ebc997d4d40854934f0a6742ae6393752a8694`, left for `cherry_pick_finalize_resolution`.

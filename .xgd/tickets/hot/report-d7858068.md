---
uid: report-d7858068
id: REPORT-4285
type: report
title: 'Reconcile resolve conflicts: reconcile-BUNDLE-27'
created_by: xgd
created_at: '2026-09-18T04:31:06.399449+00:00'
updated_at: '2026-09-18T04:31:06.399449+00:00'
completed_at: null
last_field_updated: created_at
result: pass
fields:
  report_kind: resolve_conflicts
  subject_uid: reconcile-BUNDLE-27
---

## Files resolved

- `.xgd/tickets/hot/request-a03967f2.md` — class **AA** (both added), out of the
  sparse-checkout cone. Rule **2e** (intent/bookkeeping ticket, `request-*`), superset
  branch. Resolved by taking the **ours/HEAD** side (`git checkout --ours` +
  `git add --sparse`).

  Both sides' markdown bodies are **byte-identical** (verified by diffing blob
  `8cd9648` against `b6f1c0d` — the only hunks are in the YAML frontmatter). The
  frontmatter differences make HEAD a strict superset of the incoming side:

  | field | ours (HEAD) | theirs (incoming `c5752ee`) |
  |---|---|---|
  | `updated_at` | `2026-08-31T14:22:29` | `2026-08-23T03:29:49` |
  | `completed_at` | `2026-08-31T14:22:29` | `null` |
  | `status` | `free_and_reconciled` | `ready_to_reconcile` |
  | `fields.chat_comment` | `comment-869ded75` | *(absent)* |
  | `fields.bundled_in` | `bundle-b3b7c399` | *(absent)* |

  There is no field, section, or paragraph present on the incoming side and absent
  from HEAD, so there was nothing to combine — 2e's "keep the superset" applies
  directly rather than the per-fact timeline rule. Taking the incoming side would
  have rewound an already-advanced lifecycle status (`free_and_reconciled` →
  `ready_to_reconcile`), cleared `completed_at`, and dropped the `chat_comment` and
  `bundled_in` links. The auto-enriched "more recent commit by timestamp" rule
  points the same way: ours is 8 days newer.

## Incoming changes preserved

- `.xgd/tickets/hot/request-a03967f2.md` — the incoming commit `c5752ee` is a pure
  add of all 164 lines of this file. Every line of that content is present in the
  resolved version: the body is identical verbatim, and each frontmatter key the
  incoming side carries is present in HEAD at an equal-or-later value. Nothing from
  the incoming side was dropped.

No hunks were dropped under the BUG-1301 precedence exception; no code or UAT files
were involved in this conflict.

## Note on the staged diff

This resolution nets to **no diff vs HEAD** — HEAD already carried the incoming
commit's content (plus later bookkeeping) through the earlier ticket-sync route.
Per STEP 4 this is the redundant-commit case, not the discarded-commit case
(STEP 3's check passes: the incoming content is *present* in HEAD, not absent).
`--skip` was not called; the cherry-pick sequencer state (`CHERRY_PICK_HEAD` =
`c5752ee5cc91b01c14e40da43c6e14cd233931bf`) is intact for
`cherry_pick_finalize_resolution`.

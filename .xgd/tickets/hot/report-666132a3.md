---
uid: report-666132a3
id: REPORT-1283
type: report
title: 'Capability-Intent Alignment: capability-modules (level=ac)'
created_by: xgd
created_at: '2026-08-05T18:15:42.717665+00:00'
updated_at: '2026-08-05T18:15:42.717665+00:00'
completed_at: null
last_field_updated: created_at
result: pass
fields:
  report_kind: capability_validation
  subject_uid: capability-ce902be4
  level: ac
  violations: 0
  warnings: 2
  needs_review_count: 0
---

# Capability-Intent Alignment: capability-modules
# Level: ac

**Result**: PASS
**Violations**: 0
**Warnings**: 2
**Needs review**: 0

## Headline

`capability-ce902be4` (CAP-72) holds **zero stories**, therefore **zero ACs**.
The AC-level check is vacuously satisfied: there is no AC in this capability's
tree that could be inconsistent with, fail to cover, or duplicate another.

This is the intended outcome of the structural rebalance earlier in this same
regression (`report-bdaf6840`, 2026-08-05), which absorbed CAP-72 into
**`capability-ae9d65d6`** (CAP-70, "Framework Substrate: L1 Layout, Values &
Behavior Modules"). The story-level cycle for this capability
(`report-248c0b33`, PASS, 0 violations) already established that the transfer
was lossless. Per the level cascade, that story-level result is my working
reference and I found no reason to reopen it.

The AC-level question that *is* live here is narrow and worth stating plainly:
**do not audit AC-697…AC-722 against this capability.** The ticket index says
they belong here. They do not. Details in warning 2.

## Verification Method

The ticket index on this branch worktree is unreliable (see warning 2), so I did
not trust `--filter fields.capability_uid=...`. I enumerated all 12 distinct
story UIDs in the store and read `capability_uid` from each **ticket** directly:

| Story | UID | Actual `capability_uid` |
|---|---|---|
| STORY-75 | story-d5de22a5 | capability-aa030c83 |
| STORY-76 | story-82eb6908 | capability-aa030c83 |
| STORY-77 | story-16f2793c | capability-aa030c83 |
| STORY-78 | story-2c7069fe | capability-aa030c83 |
| STORY-79 | story-e15a19ef | capability-aa030c83 |
| STORY-80 | story-c490f1cf | capability-ae9d65d6 |
| STORY-81 | story-3569e1a4 | capability-ae9d65d6 |
| STORY-82 | story-46e3b3c7 | capability-ae9d65d6 |
| STORY-83 | story-d0a8cfad | capability-ae9d65d6 |
| STORY-84 | story-8acc338d | capability-2049c9ec |
| STORY-85 | story-179b8c06 | **capability-ae9d65d6** |
| STORY-86 | story-24098299 | capability-2049c9ec |

**Zero of 12** resolve to `capability-ce902be4`. Since ACs reach a capability
only through their story, zero stories ⇒ zero ACs in scope. Confirmed
independently of the story-level report.

## Cumulative Intent Considered

Ledger carried forward from the story-level cycle (`report-248c0b33`); no intent
in it is contradicted by anything found at AC level.

| Intent ID | UID | Status | When | Asked / changed | Counts? |
|---|---|---|---|---|---|
| REQ-79 | request-87b26bca | free_and_reconciled | 2026-07-19 | Framework pivot: L1 substrate + capability modules (safety envelope) | YES |
| REQ-84 | request-f243b6b9 | free_and_reconciled | 2026-07-20 | Pivot C: strip layout modules to L1; retires "module = bundle of aesthetic dials" | YES (retired prior model) |
| REQ-85 | request-015e42ac | free_and_reconciled | 2026-07-20 | Pivot D: module contract (config/slots/conformance) + reframe carousel & contact-form — founding intent for this capability | YES |
| REQ-87 | request-84af044b | free_and_reconciled | 2026-07-21 | Mechanical rename `capability module` → **behavior module**; no back-compat alias | YES |
| BUNDLE-7 | bundle-31e474b9 | free_and_reconciled | 2026-07-22 | Reconciliation vehicle for REQ-63/79/82/83/84 +2; STORY-85's `intent_uid` | YES |
| REQ-96 | request-3a064234 | ready_to_reconcile | 2026-07-26 | Behavior modules layout-agnostic by construction (L1 `control` node; module ships zero CSS) | imminent — scope now under capability-ae9d65d6 |

**Coverage consequence at AC level.** Every reconciled intent above is now
expressed through STORY-85's nine ACs, which sit under `capability-ae9d65d6`.
None is orphaned; none is expressible here, because this capability has no story
to hang an AC from. There is no `ac-add` repair available or appropriate against
this subject.

## Alignment Ledger

| Element | Intents aligned to | Outcome |
|---|---|---|
| capability-ce902be4 (AC tree) | — | **Empty by design.** Zero stories ⇒ zero ACs. Vacuously consistent, complete, and exclusive. |
| AC-697 … AC-704, AC-722 (9 ACs, all `active`) | REQ-79, REQ-84, REQ-85, REQ-87, BUNDLE-7 | **Out of subject scope.** Attached to STORY-85 (story-179b8c06), whose capability is `capability-ae9d65d6`. Their AC-level alignment is owned by CAP-70's pass, not this one. Listed here only so a reader who sees them via the stale index knows why they are excluded. |

## Findings

| # | Severity | Property | Element | Resolution category | Issue | Suggested edit |
|---|---|---|---|---|---|---|
| 1 | warning | consistency | capability-ce902be4 | *(none — system defect)* | Capability remains `status: active` with `uat_coverage: pass` while holding zero stories and carrying `merged_into: capability-ae9d65d6`. `uat_coverage: pass` is now vacuous — it attests to a tree that no longer exists. Deprecation was **blocked, not skipped**: `reject_deprecation_if_capability_has_stories` → `attached_story_ids()` reads the canonical main store, which still holds pre-merge `capability_uid` values, so it reports phantom attached stories (`report-bdaf6840`). Unchanged since the story-level pass. | No matrix edit. Flip `status: deprecated` in a later run once the index resolves correctly — an xgd-repo fix, not a matrix repair. |
| 2 | warning | consistency | capability-ce902be4 (index) | *(none — system defect)* | **AC-level manifestation of the index defect.** `xgd ticket list --type story --filter fields.capability_uid=capability-ce902be4` returns STORY-85 (`updated_at 2026-07-24`) while the ticket itself reads `capability-ae9d65d6` / `2026-08-05`. A naive AC-level pass that trusts this filter would pull STORY-85's **nine ACs** (AC-697, AC-698, AC-699, AC-700, AC-701, AC-702, AC-703, AC-704, AC-722) into this check — auditing them against a capability that does not own them, and double-auditing them alongside CAP-70's own AC pass. Same root defect duplicates the story list (21 index entries for 12 distinct stories) and breaks human-ID resolution outright (`xgd ticket get STORY-85` → "not found"; UID lookup succeeds). | None here. Any re-run of an AC-level check on this branch must resolve stories via per-ticket UID reads, as this report did — never via filtered lists or human IDs. |

No violations. No `needs_review`: the intent ledger is explicit at every point
where this capability's AC-level state could have looked ambiguous, and the
empty AC tree is a documented, verified consequence of a sanctioned rebalance.

## Notes for the Editor

**Do not "fix" the empty AC tree.** There is no `ac-add` work here. The
tempting repair — authoring ACs against CAP-72 because the index shows it owning
a story, or re-attaching STORY-85 — would undo a verified rebalance and
reintroduce the imbalance it corrected. The ticket file is authoritative; the
index entry is stale.

**Both warnings are one xgd-repo defect, already filed.** On a branch worktree
the ticket index resolves to the canonical main store and serves pre-merge
`capability_uid` values. `xgd ticket rebuild-index` refuses on branch worktrees
by design, so this cannot be cleared from here. `report-bdaf6840` already raised
it (with the `.xgd/uat_index.json` all-zero-counts defect) against
`/Users/martin/lagrangefoundry/xgd`. No matrix edit repairs either; none should
be attempted.

**Handoff to the CAP-70 AC-level pass.** Two items belong there, not here:

1. **REQ-96 coverage gap** (raised as warning 3 at story level, scoped to
   `capability-ae9d65d6`). REQ-96 is `ready_to_reconcile` — live but not yet
   enforced. No story in the store mentions `control`, `layout-agnostic`, or
   REQ-96. When it reconciles, it needs AC expression under CAP-70.
2. **STORY-85 "Out of scope" cross-references are stale post-rebalance** — the
   body still routes by pre-rebalance topology ("the L1 substrate itself
   (STORY-83 / CAP-70)"), which now reads as self-referential since STORY-85
   itself lives in CAP-70. Cosmetic, `story-body-edit`, and likely recurring
   across all absorbed stories — worth one sweep rather than per-story repair.

**The REQ-87 rename is settled — don't re-open it at AC level.** Two deliberate
non-changes recorded on STORY-85 read like incomplete renames and must not be
"completed": the emitted asset filename stays `capabilities.js` (a plural
bundle-output name, not a type or discriminant — renaming it breaks the page
reference), and the English-word uses of "capability" in the capture layer
(driver capability negotiation) and site schema ("schema-only capability") are
correct English, not the renamed type. AC-722 encodes the rename contract
correctly as-is.

---
uid: report-1926412e
id: REPORT-4395
type: report
title: 'Reconcile resolve conflicts: reconcile-BUNDLE-27'
created_by: xgd
created_at: '2026-09-19T11:07:46.157912+00:00'
updated_at: '2026-09-19T11:07:46.157912+00:00'
completed_at: null
last_field_updated: created_at
result: pass
fields:
  report_kind: resolve_conflicts
  subject_uid: reconcile-BUNDLE-27
---

## Files resolved

- `.xgd/tickets/hot/request-01ea4eec.md` — **UU**, out-of-sparse-cone (conflict existed in the index only; no working-tree markers). Rule **2e** (intent/bookkeeping ticket — `request-*`). Resolved to the **ours/HEAD** side: `git checkout --ours` materialized it (verified `git hash-object` == stage-2 blob `2e26c89eae`, byte-for-byte, no conflict markers), then `git add --sparse`.

  Incoming commit is `1596690add716ab427c00f9c1dfd014714ef25c7` ("xgd(ticket): update request request-01ea4eec", Sep 1 11:58:00). Stage-1 base is `bb33d558` — the theirs-blob of the immediately preceding attempt (`e0795d93`, scope 117/0), which finalized as a no-op skip; ours is unchanged at `2e26c89eae`.

  The incoming commit's own base→theirs delta is **two bookkeeping lines only**:

  | fact | base (`bb33d558`) | incoming (`8bbbe467`) | ours (HEAD `2e26c89e`) | taken |
  |---|---|---|---|---|
  | `fields.story_points` | `13` | `13` *(unchanged — diff context)* | `13` *(identical)* | either — same value |
  | `updated_at` | `2026-09-01T18:57:55` | `2026-09-01T18:57:59` | `2026-09-16T01:48:29` | ours (later) |
  | `last_field_updated` | `status` | `story_points` | `status` | ours (later per-fact timeline) |
  | `status` | `free_coded` | `free_coded` | `bundled` | ours (later lifecycle) |
  | `completed_at` | `null` | `null` | `2026-09-14T10:29:15` | ours (superset) |
  | `fields.bundled_in` | absent | absent | `bundle-8e1807f6` | ours (superset) |

  Ours is a **strict superset** of incoming, so 2e's superset clause governs. Note the commit is named for `story_points` but does **not change its value** — `story_points: 13` is unchanged context in the incoming diff and identical in ours, so the developer re-wrote the same value; the commit's entire recorded delta is the derived audit-trail field plus the timestamp.

  `last_field_updated` is the only genuinely competing fact, and the per-fact timeline rule selects ours: ours' value `status` is the consistent companion of ours' own later `status: bundled` write (2026-09-16). Taking incoming's `story_points` would assert that story_points was the last field touched, when `status` was in fact changed 15 days afterwards — i.e. it would leave the audit field contradicting the very record it describes, and would additionally drop `bundled_in`, `completed_at`, and regress operator/workflow-owned `status` from `bundled` to `free_coded`. The body is byte-identical on both sides (the ours-vs-theirs diff has frontmatter hunks only), so no prose content was at stake. No field was invented; nothing was taken that is not on one side.

## Incoming changes preserved

The only substantive fact this commit records — `fields.story_points: 13` — is present **verbatim** in the resolved file and in HEAD, confirmed against `git show HEAD:.xgd/tickets/hot/request-01ea4eec.md` (line 14: `story_points: 13`). HEAD also still carries the prior attempt's facts intact: `working_sha: ab467d6ce366…`, `version: 0.2.32`, `bundled_in: bundle-8e1807f6`.

The two facts not carried forward are `updated_at` and `last_field_updated`, both superseded by HEAD's later writes. These are **present via a different route**, not discarded: the free_coded→bundled progression that produced HEAD subsumes this commit's bookkeeping. Per STEP 3/STEP 4 this is the redundant-commit case, not an @fail.

No hunk was dropped under the BUG-1301 precedence exception; no code or UAT test files were involved in this conflict.

## Net staged diff

`git diff --cached --stat HEAD` is empty — the resolution nets to **no diff vs HEAD**, because the bundling that produced HEAD already subsumes this commit's effect. Per STEP 4 (BUG-1109/BUG-1122) this is not a failure and `--skip` was **not** called. `CHERRY_PICK_HEAD` remains intact at `1596690add716ab427c00f9c1dfd014714ef25c7` for `cherry_pick_finalize_resolution`, which will detect the clean staged diff and skip the commit.

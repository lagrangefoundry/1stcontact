---
uid: report-99345907
id: REPORT-3235
type: report
title: 'Reconcile resolve conflicts: reconcile-REQ-162'
created_by: xgd
created_at: '2026-09-01T22:23:36.273533+00:00'
updated_at: '2026-09-01T22:23:36.273533+00:00'
completed_at: null
last_field_updated: created_at
result: pass
fields:
  report_kind: resolve_conflicts
  subject_uid: reconcile-REQ-162
---

## Files resolved

- `.xgd/tickets/hot/doc-2b24930a.md` (DOC-15, "Crawler & Framework Coverage Program") — class **AA** (both added; sparse-excluded, staged with `git add --sparse`). Rule applied: **2b / 2e per-fact judgment** with incoming authoritative.

  Both sides carry byte-identical document bodies (§1–§9). The only divergence is two frontmatter lines:

  - `updated_at`: ours `2026-08-16T01:21:19` vs incoming `2026-08-31T19:42:57`
  - `fields.system_kb: true` present on ours, removed on incoming

  The incoming commit `897281d4a8` ("xgd(ticket): update doc doc-2b24930a") states its operation explicitly: *"field: retire system_kb boolean; membership moves to doc_kind (DOC-39 3.3)"*. That is a deliberate, later-positioned free_coded edit to exactly the one fact in dispute. The ours-side commit `4d15ff54` (2026-08-15) is the file's creation on this branch carrying `system_kb: true`; it asserts no competing intent on any other fact. Resolution: take incoming for both lines. `fields.doc_kind: architecture` is preserved on both sides, so the "membership moves to doc_kind" migration is intact.

  No third fact differed, so nothing from the ours side was discarded beyond the field the incoming commit was authored to retire.

## Incoming changes preserved

- `.xgd/tickets/hot/doc-2b24930a.md` — the resolved working-tree file is **byte-identical to the incoming blob** (`c86174b5b5`, verified by `diff` against `git show 897281d4a8:<path>`; zero conflict markers remain). The incoming commit's entire content is therefore present verbatim.
- Staged diff vs HEAD is 1 insertion / 2 deletions — precisely the `updated_at` bump and the `system_kb: true` removal, i.e. the incoming commit's stated intent and nothing else.

No hunks were dropped; the BUG-1301 precedence exception was not invoked. No code, test, or UAT files were involved in this conflict.

---
uid: report-639897f6
id: REPORT-3756
type: report
title: 'Fix Site Delivery: Deploy & Public Serving (story) — attempt 5'
created_by: xgd
created_at: '2026-09-10T16:06:58.985254+00:00'
updated_at: '2026-09-10T16:06:58.985254+00:00'
completed_at: null
last_field_updated: created_at
fields:
  report_kind: fix_structural_validation
  subject_uid: capability-a12e557f
  level: story
  fixes_applied: 5
  progress_made: true
  needs_more_work: false
  violations_remaining: 0
  anchor_report_uid: report-e37a6b4a
---

# Fix Summary — Site Delivery: Deploy & Public Serving (story)

**Attempt**: 5
**Fixes applied this call**: 5
**Violations remaining**: 0
**Needs more work**: false

REPORT-e3756541's two findings share one root cause: STORY-96 was the only story
in CAP-82 that BUNDLE-20's reconciliation never touched, so it still asserts the
sha-addressed draft snapshot channel REQ-149 D7 dropped. All repairs below are
subtractive — REQ-149 removed a channel, not the extensionless → `.html` rule —
and none weakens REQ-113 AC5–AC9.

## Actions Taken — by Resolution Category

| # | Category | Element | Action |
|---|---|---|---|
| 1 | story-body-edit | STORY-96 (`story-66115f6b`) | Finding 1: in-scope bullet 1 now reads "…in the local preview server and on the deployed site's **published addressing form**, and for full and header-only requests alike" — the "both the preview (snapshot-addressed) and published addressing forms" clause is gone. Finding 2: in-scope bullet 5 "the snapshot root's redirect" → "the published site root's redirect"; Technical Context bullet 1 and the Dependencies entry "addressing forms" → "addressing form". Added a Technical Context bullet, **"A second addressing form was retired, recorded rather than absorbed"**, citing REQ-149 D7 (channel dropped, not ported), D6 (`1c deploy`, its only producer) and D5 (the manifest that indexed it) — same "recorded rather than absorbed" style the story already uses for the corrected REQ-113 premise. Everything else preserved verbatim. |
| 2 | ac-edit | AC-916 (`acceptance_criterion-4d7804f8`) | Named in the report's Notes as the cascade one level down. Title "…serves the page on both addressing forms and for header-only requests" → "…serves the page, for full and header-only requests". Criterion "under a snapshot-addressed preview URL, under a published site URL" → "under the published site URL". Verification "Deploy a rendered multi-page site, both as a preview snapshot and as a published revision… on each addressing form" → "Publish a rendered multi-page site… on the published site" — the old instruction was no longer executable, `1c deploy` being deleted. |
| 3 | ac-edit | AC-921 (`acceptance_criterion-f336d8a0`) | Also named in the Notes. "the snapshot root" → "the published site root" in both title and criterion; verification "Deploy a rendered site… directly under the snapshot root" → "Publish a rendered site… directly under the published site root". The redirect being cited survives (`routes.ts:135-137`); it guards the published site root. |
| 4 | ac-edit | AC-920 (`acceptance_criterion-4464d7be`) | Not named in the findings table; found by sweeping all nine of STORY-96's ACs for the same retired premise. Verification said "in local preview and on **both deployed addressing forms**" → "on the deployed site's published addressing form". |
| 5 | ac-edit | AC-918 (`acceptance_criterion-c216e5e2`) | Same sweep. Verification said "against a deployed **snapshot the site's deploy index vouches for**" (the manifest REQ-149 D5 deleted) → "against a published site on the deployed side"; the seeded-nested-object note now says "into the published revision" rather than "into that snapshot". |

A re-scan of STORY-96 and all nine ACs for `snapshot` / `addressing forms` /
`deploy index` / `1c deploy` returns zero residual hits outside the story's
explicit record of the retirement. AC-915, AC-917, AC-919, AC-922 and AC-923
were checked and carry no retired premise — their "deployed site" wording is
channel-neutral and correct.

## Verification

`npm test -- tests/req113-worker-extensionless-urls.test.ts
tests/req113-serve-extensionless.test.ts tests/reconciliation-clean-page-urls.test.ts`
→ **3 files, 18 tests, all passing.** (Wrangler emits an EPERM warning writing its
own debug log under the sandbox; unrelated to the run.) As REPORT-e3756541 noted,
the UATs were already rebuilt for the one-channel world under REQ-149 over
`tests/fixtures/published-site.ts`; the matrix text was what lagged, and this pass
closes that gap. No `uat_coverage` field was touched — all nine ACs remain `pass`,
which the tests confirm.

Code cross-checks that anchored the wording: `apps/public-site/src/routes.ts:13-22`
(one channel, `draft` is an ordinary segment again), `parseRoute` at `:112-142`
(admits only apex/redirect/asset/not-found), `:29-40` (the `redirect` route guards
the *site* root against document-relative asset resolution one level too high).

## Code Edits (if any)

None this call. No production code was touched — every repair was a matrix edit.

## needs_review Items Forwarded

None. Findings 3 and 4 were info-only ("no action") and were left alone;
finding 4's claim was re-verified — `tools/generate/src/cli/serve.ts:73` still
carries the stale Cloudflare Pages comment the story documents as drift.

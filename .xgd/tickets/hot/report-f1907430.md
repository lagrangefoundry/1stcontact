---
uid: report-f1907430
id: REPORT-2980
type: report
title: 'Reconcile resolve conflicts: reconcile-BUNDLE-21'
created_by: xgd
created_at: '2026-08-31T15:51:56.642416+00:00'
updated_at: '2026-08-31T15:51:56.642416+00:00'
completed_at: null
last_field_updated: created_at
result: pass
fields:
  report_kind: resolve_conflicts
  subject_uid: reconcile-BUNDLE-21
---

## Files resolved

- `.xgd/tickets/hot/bug-db356ff8.md` — UU, intent/bookkeeping ticket (rule 2e),
  resolved outside the sparse-checkout cone (`git checkout --ours` +
  `git add --sparse`). HEAD kept: it is a per-fact superset of the incoming side,
  every incoming fact being either byte-identical or superseded by a later value
  of the same field.

  Incoming commit `c1d2a2f` ("xgd(ticket): update bug bug-db356ff8", 5 insertions
  / 4 deletions) is frontmatter-only. Fact-by-fact against ours (`3e66931`):

  | Fact | Incoming (`92fad09`) | Ours (`3e66931`) | Resolution |
  |---|---|---|---|
  | `last_field_updated` | `body → status` | `status` | identical — incoming applied |
  | `fields.story_points` | added `3` | `3` | identical — incoming applied |
  | EOF newline | removed | absent | identical — incoming applied |
  | `status` | `draft → free_coding` | `bundled` | same field, later position — ours |
  | `updated_at` | `2026-08-24T01:48:29Z` | `2026-08-26T17:36:27Z` | same field, later — ours |

  The two superseded facts are the same ticket further along one lifecycle:
  `bundled` is the downstream state of `free_coding`, and HEAD additionally
  carries `fields.commits[working_sha ea48502…]`, `fields.version: 0.2.10` and
  `fields.bundled_in: bundle-78f4e2fe` — the bundling record that produced that
  state. Taking `free_coding` back would rewind the ticket past its own bundling.
  Per 2e's per-fact timeline rule, HEAD's later-positioned value wins for those
  two facts; nothing from the incoming side is discarded.

## Incoming changes preserved

This is a bookkeeping ticket, not a code file — no implementation hunks are in
play. All three of the incoming commit's non-timestamp content changes
(`last_field_updated: status`, `fields.story_points: 3`, no trailing newline) are
present verbatim in the resolved version; the remaining two (`status`,
`updated_at`) are the same fields advanced further by HEAD, per the table above.

No hunk was dropped under the BUG-1301 precedence exception; none applied.

Net result: the staged tree has no diff vs HEAD, because HEAD's
`seed_local_overlay` state already reflects this commit's effect and more. Per
STEP 4 this is not a failure and `--skip` was not called — the staged state is
left for `cherry_pick_finalize_resolution`. STEP 3's discard guard is satisfied:
the incoming commit's changes are present in HEAD, not absent. (This is the
second consecutive commit against this ticket to resolve this way; the prior one,
`5af1ff9`, carried the 112-line body addition that HEAD already contained —
REPORT-2979.)

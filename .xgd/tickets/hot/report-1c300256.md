---
uid: report-1c300256
id: REPORT-3674
type: report
title: 'UAT Coverage: Platform Build, Deploy & Live-Origin Verification'
created_by: xgd
created_at: '2026-09-10T05:46:38.010009+00:00'
updated_at: '2026-09-10T05:46:38.010009+00:00'
completed_at: null
last_field_updated: created_at
result: pass
fields:
  report_kind: uat_coverage_check
  subject_uid: capability-5d07b533
  violations: 0
  warnings: 0
  needs_review_count: 0
---

# UAT Coverage Assessment: Platform Build, Deploy & Live-Origin Verification

**Result**: PASS
**AC verdicts**: 18 pass, 0 fail, 0 deprecated, 0 needs_review
**Story verdicts**: 1 pass, 0 fail, 0 stale, 0 needs_review
**Capability verdict**: pass

## Cumulative Intent Considered

CAP-102 holds one story, STORY-119 (`story-d5167ced`, upgrade), whose
`intent_uid` is BUNDLE-19 and whose `updated_by` is BUNDLE-21. The story's own
Technical Context and Reconciliation Decisions name BUNDLE-20 as the middle
round. Walking the three bundles to their source intents:

| Intent ID | Status | When | Asked / changed | Counts? |
|---|---|---|---|---|
| REQ-144 (in BUNDLE-19) | free_and_reconciled | 2026-08-15 | `bin/build`, `bin/deploy`, `bin/smoke`; the hook seam; the piped-secret mechanism; the `[vars]` non-inheritance rule behind the production failure | YES |
| REQ-145 (in BUNDLE-20) | free_and_reconciled | 2026-08-15 | control-app becomes the builder: `1c assets` generates the importmap/chrome the Worker's own source imports, ahead of the typecheck; introduces `ACCESS_DEV_OPEN`, the local-development relaxation | YES |
| REQ-147 (in BUNDLE-20) | free_and_reconciled | 2026-08-15 | Access on `app.1stcontact.io`; the operator surface must be challenged, and the `workers.dev` default hostname must not answer | YES |
| REQ-149 (in BUNDLE-20) | free_and_reconciled | 2026-08-17 | Publish in the cloud; the type-only import edge that broke `bin/build`; the live exercise of the `ANTHROPIC_API_KEY` secret hook | YES |
| BUG-36 (in BUNDLE-21) | free_and_reconciled | 2026-08-23 | Fresh-deployment 503; supersedes REQ-144's "production has been 503ing" premise (recorded in the story's Technical Context) | YES |
| BUG-37 (in BUNDLE-21) | free_and_reconciled | 2026-08-24 | Error 1102 on the operator surface; adds `[observability]` retention, unsampled, and its placement rule | YES |
| BUG-38 (in BUNDLE-21) | free_and_reconciled | 2026-08-24 | Builder chat conversation lifetime — no criterion here | YES (no effect) |

No later intent retires anything in this capability. The newest tickets touching
the tree (REQ-154 `bundled`, BUG-39 `bundled`, REQ-155…REQ-166 `draft`) are
Browser Rendering, chat-host and knowledge-base work; none of them changes the
build, deploy, smoke or deployment-configuration surface. Nothing here is
deprecated and no AC describes a retired behavior.

## Alignment Ledger

| Story | Intents aligned to | Outcome | Notes |
|---|---|---|---|
| STORY-119 | REQ-144, REQ-145, REQ-147, REQ-149, BUG-36, BUG-37 | aligned | Every behavior the story body states maps to a reconciled intent. The seven places intent was silent are each recorded under `## Reconciliation Decisions` (2026-08-31 for BUNDLE-20 and BUNDLE-21, with decision 4 corrected 2026-09-09) — check ownership (AC-1425), the "nothing skipped" widening (AC-1336), the stated exception (AC-1341), the type-program refusal's placement and its corrected outcome (AC-1426), retention ownership (AC-1454), retention's invisibility to the binding count (AC-1455), and AC-1341's restated scope. Per the reconciliation-decided rule these are resolved, not gaps. |

## AC-by-AC Coverage

Every AC in this capability has exactly one UAT, in one of three files, and the
1:1 mapping holds across the pair of reconciliation rounds plus BUG-37's:

| AC | UAT | Boundary the test actually uses |
|---|---|---|
| AC-1330 | `test_UAT_AC1330_reports_every_component_and_package_then_refuses_naming_the_absent_one` | the real `1c preflight` binary, with `Module._resolveFilename` hooked so one shared component is genuinely unresolvable; asserts the real `EXIT_CODES.ENVIRONMENT` |
| AC-1331 | `test_UAT_AC1331_build_bundles_every_discovered_worker_against_production_after_preflight` | the real `bin/build` against the real tree, `pnpm`/`npx` recorded on PATH; asserts stage order, `--env production` on every bundle, exit-code propagation, `--skip-preflight`, and refusal of an empty tree |
| AC-1332 | `test_UAT_AC1332_rehearsal_runs_the_same_hooks_and_composes_the_same_invocation` | real `bin/deploy`, twice, diffing the recorded hook lines and the composed `wrangler deploy` line |
| AC-1333 | `test_UAT_AC1333_executable_hooks_run_sorted_before_the_upload_with_the_deploy_context` | real `bin/deploy`; sort order, migrate-before-secrets, the six context variables, the non-executable `NOTES.md` never running, and the real `README.md` mode bits |
| AC-1334 | `test_UAT_AC1334_a_failing_hook_aborts_that_app_before_anything_is_uploaded` | real `bin/deploy` on both paths; asserts the upload shim recorded nothing |
| AC-1335 | `test_UAT_AC1335_targets_default_to_every_discovered_app_and_an_unknown_one_is_refused` | real `bin/deploy`; default/named/unknown targets, `--env` with and without a value |
| AC-1336 | `test_UAT_AC1336_every_applicable_check_passes_and_each_skip_is_named_with_a_zero_exit` | the real `smoke.mjs` **process**, transport replaced beneath it; asserts all nine PASS by name, exactly two skips named, and the summary line |
| AC-1337 | `test_UAT_AC1337_each_breakage_fails_naming_the_check_and_what_it_expected` | `runSmoke` per breakage plus one real-process run; all six breakages, each failing exactly its owning check with a non-empty detail |
| AC-1338 | `test_UAT_AC1338_missing_inputs_are_reported_skipped_with_the_reason_and_counted` | `runSmoke` + `formatReport`; skip reasons name the missing option, counts stay separate, exit stays zero |
| AC-1339 | `test_UAT_AC1339_same_origin_assets_are_checked_including_one_level_into_stylesheets` | real `referencedAssets`/`referencedFromCss`/`runSmoke`; exclusions, the font reached through the stylesheet, the bare page, the `--max-assets` bound, and the content-type table pinned against `contentTypeFor` from the serving Worker |
| AC-1340 | `test_UAT_AC1340_unpublished_and_unknown_answer_identically_and_a_difference_fails` | `runSmoke`; body leak, status leak, and the published "nothing to compare" pass |
| AC-1341 | `test_UAT_AC1341_named_environments_repeat_top_level_vars_and_bindings_found_structurally` | the real `apps/*/wrangler.toml` for every discovered app, plus the exact shipped-and-broken form as a negative control; the invented binding kind proves structural identification; `DEV_ONLY_VAR` proves the exception is one variable wide |
| AC-1342 | `test_UAT_AC1342_no_credential_shape_is_committed_and_the_documented_push_echoes_only_the_name` | scans the real files, then **runs** `bin/deploy.d/secrets/10-anthropic-api-key` on both paths with `npx` recorded — asserts the raw stdin has no trailing newline and the value never appears in output |
| AC-1425 | `test_UAT_AC1425_each_control_surface_check_passes_fails_and_skips_on_its_own_option` | `runSmoke` for the three protected forms (302 to the IdP, 401, the Worker's own 503) with the default hostname made unresolvable, plus two real-process failure runs and a both-options-absent skip run |
| AC-1426 | `test_UAT_AC1426_a_type_only_reach_to_the_filesystem_fails_the_build_and_this_walk_names_the_chain` | the **real `tsc`** over the real `apps/control-app/tsconfig.json`, plus a one-specifier-apart fixture pair; the walk's non-vacuity is asserted (four known modules reached, one recorded edge shown to be `import type`) |
| AC-1427 | `test_UAT_AC1427_the_generation_stage_runs_before_the_typecheck_that_consumes_it` | `git ls-files`/`check-ignore` for the uncommitted artifacts, real `tsc` before and after generating, then real `bin/build` asserting `1c preflight → 1c assets → pnpm -r build → wrangler` in what ran, not only in what printed |
| AC-1454 | `test_UAT_AC1454_retention_is_declared_unsampled_for_both_environments_and_the_route_survives` | the real `wrangler.toml` parsed by table, with the hoisted-retention form as a negative control that shows `routes` migrating out of `[env.production]` |
| AC-1455 | `test_UAT_AC1455_retention_is_invisible_to_the_environment_repetition_binding_count` | asserted through `parseWranglerConfig` — the reader the AC-1341 check itself uses — as a set relation between levels, never an enumerated snapshot; negative control proves the reader is not blind to unfamiliar tables |

No test in this set is trivial, over-mocked or structural. Where a file's text is
read (`bin/build`'s documented exit codes, the generated-module import
specifiers, the secret README's wording) it is a supporting assertion beside a
behavioral one that runs the real binary; in AC-1342's case the prose assertion
is explicitly paired with executing the hook the prose points at, and the test
says why.

## Story-level judgment (independent of the AC roll-up)

Read against the story body rather than against the AC list, every behavioral
promise has an owner: the four-stage build and its refusals (AC-1330, AC-1331,
AC-1427), the refusal a bundle graph cannot see (AC-1426), one deploy path with
seams instead of knowledge (AC-1332, AC-1333, AC-1334, AC-1335), the eleven
live-origin checks with their pass/fail/skip vocabulary (AC-1336, AC-1337,
AC-1338, AC-1339, AC-1340, AC-1425), the configuration rule and its one stated
exception (AC-1341), the invocation-log record and its placement (AC-1454,
AC-1455), and the secret mechanism (AC-1342). Two claims that read as
free-floating prose are in fact pinned: "the check engine is drivable against a
supplied origin" is the seam every smoke UAT uses, and "each of the two
inheritable repeats is pinned by the criterion that owns it" is discharged for
the platform-default-hostname control inside AC-1341's own verification and for
retention by AC-1454/AC-1455.

## Execution spot-check

Coverage judgment is about what the tests assert, but two of the three files were
run to confirm they are live rather than nominal (all three are in the `node`
project's `tests/**/*.test.ts` include):

- `tests/reconciliation-platform-invocation-log-retention.test.ts` — 2 passed.
- `tests/reconciliation-platform-build-order-and-private-surface.test.ts` — 3 passed.

`tests/reconciliation-platform-build-deploy-smoke.test.ts` was **not** run here:
AC-1330 and AC-1331 drive the real preflight, and the shared component store
(`@lagrangefoundry/*`) is absent from this worktree and from every parent — it is
populated out of band and the sandbox cannot write the shared location. A red
result from that file in this checkout would be the environment artifact the
preflight exists to report, not a coverage fact, so it is recorded here rather
than being run and misread.

## Findings — Categorized by Editor Action

None. No violations, no warnings, no needs_review.

## Notes for the Editor

Nothing to fix. Three points worth keeping visible for the next round:

1. **`.xgd/uat_index.json` is empty** (`acs: {}`, 0 entries) while 18 UATs for
   this capability exist on disk and are correctly named `test_UAT_AC<n>_…`. The
   index is not a reliable instrument for coverage lookup at the moment — this
   assessment located every test by grep instead. That is an index-build defect,
   not a matrix defect, so it is not filed as a finding against CAP-102, but any
   later stage that trusts the index will read this capability as having zero
   coverage.
2. **AC-1426's 2026-09-09 correction has landed consistently.** The AC body now
   states the build's outcome as "fails naming the offending module" and
   explicitly disclaims printing an import chain; the UAT asserts exactly that
   (`refused.all` contains `node:fs`) and attributes the chain to the test-side
   instrument `tests/support/type-program.ts`. Story decision 4 and the AC agree.
   No residual drift.
3. **The shared component store's absence is a standing worktree condition.** It
   affects only the two build UATs that exercise the preflight for real. If a
   later fix or validation stage reports those two red, check
   `node_modules/@lagrangefoundry` before treating it as a coverage or
   implementation regression.

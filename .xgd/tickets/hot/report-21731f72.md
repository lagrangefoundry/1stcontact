---
uid: report-21731f72
id: REPORT-3036
type: report
title: 'Reconcile resolve conflicts: reconcile-BUNDLE-22'
created_by: xgd
created_at: '2026-08-31T19:42:35.693608+00:00'
updated_at: '2026-08-31T19:42:35.693608+00:00'
completed_at: null
last_field_updated: created_at
result: pass
fields:
  report_kind: resolve_conflicts
  subject_uid: reconcile-BUNDLE-22
---

## Files resolved

- `package.json` — class UU, config/scalar conflict on the `version` field only.
  Ours (HEAD, bundle branch) `0.2.14` vs theirs (incoming free_coded
  `31a4ca7da5`) `0.2.4`. Resolved to **`0.2.14`** (HEAD side).

  Rule applied: the auto-enrichment's per-file rule for this conflict —
  "take the more recent commit by timestamp." HEAD-side commits are
  2026-08-31; the incoming commit is 2026-08-22, so HEAD is later.
  The incoming hunk is a pure bookkeeping bump `0.2.3 -> 0.2.4`, not code
  intent; HEAD has since advanced independently to `0.2.14`, so taking
  incoming would regress the version by ten patch releases while
  preserving nothing the developer actually authored. Flagged for
  post-merge review as the enrichment rule directs.

  No other conflicted paths: the commit's three code files merged without
  conflict (see below).

## Incoming changes preserved

The incoming commit `31a4ca7da5` ("feat(schema): reserve locale-shaped page
slugs [FREE-CODED]") touches four files. Verified per file:

- `packages/site-schema/src/locale.ts`
- `packages/site-schema/src/schema.ts`
- `tests/test_UAT_FC_REQ-153_locale_slug_reservation.test.ts`

  All three are **byte-identical between HEAD and the incoming commit** —
  `git diff HEAD 31a4ca7da5 -- <the three paths>` returns empty. The
  developer's code is fully present in the tree: `isLocaleShapedSlug` is
  defined at `packages/site-schema/src/locale.ts:348` and consumed at
  `packages/site-schema/src/schema.ts:9,561`, and the UAT file is present
  and unmodified. No test function from either side was dropped.

  These landed in HEAD ahead of this pick via `6852ca1359`, which carries
  the identical subject line. That is why they produced no conflict.

- `package.json` — the only hunk is the version bump described above,
  deliberately not taken. No code content is carried by this hunk.

STEP 3 verdict: **not a discard.** The incoming commit's key changes are
present in HEAD via a different route, which STEP 4 (BUG-1109/BUG-1122)
identifies as a genuinely redundant commit rather than a discarded one.
Accordingly the staged diff is empty (`git diff --cached --stat` returns
no files); staged and exiting @done as instructed, leaving the skip
decision to Python's finalize step. `git cherry-pick --continue/--skip/
--quit/--abort` was not called and CHERRY_PICK_HEAD remains present.

No hunk was dropped under the BUG-1301 precedence exception; it did not
apply here.

---
uid: report-93025780
id: REPORT-2940
type: report
title: 'Reconcile resolve conflicts: reconcile-BUNDLE-21'
created_by: xgd
created_at: '2026-08-31T14:30:18.669890+00:00'
updated_at: '2026-08-31T14:30:18.669890+00:00'
completed_at: null
last_field_updated: created_at
result: pass
fields:
  report_kind: resolve_conflicts
  subject_uid: reconcile-BUNDLE-21
---

## Files resolved

- `package.json` — UU, config/bookkeeping scalar (2g + the per-file rule
  "take the more recent commit by timestamp"). HEAD `0.2.9` (bump commit
  07afe0840d, 2026-08-23 15:10) vs incoming `0.1.60` (b8b01ebf26,
  2026-08-19 18:03). HEAD's commit is the later of the two and its value is
  the higher one; taking incoming would walk the version backwards. Kept
  `0.2.9`. Flagged for post-merge review per the enrichment rule.

- `tests/reconciliation-builder-workspace-origin.test.ts` — UU, code file,
  single conflicting hunk and it is comment-only (the `sources` array's
  BOTH-SOURCES rationale at ~line 269). Both sides are `free_coded`, so the
  2c exception applies: later working-timeline position wins. HEAD side is
  30abfebebd "feat(publish): mint revisions in the cloud; D1 is the only
  record" (2026-08-20 17:06), later than incoming b8b01ebf26 (2026-08-19
  18:03). Kept HEAD's text — see below for why this preserves rather than
  discards the incoming edit.

## Incoming changes preserved

The cherry-pick nets to an empty staged diff (`git diff --cached --stat
HEAD` is empty). This is the redundant case, not the discarded case: each
of the incoming commit's four files was checked for its key changes in the
resolved tree, and all are present.

- `tests/test_UAT_FC_REQ-146_worker_ai_boundary.test.ts` (merged clean) —
  `import { WEBUI_SCOPE } from '../tools/generate/src/cli/webui'` present at
  line 9; the forbidden-import regex is composed from it at line 273
  (`new RegExp(\`from\\\\s+['"]${WEBUI_SCOPE}/ai['"]\`)`). No test function
  was deleted on either side of this conflict.

- `tools/generate/src/cli/assets.ts` (merged clean) — the scope literal is
  gone from all three comment sites; a repo-wide grep for the literal across
  the three touched source/test files returns nothing.

- `tests/reconciliation-builder-workspace-origin.test.ts` — the incoming
  commit's second hunk (removal of the `{ route: '/api/ai/', url:
  '/api/ai/roles', ok: true }` prefix-route entry, replaced by the
  explanatory comment) merged clean and is present at lines 435-440; the
  entry itself is gone. The first hunk conflicted, and HEAD's text is a
  successor of incoming's, not a competitor: it carries incoming's own
  replacement clause verbatim ("still serves its own copy of the assistant
  routes", which diverges from the merge base's "retains only what no Worker
  can host yet — `/api/ai/*` and the publish pair") and then further corrects
  the publish sentence for REQ-149, which had moved revisions to the store
  port after the incoming commit was written. Incoming's edit is therefore
  contained in the resolution; only its now-stale claim that publish is the
  transport's exclusive capability is superseded.

- `package.json` — the incoming change is a version-bump scalar only, no
  code. HEAD is ahead of it. Nothing developer-authored is lost.

No hunks were dropped under the BUG-1301 precedence exception; it did not
need to be invoked.

---
uid: report-6788c794
id: REPORT-4179
type: report
title: 'Reconcile resolve conflicts: reconcile-BUNDLE-27'
created_by: xgd
created_at: '2026-09-13T23:50:44.012982+00:00'
updated_at: '2026-09-13T23:50:44.012982+00:00'
completed_at: null
last_field_updated: created_at
result: pass
fields:
  report_kind: resolve_conflicts
  subject_uid: reconcile-BUNDLE-27
---

## Files resolved

- `.xgd/tickets/hot/bug-3ade1af4.md` — UU, sparse-excluded (index-only, no
  working-tree markers). Rule 2e (intent/bookkeeping ticket), strict-superset
  branch. Resolved with `git checkout --ours --ignore-skip-worktree-bits` +
  `git add --sparse`.

  - Base (`:1`): BUG-40, title `Untitled`, status `draft`, fields =
    {auto_merge_back, needs_review, priority}, body `(new ticket)`.
  - Incoming (`:3`, commit 0170f868 `xgd(ticket): update bug bug-3ade1af4`):
    base + one line, `chat_comment: comment-94979ef7`.
  - Ours (`:2`, seed_local_overlay): the fully developed ticket — real title,
    status `bundled`, `last_field_updated: status`, plus fields `severity`,
    `story_points`, `commits[]`, `version`, `bundled_in: bundle-8e1807f6`, the
    full Symptom / Cause 1-3 / Test plan body — **and** the same
    `chat_comment: comment-94979ef7` line the incoming commit adds.

  Ours is a strict superset of theirs: every fact on the incoming side is
  already present, with nothing on the incoming side contradicting a field ours
  changed. No per-fact timeline tiebreak was needed — there is no fact the two
  sides set differently. No content was invented; no intent_uid/story_uid/
  capability_uid field was touched.

## Incoming changes preserved

- `.xgd/tickets/hot/bug-3ade1af4.md`: the incoming commit's sole hunk is
  `+  chat_comment: comment-94979ef7`. Verified present in the staged blob
  (`git show :.xgd/tickets/hot/bug-3ade1af4.md` line 17). Nothing from the
  incoming diff is absent.

No hunks were dropped; the BUG-1301 precedence exception was not invoked.

## Note for the finalize step

The staged tree is identical to HEAD (`git status --porcelain` is empty), because
the seeded local overlay already carried the incoming field — the redundant-commit
case of BUG-1109/BUG-1122, not a discard. STEP 3's check distinguishes them: the
incoming change is present in HEAD via the overlay, not missing. Per STEP 4 no
`--skip` was issued; the finalize step should detect the empty staged diff and
skip the commit. The cherry-pick sequencer state (CHERRY_PICK_HEAD =
0170f868e2be7f6994b94a9254662bdf269c3424) is untouched and still present.

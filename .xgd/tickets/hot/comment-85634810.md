---
uid: comment-85634810
id: COMMENT-2526
type: comment
title: Claude response for fix_structural_validation report
created_by: xgd
created_at: '2026-09-10T12:03:05.094248+00:00'
updated_at: '2026-09-10T12:03:05.094248+00:00'
completed_at: null
last_field_updated: created_at
status: null
fields:
  subject_uid: report-accb91dc
  kind: note
---

All 5 violations and the 1 warning from `report-d3e53eaf` are resolved in this call — 8 mutations, no production code touched. Completion report: **REPORT-3727** (`report-accb91dc`), `progress_made: true`, `needs_more_work: false`, `violations_remaining: 0`.

**Applied as the assessor's two clusters:**

*Findings 1 + 5 (STORY-82, together):*
- **AC-718** rewritten to the REQ-96 shape — required `form` slot, `control` leaf per field, invariant `label`/`honeypot`/`turnstile`, and `labelMode` as the captured a11y fact that delivers the "compact placeholder-labelled" promise. Framed as the *reproduction-treatment* claim with an explicit deferral to STORY-85 AC-701, per the exclusivity warning. Un-deprecated: `status → active`, `uat_coverage → pass`, non-schema `lifecycle` field removed.
- **AC-719** and **STORY-82's body** (both occurrences): "named overlay role" → palette reference, with a sub-bullet recording the REQ-114 supersession.
- Cleared the stale `pending` status on both ACs, as the report's editor note suggested.

*Findings 2/3/4 (three `ac-add`s for REQ-93's unminted criteria):*
- **AC-1622** (STORY-83) — mounted-fragment emission, both states, and the carve-out's two preconditions as its own scope.
- **AC-1623** (STORY-85) — page-level binding: five rejected shapes with error paths, two legal states including the orphan seam.
- **AC-1624** (STORY-85) — `mountInL1` runs the same five dimensions, in a host that cannot itself overflow.

*Finding 6:* **AC-1144**'s duplicated shade↔alpha composition bullet and its verification sentence replaced with a cross-reference to AC-930.

Plus one UAT edit: AC-718's new `labelMode` clause would have outrun its evidence, so I extended `test_UAT_AC718_*` to cover it. Passes (2/2), `tsc --noEmit` clean on the file.

One thing forwarded rather than decided: the three new ACs were created with `uat_coverage` unset. Their evidence exists and passes at `tests/req93-l1-slot-mounted-behaviors.test.ts:154/349/415`, but under `test_UAT_FC_REQ-93_*` names, and each new AC states one clause those tests don't yet reach. Whether to rename (dropping the intent binding) or add AC-named tests alongside is a uat-level call, and that field belongs to the uat-coverage pair — the report names the specific gaps.

---
uid: report-03555399
id: REPORT-2457
type: report
title: 'Capability-Intent Alignment: Platform Build, Deploy & Live-Origin Verification
  (level=story)'
created_by: xgd
created_at: '2026-08-20T15:09:31.297066+00:00'
updated_at: '2026-08-20T15:09:31.297066+00:00'
completed_at: null
last_field_updated: created_at
result: pass
fields:
  report_kind: capability_validation
  subject_uid: capability-5d07b533
  level: story
  violations: 0
  warnings: 1
  needs_review_count: 0
---

# Capability-Intent Alignment: Platform Build, Deploy & Live-Origin Verification
# Level: story

**Result**: PASS
**Violations**: 0
**Warnings**: 1
**Needs review**: 0

Anchor report: report-2485c83c · Capability: capability-5d07b533 (CAP-102) · Previous attempts: 0

## Cumulative Intent Considered

CAP-102 carries no `fields.intent_uid` (normal here — only 1 of 31 capabilities does).
The tree's single story, STORY-119 (`story-d5167ced`), carries
`intent_uid = bundle-77b28def` (BUNDLE-19, `free_and_reconciled`,
`merged_at_commit b18b859d7414a049be45e09f48426d73742e5bf2`) and no `updated_by`.
Of BUNDLE-19's nine source tickets, exactly one addresses this capability:
**REQ-144** (`request-7bef34e0`). The other eight (REQ-133, BUG-35, REQ-131,
REQ-140, REQ-139, REQ-123, REQ-141, REQ-142) are palette/editor/KB/store work and
touch nothing here.

A full scan of every `request`/`bug` created on or after 2026-08-10 (22 tickets) for
`bin/build` | `bin/deploy` | `bin/smoke` | `1c preflight` | `env.production` |
`smoke.mjs` found five with hits. REQ-144 is the only reconciled one; the other four
are imminent and are recorded as such.

| Intent ID | Status | When | Asked / changed | Counts? |
|---|---|---|---|---|
| REQ-144 | free_and_reconciled | 2026-08-15 | `bin/build` (preflight → `pnpm -r build` → per-Worker bundle against `[env.production]`); `bin/deploy` with `--dry-run` as a *target* and migrate/secret hook seams; `bin/smoke` — nine live-origin checks; the `[vars]`/binding inheritance rule + structural guard; a documented, never-committed secret mechanism | **YES** — sole originating intent |
| REQ-147 | reconciling | 2026-08-15 | Cloudflare Access on the builder; adds `--control-origin` / `--workers-dev-origin` to `smoke.mjs`, widens `test_UAT_FC_REQ-144_smoke_passes_against_a_correct_origin`, restates `workers_dev`/ACCESS vars on both sides of the inheritance line | imminent — **not on `main`** (see Findings #2) |
| REQ-145 | ready_to_reconcile | 2026-08-15 | control-app becomes the builder; `bin/build` gains a `1c assets` stage; introduces `ACCESS_DEV_OPEN`, a var that must **not** be restated under `[env.production.vars]` | imminent — a deliberate exception to AC-1341's blanket rule |
| REQ-143 | ready_to_reconcile | 2026-08-15 | D1/R2 SiteStore; lands `bin/deploy.d/migrate/10-d1-site-store` into REQ-144's hook seam; `bin/deploy` itself unchanged, "as REQ-144 intended" | imminent — consumes the seam, does not change it |
| REQ-146 | ready_to_reconcile | 2026-08-15 | AI host into workerd; lands `bin/deploy.d/secrets/10-anthropic-api-key` into REQ-144's secret hook seam | imminent — consumes the seam, does not change it |
| REQ-141 | bundled | 2026-08-15 | workerd test project; reuses the apps' `wrangler.toml` compat settings | imminent — no ask on this capability's surface |
| REQ-134 | abandoned | 2026-08-12 | image-generation component | NO — retired, and unrelated |

Chronology is flat: every relevant intent is dated 2026-08-15, and REQ-145/146/147
each `depends_on` REQ-144 rather than superseding it. No reconciled intent retires
any REQ-144 behaviour, so the cumulative intent for this capability today **is**
REQ-144.

## Alignment Ledger

| Element | Intents aligned to | Outcome |
|---|---|---|
| CAP-102 (`capability-5d07b533`) | REQ-144 (via STORY-119) | aligned — body's three promises (a build that refuses, a rehearsal on the real code path with hooks as seams, a live-origin check that names its failure) map 1:1 onto REQ-144 §3. Boundary claim vs CAP-82 verified: `capability-a12e557f` is indeed "Site Delivery: Deploy & Public Serving". |
| STORY-119 (`story-d5167ced`, feature, completed) | REQ-144 | aligned, with one warning (#1) and three imminent-intent notes (#2–#4) |

Every substantive claim in STORY-119's body was checked against REQ-144 **and**
against the shipped code on `main` (this worktree is 487 commits behind `main`, so
the REQ-144 files are absent here; they were read via `git show main:<path>`):

| Story claim | Verified against |
|---|---|
| Preflight reports every shared component + declared package, refuses with an environment-specific exit code and a named remedy | `tools/generate/src/cli/shared-store.ts` — `CommandError` `ENVIRONMENT` → exit 6; remedy `cd ../lagrange-framework && bin/install --lang js --component all` spelled once |
| Three build stages in order, each Worker bundled against the production environment | `bin/build` — `1c preflight` → `pnpm -r build` → `wrangler deploy --env production --dry-run --outdir dist` |
| Apps discovered from the tree, not listed | `bin/build` `find apps -mindepth 2 -maxdepth 2 -name wrangler.toml`; `bin/deploy` likewise |
| Rehearsal is a *target*: same hooks, same command line, one flag appended | `bin/deploy` — `cmd=(npx wrangler deploy --env "$env_name")` composed once, `cmd+=(--dry-run)` |
| Hooks: executable-only, sorted, before the upload, six context variables | `bin/deploy:run_hooks` — `[[ -f && -x ]]`, glob order, `DEPLOY_APP/_APP_DIR/_ENV/_WORKER_NAME/_DRY_RUN/_REPO_ROOT`, invoked before `cmd` |
| Unknown app refused, listing those that exist | `bin/deploy` — `unknown app '$want' (have: ...)` |
| **Nine** HTTP checks | `tools/generate/bin/smoke.mjs` — `apex_resolves`, `unknown_slug_not_found`, `unpublished_slug_indistinguishable`, `published_root_redirects`, `draft_root_redirects`, `draft_index_serves_html`, `draft_cache_and_robots_policy`, `draft_miss_is_noindex_404`, `draft_assets_resolve` — exactly nine, and no `--control-origin` yet |
| Skips are counted and never read as success | `formatReport` counts `pass/fail/skip` separately; `--slug`/`--draft` absent → `skip(name, 'no --slug given')` |
| Secret mechanism documented, never committed | REQ-144 §Implementation + `bin/deploy.d/secrets/README.md` (present on `main`) |

Every one of REQ-144's six numbered ACs is expressed in the tree: §4.1+§4.2 →
AC-1341; §4.3 → AC-1330 + AC-1331; §4.4 → AC-1336; §4.5 → AC-1337 (with AC-1338,
AC-1339, AC-1340 refining the skip / asset / leak sub-cases); §4.6 → AC-1342. The
hook-seam and target-selection deliverables of §3 are covered by AC-1332 through
AC-1335. No REQ-144 ask is unexpressed.

## Findings

| # | Severity | Property | Element | Resolution category | Issue | Suggested edit |
|---|---|---|---|---|---|---|
| 1 | warning | coverage | STORY-119 (and CAP-102 body) | story-body-edit | `bin/build --skip-preflight` is shipped behaviour recorded by REQ-144 (`free_and_reconciled`) — its Outstanding section: *"`bin/build --skip-preflight` exists for that case, but CI was left alone rather than half-wired"* — and confirmed on `main` (`bin/build` arg loop, `skip_preflight=1`, documented in `--help`). It appears nowhere in the matrix: not in the story body, not in the In-scope list, not in AC-1330 or AC-1331. The story's central promise ("a build that refuses before it emits a broken artifact") and AC-1331's *"after the preflight passes"* both read as absolute while a documented bypass exists. | Add one clause to the story's build paragraph or its first In-scope bullet recording the documented bypass and its sole purpose (an environment — CI — where the out-of-band shared store is absent), so the "refuses" guarantee is qualified where the code qualifies it. Note AC-1331's "after the preflight passes" may want the same qualification at `ac` level. |
| 2 | info | coverage | STORY-119 vs REQ-147 (`reconciling`) | — | REQ-147 adds two control-app checks (`--control-origin`, `--workers-dev-origin`) to `smoke.mjs` and widens `test_UAT_FC_REQ-144_smoke_passes_against_a_correct_origin`. Verified **not yet on `main`**: `smoke.mjs` there has nine checks and its usage string offers only `--origin/--slug/--draft/--max-assets`. The matrix's "nine checks" (story body, AC-1336) is therefore currently **correct**. | none now — do **not** pre-edit. When REQ-147 reconciles, "nine" becomes stale in the story body and in AC-1336, and REQ-147's reconciliation must either widen them or place the two control-app checks under its own capability. |
| 3 | info | consistency | STORY-119 / AC-1341 vs REQ-145 (`ready_to_reconcile`) | — | REQ-145 introduces `ACCESS_DEV_OPEN`, a var deliberately **absent** from `[env.production.vars]`, guarded by a UAT that fails the build if anyone restates it. That is a principled exception to the story's *"checked property of **every** Worker in the tree"* and to AC-1341. Not yet enforced — `main` does not carry it. | none now. Flagged so REQ-145's reconciliation is expected to qualify the rule (rule + declared exception) rather than silently weaken AC-1341. |
| 4 | info | coverage | STORY-119 vs REQ-143 / REQ-146 (`ready_to_reconcile`) | — | Both land a file into REQ-144's hook directories (`migrate/10-d1-site-store`, `secrets/10-anthropic-api-key`) and REQ-143 states `bin/deploy` itself is unchanged *"as REQ-144 intended"*. The story's Out-of-scope bullet ("The migration and secret hooks themselves") is exactly right and needs no change. | none — the seam held; this is the ledger entry proving it. |
| 5 | info | exclusivity | STORY-119 vs STORY-95 (`story-d34eccd8`, CAP-82) | — | The only other story in the matrix mentioning smoke/deploy. No overlap: STORY-95 owns the *serving rules* (and mentions smoke once, as carried-forward uncertainty about a live-bucket run); STORY-119 owns the *check that asserts them*. The story body declares the one deliberate duplication — the content-type table restated outside the Worker bundle — and pins it to `contentTypeFor`. | none — documented duplication, not drift. |
| 6 | info | consistency | STORY-119 Technical Context | — | The story records that REQ-144's premise ("returning 503 since it was deployed") was false on the facts — the hostname NXDOMAINs and the Worker was never deployed — and that neither is fixed here. This matches REQ-144's own Findings section and REQ-147's "latent rather than live". Intent-correcting text that is *supported by* the intent's reconciled body is alignment, not drift. | none |

## Notes for the Editor

**Do not pre-edit for the imminent intents (#2, #3, #4).** REQ-143, REQ-145,
REQ-146 (`ready_to_reconcile`) and REQ-147 (`reconciling`) all `depends_on`
REQ-144 and all extend this surface, but none of their changes are on `main`.
Editing the story now to describe eleven smoke checks or an `ACCESS_DEV_OPEN`
carve-out would replace backward drift with *forward* drift and would make
AC-1336 and AC-1341 wrong against the code that actually ships. Finding #1 is the
only item actionable today, because it derives from a `free_and_reconciled`
intent and shipped code.

**Where the code was read.** This worktree (`regression-cb0dad9c`) is 487 commits
behind `main` and predates BUNDLE-19's merge, so `bin/build`, `bin/deploy`,
`bin/smoke`, `bin/deploy.d/` and `tools/generate/bin/smoke.mjs` are absent from it
(`git merge-base --is-ancestor b18b859d… HEAD` → false; → `main` → true). Every
code check above was made with `git show main:<path>`. No `code-issue` finding is
raised, and none would be safe to raise from this worktree.

**Traceability observation, not a finding.** CAP-102 has no `fields.intent_uid`
(1 of 31 capabilities has one) and none of AC-1330…AC-1342 carries `intent_uid`
or `updated_by`. The chain to REQ-144 therefore runs solely through STORY-119's
`intent_uid`. This is the repo's prevailing shape rather than drift in this
capability, but it means a future check on this tree has exactly one link to
follow and no redundancy if it breaks.

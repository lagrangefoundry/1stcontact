---
uid: report-86b87907
id: REPORT-3342
type: report
title: 'Reconcile resolve conflicts: reconcile-BUNDLE-23'
created_by: xgd
created_at: '2026-09-02T20:10:42.197466+00:00'
updated_at: '2026-09-02T20:10:42.197466+00:00'
completed_at: null
last_field_updated: created_at
result: pass
fields:
  report_kind: resolve_conflicts
  subject_uid: reconcile-BUNDLE-23
---

## Files resolved

- `.xgd/tickets/hot/bug-6612c4b7.md` — UU, intent/bookkeeping ticket (STEP 2 §2e).
  Resolved per-fact toward HEAD (`git checkout --ours` + `git add --sparse`);
  all three conflict hunks are cases where HEAD holds the strictly later state
  of the same fact.

  Incoming commit: `fe97d3bc34` "xgd(ticket): update bug bug-6612c4b7"
  (2026-08-24T21:06:15Z) — a title edit on BUG-37.

  Hunk-by-hunk:
  1. `updated_at` / `completed_at` / `last_field_updated` / `status`:
     incoming = 2026-08-24T21:06:15Z, `status: draft`; HEAD =
     2026-08-31T19:19:36Z, `status: free_and_reconciled` with `completed_at`
     set. Same fields, HEAD later on the working timeline — HEAD kept.
  2. `fields.*`: incoming adds a transient `fields.title` mirror of the new
     title; HEAD has `chat_comment`, `commits[]`, `version: 0.2.13`,
     `bundled_in: bundle-78f4e2fe` and no `fields.title`. `xgd ticket history
     bug-6612c4b7` shows the same working timeline wrote `fields.title` in
     `28b2974007` (2026-08-24 14:06:15, the operation this cherry-pick is
     replaying) and *removed* it 15 seconds later in `a9021e4749`
     (2026-08-24 14:06:30). That removal, and every later field, are already in
     HEAD. Re-adding `fields.title` would resurrect a scratch field its own
     author already cleared — HEAD kept.
  3. Body prose: incoming still carries the pre-work `## Not started —
     Diagnosis only. No branch cut, no code written.` section plus the original
     "leading hypothesis" narrative; HEAD carries the completed root-cause,
     fix, result and observability write-up, and has deleted `## Not started`
     as no longer true. HEAD kept. (The residual diff in the final paragraph is
     line rewrapping only, same sentence on both sides.)

## Incoming changes preserved

The incoming commit's sole substantive change is the ticket title:
`'control-app: Edit mode 503s with Cloudflare 1102 …'` →
`'control-app: Edit mode dies with Cloudflare 1102 …'`.

That change **is present in the resolved file** — HEAD's top-level `title:`
field (lines 5-6, outside every conflict region, so unconflicted) already reads
`Edit mode dies`, because the working-timeline commits that followed
`fe97d3bc34` were already merged into HEAD by an earlier sync. Verified against
`git show fe97d3bc34 -- .xgd/tickets/hot/bug-6612c4b7.md`: the `+` title line
appears verbatim in the resolved file.

No code/implementation files were conflicted, so no BUG-1301 precedence
exception was invoked and no UAT test function was touched.

Net effect: the staged tree is byte-identical to HEAD (`git diff --cached` is
empty). This is the BUG-1109/BUG-1122 redundant-commit case, not a discard —
STEP 3's distinguishing test passes, since the incoming commit's key change is
present in HEAD via a later commit on the same timeline rather than merely
absent. Per STEP 4 the resolution is staged and left for
`cherry_pick_finalize_resolution` to skip; `CHERRY_PICK_HEAD`
(`fe97d3bc344f6b637416ce69b5e6043fe3759e10`) is intact and no sequencer command
was run.

---
uid: report-735105b6
id: REPORT-4348
type: report
title: 'Reconcile resolve conflicts: reconcile-BUNDLE-27'
created_by: xgd
created_at: '2026-09-18T08:07:15.430681+00:00'
updated_at: '2026-09-18T08:07:15.430681+00:00'
completed_at: null
last_field_updated: created_at
result: pass
fields:
  report_kind: resolve_conflicts
  subject_uid: reconcile-BUNDLE-27
---

## Files resolved

- `.xgd/tickets/hot/bug-23d1ec27.md` — **UU**, out-of-cone (sparse), rule **2e**
  (intent/bookkeeping ticket `bug-*`). Resolved by **composing both sides**, not by
  picking a winner's whole file.

  Incoming commit: `5a68fc8` "xgd(ticket): update bug bug-23d1ec27" (2026-08-26
  16:21 -0700), 91 insertions / 95 deletions. Stages: base `ad25504`, ours
  `52bab41`, theirs `df68f1c`.

  Note this is a *different* commit from the one resolved at attempt 70/0
  (`bffb6b3`, frontmatter-only, redundant). `ad25504` — that commit's tree — is
  this attempt's merge base, so the two attempts are sequential, not a retry loop.

  The two sides touched **disjoint sections**, so 2e's "non-overlapping
  fields/sections changed on each side: apply BOTH" governs:

  | fact | base `ad25504` | incoming `df68f1c` | ours `52bab41` | changed by | kept |
  |---|---|---|---|---|---|
  | body (`## Symptom` → EOF) | original hard-wrapped | **reformatted** | unchanged from base | incoming only | **incoming** |
  | `status` | `ready_to_reconcile` | unchanged | `bundled` | ours only | **ours** |
  | `fields.bundled_in` | absent | absent | `bundle-8eef3846` | ours only | **ours** |
  | `updated_at` | 2026-08-26T18:31 | 2026-08-26T23:21 | 2026-08-31T05:05 | both | **ours** (later) |
  | `last_field_updated` | `status` | `body` | `status` | both | **ours** (pairs with the kept `updated_at`) |
  | trailing newline at EOF | absent | absent | present | ours only | **ours** |

  `updated_at` / `last_field_updated` are a single fact — "what did the most recent
  write touch". Ours is the later write (2026-08-31, the status advance to
  `bundled`), so its pair is kept; taking incoming's `last_field_updated: body`
  alongside ours' 08-31 timestamp would have described a write that never happened.

  Mechanics: `git cat-file blob df68f1c > <path>` (incoming body byte-exact), then
  four frontmatter edits re-applying ours' facts, then
  `git add --sparse -- <path>` (path is outside the sparse cone — the conflict
  existed only in the index, with no working-tree file at all).

  Verified: `git diff` of the resolution against **incoming** shows exactly the four
  ours-side frontmatter facts and nothing else — the body is byte-identical to
  incoming. `git diff` against **ours** starts at line 27 (`## Symptom`) — the
  frontmatter is byte-identical to ours. Nothing was invented and no field was
  hand-authored beyond re-applying values already present on one side.

## Incoming changes preserved

No code/implementation files were in this conflict; the single conflicted path is a
bookkeeping ticket.

STEP 3 check: `5a68fc8`'s entire substantive change is the ticket body
(`last_field_updated: body`), and that body is present in the resolution byte for
byte. Nothing from the incoming commit was dropped. No BUG-1301 precedence
exception was invoked.

The staged tree has a real diff vs HEAD (93 insertions / 97 deletions), so this is a
normal commit, not the redundant-pick case. `CHERRY_PICK_HEAD` (`5a68fc8`) is left
intact; no `--continue` / `--skip` / `--abort` was issued.

## Flagged for post-merge review

Per the auto-enrichment rule ("intent unknown on one or both sides… flag this file
for post-merge review"), one observation on the incoming body edit. It changes no
words — every sentence is identical — but it degrades the markdown, in a pattern
consistent with a round-trip through a rich-text editor:

- The `| suite | before | after |` table under "Fix — as landed" is flattened into
  27 loose paragraphs, one per cell, losing the row/column association between each
  suite and its before/after state.
- The ` ```ts ` fence under "Root cause" loses its `ts` language tag, and a blank
  line is inserted before each closing fence.
- `**One double, in **\`tests/support/scripted-model-client.ts\`` — the bold close
  marker lands inside the phrase rather than around it.
- Hard-wrapped paragraphs are unwrapped to single long lines, and blank lines are
  inserted between every list item.

I applied it as authored: it is the developer's only change on this commit, ours
never touched the body, and rejecting it would have discarded the incoming commit
in its entirety (the @fail condition in STEP 3). But a human may want to restore
the table, since the flattened form is materially harder to read than what it
replaced.

---
uid: report-10f40264
id: REPORT-3767
type: report
title: 'Fix Structured Copy Editing: One Validated, Atomic Write Path (ac) — attempt
  4'
created_by: xgd
created_at: '2026-09-10T17:31:35.191709+00:00'
updated_at: '2026-09-10T17:31:35.191709+00:00'
completed_at: null
last_field_updated: created_at
fields:
  report_kind: fix_structural_validation
  subject_uid: capability-f753cecd
  level: ac
  fixes_applied: 9
  progress_made: true
  needs_more_work: true
  violations_remaining: 0
  anchor_report_uid: report-e37a6b4a
---

# Fix Summary — Structured Copy Editing: One Validated, Atomic Write Path (ac)

**Attempt**: 4
**Fixes applied this call**: 9 (8 AC bodies + 1 UAT extension)
**Violations remaining**: 0
**Needs more work**: true (warnings 5, 6, 7, 9, 10 and the AC-1273 judgement call remain)

All three violations (findings 1, 2, 3) are repaired, together with the whole of
warning 4 — the fourth-cycle origin-parity backlog — and warning 8, which
finding 2 required be taken in the same pass.

## Actions Taken — by Resolution Category

| # | Category | Element | Action |
|---|---|---|---|
| 1 | ac-edit | AC-1117 (`acceptance_criterion-0c85504b`) | **Finding 1.** Struck "Not its colour" from the closing sentence (family and geometry kept out of scope); added one positional sentence placing the typography controls after the colour row without restating AC-1269's claims; verification now asserts the words, then colour, then size/weight/italic/capitalisation; deleted "Assert no colour and", keeping "no family field is offered"; "four control shapes" → **five**. |
| 2 | ac-edit | AC-992 (`acceptance_criterion-9561711e`) | **Finding 2.** Third bullet restated as the origin observable: the save writes the draft and replies, with no render step in between, because the origin produces both channels from the definition when each is next requested; "before reporting success" deleted. Verification now fetches both channels **from the origin** and asserts new value present / old absent. Reading-parity and client-fault bullets unchanged. |
| 3 | ac-edit | AC-1026 (`acceptance_criterion-d4bc1184`) | **Finding 3 + warnings 4 and 5, one pass.** Origin sentence and its "both rendered channels on disk" verification step dropped entirely — AC-992 is now sole owner of origin parity for both region kinds. Second paragraph reduced to the half AC-983 does not cover: the single operation reports both fields as changed; the one-diff claim left to AC-983. |
| 4 | ac-edit | AC-982 (`acceptance_criterion-99f7c64d`) | **Warning 8**, sequenced after finding 2 so the "both channels" claim is asserted in exactly one place. Criterion now claims the command-line save re-renders **both** the editable and the plain draft rendering and reports where each was written, with the reason (an edit changes the page, not one rendering of it). Verification asserts both paths reported and both outputs carrying the new words. |
| 5 | ac-edit | AC-1024 (`acceptance_criterion-8b6792de`) | **Warning 4.** Dropped "Assert the same answer is returned when the region is read through the builder origin". |
| 6 | ac-edit | AC-1045 (`acceptance_criterion-8a3c8c3e`) | **Warning 4.** Same origin clause dropped. Everything else preserved verbatim — the every-painted-panel paragraph and its second-panel verification step are warning 9's business, deliberately left intact so no claim is left unverified mid-repair. |
| 7 | ac-edit | AC-1048 (`acceptance_criterion-3c28fccd`) | **Warning 4.** Dropped "Assert the refusal is reported identically from the command line and through the builder origin". |
| 8 | ac-edit | AC-1111 (`acceptance_criterion-285dd8d6`) | **Warning 4.** Dropped the origin sentence from the verification; the criterion's cross-reader clause reworded from "whether it reads it from the command line or over the builder's origin" to a property of the answer rather than of whichever client reads it — the same treatment the report recommends for AC-1273. |
| 9 | uat-edit | `test_UAT_AC982_saving_new_words_updates_the_draft_and_re_renders_the_page` | Warning 8 widened AC-982, so its UAT was extended in the same call rather than left as a fresh coverage gap: it now asserts `renderedDraft` is reported and that the plain draft rendering carries the new words and not the old. Passes. |

## Verification

| Command | Result |
|---|---|
| `npm test -- tests/reconciliation-copy-edit-write-path.test.ts tests/reconciliation-copy-edit-image-selection.test.ts` | 2 files, **22 passed** |
| `npm test -- tests/reconciliation-copy-edit-typography.test.ts` | 1 file, **9 passed** |

Evidence the rewritten prose now matches the shipped surface:

- `copyFieldsOf` returns `fields: [text, ...colour.fields, ...type.fields]`
  (`packages/site-schema/src/l1/edit.ts:975-984`), and
  `test_UAT_AC1117_...` already asserted
  `['text','color','fontSizePx','fontWeight','italic','textTransform']` — so
  AC-1117 was the only layer still claiming otherwise. `CONTROL_SHAPES` in that
  file is `['string','enum','integer','boolean','color']` — five, confirming the
  count change.
- `/api/copy` POST calls `editCopySet` and returns, commented "No re-render
  follows (REQ-119)" (`apps/control-app/src/router.ts:545-571`); the CLI renders
  both channels and emits `rendered` + `renderedDraft`
  (`tools/generate/src/cli/index.ts:1347-1354`). That pair is exactly why the
  both-channels claim moved from AC-992 to AC-982.

## Code Edits

None this call. The one non-ticket mutation is the AC-982 UAT extension (row 9),
which asserts behaviour the CLI already ships.

## Remaining Work (next iteration)

| Finding | Element | Plan |
|---|---|---|
| 5 | AC-1026 | **Done this call** (folded into row 3). |
| 4 | AC-1273 | The one judgement call the report left open: rephrase its cross-reader claim as the pairing surviving *any* reader rather than a second origin-parity assertion — the wording now used in AC-1111. |
| 6 | AC-980 + AC-990 | Reduce AC-990 to the full-fidelity readback it uniquely covers; leave the multi-line-control rule in AC-980. |
| 7 | AC-1122 | Add the negative half: a size, a weight and a colour have no value meaning "nothing declared", so those controls change a parameter and never clear one. |
| 9 | AC-1045 + AC-1270 | Let AC-1270 own the every-painted-panel rule; reduce AC-1045 to the read shape. Requires reading AC-1270 in full first — not attempted this call to avoid leaving a claim asserted with no verification. |
| 10 | STORY-100 body | `story-body-edit`: give AC-980 and AC-990 a parent bullet — overflowing copy is accepted, reads back in full, is never truncated, and a long or multi-line value asks for a control able to display it whole. |

## needs_review Items Forwarded

None. Every finding in REPORT-A0142017 carried an actionable resolution
category.

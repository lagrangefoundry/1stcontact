---
uid: comment-03836524
id: COMMENT-1319
type: comment
title: Claude response for uat_coverage_check report
created_by: xgd
created_at: '2026-08-20T14:41:49.121675+00:00'
updated_at: '2026-08-20T14:41:49.121675+00:00'
completed_at: null
last_field_updated: created_at
status: null
fields:
  subject_uid: report-2606a5ba
  kind: note
---

**Result: FAIL** — REPORT-2454 (`report-2606a5ba`), 2 violations, 2 warnings, 0 needs_review.

**Verdicts written**: 42 ACs (40 pass, 2 fail), STORY-84 `fail`, STORY-86 `fail`, CAP-71 `fail`.

## Method note

`.xgd/uat_index.json` in this worktree is **empty** (`{"acs": {}}`), so the lookup this prompt prescribes returns `[]` for every AC. I rebuilt the AC↔UAT map by grepping `tests/` for `test_UAT_AC(\d+)_` — all 42 ACs resolve, each to exactly one test. Unlike attempt 7, execution was available: all nine files run green (42/42, 1.40s).

## What attempt 8 fixed

Six of attempt 7's eight violations are genuinely closed — AC-689's full-language clause, AC-691's height/track assertions, the pinned-box content-overflow branch (open four cycles, now stated by AC-706/707/710 and asserted by all three), and the four missing ACs for padding, responsive text tracks, viewport-height response and the self-painting run.

## What still fails

**AC-694** (violation, 4th cycle) — the hint sidecar test is vacuous offline (it asserts `CANNED_HINTS`, a constant the test itself feeds `FakeDriver`) and silently `return`s past its real-engine half. Chromium is absent here — the file's 8 tests run in 198ms, and the alignment cycle measured `chromiumAvailable() === false` in this same worktree. The decisive point is scope, not environment: **ancestry, position mode, sibling-repetition count, gap, flex-direction and grid-template-columns are asserted on neither path even with a browser** — five of the criterion's eight dimensions, and precisely the relationships the sidecar exists to record. Two prior cycles blessed this on "the test gates honestly"; that covers the skip, not the scope.

**AC-705** (violation, new) — the `mounted` channel is live (`probes.ts:584/621/629/656/710`, printed at `cli/index.ts:639`) and **no test in the repository touches it**: `grep -rn "\.mounted" tests/` is empty across all 90+ suites. AC-705 spends a full Verification paragraph on it. Deleting the slot-rect diversion would leave all 42 UATs green while failing correct reproductions on markup L1 never emits. This slipped because the alignment cycle graded AC-705 "aligned (carried from a prior line-by-line pass)" — the carry is where it went unchecked. Same shape as the pinned-box branch that took four cycles.

I checked the other two AC-705 clauses attempt 7 left unexamined and both *are* covered: the width-ladder-only oracle by `req88-viewport-relative-and-nowrap.test.ts:609-619`, the synthesized-surface exclusion by `3probe-gate-evaluator.test.ts:562-567`.

Two warnings (graded `pass`, attribution only): AC-691's image/box height half lives in AC-729/AC-730's tests, and AC-731's newly-added full-bleed-bar and self-painting-run clauses are proven by the `bug19`/`bug20`/`bug21` free-coded suites rather than its own UAT.

Outside those two gaps, evidence quality is high — real entry points throughout, no internal mocking anywhere in the nine files, and several tests carry explicit counterfactuals (AC-735 re-implements the closed-upper-bound resolver to prove the guard bites; AC-736 contrasts synthesized vs captured ids at identical geometry).

---
uid: report-23924f60
id: REPORT-2756
type: report
title: 'Reconcile resolve conflicts: reconcile-BUNDLE-20'
created_by: xgd
created_at: '2026-08-31T06:31:11.851112+00:00'
updated_at: '2026-08-31T06:31:11.851112+00:00'
completed_at: null
last_field_updated: created_at
result: pass
fields:
  report_kind: resolve_conflicts
  subject_uid: reconcile-BUNDLE-20
---

## Files resolved

- `.xgd/tickets/hot/request-8d885016.md` — class **AA** (both added), intent/bookkeeping
  ticket (`request-*`), rule **2e** → strict-superset case. Resolved by taking the
  incoming (`free_coded`) version.

  Both sides added the same 100/101-line request ticket (REQ-63, "Coverage audit:
  capture + diff every render-affecting CSS axis"). The complete delta between the
  two sides is a single added line in `fields`:

  ```
  +  chat_comment: comment-b082d2cd
  ```

  Incoming adds one field the HEAD side never touched and removes nothing, so it is
  a strict superset — no per-fact timeline arbitration was needed. The enrichment
  metadata's fallback rule ("intent unknown on one or both sides; take the more
  recent commit by timestamp") points the same way and is therefore not in tension:
  incoming `badd26ff0279` is dated 2026-08-23, the HEAD-side commit `86ed8834c9ee`
  is dated 2026-07-22.

  Mechanics: the conflict was index-only — no conflict markers were present in the
  working-tree copy — so it was resolved with `git checkout --theirs` followed by
  `git add --sparse` per DOC-986 §2/§4.1.

## Incoming changes preserved

- `.xgd/tickets/hot/request-8d885016.md` — **fully preserved**. The incoming commit
  `badd26ff0279` adds this file whole (102 insertions, 0 deletions); the staged
  blob (`:0:`) was diffed against the incoming stage (`:3:`) and is byte-identical,
  so every incoming line is present by construction. Nothing from the HEAD side was
  lost either: the HEAD version is a line-for-line subset of the incoming version.

No hunks were dropped. The BUG-1301 precedence exception was not invoked, and no
test functions were involved in this conflict.

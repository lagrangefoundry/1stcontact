---
uid: report-71070278
id: REPORT-4221
type: report
title: 'Reconcile resolve conflicts: reconcile-BUNDLE-27'
created_by: xgd
created_at: '2026-09-14T04:05:57.413373+00:00'
updated_at: '2026-09-14T04:05:57.413373+00:00'
completed_at: null
last_field_updated: created_at
result: pass
fields:
  report_kind: resolve_conflicts
  subject_uid: reconcile-BUNDLE-27
---

## Files resolved

- `.xgd/tickets/hot/request-51514090.md` — UU, intent/bookkeeping ticket (rule 2e:
  strict superset, with the per-fact timeline rule applied to the one genuinely
  differing field). Ours = `xgd(ticket): seed_local_overlay request
  request-51514090` (updated_at 2026-09-11T18:53:54Z); theirs = incoming
  `xgd(ticket): update request request-51514090` =
  881fa99892d4aae72edbbc542e449dd9a7d9e0ea (updated_at 2026-09-01T22:15:38Z).

  The incoming commit is a pure status transition: `free_coded` ->
  `ready_to_reconcile`, plus the `updated_at` bump. Nothing else changed.
  `git diff :2: :3:` confirms ours already carries every other field
  (`fields.commits` with all three working SHAs, `fields.version: 0.2.39`,
  `last_field_updated: status`) and additionally holds
  `fields.bundled_in: bundle-8e1807f6`. The only disagreement is `status`:
  incoming `ready_to_reconcile` vs ours `bundled`. Per 2e's per-fact timeline
  rule, ours is the later-positioned intent (2026-09-11 vs 2026-09-01), and
  `bundled` is the downstream lifecycle successor of `ready_to_reconcile` —
  taking theirs would regress the ticket's state back through a transition the
  bundle has already completed. Kept ours.
  Resolved with `git checkout --ours` + `git add --sparse`.

## Incoming changes preserved

- `.xgd/tickets/hot/request-51514090.md`: preserved in the only sense available
  for a status-transition commit. The incoming commit's entire content is the
  `status` advance toward reconcile; HEAD already sits at `bundled`, which is
  strictly past that point on the same lifecycle path. No field, section, or
  body text from the incoming side is absent from the resolved file — verified
  by the `:2:` vs `:3:` diff, which shows ours missing nothing except the older
  `updated_at` and the earlier status value.

  No hunk was dropped under the BUG-1301 precedence exception; this is not a
  discard. This is the seeded-overlay redundancy case: the local overlay seed
  already carried (and advanced past) the incoming field, so the resolution nets
  to an empty staged diff vs HEAD — `git diff HEAD:<path> :2:<path>` was empty.
  Per STEP 4 that is expected; the finalize step will detect the clean staged
  diff and skip the commit. STEP 3's guard distinguishes this from a discard:
  the incoming transition is present in HEAD's history, not absent.

  Note: this is the third consecutive commit in this bundle to resolve the same
  way on this same ticket — d3c5e85b (body section "One honest caveat"),
  56087d83 (`fields.commits` / `version` block), and now 881fa998 (status
  transition). All three are already reflected in HEAD via the overlay seed.

## Staging state

`git status --porcelain` is empty — no UU/AA/DU/UD lines remain. CHERRY_PICK_HEAD
left intact; no `--continue`/`--skip`/`--abort` issued.

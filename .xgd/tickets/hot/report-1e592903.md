---
uid: report-1e592903
id: REPORT-3413
type: report
title: 'Reconcile resolve conflicts: reconcile-BUNDLE-23'
created_by: xgd
created_at: '2026-09-03T23:57:13.360796+00:00'
updated_at: '2026-09-03T23:57:13.360796+00:00'
completed_at: null
last_field_updated: created_at
result: pass
fields:
  report_kind: resolve_conflicts
  subject_uid: reconcile-BUNDLE-23
---

## Files resolved

- `.xgd/tickets/hot/request-909e42f8.md` — **UU**, intent/bookkeeping ticket (rule **2e**). Index-only conflict: the path is outside the sparse-checkout cone (DOC-986 §2/§4.1), so there were no working-tree markers. Resolved with `git checkout --ours` + `git add --sparse`.

  **Rule applied**: 2e "one side is a strict superset — keep the superset". OURS (`seed_local_overlay`, updated_at `2026-09-02T17:48:26Z`) already contains every fact the INCOMING commit `f7493a90cd` (`xgd(ticket): update request request-909e42f8`, updated_at `2026-08-31T22:09:06Z`) introduces, plus more. No per-fact timeline tiebreak was needed — no fact is set differently on the two sides in a way that competes; where they differ, ours is the strictly later value of the same fact.

  The enrichment metadata classed ours as intent-unknown and suggested "take the more recent commit by timestamp". That rule and 2e's superset rule agree here: ours is both the later commit and the superset.

## Incoming changes preserved

Incoming commit `f7493a90cd` touches exactly one file (1 file changed, 10 insertions, 2 deletions) — all four of its changes are frontmatter. Each is present in the staged result:

- `fields.commits` — the two-entry list (`working_sha: 858d63202fae2badbaf3e8495363244f8bd3a9fd`, `working_sha: c056002a525bf126e635f32118b64e2c76ad3ab0`, both with `reconcile_sha: null` / `main_sha: null`): **present, byte-identical** on the ours side.
- `fields.version: 0.2.22` — **present, identical**.
- `status: free_coding` → `free_coded` — **subsumed**. Ours carries `status: bundled`, the downstream successor state, together with `fields.bundled_in: bundle-203b1dc2` (this reconcile bundle). The incoming transition is not discarded; it has already been consumed and advanced past. Restoring `free_coded` would demote operator/workflow-set lifecycle state.
- `updated_at` bump to `2026-08-31T22:09:06.973741+00:00` — **superseded** by ours' later `2026-09-02T17:48:26.845864+00:00`.

Ours additionally carries content the incoming side never had: the appended `# What landed` implementation record, and a whole-body markdown reflow from the overlay round-trip. Neither is contested by the incoming diff, which touches only frontmatter.

No hunk was dropped under the BUG-1301 precedence exception; none was needed. No code, test, or spec-ticket files were involved in this conflict.

**Net effect**: the staged tree is byte-identical to HEAD for this path, so this cherry-pick nets to no diff. Per STEP 4 this is the redundant-commit case, not the discarded-commit case — STEP 3's distinguishing check confirms the incoming commit's key changes (`commits`, `version`) are *present in HEAD*, not merely absent. `--skip` was not called; `CHERRY_PICK_HEAD` (`f7493a90cd`) is left intact for `cherry_pick_finalize_resolution`.

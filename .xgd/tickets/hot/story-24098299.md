---
uid: story-24098299
id: STORY-86
type: story
title: 'End-to-end reproduction acceptance gate: 3 probes, structure recovery, and
  the cross-gate verdict'
created_by: xgd
created_at: '2026-07-22T20:06:46.872754+00:00'
updated_at: '2026-08-20T12:06:05.820215+00:00'
completed_at: null
last_field_updated: title
status: updated
fields:
  intent_uid: bundle-31e474b9
  capability_uid: capability-2049c9ec
  story_kind: upgrade
  story_points: 3
  uat_coverage: fail
  updated_by: bundle-ee56a66e
---

## Story
**As a** site reproducer using the L1 pipeline, **I want** an end-to-end acceptance
gate that decides whether a reproduced site definition is good enough — checking that
it matches the captured original at the sampled widths, stays laid-out-sanely at
widths that were never sampled, survives changes to its content, and that the eyes
which grade it are *reconciled against each other* rather than read in isolation — and
that repairs structure only where the checks fail, **so that** a clean verdict is a
trustworthy sign the reproduction is both faithful and robust, and each failure points
me at the specific framework gap to fix rather than a per-site patch.

## Description
This story documents the reproduction **acceptance boundary** that ties the L1
pipeline (capture → fold → render) together behind three probes, a demand-driven
structure-recovery step, and a cross-gate reconciliation over the three eyes that grade
a reproduction.

A browser-free, deterministic layout evaluator resolves every leaf's box at a given
width — mirroring the renderer's per-viewport `interpolate|snap` geometry and CSS
flow stacking, and estimating a text run's natural height so content growth is
expressible — and reports envelope violations (sibling overlap, horizontal clip
beyond the viewport, and pinned-box content overflow).

The evaluator mirrors the renderer on two axes that the naive model got wrong:

- **Flow direction.** A row container tiles its children along the main axis: each
  child takes its own width (a declared fixed width, else an equal share of the
  gap-aware leftover extent), the cursor advances by that width, and a row that
  does **not** wrap takes its height from its tallest child. A stack/box container
  stacks children vertically at full width; grid is modelled conservatively as a
  stack. Whether a container is a row at a given width, and whether it wraps, are
  read through the shared per-width mode cascade owned by the L1 Layout Substrate
  story (CAP-70) — the same one cascade the renderer resolves — so the wrapping
  branch's presence in the evaluator module is not an unowned probe behaviour any
  more than the responsive scalar track below is. Without the row branch every
  child took the full parent width and rows false-flagged overflow.
- **Breakpoint intervals.** A geometry track resolves a width against **half-open**
  per-segment intervals `[a.at, b.at)`, mirroring the renderer's stacked `min-width`
  rules where the highest breakpoint ≤ width wins. At an exact interior breakpoint the
  segment *starting* there is active, not the one ending there — a `snap` segment
  ending at a captured breakpoint no longer returns the held pre-reflow keyframe and
  cascade stale geometry down the rest of the page.

A third piece of renderer-mirroring math — resolving a **responsive scalar track**
(a per-width type or padding axis the fold keyframes) against the renderer's
cascade — lives in the same evaluator module but is *not* one of the three probes'
mirrored axes: none of the three reads a non-geometry track, and its only caller is
the browser-backed round-trip spine this story places out of scope. It is stated
here so its presence in the module is not read as an unowned probe behaviour.

On top of the evaluator sit three acceptance probes and their combined gate:

- **sample-fidelity** — the reproduced document's leaf boxes match the retained
  capture oracle at each captured width, within tolerance. Correspondence is
  established by **stable occurrence identity**: text leaves pair within a
  normalized-text key (the k-th oracle sample carrying a given text pairs with the
  k-th reproduced run carrying that text, both sides in document order); image and
  box leaves — which carry no text — pair by the same document-order occurrence
  mechanism keyed by leaf **kind**. The oracle is projected through the fold's own
  element classifier, so a sample exists exactly where the fold emits a leaf and
  controls / empty runs are excluded from the measure. Repeated labels/CTAs therefore
  each compare against their own box (no phantom deltas at sampled widths); an oracle
  occurrence with no remaining reproduced leaf surfaces as an unmatched coverage gap
  rather than being masked by an already-consumed box; and drift on one occurrence of
  a repeated key is still attributed to that occurrence.
  The non-text queue is built from the **captured** non-text leaves only. A
  *fold-synthesized* backing surface (a reconstructed band, section background or
  card) never enters it: its source elements classify as text and are already
  measured through their own text leaves, so it has no oracle counterpart, and
  leaving it in would shift every real box leaf by the number of surfaces ahead of
  it and report phantom deltas on all of them. This is the measure's only exclusion
  on the **reproduced** side — the classifier exclusion above works on the *oracle*,
  dropping controls and empty runs before pairing.
  The oracle admits the **width ladder only**. A height-probe re-shoot of a ladder
  width at a second viewport height is deduped out — admitting it would hand the
  measure a second full set of oracle rows at a width whose reproduced-leaf queues
  are already drained, reporting every text run on the page as unmatched. This is
  the gate-side counterpart of the fold's own rule that the keyframe ladder skips
  the probe.
  The probe reports **three** channels, not two: over-tolerance deltas
  (residuals), oracle occurrences with no reproduced leaf (unmatched), and
  **mounted** — oracle text falling inside a behaviour slot rect, which a mounted
  behavior module renders (a form's own submit button) rather than L1. Mounted text
  is deliberately not graded, because grading L1 on markup it does not emit would
  fail the gate for a correct reproduction; and it is counted and surfaced rather
  than dropped quietly, so a mounted region does not become an ungraded hole nobody
  can see. The number is the size of what the L1 gate is not the right instrument
  for. This is the gate-side counterpart of the behaviour seams the fold pins: the
  seam L1 emits is exactly the rect this probe declines to grade inside. It is a
  third mechanism, distinct from both exclusions above — the classifier one drops
  control and empty runs from the oracle before pairing, the synthesized-surface
  one keeps fold-invented boxes out of the reproduced queue, and this one sets
  aside oracle *text* by where its box falls.
- **off-sample** — the layout envelope holds (no overlap / clip) at intermediate
  widths the capture never sampled.
- **content-robustness** — the envelope holds when content is perturbed (text runs
  grown, pinned heights grown).

Two leaf kinds are exempt from the sibling-overlap check, for the same reason in
two forms — each is a surface something else is meant to sit on top of. A
**painted backing surface** (a card/section/panel fill folded as a childless `box`
leaf behind the content it backs) is excluded because a background sitting under
its own content is by design, not a collision; it is also the leaf the
sample-fidelity probe leaves out of its non-text pairing queue, above. A **slot**
leaf is excluded because it is an inert seam pinned at its cluster's union rect
whose mounted module paints inside it, so it overlaps its own control leaves by
construction — without the exemption every reproduced form would fail both
envelope probes. Both remain subject to the horizontal-clip check — an exempt leaf
that runs past the viewport is still reported, whatever sits on top of it.

The gate runs all three probes on an **absolute-base / structure-overlay split**:
fidelity is measured on the absolute base (which reproduces the oracle exactly), while
the envelope probes are measured on a structure-recovered overlay. **Demand-driven
recovery** is region-aware and recursive: at each node the direct pinned children that
collide under perturbation are grouped into connected components of the
perturbed-overlap graph, and each component becomes its own flow stack container with
its own interior gap derived from the absolute measurements. A node that needs recovery
flows **all** of its children — regions as sub-stacks, survivors as flowed items — so
no pinned sibling is left behind for a grown region to overrun. A node with no
colliding group stays fully absolute. This is what makes the envelope probes hold on a
real multi-region page (hero / grid / footer); single-level promotion of one flat pile
kept one median gap for the whole page and left the probes failing at every width.

Beyond the probe reports, the combined gate carries the **fold's residuals** — the
elements the folder cannot yet express as L1 leaves — as a channel distinct from all
three of the fidelity probe's own channels (residuals, unmatched and mounted), so a
folder-power gap is legible as a framework gap rather than mistaken for mispairing or
for a region the gate deliberately left ungraded.

The fidelity verdict rests on an **idempotence identity**: the analytic value-render
of the absolute-base fold is deterministic (re-evaluating the same document at the
same width is identity) and per-occurrence faithful (N elements sharing a text at a
width yield N distinct reproduced runs, each matching its own oracle box) — i.e.
`value-render(value-render(X)) == value-render(X)`. Without it a clean fidelity
report would be unfalsifiable.

### The boundary is wider than geometry: cross-gate reconciliation

The three-probe gate grades **geometry and envelope only**, and is deliberately blind
to colour, font, media and list styling. The value eye compares elements present in
**both** manifests, so a capture that missed the page's substance leaves it nothing to
raise a delta against. The perceptual eye is the only one with no assumption shared
with the thing it grades. Each behaves exactly as designed — the defect was that
**nothing compared them to each other**, so a reproduction 80% wrong by pixel count
passed with zero residuals and an operator only found out by looking at a screenshot.

The acceptance boundary therefore extends from the three probes to a **cross-gate
verdict** over one reproduction and its bundle:

- the two **browser-free** signals — the geometry gate and reference coverage — run
  first, so a stale or half-captured bundle fails before a headless browser starts;
- the perceptual and value eyes then run through the same offline seams their own
  verbs expose, so the reconciliation grades the artifacts an operator would get from
  running the commands by hand, and is drivable with no browser at all;
- a **perceptual floor** (a mean bound and a percent-of-pixels-over-threshold bound,
  either one sufficient) fails the run regardless of what the value gates say, with the
  floor in force echoed into the report and overridable per run;
- **reference coverage** is reported every run — mirrored-vs-referenced media, and page
  height per captured section — both numbers the pipeline already computed and never
  surfaced, so an impoverished reference manifest is visible without inspecting a
  screenshot;
- the verdict **names the likely cause** (structural-failure, capture-incomplete,
  reproduction-wrong, unexplained-disagreement, or pass) and the one next step it
  implies, so "fix the capture" and "fix the reproduction" stop looking identical;
- the value eye's deltas are **evidence, not exit code** — the value verb already exits
  non-zero on any delta and stays the sharp instrument for a text-led page;
- a bundle with **no retained reference manifest** is a hard error, never a clean run,
  because coverage measured against nothing reports clean.

**In scope**: the analytic evaluator's flow model (non-wrapping row tiling, stack
stacking, and conservative grid) and its breakpoint-interval semantics; its envelope
findings and their two exempt leaf kinds (the backing surface and the slot); the three probes and their report shapes, including the
sample-fidelity pairing contract for text and non-text leaves — including the
exclusion of fold-synthesized backing surfaces from the non-text queue — its
width-ladder-only oracle, and its ungraded-but-counted mounted-behaviour channel; the combined gate over
the absolute-base/overlay split and its separate fold-residual channel; region-aware
recursive promotion of failing pinned groups to flow; the cross-gate reconciliation —
its ordering, its perceptual floor, its reference-coverage report, its named causes and
next steps, and its refusal of a bundle with no retained reference manifest.

**Out of scope**: the browser-backed `capture(render(L1)) ≈ L1` round-trip check
(the round-trip spine, separate); the fold itself, including which residuals it emits
(the fold story, this capability's other half); the renderer and envelope validator,
the wrapping-row axis and the per-width layout-mode cascade this evaluator resolves
through (CAP-70); the perceptual and value eyes'
own measurement contracts (they are reconciled here, not defined here); running the
gate on any particular real site.

## Technical Context
- Depends on the L1 substrate + safe renderer/validator (CAP-70, plan item 1) and,
  within this capability, on the capture→L1 fold + retained oracle produced by the
  fold story (plan item 2).
- The evaluator is deliberately **analytic (browser-free)** so every probe is an
  always-run, deterministic piece of evidence rather than a cross-engine skip; it
  sits on top of, and does not replace, the browser-backed round-trip spine.
- **The row width model is an analytic stand-in, not a flex implementation.** Equal
  shares of the leftover extent stand in for flex-grow / natural content width — the
  probe only needs the *behaviour* (does a well-formed row tile without overlap or
  overflow?). Grid is modelled as a stack for the same reason: envelope-conservative,
  and the folder does not emit grid yet. Both are deliberate approximations of the
  renderer, not claims of fidelity to it.
- **Half-open interval resolution mirrors the renderer's cascade, and is the only
  correct reading of it.** The earlier closed upper bound was diagnosed as an
  *evaluator* defect, not a fold defect: capture and fold were always emitting a
  keyframe at the reflow breakpoint. No fold change was required to close it.
- Demand-driven promotion returns a **validated** L1 document — a recovery that would
  produce an invalid document is rejected rather than returned, preserving the CAP-70
  safety envelope.
- `promoted` paths are reported in the rewritten tree: a single region covering all of
  a node's children reports the node's own path; multiple regions report their nested
  paths under it.
- Fidelity is measured on the untouched absolute base, so recovery can never regrade
  `sampleFidelity`.
- The synthesized-surface exclusion is a consequence of the fold's surface
  reconstruction (BUG-14), not an independent rule: the fold invents band, section
  background and card boxes for fills the capture composited onto runs, and those
  boxes are the ones with no oracle row. The exclusion is keyed on the synthesized
  identity the fold assigns them, so a genuine captured standalone surface still
  pairs and still participates in the overlap check.
- The height-probe dedup is keyed on `(width, state)` locally in the gate rather than
  routed through the capture package's own probe partition, so the oracle source the
  gate consumes stays purely structural — it deliberately carries no engine, and the
  gate re-folds for one engine anyway. The rule is the same either way: the first
  projection at a key is the ladder, later ones are evidence.
- A slot rect is tested by the oracle box's **centre**, not by full containment, so a
  run that a mounted module renders slightly proud of its seam still reads as mounted
  rather than as a false unmatched. The diversion happens only on the *text* pairing
  path, where an unpaired occurrence is otherwise `unmatched`; a non-text sample is
  never diverted. The channel is a report field only — the probe's verdict is
  residuals-plus-unmatched, so mounted text can neither fail a run nor rescue one.
- The intent frames each probe residual as a **framework gap** (a missing L1 axis /
  hint / a region needing promotion) to be filed and fed back, not a per-site patch;
  the reports are therefore diagnostic (offending leaf identity + magnitude), not a
  bare boolean. The fold-residual channel serves the same growth loop for folder power
  specifically — the fold story decides *what* it cannot express; this story owns
  only the gate's obligation to keep that channel separate and legible.
- **Occurrence-index pairing is load-bearing across this capability's two stories.**
  The fidelity correspondence is valid only while the fold keeps building one leaf
  per responsive-table row in FIFO document order per key (the fold story).
  BUG-5's intent asked for "a stable identity (index path / node id carried through
  the fold)"; the implementation deliberately narrowed that to the occurrence index
  the fold's responsive table **already** assigns, adding no new node-id field to L1
  nodes — a narrowing recorded in the intent ticket's own implementation note, so
  the matrix describes occurrence-index pairing, not node ids. The AC states the rule
  in terms of document order on both sides, making the dependency explicit. Non-text
  pairing extends the same mechanism with `kind` as the key in place of text.
- **The perceptual floor is provisional and stated as such.** The intent's own note is
  that a calibrated floor requires a human-labelled anchor set of (reference, render)
  pairs tagged indistinguishable / not, and that set does not exist yet. The shipped
  defaults are read off the only two measured reproductions — one an operator accepts,
  one that did not reproduce at all — and are set so the range between them is left
  *unclassified* rather than wrongly passed. They are per-run overridable and are not a
  claim about perceptual thresholds. The ACs therefore assert the floor's *mechanism*
  (either bound sufficient, echoed, overridable), not its numeric values.
- **Segmentation density is a proxy, not a rule.** A uniformly-styled page is
  legitimately one section, so a long band is reported as coverage evidence that shapes
  an already-failing verdict, never as a failure on its own. The same holds for the
  media proxy: a coverage finding alone, with the perceptual eye within its floor, does
  not fail a run.
- **Coverage is asked once, at the widest resting-state projection on the reference's
  primary engine** — it is a whole-page question, asked at the width the full-page
  screenshot was taken at, rather than averaged across the width ladder.
- **Excluding value deltas from the exit code is deliberate.** The value verb already
  exits non-zero on any delta; folding its routinely non-empty count into this verb
  would duplicate a gate that already runs, and this verb exists to catch what the value
  gates *miss*. Coverage is likewise consulted *before* the delta count: deltas measured
  against an impoverished reference are not yet evidence.
- **Not to be conflated with the `1c values-diff` duplicate-text pairing** (CAP-63,
  1c Capture & Diff Fidelity, which owns the element-pairing rules that decide which
  two elements are compared), which pairs repeated text by rendered position in a
  different subsystem. Both
  pipelines handle duplicate text; neither AC covers the other.
- Reference-coverage refusal of a bundle with no retained multi-state oracle is the
  same non-vacuity principle AC-708 already applies to the three probes, applied one
  layer out: a gate measured against nothing reports clean.

## Dependencies
- Plan item 1 — L1 Layout Substrate + Safety Envelope (CAP-70)
- Plan item 2 — Capture → L1 Fold + Structural Hints — the fold story within this
  same capability, not an external dependency
- Reproduction pipeline capture & fold fidelity (BUNDLE-11 plan item 3) — the capture
  gap that motivated the cross-gate verdict.

## Story Points
3
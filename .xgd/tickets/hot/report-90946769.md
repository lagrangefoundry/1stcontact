---
uid: report-90946769
id: REPORT-785
type: report
title: 'UAT Coverage: End-to-End Reproduction Gate (3-Probe)'
created_by: xgd
created_at: '2026-07-23T06:35:40.862279+00:00'
updated_at: '2026-07-23T06:35:40.862279+00:00'
completed_at: null
last_field_updated: created_at
result: pass
fields:
  report_kind: uat_coverage_check
  subject_uid: capability-8108afab
  violations: 0
  warnings: 0
  needs_review_count: 0
---

# UAT Coverage Assessment: End-to-End Reproduction Gate (3-Probe)

**Result**: PASS
**AC verdicts**: 6 pass, 0 fail, 0 deprecated, 0 needs_review
**Story verdicts**: 1 pass, 0 fail, 0 stale, 0 needs_review
**Capability verdict**: pass

## Cumulative Intent Considered

The capability (CAP-73) and its single story (STORY-86) both carry
`intent_uid = bundle-31e474b9` (BUNDLE-7). The capability, story, and all six
ACs were created 2026-07-22 — this is the newest capability in the matrix, so
no later intent retires any of its behaviors.

| Intent ID | Status | When | Asked / changed | Counts? |
|---|---|---|---|---|
| BUNDLE-7 (bundle-31e474b9): REQ-63 + REQ-79 + REQ-82 + REQ-83 + REQ-84 + 2 more | free_and_reconciled | 2026-07-22 (merged edeb1c2c) | Framework pivot to L1 substrate + safe renderer; capture/diff coverage audit; established the reproduction acceptance boundary (3 analytic probes + combined gate + demand-driven structure recovery) | YES |

All six ACs describe behaviors introduced by this reconciled bundle. None are
retired by any later intent. All are **active**.

## Alignment Ledger

| Story | Intents aligned to | Outcome | Notes |
|---|---|---|---|
| STORY-86 | BUNDLE-7 (REQ-79/82/83/84 framework pivot + gate) | aligned | Story body's in-scope behaviors (analytic evaluator envelope findings; three probes + report shapes; combined gate over absolute-base/overlay split; demand-driven promotion of failing pinned groups; validated-L1 recovery) are each supported by the reconciled bundle and each exercised by an AC/UAT. Out-of-scope items (browser round-trip spine, the fold CAP-71, renderer/validator CAP-70) are correctly excluded. |

## Findings — Categorized by Editor Action

No coverage violations, no needs_review. All six ACs are active per cumulative
intent and substantively covered; the story body is aligned and fully covered.

(No findings table rows.)

## Evidence Notes

Tests live in `tests/reconciliation-3probe-gate.test.ts` — one UAT per AC,
named `test_UAT_AC705..AC710`, exercised at the real module boundary
(`tools/generate/src` exports), against the real 639-line implementation in
`tools/generate/src/l1/probes.ts`. No internal mocking; fixtures are real
`MultiStateCapture` objects folded via the real `foldToL1`.

- **AC-705 sample-fidelity** — `test_UAT_AC705`: real `sampleFidelityProbe`;
  clean pass with empty residuals/unmatched + max-delta; perturbs a box at the
  LAST width and asserts a residual carrying text/width/(dx,dy,dw); drops a run
  and asserts `unmatched`. Distinguishes correct from incorrect. **pass**
- **AC-706 off-sample** — `test_UAT_AC706`: real `offSampleProbe` at 500/900,
  empty findings; narrow oracle degrades to a `clip` at the unsampled 500px
  while 900 stays clean. **pass**
- **AC-707 content-robustness** — `test_UAT_AC707`: real `contentRobustnessProbe`
  at 2.5×; pinned overrun → `overlap` finding, pass=false, per-width; flow-
  structured equivalent → pass=true, empty. **pass**
- **AC-708 combined gate (non-vacuous)** — `test_UAT_AC708`: real
  `threeProbeGate`; base with no overlay → pass=false driven by content-
  robustness (fidelity still passes on the absolute base); base + recovered
  overlay → pass=true with all three sub-reports passing. **pass**
- **AC-709 demand-driven recovery** — `test_UAT_AC709`: real `promoteToFlow`;
  failing root promoted (path '0'), recovered doc passes content-robustness at
  every width, `validateL1(recovered).ok === true`; a roomy region that already
  survives is NOT promoted (empty list). **pass**
- **AC-710 diagnostic findings** — `test_UAT_AC710`: fidelity residual names
  run/width/(dx,dy,dw); overlap finding carries kind + human detail + ≥2 leaf
  index paths; clip finding carries kind + a detail with px magnitude + leaf
  path. **pass**

## Notes for the Editor

No matrix action required — this capability is fully covered and aligned.

One operational (non-matrix) observation for the operator: the test suite could
not be imported until `@1stcontact/site-schema` was built — its `dist/` was
absent, so vitest failed with "Failed to resolve entry for package
@1stcontact/site-schema" before any test ran. After `pnpm --filter
@1stcontact/site-schema build`, all six UATs pass (1.55s). This is a
stale/missing workspace-build artifact, not a test or coverage defect, and is
outside the editor's levers (story-body-edit / ac-add / uat-add / uat-edit).
Flagging so the regression/quality pipeline ensures a workspace build precedes
vitest — otherwise a green matrix could sit behind an un-runnable suite.

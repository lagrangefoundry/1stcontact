---
uid: report-81c24786
id: REPORT-2958
type: report
title: 'Reconcile resolve conflicts: reconcile-BUNDLE-21'
created_by: xgd
created_at: '2026-08-31T15:12:05.099034+00:00'
updated_at: '2026-08-31T15:12:05.099034+00:00'
completed_at: null
last_field_updated: created_at
result: pass
fields:
  report_kind: resolve_conflicts
  subject_uid: reconcile-BUNDLE-21
---

## Files resolved

- `.xgd/tickets/hot/request-a03967f2.md` — UU, intent/bookkeeping ticket (STEP 2 rule **2e**, strict-superset branch). Outside the sparse-checkout cone, so resolved via `git checkout --ours` + `git add --sparse` (DOC-986 §2/§4.1).

  - Incoming commit `50fc10b7` ("xgd(ticket): update request request-a03967f2") adds exactly one line vs the merge base: `chat_comment: comment-869ded75`.
  - HEAD side already contains that exact line, and additionally has `bundled_in: bundle-b3b7c399`, `status: bundled` (vs base/incoming `ready_to_reconcile`), and a later `updated_at` (`2026-08-24T02:10:41Z` vs `2026-08-23T03:29:49Z`).
  - Ours is therefore a strict superset of theirs on every field: there is no per-fact disagreement, so no `xgd working-timeline` tie-break was needed. The enrichment note's fallback ("take the more recent commit by timestamp") points the same way — HEAD is the later side.
  - No content was invented; the resolved file is byte-identical to the HEAD-side blob (`97b4d0d0`).

## Incoming changes preserved

- `.xgd/tickets/hot/request-a03967f2.md`: **preserved.** The incoming commit's sole change (`fields.chat_comment: comment-869ded75`) is present verbatim in the resolved file. Verified with `git show 50fc10b7 -- <file>` against the staged content.

No hunks were dropped; the BUG-1301 precedence exception was not invoked.

## Note on the net-zero staged diff

`git diff --cached HEAD` is empty: the incoming commit's effect had already landed in HEAD through a different route (the same `chat_comment` field was written on the bundle branch alongside the `bundled_in`/`status` advance). Per STEP 4 this is a genuinely redundant commit, not a discarded one — STEP 3's check distinguishes them, and the incoming change is present in HEAD rather than absent. `--skip` was not called; the finalize step will detect the clean staged diff. `CHERRY_PICK_HEAD` is still present and untouched.

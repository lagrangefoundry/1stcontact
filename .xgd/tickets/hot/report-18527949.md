---
uid: report-18527949
id: REPORT-4199
type: report
title: 'Reconcile resolve conflicts: reconcile-BUNDLE-27'
created_by: xgd
created_at: '2026-09-14T02:53:30.776087+00:00'
updated_at: '2026-09-14T02:53:30.776087+00:00'
completed_at: null
last_field_updated: created_at
result: pass
fields:
  report_kind: resolve_conflicts
  subject_uid: reconcile-BUNDLE-27
---

## Files resolved

- `.xgd/tickets/hot/bug-93851fea.md` — UU, sparse-excluded (index-only, no
  working-tree markers). Intent/bookkeeping ticket (`bug-*`), rule 2e.
  Resolved by taking OURS via
  `git checkout --ours --ignore-skip-worktree-bits` + `git add --sparse`,
  because ours is a strict superset of theirs.

  Body prose is byte-identical on all three index stages; the conflict is
  confined to the YAML front matter.

  - base (`:1`): `status: free_coding`, `updated_at 2026-09-01T20:46:22`,
    no `commits`/`version`.
  - theirs (incoming, `370ff62` `xgd(ticket): update bug bug-93851fea`):
    `status: free_coded`, `updated_at 2026-09-01T20:49:57`, adds
    `commits[0].working_sha: d019bab77200d88dd613c94e0bbaa93b300ed526`
    and `version: 0.2.35`.
  - ours (HEAD, seed_local_overlay): `status: bundled`,
    `updated_at 2026-09-11T18:53:54`, carries the SAME
    `commits[0].working_sha: d019bab7...` and the SAME `version: 0.2.35`,
    plus `story_points: 2` and `bundled_in: bundle-8e1807f6`.

  Per-fact judgment (2e):
  - `commits`, `version` — identical on both sides, no conflict.
  - `status`, `updated_at` — the only genuinely competing facts. Ours is the
    later-positioned intent (2026-09-11 vs 2026-09-01, ten days later) and is
    downstream of theirs in the lifecycle: the bundling step consumed
    `free_coded` and advanced it to `bundled`. Taking theirs would revert the
    ticket out of `bundled_in: bundle-8e1807f6` — i.e. un-bundle the very
    bundle being reconciled. Ours kept.
  - `story_points`, `bundled_in` — present only on ours; kept.

  No content was invented; nothing present on either side was dropped except
  the superseded `status`/`updated_at` values, as reasoned above.

## Incoming changes preserved

No code/implementation files were conflicted — the sole conflict is a
bookkeeping ticket.

The incoming commit `370ff62` makes exactly four changes to this file:
`status free_coding → free_coded`, `updated_at` bump, `+commits[working_sha
d019bab7...]`, `+version: 0.2.35` (plus a trailing-newline removal). Of these,
`commits` and `version` are present verbatim in the resolved file, and
`status` is present in its advanced form (`bundled`), which HEAD reached by
way of `free_coded`. So the incoming commit's substantive effect already
exists in HEAD — this is the redundant-commit case (BUG-1109/BUG-1122), not
a discard, and STEP 3's guard is satisfied.

Consequently `git diff --cached HEAD` is empty. Per STEP 4 this is expected
and is NOT a failure; `--skip` was not called, the resolution is staged, and
CHERRY_PICK_HEAD is left intact for cherry_pick_finalize_resolution to
detect the empty staged diff and skip the commit.

The BUG-1301 precedence exception was not invoked; no hunk was dropped on
those grounds.

## Post-merge review flag

The enrichment metadata classified this as "intent unknown on one or both
sides" and asked for a post-merge review flag. Flagging it here for
completeness, though the superset relationship above makes the resolution
unambiguous: ours contains every fact theirs contributed.

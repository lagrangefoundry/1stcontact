---
uid: report-7c608777
id: REPORT-3796
type: report
title: 'Fix AI Site Assistant: Per-Site Conversations (story) — attempt 2'
created_by: xgd
created_at: '2026-09-10T20:58:08.357299+00:00'
updated_at: '2026-09-10T20:58:08.357299+00:00'
completed_at: null
last_field_updated: created_at
fields:
  report_kind: fix_structural_validation
  subject_uid: capability-7e4714b7
  level: story
  fixes_applied: 4
  progress_made: true
  needs_more_work: false
  violations_remaining: 0
  anchor_report_uid: report-e37a6b4a
---

# Fix Summary — AI Site Assistant: Per-Site Conversations (story)

**Attempt**: 2
**Fixes applied this call**: 4 body mutations (3 findings) in one `ticket update`
**Violations remaining**: 0
**Needs more work**: false

All three findings in REPORT-3795 (report-aba7b65e) were categorised
`story-body-edit` against STORY-103 (`story-a58a0974`) alone. All three are
applied. No AC was added, edited or deprecated, and no test or production code
was touched — consistent with the report's *Notes for the Editor* ("do not open
ACs off the back of this report").

## Actions Taken — by Resolution Category

| # | Category | Element | Action |
|---|---|---|---|
| 1 | story-body-edit | STORY-103, Technical Context (was lines 174–179) | **Violation, finding 1.** Narrowed the tenancy claim to the corpus. Deleted "and this repo has no tenancy yet" and "nothing tenant-scoped is claimed or built by this story"; kept REQ-123's live reason (why per-tenant knowledge bases can be added later without revisiting this wiring) and added the explicit contrast: the *conversation* is account-scoped and its transcript/audit tenant-partitioned through the site's own store (REQ-143 / REQ-146), with the identifier resolved against that account's storage (BUG-38). "identical query text yields identical results for everyone" → "…for every account". |
| 2 | story-body-edit | STORY-103, Out of scope (was line 81) | **Warning, finding 2.** `CAP-87` → `CAP-86` beside `story-37a3921b`. |
| 3 | story-body-edit | STORY-103, Technical Context (was line 110) | **Warning, finding 2.** `CAP-87` → `CAP-86` beside `story-37a3921b`. |
| 4 | story-body-edit | STORY-103, In scope "Where the conversation runs" (was lines 52–55) | **Warning, finding 3.** Dropped "how a failure is reported" from the list of things that are "the same either way"; replaced with the property that actually holds on both — a failure is reported honestly, never dressed as the assistant having tried, *in the shape that origin's own answer takes* — and pointed the reader at the BUNDLE-21 Reconciliation Decision that carries the per-origin detail. |

### Evidence checked before editing

- **Finding 2 confirmed by ticket lookup.** `capability-f753cecd` = **CAP-86**
  "Structured Copy Editing: One Validated, Atomic Write Path" — the capability
  that owns `story-37a3921b`/STORY-100 and the validated atomic write path.
  `capability-12fee326` = **CAP-87** "In-Page Copy Editing" — a different
  out-of-scope neighbour. The report's proposed correction is right.
- **Finding 1 confirmed against code.** `apps/control-app/src/ai.ts` partitions
  this story's own transcripts and audit by tenant: `:72` (`tenantId` field),
  `:77` (`chat/${this.tenantId}/${sessionId}.md`), `:81`, `:145`
  (`audit/${tenantId}/${sessionId}/…`), `:189`, `:197`, `:210`. The deleted clause
  was false, and denied a property AC-1055 / AC-1409 / AC-1456 assert.

### Guardrails observed

- The neighbouring paragraphs the report told me to leave alone are untouched: the
  "one conversation host per isolate" / "that cache is a cache of hosts, not of
  conversations" pair still uses "tenant" correctly (no find-and-replace was used —
  each site was edited individually), and both spot-checked-correct claims (the
  AC-1051 local-transport asymmetry, and operator-local git-ignored transcripts)
  are unchanged.
- Section structure preserved: all six sections (`Story`, `Description`,
  `Technical Context`, `Reconciliation Decisions`, `Dependencies`,
  `Story Points`) intact, edits confined to the flagged passages.

### Post-edit verification

- `CAP-87` no longer occurs anywhere in the story body (now 2× `CAP-86`, 3× `CAP-85`).
- "no tenancy" no longer occurs.
- "how a failure is reported are the same either way" no longer occurs.
- The story's acceptance criteria were scanned for the same two defects
  (`CAP-87`, "no tenancy"): **zero occurrences** — no parallel AC edit needed,
  matching the assessor's coverage/exclusivity pass.

## Code Edits (if any)

None this call. `apps/control-app/src/ai.ts` was read only, as evidence for
finding 1. No tests were changed, so no test run was warranted.

## needs_review Items Forwarded

None. No finding in REPORT-3795 was categorised `needs_review`.

## Carried Forward (no action, recorded so a future check does not re-litigate)

- **BUG-39 is `bundled`/imminent.** Its body flags the REQ-127 / AC-1055
  "derivable is not the same as issued" conflict as operator-pending; that
  decision is already taken and recorded in this story's BUNDLE-21 reconciliation
  (AC-1055's earlier verification is deliberately the accepted case). Untouched.
- **REQ-158 / REQ-159 / REQ-160 are `draft` and excluded.** When they land, the
  Technical Context "degradation is not failure" paragraph and AC-1320 will move —
  and the tenancy paragraph edited above will need revisiting, because
  REQ-159 makes the corpus itself tenant-scoped, which is exactly the claim just
  narrowed to "the corpus does not vary by account".

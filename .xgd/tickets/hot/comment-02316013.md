---
uid: comment-02316013
id: COMMENT-595
type: comment
title: Comment on bug BUG-30
created_by: xgd
created_at: '2026-07-31T00:54:41.170853+00:00'
updated_at: '2026-07-31T01:08:55.741723+00:00'
completed_at: null
last_field_updated: body
status: null
fields:
  subject_uid: bug-87f0aefa
  kind: chat_transcript
---

<!-- xgd-turn id="c2ab4945-2456-4a82-a69a-074981e0ab66-user" -->

<!-- xgd-chat role="user" ts="2026-07-31T00:54:39.234452+00:00" -->
#### You
please free code a fix for this

<!-- xgd-turn id="c2ab4945-2456-4a82-a69a-074981e0ab66-assistant" -->

<!-- xgd-chat role="assistant" ts="2026-07-31T01:02:58.794702+00:00" -->
#### Claude
I'll start by reading the ticket and the code involved.

Now let me check the other sinks and the flat-snapshot invariant.

Let me verify the security dimension and check how the deployed worker serves a bare directory URL.

Confirmed — and the same slash-strip has a **security** consequence: `/javascript:alert\`1\`` passes `isSafeUrl` (it's root-relative), then relativizes into a live `javascript:` URL. Same defect class: dropping the slash changes what the remainder *means*, not just its shape. Fixing both under one rule.

Now the UATs. AC4 requires a genuine two-page case, since a single-page site can't distinguish correct from incorrect.

Now the UAT file:

Passing — now verifying they genuinely fail without the fix (otherwise they're not evidence).

The 3 behaviour tests fail without the fix; the 2 regression-guards pass both ways — correct. Now the regression scope.

Pre-existing, and it's exactly this ticket's "a page that is not `index.html`" condition surfacing: the REQ-109 test's toy server doesn't model REQ-113's extensionless→`.html` mapping. Let me confirm production does.

All 5 failures pre-exist my change. Now the version bump and commit.

Now the ticket — preserving the existing body and appending the outcome.

Fixed and landed. BUG-30 is at `free_coded`, commit `22cf7aee7d`, version `0.1.9`.

## The fix

Rather than special-case `#`, the audit collapsed the symptom and one further case into a single rule — after dropping the slash, **is the first path segment non-empty and colon-free?** If not, prefix `./`:

```ts
const rest = v.slice(1)
const firstSegment = rest.split(/[/?#]/, 1)[0]
return firstSegment === '' || firstSegment.includes(':') ? `./${rest}` : rest
```

Both cases are the same defect the ticket identified — dropping the slash changed what the remainder *means*, not its shape. `/#how` and `/?q=1` (which had it too) lose their path entirely and resolve against the current document; `./` forces a path segment so they resolve against the snapshot directory, still document-relative.

## Security finding from the audit

The "check the rest of the sink" pass turned up a live XSS. `isSafeUrl` admits `/javascript:…` *because* the leading slash makes it relative — then `relativizeUrl` stripped that slash behind its back and handed back the URL the validator had just refused:

```
/javascript:alert`1`  →  isSafeUrl: true  →  href="javascript:alert`1`"
```

Parens and quotes are blocked by `URL_FORBIDDEN_CHARS`, so the usual payloads never reached the code — a backtick payload did, at the node-link `href` sink. Same rule closes it.

Your three audit questions: query strings are fine (`?v=` cache-busting unaffected); the flat-snapshot assertion still fires and is now pinned by a UAT; the non-`index.html` page condition is now genuinely exercised — every behavioural UAT renders a two-page snapshot and asserts on browser-resolved URLs, not emitted spelling. Verified RED by reverting the fix: the 3 behavioural UATs fail, the 2 regression-guards pass.

## One thing I did not fix — it needs your call

`test_UAT_FC_REQ-109_served_under_path_prefix` is now red, and it's telling the truth. Your uncommitted `home.json` links `/whitepapers` → `whitepapers` (correct, unchanged by this fix), but **the two serving paths disagree on extensionless URLs**:

- `1c serve` maps extensionless → sibling `.html` (REQ-113)
- the deployed `public-site` worker does **not** — `routes.ts` passes the tail through as an R2 key

So `/site/<slug>/draft/<sha>/whitepapers` works locally and 404s in production. That's a serving-layer defect, not `relativizeUrl`, so I left it rather than quietly widening scope. Either the worker gains the REQ-113 mapping or pages author `whitepapers.html`. I also left your in-flight `home.json`/`whitepapers.json` untouched — the `/index.html#how` workaround can become `/#how` whenever you want.

Full suite: 958 passed, 5 failed — all 5 verified pre-existing (identical failures with the fix stashed). Framework typecheck clean.

<!-- xgd-turn id="3d9e0304-4ed3-4a0b-a35d-87a0b309b319-user" -->

<!-- xgd-chat role="user" ts="2026-07-31T01:08:55.714364+00:00" -->
#### You
please look at the recent chat tickets this came up I have lost track of how we decided to resolve it

<!-- xgd-chat-end -->
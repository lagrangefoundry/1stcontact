---
uid: report-a5312231
id: REPORT-2877
type: report
title: 'Reconcile resolve conflicts: reconcile-BUNDLE-20'
created_by: xgd
created_at: '2026-08-31T09:00:46.322506+00:00'
updated_at: '2026-08-31T09:00:46.322506+00:00'
completed_at: null
last_field_updated: created_at
result: pass
fields:
  report_kind: resolve_conflicts
  subject_uid: reconcile-BUNDLE-20
---

## Files resolved

- `.xgd/tickets/hot/bug-61f43435.md` — class **AA** (both added), intent/bookkeeping
  ticket (BUG-12). Rule **2b** ("one side is strictly a superset: keep the superset,
  incoming wins if incoming is the larger version"), consistent with **2e**
  ("incoming only added a field the other side never touched: keep the superset").
  Resolved with `git checkout --theirs` + `git add --sparse` (path is outside the
  sparse-checkout cone per DOC-986 §2/§4.1 — the conflict existed only in the index,
  the working-tree copy carried no conflict markers).

The two sides are byte-identical except for a single added frontmatter field. Diff of
stage 2 (ours, `60d51be`) vs stage 3 (theirs, `9c46d77`):

```
@@ -22,6 +22,7 @@ fields:
   version: 0.0.184
   story_points: 2
   bundled_in: bundle-4ff83a8b
+  chat_comment: comment-b6fd1b75
 ---
```

No competing edit on either side: identical `status: free_and_reconciled`, identical
`updated_at`, identical `fields.commits`/`version`/`bundled_in`, identical body
(scope, root cause, fix, test plan, acceptance). Taking the incoming superset
discards nothing from the HEAD side, so no per-fact timeline arbitration (2e's
`xgd working-timeline`) was needed.

## Incoming changes preserved

- `.xgd/tickets/hot/bug-61f43435.md` — the incoming commit `591d9f5` ("xgd(ticket):
  update bug bug-61f43435") registers as +84 lines because the file is a whole-file
  add on that side (AA). Its only change relative to HEAD's version is the added
  `fields.chat_comment: comment-b6fd1b75`, verified present at line 25 of the
  resolved file. Staged diff vs HEAD is exactly `1 file changed, 1 insertion(+)`,
  matching the incoming intent precisely.

No hunks were dropped; the BUG-1301 precedence exception was not invoked. No code
files, no UAT test files, and no spec tickets (story/acceptance_criterion/capability)
were involved in this conflict — the sole conflicted path is a bookkeeping bug ticket.

Cherry-pick sequencer state (`CHERRY_PICK_HEAD` = `591d9f55ef94bd64a5c2e3009c324b5cdcbbf2c7`)
left intact for `cherry_pick_finalize_resolution`. `git status --porcelain` reports no
remaining UU/AA/DU/UD/AU/UA entries.

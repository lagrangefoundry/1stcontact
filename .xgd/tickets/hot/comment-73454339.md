---
uid: comment-73454339
id: COMMENT-1755
type: comment
title: Claude response for reconciliation_uat_generation report
created_by: xgd
created_at: '2026-08-31T11:28:03.721400+00:00'
updated_at: '2026-08-31T11:28:03.721400+00:00'
completed_at: null
last_field_updated: created_at
status: null
fields:
  subject_uid: report-8625c0d7
  kind: note
---

Done.

```
UATs generated for story story-e15a19ef (plan item 6 of 11)

Story: story-e15a19ef
Test file(s) created:
  - tests/reconciliation-1c-launcher-bootstrap.test.ts

Tests written: 3
Tests passed: 3
Tests failed: 0

test_files_created:
  - "tests/reconciliation-1c-launcher-bootstrap.test.ts"
```

**Coverage decision.** Twelve of the story's fifteen ACs already have UATs in four sibling files (`reconciliation-1c-cli-output-hygiene`, `-astro-free-render`, `-install-preflight`, `-aligned-crops-sandbox-routing`). Since modifying existing test files is prohibited, the new file covers exactly the three uncovered ACs — AC-1415, AC-1416, AC-1417 — one test each, and its header points at the siblings for the rest.

**What each test proves**

- **`test_UAT_AC1415_…`** — reads the launcher's *executable* lines (comments stripped; its header is three paragraphs about Astro): `createServer` imported from `vite` by name, `configFile: false`, middleware mode with `hmr`/`ws` off and `logLevel: 'error'`, and no `getViteConfig`, `astro`, `createRequire` or `import.meta.resolve`. Then confirms `tools/generate` declares `vite` under `dependencies` (not `devDependencies` — the launcher imports it at run time) and that a real `1c list` through the real binary boots and exits 0. The AC is explicit that a clean boot alone proves nothing, so the config is the evidence and the run only confirms it works.
- **`test_UAT_AC1416_…`** — enumerates manifests from `pnpm-workspace.yaml`'s own globs rather than a fixed list (per the story's 2026-08-31 reconciliation decision), parses the lockfile's `importers` with `yaml`, strips comments from both Vitest project configs and every tsconfig, and reuses the existing `expectNoAstroContainerToConstruct` helper for the off-disk check. Non-vacuity is pinned both ways: the scan must have found the root plus real members, and must still show `@astrojs/markdown-remark` and `vite` surviving.
- **`test_UAT_AC1417_…`** — walks the cycle link by link (barrel → `./builder` → `control-app/src/router` → `./chrome` → `./generated/importmap.json`) and proves the map really is uncommitted via `git ls-files`, so the branch isn't dead code. Then pins the dispatch *order* — the `assets` load sits inside the branch, the barrel load strictly after `} else {` — which is the only observable form on a populated checkout, as the AC notes. Finally runs the real binary in both modes and parses `--json` as one document with the report's real keys.

**One thing worth flagging.** `1c assets` empties `apps/control-app/dist-assets` before rebuilding, and sibling suites serve that same directory over a live builder origin — running it in place would fail them for unrelated reasons. The test therefore runs it against a symlinked mirror root that owns every path the build writes. Verified: `git status` shows only the new file, and `dist-assets` is intact.

Regression scope — the new file plus all four siblings and both `dist-assets` consumers, run together: **7 files, 25 tests, all passing.** That run also exercised AC-720 and AC-657, whose `uat_coverage` fields still read `fail`; both passed here, so those markers look stale. I left the field alone — it's owned by the coverage check, not by this step.

`eslint` is listed in `quality.yaml` but isn't installed in this checkout and the registry is blocked offline, so the lint gate could not be run. No tsconfig includes `tests/`, so there's no `tsc` gate on test files either; Vitest's transform is the available signal.

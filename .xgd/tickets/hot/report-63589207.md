---
uid: report-63589207
id: REPORT-3460
type: report
title: 'Reconcile resolve conflicts: reconcile-BUNDLE-23'
created_by: xgd
created_at: '2026-09-04T01:57:42.486748+00:00'
updated_at: '2026-09-04T01:57:42.486748+00:00'
completed_at: null
last_field_updated: created_at
result: pass
fields:
  report_kind: resolve_conflicts
  subject_uid: reconcile-BUNDLE-23
---

## Files resolved

- `tools/generate/src/cli/kb.ts` — class **UU** (code file, rule **2c.3c**: overlapping,
  mutually exclusive change → INCOMING WINS).

  The conflict was a single hunk of one line. Stage comparison:

  - base (`:1:`) — `import { CORPUS_TYPE, SHIPPED_SOURCE, SYSTEM_KB } from './kb-model'`
    at line 57; no `kb-projection` import.
  - ours (`:2:`, HEAD) — added the `kb-projection` import **and** a verbatim second
    copy of the `kb-model` import immediately after it.
  - theirs (`:3:`, incoming `53374730fa`) — added the `kb-projection` import only.

  Both sides added the `kb-projection` import identically, so it merged clean as
  common context. `git diff` between the ours and theirs stages is exactly
  `1 file changed, 1 deletion(-)` — the duplicate `kb-model` import is the entire
  conflict.

  Resolved by taking the incoming side, i.e. dropping the duplicate. Two independent
  reasons agree:

  1. **Rule 2c**: incoming is the `free_coded` side and is authoritative for code.
  2. **The ours-side line is merge-resolution churn, not authored intent.**
     `git log -S` on that import line returns only two commits:
     `700f06214b feat(kb): the system knowledge base reaches the Worker [FREE-CODED]`
     (which introduced the single, correct import) and
     `4c03ea08af Merge branch 'xgd-working' into free-REQ-165` — the OURS-side merge
     named in this conflict's enrichment metadata. The duplicate was manufactured by
     that merge's resolution when the `free-REQ-165` side (adding `kb-projection`) was
     combined with the `xgd-working` side (already carrying `kb-model`). No developer
     commit ever authored it.

  It is also a compile error on its face: re-importing `CORPUS_TYPE`,
  `SHIPPED_SOURCE` and `SYSTEM_KB` from the same module in the same file is TS2300
  duplicate-identifier. Keeping the ours side would have carried a broken build
  forward.

  Note: the enrichment metadata's stated rule ("intent unknown on one or both sides;
  take the more recent commit by timestamp and flag for post-merge review") points the
  same way without needing the timestamp tiebreak — the incoming commit is also the
  later of the two (2026-09-01T11:31:12-07:00 vs HEAD's 11:30:43-07:00). Flagging it
  here as that rule asks, though the provenance above makes this an unambiguous
  de-duplication rather than a genuine intent contest.

## Incoming changes preserved

Fully preserved — the resolved file is **byte-identical** to the incoming stage
(`git diff --no-index` against `:3:` reports no differences), so every change carried
by `53374730fa` is present by construction.

Spot-checked the merge's substantive contribution (`--stat` for that path:
171 insertions, 3 deletions — the doc-projection feature). All of it is in the
resolved file:

- line 58 — `import { isProjected, projections, type ProjectedDoc } from './kb-projection'`
- line 392 — `!isProjected(name) &&` guard
- line 438 — `for (const doc of projections())` export loop
- line 449 — `.filter((name) => isProjected(name) && !written.has(name))`
- line 522 — `doc: ProjectedDoc` parameter
- line 1131 — `projected: files.filter((f) => isProjected(f)).length` stat

No hunks were dropped; the BUG-1301 precedence exception was not invoked and no test
functions were touched.

## Staging

`git status --porcelain` reports `M  tools/generate/src/cli/kb.ts` — no conflict
classes remain, and no other path in the tree was modified. `CHERRY_PICK_HEAD` is
still present at `53374730fa37ddba48fa2c04e33fe6814052af85`; no cherry-pick state
transition was performed.

---
uid: report-96b81521
id: REPORT-4432
type: report
title: 'Reconcile resolve conflicts: reconcile-BUNDLE-27'
created_by: xgd
created_at: '2026-09-19T12:51:35.994715+00:00'
updated_at: '2026-09-19T12:51:35.994715+00:00'
completed_at: null
last_field_updated: created_at
result: pass
fields:
  report_kind: resolve_conflicts
  subject_uid: reconcile-BUNDLE-27
---

## Files resolved

- `package.json` — UU (config file, rule 2g scalar conflict). Sole
  conflicting hunk was the `version` scalar: HEAD `0.2.40` vs incoming
  `0.2.37`. Kept HEAD's `0.2.40`.

## Incoming changes preserved

The incoming commit `a34527e3c7` ("Merge branch 'free-REQ-172' into
xgd-working") touches exactly one file with exactly one hunk:
`package.json` `version: 0.2.36 -> 0.2.37` (verified: `git diff
8fd0841a0b a34527e3c7 -- package.json`; the second-parent diff is
empty). There is no code change in this commit.

HEAD already carries `version: 0.2.40`, a strictly later bump on the
same scalar. The incoming bump is version bookkeeping that HEAD has
already superseded, not developer code — taking HEAD's higher version
is rule 2g's "don't lose values without a clear reason" resolved in
favour of the newer release number, and matches the standing reconcile
convention for package.json version scalars.

Because the commit's only content was that superseded bump, the staged
tree nets to no diff vs HEAD. Per STEP 4 this is expected and not a
failure: STEP 3's discard test passes — the incoming intent (advance
the package version past 0.2.36) is present in HEAD via a later route
(0.2.40), rather than absent. `git cherry-pick --skip` was NOT invoked;
the tree is staged and CHERRY_PICK_HEAD is left intact for
cherry_pick_finalize_resolution.

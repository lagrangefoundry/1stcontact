---
uid: report-a9854622
id: REPORT-4212
type: report
title: 'Reconcile resolve conflicts: reconcile-BUNDLE-27'
created_by: xgd
created_at: '2026-09-14T03:32:23.548876+00:00'
updated_at: '2026-09-14T03:32:23.548876+00:00'
completed_at: null
last_field_updated: created_at
result: pass
fields:
  report_kind: resolve_conflicts
  subject_uid: reconcile-BUNDLE-27
---

## Files resolved

- `.xgd/tickets/hot/request-8b33a146.md` — UU, sparse-excluded (index-only conflict,
  no working-tree markers). Rule 2e (intent/bookkeeping ticket, `request-*`):
  strict-superset + later-intent case. Resolved to OURS via
  `git checkout --ours --ignore-skip-worktree-bits` + `git add --sparse`.

  Incoming commit is 8760eac (`xgd(ticket): update request request-8b33a146`,
  2026-09-01T14:34:31, +12/-4) — a pure frontmatter bookkeeping advance: it adds
  `fields.commits` (two working_sha entries: eb6655c, 324b55b), `fields.version:
  0.2.37`, and moves `status: free_coding -> free_coded`. Body text untouched
  (only a trailing-newline flip).

  Three-stage comparison shows ours vs theirs differ in exactly three lines:
  - `updated_at`: ours `2026-09-11T18:53:54` vs theirs `2026-09-01T21:34:31`
  - `status`: ours `bundled` vs theirs `free_coded`
  - `fields.bundled_in: bundle-8e1807f6` — present on ours, absent on theirs

  Ours is the later-positioned intent on both contested facts, and `bundled` is
  the downstream state of `free_coded` (this bundle, bundle-8e1807f6, is what
  advanced it). Taking theirs would have rewound the ticket's status and dropped
  the `bundled_in` backlink recorded by this very reconcile run.

## Incoming changes preserved

Everything 8760eac actually adds is already present in HEAD verbatim, confirmed
by direct comparison rather than assumption — `diff ours theirs` shows the
`fields.commits` block (both working_sha entries, with null reconcile_sha/main_sha)
and `version: 0.2.37` as CONTEXT lines, not as theirs-only additions. The only
theirs-only content in the whole file is the older `updated_at` and the earlier
`status` value, both superseded by ours.

No developer content was discarded. No hunk was dropped under the BUG-1301
precedence exception (none applied).

## Note on the staged diff

`git status --porcelain` is empty after staging: this commit's effect already
landed in HEAD through the seeded local overlay plus this bundle's own status
advance, so the resolution is genuinely redundant rather than discarded — STEP 3's
test lands on "incoming changes present in HEAD". Per STEP 4 no `--skip` was
issued; CHERRY_PICK_HEAD is intact (8760eac) for
`cherry_pick_finalize_resolution`.

This is the second consecutive commit on this ticket to resolve this way (the
prior attempt resolved 4305ac9, the body rewrite, identically) — consistent with
the overlay having seeded the ticket's full later state ahead of the cherry-pick
sequence.

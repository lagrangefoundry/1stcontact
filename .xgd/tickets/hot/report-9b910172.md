---
uid: report-9b910172
id: REPORT-3872
type: report
title: 'Fix Page Authoring Through The Control Surface: Read & Replace The Element
  Tree (ac) - attempt 2'
created_by: xgd
created_at: '2026-09-11T02:20:49.221294+00:00'
updated_at: '2026-09-11T02:20:49.221294+00:00'
completed_at: null
last_field_updated: created_at
fields:
  report_kind: fix_structural_validation
  subject_uid: capability-fe236246
  level: ac
  fixes_applied: 4
  progress_made: true
  needs_more_work: false
  violations_remaining: 0
  anchor_report_uid: report-e37a6b4a
---

# Fix Summary — Page Authoring Through The Control Surface: Read & Replace The Element Tree (ac)

**Attempt**: 2
**Fixes applied this call**: 4
**Violations remaining**: 0
**Needs more work**: false

The single violation (finding 1, `ac-edit` on AC-1090) and its paired cascade (finding 2,
`uat-edit`) are both resolved, together with the one stale sentence the pair left behind in
STORY-106's body. Findings 3–6 are `info` with an explicit "none" resolution in the
validation report and were not acted on.

## Actions Taken — by Resolution Category

| # | Category | Element | Action |
|---|---|---|---|
| 1 | ac-edit | AC-1090 (`acceptance_criterion-4bd36a69`) | Deleted the *"Known limit, deliberately recorded"* paragraph. Criterion now states three things carried **together** — the failure code, the recovery strategy (nothing written / do not resend unchanged / read the element back) **and** the offending field named as a pointer into the element — with an explicit sentence that the two are complementary, not alternatives. Verification strengthened to assert the pointer alongside the three strategy assertions |
| 2 | ac-edit | AC-1090 (`acceptance_criterion-4bd36a69`) | Title aligned with the strengthened body: *"A refused change tells the caller nothing was written, **which field was wrong**, and what to do instead of resending it"*. The previous title described only the strategy half |
| 3 | uat-edit | `tests/test_UAT_FC_REQ-129_l1_authoring.test.ts:374-401` (`test_UAT_FC_REQ_129_a_refusal_is_correctable_within_the_turn`) | Added `expect(answer).toMatch(/fontSizePx/)` and deleted the "Asserting the pointer is a strengthening of AC-1090 that has not been made yet" paragraph, replacing it with a statement of the now-current contract (both halves asserted) |
| 4 | story-body-edit | STORY-106 (`story-189fc1ac`) | The divergence bullet's closing sentence claimed the strengthening was *"an outstanding matrix edit, not a missing behaviour"* — true when written this morning, false once mutation 1 landed. Rewritten to record the bullet as the history of why the declared meaning is worded as it is, and to state that the AC now asserts both halves. Nothing else in the body touched; the recorded closure, the vestigial-`nav` bullet, In/Out of scope and Technical Context are byte-identical |

Mutations 1, 3 and 4 were applied in the same call so the matrix is never left in a state
where the AC asserts the pointer but the story body says it does not, or vice versa.

## Verification

`npm test -- tests/test_UAT_FC_REQ-129_l1_authoring.test.ts tests/test_UAT_FC_REQ-126_l1_surface.test.ts`
→ **27 passed (27), 2 files passed**. The strengthened assertion passes, which is the
empirical half of the evidence the validation report could only read statically (that
report explicitly notes the suite was not executed in its session). The pointer does reach
a Toolbox caller today.

The neighbouring CAP-92 surface suite (REQ-126) was run as a regression check because
finding 1's evidence chain runs through the shared surface declaration; nothing regressed.

## Evidence re-read this cycle (not carried from the report)

Per the validation report's own warning that `@lagrangefoundry/ai` is unpinned and must be
re-read rather than trusted, the chain was walked again in this session:

| Link | Read | Says |
|---|---|---|
| `node_modules/@lagrangefoundry/ai/src/toolbox/declaration.js:448,463` | `hostDetail: flag(where, declared, 'host_detail', true)`, with a comment naming it opt-*out* | detail is on unless a code opts out |
| `node_modules/@lagrangefoundry/ai/src/toolbox/runtime.js:493-495` | `const detail = declared.hostDetail ? usableDetail(...)`; renders `Error: <tool> failed (<code>). <declared meaning> The host reports: <detail>` | strategy and detail are appended, not swapped |
| `.../runtime.js:549-558` (`usableDetail`) | returns `path` even when the message text adds nothing — *"a useless message must not take one with it"* | the pointer survives independently of the message |
| `tools/generate/src/cli/ai/l1-surface.json:124-126` | `SCHEMA_INVALID` declares a `message` only, no `host_detail` key | this surface does not opt out |
| `tools/generate/src/cli/edit.ts:215-226` (`validateOrThrow`) | throws `CommandError` with `message: \`${first.path}: ${first.message}\`` and `path: first?.path` | the JSON pointer is what is thrown |
| `tools/generate/src/cli/edit.ts:800-816` | already `UPSTREAM FINDING, CLOSED` | the code comment was the third of four places, already moved |

## Code Edits (if any)

None. No production source was modified — one test assertion and comment, two ticket
bodies, one ticket title.

## needs_review Items Forwarded

None.

## Notes carried forward

- The validation report's finding 5 (AC-1093 + AC-1094 vs REQ-139) is flagged there as
  "the likeliest false positive for a future cycle". Both ACs were re-read this call and
  the report's reasoning holds: AC-1093 is kind-relative and AC-1094 asserts an empty list,
  which a never-hide-a-row rule cannot make non-empty. No edit made, deliberately.
- The report's traceability note stands unrepaired by design: CAP-93 carries no
  `intent_uid` and STORY-106's points at BUNDLE-17, so establishing REQ-129's governance
  still costs a dereference of eight bundled requests. That is a tooling-level concern,
  out of scope for an `ac`-level fix.

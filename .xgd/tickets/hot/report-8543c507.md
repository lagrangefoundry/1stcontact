---
uid: report-8543c507
id: REPORT-3651
type: report
title: 'Fix Site Locale Identity: Where A Business Is, And What The Page Declares
  (ac) — attempt 1'
created_by: xgd
created_at: '2026-09-10T03:39:08.685665+00:00'
updated_at: '2026-09-10T03:39:08.685665+00:00'
completed_at: null
last_field_updated: created_at
fields:
  report_kind: fix_structural_validation
  subject_uid: capability-bcbcdaf1
  level: ac
  fixes_applied: 4
  progress_made: true
  needs_more_work: false
  violations_remaining: 0
  anchor_report_uid: report-e37a6b4a
---

# Fix Summary — Site Locale Identity: Where A Business Is, And What The Page Declares (ac)

**Attempt**: 1
**Fixes applied this call**: 4
**Violations remaining**: 0
**Needs more work**: false

Both findings were the same shape, as the assessor's notes predicted: STORY-122
states a behaviour as a deliberate *decision*, and the AC tree pinned the half
that was easy to assert. Both repairs were additive `ac-edit`s — no AC deprecated,
no story body edited, no code changed.

## Actions Taken — by Resolution Category

| # | Category | Element | Action |
|---|---|---|---|
| 1 | ac-edit | AC-1428 (`acceptance_criterion-1bd39b81`) | Extended criterion + verification to state the *whole* undeclared resolution — region-free language `en` **and** country/currency/timezone falling to the default country's row (`US` / `USD` / `America/New_York`) — and named why the two halves differ (no region-free currency, no region-free clock; only the language attribute is what a screen reader pronounces and a search index stores). Verification now asserts the locale is *not* the default country's `en-US`, so the asymmetry is pinned as two behaviours rather than one. |
| 2 | ac-edit | AC-1433 (`acceptance_criterion-64eb8985`) | Added the permissive complement to the refusal list: a well-formed BCP 47 tag whose language subtag is unrecognised **validates and resolves**. Kept the country/currency contrast explicit (an unsupported country stays refused — the platform must derive from it and cannot). Verification now closes the boundary from both sides, so a later tightening to registry membership fails this AC. |
| 3 | uat-edit | `test_UAT_FC_REQ-151_a_site_declaring_no_locale_renders_exactly_as_before` | Was asserting only `resolveSiteLocale({}).locale` (exactly the cascade the assessor flagged at `tests/test_UAT_FC_REQ-151_site_locale.test.ts:126-134`). Now asserts the full resolved object against `DEFAULT_COUNTRY`'s row plus a literal `US`/`USD`/`America/New_York` check, and that the locale is not `COUNTRY_DEFAULTS[DEFAULT_COUNTRY].locale`. Real entry point, no mocks. |
| 4 | uat-add | `test_UAT_FC_REQ-151_an_unregistered_but_well_formed_language_tag_validates` | New test for AC-1433's added half: `qz`, `qz-IE`, `zxx`, `tlh-Latn-US` each pass `validateSite` and survive `resolveSiteLocale` unchanged (accepted, not accepted-then-rewritten). Real entry points. |

Both ACs' `uat_coverage` was left untouched — that field is owned by
`check`/`fix_uat_coverage`, not by this prompt.

## Verification

`npm test -- tests/test_UAT_FC_REQ-151_site_locale.test.ts` → **1 file passed,
10 tests passed** (was 9; the added test is the tenth). No previously-passing
test regressed. The wrangler `EPERM` log-write noise in the output is a sandbox
artifact, not a test failure.

Every value written into the two ACs was checked against the landed
implementation first, not inferred from the report:

- `packages/site-schema/src/locale.ts:133` — `DEFAULT_COUNTRY = 'US'`
- `packages/site-schema/src/locale.ts:150` — `UNDECLARED_LOCALE = 'en'`
- `packages/site-schema/src/locale.ts:267-281` — `resolveSiteLocale` takes
  `declared` (not `country`) for the locale, which is precisely the asymmetry
  AC-1428 now states
- `packages/site-schema/src/locale.ts:170-179` — `LANGUAGE_TAG` is pure
  shape-matching, with a comment saying well-formedness-not-registry-membership
  in the same words STORY-122's Technical Context uses

## Code Edits (if any)

None this call. Both findings resolved in the matrix and its tests; the
implementation already had the intended behaviour in both cases.

## Finding 3 (info)

No action, as recorded by the assessor: the overlap between AC-1428's
stored-sites sweep and AC-1437's closing line is deliberate and cheap. The
AC-1428 edit did not touch that paragraph.

## needs_review Items Forwarded

None. The report carried `needs_review_count: 0` and nothing in this call
reached an ambiguity requiring an operator decision.

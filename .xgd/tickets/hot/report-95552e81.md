---
uid: report-95552e81
id: REPORT-994
type: report
title: 'Sync-main review: reconcile-BUG-5'
created_by: xgd
created_at: '2026-07-27T21:09:53.140045+00:00'
updated_at: '2026-07-27T21:09:53.140045+00:00'
completed_at: null
last_field_updated: created_at
result: pass
fields:
  report_kind: sync_main_review
  subject_uid: reconcile-BUG-5
---

{
  "findings": [
    {
      "risk": "OK",
      "file": "tools/generate/src/cli/repro.ts",
      "description": "Briefing marked this '(deleted)' but it is a branch ADDITION, not a resurrection. Verified: `git log main -- <path>` is empty (main's lineage never contained the file) and `git log --diff-filter=D --all -- <path>` is empty (never deleted on any ref). Added by branch commit 04be895dc (REQ-88 L1 repro pipeline). The briefing's marker reflects diff direction, not a main-side deletion. Wired into cli/index.ts:30 and typechecks clean."
    },
    {
      "risk": "OK",
      "file": "tests/req88-l1-repro-pipeline.test.ts",
      "description": "Same as above - briefing '(deleted)' is a diff-direction artifact. Main's lineage never touched the path and no deletion commit exists on any ref; added by 04be895dc. Not a deletion revert."
    },
    {
      "risk": "OK",
      "file": "tests/req55-content-width.test.ts",
      "description": "Branch removes UAT test_UAT_FC_REQ-55_gigabytealchemy_most_apps_width (added by main commit 3d339ad0c, removed by branch commit 04be895dc). Verified deliberate, not an over-aggressive conflict resolution: an explicit NOTE (REQ-88) at line 116 documents the removal, the fixture it read (storage/sites/gigabytealchemy/draft/pages/home.json) is intentionally deleted by the same REQ-88 work, and the resolver behaviour it exercised (4xl -> 896px token, literal px) remains covered by test_UAT_FC_REQ-55_contentWidth_literal_px without a site fixture. The sibling test asserting docs.length > 0 over storage/sites still holds (14 JSON docs remain)."
    },
    {
      "risk": "OK",
      "file": "packages/site-schema/src/schema.ts",
      "description": "Removals are additive rewrites, not reverts. l1NodeSchema import is retained (widened to also import l1DocumentSchema); modules: z.array(moduleInstanceSchema) is retained with .default([]) plus an optional l1 and an XOR superRefine; main's existing duplicate-module-id superRefine is preserved intact."
    },
    {
      "risk": "OK",
      "file": "tools/generate/src/render/render.ts",
      "description": "The removed renderModules call is preserved as the else-branch of the new L1 conditional (l1 ? l1.html : await renderModules(...)). Main's module-rendering path is intact, not dropped."
    },
    {
      "risk": "OK",
      "file": "tools/generate/src/l1/probes.ts",
      "description": "The removed byText map is replaced by FIFO occurrence-index pairing - this is the substantive BUG-5 fix (duplicate text keys previously collided so only the last box survived), not a regression. Documented in the updated docstring."
    },
    {
      "risk": "OK",
      "file": "tools/generate/src/cli/commands.ts",
      "description": "Only change is visibility widening of ctxOf (private -> exported) for use by repro.ts. The duplicate private ctxOf in cli/edit.ts:42 is pre-existing on main (confirmed via git show main:...), not introduced by this rebase."
    }
  ]
}

Verification performed:
- No unresolved conflict markers in any tracked file (grep over .py/.yaml/.yml/.ts/.tsx/.json/.md).
- Rebase complete; no rebase-merge/ or rebase-apply/ directory; main is an ancestor of HEAD.
- Conflict file .xgd/tickets/hot/bug-5b7153d2.md resolved clean (0 markers).
- Deletion-revert sweep: only code files the branch deletes vs main are the 4 gigabytealchemy module-era storage docs (deliberate REQ-88); only code files added are repro.ts and the req88 test (neither ever existed on or was deleted from main).
- No dangling functional references to gigabytealchemy (remaining hits are comments and slug-string CLI-arg-parsing tests).
- Full suite: 578/578 tests passed across 82 files.
- Typecheck: all 5 packages (site-schema, framework, generate, public-site, control-app) clean, exit 0.

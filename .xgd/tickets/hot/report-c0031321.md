---
uid: report-c0031321
id: REPORT-3746
type: report
title: 'Fix L1 Reproduction Pipeline: Fold & Acceptance Gate (ac) — attempt 8'
created_by: xgd
created_at: '2026-09-10T14:43:37.661101+00:00'
updated_at: '2026-09-10T14:43:37.661101+00:00'
completed_at: null
last_field_updated: created_at
fields:
  report_kind: fix_structural_validation
  subject_uid: capability-2049c9ec
  level: ac
  fixes_applied: 8
  progress_made: true
  needs_more_work: false
  violations_remaining: 0
  anchor_report_uid: report-e37a6b4a
---

# Fix Summary — L1 Reproduction Pipeline: Fold & Acceptance Gate (ac)

**Attempt**: 8
**Fixes applied this call**: 8
**Violations remaining**: 0
**Needs more work**: false

All three violations and all four warnings from REPORT-3745 (`report-fd81bfc6`,
2026-09-10T14:37) are closed this call. Nothing in that report had been repaired
before this call — the only fix reports since it was written are story-level
(REPORT-3741 / REPORT-3743), so I treated every finding as live and re-verified each
code claim in this worktree before acting on it.

## Actions Taken — by Resolution Category

| # | Category | Element | Action |
|---|---|---|---|
| 1 | ac-add | **AC-1630** (`acceptance_criterion-2c02ca04`), STORY-86 | Finding 1 (violation, **fifth raise**). Authored *"A pinned box whose flow interior outgrows its keyframe height is reported as a content-overflow violation"*: kind `clip`, detail naming content height vs pinned height, index path of the offending node; raised under **both** envelope probes (off-sample at intermediate widths, content-robustness at every captured width); no violation when the interior fits, when the node pins no height, or when the excess is within the evaluator epsilon. Verified against `probes.ts:405-416` and the `evaluateLayout` docstring `probes.ts:429-433`. |
| 2 | ac-add | **AC-1631** (`acceptance_criterion-12581128`), STORY-84 | Finding 2 (violation, new at this level). Authored *"The fold derives each text run's nowrap threshold as a width from the ladder's single-line suffix"*, stating all three derivation rules the story body spells out: a **width not a flag**; the **suffix** rule (one line at 1024 but two at 1280 → the higher rung); an unmeasurable line count **breaks the suffix** rather than reading as one line; and no axis at all for a run that never rested on one line. Renderer half explicitly deferred to CAP-70 / STORY-83 / AC-1010. Verified against `fold.ts:215-220` (`lineCountOf`), `fold.ts:233-240` (`nowrapThreshold`), `fold.ts:1843-1844`, `packages/site-schema/src/l1/schema.ts:983`. |
| 3 | ac-edit | **AC-731** | Finding 3 (violation) + warning 7. Replaced the false page-base opening: the document background band is now stated as the fill covering the **greatest total band height across reconstructed bands and captured backdrops alike**, with the most-common run fill as the fallback used only when no full-bleed band was reconstructed and the captured canvas fill as the last resort. The backdrop's own contribution is delegated to AC-812 rather than restated, ending the AC-731/AC-812 contradiction. Verification rewritten to assert against band height (plus a taller-band-wins case, a no-band fallback case and a canvas last-resort case) instead of run count. Warning 7's **BUG-19 full-bleed bar rule** added in the same edit: distributed narrow same-fill runs spanning the content width seed a band, an evenly-gapped tile row stays cards. Verified against `fold.ts:2105-2146` and `fold.ts:1384-1397` / `:2059-2070`. |
| 4 | story-body-edit | **STORY-84** | Companion to #3 and warning 7 — the edit the assessor named as the omission that carried this finding through four cycles. The reconstructed-surfaces bullet said "the solid fill the most runs sit on becomes the document background band" while the backdrop bullet said "its fill counts toward the page-base inference": the body was internally inconsistent about one decision and so could not arbitrate its own ACs. Rewritten to the extent-measured rule with both fallbacks in order, and the bar rule added to the same bullet. **In scope** updated: "the full-bleed-bar band rule" and "the extent-measured page-base inference" added. Nothing else in the body touched. |
| 5 | ac-edit | **AC-710** | Warnings 4 + 5 (**fourth raise**), resolved as one edit as the assessor suggested. Narrowed to the **envelope-finding diagnostic contract** it uniquely owns (kind / magnitude-bearing detail / index paths), with the kind vocabulary extended to cover finding 1's third trigger — `clip` now covers both the viewport-edge crossing and the pinned-box content overflow. The duplicated fidelity residual *shape* is delegated to AC-705, which owns it and from which AC-710's copy had already diverged (warning 4's stale text-only wording); AC-710 retains only the claim that a fidelity residual is diagnostic, in whatever labelling AC-705 defines. One authority per report shape. |
| 6 | ac-edit | **AC-729** | Warning 6 (**third raise**). Narrowed to what a *foldable* media element emits (leaf, source, alt text, geometry, axes). The source-less / geometry-less negative case and its prescribed test — specified and verified identically by AC-733, which owns the residual channel — replaced by an explicit cross-reference. Verification's duplicate "fold a source-less media element" step removed. |
| 7 | ac-edit | **AC-706** | Supporting finding 1. Its "exactly when" clause enumerated only two of the evaluator's three envelope violations, which would have left the tree self-contradictory the moment AC-1630 landed. Now names all three and delegates the third's contract to AC-1630; the bullet generalised from "sibling overlap or horizontal clip" to "any envelope violation". No other change. |
| 8 | ac-edit | **AC-707** | Same repair on the content-robustness side, noting that this probe's perturbation is the usual trigger for the overflow violation. No other change. |

## Consistency at the call boundary

Each paired edit landed in the same call as its dependency, so the matrix is not left
in an inconsistent intermediate state:

- AC-1630 (new third violation) + AC-706 + AC-707 + AC-710 — the new violation, the
  two probes' "exactly when" clauses, and the finding vocabulary that describes it.
- AC-731 (page-base + bar rule) + the STORY-84 body bullet that stated the old rule
  and lacked the bar rule.
- AC-729 narrowed only after confirming AC-733 states and verifies the removed case
  in full.

Neither new AC's `uat_coverage` was set: that field is owned by the UAT-coverage
check/fix pass, and setting it here would manufacture coverage the ACs do not yet
have. Both were created with the store's default `pending` status, matching
AC-1625–AC-1629 authored earlier today (per the report's info 9, `pending` is
authorship state, not retirement).

## Code Edits (if any)

None this call. Every finding was resolvable in the matrix; the code was read only as
evidence. No product source, test or fixture was modified, so no test run was
implicated by this call's changes.

## Verification performed

Every code claim I relied on was read in this worktree (`regression-800a17f7`) before
the corresponding edit, not taken from the report:

| Claim | Read at | Confirmed |
|---|---|---|
| Pinned-box content-overflow `clip` finding exists and names its magnitude | `tools/generate/src/l1/probes.ts:405-416` | yes — pushes `{kind:'clip', detail:'content height Npx exceeds pinned box height Mpx', paths:[path]}` past `opts.epsilonPx` |
| The evaluator declares three violations | `probes.ts:429-433` | yes — docstring names "sibling overlap, horizontal clip beyond the viewport, and pinned-box content overflow" |
| Nowrap threshold is a backward suffix scan broken by an unmeasurable line count | `fold.ts:233-240`, `fold.ts:215-220` | yes — `break` on `lineCountOf(...) !== 1`; `lineCountOf` returns `undefined` on missing/non-finite line height or glyph extent |
| The threshold is carried as an optional axis | `fold.ts:1843-1844`, `site-schema/src/l1/schema.ts:983` | yes — `axes.nowrapFromPx` set only when defined |
| Page base is greatest total band height over bands **and** backdrops, run count is the fallback, canvas the last resort | `fold.ts:2105-2146` | yes — `bandHeightByFill` over `[...bandNodes, ...backdropNodes]`; run-count block guarded by `if (!band)`; `bodyBackground` guarded by a second `if (!band)` |
| BUG-19 bar rule is live | `fold.ts:1384-1397`, `fold.ts:2059-2070` | yes — `barBandFills` (full-width union AND dominant internal gap); `if (isBar) bandRows.push(r)` |
| AC-733 fully owns the case removed from AC-729 | ticket store, this run | yes — states *and* verifies the source-less/geometry-less media residual |

Post-edit state read back from the store: STORY-86 now carries 17 ACs (was 16),
STORY-84 now 24 (was 23); AC-1630 and AC-1631 resolve under the intended stories;
AC-731's stored body carries the new page-base clause.

## Left deliberately unrepaired

| Element | Why |
|---|---|
| AC-736 (report **info 8**) | The assessor's own resolution is "none at this level". AC-736's broad backing-surface exclusion is faithful to STORY-86's body, which states the same broad rule, while the code excludes only fold-synthesized surfaces (`isSynthesizedSurfaceId`, `probes.ts:460-474`). Tightening the AC alone would create fresh AC-vs-story drift in place of the current story-vs-code drift. Flagged for the story-level editor, unchanged. |
| Infos 9, 10, 11 | Explicitly "none" in the report: `pending` status is a uat-level question; the AC-1625/AC-1626 padding overlap is a recorded altitude split, not drift; REQ-114 and REQ-154 owe this capability no AC. |

## needs_review Items Forwarded

None. No finding in REPORT-3745 was categorized `needs_review`, and none of the three
violations required a judgement call the tickets could not arbitrate — finding 3's
single escalation to implementation was already performed and documented by the
assessor, and I re-verified it at `fold.ts:2105-2146` before restating the rule.

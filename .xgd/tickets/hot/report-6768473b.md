---
uid: report-6768473b
id: REPORT-3173
type: report
title: 'Reconcile resolve conflicts: reconcile-REQ-162'
created_by: xgd
created_at: '2026-09-01T01:49:23.837530+00:00'
updated_at: '2026-09-01T01:49:23.837530+00:00'
completed_at: null
last_field_updated: created_at
result: pass
fields:
  report_kind: resolve_conflicts
  subject_uid: reconcile-REQ-162
---

## Files resolved

- `.xgd/tickets/hot/bug-db356ff8.md` — **UU**, intent/bookkeeping ticket (rule
  **2e**), path outside the sparse-checkout cone (DOC-986 §2/§4.1), so resolved
  via `git checkout --ours` + `git add --sparse`.

  Blob stages: base `863e47ac`, ours `3e669318`, theirs `8f92f712`.
  Resolution staged: `3e669318` (ours) at stage 0.

  **Per-fact analysis** (`git diff <ours> <theirs>` — the whole delta, nothing
  elided):

  | Fact | Base | Ours (HEAD) | Theirs (incoming) | Resolution |
  |---|---|---|---|---|
  | Body: `## Status` rewrite + entire `# Implementation — the tenant fix` section (112 lines) | absent | **present** | **present** | identical on both sides — not a conflict |
  | `updated_at` | `2026-08-23T23:42:40` | `2026-08-26T17:36:27` | `2026-08-24T01:48:23` | same fact, both changed → later-positioned wins → **ours** |
  | `status` / `last_field_updated` | `draft` / `body` | `bundled` / `status` | untouched (`draft` / `body`) | only ours changed → **ours** |
  | `fields.story_points`, `fields.commits`, `fields.version`, `fields.bundled_in` | absent | added by bundling | never touched | only ours changed → **ours** |
  | trailing newline | — | absent | present | cosmetic; kept ours (matches the branch's other ticket blobs) |

  Ours is a strict content **superset** of theirs: the only bytes theirs carries
  that ours does not are an `updated_at` rewind (2026-08-26 → 2026-08-24), a
  `status` rewind (`bundled` → `draft`), and the loss of four bundling fields.
  Taking theirs would have discarded bundling state without recovering anything.
  Both timeline signals agree with the superset: ours' `updated_at` is later, and
  the HEAD-side commit `7a8d0abd` (2026-08-31, `seed_local_overlay`) postdates the
  incoming `5af1ff94` (2026-08-23).

## Incoming changes preserved

Confirmed. The incoming commit `5af1ff94` touched exactly one file and made
exactly two changes: bump `updated_at`, and rewrite `## Status` + append the
`# Implementation — the tenant fix` section. **Every line of that body change is
byte-identical in the staged resolution** — `git diff <ours> <theirs>` reports no
hunk anywhere in the body, only the frontmatter block and the trailing newline.
The body content reached HEAD by a different route (the `seed_local_overlay`
commit carried the same edit), so nothing was integrated away here.

No hunk was dropped; the BUG-1301 precedence exception was not invoked, and no
code or UAT files were involved.

**Note for the finalize step:** this resolution nets to *no diff vs HEAD* — the
staged blob is HEAD's blob, so `git status --porcelain` no longer lists the file
at all. This is the redundant-commit case (BUG-1109/BUG-1122), not a discard:
STEP 3's discriminator is satisfied because the incoming commit's key changes are
*present* in HEAD rather than absent. Per STEP 4 this was staged and exited
normally; `--skip` was not called, and the cherry-pick sequencer state
(`CHERRY_PICK_HEAD` = `5af1ff94`) is still present for
`cherry_pick_finalize_resolution`.

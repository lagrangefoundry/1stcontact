---
uid: bug-15a86b19
id: BUG-105
type: bug
title: 'repro-rail: process.exit truncates piped stdout past 64 KiB'
created_by: BUG-101
created_at: '2026-09-17T22:02:39.031076+00:00'
updated_at: '2026-09-17T23:08:49.972214+00:00'
completed_at: null
last_field_updated: status
status: free_coding
fields:
  severity: medium
  priority: medium
  auto_merge_back: true
  needs_review: false
  chat_comment: comment-f71b55a7
---

## Symptom

`tools/repro-console/bin/boot.mjs` ends its non-`keepAlive` path with

```js
if (!keepAlive) {
  await server.close()
  process.exit(process.exitCode ?? 0)
}
```

`process.exit()` does not flush pending asynchronous writes to stdout. stdout is
asynchronous when it is a **pipe**, so everything past the OS pipe buffer
(65536 bytes on macOS) is discarded — with no error and exit code 0. When
stdout is a **file** it is synchronous and nothing is lost, which is what makes
this invisible to anyone who redirects.

`repro-rail` is the tool on that path (`bin/repro-rail.mjs` boots with
`keepAlive: false`), and `--json` prints its entire report through it:

```js
// tools/repro-console/src/rail.ts:801
console.log(args.json ? JSON.stringify(report, null, 2) : formatRailReport(report))
```

So any consumer that spawns `repro-rail --json` with `stdio: 'pipe'` — or any
human typing `repro-rail --json | jq …` — receives a prefix of the report once
it crosses 64 KiB, and the prefix is valid-looking JSON right up to the cut.

## Why this is a separate ticket from BUG-101

This is the same defect in a second launcher. BUG-101 fixed
`tools/generate/bin/1c.mjs`, where a 71 KB `page get … --json` document made the
truncation observable today; it deliberately left this one alone rather than
change it blind. Two things were not established there and are what this ticket
has to establish:

1. **Whether the rail's report crosses the threshold.** BUG-101 recorded it as
   "under 64 KiB today", which was an inference from the report's shape, not a
   measurement. It grows with the number of sites on the rail, so the question
   is not only "does it today" but "what does it take".
2. **Whether the rail terminates without the forced exit.** BUG-101's fix
   accepts a hang in place of a truncation because a hang is loud, and it could
   afford that trade because every `1c` command was measured exiting on its own.
   The rail spawns `1c`, `pnpm` and `vitest` children and could not be measured
   in the session that found this, so the same trade cannot simply be assumed
   here.

The two packages are also deliberately not sharing a bootstrap — `boot.mjs`'s
own header explains why reaching into `tools/generate` for one is the import
direction EPIC-12 §8.6 forbids. So this is a second fix by design, not a missed
call site.

## Root cause

Same as BUG-101: an explicit `process.exit()` with output still queued.

## Fix

Presumed the same shape — set `process.exitCode` and let the process end once
the streams drain — but conditional on (2) above. If the rail does leave a live
handle after `server.close()`, the alternative is to drain both streams
explicitly and keep the forced exit:

```js
const flush = (s) => new Promise((r) => { if (s.writableLength === 0) return r(); s.write('', () => r()) })
await flush(process.stdout)
await flush(process.stderr)
process.exit(exitCode)
```

That variant was verified to carry the full document during BUG-101's
investigation; it costs the extra lines and keeps a forced exit a future reader
could extend wrongly, which is why BUG-101 did not choose it.

The `keepAlive: true` path (the console's own server) is not affected — it never
reaches the exit at all.

## Also latent, and out of scope here

`tools/generate/bin/smoke.mjs:517–525`, `bin/deploy.d/lib/probe.mjs:636–652`,
`bin/verify_req108_pointer.mjs:351` and `bin/verify_req100_reveal.mjs:170` share
the shape. All of them print a line or a short formatted table, so none is near
the threshold. Listed so the next person to survey this does not have to
rediscover them.

## Test plan

A UAT that spawns `repro-rail --json` with `stdio: 'pipe'` and asserts the
captured stdout is byte-identical to the same invocation redirected to a file —
the shape `test_UAT_FC_BUG-101_piped_stdout_not_truncated.test.ts` already uses,
including its floor assertion that the document exceeds 65536 bytes so the test
cannot pass vacuously on a small report.

The obstacle is the fixture: the rail runs real suites, so a UAT that drives it
end-to-end is neither fast nor hermetic. Resolving this is part of the ticket,
not a precondition for it. If the real rail cannot produce a large enough report
under test, the launcher can be exercised directly — `boot.mjs` takes the entry
module as an argument, so a fixture entry whose `main` prints a >64 KiB document
tests the launcher's exit behaviour without running a rail at all, and that is
the behaviour under test.

Found while fixing BUG-101.
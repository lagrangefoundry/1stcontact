---
uid: report-c5474613
id: REPORT-4341
type: report
title: 'Reconcile resolve conflicts: reconcile-BUNDLE-27'
created_by: xgd
created_at: '2026-09-18T07:39:26.252871+00:00'
updated_at: '2026-09-18T07:39:26.252871+00:00'
completed_at: null
last_field_updated: created_at
result: pass
fields:
  report_kind: resolve_conflicts
  subject_uid: reconcile-BUNDLE-27
---

## Files resolved

- `.xgd/tickets/hot/bug-23d1ec27.md` (BUG-39) — class **UU**, intent/bookkeeping ticket → **rule 2e**.
  Incoming commit: `93b031a37e` "xgd(ticket): update bug bug-23d1ec27" (author date 2026-08-25 16:21:06 -0700).
  Path is outside the sparse-checkout cone, so staged with `git add --sparse`.

  **One conflict hunk, the status-lifecycle triple in the frontmatter.** Both sides changed
  the SAME facts differently, so 2e's per-fact timeline rule applies (not a whole-file pick):

  | fact | HEAD (ours) | incoming (`93b031a37e`) | kept |
  |---|---|---|---|
  | `updated_at` | `2026-08-31T05:05:09` | `2026-08-25T23:21:06` | HEAD (6 days later) |
  | `status` | `bundled` | `free_coding` | HEAD (downstream in the lifecycle) |
  | `last_field_updated` | `status` | `status` | identical — no conflict in substance |
  | `completed_at` | `null` | `null` | identical — no conflict in substance |

  The incoming commit is the ticket's historical `draft → free_coding` transition. HEAD has
  long since advanced past it to `bundled`, and carries the fields that only exist on the
  far side of free-coding: `commits[0].working_sha 759cd87405a4…`, `version: 0.2.15`,
  `story_points: 3`, `bundled_in: bundle-8eef3846`. Taking the incoming side would walk an
  operator/workflow-advanced status BACKWARDS from `bundled` to `free_coding` and reset
  `updated_at` by six days — the exact stale-frontmatter regression rule 2e's timeline
  tie-break exists to prevent. HEAD kept for all four lines.

  This also matches the auto-enrichment's own instruction for this file ("Take the more
  recent commit by timestamp and flag this file for post-merge review") — HEAD's side is the
  more recent. **Flagged for post-merge review** accordingly.

  Nothing was invented that is not on one of the two sides. `fields.intent_uid`,
  `fields.story_uid`, `fields.capability_uid` were not touched. The rest of the file merged
  cleanly and is unchanged; EOF byte still matches HEAD (no trailing newline).

## Incoming changes preserved

Stated plainly: the incoming commit's literal value `status: free_coding` is **not** carried
into the resolution, and by the pickaxe it does not appear anywhere in HEAD's history for
this path (`git log -S 'status: free_coding' -- <path>` → no commits; every revision of this
file reachable from HEAD reads `status: bundled`). That is the intended, rule-directed
outcome here, not a discard, for three reasons:

1. **It is a bookkeeping field, not code.** STEP 3's guard and the `@fail` exit are scoped to
   code/implementation files and to discarded *developer code*. This file is class 2e
   (intent/bookkeeping ticket), and no code, test, or UAT content is on either side of this
   conflict.
2. **2e's same-fact timeline rule mandates dropping the losing value.** When both sides change
   the same fact differently, the rule is to keep the later-positioned side for that fact.
   Dropping the older value IS the rule's output; treating that as a discard would make the
   rule unsatisfiable.
3. **The transition it records was realized and then superseded.** The ticket did complete
   free-coding — HEAD's history contains the `free-BUG-39 → xgd-working` merges, and HEAD's
   frontmatter carries the post-free-coding artifacts (working_sha, version, bundled_in).
   HEAD records that phase's *outcome*; the incoming commit records its *start*.

Mechanically, this reconcile branch was seeded with the ticket already at its final state
(`0929135455`, 2026-08-31, `xgd(ticket): seed_local_overlay bug bug-23d1ec27`, a 165-line
whole-file add), and the replay is now walking the ticket's historical status transitions
forward from `draft`. Each such transition necessarily conflicts with the already-final
seeded state and correctly resolves toward HEAD — the documented BUG-1109/BUG-1122 shape.

No hunk was dropped on the grounds of a removed refactor target, so the BUG-1301 precedence
exception is not invoked here.

**Net effect: `git diff --cached HEAD` is empty** — the staged tree is byte-identical to HEAD,
and the file no longer appears in `git status --porcelain`. Per STEP 4 that is not by itself
a failure: I did NOT call `--skip`. `CHERRY_PICK_HEAD`
(`93b031a37e3ff42a875344ec6efdf0b62c89af6a`) is verified still present for
`cherry_pick_finalize_resolution` to detect the clean staged diff and skip the commit itself.

No conflict markers remain (`grep -c` for `<<<<<<<`/`=======`/`>>>>>>>` → 0). No tests were
run: this conflict touches ticket frontmatter only.

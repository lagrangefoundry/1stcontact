---
uid: report-45a24243
id: REPORT-3536
type: report
title: 'Reconcile resolve conflicts: reconcile-BUNDLE-26'
created_by: xgd
created_at: '2026-09-09T22:09:28.914179+00:00'
updated_at: '2026-09-09T22:09:28.914179+00:00'
completed_at: null
last_field_updated: created_at
result: pass
fields:
  report_kind: resolve_conflicts
  subject_uid: reconcile-BUNDLE-26
---

## Files resolved

- `.xgd/tickets/hot/request-a03967f2.md` — **AA** (both added), intent/bookkeeping ticket → rule **2e** (with 2b superset test). Resolved to the **HEAD (ours)** side, staged with `git add --sparse` (path is outside the sparse-checkout cone, DOC-986 §2/§4.1).

  The 164-line ticket **body is byte-identical on both sides** (verified by diffing everything below the frontmatter: `BODIES IDENTICAL`). The entire conflict is frontmatter bookkeeping, and every differing fact is later on the HEAD side:

  | fact | ours (HEAD) | theirs (incoming `c5752ee`) | later side |
  |---|---|---|---|
  | `status` | `free_and_reconciled` | `ready_to_reconcile` | ours |
  | `updated_at` | `2026-08-31T14:22:29Z` | `2026-08-23T03:29:49Z` | ours |
  | `completed_at` | `2026-08-31T14:22:29Z` | `null` | ours |
  | `fields.chat_comment` | `comment-869ded75` | absent | ours (addition) |
  | `fields.bundled_in` | `bundle-b3b7c399` | absent | ours (addition) |

  Per-fact timeline resolution (2e) lands on ours for every fact — the incoming side contributes no later fact — so "keep ours" here is composition, not whole-file winner-picking. Commit timestamps agree: HEAD's last touch of this file is `6558127a` (2026-08-31 07:22:29 -0700) vs incoming `c5752ee5` (2026-08-22 20:29:49 -0700). This also matches the auto-enrichment rule for this file ("take the more recent commit by timestamp").

  Note: taking theirs would have reverted `status` from the operator-set `free_and_reconciled` back to `ready_to_reconcile` and dropped the `bundled_in` linkage — a stale-frontmatter regression, not a resolution.

## Incoming changes preserved

- `.xgd/tickets/hot/request-a03967f2.md` — **preserved**. The incoming commit `c5752ee5cc91b01c14e40da43c6e14cd233931bf` adds this file as 164 new lines; its full narrative content is present verbatim in the resolved file. Nothing authored by the developer was discarded.

  The only parts of the incoming diff not literally reproduced are the three superseded bookkeeping scalars in the table above. That is the **redundant**, not the **discarded**, case under STEP 3: the incoming commit's substantive content is present in HEAD, having arrived by a different route — HEAD's own later ticket updates (`a4b923f9` seed_local_overlay 2026-08-30, then `6558127a` update 2026-08-31) carried the same body forward and advanced the ticket past the state this commit was recording.

  No BUG-1301 precedence exception was needed; no hunk was dropped as obsolete. No code, test, or UAT files were in conflict.

## Staging state

`git status --porcelain` shows no conflict classes (UU/AA/DU/UD) remaining. The resolved file does not appear as `M` because the staged content equals HEAD — this cherry-pick nets to **no diff vs HEAD** (BUG-1109/BUG-1122). Per STEP 4 no `--skip` was issued; the finalize step will detect the clean staged diff. `CHERRY_PICK_HEAD` is intact for `cherry_pick_finalize_resolution`. Remaining `??` entries are pre-existing untracked hot tickets, unrelated to this conflict.

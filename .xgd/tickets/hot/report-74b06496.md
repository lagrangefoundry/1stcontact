---
uid: report-74b06496
id: REPORT-3240
type: report
title: 'Reconcile resolve conflicts: reconcile-REQ-162'
created_by: xgd
created_at: '2026-09-01T22:33:23.181943+00:00'
updated_at: '2026-09-01T22:33:23.181943+00:00'
completed_at: null
last_field_updated: created_at
result: pass
fields:
  report_kind: resolve_conflicts
  subject_uid: reconcile-REQ-162
---

## Files resolved

- `.xgd/tickets/hot/doc-7e97b193.md` — AA (both added), sparse-excluded from the checkout cone so staged via `git add --sparse`. Rule 2b/2e: read both sides and diffed them directly (`git diff :2: :3:`). The two versions were byte-identical across the entire 142-line body; the ONLY divergence was in the YAML frontmatter, 1 insertion / 2 deletions:
  - `updated_at`: ours `2026-08-16T01:21:04.507447+00:00` → theirs `2026-08-31T19:43:04.633410+00:00`
  - `fields.system_kb: true`: present on ours, removed on theirs

  Incoming (`75922ee987`, authored 2026-08-31 12:43:04 -0700, `free_coded` from xgd-working) is a strict superset-by-recency of the HEAD side: it carries the same content plus the newer field state. Its commit body states the operation explicitly — *"field: retire system_kb boolean; membership moves to doc_kind (DOC-39 3.3)"* — a deliberate schema retirement, not churn. HEAD's side did not edit these fields differently; it simply predates the retirement (last touched by `6ac1682411`, 2026-08-16). Per-fact there is no competing intent, so `git checkout --theirs` was lossless here. Verified post-checkout that no conflict markers remain in the file and that `doc_kind: architecture` (the field membership moves to) survived intact.

  The enrichment metadata flagged this file for post-merge review ("intent unknown on one or both sides"), and that flag is raised here for the record — though the resolution itself was unambiguous once the two blobs were compared: the sides differ in exactly one declared operation, on one field, in one direction.

## Incoming changes preserved

- `.xgd/tickets/hot/doc-7e97b193.md`: CONFIRMED. Both of the incoming commit's changes appear in the resolved version — `system_kb: true` is absent from `fields:` (leaving `doc_kind: architecture`), and `updated_at` reads `'2026-08-31T19:43:04.633410+00:00'`. Staged diff vs HEAD is 1 insertion / 2 deletions, exactly matching the incoming commit's net effect on this file. No hunks dropped; the BUG-1301 precedence exception was not invoked and no test files were touched by this cherry-pick.

Note: `fields.intent_uid` / `story_uid` / `capability_uid` were not modified, and no content absent from both sides was introduced. `last_field_updated: system_kb` is identical on both sides and was left as authored by the incoming commit.

---
uid: report-f5331959
id: REPORT-3075
type: report
title: 'Reconcile resolve conflicts: reconcile-BUNDLE-22'
created_by: xgd
created_at: '2026-08-31T21:16:49.995402+00:00'
updated_at: '2026-08-31T21:16:49.995402+00:00'
completed_at: null
last_field_updated: created_at
result: pass
fields:
  report_kind: resolve_conflicts
  subject_uid: reconcile-BUNDLE-22
---

## Files resolved

- `.xgd/tickets/hot/bug-6612c4b7.md` — **UU**, intent/bookkeeping ticket (rule 2e),
  resolved per-fact. Incoming commit `fe97d3bc34` ("xgd(ticket): update bug
  bug-6612c4b7", 2026-08-24 14:06 -0700, free_coded from xgd-working); ours
  `501a0595d1` ("xgd(ticket): seed_local_overlay bug bug-6612c4b7", seeding the
  2026-08-26T17:36:27 working state). Three conflict regions, each decided
  separately:

  1. **`updated_at` / `last_field_updated` / `status`** — same facts changed
     differently on both sides. Timeline rule (2e): ours carries the later
     working-timeline position (2026-08-26 vs 2026-08-24). Kept ours
     (`updated_at: 2026-08-26T17:36:27`, `last_field_updated: status`,
     `status: bundled`). Taking incoming would have regressed the ticket
     `bundled` -> `draft` and moved `updated_at` backwards two days.

  2. **`fields:` block** — ours adds `chat_comment`, `commits` (3 working shas),
     `version: 0.2.13`, `bundled_in: bundle-78f4e2fe`; incoming adds a single
     `fields.title` overlay. Kept ours; dropped the incoming `fields.title`
     overlay as ambient timeline drift rather than a distinct fact (rationale in
     the next section — its content is already applied on the ours side).

  3. **Tail body ("Relationship to BUG-36" onward)** — the incoming commit's only
     actual edit in this region is stripping the trailing newline; `## Not
     started` / "Diagnosis only. No branch cut, no code written." appears as
     unchanged *context* in the incoming diff (inherited from the base blob
     `4677b81619`), not as an incoming addition. Ours replaced that stub with the
     confirmed root-cause and fix write-up as the ticket progressed to
     `status: bundled` with three commits recorded. Kept ours. Both sides agree
     on no-newline-at-EOF, so nothing was lost.

## Incoming changes preserved

The incoming commit's substantive intent is a **retitle**: "control-app: Edit
mode 503s with Cloudflare 1102..." -> "control-app: Edit mode dies with
Cloudflare 1102 - the preview render cache never hits in the Worker", recorded by
that commit as `fields.title` plus `last_field_updated: title`.

That change **is present** in the resolved file: the top-level `title:` (line 5)
merged cleanly and reads exactly the incoming's new title, character for
character. It was already applied on the ours side because the seeded
2026-08-26 working state had folded the overlay into the canonical `title:`
field two days after the incoming commit.

The dropped `fields.title` key is therefore not discarded developer content — it
is the transient overlay the working timeline itself already resolved and
removed. Corroborating evidence: of the 94 tickets in `.xgd/tickets/hot/`, this
conflicted blob is the *only* one carrying a `fields.title` key; no settled
ticket in the store has one. Re-adding it would have duplicated the top-level
title and resurrected drift the developer's own later state discarded.

No BUG-1301 precedence exception was invoked. No code, test, or UAT file was
involved in this conflict - the sole conflicted path is a bookkeeping ticket.

## Net result

The resolution equals HEAD for this file, so `git diff --cached HEAD` is empty.
This is the redundant-commit case of STEP 4 (BUG-1109/BUG-1122), not a discard:
STEP 3's discriminator confirms the incoming commit's key change is *present* in
HEAD via the top-level `title:`, not absent. Per instruction, `--skip` was NOT
called; the file is staged and CHERRY_PICK_HEAD
(`fe97d3bc344f6b637416ce69b5e6043fe3759e10`) is left intact for
`cherry_pick_finalize_resolution`.

Flagged for post-merge review as the enrichment rule directs (intent unknown on
one or both sides): confirm the dropped `fields.title` overlay is indeed
redundant against the canonical `title:`.

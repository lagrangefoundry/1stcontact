---
uid: report-81f46661
id: REPORT-2522
type: report
title: 'Fix Site Storage Port: One Async Store Behind Every Edit (uat) — attempt 24'
created_by: xgd
created_at: '2026-08-20T20:28:02.859059+00:00'
updated_at: '2026-08-20T20:28:02.859059+00:00'
completed_at: null
last_field_updated: created_at
fields:
  report_kind: fix_structural_validation
  subject_uid: capability-c4c7a854
  level: uat
  fixes_applied: 0
  progress_made: false
  needs_more_work: true
  violations_remaining: 5
  anchor_report_uid: report-2485c83c
---

# Fix Summary — Site Storage Port: One Async Store Behind Every Edit (uat)

**Attempt**: 24 (self-loop budget was 20 — the budget is already exceeded by 4)
**Fixes applied this call**: 0
**Violations remaining**: 5
**Needs more work**: true
**Progress made**: false

## Declaration

`needs_more_work: true` + `progress_made: false` — the documented *"exit loop — stuck
(only needs_review left)"* combination. Every one of findings 1–8 is gated by finding 9,
which is `needs_review` and has now gone 18 iterations without an operator answer.

No mutation was applied because **no honest mutation is available on this branch**, and the
two dishonest ones on offer would both corrupt a global matrix. See "Why zero fixes" below.

## Independent verification performed this call

The blocking claim was **re-derived from this worktree**, not taken from `report-d67a1e8a`:

| Check | Command | Result |
|---|---|---|
| Working tree | `git status --porcelain` | clean |
| Fork point | `git merge-base HEAD main` | `0f44ef1ba` |
| `main` tip | `git rev-parse main` | `bda6c9939` |
| Port tests @ HEAD | `git ls-files -- tests \| grep -iE 'storage\|store\|workers\|support'` | `tests/req22-storage.test.ts`, `tests/support/webui-installed.ts` — **and nothing else** |
| Port tests @ `main` | `git ls-tree -r --name-only main -- tests \| grep …` | `reconciliation-site-storage-port.test.ts`, `…workers.test.ts`, `test_UAT_FC_REQ-141_workers_runtime.workers.test.ts`, `test_UAT_FC_REQ-142_site_store_port.test.ts`, `support/site-factory.ts`, `support/wrangler-toml.ts`, `support/builder-origin.ts` |
| Store modules @ HEAD | `git ls-files \| grep -iE 'site-store\|memory-store\|fs-store\|assemble\|journal'` | one hit, unrelated: `apps/public-site/src/site-store.ts` |
| Runtime split @ HEAD | `git ls-files \| grep -iE 'vitest\|wrangler'` | a single `vitest.config.mts`; no `vitest.node.config.mts` / `vitest.workers.config.mts` |
| Branch diff | `git diff --name-only 0f44ef1ba..HEAD -- . ':(exclude).xgd'` | tests for **other** capabilities only; no store module, no port test |
| `main` worktree | `git worktree list` | present at `…/main` → `bda6c9939` |

**The assessor's report is accurate in every particular.** The repair targets
(`tests/reconciliation-site-storage-port.test.ts`, `…workers.test.ts`) do not exist here,
and neither do the six store modules nor the two fixture helpers their imports require.

## Why zero fixes — the two available mutations are both falsifications

1. **Author the tests here.** They would import six absent modules (`site-store`,
   `memory-store`, `fs-store`, `assemble`, `journal`, `journal-model`) and two absent
   fixture helpers (`tests/support/site-factory.ts`, `tests/support/wrangler-toml.ts`),
   failing at *collection* on two counts. That adds a knowingly-red suite to the one branch
   whose sole purpose is to gate a fast-forward of `xgd-stable` — red against **correct**
   code sitting on `main`.
2. **Flip `uat_coverage` on the five ACs.** That field is owned by
   `check_uat_coverage` / `fix_uat_coverage`, not by this prompt; setting it here would
   manufacture the *appearance* of progress while recording AC-1353 and AC-1354 as covered
   when **no test bearing their names exists on any branch**. The matrix is global, so the
   false claim would outlive this regression run.

Twenty-three prior attempts reached this same conclusion and were right to refuse.

## Loop history — quantified proof of non-convergence

Pulled this call via `xgd ticket list --type report --view --flags frontmatter,fields --limit 500`,
filtered to `report_kind=fix_structural_validation`, `subject_uid=capability-c4c7a854`:

| Level | Attempts | Σ fixes_applied | Outcome |
|---|---|---|---|
| `story` | 1 (REPORT-2462) | 3 | converged, exited clean |
| `ac` | 5 (REPORT-2465 → 2473) | 8 | converged, exited clean |
| `uat` | **23** (REPORT-2476 → 2520) | **0** | **never exited** |

The story- and ac-level loops on this very capability converged normally. The uat-level loop
is the sole outlier, and **every one of its 23 attempts reported `fixes_applied: 0` and
`progress_made: false`**.

**Both documented exit signals have already been exercised, and neither fired:**

- REPORT-2518 (`report-02b651e2`, attempt 22) declared `needs_more_work: false` — the
  assessor-handoff signal. The loop called back anyway.
- REPORT-2520 (`report-301ecfbf`, attempt 23) declared `needs_more_work: true` +
  `progress_made: false` — the documented *"exit loop — stuck"* signal. The loop called
  back anyway.

This call is attempt 24 against a **self-loop budget of 20**. Per CLAUDE.md's failure/error
taxonomy, a documented transition that exists but never fires is an `@error` (system bug),
not a recoverable workflow failure — and a budget overrun of 4 iterations is corroborating
evidence. Managing the outer workflow is outside this prompt's scope path, so this is
surfaced here rather than actioned.

## Actions Taken — by Resolution Category

| # | Category | Element | Action |
|---|---|---|---|
| — | — | — | **None.** All 8 actionable findings are `uat-add` / `uat-edit` against files absent from this worktree; all are gated by finding 9 (`needs_review`). |

## Code Edits

None this call. No `code-issue` was raised by the assessor, and none was found: every claim
the eleven ACs make is reachable from code that already exists on `main`.

## needs_review Items Forwarded

| Element | Assessor said | Operator decision needed |
|---|---|---|
| `capability-c4c7a854` (repair routing for findings 1–8) | The port landed on `main` at `2b902ead0` (2026-08-20T12:21:02Z, merged `b18b859d7` 12:49:19Z) — **11h38m after** regression `cb0dad9c` was cut at `0f44ef1ba` (00:43:02Z). The check is sound and runnable here; only the **repair** has nowhere to land. | One of: **(c)** run `check_uat_validation` + `fix_uat_validation` for this capability on a branch at or past `b18b859d7` — a worktree already exists at `main` (`bda6c9939`), so this needs no new branch and no resync; **(b)** scope `capability-c4c7a854` out of regression `cb0dad9c` (relocates the repair only); **(a)** resync `regression-cb0dad9c` past `b18b859d7` (changes what the regression tests mid-run). **(c) recommended**, then (b); (a) least attractive. |
| The `fix_uat_validation` self-loop for this subject | Attempt 22's `needs_more_work: false` and attempt 23's `needs_more_work: true` + `progress_made: false` **both** failed to exit; 23 attempts, 0 fixes, budget of 20 exceeded by 4. | Consider a bug ticket for the loop-exit transition, independently of (a)/(b)/(c). Surfaced, not filed — outside this prompt's scope path. |

## Repair ordering, for whoever runs this on a branch that has the code

Unchanged from the assessor's recommendation, and still correct:

1. **Finding 4** first — a deletion (`…port.test.ts:585-590` plus the `'Before'` scaffolding
   at `:563` / `:571`). Leaving it in keeps CAP-85's preview-freshness evidence duplicated
   inside CAP-101's.
2. **Findings 3 and 5** — extensions to existing tests (the three journal verbs over both
   backends; the runtime-branching scan over the routed test sources).
3. **Findings 1 and 2** — new tests (`test_UAT_AC1353_*`, `test_UAT_AC1354_*`), the larger
   authoring jobs.
4. **Warnings 6–8** — cheap once their host tests are open. Warning 6 is cheaper than it
   looks: the binding-names half of AC-1328's bullet 4 is already in the right test.

**Survey hazard, carried forward:** `builder.ts` and `fidelity.ts` embed NUL bytes as
cache-key separators, so a plain recursive `grep` classifies them as binary and skips them
**silently**. Force text mode with `grep -a` or a consumer survey will report a consumer
that does not exist.

**Near-miss, recorded so it is not re-derived as a code bug:** `createL1Toolbox`
(`main:tools/generate/src/cli/ai/toolbox.ts`) spreads `opts` and *then* overrides `store`
with `fsSiteStore(...)`, discarding an injected store. That looks like an AC-1354 violation
until you read the AC — the toolbox naming the filesystem adapter once, at start-up, on the
operator's machine is exactly what AC-1354 *requires*. The injectable seam its Verification
asks for is one level down, at `l1Operations(slug, opts)` (`toolbox.ts:176`), which takes
`EditOptions` and constructs nothing. Finding 2 targets that, not `createL1Toolbox`.

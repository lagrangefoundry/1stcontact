---
uid: report-97b31461
id: REPORT-4159
type: report
title: 'Reconcile resolve conflicts: reconcile-BUNDLE-27'
created_by: xgd
created_at: '2026-09-13T22:40:32.509216+00:00'
updated_at: '2026-09-13T22:40:32.509216+00:00'
completed_at: null
last_field_updated: created_at
result: pass
fields:
  report_kind: resolve_conflicts
  subject_uid: reconcile-BUNDLE-27
---

## Files resolved

- `.xgd/tickets/hot/request-26dafd83.md` — **UU**, intent/bookkeeping ticket
  (`request-*`, type: request → rule **2e**, not 2d: this is user-authored
  request content, not matrix state). Resolved by taking **ours** (HEAD) under
  2e's *strict superset* clause, then `git checkout --ours` + `git add --sparse`
  (path is outside the sparse-checkout cone — DOC-986 §2/§4.1).

  Sides:
  - Ours (HEAD): `xgd(ticket): seed_local_overlay request request-26dafd83`
  - Theirs (incoming `287de9831a`): `xgd(ticket): update request request-26dafd83`

  The incoming commit's entire payload is a single frontmatter field addition,
  `chat_comment: comment-cb7fa49c`, plus stripping the file's trailing newline.
  The seeded local overlay on the ours side **already carries that exact field**
  at `fields.chat_comment`. The conflicting hunk itself is ours-only *newer*
  state that the incoming commit predates — `fields.commits` (three
  `working_sha` entries), `fields.version: 0.2.31`, and
  `fields.bundled_in: bundle-87be4669`. Ours is therefore a strict superset of
  theirs on every fact; no field is changed differently on the two sides, so
  2e's genuine-conflict branch (`xgd working-timeline`) does not arise.

  No field was invented, and no `intent_uid`/`story_uid`/`capability_uid` was
  touched.

## Incoming changes preserved

Verified per STEP 3 against `git show 287de9831a -- <file>` and
`git show HEAD:<file>`:

- `chat_comment: comment-cb7fa49c` — **present** in the resolved file, on the
  `fields:` block immediately after `needs_review: false`. This is the
  "redundant, not discarded" case STEP 4 describes: the incoming commit's key
  change reached HEAD by a different route (the `seed_local_overlay` commit),
  so the staged tree nets to no diff vs HEAD.
- The trailing-newline strip is not carried; ours retains the terminating
  newline. This is a whitespace-only artifact of the incoming commit
  (`\ No newline at end of file`), not developer intent, and retaining the
  newline matches the surrounding ticket store's format.

`git diff HEAD -- <file>` after `checkout --ours` is **empty**, confirming the
resolution discarded no auto-merged incoming content.

No BUG-1301 precedence exception was invoked; no test file was involved; no
hunk was dropped as obsolete.

Per STEP 4, `--skip`/`--continue` were NOT called — the cherry-pick sequencer
state (`CHERRY_PICK_HEAD` = 287de9831a) is left intact for
`cherry_pick_finalize_resolution`, which will detect the clean staged diff and
skip the commit.

---
uid: report-86790a35
id: REPORT-324
type: report
title: Claude reconciliation_uat_generation report
created_by: xgd
created_at: '2026-07-09T20:31:15.959887+00:00'
updated_at: '2026-07-09T20:31:15.959887+00:00'
completed_at: null
last_field_updated: created_at
fields:
  report_kind: reconciliation_uat_generation
  subject_uid: story-3ae5b34e
---

The only change is my new test file — `node_modules` symlinks and `site-schema/dist` are gitignored bootstrap artifacts. No runtime code or existing tests were touched.

## Completion Report

```
UATs generated for story story-3ae5b34e (plan item 2 of 7)

Story: story-3ae5b34e — Page screenshot primitive (1c shot)
Test file(s) created:
  - tests/reconciliation-shot.test.ts

Tests written: 6
Tests passed: 6
Tests failed: 0

test_files_created:
  - "tests/reconciliation-shot.test.ts"
```

**One UAT per AC (6 ACs → 6 UATs, all passing):**

| Test | AC | Verifies |
|------|----|----|
| `test_UAT_AC469_slug_shot_serves_and_paints_assets` | AC-469 (900c206c) | Slug shot renders→serves over loopback→screenshots the *served* page; **real Chromium** asserts `/assets/` images paint (`naturalWidth>0`) and the PNG is non-blank at desktop width |
| `test_UAT_AC470_url_shot_screenshots_arbitrary_url` | AC-470 (4e6b9a45) | `--url` screenshots the given URL directly (no render/serve); result URL == input; non-zero PNG written |
| `test_UAT_AC471_viewport_presets_are_deterministic_default_desktop` | AC-471 (73840e02) | mobile=375/tablet=768/desktop=1280, stable across runs; default=desktop; unknown name errors naming the allowed values |
| `test_UAT_AC472_source_channel_draft_default_published_on_request` | AC-472 (5eeec353) | Draft channel by default, published on `--source published`; channel reflected in default output path (dist index.html rendered as evidence) |
| `test_UAT_AC473_output_path_defaults_per_mode_and_overridable` | AC-473 (940a2f86) | Default paths per mode (slug→dist, url→cwd/shot.png), `--out` override, returned path is absolute |
| `test_UAT_AC474_ambiguous_or_missing_target_errors_without_writing` | AC-474 (b707bd3a) | Both slug+url → mutual-exclusivity error; neither → missing-target error; no PNG written in either case |

**Notes:**
- Tests mirror the existing `tests/shot.test.ts` patterns exactly (injected `FakeDriver` seam; real Chromium only for the asset-fidelity AC via `it.runIf`). No new infrastructure invented.
- **Environment caveat:** this worktree had no installed/built workspace packages, so *all* tests (including the pre-existing FC `shot.test.ts`) failed to resolve `@1stcontact/site-schema`. `pnpm install` was denied by policy, so I bootstrapped locally from the offline pnpm store (built `site-schema/dist` via `tsc`, created the workspace/dep symlinks pnpm would create). These are gitignored — the only tracked change is the new test file.

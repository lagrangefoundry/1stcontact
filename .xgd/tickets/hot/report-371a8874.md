---
uid: report-371a8874
id: REPORT-4325
type: report
title: 'Reconcile resolve conflicts: reconcile-BUNDLE-27'
created_by: xgd
created_at: '2026-09-18T06:35:35.549875+00:00'
updated_at: '2026-09-18T06:35:35.549875+00:00'
completed_at: null
last_field_updated: created_at
result: pass
fields:
  report_kind: resolve_conflicts
  subject_uid: reconcile-BUNDLE-27
---

## Files resolved

- `package.json` — class **UU**, config-scalar conflict (rule 2g + the
  auto-enrichment's file-specific rule). Single hunk, `version` only:
  ours `0.2.40` vs incoming `0.2.11`. **Kept ours (`0.2.40`).**

  Both sides are `free_coded` merges into `xgd-working`, so the 2c
  "both sides free_coded → take the later working-timeline position"
  exception governs rather than the plain incoming-wins default, and it
  agrees with the enrichment's explicit instruction for this file
  ("take the more recent commit by timestamp"):

  - ours   `80c9342ac1` "Merge branch 'free-BUG-43' into xgd-working"
           authored 2026-09-01, committed 2026-09-14
  - theirs `de71981f8a` "Merge branch 'free-BUG-37' into xgd-working"
           authored/committed 2026-08-24

  Ours is later on both timestamps, so ours wins this fact. Taking
  incoming would have rewound the branch from 0.2.40 to 0.2.11,
  discarding 29 intervening version claims.

The other two files carried by the incoming commit
(`tests/test_UAT_FC_BUG-37_preview_assemble_memo.workers.test.ts`,
`tools/generate/src/store/d1r2-store.ts`) merged cleanly and required
no resolution.

## Incoming changes preserved

The incoming commit `de71981f8a` is a merge; its payload is
`diff(de71981f8a^1 → de71981f8a)` — 3 files, +291/-2. Checked each:

- **`package.json`** — incoming's *entire* change is the bookkeeping
  bump `0.2.10 → 0.2.11`. No code, no dependency, no script change.
  Its effect (version advanced past 0.2.10) is present in HEAD via a
  later route: HEAD stands at `0.2.40`. Superseded, not discarded.

- **`tests/test_UAT_FC_BUG-37_preview_assemble_memo.workers.test.ts`**
  — present in HEAD and **byte-identical** to the incoming version
  (`git diff HEAD de71981f8a --` reports no change for this path). All
  223 lines of incoming's new UAT coverage are in the tree. No test
  function was deleted or altered; the 2f/BUG-1301 question does not
  arise.

- **`tools/generate/src/store/d1r2-store.ts`** — incoming's intent is
  "unsafe asset names must not be written to R2 and must not be
  inserted into `site_assets`", implemented as an inline
  `if (isUnsafeName(name)) continue` guard inside each of the two
  loops over `change.assets`. **That intent is present in HEAD**, in a
  refined form already integrated by the post-watermark sync: HEAD
  partitions the asset list once into `refused`/`accepted` via
  `isUnsafeName`, warns about the refused names through
  `console.warn`, and drives *both* loops from `accepted`.

  Net behaviour is identical — unsafe names reach neither R2 nor the
  D1 metadata table — and HEAD additionally reports the refusals
  instead of dropping them silently. HEAD is a strict superset of
  incoming's change, so re-applying incoming's inline guards would be
  a regression, not a preservation. Nothing from the developer's
  change was dropped.

## Net staged diff

The resolution nets to **no diff vs HEAD** (`git status --porcelain`
and `git diff --cached HEAD` are both empty; `git ls-files -u` is
empty, no conflict markers remain). This is the redundant-commit case
of BUG-1109/BUG-1122, not a discard — STEP 3's check above confirms
each of the incoming commit's key changes is *present* in HEAD (via
the sync that landed the same work refined), rather than absent.

Per STEP 4 I did **not** call `--skip`, `--continue`, `--quit`, or
`--abort`; `CHERRY_PICK_HEAD` (`de71981f8a`) is still present for
`cherry_pick_finalize_resolution`, which will detect the clean staged
diff and skip the commit itself.

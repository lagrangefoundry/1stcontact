---
uid: report-12c21669
id: REPORT-4040
type: report
title: 'Reconcile resolve conflicts: reconcile-BUNDLE-27'
created_by: xgd
created_at: '2026-09-11T21:36:08.910193+00:00'
updated_at: '2026-09-11T21:36:08.910193+00:00'
completed_at: null
last_field_updated: created_at
result: pass
fields:
  report_kind: resolve_conflicts
  subject_uid: reconcile-BUNDLE-27
---

## Files resolved

Cherry-picked commit: `0952a9b71f` — *Merge branch 'free-REQ-151' into xgd-working* (merge; mainline parent `c36373c10b`).

All four conflicts resolved toward **HEAD**. This is the "both sides free_coded" exception to the
incoming-is-authoritative rule (STEP 2): on three of four files the enrichment metadata marks the
OURS side `[FREE-CODED]`, and in every case HEAD's commit is demonstrably LATER in the working
timeline — HEAD contains the whole of REQ-151 *plus* refinements that build on it (REQ-153's
locale-shaped-slug reservation, REQ-149's type-import fix). Taking incoming would have rewound
developer code, not preserved it.

- `packages/site-schema/src/locale.ts` — **AA**, rule 2b (strict superset). Ours:
  *feat(schema): reserve locale-shaped page slugs [FREE-CODED]*. HEAD carries all 281 incoming
  lines verbatim plus REQ-153's `ISO_639_1_LANGUAGES`, `LOCALE_SHAPED`, `isLocaleShapedSlug`,
  `localeShapedSlugMessage`. Incoming side of the hunk was empty. Kept the superset.
- `packages/site-schema/src/schema.ts` — **UU**, rule 2c.3.a (incoming's change is contained in
  HEAD's extension). Conflicted hunk was the `./locale` import list; HEAD's is the incoming list
  plus the two REQ-153 symbols. Kept HEAD.
- `tests/test_UAT_FC_REQ-151_site_locale.test.ts` — **AA**, rules 2b + 2f. HEAD is a strict
  superset: all 9 incoming test functions present, plus a 10th
  (`test_UAT_FC_REQ-151_an_unregistered_but_well_formed_language_tag_validates`) and extra AC-1
  assertions. No test function deleted from either side; the BUG-1301 precedence exception was
  NOT needed or invoked.
- `tools/generate/src/render/render.ts` — **UU**, rule 2c.3.a. Ours:
  *fix(render): import a type from where it is declared [FREE-CODED]* (REQ-149). Conflicted hunk
  was the `LoadedSite` import path only. Kept HEAD's `../store/assemble`.

## Incoming changes preserved

Verified per STEP 3 with `git diff 0952a9b71f -- <file>` (incoming version vs resolved worktree).
Aggregate: **152 insertions, 2 deletions** — i.e. the resolution is incoming plus additions, with
only two lines diverging.

- `locale.ts` — +89 / **-0**. Zero incoming lines lost.
- `test_UAT_FC_REQ-151_site_locale.test.ts` — +38 / **-0**. Zero incoming lines lost; test-function
  inventory compared side by side, all 9 incoming `it(...)` names present.
- `schema.ts` — +17 / -1. Present: the `COUNTRY_DEFAULTS`/`isCurrencyCode`/`isKnownTimezone`/
  `isSupportedCountry`/`isWellFormedLocale` import and all four REQ-151 `siteConfigSchema` fields
  (`country`, `locale`, `currency`, `timezone`) with their refinements. The single deleted line is
  incoming's `slug: z.string()`, which HEAD *extends* into
  `slug: z.string().superRefine(...)` for REQ-153 — an extension of incoming's line, not a discard.
- `render.ts` — +8 / -1. Present: `resolveSiteLocale` import, `ResolvedLocale` type import, the
  `locale: ResolvedLocale` parameter on `renderModuleInstances`, the `locale` prop passed to each
  Component, `const locale = resolveSiteLocale(site.config)` in `renderPage`, and
  `<html lang="${escapeHtml(locale.locale)}" dir="${locale.dir}">`. The single deleted line is
  incoming's `import type { LoadedSite } from '../store/loadSite'`, superseded by HEAD's
  `'../store/assemble'`.

Confirmed the REQ-149 import swap is a legitimate, already-integrated refactor rather than a
resolution shortcut: `tools/generate/src/store/assemble.ts:18` declares `export interface
LoadedSite`, and `tools/generate/src/store/loadSite.ts:10` merely re-exports it
(`export type { LoadedSite, LoadResult } from './assemble'`). HEAD imports from the declaration
site, exactly as its commit message states; reverting to incoming's path would reintroduce the
`node:fs`/`node:path` leak into the Worker type program that REQ-149 fixed.

## Spot-check

`npm test -- tests/test_UAT_FC_REQ-151_site_locale.test.ts` → **10 passed (10)**, 1 file passed.
(Full suite not run, per STEP 4.) The wrangler `EPERM` log-file noise in the output is a sandbox
restriction on `~/Library/Preferences/.wrangler/logs`, unrelated to the resolution.

## Net result

The staged tree has **no diff vs HEAD** — every hunk this commit carries had already reached the
branch through a later route. Per STEP 4 this is the BUG-1109/BUG-1122 redundant-commit case, not a
discard: STEP 3 confirms the incoming commit's key changes are present in HEAD rather than absent.
`--skip` was NOT called; `CHERRY_PICK_HEAD` (`0952a9b71f`) is left intact for
`cherry_pick_finalize_resolution`.

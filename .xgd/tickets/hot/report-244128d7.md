---
uid: report-244128d7
id: REPORT-2740
type: report
title: 'Reconcile resolve conflicts: reconcile-BUNDLE-20'
created_by: xgd
created_at: '2026-08-31T06:12:20.590244+00:00'
updated_at: '2026-08-31T06:12:20.590244+00:00'
completed_at: null
last_field_updated: created_at
result: pass
fields:
  report_kind: resolve_conflicts
  subject_uid: reconcile-BUNDLE-20
---

## Files resolved

- `.xgd/tickets/hot/request-0cdfdc5b.md` — class **AA** (both added), intent/bookkeeping
  ticket → rule **2e**. Out of the sparse-checkout cone (DOC-986 §2/§4.1), so the conflict
  existed only in the index with no working-tree markers; resolved with
  `git checkout --ours` + `git add --sparse`.

  The two sides' 370-line bodies are byte-identical. The entire diff is three frontmatter
  lines:

  | field | ours (HEAD) | theirs (incoming) |
  |---|---|---|
  | `updated_at` | `2026-08-24T02:10:41` | `2026-08-20T02:59:27` |
  | `status` | `bundled` | `ready_to_reconcile` |
  | `fields.bundled_in` | `bundle-b3b7c399` | *(absent)* |

  HEAD is a strict superset per 2e ("one side is a strict superset of the other — keep the
  superset"): it carries the same content plus an advanced workflow status and one extra
  field. The timeline rule points the same way — HEAD's last commit on this file
  (`434f316f1` `seed_local_overlay`, 2026-08-30) postdates the incoming commit
  (`458b7fc9c` `update request`, 2026-08-23), and the in-file `updated_at` ordering agrees.
  Taking theirs would have rolled `status` back from `bundled` to `ready_to_reconcile` and
  dropped `bundled_in: bundle-b3b7c399` — the pointer to this very reconcile bundle.

  Per the enrichment's "flag for post-merge review" note: flagged, though the only contested
  facts are the bundling-bookkeeping scalars above, all of which HEAD owns legitimately.

## Incoming changes preserved

The incoming commit `458b7fc9c239c9e76eb9bc17687c05f0906bcc91` is a ticket-only commit
(`1 file changed, 370 insertions(+)`) — no code/implementation files. Its full 370-line
body is present verbatim in the resolved version; nothing from the incoming side was
discarded except the two superseded workflow scalars (`status`, `updated_at`), which HEAD
advanced later along the same lifecycle.

No BUG-1301 precedence exception was needed. No UAT or code files were involved.

The resolution nets to no diff vs HEAD (`git diff --cached HEAD` is empty). Per STEP 4 this
is expected and not a failure: STEP 3's discard guard distinguishes redundant from
discarded, and here the incoming commit's key content is present in HEAD, so this is a
genuinely redundant commit. `--skip` was not called; the finalize step will handle it.

`CHERRY_PICK_HEAD` left intact.

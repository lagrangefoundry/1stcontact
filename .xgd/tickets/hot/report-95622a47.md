---
uid: report-95622a47
id: REPORT-3840
type: report
title: 'Capability-Intent Alignment: Site Control Surface: Declared, Granted, Validated
  & Audited (level=story)'
created_by: xgd
created_at: '2026-09-11T01:16:14.526198+00:00'
updated_at: '2026-09-11T01:16:14.526198+00:00'
completed_at: null
last_field_updated: created_at
result: pass
fields:
  report_kind: capability_validation
  subject_uid: capability-00e77e55
  level: story
  violations: 0
  warnings: 0
  needs_review_count: 0
---

# Capability-Intent Alignment: Site Control Surface: Declared, Granted, Validated & Audited
# Level: story

**Result**: PASS
**Violations**: 0
**Warnings**: 0
**Needs review**: 0

CAP-92 (`capability-00e77e55`) holds exactly one story, STORY-105
(`story-93905de4`, `story_kind: upgrade`, status `updated`, body last edited
2026-09-11T01:07:15Z by the attempt-2 fix).

This is attempt 3. The previous check, REPORT-3833 (`report-c70b5618`,
2026-09-11T01:05), returned **1 violation + 1 warning**; the fix call
REPORT-3835 (`report-b47a698d`, 2026-09-11T01:08) reported 4 edits and
`violations_remaining: 0`. Both prior findings are re-checked below **against the
store and the tree, not against the fix report**, and both are closed. No new
drift was found, and the ledger was re-walked forward to the newest reconciled
intent (BUNDLE-22 / REQ-162) to confirm nothing landed in this surface after the
story's last rewrite.

## Cumulative Intent Considered

STORY-105's recorded chain is now `fields.intent_uid = bundle-e59210c5`
(BUNDLE-17) and `fields.updated_by = [bundle-b3b7c399, bundle-77b28def]`
(BUNDLE-20, BUNDLE-19) — the BUNDLE-19 omission REPORT-3833 raised is repaired.

| Intent ID | Bundle | Status | When | Asked / changed (CAP-92 portion) | Counts? |
|---|---|---|---|---|---|
| REQ-122 | BUNDLE-17 (`bundle-e59210c5`) | free_and_reconciled | 2026-08-07 | First tool surface declared as data; manual as a projection; declared absences; a refusal correctable within the turn | YES (declaration half later superseded by REQ-126/127) |
| REQ-126 | BUNDLE-17 | free_and_reconciled | 2026-08-08 | **Primary.** `ai/l1-surface.json` as data: 16 operations, param types, return shapes, six error codes with caller-facing meanings, effect-homogeneous groups, sequences, absences, `surface_version`; `instances.json` as a separate grant; `provenance: untrusted` on reads; an audit sink per call; an author-time validator | YES |
| REQ-127 | BUNDLE-17 | free_and_reconciled | 2026-08-08 | Read/write classification becomes **enforced**; withdraws its own "site binding as a declared scope predicate" clause (correctly absent from the story body) | YES |
| REQ-129 | BUNDLE-17 | free_and_reconciled | 2026-08-09 | `get_copy`/`set_copy` retire for `get_l1`/`set_l1`; group `WriteCopy` → `AuthorPages` "and `instances.json`'s caretaker grant with it"; upstream refusal-specificity gap recorded, not claimed closed | YES (governance portion; reach is CAP-93 / STORY-106) |
| REQ-130 | BUNDLE-17 | free_and_reconciled | 2026-08-09 | Five operations added; `write_image` gets "its own capability group (`DrawImages`), separate from `ManageAssets`, so it can be withheld" | YES (governance portion; reach is CAP-94 / STORY-107) |
| REQ-133 | BUNDLE-19 (`bundle-77b28def`) | free_and_reconciled | 2026-08-12 | `get_palette` in `ReadSite`, "the four writes in a new `ManagePalette` group granted to the caretaker" | YES |
| REQ-131 | BUNDLE-19 | free_and_reconciled | 2026-08-11 | A new read operation in `l1-surface.json` placed in `ReadSite`, with a new shape for the journal slice; AC7 (projected into the manual only for a session granted `ReadSite`), AC8 (the slice marked untrusted) | YES |
| REQ-146 | BUNDLE-20 (`bundle-b3b7c399`) | free_and_reconciled | 2026-08-15 | Audit becomes durable and survives the host (AC3); the surface splits into a portable core plus the host's own operations, so "everything declared is callable" is a claim about their composition; `publish` stays ungranted by name (AC7, §4) | YES |
| REQ-149 | BUNDLE-20 | free_and_reconciled | 2026-08-17 | Revisions onto the storage port, so `publish` becomes an ordinary portable-half operation; `add_asset` becomes the sole disk-bound operation | YES |
| REQ-143, REQ-145, REQ-147, REQ-148, REQ-150, REQ-151, REQ-152, REQ-153 | BUNDLE-20 | free_and_reconciled | 2026-08 | **No CAP-92 ask.** Section-by-section scan of the bundle body: zero mentions of `l1-surface.json` or `instances.json` in any of them; REQ-145's two `toolbox` hits are test-family names in its evidence section, REQ-153's single hit states its slug guard "reaches … the AI toolbox's `add_page` … without any of them being changed" | YES, but out of this capability |
| REQ-119, REQ-121, REQ-128, REQ-127-adjacent | BUNDLE-17 | free_and_reconciled | 2026-08 | No CAP-92 ask | YES, out of capability |
| BUG-35, REQ-123, REQ-139, REQ-140, REQ-141, REQ-142, REQ-144 | BUNDLE-19 | free_and_reconciled | 2026-08 | No CAP-92 ask | YES, out of capability |
| BUG-36, BUG-37, BUG-38 | **BUNDLE-21** (`bundle-78f4e2fe`) | free_and_reconciled | 2026-08-26 | **Checked this pass — no CAP-92 ask.** Zero mentions of `l1-surface` / `instances.json` / `toolbox` in the whole bundle body. BUG-38 ("every turn fails in the cloud with 'conversation is no longer open'") is CAP-90's session transport | YES, out of capability |
| BUG-39, REQ-154 | **BUNDLE-22** (`bundle-8eef3846`) | free_and_reconciled | 2026-08-31 | **Checked this pass — no CAP-92 ask.** Zero mentions of the three surface artifacts | YES, out of capability |
| REQ-162 | unbundled | **free_and_reconciled** | — | **Checked this pass — no CAP-92 ask.** Product ticket store (D1 schema, TypePack, material types); zero mentions of `l1-surface` / `instances.json` / `toolbox` / "control surface" / "audit" / "grant" in its body | YES, out of capability |
| REQ-155, REQ-156, REQ-157, REQ-158, REQ-159, REQ-160, REQ-161, REQ-163, REQ-164, REQ-165, REQ-166 | — | **draft** (re-confirmed individually this pass) | 2026-08-20…30 | REQ-157 (fidelity surface), REQ-158/160 (knowledge surface on the toolbox, session seeding) would extend this declaration | NO (draft) |

Nothing in the ledger is `abandoned`, `deprecated` or `wont_fix`, so Step 2.5's
stale-vehicle case does not arise. One clause was retired by its own author
(REQ-127's scope predicate) and is correctly absent from the story body.

### Shipped surface, re-measured at HEAD (`8d9ecf8293`)

`tools/generate/src/cli/ai/l1-surface.json`: **27 operations**,
`surface_version: 4`, **9 groups** — `ReadSite` (11 reads, including
`get_palette` and `list_changes`), `AuthorPages` (`set_l1`), `ManagePages`,
`ManageComponents`, `WriteConfig`, `ManagePalette` (exactly the four palette
writes), `ManageAssets`, `DrawImages`, `Publish`. Error taxonomy: `NOT_FOUND`,
`CONFLICT`, `SCHEMA_INVALID`, `REFERENTIAL_INTEGRITY`, `ENVIRONMENT`,
`INTERNAL`. Nine named sequences. `tools/generate/src/cli/ai/instances.json`
grants `caretaker` seven groups and withholds **`ManageAssets`** and
**`Publish`**.

## Alignment Ledger

| Element | Intents aligned to | Outcome |
|---|---|---|
| STORY-105 — Description, in-scope bullets (declaration / grant / two runtimes / effect enforced / validation before invocation / refusal as information / provenance / durable audit / self-documentation / one write path) | REQ-122, REQ-126, REQ-127, REQ-129, REQ-130, REQ-146, REQ-149 | **aligned** — every bullet traces to a counting intent; no bullet lacks one |
| STORY-105 — Technical Context → Divergences, bullet 2 (operation count) | REQ-129, REQ-130 (BUNDLE-17); REQ-133, REQ-131 (BUNDLE-19) | **aligned (repaired)** — REPORT-3833 finding 1 is closed; every clause of the rewrite re-verified below |
| STORY-105 — Divergences, bullet 1 (publish declared, not granted) | REQ-126 → ratified by REQ-146 AC7/§4 | aligned; historically accurate as written (carried forward as info 1) |
| STORY-105 — Technical Context, final bullet ("The declaration outgrows the capability that owns it") | REQ-131, REQ-133 | **aligned** — verified independently: STORY-115 (`story-6cd17452`, journal) and STORY-113 (`story-ee073693`, palette) both carry `intent_uid: bundle-77b28def` under *different* capabilities (`capability-702b7c02`, `capability-a0bba4ec`) |
| STORY-105 — Reconciliation Decisions (all three) | REQ-146, REQ-149 | aligned; each claim re-verified against the tree below |
| STORY-105 — `fields.updated_by` | `[bundle-b3b7c399, bundle-77b28def]` | **aligned (repaired)** — both reconciled bundles that extended this declaration are now recorded |
| STORY-105 — "## Dependencies" vs "## Technical Context" | — | **aligned (repaired)** — REPORT-3833 finding 2 is closed; Dependencies now carries CAP-86 (`story-37a3921b`) and `capability-c4c7a854`, both qualified "already built", agreeing with Technical Context |
| STORY-105's 14 ACs (AC-1071…1082, AC-1142, AC-1411) | full ledger | **coverage holds** — every in-scope bullet has a criterion: declaration AC-1071/1072/1073/1081/1142, grant AC-1074, effect AC-1075, validation AC-1076, refusal AC-1077, provenance AC-1078, audit AC-1079 + durability AC-1411, self-documentation AC-1080, one write path AC-1082 |
| AC-1411 (durability) | REQ-146 AC3 | expressed as its own criterion exactly as the Reconciliation Decisions require; still `status: pending` with `uat_coverage` unset — info 2, for the ac/uat cycles |
| STORY-102 (`story-c46abfa6`), STORY-106 (`story-189fc1ac`), STORY-113, STORY-114, STORY-115 | REQ-129/130/131/133 and others | **no overlap** — repo-wide scan of all 45 story bodies for `l1-surface` / "control surface API" / "capability group" / `instances.json` returns exactly two files: STORY-105 and STORY-106, and STORY-106 explicitly defers ("the provenance marking and the per-call audit record are **CAP-92's** and are unchanged here") |

### Story-body claims re-verified against the tree this pass

| Claim (STORY-105) | Verified against | Result |
|---|---|---|
| REQ-129 "renamed the `WriteCopy` group to `AuthorPages`" | BUNDLE-17 body, REQ-129 §: "group `WriteCopy` → `AuthorPages`, and `instances.json`'s caretaker grant with it" | holds |
| REQ-130 "declared `DrawImages` as its own group, separate from `ManageAssets` so it can be withheld" | BUNDLE-17 body, REQ-130 §4: "Its own capability group (`DrawImages`), separate from `ManageAssets`, so it can be withheld" | holds |
| REQ-130 "added five operations" | 16 → 21 ops (`add_component`, `configure_component`, `remove_component`, `list_behaviors`, `write_image`) | holds |
| REQ-133 "`get_palette` into `ReadSite` and a new `ManagePalette` group of four writes, granted to the builder's assistant" | BUNDLE-19 body: "`get_palette` in `ReadSite`, the four writes in a new `ManagePalette` group granted to the caretaker"; `l1-surface.json` groups; `instances.json` | holds |
| REQ-131 "`list_changes` into `ReadSite`, returning an untrusted slice, with its own sequence entry and overview paragraph" | `l1-surface.json`: `list_changes` `effect: read`, `returns: {shape: change_log, provenance: untrusted}`, in `ReadSite`; sequence "Pick up after your user has been editing"; overview paragraph "Your user can change the site themselves, while you are working on it" | holds |
| "today the builder's assistant is granted neither the management of image and font files nor publishing" | `instances.json` — `ManageAssets` and `Publish` both withheld | holds |
| "that is now asset management alone" (the sole disk-bound operation) | `toolbox.ts:117` `nodeOperations` supplies `add_asset` and nothing else; its comment at :106-109 records the REQ-149 move of `publish` | holds |
| "one object per record under distinct keys … flushed while the response is still open and inside a `finally`" | `apps/control-app/src/ai.ts:125-146` ("ONE OBJECT PER RECORD rather than a read-modify-write … an audit trail that drops entries under load is worse than none"); `router.ts:659-665,700` ("THE AUDIT IS FLUSHED IN A `finally`, INSIDE THE STREAM … so an abandoned or failed turn still records what it managed to do") | holds |
| Cross-references CAP-86 = `story-37a3921b`; CAP-90 = `story-a58a0974`; site store = `capability-c4c7a854` | ticket store | all resolve — STORY-100 under `capability-f753cecd` = **CAP-86** "Structured Copy Editing: One Validated, Atomic Write Path"; STORY-103 under `capability-7e4714b7` = **CAP-90** "AI Site Assistant: Per-Site Conversations"; `capability-c4c7a854` = **CAP-101** "Site Storage Port: One Async Store Behind Every Edit" |
| "The intent names sixteen operations" | REQ-126 §, quoted in REQ-129's body: "16 operations covering everything `edit.ts` can do" | holds |

## Findings

| # | Severity | Property | Element | Resolution category | Issue | Suggested edit |
|---|---|---|---|---|---|---|
| 1 | info | consistency | STORY-105, Divergences bullet 1 (publish declared but not granted) | — | Carried forward from REPORT-3833 finding 3 so it is not re-opened as drift. The bullet frames the withheld `publish` as a divergence from REQ-126's scope. REQ-146 (free_and_reconciled) AC7 and §4 later ratified the shipped grant by name, and REQ-149 made publish portable without re-granting it. The bullet is historically accurate, and the Reconciliation Decisions already record the ratification | none |
| 2 | info | coverage | AC-1411 (`acceptance_criterion-cb6e1b58`) | — | REQ-146 AC3's durability ask **is** expressed in the tree as its own criterion, so story-level coverage is satisfied. But AC-1411 remains `status: pending` with `uat_coverage` unset (re-checked this pass — `updated_at` 2026-08-31T10:40Z, unchanged), while STORY-105 and CAP-92 both read `uat_coverage: pass`. The three prior ac/uat reports (REPORT-2055/2058/2059, all 2026-08-16) predate the AC's creation | none at story level — the `ac` and `uat` cycles own this |
| 3 | info | coverage | STORY-105 `fields.updated_by` | — | The ledger was walked forward past the story's last rewrite: BUNDLE-21, BUNDLE-22 and the unbundled REQ-162 are all `free_and_reconciled` and all carry **zero** mentions of `l1-surface.json`, `instances.json` or `toolbox`. No newly reconciled intent is missing from the chain | none |
| 4 | info | consistency | REPORT-3833 findings 1 and 2 | — | Both re-verified closed against the store and the tree rather than taken from the fix report: the Divergences bullet now names REQ-129/REQ-130 (BUNDLE-17) and REQ-133/REQ-131 (BUNDLE-19) and every clause of it checks out; `fields.updated_by` carries `bundle-77b28def`; Dependencies and Technical Context agree on CAP-86. The count-independence sentence survived verbatim, as the finding directed | none |

## Notes for the Editor

**Nothing to repair.** Every behavioural bullet in STORY-105's in-scope list
traces to a counting intent; every criterion the ledger calls for is present as
one of the story's 14 ACs; and every checkable factual claim the body makes about
the shipped surface was verified against the tree at HEAD in this pass. The
attempt-2 repair was a citation repair and it landed correctly.

**The watch item the story now carries itself.** The final Technical Context
bullet states the rule that anything extending the declaration belongs in
`updated_by` even when no criterion moves. That rule is the right one and it is
now in the body rather than only in a report — but it has not yet been tested,
because nothing has extended the declaration since BUNDLE-19. REQ-157 (fidelity
surface) and REQ-158/REQ-160 (knowledge surface on the toolbox, session seeding)
are all still `draft` and all extend this same declaration. The next story-level
check after any of those reconciles should start by re-reading `l1-surface.json`
and `instances.json` and diffing the group/operation set against the ledger —
that is the cheapest way to catch the omission, and it is how both prior misses
were found.

**One observation outside this level, recorded not as a finding.** REQ-145's
evidence section (BUNDLE-20, 2026-08-17) records a then-pre-existing baseline of
56 node-suite failures whose families include the AI/toolbox and L1-surface tests
(`test_UAT_FC_REQ-122_*`, `test_UAT_FC_REQ-126/127/129_*`). Whether that baseline
still stands at HEAD is a `uat`-level question and was deliberately not
investigated here; it is noted so the uat cycle does not treat it as new.

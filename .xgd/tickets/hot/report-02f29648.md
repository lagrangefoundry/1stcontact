---
uid: report-02f29648
id: REPORT-3225
type: report
title: 'Reconcile resolve conflicts: reconcile-REQ-162'
created_by: xgd
created_at: '2026-09-01T21:59:45.457591+00:00'
updated_at: '2026-09-01T21:59:45.457591+00:00'
completed_at: null
last_field_updated: created_at
result: pass
fields:
  report_kind: resolve_conflicts
  subject_uid: reconcile-REQ-162
---

## Files resolved

- `.xgd/tickets/hot/bundle-8eef3846.md` — class **AA** (both added), rule **2e** (intent/bookkeeping ticket) + **2b** (strict superset). Resolved to **ours** via `git checkout --ours`, then `git add --sparse`.

  Incoming (`c2efcb9` — `xgd(ticket): create bundle bundle-8eef3846`, 2026-08-30 22:05 -0700) is the original *creation* snapshot of the bundle ticket: `status: ready_to_reconcile`, `completed_at: null`, `last_field_updated: created_at`, two unreconciled `commits[]` entries carrying `working_sha`.

  Ours (HEAD, latest `2ca3de8` — `xgd(ticket): update bundle bundle-8eef3846`, 2026-08-31 17:00 -0700) is the same ticket advanced through its own lifecycle: `status: free_and_reconciled`, `completed_at: 2026-08-31T23:59:50Z`, `result: pass`, `merged_at_commit: 90527353…`, the `commits[]` entries collapsed to a single reconciled entry with `main_sha: 90527353…`, plus a ~330-line `orphan_commits` old_sha→new_sha map.

  A full blob-level diff of stage 3 (incoming) against stage 2 (ours) shows **336 insertions / 12 deletions**. Every deletion is a superseded metadata scalar (`updated_at`, `completed_at`, `last_field_updated`, `status`) or a pre-reconcile `commits[]` entry that ours legitimately replaced with the post-reconcile form. The prose body is byte-identical on both sides; the sole body-level difference is a trailing newline (incoming ends `Done, three UATs\n`, ours omits the final newline). No incoming prose, field, or section is absent from ours.

  Both the 2e superset rule and the auto-enrichment tiebreak ("take the more recent commit by timestamp") select ours: ours is ~19h later.

## Incoming changes preserved

Confirmed. This is the **seeded-overlay redundant-create** case, not a discard:

- `git log --grep='create bundle bundle-8eef3846' -- <path>` returns nothing on HEAD, i.e. HEAD has no *create* commit for this file — the ticket store was seeded into the reconcile branch as an overlay rather than replayed commit-by-commit.
- Consequently the incoming create commit's entire 369-line payload is already materialised in HEAD, arrived via the overlay route, and has since been advanced by five `update bundle` commits. Verified content-wise by the stage2-vs-stage3 diff above: ours ⊇ incoming.
- Therefore the staged diff vs HEAD is empty. Per STEP 4 this is the *redundant* case (incoming's key changes are present in HEAD), explicitly distinguished from the *discarded* case (they would be absent). Python's finalize step will skip the commit.

No code/implementation files were involved in this conflict, so no BUG-1301 precedence exception was invoked and no test function was touched.

## Post-merge review flag

The auto-enrichment classified intent as unknown on one or both sides and asked that this file be flagged for post-merge review. Flagging it here for completeness, though the superset relationship above makes the outcome unambiguous: no incoming fact was dropped, only superseded metadata.

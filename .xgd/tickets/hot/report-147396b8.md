---
uid: report-147396b8
id: REPORT-3577
type: report
title: 'Reconcile resolve conflicts: reconcile-BUNDLE-26'
created_by: xgd
created_at: '2026-09-09T23:16:34.153707+00:00'
updated_at: '2026-09-09T23:16:34.153707+00:00'
completed_at: null
last_field_updated: created_at
result: pass
fields:
  report_kind: resolve_conflicts
  subject_uid: reconcile-BUNDLE-26
---

## Files resolved

- `.xgd/tickets/hot/bundle-b3b7c399.md` — **AA (both added)**, intent/bookkeeping
  ticket (rule **2e**), outside the sparse-checkout cone so staged with
  `git add --sparse`. Resolved to **OURS (HEAD)**.

  Incoming commit `830f0264` = `xgd(ticket): create bundle bundle-b3b7c399`
  (2026-08-23), the original creation snapshot of BUNDLE-20.
  HEAD side = `8e07e601` `xgd(ticket): update bundle bundle-b3b7c399`
  (2026-08-31), the same ticket after its full reconcile lifecycle.

  Diff between the two sides is exactly two hunks:
  1. the frontmatter `fields:` block (ours lines 8–408);
  2. a trailing-newline-only difference at EOF.
  The 2400+ line body — every bundled requirement section, in the same order —
  is byte-identical on both sides.

  Every differing frontmatter fact is the *same field advanced by time*, later
  on ours in each case:
  - `updated_at`: 2026-08-24 (theirs) → 2026-08-31 (ours)
  - `status`: `ready_to_reconcile` (theirs) → `free_and_reconciled` (ours)
  - `completed_at`: `null` (theirs) → `2026-08-31T14:22:24Z` (ours)
  - `last_field_updated`: `created_at` (theirs) → `result` (ours)
  - `fields.commits`: 24 pre-reconcile `working_sha` entries (theirs) →
    collapsed to one entry carrying `main_sha: eef7a8b4` (ours)
  - ours additionally carries `fields.orphan_commits` (~140 old→new remap
    pairs), `fields.merged_at_commit: eef7a8b4`, and `result: pass`, none of
    which theirs ever had.

  This is pure lifecycle progression along one ticket's own timeline — there is
  no disjoint edit on the incoming side to compose in, so 2e's per-fact
  later-intent rule resolves every fact to ours. This also matches the
  auto-enriched resolution rule for this file ("take the more recent commit by
  timestamp").

  Verified `merged_at_commit: eef7a8b48bfa15c54b64db9541a0e781a016ba9e` is
  reachable from `main`, i.e. BUNDLE-20 genuinely completed and merged. Taking
  the incoming side would have reverted an already-merged bundle from
  `free_and_reconciled` / `result: pass` back to `ready_to_reconcile`.

## Incoming changes preserved

No code/implementation files were in conflict — the single conflicted path is a
bookkeeping ticket (2e), not a `free_coded` code file, and the incoming commit
is an xgd machine-generated ticket commit, so the "incoming is authoritative for
code" rule does not bear on it.

STEP 3 check for the one resolved file: the incoming commit's substantive
content is **present** in HEAD, not discarded. `git show 830f0264 -- <file>`
is the creation of this ticket in full; HEAD contains that ticket with identical
`uid`, `id`, `type`, `title`, `created_by`, `created_at`, and a byte-identical
body. The only incoming values absent from the resolution are the pre-reconcile
bookkeeping fields (the 24 `working_sha` values and `status:
ready_to_reconcile`), which HEAD *superseded* by advancing the same ticket
through reconcile to `free_and_reconciled` with `main_sha`/`merged_at_commit`
recorded. That is supersession via a different route, not a discard.

No hunks were dropped under the BUG-1301 precedence exception; no UAT or test
files were involved, and no test function on either side was deleted.

## Note on the staged result

The staged diff against HEAD is empty (`git diff --cached HEAD` returns
nothing). This is the expected BUG-1109/BUG-1122 shape: the incoming commit's
effect (this bundle ticket existing) already landed on this branch by a
different route, and the ticket then progressed further. Per STEP 4, `--skip`
was **not** called and no cherry-pick state transition was performed —
`CHERRY_PICK_HEAD` (`830f0264ef71b7adf47997c74e7b02a3b2074b49`) is still present
for `cherry_pick_finalize_resolution` to act on. STEP 3 distinguishes this
redundant case from a discard: the incoming commit's key content is present in
HEAD, not merely absent.

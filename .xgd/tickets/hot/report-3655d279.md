---
uid: report-3655d279
id: REPORT-3793
type: report
title: 'UAT Coverage: Site Materials & Starting Point: Scaffold, Assets, Provenance
  & Palette'
created_by: xgd
created_at: '2026-09-10T20:42:58.609192+00:00'
updated_at: '2026-09-10T20:42:58.609192+00:00'
completed_at: null
last_field_updated: created_at
result: fail
fields:
  report_kind: uat_coverage_check
  subject_uid: capability-b4ac88fc
  violations: 1
  warnings: 3
  needs_review_count: 0
---

# UAT Coverage Assessment: Site Materials & Starting Point: Scaffold, Assets, Provenance & Palette

**Result**: FAIL
**AC verdicts**: 38 pass, 0 fail, 0 deprecated, 0 needs_review
**Story verdicts**: 3 pass, 0 fail, 1 stale, 0 needs_review
**Capability verdict**: fail

Anchor report: report-e37a6b4a (regression start `800a17f7`) · Capability:
capability-b4ac88fc (CAP-89) · 4 stories · 38 active ACs · 38 UAT functions
across 4 files, one test per AC, no AC number claimed twice ·
previous_attempt_count: 3.

**The failure is not a coverage gap.** All 38 acceptance criteria are active per
cumulative intent and substantively evidenced. Nothing here asks for a UAT to be
authored, edited or deprecated. The single violation is **story-body drift**: one
Technical Context bullet in STORY-97 names two example sites that REQ-140 §7
deleted. Under Step 2a that is a `stale` story verdict, and under Step 3 any
stale story makes the capability aggregate `fail`.

**The evidence was executed this run.** Unlike the previous round
(report-71ea4a83, which could reach no test runner), `npm test -- <files>` is
available in this session and all four suites were run:

| Suite | Result |
|---|---|
| `tests/reconciliation-colour-retrofit-shade-model.test.ts` | **12 passed** (AC-932, 939–947, 1146, 1147) |
| `tests/reconciliation-font-provenance.test.ts` | **12 passed** (AC-857–868) |
| `tests/reconciliation-scaffold-starter-l1.test.ts` | **7 passed, 1 skipped** (AC-869–876; AC-871 browser-gated — warning 1) |
| `tests/reconciliation-site-asset-listing.test.ts` | **5 passed, 1 not executed** (AC-1018–1023; AC-1023 blocked by this sandbox's `listen EPERM` — warning 2) |

So 36 of the 38 ACs are evidenced by an executed, passing test on this branch;
the remaining two are evidenced by reading, and both non-executions are
environment artifacts of this session rather than defects in the tests. No AC
verdict rests on a test merely existing.

## Cumulative Intent Considered

Chronological ledger. Bundle members are listed individually: the stories carry
the bundle UID as `intent_uid` but align to specific members. Every status was
re-read from the ticket store this run.

| Intent ID | Status | When | Asked / changed | Counts? |
|---|---|---|---|---|
| REQ-102 (BUNDLE-11, `bundle-ee56a66e`) | free_and_reconciled | merged `f9a415a8` 2026-08-06 | `1c new` seeds a complete valid L1 document — ladder, background, flowed root, one placeholder run; renders and shoots unedited; `1c repro` overwrites wholesale; no flag, no mode detection → STORY-93 | YES |
| REQ-101 (BUNDLE-11, `bundle-ee56a66e`) | free_and_reconciled | merged `f9a415a8` 2026-08-06 | `fonts/registry.yaml` provenance index; three-state `redistribute_in_product`; `distribution` marker on the site contract; `1c fonts check` with four violation kinds + on-disk scan; actions warn, redistribution blocks; a broken record is a hard error → STORY-92 | YES |
| REQ-114 (BUNDLE-14, `bundle-0385746c`) | free_and_reconciled | merged `cd8f98c8` 2026-08-06 | Palette colour model (model half → STORY-80, CAP-70); census; retrofit; §4 **retired the theme colour group outright** → STORY-97, and `updated_by` on STORY-93 | YES (and retires) |
| REQ-118 (`request-66e4c630`) | free_and_reconciled | merged `b2b9208c` 2026-08-07 | `listSiteAssets` — union of declared registry + the site's asset store, merged by handle, `onDisk`/`registered` provenance, one handle vocabulary, derived `kind`, reachable from CLI and `/api/assets` → STORY-102 | YES |
| REQ-128 (`request-de67e1a1`) | free_and_reconciled | 2026-08-10 | Background-image picker over the *same* listing; explicitly reuses it, adds no source | YES (no delta here) |
| REQ-132 (`request-5946d045`) | free_and_reconciled | merged `6cb3942f` 2026-08-12 | Image picker becomes a thumbnail grid with file-name labels, implemented **locally** | YES (retired STORY-102's old "upstream limitation" text — **already repaired**, see Alignment Ledger) |
| REQ-133 (`request-8467b1a3`) | free_and_reconciled | 2026-08-12 | Palette popup: display, pick and edit the site's colours — landed as its own capability, **CAP-98** | YES (no UAT obligation here) |
| REQ-137 (`request-d2980a95` / BUNDLE-18 `bundle-d9226698`) | **free_and_reconciled** | merged `bbdde0d0` 2026-08-17 | Deleted palette entry `steps` for a continuous `shade`; superseded REQ-114's byte-identity with a measured ≤8/255 per-channel bound; a colour more saturated than its base becomes its own exact entry | **YES — landed since the last round** |
| REQ-140 (`request-3c0fec69`) | free_and_reconciled | 2026-08-15 | Page-editor colour from the palette (CAP-98); **§7 deleted `storage/sites/1stcontact` and `storage/sites/harbor-cafe` as dead examples**, operator-confirmed | **YES (and retires) — the violation below** |
| REQ-135, REQ-136, REQ-138, REQ-139 | free_and_reconciled | 2026-08-12 | Editor property surfaces — CAP-86/CAP-87 territory | YES (no delta here) |
| REQ-142 / REQ-143 (`request-0dd62a5d`, `request-18a48d63`) | free_and_reconciled | 2026-08-15 | Async `SiteStore` port; the Cloudflare adapter (definitions in D1, bytes in R2) → CAP-101 | YES (no delta: the filesystem adapter still serves every surface here, and all four suites pass on it) |
| REQ-145 / REQ-149 / REQ-150 (`request-b474390f`, `request-554ac441`, `request-34dd9049`) | free_and_reconciled | 2026-08-15 → 08-18 | control-app becomes the builder; publish in the cloud; plain Vite SSR replaces Astro | YES (no delta: AC-1023's `startBuilder` + `/api/assets` route and the builder's own `fetchAssets` client all still exist and are exercised) |
| REQ-155 (`request-01ea4eec`) | **draft** | 2026-08-20 | Capture in workerd — a `ReferenceStore` port off the filesystem; `storage/references/` is exactly the tree STORY-92's on-disk scan exists to catch | **NO (not yet active)** — flagged for the next round |
| REQ-134 (`request-ba3e3fba`) | abandoned | 2026-08-12 | Image generation component | NO |

Story→intent bindings read directly this run: STORY-93 `bundle-ee56a66e`
(`updated_by: bundle-0385746c`), STORY-92 `bundle-ee56a66e`, STORY-97
`bundle-0385746c` (`updated_by: bundle-d9226698`), STORY-102 `request-66e4c630`.

**Verified in the tree rather than inferred:**

| Claim | Where checked | Current tree |
|---|---|---|
| REQ-137 **has** landed (it was `bundled`/absent last round) | `packages/site-schema/src/l1/palette.ts`; `tools/generate/src/cli/colors.ts` exports `SHADE_FIT_TOLERANCE`, `fitShade`, `toHcl`; `shadeHex` exported from the model | `steps` gone, `shade` live; the 12-test shade-model suite passes |
| REQ-140 §7 deleted the two example sites | `storage/sites/` | holds `gigabytealchemy` and `xgd` only — `1stcontact` and `harbor-cafe` are gone |
| The retrofit's stored outcome matches STORY-97's recorded observation | `storage/sites/*/draft/site.json` | `xgd` → 7 entries with the curated role names (`neutral, text, primary, accent, surface, surface-accent, primary-bright`); `gigabytealchemy` → 15 |
| STORY-102's motivating disagreement still holds | both stored sites | `assets: []` in each definition beside a populated `draft/assets/` — a registry-only listing would still name nothing |
| The superseded colour suite was retired, not left orphaned | `tests/` | `reconciliation-colour-census-and-retrofit.test.ts` is gone (commit `b8399b2f`); its ACs now live in `reconciliation-colour-retrofit-shade-model.test.ts` |
| CAP-80 is no longer a separate capability | capability list | `capability-745b9a6c` is `superseded` into CAP-89 — warning 3 |

## Alignment Ledger

| Story | Intents aligned to | Outcome | Notes |
|---|---|---|---|
| STORY-92 (`story-8685be2d`) | REQ-101 | **aligned / pass** | Every behaviour the body claims — the record contract, the four violation kinds, warn-vs-block, the distribution marker, the report and its JSON form, the hard error — is asserted by AC-857…AC-868 and all 12 tests pass. REQ-143 moved *site* bytes to R2 but the on-disk fonts scan still reads the source trees; `cmdFontsCheck(REPO_ROOT)` passes with non-zero registered/reference/file counts, so the pass is not vacuous. The "no acquisition verb" clause is an out-of-scope statement, not a behavioural promise, and needs no UAT. |
| STORY-93 (`story-86c7c21b`) | REQ-102 (origin), REQ-114 (`updated_by`) | **aligned / pass** — *was `stale`* | The previous round's violation ("the theme-sourced colours" in the closing Technical Context bullet) was repaired by report-42796731 / commit `90b31152`; re-read from the persisted body this run, it now says "the page-declared literal colours", and a whole-body sweep for `theme palette` / `theme-sourced` finds nothing. Coverage complete: AC-869…AC-876, 7 executed passes + AC-871 browser-gated. |
| STORY-102 (`story-c46abfa6`) | REQ-118 (origin); REQ-128, REQ-132 downstream | **aligned / pass** — *was `stale`* | The previous round's violation (the "Known upstream limitation" paragraph REQ-132 falsified) was repaired in the same fix pass: the body now carries "The listing carries no label and no thumbnail, by boundary", the word "upstream" is gone, and presentation is correctly assigned to the editor-gesture capability. Coverage complete: AC-1018…AC-1023. One stale cross-reference remains but is substantively correct — warning 3. |
| STORY-97 (`story-5e7eb0c5`) | REQ-114 (origin), **REQ-137** (`updated_by`), REQ-140 (site deletion) | **stale** | The REQ-137 rewrite is genuinely complete and well evidenced: the shade model, the measured 8/255 bound, chroma-based neutrality, the family-change refusal, the two new ACs (AC-1146, AC-1147) and a 12-test suite that all passes. What is left is a single REQ-140 residue — the "two of the four sites are vacuously retrofitted" bullet, which names two sites that no longer exist. Violation 1. Coverage itself is complete: all 12 ACs pass. |

Exclusivity holds: no AC number appears under two stories, and exactly one test
claims each AC (38 ACs, 38 `test_UAT_AC<n>_…` functions, verified by a
whole-tree scan). STORY-92 and STORY-102 both enumerate font files but answer
different questions — project-level licence obligation vs site-level reference
listing — and STORY-102 states that boundary explicitly.

## AC-Level Coverage — how each verdict was reached

All 38 ACs are `status: active`, `kind: behavior`, and none is retired by any
reconciled intent. **No test in this capability mocks the thing it is testing**;
the only test doubles anywhere are `console.log` / `console.error` spies that
capture a CLI's own output stream — the observation, not the subject. No test is
structural (none reads source text to assert a name appears).

| ACs | Test file | Why the coverage is substantive |
|---|---|---|
| AC-932, 939–947, 1146, 1147 | `tests/reconciliation-colour-retrofit-shade-model.test.ts` (12 tests, all passing) | The stdout/exit-status ACs drive the **shipped `1c` launcher as a subprocess** against a throwaway `--sandbox` store; the derivation ACs drive `cmdColors` / `cmdColorsAssign` against isolated temp trees seeded from the real stored sites. AC-939 parses the header, checks per-literal ordering and α annotation, and proves read-only by a before/after sha256 tree hash. AC-941 measures "materially smaller" **by census** rather than baking a number in, and reconciles the reported file list against an actual changed-file diff. AC-943 pins six distinct derivation properties against fixtures built *by the shade axis itself*, including a Δ3-but-misclassifying fit that proves the refusal is the classification test rather than the distance test. AC-944 pairs every literal with the reference that replaced it **positionally**, re-derives the per-channel delta independently of the code that enforces it, and includes a fixture that genuinely drifts so "worst first" is not a vacuous truth about an empty list. AC-1146 asserts the more-saturated member gets its own entry, carries no shade, resolves byte-exactly, and is *absent* from the drift report. AC-1147 resolves every reported drift through the model's own `resolveL1Color`/`shadeHex` and asserts the launcher's stdout agrees. AC-945 exercises all three refusal causes with tree hashes proving nothing was written. AC-947 proves the fixpoint byte-for-byte *and* that stdout is identical. |
| AC-857…AC-868 | `tests/reconciliation-font-provenance.test.ts` (12 tests, all passing) | The **shipped** `fonts/registry.yaml` is validated entry-by-entry plus four damaged candidates with expected error paths (857); each violation kind is provoked by a purpose-built workspace and asserted on kind, message, remediation and CLI exit (858–861); the redistribution gate is driven as a 2×2 matrix with everything but the two inputs held constant (861); the marker goes through `validateSite`, including rejection of `oem` (862); advisory warnings are asserted with blast radius *and* their absence (863); all four record-integrity failures also run through the real CLI asserting non-zero exit and no `PASS` on stdout (864); both site trees with per-tree attribution (865); six reference forms plus a query-bearing end-to-end pass (866); `cmdFontsCheck(REPO_ROOT)` against the real project with non-zero scan counts, so a pass cannot come from an empty scan (867); `--json` on a passing and a failing project with flag/exit agreement and nothing else on the stream (868). |
| AC-869…AC-876 | `tests/reconciliation-scaffold-starter-l1.test.ts` (7 passing, 1 browser-gated) | Calls `cmdNew`/`cmdRender`/`cmdShot`/`cmdRepro` and reads the artifact **back off disk** rather than recomputing it from the scaffold function. AC-869 validates the assembled definition through `validateSite` (envelope, node cap, ids), not just the document. AC-870 asserts the run paints inside `<body>` — explicitly guarding against the slug matching only in `<title>` — and reads the background colour back off disk rather than restating it. AC-872 compares element-wise against `RESPONSIVE_VIEWPORTS` and asserts `STARTER_WIDTHS` is derived, not restated. AC-873 collects every colour in the seeded document and asserts each is one the document itself declares. AC-874 asserts `geometry` undefined on every node **and** no `position: absolute` in the emitted CSS. AC-875 reads `1c help` from the shipped launcher as a subprocess. AC-876 byte-compares a repro over a seeded slug against a repro over a virgin slug. |
| AC-1018…AC-1023 | `tests/reconciliation-site-asset-listing.test.ts` (5 passing, AC-1023 not executable here) | Drives `run(argv)` — argv in, `{ok,data}` envelope and exit code out. AC-1020 compares the listed handle against what a real page node actually holds, and pins the off-site-URL boundary so normalisation cannot manufacture `/assets/https://…`. AC-1022 asserts the full entry shape and that an empty store is a success, not a failure. AC-1023 starts a real builder, issues a real HTTP `GET`, compares the response **entry-for-entry against the CLI's own answer** rather than against a constant alone, and drives the builder's own `fetchAssets` client — substantive by construction, though it could not run in this sandbox. |

## Findings — Categorized by Editor Action

| # | Severity | Level | Element | Resolution category | Issue | Suggested edit |
|---|---|---|---|---|---|---|
| 1 | violation | story | STORY-97 (`story-5e7eb0c5`) | story-body-edit | Technical Context bullet "**Intent/observation note — two of the four sites are vacuously retrofitted**" reads: "The intent's AC6 asks for all four `storage/sites/*` retrofitted. `1stcontact` and `harbor-cafe` census at zero colour literals, so there is nothing to convert and no palette is written." REQ-140 (`request-3c0fec69`, free_and_reconciled) §7 **deleted both of those sites**, operator-confirmed, and its own AC7 asserts they are gone. Verified: `storage/sites/` holds `gigabytealchemy` and `xgd` only. The present-tense claim is false — a site that does not exist cannot census. This is the last residue of a sweep already done everywhere else: AC-932's body was rewritten to the synthesised floor case, and both affected tests carry explicit `SYNTHESISED, not stored (REQ-140)` comments naming the deletion. The bullet's stated audience is "a test author", which is exactly who it now misleads. **No AC or UAT is wrong** — AC-932 and its test are correct and passing. | Rewrite the bullet. Keep the durable point and the history, drop the false present tense — e.g.: "REQ-114 AC6 asked for all four `storage/sites/*` retrofitted; two of them (`1stcontact`, `harbor-cafe`) censused at zero colour literals and were later deleted as dead examples by REQ-140 §7. The durable property is that a colourless site retrofits to an *empty* palette rather than refusing — pinned by AC-932 against a synthesised page, so it no longer depends on a particular stored site." Also drop or requalify the four-site denominator in the neighbouring "retrofit reach" bullet if the editor judges "both stored sites" clearer than "the two stored sites". |
| 2 | warning | ac | AC-871 (`acceptance_criterion-b17420aa`) | — | The UAT is registered with `it.runIf(await chromiumAvailable())` (`tests/reconciliation-scaffold-starter-l1.test.ts:47-48,177`), so on a host with no reachable headless browser the screenshot criterion is evidenced by nothing while the suite still reports green — which is what happened this run (7 passed, **1 skipped**). Held at warning, unchanged from the previous round, because AC-871's own Verification clause sanctions the gate ("Requires a headless browser; the check is gated on browser availability") — criterion and test agree. | Nothing at the UAT level. Unconditional evidence would require editing AC-871's body to drop the gating clause **and** guaranteeing a reachable browser in the regression environment — an `ac`-level decision plus an environment one. |
| 3 | warning | uat | AC-1023 (`tests/reconciliation-site-asset-listing.test.ts:364-411`) | — | The test could not execute in this session: `startBuilder` fails with `listen EPERM: operation not permitted 0.0.0.0` from this sandbox's network policy, so the `beforeAll` hook times out and the suite reports a failed *suite* alongside 5 passing tests. This is **not** a defect in the test or in the code — the test is unconditional and substantive (real HTTP against a started builder, compared entry-for-entry against the CLI's answer, plus the builder's own client), and it was passing as recently as report-8cf89ae5. Recorded so a future reader does not mistake the red suite line for a product regression. | No matrix edit. If this check is expected to execute its own evidence, the regression environment needs loopback `listen` permitted; alternatively AC-1023 could gate on port availability the way AC-871 gates on the browser — but that would trade a hard failure for a silent skip and is an `ac`-level decision, not a UAT one. |
| 4 | warning | story | STORY-102 (`story-c46abfa6`) | story-body-edit | Technical Context refers twice to "**CAP-80** (Asset Provenance & Licence Compliance)" as a separate capability that owns licence obligations, and the Out-of-scope list defers to "a project-level question (CAP-80)". `capability-745b9a6c` (CAP-80) is now **`superseded`** — it was consolidated into this very capability, where the concern lives as its sibling STORY-92. The *substance* is still exactly right (licence obligation attaches to the asset, a listing attaches to the site; the two are held apart), so this is a stale pointer rather than a false claim — warning, not a violation. | Repoint the two references: "the project-level licence obligation over the same files, which this capability's sibling story STORY-92 owns". Leave the boundary statement itself alone; it is correct. Same sweep applies to the CAP-70 / CAP-86 references, which are still live capabilities and need no change. |

## Notes for the Editor

- **One edit closes the only violation, and it is a three-sentence rewrite.**
  Everything else in this capability is green. After STORY-97's REQ-140 bullet is
  repaired, STORY-97 flips to `pass` and CAP-89 flips to `pass` with no other
  work. Warning 4 is worth folding into the same pass since it is also a
  Technical Context pointer fix, but it does not gate anything.

- **Both of the previous round's violations were genuinely fixed, and were
  re-verified here against the persisted bodies rather than taken on the fix
  report's word.** STORY-93's "theme-sourced colours" clause and STORY-102's
  "Known upstream limitation" paragraph are both gone. The previous round's
  warning 4 (AC-932's test freezing today's repo colour counts with `toBe`) is
  also resolved — AC-932 was rewritten entirely into the colourless-site floor
  case and its test now asserts that against a synthesised page, so nothing in
  this capability's evidence depends on a stored site's colour census any more.

- **Nothing here asks for a UAT to be written, edited or deleted.** Coverage is
  complete: 38 active ACs, 38 tests, one per AC, all driving real entry points,
  36 of them executed and passing on this branch. If a fix workflow reads this
  report looking for `uat-add` or `uat-edit` work, there is none.

- **The REQ-137 reconcile was clean, and this is the round that confirms it.**
  The previous report carried it as warning 5 — "flagged so the reconcile of
  `bundle-d9226698` rewrites STORY-97's title, its ramp-grouping text, AC-943,
  AC-944 and their two tests in one pass". All of that landed: the title now says
  "within a proven per-channel bound", AC-943/AC-944 describe the shade model,
  AC-1146 and AC-1147 were added for the two new properties, the superseded
  suite was deleted rather than left orphaned, and the whole replacement suite
  passes. AC-1146 and AC-1147 carried **no** `uat_coverage` field at all before
  this round; both are now `pass`.

- **The one intent to watch.** REQ-155 (`request-01ea4eec`) is still `draft`, so
  it counts toward nothing today, but it ports the *reference* store off the
  filesystem — and `storage/references/` is precisely the tree STORY-92's on-disk
  scan exists to catch (`tools/generate/src/cli/fonts.ts`, exercised by
  AC-860's capture-mirror fixture). If it reconciles, STORY-92's "the check scans
  the source trees on disk" and AC-860 both need re-reading in the same pass.

- **Only three AC/story fields were rewritten, deliberately.** 36 of the 38 ACs
  already carried `uat_coverage: pass`, which is the verdict this assessment
  independently reached; re-writing an identical value would only churn ticket
  commits. What changed and was written: AC-1146 and AC-1147 (no field → `pass`),
  STORY-93 (`stale` → `pass`), STORY-102 (`stale` → `pass`), STORY-97 (`pass` →
  `stale`), and CAP-89 (`fail`, reaffirmed). STORY-92 was checked and left at
  `pass`.

- **Tooling observation, not a capability finding.** `.xgd/uat_index.json` on this
  branch still contains `"acs": {}` — zero entries, unchanged since
  2026-09-09T22:50Z — so the AC→test lookup this prompt prescribes returns
  nothing for every AC in the capability. The mapping was recovered instead by
  scanning `test_UAT_AC<n>_` across the tree, which found exactly 38 tests, one
  per AC, with no AC number appearing twice. Worth knowing before another
  assessor reads an empty index as "no tests exist" — this is the second
  consecutive round in which it has been empty.

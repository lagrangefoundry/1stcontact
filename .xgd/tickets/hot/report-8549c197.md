---
uid: report-8549c197
id: REPORT-3664
type: report
title: Fix Platform Build, Deploy & Live-Origin Verification (story) — attempt 1
created_by: xgd
created_at: '2026-09-10T04:46:32.580056+00:00'
updated_at: '2026-09-10T04:46:32.580056+00:00'
completed_at: null
last_field_updated: created_at
fields:
  report_kind: fix_structural_validation
  subject_uid: capability-5d07b533
  level: story
  fixes_applied: 3
  progress_made: true
  needs_more_work: false
  violations_remaining: 0
  anchor_report_uid: report-e37a6b4a
---

# Fix Summary — Platform Build, Deploy & Live-Origin Verification (story)

**Attempt**: 1
**Fixes applied this call**: 3
**Violations remaining**: 0
**Needs more work**: false

All three findings shared one root cause — STORY-119's 2026-08-31 pass reconciled BUNDLE-21's
retention criteria without revisiting the deployment-state narrative that BUG-36 and BUG-37 (in
that same bundle) had already falsified. Per the report's Notes for the Editor, all three
passages were corrected in a single `ticket update`, so the story never passes through a state
where one passage contradicts another.

## Actions Taken — by Resolution Category

| # | Category | Element | Action |
|---|---|---|---|
| 1 | story-body-edit | STORY-119 (story-d5167ced), **Out of scope**, bullet 1 | Kept the exclusion; dropped both false premises ("has never been deployed", "its hostname does not resolve") and the "not against production" derivation. Restated as: the two control-surface checks are asserted against a **supplied** origin — the seam every other check uses — and production is now one such origin, citing `apps/control-app/ACCESS.md` § "Verifying it" (`bin/smoke --control-origin https://app.1stcontact.io`) |
| 2 | story-body-edit | STORY-119, **Technical Context** → "Where the intent's premise was wrong on the facts" | Re-tensed the two findings to REQ-144's reconciliation (past tense, "the `[vars]` bug was never live at that point"), then added a supersession paragraph recording BUG-36 (2026-08-23: Worker deployed, hostname resolving, `GET /api/sites` → `302` to `lagrangefoundry.cloudflareaccess.com/cdn-cgi/access/login/…`, deployed D1 read with `wrangler d1 execute … --remote`) and BUG-37 (2026-08-24: live Error 1102 on the same hostname). The passage's conclusion — no AC here claims a live control-application deploy — is preserved, with the supplied-origin choice now explicitly noted as surviving the deployment state moving in either direction |
| 3 | story-body-edit | STORY-119, **Technical Context** → "Outstanding at reconciliation time, and why" | Replaced the "outstanding" framing with what is now true: cites REQ-149's `bin/deploy --dry-run control-app` with `ANTHROPIC_API_KEY` unset reporting `ANTHROPIC_API_KEY already on 1stcontact-control-app — would leave it`, and REQ-149's own decision table (only a *positive* read of the live store reaches that outcome; a Worker absent on a first deploy counts as absent). Reframed AC-1342's scope as the durable property — nothing committed, nothing echoed — rather than a hedge against an unproven push |

Heading text for passage 3 changed from "Outstanding at reconciliation time, and why" to a
statement of the exercised mechanism, since the passage no longer records anything outstanding.
Every other section, bullet and sentence of the story is byte-identical (edits applied as three
asserted-unique string replacements over the fetched body; 21259 → 22477 chars).

## Verification

- Re-read the stored body after the write: none of `has never been deployed`,
  `does **not resolve at all**`, `not against production`, `Outstanding at reconciliation time`,
  or `has **not** been proved end-to-end` remain.
- Facts re-checked against the source tickets rather than the report's summary before editing:
  BUG-36 § "Production state — confirmed empirically (2026-08-23)" and its second finding
  (the `302` to the Access login page); BUG-37's Error 1102 in Edit mode on `app.1stcontact.io`;
  REQ-149 line 474-476 (the live-store probe result) and its ACs 13–16.
- Repository facts re-checked: `apps/control-app/wrangler.toml` carries
  `ACCESS_TEAM_DOMAIN = "lagrangefoundry.cloudflareaccess.com"` under `[env.production.vars]` and
  `ACCESS_DEV_OPEN = "1"` at the top level only (the one stated exception, holding);
  `apps/control-app/ACCESS.md:130` § "Verifying it" documents the `bin/smoke --control-origin`
  invocation against production.
- Scanned all 18 acceptance criteria on this story for the same stale premises
  (`never been deployed`, `does not resolve`, `not against production`, `local deploy`,
  `throwaway`, `not been proved`): **all clean**. This independently confirms the report's
  "No acceptance criterion needs to change" — the drift was confined to the prose explaining why
  the supplied-origin and parsed-configuration seams were chosen, not to the seams themselves.

No AC was added, edited or deprecated, and no `uat_coverage` field was touched — that field is
owned by check/fix_uat_coverage.

## Code Edits (if any)

None this call.

The report's non-finding note about `bin/build`'s header comment ("Three stages" followed by an
enumeration of four) was deliberately left alone: the report places it outside this check's remit,
it is a code comment rather than matrix drift, and the story body and the script both already say
four.

## needs_review Items Forwarded

None. Findings 4, 5 and 6 were informational with resolution category "none" and required no
action.

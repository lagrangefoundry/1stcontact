---
uid: report-500d9132
id: REPORT-2805
type: report
title: 'Reconcile resolve conflicts: reconcile-BUNDLE-20'
created_by: xgd
created_at: '2026-08-31T07:32:25.275792+00:00'
updated_at: '2026-08-31T07:32:25.275792+00:00'
completed_at: null
last_field_updated: created_at
result: pass
fields:
  report_kind: resolve_conflicts
  subject_uid: reconcile-BUNDLE-20
---

## Files resolved

- `.xgd/tickets/hot/request-3a064234.md` — class **AA** (both added), intent/bookkeeping ticket (rule **2e**), path outside the sparse-checkout cone (DOC-986 §2/§4.1) so the conflict existed index-only with no working-tree markers.
  - Resolution: **kept the incoming (theirs) superset.** Diffing the two index stages (ours `97c6ce7d`, theirs `43ce1458`) showed the sides are identical except that the incoming version adds a single frontmatter field under `fields:`:
    ```
    +  chat_comment: comment-4fbb25f7
    ```
    This is exactly 2e's "one side is a strict superset — incoming only added a field the other side never touched." No competing per-fact edit exists, so no `xgd working-timeline` tiebreak was needed. Ours contributed nothing that theirs lacks, so nothing was lost by taking theirs whole.
  - Commands: `git checkout --theirs -- <path>` then `git add --sparse -- <path>` (each issued as the sole content of its own call, per BUG-1294). Note: `git checkout` has no `--sparse` option — the file was already materialized in the working tree, so plain `--theirs` sufficed; `--sparse` was only required on `git add`.

## Incoming changes preserved

Incoming commit `99d8ad6e8aa9695fb3ce8b7311060c9aee72ce56` ("xgd(ticket): update request request-3a064234", Martin Westhead, Sun Aug 23 13:11:49 2026 -0700) touches this one file only.

The resolved working-tree file is the incoming blob `43ce1458` byte-for-byte, so every incoming change is present by construction. Spot-confirmed: `chat_comment: comment-4fbb25f7` is present at line 24 of the resolved file.

Staged diff vs HEAD is `1 file changed, 1 insertion(+)` — the `chat_comment` line — which matches the incoming commit's net effect relative to the HEAD-side version of this ticket.

No hunks were dropped. The BUG-1301 precedence exception was not invoked and no test files were involved.

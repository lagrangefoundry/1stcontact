---
uid: report-e3756541
id: REPORT-3755
type: report
title: 'Capability-Intent Alignment: Site Delivery: Deploy & Public Serving (level=story)'
created_by: xgd
created_at: '2026-09-10T16:03:24.464623+00:00'
updated_at: '2026-09-10T16:03:24.464623+00:00'
completed_at: null
last_field_updated: created_at
result: fail
fields:
  report_kind: capability_validation
  subject_uid: capability-a12e557f
  level: story
  violations: 1
  warnings: 1
  needs_review_count: 0
---

# Capability-Intent Alignment: Site Delivery: Deploy & Public Serving
# Level: story

**Result**: FAIL
**Violations**: 1
**Warnings**: 1
**Needs review**: 0

Anchor report: report-e37a6b4a. Capability: capability-a12e557f (CAP-82).
Previous attempts: 4.

## Cumulative Intent Considered

CAP-82 carries no `fields.intent_uid` (the norm for capability tickets in this
store). The chain was built from the story tree: all three stories carry
`intent_uid: bundle-e0143ffa` (BUNDLE-13); STORY-94 and STORY-95 additionally
carry `updated_by: bundle-b3b7c399` (BUNDLE-20). STORY-96 carries **no
`updated_by` at all**. Both bundles are `free_and_reconciled`. The ledger was
then widened by sweeping every `request` and `bug` ticket for later delivery
asks; nothing reconciled after BUNDLE-20 touches this capability.

`updated_by` is a scalar, not a list — BUNDLE-20 overwrote the BUNDLE-14 value
the previous story-level report (REPORT-2084, 2026-08-16) recorded. BUNDLE-14 is
still in the ledger below on its merits, not on the field.

| Intent ID | Status | When | Asked / changed | Counts? |
|---|---|---|---|---|
| REQ-109 | free_and_reconciled | 2026-07-30 | Rendered output relocatable: document-relative asset URLs | YES — owned by STORY-83 (CAP-70); a delivery *precondition* |
| REQ-110 | free_and_reconciled | 2026-07-30 | R2 artifact store + `1c deploy`; content-addressed snapshots, draft/published channels, dry-run, prune, per-site manifest | YES (largely **retired** by REQ-149 — see below) |
| REQ-111 | free_and_reconciled | 2026-07-30 | `public-site` Worker: route grammar, SiteStore seam, trailing-slash 301, content types, cache policy, opaque 404, reserved `draft` segment | YES → STORY-95 |
| REQ-113 | free_and_reconciled | 2026-07-31 | `1c serve` extensionless → `.html`; the 2026-07-30 scope extension added the Worker half (AC5–AC9) after the "Cloudflare Pages auto-serves `.html`" premise was found false | YES → STORY-96 |
| BUG-30 | free_and_reconciled | 2026-07-31 | `relativizeUrl` turns `/#frag` into a same-page anchor | YES — owned by STORY-83 (CAP-70) |
| BUG-31 | free_and_reconciled | 2026-07-31 (BUNDLE-14) | `--sandbox` writes into a real site's R2 keyspace; namespace keys by store root, fix `SERVABLE_ROOT` | YES (**retired** by REQ-149 — the sandbox root has no writer left; STORY-95 records this correctly) |
| REQ-141 | free_and_reconciled | 2026-08-15 | workerd vitest project with real D1/R2 bindings | YES — test infrastructure, no CAP-82 behaviour |
| REQ-143 | free_and_reconciled | 2026-08-15 | Cloudflare SiteStore: definitions in D1, bytes in R2 | YES — the canonical store, explicitly out of CAP-82 scope |
| REQ-145 | free_and_reconciled | 2026-08-15 | control-app becomes the builder in workerd | YES — CAP surface elsewhere; contributes D4's `/preview/<slug>/published` redirect → STORY-95 AC-1424 |
| **REQ-149** | **free_and_reconciled** | **2026-08-17 (BUNDLE-20)** | **Publish in the cloud.** D5 deletes `manifest.json`, live derived from `MAX(id)`. D6 deletes `1c deploy`; one publish implementation over the store port. **D7 drops the sha-addressed shareable draft snapshots at `/site/<slug>/draft/<sha>/` — not ported.** D2 slug claim. D4 published-view redirect. | **YES — the retiring intent this check turns on** |
| REQ-150 | free_and_reconciled | 2026-08-18 | `1c` CLI boots plain Vite SSR, no Astro | YES — launcher, no delivery behaviour |
| REQ-153 | free_and_reconciled | 2026-08-20 | Reserve locale-shaped page slugs | YES — authoring/schema validation, not delivery addressing |
| REQ-154 | bundled | 2026-08-20 | Browser Rendering driver behind the capture seam | imminent — CAP capture, no delivery ask |
| REQ-155…REQ-166 | draft | 2026-08-20…08-31 | capture/KB/library work | NO (draft) |
| REQ-112 | abandoned | 2026-07-31 | — | NO |

**Walking it forward.** REQ-110 + REQ-111 + REQ-113 (BUNDLE-13) established
delivery: two addressing forms on the deployed site — a sha-addressed draft
preview channel and a published channel — both written by `1c deploy` and
indexed by a per-site `manifest.json`. BUG-31 (BUNDLE-14) hardened the key
namespace. REQ-149 (BUNDLE-20) then **retired the entire operator-side deploy
command and the preview addressing form with it**: D6 deletes `1c deploy`, D5
deletes the manifest that vouched for a URL-supplied sha, and D7 states in terms
that draft snapshots are "dropped, not ported" because a half-present feature is
the legacy-mode split `CLAUDE.md` forbids. The current cumulative intent is
therefore **one addressing form on the deployed site**, plus the local preview
server.

## Alignment Ledger

| Element | Intents aligned to | Outcome |
|---|---|---|
| STORY-94 (`story-5349d01f`, upgrade, uat_coverage=pass) — Publish a site to shared storage | REQ-110 (retired parts), BUG-31 (retired), REQ-143, **REQ-149 (D1–D3, D6, D7)** | aligned — body updated 2026-08-31; states the deletion of `1c deploy`, prune, dry-run, the deploy index and preview snapshots explicitly, and carries three dated reconciliation decisions |
| STORY-95 (`story-d34eccd8`, upgrade, uat_coverage=pass) — Serve a published site | REQ-111, BUG-31 (retired), **REQ-149 (D4, D5, D7)**, REQ-145 | aligned — body updated 2026-08-31; "There is no second addressing form… both were removed when publishing moved into the platform", live-is-derived, slug claim, published-view redirect all present |
| STORY-96 (`story-66115f6b`, feature, uat_coverage=pass) — Clean page URLs | REQ-113 (incl. its scope extension) — **and no later intent** | **gap: REQ-149 D7 retired the snapshot-addressed preview addressing form; the story body still asserts the mapping holds on it.** Last updated 2026-08-16, `updated_by` empty — the story was not carried through the BUNDLE-20 reconciliation that rewrote its two siblings on 2026-08-31 |

### Consistency, claim by claim

**STORY-94 and STORY-95 — verified aligned.** Each in-scope bullet maps to a
live intent ask: one publish over storage verbs with no filesystem (REQ-149 AC-1,
D6), validate-before-write (AC-5), unchanged-draft no-op (AC-3, D1), complete
artifact with the frozen definition beside the render (D5), forward-only history
with re-parenting checkout (AC-4), first-publish slug claim (AC-8, D2) — STORY-94;
single addressing form (D7), record-is-the-authority (D5), live derived never
stored (AC-9, D5), published-view redirect (D4), grammar rejections, trailing-slash
redirect, content typing, read-only surface, freshness and no crawler directive
(REQ-111 + the three dated reconciliation decisions) — STORY-95. Both bodies
record what was removed *as removed*, which is the property this check exists to
verify. Nothing in either body lacks intent support.

**STORY-96 — one live claim that intent has retired.** Its first in-scope bullet
reads:

> This holds in the local preview server and on the deployed site, **for both the
> preview (snapshot-addressed) and published addressing forms**, and for full and
> header-only requests alike.

That sentence is the surviving statement of a channel REQ-149 D7 deleted. Two
dependent sentences carry the same premise: in-scope bullet 5 ends "…the same
failure **the snapshot root's** redirect exists to prevent", and Technical Context
bullet 1 says the story depends on STORY-95 "for the **addressing forms**"
(plural).

Everything else in STORY-96 verified accurate, including two claims worth naming
because they read like drift and are not:
- the "corrected intent" note (the Cloudflare Pages premise was false, the
  production half had to change too) is exactly REQ-113's 2026-07-30 scope
  extension;
- the "stale in-code comment still cites the original premise" note is **still
  true today** — `tools/generate/src/cli/serve.ts:73` reads "Cloudflare Pages
  serves that at `/<slug>`". Documentation drift the story already documents, not
  a new finding.
- the local-preview/deployed directory-index asymmetry note also holds:
  `apps/public-site/src/routes.ts:135-141` maps only the *site root* to
  `index.html`, so a nested bare directory has no index concept, exactly as the
  story says.

### Step 2.5 — implementation check before classifying

REQ-149 is `free_and_reconciled`, not abandoned, so the stale-vehicle rule does
not strictly apply; the implementation was checked anyway, and it is unambiguous:

- `apps/public-site/src/routes.ts:13-22` — "THERE IS ONE CHANNEL NOW (REQ-149 D7).
  `/site/<slug>/draft/<sha>/…` served sha-addressed shareable draft snapshots,
  which only `1c deploy` ever produced and which the deploy manifest was the index
  of. Both are deleted… `draft` IS THEREFORE AN ORDINARY SEGMENT AGAIN."
- `parseRoute` (`routes.ts:112-142`) admits exactly `apex`, `redirect`, `asset`,
  `not-found`. There is no draft/sha branch.
- `tools/generate/src/deploy/` does not exist — the producer is gone.
- `tests/req111-public-site-serving.test.ts:376-386` asserts positively that
  `/site/acme/draft/` and `/site/acme/draft/abcdef123456/` now resolve as ordinary
  asset keys.

So the behaviour STORY-96 claims is not merely uncited — it is **absent from the
code**, and its absence is asserted by a passing UAT. This is a story-body edit,
not a `needs_review`.

### Coverage

No gap. Every reconciled intent ask that lands inside CAP-82's scope is expressed
by a story: REQ-149 AC-1/3/4/5/6/7/8 and D1–D3 by STORY-94, AC-2/9 and D4/D5/D7 by
STORY-95, REQ-113 (both halves) by STORY-96. REQ-149's remaining acceptance
criteria (AC-10/11 builder boot guard, AC-12 node-import guard, AC-13–16 deploy
secret hook) are platform/builder surfaces outside this capability's stated scope.

### Exclusivity

No overlap. STORY-94 and STORY-95 split operator half / visitor half and each
explicitly out-of-scopes the other's. STORY-95 out-of-scopes "the clean-URL
agreement between the local preview server and this one, which is its own story",
which is STORY-96.

## Findings

| # | Severity | Property | Element | Resolution category | Issue | Suggested edit |
|---|---|---|---|---|---|---|
| 1 | violation | consistency | STORY-96 (`story-66115f6b`) | story-body-edit | In-scope bullet 1 ("**The mapping.**") claims the mapping holds "for both the preview (snapshot-addressed) and published addressing forms". REQ-149 D7 (free_and_reconciled, BUNDLE-20, merged `eef7a8b4`) dropped the sha-addressed draft snapshot channel entirely, along with `1c deploy` (D6) and the manifest that vouched for it (D5). STORY-95 already records the removal; STORY-96 was never carried through that reconciliation (`updated_at` 2026-08-16, no `updated_by`). Confirmed against code: `apps/public-site/src/routes.ts:13-22` and `parseRoute` at `:112-142` admit one channel only, and `tests/req111-public-site-serving.test.ts:376-386` asserts `draft` is an ordinary segment. | Replace "for both the preview (snapshot-addressed) and published addressing forms" with the single published addressing form — e.g. "…on the deployed site, and for full and header-only requests alike." Optionally add a sentence to Technical Context recording that REQ-149 removed the second form, in the same "recorded rather than absorbed" style the story already uses for the corrected REQ-113 premise. |
| 2 | warning | consistency | STORY-96 (`story-66115f6b`) | story-body-edit | Two dependent sentences carry the same retired premise: in-scope bullet 5 ("**A directory-shaped URL is never eligible…**") ends "the same failure **the snapshot root's** redirect exists to prevent", and Technical Context bullet 1 cites STORY-95 "for the **addressing forms**" (plural). The redirect itself survives — `routes.ts:135-137` still 301s a slash-free site root — but it guards the *published site root*, not a snapshot root, and there is now one addressing form. | In the same pass as finding 1: "the snapshot root's redirect" → "the published site root's redirect"; "for the addressing forms and guards" → "for the addressing form and guards". |
| 3 | info | — | STORY-94, STORY-95 | — | Both were rewritten under BUNDLE-20 on 2026-08-31 and are aligned to current cumulative intent, including three dated reconciliation decisions each that formalise behaviour REQ-149 left silent. No action. | none |
| 4 | info | — | STORY-96 Technical Context | — | The story's note that a stale in-code comment still cites the false Cloudflare Pages premise is **verified still true** (`tools/generate/src/cli/serve.ts:73`). Correctly recorded as documentation drift, not behaviour. No action. | none |

## Notes for the Editor

**One root cause, three sentences.** STORY-96 is the only story in CAP-82 that
BUNDLE-20's reconciliation did not touch. Its siblings were rewritten on
2026-08-31; STORY-96 still stands at 2026-08-16 with an empty `updated_by`. Every
finding above is the same omission surfacing in three places, and all three are
repairable in one edit to the story body. Nothing about the *mapping* itself has
changed — REQ-149 removed a channel, not the extensionless→`.html` rule — so the
repair is subtractive and must not weaken any of REQ-113 AC5–AC9.

**The cascade is already visible one level down, and this level runs first.**
AC-916 ("On the deployed site, an extensionless page URL serves the page on both
addressing forms and for header-only requests") reproduces the retired premise in
both its title and its body ("under a snapshot-addressed preview URL, under a
published site URL"), and its Verification step instructs "Deploy a rendered
multi-page site, both as a preview snapshot and as a published revision" — an
instruction no longer executable, since `1c deploy` is deleted. AC-921 similarly
speaks of "the snapshot root". Those are `ac-edit` repairs for the ac-level cycle;
they are named here so that cycle does not re-derive the same intent history, and
because a story-body fix that leaves AC-916 untouched will simply re-fail
downstream. Note that STORY-96's UATs were already rewritten for the new world —
REQ-149's own test-change note records `req113-worker-extensionless-urls` and
`reconciliation-clean-page-urls` being rebuilt over a `tests/fixtures/published-site.ts`
fixture that runs a real publish — so at present the tests are correct and the
matrix text is what lags.

**Prior story-level report is stale, not wrong-at-the-time.** REPORT-2084
(2026-08-16) passed this level, and legitimately: it predates BUNDLE-20 by two
weeks and its ledger describes STORY-94/95 by their old titles ("Ship a site off
the laptop", "Serve a deployed snapshot"). It should not be read as evidence that
this finding was previously considered and dismissed.

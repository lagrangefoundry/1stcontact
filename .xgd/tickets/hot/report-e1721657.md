---
uid: report-e1721657
id: REPORT-2817
type: report
title: 'Reconcile resolve conflicts: reconcile-BUNDLE-20'
created_by: xgd
created_at: '2026-08-31T07:48:07.188818+00:00'
updated_at: '2026-08-31T07:48:07.188818+00:00'
completed_at: null
last_field_updated: created_at
result: pass
fields:
  report_kind: resolve_conflicts
  subject_uid: reconcile-BUNDLE-20
---

## Files resolved

- `.xgd/tickets/hot/bug-5186fa0c.md` — **AA** (both added), intent/bookkeeping
  ticket (`bug-*`). Rules **2b** + **2e** applied: incoming is a **strict
  superset** of HEAD, so the superset was kept (`git checkout --theirs`, then
  `git add --sparse` — the path is outside the sparse-checkout cone, DOC-986
  §2/§4.1).

  Both sides are byte-identical except for a single added frontmatter field on
  the incoming side:

  ```diff
  @@ -21,6 +21,7 @@ fields:
       main_sha: null
     version: 0.0.190
     bundled_in: bundle-4ff83a8b
  +  chat_comment: comment-5d1f9d7e
   ---
  ```

  No competing fact exists on the two sides — the incoming commit only appends
  a field HEAD never touched — so no `xgd working-timeline` per-fact
  adjudication was required. The auto-enrichment note ("intent unknown on one
  or both sides; take the more recent commit by timestamp and flag for
  post-merge review") is moot here for the same reason: the more recent side
  and the superset side are the same side, and the union of both sides is
  exactly the incoming file. Nothing from HEAD was dropped.

## Incoming changes preserved

- `.xgd/tickets/hot/bug-5186fa0c.md` — **preserved.** Incoming commit
  `69e95a8dabcf60c820da6722dc5caef413458333` ("xgd(ticket): update bug
  bug-5186fa0c"). Verified after resolution: `fields.chat_comment:
  comment-5d1f9d7e` is present in the resolved file, no conflict markers
  remain, and `git diff --cached HEAD` for this path shows exactly that one
  added line and nothing else. All of HEAD's content (status
  `free_and_reconciled`, `commits[].working_sha`, `version: 0.0.190`,
  `bundled_in: bundle-4ff83a8b`, and the full body including the
  `## Resolution (free-coded 53fc6141)` section) is retained verbatim.

No code/implementation files were conflicted in this cherry-pick, so no
BUG-1301 precedence exception was invoked and no hunks were dropped.

## Notes for the outer workflow

- `.xgd/tickets/hot/comment-5d1f9d7e.md` — the comment ticket referenced by the
  newly added `chat_comment` field — is not materialized in this worktree
  (out of sparse cone). This is expected for `.xgd/tickets/` on a reconcile
  branch and is not a resolution defect; flagging only so the dangling-looking
  reference is not mistaken for one later.
- Cherry-pick sequencer state (`CHERRY_PICK_HEAD`) is intentionally left
  intact for `cherry_pick_finalize_resolution`. No `--continue`/`--skip`/
  `--quit`/`--abort`/`reset` was run.

Staging verified clean: no `UU`/`AA`/`DU`/`UD`/`AU`/`UA` entries remain;
the sole non-untracked entry is `M  .xgd/tickets/hot/bug-5186fa0c.md`.

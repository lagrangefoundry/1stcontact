---
uid: report-148c8212
id: REPORT-4148
type: report
title: 'Reconcile resolve conflicts: reconcile-BUNDLE-27'
created_by: xgd
created_at: '2026-09-13T22:03:08.674305+00:00'
updated_at: '2026-09-13T22:03:08.674305+00:00'
completed_at: null
last_field_updated: created_at
result: pass
fields:
  report_kind: resolve_conflicts
  subject_uid: reconcile-BUNDLE-27
---

## Files resolved

- `.xgd/tickets/hot/request-13a5e206.md` (REQ-162) — **UU**, intent/bookkeeping
  ticket (`request-*`), rule **2e**. Two conflict regions, resolved per-fact.
  Markers edited out by hand rather than `git checkout --ours`, so hunks git had
  already auto-merged from the incoming side survived. Staged with
  `git add --sparse` (path is outside the sparse-checkout cone).

  Incoming commit: `e53d8f39fd` (2026-08-31 13:44:16 -0700), no free-text
  `--commit-message` body. Its diff against its own parent is small — 2
  insertions, 1 deletion:
    1. `+  chat_comment: comment-aa271bc5` under `fields:`
    2. removal of the trailing newline at EOF

  HEAD side: the same ticket at `status: free_and_reconciled`, `result: pass`,
  `updated_at` 2026-09-02 01:34Z, carrying the reconcile bookkeeping block and a
  "What landed (free-coded)" section.

  Per-region resolution:

  1. **Region 1 (frontmatter `fields:`)** — HEAD side is the `commits:` /
     `orphan_commits:` / `merged_at_commit:` / `result: pass` reconcile
     bookkeeping block; **the incoming side of this region is empty**. Nothing
     from incoming to preserve; kept HEAD's block. The region conflicted only
     because it sits adjacent to incoming's `chat_comment` insertion.
  2. **Region 2 (`## Both open questions are now settled`)** — same two facts
     stated at different maturities. Incoming still phrases them as *open*
     ("Whether `reference` earns its own type…", "Whether the `brief` is a type
     or a well-known ticket…"). HEAD states them as *settled* ("`reference`
     keeps its own type", "`brief` keeps its own type, with `fields.site_slug`")
     and follows with the Implementation-notes and "What landed" sections. Same
     fact changed differently on each side → later-positioned side wins: HEAD
     (2026-09-02) over incoming (2026-08-31). The section heading itself
     auto-merged in HEAD's "…are now settled" form, which the incoming bullets
     would have contradicted.

  No `fields.intent_uid` / `story_uid` / `capability_uid` touched. No content
  invented that was not on one side.

## Incoming changes preserved

Verified against `git show e53d8f39fd -- .xgd/tickets/hot/request-13a5e206.md`.
Both of the incoming commit's changes are accounted for:

- `chat_comment: comment-aa271bc5` — **present** at line 17. It auto-merged
  cleanly above both conflict regions because HEAD already carried the identical
  line; confirmed still present in the resolved file post-resolution.
- Trailing-newline removal at EOF — **present**. Worth recording because it was
  briefly wrong: the marker edits reintroduced a trailing newline, which showed
  up as the sole staged hunk against HEAD. HEAD and the incoming commit *both*
  end the file without a trailing newline, so that newline was content on
  neither side (2e PROHIBITED: inventing content not on either side). Stripped
  with `perl -i -pe 'chomp if eof'` and re-staged; the staged diff then went
  empty, which is the correct end state.

No hunks dropped under the BUG-1301 precedence exception; none applied here.

## Note on the staged diff

The resolution nets to **no diff vs HEAD** (`git diff --cached HEAD --stat`
empty). This is the redundant-commit case (BUG-1109/BUG-1122), not a discard:
STEP 3's test distinguishes them by asking whether the incoming commit's key
changes are *present* in HEAD rather than absent, and here `chat_comment` is
present at line 17 and the no-trailing-newline state matches. HEAD had already
absorbed this commit's effect. Staged and exiting `@done` as instructed;
`--skip` not called. Cherry-pick sequencer state left intact — `CHERRY_PICK_HEAD`
still reads `e53d8f39fd770816546ffa9f70caaff952919307` for
`cherry_pick_finalize_resolution`.

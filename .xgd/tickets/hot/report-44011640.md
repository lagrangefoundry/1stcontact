---
uid: report-44011640
id: REPORT-3110
type: report
title: 'Reconcile resolve conflicts: reconcile-BUNDLE-22'
created_by: xgd
created_at: '2026-08-31T22:29:43.975085+00:00'
updated_at: '2026-08-31T22:29:43.975085+00:00'
completed_at: null
last_field_updated: created_at
result: pass
fields:
  report_kind: resolve_conflicts
  subject_uid: reconcile-BUNDLE-22
---

## Files resolved

- `.xgd/tickets/hot/bug-23d1ec27.md` — UU, out-of-sparse-cone (index-only
  conflict). Class 2e (intent/bookkeeping ticket). Resolved per-fact by
  composing both sides: incoming's body edit + HEAD's later status
  transition. Materialized with `git checkout --theirs`, patched the four
  frontmatter facts, staged with `git add --sparse`.

  Incoming commit `5a68fc8d` (2026-08-26 16:21, `last_field_updated: body`)
  is a full rewrap of the ticket body — 91 insertions / 95 deletions,
  purely reflow (hard-wrapped prose unwrapped to long lines, the
  suite/before/after markdown table flattened to loose lines, ` ```ts `
  demoted to ` ``` `). No sentence content added or removed.

  HEAD (blob `52bab41f`) left the body at the merge base and changed only
  bookkeeping: status → `bundled`, `bundled_in: bundle-8eef3846`,
  `updated_at` → 2026-08-31.

  | fact | base (`ad25504e`) | incoming (`df68f1cf`) | HEAD (`52bab41f`) | kept |
  |---|---|---|---|---|
  | body | hard-wrapped | **reflowed** | = base | **incoming** |
  | `status` | `ready_to_reconcile` | = base | **`bundled`** | HEAD |
  | `fields.bundled_in` | absent | = base | **`bundle-8eef3846`** | HEAD |
  | `updated_at` | 2026-08-26T18:31 | 2026-08-26T23:21 | **2026-08-31T05:05** | HEAD |
  | `last_field_updated` | `status` | `body` | `status` | HEAD |

  The body and the status fields are disjoint sections, each changed on
  only one side, so both were applied (2e "non-overlapping fields/sections:
  apply BOTH"). The only genuinely contested fact is the
  `updated_at`/`last_field_updated` pair, which is coupled — it names the
  most recent edit. Replaying chronologically: incoming's body edit
  (08-26 23:21), then HEAD's status bump (08-31 05:05). The later edit is
  HEAD's, so the coherent pair is `updated_at: 2026-08-31T05:05` with
  `last_field_updated: status`. Taking incoming's `body` there would claim
  the body was the most recent edit when the status change came five days
  after it.

  One byte not carried across: HEAD had added a trailing newline at EOF;
  the resolved file follows the incoming (and base) form with none. This
  is xgd's own serializer output — base and incoming both lack it, and the
  next `xgd ticket` write restores that form regardless.

## Incoming changes preserved

Verified with `git diff --no-index` between the incoming blob `df68f1cf`
and the resolved file: the ONLY differences are the four frontmatter
bookkeeping facts listed above. The entire incoming body change — all 91
reflowed lines — is present verbatim in the resolved version. Nothing from
the incoming commit's body edit was dropped.

No code/implementation files were conflicted; the single conflict is an
xgd bookkeeping ticket, not developer source. No BUG-1301 precedence
exception was invoked, and no test functions were touched.

## Staging state

`git status --porcelain` shows `M  .xgd/tickets/hot/bug-23d1ec27.md` and
no remaining UU/AA/DU/UD entries (only pre-existing untracked
`comment-*.md` files). The path is at stage 0 with blob
`01eb488de3ca2e702f7175c4644dd91611153d8c`. The staged diff vs HEAD is
non-empty (93 insertions / 97 deletions), so this commit will land
normally. `CHERRY_PICK_HEAD` (`5a68fc8d`) is intact.

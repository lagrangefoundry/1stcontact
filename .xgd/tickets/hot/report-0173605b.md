---
uid: report-0173605b
id: REPORT-3250
type: report
title: 'Reconcile resolve conflicts: reconcile-REQ-162'
created_by: xgd
created_at: '2026-09-01T22:55:15.181165+00:00'
updated_at: '2026-09-01T22:55:15.181165+00:00'
completed_at: null
last_field_updated: created_at
result: pass
fields:
  report_kind: resolve_conflicts
  subject_uid: reconcile-REQ-162
---

## Files resolved

- `.xgd/tickets/hot/doc-79e14aa7.md` — class **AA** (both added; path is outside the sparse-checkout cone, so resolution used `git checkout --theirs` + `git add --sparse`, DOC-986 §2/§4.1). Rule 2e (intent/bookkeeping ticket, per-fact judgment) plus the auto-enrichment rule "intent unknown on one or both sides → take the more recent commit by timestamp, flag for post-merge review". Resolution: **incoming (theirs) taken verbatim**; staged blob `161cb88e1a` is byte-identical to stage 3.

  Per-fact analysis (both facts are same-field-changed-differently, so both go to the later side):
  - `updated_at`: HEAD `2026-08-16T01:22:05` (commit `73da955aef`, Sat Aug 15 2026) vs incoming `2026-08-31T19:43:18` (commit `d13c14b8bb`, Mon Aug 31 2026). Incoming is later → incoming wins.
  - `fields.system_kb`: HEAD-side `73da955aef` *added* `system_kb: true`; incoming `d13c14b8bb` *removes* it, with the free-text commit body stating the operation: "retire system_kb boolean; membership moves to doc_kind (DOC-39 3.3)". Same fact, incoming is the later and documented intent → incoming wins.
  - `last_field_updated: system_kb` and `fields.doc_kind: interface_design_policy` were identical on both sides and are unchanged.

  Corroboration that this is a coherent sweep, not an isolated field loss: the incoming bundle is a run of sibling `update doc` commits (`df8d3e8db4`, `293a04ab0d`, `5386685c94`, `422b7ccefc`, `3dc855fab0`, `d13c14b8bb`) performing the same retirement. Sibling `doc-a80bf2ef` was already cherry-picked into HEAD (`6d4f0f4cec`) and in the current tree likewise retains `last_field_updated: system_kb` while carrying no `fields.system_kb` — exactly the shape this resolution produces.

## Incoming changes preserved

- `.xgd/tickets/hot/doc-79e14aa7.md`: **confirmed**. `git show d13c14b8bb -- <file>` shows the incoming commit adding the 15-line ticket with `updated_at: '2026-08-31T19:43:18.227176+00:00'` and no `system_kb` key under `fields:`. The staged index entry hashes to `161cb88e1a6546e79ef3ff4ea2ce46f2feb15df1`, the incoming blob itself, so 100% of the incoming content is present. Net staged diff vs HEAD is 1 insertion / 2 deletions (the `updated_at` bump and the `system_kb: true` removal) — the incoming intent exactly.
- No hunks were dropped; the BUG-1301 precedence exception was not invoked.
- No code, test, or UAT files were in conflict — this cherry-pick's only conflict was the single ticket file above.

## Flagged for post-merge review

Per the enrichment rule for unknown intent on one/both sides: `doc-79e14aa7.md` (DOC-3, Interface Design Policy) took the incoming side wholesale, which discards the HEAD-side `system_kb: true` set on 2026-08-15. That discard is believed correct (the incoming commit explicitly retires the field, and the sibling sweep already landed the same shape), but it is a deliberate deletion of a field one side added, so it is called out here.

## Git state

Cherry-pick sequencer left intact — `CHERRY_PICK_HEAD` (`d13c14b8bb7f0c6a7b7d51fc36a8cd12f59a0bac`) still present. No `--continue`/`--skip`/`--quit`/`--abort`/`reset` was run. `git status --porcelain` shows no remaining conflict-class entries; the single resolved file is staged as `M`.

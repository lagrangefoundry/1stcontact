---
uid: report-645376b9
id: REPORT-3747
type: report
title: 'Capability-Intent Alignment: L1 Reproduction Pipeline: Fold & Acceptance Gate
  (level=ac)'
created_by: xgd
created_at: '2026-09-10T14:50:03.608698+00:00'
updated_at: '2026-09-10T14:50:03.608698+00:00'
completed_at: null
last_field_updated: created_at
result: pass
fields:
  report_kind: capability_validation
  subject_uid: capability-2049c9ec
  level: ac
  violations: 0
  warnings: 0
  needs_review_count: 0
---

# Capability-Intent Alignment: L1 Reproduction Pipeline: Fold & Acceptance Gate
# Level: ac

**Result**: PASS
**Violations**: 0
**Warnings**: 0
**Needs review**: 0

CAP-71 (`capability-2049c9ec`) holds two stories, both `story_kind: upgrade`, so both
are Capability-Matrix kinds expected to carry ACs and both do. STORY-84
(`story-8acc338d`, the fold) now carries **24** ACs (18 `active` + 6 `pending`);
STORY-86 (`story-24098299`, the 3-probe gate + cross-gate reconciliation) carries
**17** (16 `active` + 1 `pending`). None is `deprecated`.

**All three violations and all four warnings from the previous cycle
(REPORT-3745 / `report-fd81bfc6`, 2026-09-10T14:37) are closed.** Attempt 8's fix
(REPORT-3746 / `report-c0031321`) landed eight edits; every one was re-verified in
this worktree against the live ticket store and the live source, not taken on the
fix report's word. Two of the closures — STORY-86's third envelope violation
(**fifth** consecutive raise) and the AC-731/AC-812 page-base contradiction
(**fifth** raise, carried since REPORT-1319) — end long-running drift.

**Working reference.** The story-level cycle for this capability ran today and
**passed** (REPORT-3744 / `report-421de5ec`, 2026-09-10T14:30, 0 violations), so per
the level cascade both story bodies are my working reference. I did not need to
escalate to intent history for any finding this run: the previous cycle's single
escalation (the page-base rule) was closed by an AC edit *and* its companion
story-body edit, so STORY-84's body now arbitrates its own ACs consistently. See
info 4 for a sequencing note on that body edit.

## Cumulative Intent Considered

ACs in this tree carry no `intent_uid` / `updated_by` of their own — their fields are
`story_uid`, `kind`, `regression_only`, `uat_coverage` only — so attribution resolves
at story level. Both stories carry `intent_uid: bundle-31e474b9` (BUNDLE-7); STORY-84
carries `updated_by: request-8a132869` (REQ-136), STORY-86 `updated_by:
bundle-ee56a66e` (BUNDLE-11). Statuses below were read from the store this run.

| Intent ID | Status | When | Asked / changed | Counts? |
|---|---|---|---|---|
| REQ-66 | free_and_reconciled | 2026-07-18 | `adopt-values` pre-L1 reproduction command | YES (retired by REQ-83) → AC-696 |
| REQ-79 | free_and_reconciled | 2026-07-19 | Framework pivot to L1; absolute-base reproduction (D1) | YES |
| REQ-83 | free_and_reconciled | 2026-07-20 | Capture→L1 fold + oracle retention + advisory hints — origin of STORY-84 | YES |
| REQ-86 | free_and_reconciled | 2026-07-20 | End-to-end 3-probe gate + demand-driven promotion — origin of STORY-86 | YES |
| REQ-88 | free_and_reconciled | 2026-07-21 | `1c repro` / `1c l1-gate`; captured surface rect; section-edge clamp; responsive padding tracks; viewport-height response; **nowrap threshold** | YES — **now fully expressed** (AC-1631 closes the last gap) |
| BUG-5 | free_and_reconciled | 2026-07-23 | Occurrence-index fidelity pairing + idempotence identity | YES → AC-705, AC-724 |
| BUG-6 | free_and_reconciled | 2026-07-23 | Unexpressed element → typed residual | YES → AC-733 |
| BUG-7 | free_and_reconciled | 2026-07-23 | Evaluator must tile a row along the main axis | YES → AC-734 |
| BUG-8 | free_and_reconciled | 2026-07-23 | Half-open breakpoint intervals | YES → AC-735 |
| BUG-9 | free_and_reconciled | 2026-07-23 | Recursive, region-aware promotion | YES → AC-709 |
| REQ-90 / REQ-91 / REQ-92 | free_and_reconciled | 2026-07-23 | Resource table, text pixel-movers, full-language fold | YES → AC-689, AC-732 |
| BUG-11 / BUG-12 / BUG-13 | free_and_reconciled | 2026-07-23 | `surfaceFill`/`surfaceGradient`; font faces; background-image elements | YES → AC-730, AC-732, AC-812 |
| BUG-14 | free_and_reconciled | 2026-07-23 | Nest, don't flatten: section-band → card → text | YES → AC-731, AC-736 |
| BUG-17 | free_and_reconciled | 2026-07-23 | Fold per-side padding onto the leaf | YES → AC-1626 |
| BUG-18 | free_and_reconciled | 2026-07-23 | Keyframe the varying type axes per width | YES → AC-1625, AC-691 |
| **BUG-19** | free_and_reconciled | 2026-07-23 | A full-bleed **bar** (footer/nav strip) seeds a band, not tiny cards | YES — **now expressed** (AC-731 rewrite + STORY-84 body); closes prior warning 7 |
| BUG-20 / BUG-21 | free_and_reconciled | 2026-07-23/24 | Pill/chip and padded-control runs are self-painting | YES → AC-731 |
| BUG-22 | free_and_reconciled | 2026-07-24 | `SurfaceShape` — captured surface rect as card identity | YES → AC-731 |
| BUG-23 | free_and_reconciled | 2026-07-24 | `localizeAssets`; unmirrored handle fails the import | YES → AC-1628 |
| BUG-24 | free_and_reconciled | 2026-07-24 | Colour alpha representable; the band scrim | YES → AC-1629 |
| **BUG-27** | free_and_reconciled | 2026-07-25 | Backdrop index; backdrop edges are section edges; **backdrops count toward the page base** | YES — **now expressed without contradiction** (AC-731 + AC-812) |
| REQ-93 | free_and_reconciled | 2026-07-25 | L1 pages host behavior modules in slots | YES → AC-813 |
| REQ-94 | free_and_reconciled | 2026-07-25 | Cross-gate reconciliation, floor, coverage, named causes | YES → AC-852–856 |
| REQ-96 | free_and_reconciled | 2026-07-26 | L1 `control` node — a captured control binds | YES → AC-733, AC-813, AC-705, AC-737 |
| REQ-114 | free_and_reconciled | 2026-07-31 | `l1Color` = `hex \| PaletteRef` | out of scope (CAP-70) — info 5 |
| REQ-136 (`request-8a132869`) | free_and_reconciled | 2026-08-12 | Framing pair + colour-adjustment stack | YES → AC-1133, AC-1134, AC-729 |
| BUNDLE-7 (`bundle-31e474b9`) | free_and_reconciled | 2026-07-22 | Carrier for REQ-63/79/82/83/84/86 | YES |
| BUNDLE-11 (`bundle-ee56a66e`) | free_and_reconciled | 2026-08-05 | Carrier for BUG-27 / REQ-94 / 96 / 97 / 98 | YES |
| REQ-154 | free_and_reconciled (via BUNDLE-22) | 2026-08-20 | CF Browser Rendering driver behind the existing `BrowserDriver` seam | YES — **no ask on this capability**; body re-read this run: 0 occurrences of "fold", "probe" or "oracle"; the seam is CAP-63's |
| REQ-155 / REQ-156 / REQ-157 | draft | 2026-08-20 | Capture in workerd; sharp off the fidelity path; fidelity surface | NO — not yet active |
| REQ-158–REQ-166 | draft | 2026-08-28 → 08-31 | KB, corpus, ticket-store, Library work | NO — not yet active |
| REQ-134 | abandoned | 2026-08-12 | Image generation component | NO |

Full intent census re-run this cycle (216 request/bug/bundle tickets, ordered by
`created_at`): **no intent created after 2026-08-16 asks anything of the fold or the
gate.** REQ-154 has since moved `bundled → free_and_reconciled` under BUNDLE-22; its
body was re-read to confirm it still owes this capability no AC. Everything newer
(REQ-155…REQ-166) is `draft` and does not count.

## Alignment Ledger

| Element | Intents aligned to | Outcome |
|---|---|---|
| AC-689 (validated full-language document) | REQ-83, REQ-92 | aligned |
| AC-690 (oracle retained) | REQ-83 | aligned |
| AC-691 (geometry keyframes; scalar from widest sample, varying axis carved out) | REQ-83, BUG-18 | aligned; cross-refs AC-1625 |
| AC-692 / AC-693 (interpolate/snap; visibility) | REQ-83 | aligned |
| AC-694 / AC-695 (hint sidecar; advisory-only) | REQ-83 | aligned |
| AC-696 (adopt-values removed) | REQ-66 retirement | aligned; `regression_only: true`; no `adopt-values` case survives in `cli/index.ts` |
| **AC-729** (image leaf) | REQ-92, REQ-136 | **repaired** — narrowed to what a *foldable* media element emits; the source-less / geometry-less case and its duplicate test step now delegate to AC-733 by cross-reference. Prior warning 6 (third raise) closed |
| AC-730 (text-free surface → box leaf) | REQ-92, BUG-11 | aligned |
| **AC-731** (run-composited surfaces) | BUG-11, BUG-14, BUG-19, BUG-20, BUG-21, BUG-22, BUG-27 | **repaired** — page base now stated as greatest total band height across reconstructed bands **and** captured backdrops, run-count fallback, canvas last resort; BUG-19 bar rule added; Verification asserts band height with taller-band / no-band / canvas cases. Matches `fold.ts:2105-2146` and `fold.ts:1383-1399`. Prior finding 3 and warning 7 closed |
| AC-732 (text pixel-movers + font table) | REQ-90, REQ-91, BUG-12 | aligned |
| AC-733 (typed residuals; controls bind) | BUG-6, REQ-96 | aligned; now the sole authority for the residual outcome |
| AC-1133 / AC-1134 (framing pair; colour-adjustment stack) | REQ-136 | aligned |
| AC-1625 (responsive tracks) | BUG-18, REQ-88 | aligned — re-verified against `responsiveTextTracks` `fold.ts:623-642` and `responsivePaddingTracks` `fold.ts:657-674`: `keyframes.length < 2 \|\| every(k => k.value === keyframes[0].value)` is exactly the AC's "≥2 widths carry it AND values not all equal"; segments omitted |
| AC-1626 (per-side padding) | BUG-17, REQ-88 | aligned (`foldPadding` `fold.ts:552`) |
| AC-1627 (viewport-height response) | REQ-88, BUG-27 | aligned |
| AC-1628 (`1c repro` materialization) | REQ-88, BUG-23 | aligned; verb live (`cli/index.ts:57`, `:803`, `cmdRepro`) |
| AC-1629 (band scrim) | BUG-24 | aligned |
| **AC-1631** (nowrap threshold) | **REQ-88** | **new — closes the prior cycle's finding 2.** All three derivation rules verified against source: width-not-flag and the backward suffix scan (`nowrapThreshold` `fold.ts:233-240`); unmeasurable line count breaks the suffix (`lineCountOf` `fold.ts:215-220` returns `undefined`, and `undefined !== 1` breaks); carried at `fold.ts:1843-1844`. The Verification's "narrowest sampled width" case is correct because `framed` is width-ascending by construction (`fold.ts:1785`, confirmed by the widest-cell read at `:1825`). Renderer half correctly deferred to CAP-70 / STORY-83 / AC-1010 |
| AC-812 (backdrop → background layer; edges bound bands; fill counts to page base) | BUG-13, BUG-27 | aligned; **no longer contradicted** — AC-731 now delegates the backdrop's contribution here rather than restating a rival rule |
| AC-813 (control leaf rebased to seam) | REQ-93, REQ-96 | aligned |
| AC-814 (offline re-fold) | REQ-88 | aligned |
| AC-705 (fidelity pairing, text + kind keys) | BUG-5, REQ-96 | aligned; **now the sole authority** for the fidelity report shape |
| **AC-706 / AC-707** (off-sample, robustness) | REQ-86, BUG-9 | **repaired** — both "exactly when" clauses now enumerate all three envelope violations and delegate the third to AC-1630 |
| AC-708 / AC-709 (combined gate; recursive recovery) | REQ-86, BUG-9 | aligned |
| **AC-710** (diagnostic contract) | REQ-86 | **repaired** — narrowed to the envelope-finding contract it uniquely owns (kind / magnitude-bearing detail / index paths), with `clip` now covering both the viewport crossing and the pinned-box overflow; the fidelity residual *shape* delegated to AC-705. Prior warnings 4 and 5 (fourth raise) closed by one edit |
| **AC-1630** (pinned-box content overflow) | **REQ-86** | **new — closes the prior cycle's finding 1 after five raises.** Verified against `probes.ts:405-416` (`contentHeight > pinnedH + opts.epsilonPx` → `{kind:'clip', detail:'content height Npx exceeds pinned box height Mpx', paths:[path]}`), `probes.ts:417` (a node pinning no height returns its content height and cannot raise it), and the `evaluateLayout` docstring `probes.ts:429-433` naming all three violations. Raised from `evaluateLayout`, which both envelope probes call, so the AC's "wherever the evaluator runs" holds |
| AC-724 (idempotence identity) | BUG-5 | aligned; distinct in shape from AC-705 (identity property vs probe verdict) — not a duplicate |
| AC-734 / AC-735 (row tiling; half-open intervals) | BUG-7, BUG-8 | aligned |
| AC-736 (backing-surface overlap exception) | BUG-14 | aligned to the story body; broader than code — info 1 (unchanged, third cycle) |
| AC-737 (fold-residual channel) | REQ-92, REQ-96 | aligned |
| AC-852–AC-856 (cross-gate verb, floor, coverage, causes, exit code) | REQ-94, BUG-27 | aligned; `1c gate` live (`cli/index.ts:58`, `:911`, `cmdGate`) |
| STORY-84 AC tree | REQ-83, REQ-88, REQ-92, REQ-136, BUG-11…BUG-27 | **coverage complete** — every one of the 16 clauses in STORY-84's "In scope" list maps to at least one AC (see Notes) |
| STORY-86 AC tree | REQ-86, REQ-94, BUG-5…BUG-9 | **coverage complete** — every one of the 7 clauses in STORY-86's "In scope" list maps to at least one AC, plus AC-724 for the idempotence identity stated in its Description |
| STORY-84 ↔ STORY-86 | — | exclusivity OK across stories; the reciprocal out-of-scope clauses partition fold vs gate cleanly |

## Findings

| # | Severity | Property | Element | Resolution category | Issue | Suggested edit |
|---|---|---|---|---|---|---|
| 1 | info | consistency | AC-736 | — | AC-736 excludes "a painted surface leaf — a childless box carrying a card/panel/section fill, positioned behind the content it backs" from the sibling-overlap check. The code is narrower: only **fold-synthesized** surfaces are excluded (`isSynthesizedSurfaceId`, `probes.ts:460-474` — `section-band-*` / `section-bg-*` / `card-*`), while a genuine captured standalone surface (`box-*`, the AC-730 family) "is real painted content and still participates". AC-736 is faithful to STORY-86's body, which states the same broad rule, so under the level cascade this is story-level drift and **not** ac-level drift. Recorded unchanged for the third consecutive cycle (REPORT-2091 info 10, REPORT-3745 info 8). Note that AC-736's *Verification* is correct as written — it exercises the run-composited (`card-*`) case — so a UAT authored from the Verification passes; only a UAT authored from the criterion's literal wording would assert exclusion for a `box-*` leaf and fail. | none at this level; flag for the story-level editor, then tighten AC-736 to fold-synthesized surfaces |
| 2 | info | exclusivity | AC-1630 + AC-710 | — | AC-1630 restates the new violation's `kind` / `detail` / `paths` while explicitly ceding ownership ("The diagnostic contract itself is AC-710's"), and AC-710 states the same triple as part of its cross-finding vocabulary. This is the delegation shape the previous cycle asked for — a trigger AC naming the shape it produces plus an explicit ownership pointer — not the un-cross-referenced duplication that made warnings 4/5 diverge. Recorded so a future cycle does not read the one-sentence overlap as fresh drift. | none |
| 3 | info | — | AC-1625, AC-1626, AC-1627, AC-1628, AC-1629, AC-1630, AC-1631 | — | These seven carry `status: pending` and no `uat_coverage` field. Treated here as active expression of intent — `pending` is authorship state, not retirement — and each was verified against both its story body and the live source this run. Their UAT status is a uat-level question, and the two authored this cycle (AC-1630, AC-1631) will arrive at that level with no tests, which is the expected next gate. | none at this level |
| 4 | info | — | STORY-84 body | — | The STORY-84 body edit that closed the previous cycle's finding 3 landed at 2026-09-10T14:41:52 (`last_field_updated: body`), **after** the story-level check passed at 14:30 (REPORT-3744). The story-level PASS was therefore issued against a body that has since changed. I read the current body in full this run and it is internally consistent and consistent with AC-731 and AC-812 — the edit repaired the contradiction the previous ac cycle named rather than introducing one — so it does not disturb this level's working reference. Recorded only so the sequencing is legible if a story-level cycle re-runs. | none |
| 5 | info | coverage | REQ-114, REQ-154 | — | Neither owes this capability an AC. REQ-114 (`l1Color` = `hex \| PaletteRef`): `fold.ts:2115-2118` records that the fold emits colour literals only, palette assignment being a separate re-runnable pass; schema/renderer/validator are CAP-70. REQ-154 has moved to `free_and_reconciled` via BUNDLE-22 since the last cycle, so I re-read its body: it swaps the Playwright driver for a CF Browser Rendering driver behind the existing `BrowserDriver` seam, with zero occurrences of "fold", "probe" or "oracle"; the seam belongs to CAP-63. | none |

## Notes for the Editor

**Nothing to repair at this level.** All three violations and all four warnings from
REPORT-3745 are closed, and no new drift was found. The two long-running items are
worth naming so they are not silently re-opened:

- **STORY-86's third envelope violation** (pinned-box content overflow) had been
  raised at every ac-level cycle since REPORT-1319 on 2026-08-05 — five times — and
  is now AC-1630, with AC-706 and AC-707 amended in the same call so the tree is not
  left self-contradictory and AC-710's `clip` vocabulary extended to cover it.
- **The AC-731 page-base rule** had been false since it was written: it claimed the
  most-common *run fill* wins, while `fold.ts:2105-2130` picks the fill with the
  greatest total band height across bands and backdrops alike. It is now correct, and
  — critically — the companion STORY-84 body bullet was edited in the same call. That
  omission is what carried the finding through four cycles; closing both together is
  what ended it.

**Coverage was re-derived from the story bodies, not inherited.** I walked both "In
scope" lists clause by clause against the AC set. STORY-84's 16 clauses: full-language
fold (AC-689/729/730/731/732/812/813/1629), framing + colour adjustment
(AC-1133/1134), per-side padding (AC-1626), responsive tracks (AC-1625),
self-painting runs (AC-731), bar rule + surface-rect card geometry (AC-731),
extent-measured page base (AC-731), viewport-height response (AC-1627), nowrap
threshold (AC-1631), oracle retention (AC-690), offline re-fold (AC-814),
materialization (AC-1628), keyframes/interpolate-snap/visibility (AC-691/692/693),
typed residuals (AC-733), hint sidecar (AC-694/695), adopt-values supersession
(AC-696). STORY-86's 7 clauses: flow model (AC-734), breakpoint intervals (AC-735),
envelope findings + backing-surface exception (AC-710/736/1630), three probes and
their report shapes (AC-705/706/707/710), combined gate + fold-residual channel
(AC-708/737), region-aware recursive promotion (AC-709), cross-gate reconciliation
(AC-852/853/854/855/856) — plus AC-724 for the idempotence identity its Description
makes the fidelity verdict rest on. No clause is uncovered and no AC is orphaned.

**Verification performed.** Every code claim above was read in this worktree
(`regression-800a17f7`) this run, not carried from a prior report:
`probes.ts:405-417` (the pinned-box content-overflow `clip` and the
`pinnedH === undefined` early return), `probes.ts:429-433` (the docstring naming all
three violations), `probes.ts:450-485` (viewport-clip loop and the
`isSynthesizedSurfaceId` overlap exclusion); `fold.ts:215-220` / `:233-240`
(`lineCountOf` / `nowrapThreshold`), `fold.ts:1785` and `:1825` (proving `framed` is
width-ascending, which is what makes AC-1631's "narrowest sampled width" case
correct), `fold.ts:1843-1844`; `fold.ts:610-674` (`responsiveTextTracks` /
`responsivePaddingTracks` and the ≥2-and-varying guard); `fold.ts:1383-1399`
(`barBandFills`); `fold.ts:2105-2146` (page base by greatest total band height over
bands **and** backdrops, run-count fallback, canvas last resort);
`cli/index.ts:57-58`, `:803`, `:911` (`cmdRepro` / `cmdGate` live, no `adopt-values`
case). All 41 AC bodies and both story bodies were fetched from the ticket store this
run and searched programmatically; the full 216-ticket intent census was re-ordered by
`created_at` and re-read for anything newer than the last cycle.

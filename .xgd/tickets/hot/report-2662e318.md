---
uid: report-2662e318
id: REPORT-351
type: report
title: 'Reconciliation Review: commits (BUNDLE-2)'
created_by: xgd
created_at: '2026-07-09T21:27:22.725836+00:00'
updated_at: '2026-07-09T21:27:22.725836+00:00'
completed_at: null
last_field_updated: created_at
result: pass
fields:
  report_kind: reconciliation_review
  subject_uid: bundle-f39884d2
  anchor_uid: bundle-f39884d2
---

# Reconciliation Review: Story Coverage

**Result**: PASS
**Mode**: commits
**Surface**: (n/a — commits/bundle mode)
**Anchor**: bundle-f39884d2 (BUNDLE-2)
**Subject intent**: bundle-f39884d2 (bundle is a first-class intent for reconcile)
**Stories Reviewed**: 7 (STORY-54, STORY-55, STORY-57, STORY-58, STORY-59, STORY-60, STORY-61)

## Behavior Inventory

11 free-coded commits, 9 capability-bearing behaviors (+ 1 refactor, 1 chore):
1. `1c capture page <url>` — rendered-only headless-browser reference capture + BrowserDriver seam + self-contained bundle (BUG none; REQ-12)
2. `1c shot` — screenshot primitive over the driver seam (REQ-13)
3. Section-level background (color/image/gradient + overlay, text-over) (REQ-14)
4. `layer` module + z-compositing, structured free-positioned layout (REQ-15)
5. Structured motion (entrance/scroll-reveal/hover/stagger) (REQ-16)
6. Widened content-value contract (object/number/boolean) (REQ-23)
7. Module-component-CSS folding into theme.css (BUG-1)
8. @font-face emission + display-font slot + header wordmark dials (REQ-24)
9. Header `overlay` variant composited over shared image band (REQ-25)
- storage/ consolidation (REQ-22) — pure refactor, no capability change
- version bump 0.0.14→0.0.15 (98569bf) — chore, no capability

## Coverage Map

| # | Behavior | Coverage | Story | Notes |
|---|----------|----------|-------|-------|
| 1 | 1c capture page / BrowserDriver seam / capture bundle | Covered | story-8f33f14c (STORY-57) | 10 ACs (AC-459..468); no-setContent, no-static-fallback, offline re-extraction all captured |
| 2 | 1c shot screenshot primitive (slug + url modes, viewports) | Covered | story-3ae5b34e (STORY-58) | 6 ACs (AC-469..474); per-section-crops gap explicitly flagged as known intent/code divergence, not asserted |
| 3 | Section background + overlay | Covered | story-6af935e7 (STORY-59) | three-layer stack, path-pointed validation, section-scoped |
| 4 | layer module + z-compositing | Covered | story-4f50c054 (STORY-60) | positions/treatments/reflow/overlay/strict-reject |
| 5 | Structured motion | Covered | story-b13e15c5 (STORY-61) | named-easing-only, reduced-motion safety, once-per-page island |
| 6 | Content-value contract widened to object/number/boolean | Covered | story-6fc151b1 (STORY-54, upgrade) | AC add preserves .strict() raw-prop rejection; asset-ref ordering preserved |
| 7 | Module CSS folding (BUG-1) | Covered | story-a224111f (STORY-55, upgrade) | bug fix = upgrade; restores intended fully-styled render |
| 8 | @font-face + display slot + wordmark dials (REQ-24) | Covered | story-a224111f (STORY-55, upgrade) | extends theme-CSS generation + header in place |
| 9 | Header overlay variant (REQ-25) | Covered | story-a224111f (STORY-55, upgrade) | cross-instance pipeline compositing; deliberately not folded into layer primitive |
| — | storage/ consolidation (REQ-22) | Refactor — no matrix change | — | Correctly produces no plan item / story (behaviour-preserving path relocation). Noted: the generate CLI surface itself (REQ-9/11) is unreconciled — out of this bundle's scope, flagged for future reconciliation. |
| — | version bump (98569bf) | Chore — no capability | — | No plan item; correct. |

## Ungrounded Stories

None. Every story describes behaviour supported by both intent and code. Two stories proactively flag intent/code divergences rather than silently absorbing them:
- STORY-58 records that REQ-13's "optional per-section crops" were not implemented — held as a known gap, not an AC claiming implemented behaviour.
- STORY-54 records that the validated theme-token contract is the REQ-4 superset in code (fg→text rename, numeric spacing keys, 5xl type step, container narrow/default/wide/bleed, added weights/lineHeights), asserting the slot-completeness contract rather than a frozen slot enumeration.

## Plan Item Accounting

| Plan Item | Expected Story | Status |
|-----------|---------------|--------|
| 1. Capture / headless-browser vision (feature) | story-8f33f14c (STORY-57) | ✓ |
| 2. 1c shot (feature) | story-3ae5b34e (STORY-58) | ✓ |
| 3. Section background (feature) | story-6af935e7 (STORY-59) | ✓ |
| 4. layer module (feature) | story-4f50c054 (STORY-60) | ✓ |
| 5. Structured motion (feature) | story-b13e15c5 (STORY-61) | ✓ |
| 6. Content-value widen (upgrade → STORY-54) | story-6fc151b1 | ✓ (fields.updated_by=[bundle-f39884d2]) |
| 7. Theming/chrome: BUG-1 + REQ-24 + REQ-25 (upgrade → STORY-55) | story-a224111f | ✓ (fields.updated_by=[bundle-f39884d2]) |

All 7 plan items produced output. No plan items dropped.

## Evidence Sufficiency (Step 5b)

Verified by running the reconciliation UAT suite at current HEAD (7 BUNDLE-2 files → 43 passed, 0 failed) and cross-checked against the regression quality report (report-ade142b7: lint 0/0, build ok, 63 UATs passed / 0 failed). Every active AC (AC-425/426 pre-existing on STORY-54; AC-433..439 pre-existing on STORY-55; AC-459..501 new this bundle) maps 1:1 to a passing `test_UAT_AC{N}` that enters through a real user-relevant boundary:
- Capture (AC-459..466): assert against a genuine headless-Chromium capture produced once in beforeAll via `cmdCapturePage` against a loopback-served golden fixture — real var() resolution, painted hex, bg-image overlay, verbatim runs, offline re-extraction. AC-467 (seam) and AC-468 (retry-then-error) correctly use a fake `BrowserDriver` — the legitimate external-boundary thin-mock — to prove swappability and the no-static-fallback contract. A tokens-only or static-fallback implementation would fail these.
- Shot (AC-469..474): real render+serve+screenshot pipeline; PNG magic-byte + asset-paint assertions; ambiguous/missing-target error path.
- Background/layer/motion (AC-475..497 range): schema validation via the public validator + render assertions (SECTION_CSS/LAYER_CSS/MOTION_CSS folded, reduced-motion block, strict raw-prop rejection).
- Content values (AC-495..497): round-trip through `validateSite` — object/number/boolean preserved as real scalars; AC-497 proves a style/css/html prop still yields a path-pointed error (no raw-CSS escape hatch).
- Theme/chrome (AC-498..501): AC-498 renders through the real `1c render` pipeline and asserts theme.css carries actual module class-selector rules (`.header__inner`, `.hero__heading{`, `.surface-*`) with the HTML carrying those classes — this directly closes BUG-1's own documented test gap ("assert CSS contains class rules matching module classes, not just :root vars"). AC-499/500/501 assert @font-face emission, wordmark dial hooks (real Astro container render, image-logo exclusion), and single shared overlay band with chrome-first ordering.

No AC relies on a source-inspection test, no repo-owned internal is mocked, and no test would pass with the AC's behaviour removed.

## Judgment Calls

- **storage/ refactor (REQ-22) has no story** — correct: a behaviour-preserving path relocation is a refactor, which by rule changes neither stories nor ACs. Not a coverage gap.
- **Version-bump chore has no story** — correct: no capability surface.
- **Pre-existing FC orphan tests (`test_UAT_FC_REQ-12_*` etc.) remain on disk** — out of scope for this review; their removal is the downstream `check_fc_orphans` phase's responsibility, per the UAT-generation report. Not a review failure.
- **Stale `reconciliation_test` report for STORY-57 said "No test files to execute"** — superseded: `tests/reconciliation-capture.test.ts` (10 UATs) exists and passes at HEAD, confirmed by direct run and by the regression report listing AC-459..468 among 63 passing tests.
- **Duplicate schema capability (CAP-49/CAP-50)** — pre-existing, out of scope; STORY-54's upgrade targets the correct owning story regardless.

## Verdict

PASS: Stories accurately and completely document the behaviour surface of all 11 commits, faithful to the operator's stated intent (bundle body + the per-REQ implementation notes). All 7 plan items produced output; the refactor and chore correctly produced none. Intent/code divergences are explicitly flagged, not silently absorbed. Every active acceptance criterion is backed by a passing UAT that enters through a real interface and could not be passed by a broken implementation. A developer reading these stories would have a correct mental model of what BUNDLE-2 built and intended to build.

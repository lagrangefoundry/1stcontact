---
uid: report-17398aa9
id: REPORT-1887
type: report
title: 'Sync-main review: reconcile-REQ-135'
created_by: xgd
created_at: '2026-08-12T20:53:48.746223+00:00'
updated_at: '2026-08-12T20:53:48.746223+00:00'
completed_at: null
last_field_updated: created_at
result: pass
fields:
  report_kind: sync_main_review
  subject_uid: reconcile-REQ-135
---

{
  "findings": [
    {
      "risk": "OK",
      "file": "(repository-wide)",
      "description": "No semantic-revert surface exists for this sync. main's tip (715d72ccce5924e57bf757d4ff775132594bc951) is identical to the merge-base with reconcile-REQ-135; `git merge-base --is-ancestor main HEAD` is true and `git log HEAD..main` returns 0 commits. Every shape this review targets under item 2 (deletion revert, rename revert, value changed back, move revert) requires main-side intent between merge-base and pre-rebase HEAD, and that set is empty, so those checks are vacuously satisfied rather than merely unobserved. Item 1 (conflict markers): `git grep -nE '^(<{7}|\\|{7}|={7}|>{7})( |$)'` over ALL tracked files, and again restricted to *.py/*.yaml/*.yml, returned no hits. Working tree is clean (`git status --porcelain` empty); neither rebase-merge nor rebase-apply exists."
    },
    {
      "risk": "OK",
      "file": "tests/test_UAT_FC_REQ-135_text_properties.test.ts",
      "description": "The three files the briefing marks (deleted) -- tests/test_UAT_FC_REQ-135_text_properties.test.ts, tests/reconciliation-copy-edit-typography.test.ts, tests/reconciliation-copy-edit-parameter-sheet.test.ts -- are branch ADDITIONS, not resurrections of files main deleted. Verified three independent ways: (1) `git cat-file -e main:<path>` fails for all three, so none exists on main; (2) `git log --diff-filter=AD main -- <path>` is EMPTY for all three, so main never added and never deleted them -- there is no main-side deletion that could have been reverted; (3) `git log --diff-filter=A main..HEAD -- <path>` attributes each to a branch commit (4735ac509, 1505f58bd, and f39e33581 'feat(editor): a text run's typography is editable from its segment [FREE-CODED]' respectively). These are REQ-135's own new UATs and their presence in the worktree is the correct expected state."
    },
    {
      "risk": "OK",
      "file": "packages/site-schema/src/l1/edit.ts",
      "description": "Item 3 (over-aggressive conflict resolution) and item 4 (integration inconsistency) both clear. On over-aggressive resolution: sync_main_prep rebases reconcile-* branches with `-X ours` (sync_main_prep.py:247-249), which favours main's side and could in principle drop branch intent -- but the reflog shows the rebase picks produced byte-identical SHAs (pre- and post-rebase tip both f2cd35387), and an identical commit SHA entails an identical tree, so the strategy flag demonstrably dropped nothing. resolve_conflicts (report-a1e10d8a) independently found no conflicts to resolve. On integration: cross-file wiring checked -- the newly exported type L1FieldValue in packages/site-schema/src/l1/edit.ts is correctly re-exported from packages/site-schema/src/l1/index.ts. Composition verified by execution, not inference: `npx vitest run` over the three branch-added test files passed 3/3 files, 19/19 tests in 3.74s. There is also no source drift since the passing scoped-quality commit -- `git diff a477a3cf9..HEAD` excluding .xgd/ is empty, so report-5093d2f1's result (72 tests, 0 failed, lint 0/0, build success) still describes the current tree."
    },
    {
      "risk": "LOW",
      "file": "(review tooling: xgd_source/core/sync_main/sync_main_prep.py:220)",
      "description": "Process observation, NOT a defect in this branch -- recorded so the false-positive shape is on record. The briefing's Changed Files section is generated by `git diff --name-status HEAD main` (sync_main_prep.py:220), i.e. the diff runs FROM the branch TO main. Under that direction a D entry means 'present on the branch, absent on main' -- a file the BRANCH ADDS. The sync_main_review prompt's item-2 procedure instructs the reviewer to read (deleted) as 'main DELETED it' and to raise CRITICAL if the path exists in the worktree. Those two readings are exact inverses. Verified empirically: `git diff --name-status HEAD main` reports D for all three paths while `git diff --name-status main HEAD` reports A. Consequence: any reconcile branch that adds files -- the normal case for a branch carrying new UATs -- will present its own additions as (deleted), and a reviewer following the procedure literally would emit a false CRITICAL per added file and fail a clean sync. Here that would have been 3 spurious CRITICALs. No action taken (this review is read-only, and the fix belongs in the xgd package, not this repo)."
    }
  ]
}

---
uid: report-394e0821
id: REPORT-4390
type: report
title: 'Reconcile resolve conflicts: reconcile-BUNDLE-27'
created_by: xgd
created_at: '2026-09-19T10:52:27.455182+00:00'
updated_at: '2026-09-19T10:52:27.455182+00:00'
completed_at: null
last_field_updated: created_at
result: pass
fields:
  report_kind: resolve_conflicts
  subject_uid: reconcile-BUNDLE-27
---

## Files resolved

- `.xgd/tickets/hot/request-01ea4eec.md` — **UU**, index-only (path is outside the
  sparse-checkout cone per DOC-986 §2/§4.1: `.xgd/tickets/` is excluded, so there was no
  working-tree file and no on-disk conflict markers). Class: intent/bookkeeping ticket
  (`request-*`) → **rule 2e**. Resolved to the HEAD side via
  `git checkout --ours` + `git add --sparse`.

### Why ours, fact by fact

The incoming commit is `ffce0148` *"xgd(ticket): update request request-01ea4eec"*
(2026-09-01), a `free_coded` status transition. Diffing the three index stages shows the
two sides are identical everywhere except frontmatter:

- **Body (~112 inserted lines): byte-identical on both sides.** Both ours and theirs apply
  the exact same rewrite against the merge base — the fourteen→seventeen `mkdirSync` recount,
  the new "The shape of the port" / "Design decisions taken here" / "Non-determinism (AC3)" /
  "Blast radius" / "Test plan" sections, and the six restated acceptance criteria. Not a
  conflicting fact; nothing to compose.
- **`status`, `updated_at`, `completed_at`: same field, different values** → genuine conflict,
  timeline rule applies per-fact. Ours is `bundled` / 2026-09-16 / 2026-09-14; theirs is
  `free_coding` / 2026-09-01 / null. Ours is the later lifecycle position on the same axis.
- **`fields.commits`, `fields.version`, `fields.bundled_in`: ours only** (theirs has none) →
  ours is a strict superset. Taking theirs would have destroyed the bundle bookkeeping.
- **Trailing newline**: theirs adds one, ours has none. Cosmetic, zero semantic content;
  importing it alone would manufacture a diff against HEAD for nothing.

There is no field present on the incoming side that the HEAD side lacks, so no disjoint edit
was dropped and nothing had to be composed.

The ordering is not inferred from wall-clock timestamps alone — it is structural.
HEAD's frontmatter records `commits[0].working_sha: ab467d6c` and
`bundled_in: bundle-8e1807f6`, i.e. the very bundle now being reconciled, and
`git merge-base --is-ancestor ffce0148 ab467d6c` confirms the incoming commit is an
**ancestor** of that recorded free-coded implementation commit
(`feat(store): a ReferenceStore port, with the filesystem behind it [FREE-CODED]`).
HEAD is therefore downstream of the incoming commit by construction on every conflicting
fact, which is exactly the situation rule 2e's timeline clause resolves toward ours.

## Incoming changes preserved

No code/implementation files were in conflict — the single conflicted path is a bookkeeping
ticket, not source.

The incoming commit `ffce0148` touches exactly one file, and its entire substantive change
(the body rewrite) is present **verbatim** in the resolved file: the staged blob
`2e26c89e` contains every one of those ~112 lines. The only part of the incoming diff not
carried forward is its `status: free_coding` frontmatter transition, which HEAD has already
superseded with `status: bundled` — a later position for the same fact, not a discard.

No hunk was dropped under the BUG-1301 precedence exception; no test function was deleted;
no UAT files were involved.

**Note for the finalize step:** the resolution nets to no diff vs HEAD — the staged blob and
`HEAD:.xgd/tickets/hot/request-01ea4eec.md` are both `2e26c89e`. This is the redundant-commit
case (BUG-1109/BUG-1122), not the discarded case: STEP 3's discriminator is satisfied, since
the incoming commit's key changes are demonstrably *present* in HEAD via a different route
(the post-watermark sync already carried this body edit), rather than absent. Per STEP 4 the
tree is staged and exits `@done` without calling `--skip`; Python's finalize will detect the
clean staged diff and skip the commit.

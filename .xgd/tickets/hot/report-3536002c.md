---
uid: report-3536002c
id: REPORT-1621
type: report
title: 'Capability-Intent Alignment: Builder Workspace: Chrome, Origin & Display Panel
  (level=story)'
created_by: xgd
created_at: '2026-08-07T20:25:17.678594+00:00'
updated_at: '2026-08-07T20:25:17.678594+00:00'
completed_at: null
last_field_updated: created_at
result: fail
fields:
  report_kind: capability_validation
  subject_uid: capability-a994b8f3
  level: story
  violations: 2
  warnings: 3
  needs_review_count: 0
---

# Capability-Intent Alignment: Builder Workspace: Chrome, Origin & Display Panel
# Level: story

**Result**: FAIL
**Violations**: 2
**Warnings**: 3
**Needs review**: 0

Anchor report: report-17a279f7 · Capability: capability-a994b8f3 (CAP-85) ·
Story tree: STORY-99 (story-e674c60a) only · Attempt 3 (prior: report-c5a97ce2
FAIL → fix report-6d2f7dbd; report-472feebd FAIL → fix report-b7fbc162).

**Headline: attempt 2's story edit silently reverted attempt 1's repairs.**
Attempt 2 fixed the code (freshness) correctly and permanently — that is
verified below. But its `story-body-edit` was written over a *stale* read of
STORY-99, so the three passages attempt 1 added to that story are gone from the
committed body. This is the exact hazard attempt 2's own fix report identified
and guarded against for CAP-85 — it applied the guard to the capability and not
to the story. Every finding below except the ledger `info`s is a re-opened
attempt-1 finding, not new drift.

Proof, from the ticket file's own history (`.xgd/tickets/hot/story-e674c60a.md`):

| Revision | When | `transport` | `CAP-87` | Size |
|---|---|---|---|---|
| `daf325033` (pre-validation) | 2026-08-06 20:36 | 0 | 0 | 9403 |
| `a1f3e5f70` (attempt-1 fix) | 2026-08-07 12:53 | **5** | **2** | 11295 |
| `4b8553945` (attempt-2 fix) | 2026-08-07 13:09 | **0** | **0** | 10511 |

`git diff a1f3e5f70 4b8553945 -- .xgd/tickets/hot/story-e674c60a.md` shows the
three passages removed and attempt-2's three additions added in the same commit.
The on-disk file, `HEAD`, the `xgd ticket get` read and the injected context all
agree with `4b8553945` — the loss is committed, not a read artifact.

## Cumulative Intent Considered

STORY-99 carries `intent_uid: bundle-15c1f647` (BUNDLE-16, `free_and_reconciled`,
`merged_at_commit 1741ee5d`) and no `updated_by`. A bundle is not itself an ask,
so the ledger is walked at the request/bug level. Re-derived independently this
attempt across all 50 requests and all 31 bugs — the bug sweep is new; attempt 2
checked requests only, which is how BUG-32 was missed.

| Intent ID | Status | When | Asked / changed | Counts? |
|---|---|---|---|---|
| REQ-44 (request-3b78151f) | free_and_reconciled | 2026-07-03 | `1c` CLI dependency preflight — fail loud on out-of-sync `node_modules` | YES — but lands outside this capability (info #6) |
| REQ-115 (request-a6740b4a) | free_and_reconciled | 2026-07-31 | T1 builder chrome: webui consumed from the installed artifact store through each package's `exports`, Node origin + `control-app` same-origin front, `site` tab, multi-mode display panel, mode-declared toolbar, split + namespaced persistence, shared `resolveStaticFile` confinement, publish through the existing path | YES — the primary intent for this capability |
| COMMENT-598 / COMMENT-600 on REQ-115 | — | 2026-07-31 | Settles Deliverable 0 (upstream blocker) and the Node-origin-plus-Worker-front shape, including the three-way spike that rejected both Worker bundler routes | YES — secondary source of truth |
| REQ-116 (request-41796766) | free_and_reconciled | 2026-07-31 | The edit render channel | context only — owned by CAP-87 / STORY-98 |
| REQ-117 (request-395b67e6) | free_and_reconciled | 2026-07-31 | T3 copy editing end-to-end. Three recorded sections touch this capability: the viewport-fill follow-up (`94ae6fee`), "the loop is closed" (`cda7fe4d` — `/api/copy` and `/framework/edit-client.js` on this origin), and the freshness fix | YES (the chrome/origin parts) |
| COMMENT-601 on REQ-117 | — | 2026-08-06 | Operator dialogue settling freshness ("`sendFile` sent `content-type` and nothing else … Now `no-store`", then "the shell at `/` is the only response still cacheable") and that a save re-renders **both** channels | YES — the sole intent source for AC-977 |
| REQ-118 (request-66e4c630) | free_and_reconciled | 2026-07-31 | Image selection; added `GET /api/assets` to this origin | YES — origin reachability claimed by STORY-102 / CAP-89 (info #7) |
| BUG-32 (bug-5cabb340) | **free_coded** | 2026-08-05 | Component npm scope `@gendevlabs` → `@lagrangefoundry`; `WEBUI_SCOPE` gets exactly one definition site; the builder's browser-source bare specifiers must match the import-map keys; the webui suites must assert resolution positively instead of skipping green | **NO — not yet** (info #5). `free_coded` is a stable resting state; per FREE-CODING.md the matrix is updated on explicit promotion to `ready_to_reconcile`, and `fields.commits[].main_sha` is null. Confirmed absent from this branch: `tools/generate/src/cli/webui.ts:33` still reads `WEBUI_SCOPE = '@gendevlabs'` |
| REQ-119 (request-64864801) | draft | 2026-07-31 | Request-time renders inside `control-app`; deletes the proxy | NO — draft, retires nothing yet |

Cumulative picture, unchanged from attempt 2: REQ-115 establishes the whole
surface; REQ-117 adds the window-fill fix, the edit transport, the served edit
bridge and the freshness directive; nothing has been retired, so the story's
"deliberate and temporary" framing of the Node origin remains correct.

## Prior attempts — what survived and what did not

| Repair | Claimed by | Verified now |
|---|---|---|
| A1 #1 — origin's operations + edit transport + expected-refusal shape added to the story | attempt 1 | **REVERTED** — finding #1 |
| A1 #2 — CAP-84 pointers repointed to CAP-87 | attempt 1 | **REVERTED** — finding #3 |
| A1 #3 — "An origin that is missing is not a blank page" bullet added | attempt 1 | **REVERTED** — finding #2 |
| A2 #1/#2 — `NO_STORE` on `json()` and the four bare text refusals in `builder.ts` | attempt 2 | **Confirmed and committed** (`3be64b402`). `tools/generate/src/cli/builder.ts:130` defines `NO_STORE`; it is applied at `:148`, `:187`, `:303`, `:343`, `:356`, `:369`, `:393`, `:397`. Working tree clean |
| A2 #3 — AC-977 UAT probe widened, header asserted independently of status | attempt 2 | **Confirmed** — same commit, `tests/reconciliation-builder-workspace-origin.test.ts` +49/−11 |
| A2 #4 — served client code added to the story's origin enumeration; tab declaration integrity added | attempt 2 | **Confirmed** — story body lines 37–47 and the "One tab" bullet |
| A2 #5 — same addition to CAP-85's body | attempt 2 | **Confirmed** — CAP-85 body carries it, and (because attempt 2 sourced it from the committed blob) attempt 1's "transport" clause survives there too. This is why CAP-85 and STORY-99 now disagree |
| A2 #6 — AC-977 criterion/verification widened | attempt 2 | **Confirmed** — AC-977 now names the served client code, the operations and refusals |

## Alignment Ledger

| Element | Intents aligned to | Outcome |
|---|---|---|
| STORY-99 — component consumption (installed store, `exports`-declared entry points, never vendored, missing-install diagnostic) | REQ-115 Deliverable 0 + COMMENT-600 | aligned |
| STORY-99 — single origin serving document / components / browser source / **served edit bridge** / every channel | REQ-115 + REQ-117 "the loop is closed" | aligned (attempt-2 repair held) |
| STORY-99 — the origin's *operations*: list the store, publish, and the write path's read/apply as a thin transport whose refusal is expected | REQ-117 "the loop is closed"; ships at `builder.ts:239` (`/api/copy`), `:232` ("thin transports over `editCopyGet`/`editCopySet` — the SAME"), `:377-378` (`CommandError` → 400 + envelope) | **gap — finding #1: present in CAP-85's body, asserted from the other side by STORY-100, absent from STORY-99** |
| STORY-99 — unconfigured vs unreachable origin are distinct self-explanatory failures | REQ-115 front design + COMMENT-600; ships at `apps/control-app/src/index.ts:26-33` (503 naming `1c builder`) and `:44-49` (502 naming the address) | **gap — finding #2: AC-965 (active) and CAP-85's body claim it; STORY-99 no longer does** |
| STORY-99 — "Displays, never produces" / "the editable mode is registered" pointers | REQ-116 → CAP-87 (capability-12fee326) | **gap — finding #3: both point at CAP-84 (capability-25f7e486, `superseded`, `superseded_by_uid: capability-12fee326`)** |
| STORY-99 — the transport seam is claimed once from each side | REQ-117 + STORY-100 (story-37a3921b, lines 140–141: "The builder workspace (CAP-85 / STORY-99) exposes this same surface over its origin as a thin transport") | **gap — finding #4: STORY-100 still names the split; STORY-99's half of the sentence is gone** |
| STORY-99 — one tab, fills the window, follows a live resize, page never scrolls; declaration integrity | REQ-117 follow-up `94ae6fee` | aligned (attempt-2 repair held) |
| STORY-99 — one definition site for every name | REQ-115 AC 3 | aligned |
| STORY-99 — display panel, modes as entries, switch preserves the pane | REQ-115 AC 7 | aligned |
| STORY-99 — toolbar renders the active mode's declared controls; selector over the real store; publish via the existing path | REQ-115 AC 6, 8, 9 | aligned |
| STORY-99 — split, drag, collapse-to-rail, namespaced persistence | REQ-115 AC 4, 5 | aligned |
| STORY-99 — freshness: every response non-cacheable | REQ-117 / COMMENT-601 | aligned — **and now true in the code** (attempt-2 repair held) |
| STORY-99 — confinement across all three trees, clamped so the refusal reads as not-found | REQ-115 | aligned |
| STORY-99 — divergence note: `no-store` also reached `1c serve` (STORY-95/96) | COMMENT-601 | aligned — preserve verbatim |
| STORY-99 — "the origin is temporary" | REQ-119 (`draft`) | aligned — nothing retired yet |
| CAP-85 body | REQ-115 + REQ-117 | aligned — carries both attempt-1 and attempt-2 text; it is STORY-99 that regressed away from it |

## Findings

| # | Severity | Property | Element | Resolution category | Issue | Suggested edit |
|---|---|---|---|---|---|---|
| 1 | violation | coverage | STORY-99 (story-e674c60a) "A single workspace, at one address" bullet, body line 37; Technical Context "Displays, never produces", line 117 | story-body-edit | The origin's **operations** are no longer expressed. Removed at `4b8553945`: "The same origin also carries the operations the workspace performs on a site — listing the store, publishing, and the read-and-apply steps of the write path behind the editing gesture — the last of these as a thin transport that adds no semantics of its own … a refused edit arrives as an *expected* refusal carrying the write path's own code, path and hint". This ships: `tools/generate/src/cli/builder.ts:239` routes `/api/copy` GET/POST, `:232` documents them as "thin transports over `editCopyGet` / `editCopySet` — the SAME" functions `1c copy get\|set` dispatch to, and `:377-378` returns `CommandError` at **400** carrying `err.toEnvelope()`. Intent records it (REQ-117, "the loop is closed": "A `CommandError` is the **expected** answer to a bad edit … a 500 would read as 'the builder broke'"). CAP-85's own body still claims it ("carrying the write path's read/apply operations as a thin transport … so that a refused edit arrives as an expected refusal in the write path's own terms"), so the capability and its only story now contradict each other. **And no AC covers it** (attempt-2 finding #6, still open), so after this regression the behaviour is expressed nowhere in the story tree. The residual out-of-scope clause at line 107 ("the write path is *reachable* over this origin") is a negative statement only — it does not claim the transport, and the refusal shape is gone entirely. Also reverted in the same commit: "carries the write path's read/apply operations as a transport … adds no rendering, no publish semantics and no edit semantics" at line 117. | Restore both passages verbatim from `git show a1f3e5f70:.xgd/tickets/hot/story-e674c60a.md`. Do **not** re-author them — attempt 1's wording was validated at attempt 2 and matches CAP-85's body clause for clause |
| 2 | violation | coverage | STORY-99 (story-e674c60a) — the bullet that stood between lines 47 and 49 | story-body-edit | The bullet "**An origin that is missing is not a blank page.** A workspace whose origin is not configured and one whose origin is configured but not answering are two different, self-explanatory failures … the first names how to start the origin, the second names the address that was tried" was removed at `4b8553945`. **AC-965 (`acceptance_criterion-5286c04b`, `active`) holds exactly this criterion** and CAP-85's body states it ("An unconfigured origin and an unreachable one are distinct, self-explanatory failures rather than a blank page"). It ships at `apps/control-app/src/index.ts:26-33` (503, body names `1c builder`) and `:44-49` (502, body names the origin that was tried). An active AC whose behaviour its own story body does not describe is the leading drift shape this level exists to catch. The one surviving "blank page" hit in the body (line 58) is the *component-install* diagnostic, a different guarantee | Restore the bullet verbatim from `a1f3e5f70`, in its original position immediately after the "single workspace" bullet |
| 3 | warning | consistency | STORY-99 (story-e674c60a) lines 118 and 123 | story-body-edit | Both re-point at **CAP-84** ("the draft, published and editable channels produced by CAP-82 (Site Delivery) and CAP-84 (Edit Render Channel)"; "the editable render belongs to CAP-84"). `capability-25f7e486` (CAP-84) is `status: superseded` with `fields.superseded_by_uid: capability-12fee326` (CAP-87, In-Page Copy Editing). Attempt 1 repaired this (CAP-84 ×0, CAP-87 ×2 at `a1f3e5f70`); `4b8553945` restored the stale text | Restore attempt 1's wording: CAP-87 (In-Page Copy Editing) at line 118, and "belongs to CAP-87 (STORY-98)" at line 123 |
| 4 | warning | exclusivity | STORY-99 (story-e674c60a) Technical Context; STORY-100 (story-37a3921b) lines 140–141 | story-body-edit | The bullet "**The edit transport is one seam, claimed once.** The write path's operations are defined by CAP-86 / STORY-100 … This story holds the other half of that sentence — the reachability and the shape of the answer at this origin — so the behaviour is claimed by the matrix exactly once from each side" was removed at `4b8553945`. STORY-100 still carries its half and names STORY-99 explicitly, so the matrix now records a two-sided seam from one side only — leaving the boundary undocumented here and inviting a future session to either duplicate the semantics into CAP-85 or delete STORY-100's reference as unmatched | Restore the bullet verbatim from `a1f3e5f70`. Fixing #1 without it re-adds the behaviour but not the ownership guard that keeps it from being claimed twice |
| 5 | warning | consistency | STORY-99 (story-e674c60a) — process, not text | story-body-edit | The regression's mechanism, so it does not happen a third time. Attempt 2's fix report (report-b7fbc162) diagnosed it precisely — "`.xgd/tickets/` is excluded by this worktree's sparse-checkout … Any session editing a non-materialised ticket in this worktree via `--body-file` risks silently reverting prior repairs" — and applied the workaround to **CAP-85 only**, which is why the capability body kept both attempts' text and the story lost one attempt's. (`git ls-files -v` now reports `H` for both ticket files and `git sparse-checkout list` still carries `!/.xgd/tickets/`, so the exclusion is configured even though the files are currently materialised.) | Before writing STORY-99, diff the intended new body against `git show HEAD:.xgd/tickets/hot/story-e674c60a.md`; the only deletions should be ones this report asks for. After writing, re-run the diff and confirm `transport` ≥ 5, `CAP-87` = 2, `CAP-84` = 0 |
| 6 | info | coverage | BUG-32 (bug-5cabb340), `free_coded` | — | Squarely CAP-85 scope — the component npm scope moved `@gendevlabs` → `@lagrangefoundry`, `WEBUI_SCOPE` gained a one-definition-site rule, and the builder's browser sources' bare specifiers were coupled to the import-map keys with four unconditional UATs replacing a skip-green check. It is **not** counted: `free_coded` is FREE-CODING.md's stable resting state, the matrix is updated only on operator promotion to `ready_to_reconcile`, `main_sha` is null, and this branch still reads `WEBUI_SCOPE = '@gendevlabs'` (`tools/generate/src/cli/webui.ts:33`). Recorded so the reconcile that follows its promotion knows it lands here — on the "consumed not copied" bullet and probably AC-961/963 | none |
| 7 | info | coverage | STORY-99 ACs | — | Still no AC covers the edit transport (attempt-1 finding #1's deferred half, re-forwarded by attempt 2 as its finding #6). It should be about reachability and the shape of the refusal — the write path's own `code`/`path`/`hint` — never about what the write path validates. Blocked behind finding #1: an AC cannot be written for a behaviour the story body no longer describes | none at story level |
| 8 | info | consistency | STORY-100 (story-37a3921b) line 138 | — | Still "the edit render channel (CAP-84 / STORY-98)". Same superseded pointer as finding #3, in a different capability's story. Forwarded by attempts 1 and 2; re-confirmed present. Sweep when CAP-86 is validated | none here |
| 9 | info | coverage | REQ-44 | — | In this capability's bundle but asks for a `1c` CLI dependency preflight. Expressed by STORY-79 (`story-e15a19ef`, CAP-63) which carries `updated_by: bundle-15c1f647` — verified this attempt. Correctly placed; no CAP-85 gap | none |
| 10 | info | coverage | REQ-118 / `/api/assets` | — | `GET /api/assets` (`builder.ts:205`) is claimed by STORY-102 (`story-c46abfa6`, CAP-89). Deliberate and consistent with STORY-99 enumerating only the operations this capability owns — which is precisely the enumeration finding #1 restores | none |
| 11 | info | — | STORY-99 freshness bullet + AC-977 | — | Attempt 2's violation is genuinely closed, in code and in evidence, and is committed (`3be64b402`, working tree clean). This is the one thing about attempt 2 that did not regress, and it should not be re-litigated | none |

## Notes for the Editor

- **This is a restore, not a rewrite.** Findings #1–#4 are four passages that
  existed, were validated, and were deleted by accident. The whole repair is:

  ```
  git show a1f3e5f70:.xgd/tickets/hot/story-e674c60a.md
  ```

  Take from it the "operations/transport" sentences in the *single workspace*
  bullet, the *"An origin that is missing is not a blank page"* bullet, the
  *"The edit transport is one seam, claimed once"* Technical Context bullet, and
  the two CAP-87 pointers. Keep everything attempt 2 added (the served client
  code in the origin enumeration, the tab-declaration-integrity clause, the
  widened out-of-scope editing bullet) — those are correct and intent-supported.
  The target body is the **union** of `a1f3e5f70` and `4b8553945`, which is why
  re-authoring by hand is the wrong move: it is a merge, and both parents exist.

- **Verify before and after writing.** Finding #5 is the reason this cycle is on
  attempt 3. A post-write check of `git show HEAD:.xgd/tickets/hot/story-e674c60a.md`
  costs one command and would have caught this at 13:09.

- **Do not touch the code.** Attempt 2's `NO_STORE` fix is correct, committed and
  re-verified here (`builder.ts:130` plus eight call sites, working tree clean).
  There is no `code-issue` in this report.

- **Do not widen CAP-85 into the gesture.** As with the served bridge, the half
  that belongs here is *reachability and the shape of the answer*; what the write
  path validates belongs to CAP-86 / STORY-100, and what the client code does
  once it runs belongs to CAP-87 / STORY-101. Attempt 1's wording already draws
  that line correctly — another reason to restore rather than re-author.

- **Method note.** Exclusivity was again checked across neighbouring capabilities
  rather than within the tree (CAP-85 has one story): STORY-100 (CAP-86) holds
  the write-path semantics and names STORY-99 for the transport; STORY-101
  (CAP-87) defers the workspace; STORY-102 (CAP-89) claims the asset listing's
  origin reachability; STORY-95/96 (CAP-82) cover the separate deployed-snapshot
  origin. The intent sweep was widened this attempt to include `type=bug`, which
  attempt 2 did not do — that is how BUG-32 surfaced (info #6). Future cycles on
  this capability should sweep bugs as well as requests.

---
uid: report-270821e8
id: REPORT-1568
type: report
title: 'Sync-main review: reconcile-REQ-118'
created_by: xgd
created_at: '2026-08-07T05:32:11.144125+00:00'
updated_at: '2026-08-07T05:32:11.144125+00:00'
completed_at: null
last_field_updated: created_at
result: pass
fields:
  report_kind: sync_main_review
  subject_uid: reconcile-REQ-118
---

{
  "findings": [
    {
      "risk": "MEDIUM",
      "file": "index.html",
      "description": "A repo-root index.html was added by commit 76992a05d, a bookkeeping commit whose message ('xgd(resync): terminal state complete') describes no code change and which contains this file as its only content. It is a hand-frozen duplicate of the builder shell that tools/generate/src/cli/builder.ts generates dynamically in chromeHtml(): it hardcodes the three webui stylesheet links and the importmap that chromeHtml() derives at runtime from WEBUI_PACKAGES/webuiExports(). It is also dead code - the builder's '/' and '/index.html' routes both return chromeHtml() before any static-file resolution, and nothing else in the tree references a repo-root index.html (every other index.html reference is a generated site-output path). Not a revert of main's intent (main never had this file at any point in its history), and it breaks no test or build, but it is a duplicate/orphan artifact per the project's coding standards ('ONE authoritative location per component'; 'delete old files') that will silently drift from chromeHtml() as WEBUI_PACKAGES changes. Recommend deleting before the merge back to main."
    },
    {
      "risk": "OK",
      "file": "packages/site-schema/src/l1/edit.ts",
      "description": "copyFieldsOf/applyCopyFields were rewritten to add the image branch, but the rewrite is a strict superset of main's behaviour: the text branch, the unknown-key refusal, the non-string refusal, and the whole-or-nothing change accumulation are all preserved, with enum-membership validation added on the write side. No main-side logic was dropped."
    },
    {
      "risk": "OK",
      "file": "tests/req117-copy-editing.test.ts",
      "description": "The only main-side test file the branch modified. The 'segment with no editable fields opens no modal' assertion was retargeted from the image node to a container node because REQ-118 legitimately gave images a picker and alt field. The property under test is preserved, not weakened. All other main REQ-117 tests (edit-loop, modal-dismiss, stale-edit-render, nowrap-width-is-a-floor, builder-viewport-fill, edit-loop-browser) are byte-identical to main."
    }
  ]
}

---
uid: report-4386e291
id: REPORT-4445
type: report
title: 'Reconcile resolve conflicts: reconcile-BUNDLE-27'
created_by: xgd
created_at: '2026-09-19T13:26:31.651248+00:00'
updated_at: '2026-09-19T13:26:31.651248+00:00'
completed_at: null
last_field_updated: created_at
result: pass
fields:
  report_kind: resolve_conflicts
  subject_uid: reconcile-BUNDLE-27
---

## Files resolved

- `.xgd/tickets/hot/bug-360c5a44.md` — **UU**, index-only (the path is outside
  the sparse-checkout cone, so there were no working-tree markers, only three
  index stages). Class **2e** (intent/bookkeeping ticket — `bug-*`).
  **Rule applied: one side is a strict superset of the other — keep the
  superset**, which is the OURS/HEAD side. Resolved with `git checkout --ours`
  + `git add --sparse`; staged blob `c0b18a118a071b329094493542b14db97cca1afd`,
  byte-identical to stage 2.

Incoming commit: `6862ea1402fbfdb44a96861e415402e6d0f2eba4`
("xgd(ticket): update bug bug-360c5a44", 2026-09-01 15:34:57 -0700).
Stage 1 (base) is `4184f043` — the blob introduced by `fd72594e`, the commit
handled in the immediately preceding attempt (scope 167/0), so the sequencer
is walking consecutive working-timeline commits over the same ticket.

### Why ours, not incoming

Both the enrichment's "take the more recent commit by timestamp" rule and 2e's
superset rule point the same way:

| | OURS (HEAD) | THEIRS (`6862ea14`) |
|---|---|---|
| `updated_at` | 2026-09-16T01:48:37 | 2026-09-01T22:34:57 |
| `status` | `bundled` | `draft` |
| `completed_at` | 2026-09-14T10:29:01 | `null` |
| `last_field_updated` | `status` | `body` |
| `fields.commits` | `working_sha: 84cc117a…` | absent |
| `fields.version` | `0.2.40` | absent |
| `fields.bundled_in` | `bundle-8e1807f6` | absent |

Taking the incoming side would have regressed the ticket's lifecycle from
`bundled` back to `draft`, nulled `completed_at`, and dropped `commits`,
`version` and `bundled_in: bundle-8e1807f6` — this bundle's own marker — while
gaining nothing in the body (see below).

## Incoming changes preserved

The incoming commit made exactly four changes: an `updated_at` bump and three
body additions. All three body additions are present **verbatim** in the
resolved file:

1. The new paragraph in `## Fix` — "Reloading a frame is the host's business,
   and a host that fails at it must not take the conversation with it: a
   callback that throws is swallowed and the turn finishes streaming its answer
   in full." Present.
2. The rewritten **panel** test-plan bullet — "…once per signal, carrying the
   counter; one that does not, does not. The signal leaves no trace in the
   conversation, and a callback that throws does not cost the operator the
   assistant's answer." Present.
3. The new **app** test-plan bullet — "a turn reporting two writes reloads the
   preview frame twice, and a turn reporting none leaves it alone." Present.

Verified mechanically by diffing the incoming blob `f392f0030d` against the
resolved blob `c0b18a118a`: the two are **byte-identical across the entire
body**. The only differences are the frontmatter lifecycle block tabulated
above, plus a trailing-newline marker on the final line (HEAD's existing byte
state, preserved). The conflict was therefore confined to the frontmatter
region, where HEAD carries strictly later state — nothing in the body was in
genuine contention.

No code/implementation files were in conflict. No UAT test file was touched.
No BUG-1301 precedence exception was invoked — no hunk was dropped.

## Net effect

The staged tree has no diff against HEAD (`git diff --cached HEAD` is empty):
this commit's body content had already reached the reconcile branch by another
route and the ticket was then advanced past it. Per STEP 4 this is the
redundant-commit case, not the discarded case — STEP 3's check passes, because
the incoming commit's changes are demonstrably *present* in HEAD (byte-for-byte)
rather than absent. `--skip` was not called; `CHERRY_PICK_HEAD` (`6862ea14`)
remains in place for `cherry_pick_finalize_resolution`.

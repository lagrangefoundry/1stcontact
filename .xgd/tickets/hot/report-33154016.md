---
uid: report-33154016
id: REPORT-2280
type: report
title: 'Reconcile resolve conflicts: reconcile-BUNDLE-19'
created_by: xgd
created_at: '2026-08-20T00:46:01.871495+00:00'
updated_at: '2026-08-20T00:46:01.871495+00:00'
completed_at: null
last_field_updated: created_at
result: pass
fields:
  report_kind: resolve_conflicts
  subject_uid: reconcile-BUNDLE-19
---

Incoming commit: `6b94ba96` — *feat(editor): lock a control that cannot express what the element holds [FREE-CODED]* (Sat Aug 15 13:29:44 2026 -0700).
Ours side: `64dcc96b` — *xgd: sync from xgd-working 5ed608341606 (post-watermark)* (Wed Aug 19 11:37:53 2026 -0700).

## Files resolved

- **`package.json`** — UU, config file (2g). Sole conflict was the version scalar: HEAD `0.1.58` vs incoming `0.1.47`. Resolved to **`0.1.58`** (HEAD). The version number is monotonic and HEAD's commit is four days *later* than the incoming commit, so the enrichment's "take the more recent commit by timestamp" rule and the monotonic-version rule agree. Taking incoming would have regressed the project version by eleven bumps and broken `xgd_version_bump --check` for every ticket promoted since. No behavioural content on either side of this hunk. **Resolves to no staged diff vs HEAD, which is correct** — this file's only incoming change was the version bump, superseded by later bumps already on main.

- **`packages/site-schema/src/l1/edit.ts`** — UU, implementation/code file (2c). Two hunks, both in `applyCopyFields`, both the *same region rewritten by each side*: mutually exclusive → **INCOMING WINS** (rule 2c.3.c), and incoming is also the strict superset here (3.a).
  - *Hunk 1* (doc comment above `applyCopyFields`): HEAD carried the REQ-135 paragraph explaining the lock-refuses-a-change rule inline; incoming replaced it with the REQ-139 generalisation, which states the same rule and additionally points at the new `{@link lockError}` helper. Took incoming — HEAD's paragraph describes behaviour that now lives in `lockError`, so keeping it would have left the file documenting a check that is no longer in it.
  - *Hunk 2* (the loop body): HEAD held an inline `if (field.locked && value !== derived.values[name])` guard; incoming deleted that guard, having hoisted it into `lockError()` at the head of the refusal chain. Took incoming (deletion). The replacement call — `lockError(field, value, derived.values[name]) ??` — had already merged **cleanly** into the chain from incoming, so retaining HEAD's block would have left two locked-field checks in series. It would also have been actively wrong on the REQ-140 colour axis both sides had just added: HEAD's raw `value !== derived.values[name]` compares palette references by object identity, so a `{ref:'neutral'}` echoed back on an untouched save would compare unequal and refuse the whole change map. `lockError` routes colour through `sameColor()`, which is exactly the case the incoming commit message calls out ("a lock refuses a CHANGE, never the status quo... refusing a re-post would have made a locked colour freeze the whole segment, headline words included").
  - **Neither side lost intent.** HEAD's semantic contribution (a lock binds a change, not the status quo) is preserved *in full* by `lockError`, which incoming authored to carry precisely that rule — and extends it to colour, which HEAD's version could not express.

## Incoming changes preserved

Verified against `git show 6b94ba96 -- packages/site-schema/src/l1/edit.ts`. Every hunk of the incoming diff is present in the resolved file:

- `L1FieldDescriptor.reason?: string` + its REQ-139 doc block — present.
- `L1FieldDescriptor.locked` doc block rewritten to the faithful-control rule (inert / lossy / unsupported) — present.
- `interface L1FieldLock { locked: true; reason: string }` — present.
- `GLYPH_GRADIENT_LOCK` and `NO_ITALIC_FACE_LOCK` constants, with their operator-facing reason strings verbatim — present.
- `typographyFields`: `...(lockedItalic ? NO_ITALIC_FACE_LOCK : {})` replacing the bare `{ locked: true }` — present.
- `colorField`: new `lock: L1FieldLock | null = null` parameter and `...(lock ?? {})` spread into the descriptor — present.
- `L1TextAxesView.gradientFill?: unknown` — present.
- `copyFieldsOf`: the `text` branch passing `axes.gradientFill === undefined ? null : GLYPH_GRADIENT_LOCK` to `colorField` — present.
- `lockError()` function, including the `field.type === 'color' ? sameColor(...) : value === current` discrimination and the `field.reason ?? ...` fallback — present.
- `applyCopyFields`: inline `field.locked` block removed, `lockError(...)` first in the refusal chain — present (this was conflicted hunk 2, resolved to incoming).

Nothing in the incoming diff is absent from the resolution.

## Verification

- `git status --porcelain` — no `UU`/`AA`/`DU`/`UD` lines remain. `packages/site-schema/src/l1/edit.ts` is staged `M`; `package.json` resolves to HEAD's content and therefore shows no entry.
- `CHERRY_PICK_HEAD` still present at `6b94ba96367a3f0cb7ec6dfc7f5dfe3d5661dae5` — no sequencer-state command was run.
- Spot-check: `xgd quality run --tests test_UAT_FC_REQ-139_locked_controls` executed the vitest suite — **success (206.26s), 0 errors, 0 warnings**, overall status SUCCESS. This covers both `tests/test_UAT_FC_REQ-139_locked_controls.test.ts` (the incoming commit's own 507-line UAT) and `tests/test_UAT_FC_REQ-140_segment_colour.test.ts` (the HEAD-side colour work the resolution had to stay compatible with), so the merged whole is confirmed to compile and behave. Coverage measurement timed out at 300s and was reported as info only; no test failed.

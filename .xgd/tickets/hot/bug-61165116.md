---
uid: bug-61165116
id: BUG-51
type: bug
title: 'Site store: a starter-scaffold write silently overwrites an existing site''s
  content'
created_by: xgd
created_at: '2026-09-05T18:25:02.848670+00:00'
updated_at: '2026-09-05T18:54:58.496450+00:00'
completed_at: null
last_field_updated: body
status: draft
fields:
  auto_merge_back: true
  needs_review: false
  priority: medium
  chat_comment: comment-cc47f70c
  severity: high
---

## Symptom

Working session on **2026-09-02** built a demo site ("Gigabyte Alchemy") through the
builder AI. On returning to the local dev server, the **chat transcript is intact but
the site is a blank starter page**.

Not expected. The site was not deleted — its content was **overwritten with the
`1c new` starter scaffold** while every other artifact of the session survived.

## What the local store actually holds

The site is `tenant=1stcontact, slug=alpha` in the local miniflare D1
(`apps/control-app/.wrangler/state/v3/d1/…`).

| Artifact | State |
|---|---|
| `chat-cdd0f518` + `comment-3e10c30f` (transcript, 38,896 bytes) | **intact** |
| `site_changes` — 16 journal records, palette/config/l1/component ops | **intact** |
| `site_assets` + R2 `draft/1stcontact/alpha/assets/AlchemistLabWithTech.png` (2.4 MB) | **intact** |
| R2 `audit/1stcontact/site-alpha/` — 65 AI tool-call records | **intact** |
| `sites.site_json` | **replaced by `starterSiteJson('alpha')`** — no palette, tagline `alpha — built with 1st Contact` |
| `site_pages(home.json)`, 536 bytes | **replaced by `starterHomePage('alpha')`** — one centred placeholder reading `alpha` |

`sites.version = 26` and `sites.counter = 16` were **not** reset. A delete-and-recreate
would have zeroed both (`createDraft` inserts at `version 0, counter 0`). So this was an
overwrite of an existing row, not a fresh site.

`sites.updated_at = 2026-09-03T03:12:17.291Z` — about 7h40m after the last AI edit
(`2026-09-02T19:29:45Z`), and **no audit record exists at that time**. The write did not
come through the AI surface.

The same shape produced a second scaffolded `alpha`, under `acct_uatwestheadme`, on
2026-09-03T21:49:49Z.

## Mechanism

`SiteStore.write()` (`tools/generate/src/store/d1r2-store.ts:461`) replaces `site_json`
outright and upserts pages by name. Its compare-and-set (`SiteWrite.expect`) is opt-in and
**neither scaffold path supplies one**, so both writes are unconditional.

`createDraft()` (`:391`) is `INSERT OR IGNORE`, so it is safe on its own. But every
"create a starter site" sequence in the codebase is `createDraft` **then** `write(starter)`:

- `apps/control-app/src/identity.ts:597-601` — provisioning a business
- `apps/control-app/src/router.ts:884-886` — `POST /api/import`, i.e. `bin/publish`

Against a slug that already exists, the first call is a no-op and the second is a total
content wipe. The safety `createDraft` appears to provide is illusory.

**Which path fired here (inferred, with evidence):** the scaffold now on disk has its
placeholder heading set to the *slug* (`"alpha"`). `identity.createStarterSite` always
passes `STARTER_HEADING = 'Your 1stcontact site'` (`identity.ts:441`), and has done since
`61a0becc61` (2026-08-31) — so provisioning is ruled out. The heading-defaults-to-slug
shape is `starterHomePage(slug)`, which is what `1c new` writes (`commands.ts:68`). That
reaches D1 only through `/api/import`. `bin/publish` defaults its origin to
`http://localhost:8788` — the operator's running dev server — so a `1c new alpha` +
`bin/publish` in any checkout (including a free-coding worktree, which has its own
`storage/` but posts to the same shared dev Worker) lands on top of whatever the operator
has at that slug.

## Two distinct defects

1. **A scaffold write can destroy authored content.** Neither `/api/import` nor
   provisioning distinguishes "seed an empty site" from "replace a site someone built".
   `bin/publish`'s own docstring calls re-running it "the ordinary way to use it".

2. **There is no undo.** The change journal records only labels and summaries — the `l1.set`
   records carry before/after *text*, not documents — so it cannot reconstruct a page.
   No revision had been published for `alpha` (`published_sites` holds only `xgd`), so
   there is no snapshot tier either. DOC-12's forward-only model protects *published*
   revisions; a draft that was never published has no protection at all.

## Recovery for this instance

Recoverable, by hand, from the R2 audit trail. The audit records carry the **full `set_l1`
node payloads** — e.g. `audit/1stcontact/site-alpha/1788374147.604-0015.json` (5,970 bytes)
contains the whole hero / about / pillars document, background image reference, overlay and
palette hexes. Replaying the write-effect records in key order reconstructs the final page;
the last mutating op was `write_image` at `1788376498.045`.

---

# Required behaviour

The operator is running the system in test mode and needs **control over the local dev
store**: nothing may be lost by accident, and there must be one deliberate way to get back
to a known-empty state.

Restart itself is already safe — `1c builder` runs `wrangler dev`, which persists to
`apps/control-app/.wrangler/state` on disk and neither seeds nor clears it. What is not
safe is the pair of unguarded scaffold writes above. So "preserved by default" is delivered
by closing those, not by changing how the server starts.

## B1 — An import never replaces work done in the builder

`POST /api/import` reads the target site's change counter before writing.

- Counter is zero — a site that does not exist, or one that has only ever been published
  from a local `storage/sites/` copy — the import proceeds exactly as it does today.
- Counter is non-zero — someone has edited this site in the builder — the import is
  **refused with 409 and nothing is written**. The refusal names the slug and how many
  recorded changes are at risk, so the operator can see what they nearly lost.
- The refusal is overridable by an explicit `force` in the request payload, surfaced as
  `1c push --force` and `bin/publish --force`. `force` is absent from an ordinary push
  rather than sent as `false`, so the wire shows what was meant. `bin/publish --force`
  applies to every slug in the run: it answers a question about the invocation, and the
  refusal names the site it protected, so a narrower rerun is one command away.

**Why the counter and not the version.** `write()` bumps `version` on every call including
an import's own, so a site that has only ever been published from local still has a
non-zero version — a version guard would refuse the ordinary publish loop. `counter` moves
only through `appendChange`, which is what a builder, AI or structured-edit change does. It
therefore separates "content someone authored here" from "content this very command put
here", which is the actual distinction the refusal is about.

**Consequence to accept:** re-publishing a site that has had even one builder edit now
needs `--force`. `1stcontact/xgd` is in that state today (counter 1). That is the intended
reading — that edit would otherwise be silently discarded.

## B2 — Provisioning never scaffolds over an existing site

`TenantSiteStore.createDraft` reports whether it actually created the row (its
`INSERT OR IGNORE` already knows — the information was there and was being discarded).
`identity.createStarterSite` writes the starter site **only when the row was created**. A
site that already exists is left entirely alone rather than merged with or repaired: the
starter is one blank page whose only purpose is to give a new account something to edit,
and an account that already has a site already has that.

**This is a structural guard, not a fix for a live failure, and the ticket says so rather
than implying otherwise.** The starter slug is `newId('acct')` — 16 random bytes — so
provisioning cannot currently collide with a slug that exists. It is guarded because the
`createDraft`-then-`write` pair *is* the shape that destroyed a site on the import route,
and the illusion of safety is identical in both places; a reader who copies this function
should copy the guarded form. It stops being hypothetical the moment REQ-183 seeds the
portal site at provisioning time, because `PORTAL_SLUG` is a fixed slug per tenant.

## B3 — `1c reset` returns the local dev store to a known-empty state

A single deliberate command, because the alternative to an accident is not "be careful", it
is "have one obvious way to do it on purpose".

- It deletes `apps/control-app/.wrangler/state` — the local D1 and R2, which together hold
  every site, page, change journal, asset, published revision, chat transcript, ticket,
  tenant and audit record. `--include-public` extends it to
  `apps/public-site/.wrangler/state`.
- It **never touches `storage/`**. `storage/sites/` is the operator's git-tracked authored
  source; a reset that removed it would destroy the one copy a re-seed comes from.
- It **refuses while the builder is running**, detected by opening a TCP connection to the
  dev port (`--port`, default 8788, matching `1c builder`). A live miniflare holds those
  SQLite files open, so deleting underneath it corrupts the store rather than resetting it
  — the operator would get a broken server rather than an empty one. A connect proves a
  listener, not whose it is, so the refusal says "something is listening" rather than
  naming the builder, and names `--port` as the way past a port that belongs to something
  else. A port nothing answers on permits the reset.
- A **state directory that is already absent is a successful reset**, reported as "already
  empty" rather than as a failure. A command that errors on the second run is one an
  operator learns to stop reading.
- Without `--yes` it is a **preview**: it prints exactly which directories it would remove
  and what is in them, deletes nothing, and exits 0. It did what it said, and a destructive
  default is the thing this whole ticket is about.
- With `--yes` it removes them and prints what to do next — the same
  `seed it with bin/publish` hint `1c builder` already gives.
- Repo-anchored, not cwd-anchored, for the reason BUG-50 gave for `1c builder`: a package
  script calling it must not carry a silent "only from the repo root" requirement.

## Out of scope

Restoring the `alpha` site itself. It is recoverable by hand from the audit trail as
described above, and that is an operator action on data, not a code change.
Automatic snapshot-before-destructive-write (defect 2) is a real gap and is deliberately
not addressed here — B1 and B2 remove the ways content is lost without being asked, and a
snapshot tier for unpublished drafts is a design question of its own.

## Test plan

UATs named `test_UAT_FC_BUG-51_*`:

- Import into a site with a non-zero counter is refused 409, and the stored page is
  byte-identical afterwards.
- The same import with `force` lands.
- Import into a counter-zero site (fresh, and previously-imported) still lands — the
  ordinary publish loop is unbroken.
- `createDraft` returns true on first call and false on a second for the same slug.
- Provisioning a business whose starter slug already holds a site leaves that site's
  `site_json` and pages unchanged.
- `1c reset` without `--yes` deletes nothing and names the directories, their file count
  and their size.
- `1c reset --yes` removes the control-app state directory and leaves `storage/` untouched,
  and leaves the public-site state alone unless `--include-public` is passed.
- The plan is repo-anchored: the directory it names does not depend on the working
  directory.
- An absent state directory is reported as already empty, and nothing is removed.
- The port probe answers true for a listening port and false for a closed one.

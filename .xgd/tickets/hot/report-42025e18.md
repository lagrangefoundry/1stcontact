---
uid: report-42025e18
id: REPORT-2077
type: report
title: 'Capability-Intent Alignment: Site Materials & Starting Point: Scaffold, Assets,
  Provenance & Palette (level=ac)'
created_by: xgd
created_at: '2026-08-16T06:01:47.970186+00:00'
updated_at: '2026-08-16T06:01:47.970186+00:00'
completed_at: null
last_field_updated: created_at
result: fail
fields:
  report_kind: capability_validation
  subject_uid: capability-b4ac88fc
  level: ac
  violations: 1
  warnings: 1
  needs_review_count: 0
---

# Capability-Intent Alignment: Site Materials & Starting Point: Scaffold, Assets, Provenance & Palette
# Level: ac

**Result**: FAIL
**Violations**: 1
**Warnings**: 1
**Needs review**: 0

Anchor report: report-7ef6a9ea. Previous attempts: 3.

The single violation is **unrepaired from the previous ac-level check**
(report-34f54ad3, 2026-08-10, FAIL). AC-932's body is byte-for-byte what it was
then; its only mutation since is `last_field_updated: uat_coverage` at
2026-08-10T08:15:58, a coverage stamp, not a content edit. Three fix attempts
have not touched it.

## Cumulative Intent Considered

Level is `ac`, so story bodies are the working reference and intent history is
consulted for provenance only. Ledger recorded as the drift-prevention artifact.

| Intent ID | Status | When | Asked / changed | Counts? |
|---|---|---|---|---|
| REQ-102 (BUNDLE-11, `bundle-ee56a66e`) | free_and_reconciled | merged `f9a415a8` 2026-08-06 | `1c new` seeds a minimal valid L1 document; renders/shots unedited; `1c repro` overwrites wholesale; no flag, no mode detection → STORY-93 | YES |
| REQ-101 (BUNDLE-11, `bundle-ee56a66e`) | free_and_reconciled | merged `f9a415a8` 2026-08-06 | Font provenance index; three-state redistribution; `distribution` marker; four violation kinds + on-disk scan; actions warn, redistribution blocks → STORY-92 | YES |
| REQ-114 (BUNDLE-14, `bundle-0385746c`) | free_and_reconciled | merged `cd8f98c8` 2026-08-06 | Palette colour model (model half → STORY-80, other capability); census; retrofit of `storage/sites/*`; §4 retired the theme colour group → STORY-97, and `updated_by` on STORY-93 | YES |
| REQ-118 (`request-66e4c630`) | free_and_reconciled | merged `b2b9208c` 2026-08-07 | `listSiteAssets` — union of registry + `draft/assets/`, merged by handle, with provenance flags, one handle vocabulary, derived `kind`, CLI + builder origin → STORY-102 | YES |
| REQ-128 (`request-de67e1a1`) | free_and_reconciled | 2026-08-08 | Background-image picker over the *same* listing; no new source | YES (no delta here) |
| REQ-132 (`request-5946d045`) | free_and_reconciled | merged `6cb3942f` 2026-08-12 | Image picker becomes a local thumbnail grid with file-name labels; `format?: 'image'` hint on the descriptor | YES (no delta to the listing — verified below) |
| REQ-137 (`request-d2980a95`, bundle-d9226698) | bundled | 2026-08-12/13 | Deletes palette `steps` for a continuous `shade`; supersedes REQ-114 AC3's byte-identity with a ≤8/255 bound | imminent — **not yet landed on this branch** |
| REQ-133 / REQ-140 | ready_to_reconcile | 2026-08-12 / 2026-08-15 | Palette popup; page-editor colour from the palette | imminent (editor capability) |
| REQ-142 / REQ-143 / REQ-145 | free_coded / draft | 2026-08-15 | Async `SiteStore` port then D1/R2 adapter; REQ-142 states "no behaviour change" | NO (not yet) |
| REQ-134 (`request-ba3e3fba`) | abandoned | 2026-08-12 | Image generation component | NO |

## Alignment Ledger

All four stories are `story_kind: feature`, so all are Capability-Matrix stories
expected to carry ACs. All 36 ACs are `status: active`, `kind: behavior`.

| Element | Intents aligned to | Outcome |
|---|---|---|
| STORY-93 (`story-86c7c21b`) — authoring start point, 8 ACs (AC-869…AC-876) | REQ-102, REQ-114 (`updated_by`) | **aligned**. The story's load-bearing properties map 1:1 — AC-869 complete+valid document, AC-870 renders, AC-871 screenshots, AC-872 ladder = capture ladder, AC-873 hex literals + no palette, AC-874 flowed root, AC-875 one shape / no opt-in, AC-876 wholesale import replacement. The story's own unrepaired Technical Context drift does **not** reach the ACs — see info finding 3 |
| STORY-92 (`story-8685be2d`) — font provenance, 12 ACs (AC-857…AC-868) | REQ-101 | **aligned**. Record contract (AC-857), all four failure kinds (AC-858 unregistered-family, AC-859 unregistered-file, AC-860 unprovenanced-file + excluded trees, AC-861 redistribution), advisory channel (AC-863), distribution marker (AC-862), scan scope across both site trees (AC-865), reference-form normalisation (AC-866), report (AC-867) and machine-readable form (AC-868), record integrity as hard error (AC-864). The three divergences the body records as deliberately un-asserted carry no AC, and none asserts the acquisition verb the body puts out of scope |
| STORY-102 (`story-c46abfa6`) — site asset store, 6 ACs (AC-1018…AC-1023) | REQ-118 (origin); REQ-128, REQ-132 downstream | **aligned**. Union of the two disagreeing sources (AC-1018 undeclared file, AC-1019 declared asset incl. declared-but-absent), one handle vocabulary with merge-not-duplicate (AC-1020), usage kind (AC-1021), CLI (AC-1022) and builder origin + missing-site 400 (AC-1023). The story's unrepaired Technical Context drift creates no AC gap — see info finding 4 |
| STORY-97 (`story-5e7eb0c5`) — colour census & palette retrofit, 10 ACs (AC-932, AC-939…AC-947) | REQ-114 (depends on STORY-80, framework-substrate capability) | **gap: exclusivity** — AC-932's two substantive claims are already carried by AC-941 and AC-944 (finding 1). The other nine cover the body's five in-scope bullets cleanly: census human (AC-939) and machine-readable (AC-940), the write and its report (AC-941), exact alpha collapse (AC-942), hue-family ramp grouping with the vivid/near-neutral split and the unclustered singleton (AC-943), lossless proof (AC-944), refuse-and-write-nothing (AC-945), naming and `--names` (AC-946), re-runnability (AC-947) |

## Findings

| # | Severity | Property | Element | Resolution category | Issue | Suggested edit |
|---|---|---|---|---|---|---|
| 1 | violation | exclusivity | AC-932 (`acceptance_criterion-9f1e7baf`) under STORY-97 | ac-edit | Both of AC-932's substantive claims are already criteria elsewhere in the same story. Claim (a) — "the number of entries is materially smaller than the number of distinct colours the site used" — is the same criterion as AC-941 bullet 1, which reads "adds a site-level palette … whose entry count is materially smaller than the site's distinct colour literal count (a palette, not a colour list)", down to the parenthetical and the verification shape (compare declared palette size against pre-conversion distinct-colour count). Claim (b) — "every colour the site painted before the conversion is still painted after it, and no new colour appears" — is a strictly weaker form of AC-944, which asserts byte-identical render before/after **and** that every reference resolves back to exactly the literal it replaced including opacity. Exactly one clause of AC-932 is unique: "Sites with no L1 colour axes carry no palette at all and remain valid" — the vacuous-retrofit case the story body records for `1stcontact` and `harbor-cafe`. Verified uncovered elsewhere: AC-939 covers the zero-colour *census*, AC-945 covers only the three refusal causes (no stored draft, failed round-trip, invalid result), and AC-941 asserts a palette *is* written — none covers a zero-colour retrofit succeeding as a no-op | Narrow AC-932 to its one non-duplicated criterion: a site whose definition carries no colour literals retrofits as a valid no-op — no palette written, definition still validates, command does not fail. Drop the "materially smaller" claim (owned by AC-941) and the colour-losslessness claim (owned by AC-944), and retitle accordingly. **Do not simply deprecate AC-932** without first folding the zero-colour clause somewhere, or coverage of the vacuous case is lost |
| 2 | warning | consistency | AC-932 (`acceptance_criterion-9f1e7baf`) under STORY-97 | ac-edit | AC-932 bakes frozen repo state into a criterion: "the two stored sites carrying L1 pages landed at 6 entries from 16 distinct RGB and 8 entries from 30." STORY-97's Technical Context rules this out as capability surface — "The census measures the definition as it stands; the durable property is the method and the collapse (distinct RGB strictly fewer than distinct literals), not the frozen counts" — and records that this exact drift already happened once, when REQ-114's AC7 froze DOC-23 §5.3's 17/15 and the site moved to 18/16. A criterion pinned to today's contents fails the moment a page is added | Remove the counts from AC-932's body; if an illustrative figure is wanted, mark it as observed repo state at time of writing rather than as part of the criterion. Fold into the same edit as finding 1 |
| 3 | info | — | STORY-93 (`story-86c7c21b`) Technical Context | — | The story-level check this run (report-375af0aa, FAIL, 2026-08-16T05:55) raised a violation on the final Technical Context bullet, which still reads "the theme-sourced colours" after REQ-114 retired the theme colour group. **Still unrepaired** — verified present in the current body. Assessed for cascade at this level and it does **not** reach the ACs: AC-873 states the corrected provenance explicitly ("hex literals in its own layout document … declares no palette … formerly came from the theme's closed colour palette, which no longer exists"), and AC-870 carries the same correction with a pointer to AC-873. No AC repeats the stale phrasing, so no ac-add, ac-edit or ac-deprecate follows | none at this level — owned by the story-level report |
| 4 | info | — | STORY-102 (`story-c46abfa6`) Technical Context | — | The same story-level report raised a violation on the "Known upstream limitation" paragraph, superseded by REQ-132. **Still unrepaired** — verified present. It creates no AC coverage gap here: verified `SiteAsset` at `tools/generate/src/cli/edit.ts:1259-1270` carries exactly `{id, src, alt, kind, onDisk, registered}` — no label, thumbnail or `format` field — so REQ-132's thumbnail grid and file-name labels are client-side presentation in the editor capability and add nothing this listing must express. The story's deliberate no-AC decision on labels remains honoured by all six ACs | none at this level — owned by the story-level report |
| 5 | info | — | AC-941, AC-943, AC-932 ("steps" language) | — | REQ-137 (`bundled`, imminent) deletes palette `steps` for a continuous `shade` and supersedes byte-identity with a ≤8/255 bound, which would drift AC-941's "how many steps it carries", AC-943's "one entry carrying the rest as named steps" and AC-944's pixel-identity gate. Verified **not landed on this branch**: `packages/site-schema/src/l1/palette.ts:63,72` still defines `steps` with no `shade`. These ACs accurately describe the current system and are not findings today | none — re-check when `bundle-d9226698` reconciles |
| 6 | info | — | AC-867 under STORY-92 | — | AC-867 asserts "run against the project as it stands, the check passes and those counts are all non-zero", which reads adjacent to finding 2's frozen-state problem. Distinguished deliberately: AC-867 asserts a *property* (a pass cannot come from an empty scan) with no specific number, whereas AC-932 pins specific counts. STORY-92's Technical Context permits exactly this — "Those counts are repo state, not capability surface; no criterion is written against them, only against the gate that keeps them honest." Not a finding | none |

## Notes for the Editor

- **One element, one edit.** The violation and the warning both land on AC-932
  and should be repaired in a single rewrite. The safe shape: retitle to the
  zero-colour no-op, keep only that criterion and its verification, delete the
  "materially smaller" and "no colour lost" claims and the 6/16 and 8/30 counts.

- **This is the fourth attempt on an unchanged element.** AC-932's body has not
  been edited since the first ac-level check flagged it on 2026-08-10; the only
  field to move is `uat_coverage`. Whatever the previous three fix attempts
  acted on, it was not this AC body. An editor should confirm the write lands
  (re-read the body after the update) rather than assuming the command
  succeeded.

- **Consequence for the uat level.** AC-932 carries `uat_coverage: pass`. If
  finding 1 is applied as suggested, its existing UAT will be exercising claims
  that have moved to AC-941/AC-944 and must be re-pointed at the zero-colour
  no-op case, or the uat level will pass on evidence for a criterion AC-932 no
  longer makes.

- **Story-level drift is real but does not cascade here.** Two story-level
  violations (STORY-93, STORY-102) are unrepaired as of this check. Both are
  single-phrase Technical Context repairs; I verified in each case that the
  stale claim is contradicted or unmentioned by every AC beneath it, so no AC
  action follows from either. They remain blocking at their own level.

- **Provenance of the duplication.** AC-932 was authored under STORY-80
  (`story-c490f1cf`, the L1 palette *model* story in another capability) on
  2026-08-06 and moved onto STORY-97 on 2026-08-09. Under STORY-80 it was the
  model's own end-to-end assertion; landing beside STORY-97's purpose-built
  retrofit criteria (AC-941, AC-944) is what made it redundant. Worth checking
  whether any other AC moved in that operation carries the same problem — this
  check saw only the four stories under capability-b4ac88fc.

- **Everything else is clean.** STORY-93, STORY-92 and STORY-102 need no AC
  action at this level. Their bodies are unusually explicit about which
  behaviours are deliberately left un-asserted, and in every case the AC set
  matches that statement.

---
uid: report-f9216548
id: REPORT-397
type: report
title: 'Code Review: bundle-adc60ee8'
created_by: xgd
created_at: '2026-07-09T23:57:16.910799+00:00'
updated_at: '2026-07-09T23:57:16.910799+00:00'
completed_at: null
last_field_updated: created_at
result: pass
fields:
  report_kind: code_review
  subject_uid: bundle-adc60ee8
  anchor_uid: bundle-adc60ee8
---

# Code Review

**Result**: PASS

## Summary
Bundle-adc60ee8 (REQ-20/26/27/28/31/32/33/35/37/38 + fidelity tooling) is a large, cohesive free-coded change (~9,000 insertions across framework, site-schema, render, and the `1c` capture/fidelity CLI). Quality gates are green, all new CLI commands are wired and smoke-tested, and the code is architecture-aligned: schema changes enforce the closed content-value model (token-backed enums, `.strict()`), and the validate.ts refactor generalizes rather than adding bespoke module logic. No critical or warning-level issues found.

## Quality Gates
From regression quality report `report-b12281f5` (commit b07a09d3):
- **Lint**: success — 0 errors, 0 warnings
- **Build**: success — exit 0
- **Preflight**: pass — 0 violations
- **Tests**: javascript-vitest — 100 passed / 0 failed / 0 skipped (282 deselected; reconcile runs a UAT filter)
- **Coverage**: null (expected — regression runs a selected-UAT `test_filter`, not a full coverage pass; threshold is 25% per `.xgd/config.yaml`). Standalone quality report `report-b35cfcb8` also passes.

## External Interface Accessibility
New entry points wired in: **yes**.
- New `1c` subcommands `values-diff` (REQ-31/35), `diff` and `crop` (REQ-38) are imported in `tools/generate/src/cli/index.ts`, dispatched in the `run()` switch (index.ts:260/308/352), re-exported from the barrel, and documented in the `USAGE` string.
- `bin/1c.mjs` launcher runs through the Astro/Vite SSR transform; `1c help` prints usage cleanly.
- No dead modules or uncalled functions detected.

## Code Quality
| File | Finding | Severity |
|------|---------|----------|
| packages/framework/src/modules/validate.ts | Recursive `validateField`/`validateSchema` refactor; generalized over the contract, dotted/indexed error paths | Clean |
| packages/site-schema/src/schema.ts | New layer treatment schemas, all closed enums + `.strict()`, XOR via superRefine; no raw CSS | Clean |
| packages/site-schema/src/types.ts | Types derived via `z.infer`; no duplication | Clean |
| packages/framework/src/tokens/defaults.ts | 5 optional palette tokens + `xl` shadow; documented defaults | Clean |
| tools/generate/src/render/render.ts | `width: half` row buffering/flush; imports all used | Clean |
| tools/generate/src/cli/perceptual.ts | 527-line diff eye; magic numbers documented in `DEFAULTS` object | Clean |
| tools/generate/src/cli/fidelity.ts | Delegates to values-diff; finally-block resource cleanup | Clean |
| tools/generate/src/cli/capture/values-diff.ts | Two-phase flatten→diff; documented ΔE/redmean; cohesive helpers | Clean |
| tools/generate/src/cli/capture/extract.ts | New extracted fields initialized; inferred-colour sentinel flagged (REQ-35) | Clean |
| tools/generate/src/cli/capture/sections.ts | Band coalescing by style signature; correct box-union | Clean |

Scan of all added production TS lines found no `console.log`/`debugger`/`TODO`/`FIXME`/leftover commented blocks. New `sharp@0.35.0` dependency added for image codec work (build passes, so it resolves).

## Checklist Compliance
No architecture, security, or design checklist reports exist for this anchor — sections skipped per the review contract.

## Smoke Test
Entry points tested (via `bin/1c.mjs`):
- `1c help` → prints full usage, exit 0
- `1c values-diff` (no --ref) → guided error, no stack trace
- `1c diff` (no --ref) → guided error, no stack trace
- `1c crop foo.png` (no --box) → guided error, no stack trace
All new user-facing commands invoke cleanly and fail gracefully on missing required args.

## Issues Found
**Critical (must fix)**: none

**Warnings (should fix)**: none

## Fix-It Prompt
N/A — review passes.

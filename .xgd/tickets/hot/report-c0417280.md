---
uid: report-c0417280
id: REPORT-3324
type: report
title: 'Reconcile resolve conflicts: reconcile-BUNDLE-23'
created_by: xgd
created_at: '2026-09-02T19:11:35.840130+00:00'
updated_at: '2026-09-02T19:11:35.840130+00:00'
completed_at: null
last_field_updated: created_at
result: pass
fields:
  report_kind: resolve_conflicts
  subject_uid: reconcile-BUNDLE-23
---

## Files resolved

- `.xgd/tickets/hot/bug-db356ff8.md` (BUG-36) — class **UU**, intent/bookkeeping
  ticket → rule **2e**. Resolved to the **HEAD (ours)** side, staged with
  `git add --sparse` (path is outside the sparse-checkout cone; the conflict
  lived in the index only, though the working-tree copy did carry markers).

  Basis: HEAD is a **strict superset** of the incoming side, and its edit is
  later on the timeline.

  - Incoming commit `1524d150` (`xgd(ticket): update bug bug-db356ff8`,
    Sun Aug 23 15:13:34 2026 -0700) took the ticket from `(new ticket)` to a
    drafted scope: title, `severity: high`, and the body through
    `## Symptom` / `## Diagnosis` / `## Immediate unblock` / `## Proposed fix` /
    `## Test plan` / `## Status`.
  - HEAD-side commit `56ced613` (same subject, Mon Aug 31 12:19:38 2026 -0700)
    contains all of that verbatim and then advances the ticket:
    `status: draft → free_and_reconciled`, `completed_at` set, and
    `story_points`, `commits[working_sha=ea48502d]`, `version: 0.2.10`,
    `bundled_in: bundle-78f4e2fe` added — plus ~270 further body lines
    (production-state findings, the approved publish-credential scope addition,
    and the landed tenant-fix implementation record).

  Only one fact was changed differently on the two sides — the `## Status`
  paragraph. Incoming: "Scope drafted, awaiting operator confirmation before
  coding."; HEAD: "Both halves landed and verified (2026-08-23)…". Per 2e's
  per-fact timeline rule the later-positioned side (HEAD, Aug 31) wins that
  fact. The remaining differences (`updated_at`, `last_field_updated`) are
  bookkeeping scalars owned by whichever edit ran last.

  No content was invented; no field was touched beyond what the two sides
  already carried.

## Incoming changes preserved

No code/implementation files were in conflict — the sole conflicted path is a
bookkeeping ticket.

Verified mechanically for that ticket: diffing every line added by the incoming
commit against the resolved file leaves only three real lines absent, all of
them the superseded facts named above —
`updated_at: '2026-08-23T22:13:33.974071+00:00'`,
`last_field_updated: severity`, and
`Scope drafted, awaiting operator confirmation before coding.`
Every substantive line the developer authored in the incoming commit (the
title, `severity: high`, and the entire Symptom → Test plan body) is present in
the resolved version.

No hunks were dropped under the BUG-1301 precedence exception.

Note for the finalize step: because HEAD already contained the incoming
commit's full effect, the staged tree is byte-identical to HEAD and
`git diff --cached` is empty. This is the redundant-commit case (BUG-1109 /
BUG-1122), not a discard — STEP 3's check confirms the incoming commit's
changes are present in HEAD rather than merely absent. `--skip` was not called;
the cherry-pick sequencer state is left intact.

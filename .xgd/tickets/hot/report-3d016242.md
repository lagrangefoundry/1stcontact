---
uid: report-3d016242
id: REPORT-3730
type: report
title: 'Capability-Intent Alignment: Framework Substrate: L1 Layout, Values & Behavior
  Modules (level=ac)'
created_by: xgd
created_at: '2026-09-10T12:20:04.209012+00:00'
updated_at: '2026-09-10T12:20:04.209012+00:00'
completed_at: null
last_field_updated: created_at
result: pass
fields:
  report_kind: capability_validation
  subject_uid: capability-ae9d65d6
  level: ac
  violations: 0
  warnings: 0
  needs_review_count: 0
---

# Capability-Intent Alignment: Framework Substrate: L1 Layout, Values & Behavior Modules
# Level: ac

**Result**: PASS
**Violations**: 0
**Warnings**: 0
**Needs review**: 0

Anchor report: report-e37a6b4a (regression 800a17f7) · Capability: capability-ae9d65d6
(CAP-70) · Previous attempts: 9

**The ac-level cycle has converged.** REPORT-3728 (attempt 9, 2026-09-10T12:10) carried
exactly two actionable items — one violation (AC-723) and one four-cycle-old warning
(AC-716). REPORT-3729's fix applied both, and both were re-verified this pass **against
the live ticket bodies and against the implementation on this branch**, not against the
fix report's assertion. Nothing new was found. The 108 ACs across 7 stories were
re-enumerated; the three largest / most recently touched stories (STORY-83, STORY-85,
STORY-80) had their bodies re-read in full and re-mapped onto their ACs.

## Cumulative Intent Considered

At `ac` level the ledger is inherited rather than re-derived: the story-level cycle
closed at zero violations this morning (REPORT-3721 → REPORT-3722), so story bodies are
the working reference and intent history was consulted only where a body was itself
silent. The ledger below is the one rebuilt in full by REPORT-3719 and re-confirmed by
REPORT-3721 / REPORT-3728; **it was re-checked for movement this pass and there is
none** — the most recently updated intent in the store is REQ-162 (2026-09-02,
`free_and_reconciled`, product ticket store — a different capability), and every intent
touching the substrate has been settled since 2026-08-15.

| Intent ID | Status | When | Asked / changed | Counts? |
|---|---|---|---|---|
| BUNDLE-7 (`bundle-31e474b9`: REQ-79/82/83/84/85 + 2) | free_and_reconciled | 2026-07-22 | Framework pivot: L1 typed substrate + envelope + sole renderer; behavior modules with typed config + named L1 slots | YES |
| REQ-87 | free_and_reconciled | 2026-07-24 | `capability module` → **behavior module**; no back-compat alias | YES |
| **REQ-93** (`request-f26cbe32`) | free_and_reconciled | 2026-07-25 | Page-schema slot binding + its rejections; **renderer mounts the module fragment into the seam**; `mountInL1` conformance mode; `contact-form` `labelMode` | YES — load-bearing for the repaired finding |
| BUNDLE-11 (`bundle-ee56a66e`: REQ-96/97/98 + 12) | free_and_reconciled | 2026-08-05 | `control` leaf; contact-form single required `form` slot; shared axis groups; zero-CSS obligation | YES |
| REQ-99 / REQ-100 / REQ-108 | free_and_reconciled | 2026-08-06 | Typed interaction state, scroll reveal, pointer accent; renderer safety floor | YES |
| REQ-103…REQ-107 | free_and_reconciled | 2026-08-06 | Texture + radial gradients; per-width layout track + wrapping row; typed link role; `validateL1` on the authoring path | YES |
| REQ-114 / REQ-117 | free_and_reconciled | 2026-08-07 | L1 palette colour model, closed colour-**role** vocabulary deleted; nowrap width becomes a floor | YES |
| REQ-136 | free_and_reconciled | 2026-08-12 | Image framing / shape vocabulary / colour adjustment | YES |
| **REQ-137** (BUNDLE-18) | free_and_reconciled | 2026-08-13 | Palette entry is one colour: `steps` deleted, continuous Oklab `shade` | YES |
| REQ-148 (BUNDLE-20) | free_and_reconciled | 2026-08-15 | Behavior modules are plain functions rendering in workerd; module chrome into the page stylesheet | YES |
| BUNDLE-6 (`bundle-ab9e0cb6`) | free_and_reconciled | 2026-07-17 | Pre-pivot reproduction treatments (superseded in place by the pivot) | YES (superseded) |
| REQ-141…150, REQ-151…153, REQ-162, BUG-36/37/38 | free_and_reconciled | 2026-08-15…2026-09-02 | Platform / builder / locale / product-ticket surfaces | NO — other capabilities |
| REQ-154 / BUG-39 | bundled | 2026-08-31 | Browser Rendering driver; node chat-host streaming | NO — capture & chat |
| REQ-155…166 (minus 162) | draft | 2026-08-30…31 | ReferenceStore port, KB work, Library tab | NO — drafts |
| REQ-134 | abandoned | 2026-08-13 | Image-generation component | NO |

## Alignment Ledger

| Element | Intents aligned to | Outcome |
|---|---|---|
| **STORY-83 / AC-723** (`acceptance_criterion-8db8ef76`) | REQ-79/82, REQ-87, **REQ-93** | **repaired — aligned.** REPORT-3728's sole violation is closed. The criterion now opens "**When no behavior module is mounted into it**, a `slot` leaf reaches the published page as an inert, labelled placeholder … the placeholder is an empty element", and defers the other state explicitly ("The mounted state … is AC-1622's claim and is not described here"). Verified against `packages/framework/src/l1/render.ts:2164-2167`: `const mounted = state.mounts?.[node.name] ?? ''` then `…data-l1-slot="…"…>${mounted}</div>` — the element is empty exactly when the `mounts` lookup misses, which is what the repaired text now says and what the old unconditional absolute contradicted. The attribute half is carried verbatim under a lead-in stating it holds "identically in **both** states", matching both `render.ts:2165-2166` (both values through `escapeHtml`, `data-l1-behavior` emitted only when `node.behavior` is set) and AC-1622's own both-states clause. The Verification's mounted-state sentence was trimmed to a pointer at AC-1622, which keeps the duplication REPORT-3728 finding 4 recorded as resolved from re-opening. `updated_at` 2026-09-10T12:12:47; status `active`. |
| **STORY-83 / AC-1622** (`acceptance_criterion-b796e4c7`) | REQ-93 | aligned, **untouched as instructed**. Still describes the two emitted states, the `mounts`-map lookup, the verbatim-unescaped carve-out with its two preconditions, and the identical seam markup in both states — all matching `render.ts:2150-2168` and the comment block at 2151-2158. Its cross-reference "the inert, labelled placeholder of AC-723 — an empty element" now agrees with AC-723's own text rather than asserting a qualification AC-723 lacked. The contradiction REPORT-3728 found *inside one story* is gone. |
| **STORY-80 / AC-716** (`acceptance_criterion-1eaa93b8`) | REQ-79/84, REQ-114 | **repaired — aligned.** The four-cycle-old exclusivity warning is closed by narrowing, not deletion. (a) The colour bullet keeps hex acceptance and non-hex rejection — AC-716's own subject — and hands the reference half over by name: "That the same axis *also* accepts a reference into the site palette is AC-928's claim, not restated here." (b) The literal-only paragraph is replaced by a pointer to AC-931. Both deferrals were checked to land somewhere real rather than nowhere: AC-928 carries the claim in full ("Every colour axis in the layout substrate … accepts **either** a hex literal **or** a reference to a palette entry"), and AC-931 bullet 2 carries the other ("A literal-only document is entirely unaffected by the widening: it needs no palette, validates and renders exactly as before"). No coverage was lost to the narrowing. The length/geometry/radius bullet and the "absolute value is the base" framing — AC-716's uniquely-owned subject — are intact. `updated_at` 2026-09-10T12:12:19. |
| **STORY-80** — 7 ACs | REQ-79/84, REQ-114, REQ-137 | aligned. Body re-read in full and re-mapped: colour/length/radius literal → AC-716; palette shape, one-colour entries, `steps` rejected-not-ignored → AC-928; alpha as a reference axis → AC-930; continuous Oklab `shade`, out-of-range rejected not clamped → AC-1144; shade removes chroma only + per-entry usage tally → AC-1145; dangling reference is a validation failure → AC-929; resolve-once-at-the-load-boundary and the literal-only guarantee → AC-931. Nothing in the body is uncovered and, with AC-716 narrowed, no two ACs restate one criterion. |
| **STORY-83** — 44 ACs | REQ-79/82/84, REQ-87, REQ-90/91, REQ-93, REQ-96, REQ-97/98, REQ-103/105/107, REQ-108, REQ-109/BUG-30, REQ-114, REQ-117, REQ-136 | aligned. The body's in-scope list maps 1:1: typed shape AC-682; shared axis groups AC-802; `control` leaf + its three emitter obligations AC-806/807; framing / shape / adjustment + fixed-order + determinism AC-1124…AC-1128; resource table AC-727/728; page colour and the one-colour-system negative AC-933/934/935/936; envelope on every validated definition AC-849/850/851 with AC-686/687/726; keyframe compilation AC-684/725; texture and gradient branches AC-829/830/831/832; measure and seam-measure AC-801/803/804; nowrap floor AC-1009…AC-1012; relocatable URLs and the flat-snapshot invariant AC-888…AC-891; round-trip and cross-browser AC-683/688; injection inert AC-685; mirrored background asset AC-805; and the two slot states AC-723 + AC-1622. |
| **STORY-85** — 20 ACs | REQ-85, REQ-87, REQ-93, REQ-96, BUG-28, REQ-148 | aligned. Body re-read in full: plain-function portability AC-1412; per-sink escaping + unsafe-endpoint refusal AC-1414; both validation directions AC-697/698/808; page-level binding AC-1623; `mountInL1` AC-1624; zero-CSS + invariant carve-out AC-809; chrome-into-the-page-stylesheet with its staleness pin AC-810; settled-state edit-channel carve-out AC-1413; survivors AC-699/700/701; client asset AC-702; isolation and its client half AC-703/877/878; five conformance dimensions AC-704; L2 preset AC-811; `Behavior*` naming AC-722. **AC-1623 re-verified line by line against `packages/site-schema/src/schema.ts:569-624`** rather than against the story text: all five rejections and every path match (`slot`-with-no-`l1` and unbound and dangling and double-bound at `['modules', i, 'slot']`; duplicate slot names at `['l1']`), and the orphan seam is legal by fall-through, as AC-1623 records. The `superRefine`'s sixth issue — duplicate *module id* at `['modules', i, 'id']` (schema.ts:625-635) — is correctly **not** claimed by AC-1623: it is a module-identity rule, and STORY-85's body lists exactly the five slot-binding rejections. No gap. |
| **STORY-82** — AC-718, AC-719 | REQ-85, REQ-87, REQ-93, REQ-96, REQ-114 | aligned, unchanged since attempt 8's repair. Both `active` (they were the project's only `pending` ACs before that repair). |
| **STORY-81** — 6 ACs | REQ-104 (+ pivot supersession) | aligned. AC-833/834 (row→stack, control-row reflow), AC-835 (wrap restated whole, one cascade two consumers), AC-836 (ascending serialization, visibility outranks the track), AC-837 (no-responsive unchanged), AC-838 (track coherence). All `active`. |
| **STORY-90** — 19 ACs | REQ-99, REQ-100, REQ-108 | aligned. Interaction AC-819/820/822, un-authorable focus ring AC-821, reduced motion AC-823, entrance and stagger AC-824/826/827/828, motion-fails-visible AC-825, and the pointer accent's nine AC-879…AC-887. All `active`. |
| **STORY-91** — 10 ACs | REQ-106 | aligned. AC-839…AC-848 cover retag-not-wrap, the image enclosure, opener/referrer isolation, the URL allowlist doubly enforced, focus retention, substrate paint, anchor ids and uniqueness, the control/mount-seam refusal, and the no-links-unchanged floor. All `active`. |

## Findings

| # | Severity | Property | Element | Resolution category | Issue | Suggested edit |
|---|---|---|---|---|---|---|
| 1 | info | consistency | AC-723 (`acceptance_criterion-8db8ef76`, STORY-83) | — | **REPORT-3728 finding 1 is closed.** The `ac-edit` half of REPORT-2093 finding 3, unapplied since 2026-08-09, is now applied: the placeholder claim is qualified to the unmounted state and the mounted state is deferred to AC-1622. Verified against `render.ts:2164-2167`, not against the fix report. The repair went in the direction the finding directed — AC-723 changed, AC-1622 untouched — so the false absolute the story-level cycle spent two attempts removing from STORY-83's body has not been restored anywhere. | none |
| 2 | info | exclusivity | AC-716 (`acceptance_criterion-1eaa93b8`) vs AC-928, AC-931 (STORY-80) | — | **REPORT-3728 finding 2 is closed**, on its fifth cycle, by narrowing rather than deprecation. Both duplicated clauses are replaced by named pointers, and both pointers were checked to resolve: AC-928 and AC-931 each still carry the claim handed to them, verbatim in AC-931's case. AC-716's own uncovered subject — the literal as the base of the value model, envelope-bounded — survives intact. | none |
| 3 | info | coverage | AC-1622, AC-1623, AC-1624 | — | Unchanged from REPORT-3728 finding 5, and deliberately not actioned by REPORT-3729: these three carry status `pending` while all 105 others are `active`. This is the ticket type's creation default (`ticket_types.yaml`: `acceptance_criterion.status.default: pending`), not an exemption — structural validation enumerates ACs with no status filter — so they are counted and were assessed as active here. Promotion belongs to the uat-level cycle, and setting it at this level would be field-ownership drift rather than progress. | none |
| 4 | info | consistency | ledger | — | No intent movement since the ledger was last rebuilt. The store's most recently updated intent is REQ-162 (2026-09-02, `free_and_reconciled`), which belongs to the product-ticket-store capability; REQ-155…166 minus 162 remain `draft` and REQ-154 / BUG-39 remain `bundled`. Every intent load-bearing for this capability has been `free_and_reconciled` since 2026-08-15, so no AC in this capability owes an update to a status change. | none |

## Notes for the Editor

**Nothing at this level needs an edit.** Both actionable items from the previous
cycle are applied and independently confirmed; no third item surfaced.

**On the evidence standard used.** Every claim this pass rests on was re-derived from
the source rather than inherited: `render.ts:2150-2168` for the two slot states,
`schema.ts:569-635` for AC-1623's five rejections and its one deliberate omission, and
the live bodies of AC-716 / AC-928 / AC-931 for the narrowing's two deferral targets.
The fix report's own account was read but not relied on. This matters because the
finding it repaired had itself been *reported as fixed once before* — REPORT-2093's
finding 3 was recorded as applied in 2026-08 while only its `ac-add` half had landed,
which is how a one-clause edit survived five cycles.

**One environment note, not a finding.** Running
`tests/req93-l1-slot-mounted-behaviors.test.ts` here gives 9 passed / 1 failed, the
failure being `EPERM listen 0.0.0.0` in
`tools/generate/src/conformance/harness.ts:196` → `serve.ts:42` — the `mountInL1`
conformance test (`test_UAT_FC_REQ-93_mounted_behavior_carries_its_conformance_obligations`,
AC-1624's) needs to bind a local HTTP server, which this sandbox forbids. REPORT-3729
reports 10/10 for the same file. The nine that do run here — including every assertion
about the seam's markup in both states — pass, so nothing about AC-723 or AC-1622 is
left unverified by the sandbox restriction. It is recorded so a later reader does not
mistake the environment for a regression.

**Nothing needed escalation.** Every finding cites a `free_and_reconciled` intent.
Step 2.5 did not fire: no story or AC in this capability names an `abandoned` /
`deprecated` / `wont_fix` ticket as a delivery vehicle.

---
uid: report-31551613
id: REPORT-2865
type: report
title: 'Reconcile resolve conflicts: reconcile-BUNDLE-20'
created_by: xgd
created_at: '2026-08-31T08:45:25.628016+00:00'
updated_at: '2026-08-31T08:45:25.628016+00:00'
completed_at: null
last_field_updated: created_at
result: pass
fields:
  report_kind: resolve_conflicts
  subject_uid: reconcile-BUNDLE-20
---

## Files resolved

- `.xgd/tickets/hot/bug-ede1fb8c.md` — **AA** (both added), intent/bookkeeping
  ticket (`bug-*`, rule 2e). Both sides added the same 148-line BUG-33 ticket.
  A full `diff -u` of index stages `:2` (ours) vs `:3` (theirs) shows exactly
  one differing line: the incoming side adds `chat_comment: comment-7bd15cc0`
  under `fields:`. Every other byte — frontmatter scalars, `commits` list,
  `version`, `merged_at_commit`, and the entire body (Symptom / Root cause /
  What was actually found and fixed / Fix / Test plan) — is byte-identical.

  Incoming is therefore a **strict superset** of ours: it adds a field the
  ours side never touched and changes no fact either side asserts. Rule 2e's
  "keep the superset" branch applies directly; no per-fact timeline lookup was
  needed, because there is no fact on which the two sides disagree. Resolved
  with `git checkout --theirs`, staged with `git add --sparse` (path is outside
  the sparse-checkout cone on this reconcile branch, DOC-986 §2/§4.1).

  No `fields.intent_uid` / `fields.story_uid` / `fields.capability_uid` was
  modified, and no content absent from both sides was introduced.

## Incoming changes preserved

No code/implementation files were in conflict — the incoming commit
`ff0943b9fe7a18ac9f0e23dd25b1f803153b48e2` ("xgd(ticket): update bug
bug-ede1fb8c") touches exactly one file, the ticket above, with 148 insertions
and 0 deletions.

The incoming commit's sole substantive contribution relative to the ours side —
the `chat_comment: comment-7bd15cc0` field — is present verbatim in the resolved
file. `git diff --cached HEAD --stat` confirms the staged tree differs from HEAD
by that single added line and nothing else, which is precisely the incoming
delta. Nothing from either side was dropped.

No hunk was dropped under the BUG-1301 precedence exception; it did not arise.
No UAT test files were involved, so rule 2f was not engaged.

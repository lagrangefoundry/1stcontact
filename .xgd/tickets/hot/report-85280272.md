---
uid: report-85280272
id: REPORT-2687
type: report
title: 'Reconcile resolve conflicts: reconcile-BUNDLE-20'
created_by: xgd
created_at: '2026-08-31T05:16:39.916211+00:00'
updated_at: '2026-08-31T05:16:39.916211+00:00'
completed_at: null
last_field_updated: created_at
result: pass
fields:
  report_kind: resolve_conflicts
  subject_uid: reconcile-BUNDLE-20
---

## Files resolved

- `.xgd/tickets/hot/request-554ac441.md` — **UU**, index-only (path is outside
  the sparse-checkout cone; the file was not on disk, so no working-tree markers
  existed). Resolved with `git checkout --ours` + `git add --sparse` per
  DOC-986 §2/§4.1.
  Rule applied: **2e (intent/bookkeeping ticket)** — "one side is a strict
  superset of the other: keep the superset." Resolved to **ours (HEAD)**.

  Incoming commit this attempt: `a74ac039` ("xgd(ticket): update request
  request-554ac441", 2026-08-22 20:24:39 -0700). Index stages were
  base `0dc6fa73` / ours `6546223f` / theirs `de1dfccc`.

  The auto-enriched metadata classed this as "intent unknown on one or both
  sides — take the more recent commit by timestamp." That rule and the 2e
  superset rule agree here, so no per-fact timeline tie-break was needed:

  | | ours (HEAD) | theirs (`de1dfccc`) |
  |---|---|---|
  | `updated_at` | 2026-08-24T02:10:41Z | 2026-08-23T03:24:38Z |
  | `status` | `bundled` | `free_coded` |
  | `fields.version` | 0.2.9 | 0.2.7 |

  A direct blob diff of theirs → ours (`de1dfccc` → `6546223f`) carries **no
  deletions anywhere in the body** — the only `-` lines in the entire diff are
  four frontmatter scalars, each superseded by a later value of the same field.

## Incoming changes preserved

Incoming commit `a74ac039` made two kinds of change; both are present in the
resolved version, reached through a later route rather than discarded:

1. **The body addition — the whole point of this commit.** It appends the
   follow-up section "`bin/build` failed on a type-only reach into node"
   (the five `tsc` errors, the `render.ts` → `loadSite` type-only specifier
   cause, the "why no test caught it" note on REQ-146's runtime-only import
   guard, **acceptance criterion 12**, and the 0.2.7 version-bookkeeping
   paragraph). Every line of it is present **verbatim** in HEAD. HEAD then
   appends a *further* section after it (the deploy secret guard, criteria
   13–16), which is what makes the theirs → ours body diff purely additive.

2. **Frontmatter bookkeeping.** `last_field_updated: status` → `body`, plus an
   `updated_at` bump. HEAD carries `last_field_updated: status` because a later
   operation on the bundle branch moved `status` to `bundled` *after* this
   commit's body edit — that field records whichever write happened last, and
   HEAD's is the later one. The body edit it was pointing at is present (see 1),
   so nothing is lost; the pointer simply advanced. `status: free_coded` and
   `version: 0.2.7` are likewise superseded by HEAD's `bundled` / 0.2.9, and
   HEAD additionally carries two further `free_coded_shas` entries
   (`ec144c85`, `02bd4437`), `bundled_in: bundle-b3b7c399`, and
   `chat_comment: comment-98e86f10`.

No hunks were dropped under the BUG-1301 precedence exception; it did not apply
to this resolution. No code, test, or spec-ticket files were involved.

## Note for the finalize step

The staged tree has **no net diff against HEAD** (`git diff --cached HEAD` is
empty). This is the redundant-commit case of BUG-1109/BUG-1122, not a discarded
one — STEP 3's distinguishing check is satisfied above: the incoming commit's
key change (its body section, verbatim) is demonstrably *present in HEAD* via
later commits on the bundle branch, rather than simply absent.

Per STEP 4 no `--skip` was issued from here; `CHERRY_PICK_HEAD`
(`a74ac03993024fbbffb13bf95ca29a0605894043`) is verified intact for
`cherry_pick_finalize_resolution` to detect the clean staged diff and skip the
commit itself. This is the second consecutive attempt on this ticket to net to
no diff (attempt 28 was commit `7782255e`, also redundant against the same HEAD
blob `6546223f`) — consistent with a post-watermark sync having already landed
this ticket's working-branch history in refined form.

---
uid: report-779087f0
id: REPORT-1919
type: report
title: Claude fix_reconciliation_review report
created_by: xgd
created_at: '2026-08-12T22:29:31.827119+00:00'
updated_at: '2026-08-12T22:29:31.827119+00:00'
completed_at: null
last_field_updated: created_at
fields:
  report_kind: fix_reconciliation_review
  subject_uid: request-8a132869
---

The fix is complete in the working tree. REPORT-1917 (`report-63d4d81d`) is the only reconciliation_review for this anchor — this was the first fix iteration, not a re-fix, so there was no prior attempt to build on.

The review's failure fell into neither of the two standard categories. It reported **"Ungrounded Stories: None"** and confirmed all four plan items landed — the four new ACs, fourteen of them, were praised as above-bar. The failure was narrower: four ACs were **broadened** to claim image framing while the UAT each links to still exercised the old, narrower subject. Each would have passed unchanged if the newly-claimed behaviour were deleted. The review's own Remediation section prescribed the fix explicitly — mirror assertions that already exist in the FC suite — and specifically warned *against* narrowing the AC texts ("Do **not** weaken the clause instead — it is a DOC-2 claim and should gain evidence, not lose the claim"). I followed that.

**Stories created**: none — no uncovered behavior; the review's behavior inventory found all 31 behaviours documented.

**Stories modified**: none — no ungrounded claims. All four ACs' texts are correct as written and were left intact.

**Stories deleted**: none.

**Code changes** (test files only; no production code — the behaviour was already correct):

| File | Change |
|---|---|
| `tests/reconciliation-copy-edit-image-selection.test.ts` | **AC-1027**: added a framing + shape + colour save between `assetFingerprint` calls — no file touched, none added, handle unchanged, unnamed parameters surviving; dropped the stale "eventual home of framing" comment the commit had made false. **AC-1024**: added the closed-control sweep (every field after `src`/`alt` is a bounded integer carrying both bounds or a non-empty enum; none free-form), with a both-shapes-seen guard against vacuity. Added `min`/`max` to the local `Field` interface. |
| `tests/reconciliation-copy-edit-image-framing.test.ts` | New `test_UAT_AC1121_a_pictures_bounds_bind_a_change_and_never_the_status_quo` and `test_UAT_AC1122_a_framing_edit_writes_among_the_parameters_the_picture_already_carries`, mirroring FC tests 7 and 6 onto the file that already carries the image fixtures. |
| `tests/reconciliation-l1-language.test.ts`, `-shared-axis-groups.test.ts`, `-fold-full-language.test.ts` | The optional tidy: pointer comments on AC-725 / AC-802 / AC-729 naming the sibling AC-named UAT that carries each one's new evidence. |

I left `uat_coverage` alone on AC-1121/AC-1122. Those two lack the field while AC-1024/AC-1027 carry `pass`, and the review didn't flag it — it looks evaluator-owned, so setting it by hand would fabricate a signal rather than record one.

**Confidence: high** that the coverage failure is closed. I verified by mutation rather than by a green run: with `imageFramingFields` stubbed to return nothing, all four tests fail — previously each passed unchanged, which was precisely the complaint. Reverted after. All 39 tests across the six touched suites pass; the 13 full-suite failures in `test_UAT_FC_REQ-127_session_binding`, `reconciliation-assistant-conversation` and `test_UAT_FC_REQ-122_chat_host` reproduce identically on a stashed clean tree and are pre-existing. (These runs predate this turn's instruction not to run tests.)

Two caveats on the next review, both outside what this fix can reach:

- The review flagged the pipeline's own gates as **vacuous** — three scoped-quality reports read `pass (0 tests, 0 failed)` with an empty suites map, and both naming checks reported "no test files modified since coding_red" despite four new suites on this branch. That's why seven un-evidenced AC modifications reached review unflagged. It's a workflow-scoping defect, so it survives this fix and could let the same class of gap through again.
- The ticket-store index appears stale in this worktree: `AC-1124` and other ACs created during this run aren't resolvable by ID (UID lookup works). Harmless for my changes, which link by test-name convention, but worth knowing if a later step queries the matrix by ID.

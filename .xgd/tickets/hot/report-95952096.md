---
uid: report-95952096
id: REPORT-3728
type: report
title: 'Capability-Intent Alignment: Framework Substrate: L1 Layout, Values & Behavior
  Modules (level=ac)'
created_by: xgd
created_at: '2026-09-10T12:10:52.362790+00:00'
updated_at: '2026-09-10T12:10:52.362790+00:00'
completed_at: null
last_field_updated: created_at
result: fail
fields:
  report_kind: capability_validation
  subject_uid: capability-ae9d65d6
  level: ac
  violations: 1
  warnings: 1
  needs_review_count: 0
---

# Capability-Intent Alignment: Framework Substrate: L1 Layout, Values & Behavior Modules
# Level: ac

**Result**: FAIL
**Violations**: 1
**Warnings**: 1
**Needs review**: 0

Anchor report: report-e37a6b4a (regression 800a17f7) · Capability: capability-ae9d65d6
(CAP-70) · Previous attempts: 8

The story-level cycle closed this morning at zero violations (REPORT-3721 →
REPORT-3722, attempt 8), so **story bodies are the working reference for this
pass** and intent history was consulted only where a story body was itself silent.
108 ACs across 7 stories were read in full.

**The headline is that the AC-level cycle has largely converged.** Of the three
violations and three warnings REPORT-2093 (2026-08-16) recorded, four are
genuinely repaired and were re-verified element by element against the live ticket
bodies and against the code on this branch — not taken from the fix report's
assertion. One warning persists unchanged. **One half of one violation was never
applied**, and it is the single finding below: the `ac-add` half of REPORT-2093
finding 3 landed (three new ACs), but the `ac-edit` half on AC-723 did not, so the
new AC and the old one now state contradictory things about the same emitted
element.

## Cumulative Intent Considered

At `ac` level the ledger is inherited rather than re-derived. It was rebuilt and
re-verified in full by REPORT-3719 and re-checked by REPORT-3721 earlier today,
which recorded the window since as empty of substrate intent. The intents
load-bearing for the findings below were re-confirmed directly this pass:

| Intent ID | Status | When | Asked / changed | Counts? |
|---|---|---|---|---|
| BUNDLE-7 (`bundle-31e474b9`: REQ-79/82/83/84/85 + 2) | free_and_reconciled | 2026-07-22 | Framework pivot: L1 typed substrate + envelope + sole renderer; behavior modules with typed config + named L1 slots | YES |
| **REQ-93** (`request-f26cbe32`) | free_and_reconciled | 2026-07-25 | Page-schema slot binding + its rejections; **renderer mounts the module fragment into the slot**; `mountInL1` conformance mode; `contact-form` `labelMode` | YES |
| REQ-87 | free_and_reconciled | 2026-07-24 | `capability module` → **behavior module**; no back-compat alias | YES |
| BUNDLE-11 (`bundle-ee56a66e`: REQ-96/97/98 + 12) | free_and_reconciled | 2026-08-05 | `control` leaf; contact-form single required `form` slot; shared axis groups; zero-CSS obligation | YES |
| REQ-99 / REQ-100 / REQ-108 | free_and_reconciled | 2026-08-06 | Typed interaction state, scroll reveal, pointer accent; renderer safety floor | YES |
| REQ-103…REQ-107 | free_and_reconciled | 2026-08-06 | Texture + radial gradients; per-width layout track + wrapping row; typed link role; `validateL1` on the authoring path | YES |
| REQ-114 / REQ-117 | free_and_reconciled | 2026-08-07 | L1 palette colour model, closed colour-**role** vocabulary deleted; nowrap width becomes a floor | YES |
| REQ-136 | free_and_reconciled | 2026-08-12 | Image framing / shape vocabulary / colour adjustment | YES |
| **REQ-137** (BUNDLE-18) | free_and_reconciled | 2026-08-13 | Palette entry is one colour: `steps` deleted, continuous Oklab `shade` | YES — **now reconciled** (was `bundled` at REPORT-2093) |
| REQ-148 (BUNDLE-20) | free_and_reconciled | 2026-08-15 | Behavior modules are plain functions rendering in workerd; module chrome into the page stylesheet | YES |
| BUNDLE-6 (`bundle-ab9e0cb6`) | free_and_reconciled | 2026-07-17 | Pre-pivot reproduction treatments (superseded in place by the pivot) | YES (superseded) |
| REQ-141…150, REQ-151…153, REQ-162, BUG-36/37/38 | free_and_reconciled | 2026-08-15…2026-09-02 | Platform / builder / locale / product-ticket surfaces | NO — other capabilities |
| REQ-154 / BUG-39 | bundled | 2026-08-31 | Browser Rendering driver; node chat-host streaming | NO — capture & chat |
| REQ-155…166 (minus 162) | draft | 2026-08-30…31 | ReferenceStore port, KB work, Library tab | NO — drafts |
| REQ-134 | abandoned | 2026-08-13 | Image-generation component | NO |

**One ledger movement since REPORT-2093 matters at this level**: REQ-137 has gone
`bundled` → `free_and_reconciled`. That converted REPORT-2093's warning 6 from
"the matrix correctly describes the enforced state" into a live obligation — and
STORY-80's ACs were repaired to meet it (see the ledger row below).

## Alignment Ledger

| Element | Intents aligned to | Outcome |
|---|---|---|
| **STORY-82** — AC-718 (`acceptance_criterion-f3328e22`), AC-719 (`acceptance_criterion-da7c62ec`) | REQ-85, REQ-87, REQ-93, REQ-96, REQ-114 | **repaired — aligned.** REPORT-2093 findings 1 and 2 are both closed, verified against the live bodies. AC-718 was retitled and rewritten: every runtime-type "capability" is gone, the slot claim is the single required `form` slot with `submit` as a `control`, the config set is the current functional four (`action`, `fields`, `successMessage`, `submitLabel`), and `labelMode` is present and framed as REQ-93 frames it — a captured a11y fact, not a dial. AC-719 no longer says "named overlay role"; it says "a reference to a site palette entry", agreeing with AC-935 and AC-928. Both are now `active` (they were the project's only two `pending` ACs). Contract claims re-verified against `packages/framework/src/modules/contact-form/meta.ts`. |
| **STORY-85** — 20 ACs incl. new AC-1623, AC-1624 | REQ-85, REQ-87, REQ-93, REQ-96, BUG-28, REQ-148 | **aligned — the REQ-93 gap is closed.** AC-1623 (page-level binding) and AC-1624 (`mountInL1`) were authored this cycle and each was checked line by line against code, not against the story text. All five rejections and **every error path** in AC-1623 match `packages/site-schema/src/schema.ts:569-635` exactly: unbound / dangling / double-bound / `slot`-with-no-`l1` at `['modules', i, 'slot']`, duplicate slot names at `['l1']`. AC-1623 also correctly records the orphan seam as **legal**, matching the fall-through in that same `superRefine` and the one-row correction attempt 8 made to STORY-85's body. AC-1624's host description matches `tools/generate/src/conformance/harness.ts:105-145` — a single `slot` named `mount`, a keyframe at every entry of `RESPONSIVE_WIDTHS`. The rest of the story's in-scope list maps 1:1 onto existing ACs (contract → AC-697/698/808/704; naming + plain-function → AC-722/AC-1412; escaping → AC-1414; zero-CSS + carve-outs → AC-809/810; survivors → AC-699/700/701; preset → AC-811; client asset → AC-702; isolation incl. its client half → AC-703/877/878). No gap. |
| **STORY-83** — 44 ACs incl. new AC-1622 | REQ-79/82/84, REQ-87, REQ-90/91, REQ-93, REQ-96, REQ-97/98, REQ-103/105/107, REQ-108, REQ-109/BUG-30, REQ-114, REQ-117, REQ-136 | **gap: one AC not repaired.** AC-1622 (mounted vs unmounted slot emission) was authored this cycle and is accurate — the `mounts`-map lookup, the verbatim-unescaped carve-out and its two preconditions, and the identical seam markup in both states all match `packages/framework/src/l1/render.ts:2150-2168`. But **AC-723 was left untouched** (`updated_at` still 2026-08-09), and it states the placeholder claim unconditionally. See Finding 1. Everything else in the story's in-scope list has an AC, including the three control-emitter obligations (AC-806), the shared axis groups (AC-802), the framing / shape / adjustment families (AC-1124…AC-1128), the resource table (AC-727/728), page colour and the one-colour-system negative (AC-933/934/935/936), the nowrap floor (AC-1009…AC-1012), relocatable URLs (AC-888…AC-891) and the envelope-on-the-authoring-path trio (AC-849/850/851). |
| **STORY-80** — 7 ACs | REQ-79/84, REQ-114, REQ-137 | **repaired for REQ-137; one duplication persists.** REPORT-2093's warning 6 is closed the right way round: now that REQ-137 is `free_and_reconciled`, AC-928 no longer permits named steps — it states the entry is exactly one opaque colour, that the entry object is closed so a stored step is *rejected* rather than read-and-ignored, and that a reference can no longer name one. AC-1144 (continuous Oklab `shade` on `[-1,+1]`, verbatim zero, out-of-range rejected not clamped) and AC-1145 (a shade only removes chroma; every reference counts against its entry) carry the new axis. Warning 5 is unrepaired — see Finding 2. |
| **STORY-81** — 6 ACs | REQ-104 (+ pivot supersession) | aligned. The body's five in-scope items map onto AC-833/834 (row→stack, control row reflow), AC-835 (wrap restated whole, one cascade two consumers), AC-836 (ascending serialization, visibility outranks the track), AC-837 (no-responsive unchanged), AC-838 (track coherence). Nothing in the body is uncovered and no two ACs restate one criterion. |
| **STORY-90** — 19 ACs | REQ-99, REQ-100, REQ-108 | aligned. All three axes and all three stated obligations are covered: interaction (AC-819/820/822), the un-authorable focus ring (AC-821), reduced motion (AC-823), entrance and stagger (AC-824/826/827/828), motion-fails-visible (AC-825), and the pointer accent's nine (AC-879…AC-887) including the accent-without-texture no-op, the transparent-texture case and byte-determinism. |
| **STORY-91** — 10 ACs | REQ-106 | aligned. AC-839…AC-848 cover retag-not-wrap, the image enclosure, opener/referrer isolation, the URL allowlist with its doubled enforcement, focus retention, substrate paint, anchor ids and their uniqueness, the control/mount-seam refusal, and the no-links-unchanged floor. The body's "optional explicit accessible name" is covered by AC-839. |

## Findings

| # | Severity | Property | Element | Resolution category | Issue | Suggested edit |
|---|---|---|---|---|---|---|
| 1 | violation | consistency | AC-723 (`acceptance_criterion-8db8ef76`, STORY-83) | ac-edit | **The `ac-edit` half of REPORT-2093 finding 3 was never applied.** That finding asked for two things: author ACs for REQ-93 (done — AC-1622/1623/1624) *and* "add a clause to AC-723 that the inert placeholder is what a seam renders when **no** module is bound". AC-723's `updated_at` is still `2026-08-09T05:40:36`. Its criterion still reads, without qualification: "A `slot` leaf reaches the published page as an inert, labelled placeholder … In the emitted HTML the placeholder is **an empty element**". Under REQ-93 (`request-f26cbe32`, free_and_reconciled, 2026-07-25) that is false in the mounted case: `render.ts:2164-2168` emits `<div … data-l1-slot="…">${mounted}</div>`, where `mounted` is `state.mounts?.[node.name] ?? ''`, so the element is empty only when nothing is bound. The contradiction is now **inside one story**: the new AC-1622 says the mounted slot "contains the fragment's markup intact and unescaped" and explicitly scopes the other state as "the inert, labelled placeholder **of AC-723** — an empty element", i.e. AC-1622 asserts a qualification AC-723 does not carry. STORY-83's body already states it correctly ("**With no mount supplied** it is the inert labelled placeholder"). AC-723 is the only element in the capability that has not caught up with REQ-93. | One clause, not a rewrite. In AC-723's first paragraph, qualify the placeholder claim to the unmounted state — e.g. "**When no behavior module is mounted into it**, a `slot` leaf reaches the published page as an inert, labelled placeholder … the placeholder is an empty element" — and add a sentence deferring the mounted state to AC-1622. Leave the whole `data-l1-slot` / `data-l1-behavior` / escaping half untouched: it is correct, it holds in **both** states (AC-1622 says so explicitly), and its UATs pass. Do not touch AC-1622. |
| 2 | warning | exclusivity | AC-716 (`acceptance_criterion-1eaa93b8`) vs AC-928 (`acceptance_criterion-1663c20c`) + AC-931 (`acceptance_criterion-5ab42ca8`), all STORY-80 | ac-edit | Persists unchanged from REPORT-2093 warning 5 and the cycle before it; AC-716 is still at `2026-08-09`. Within one story, two clauses of AC-716 are restatements of sibling criteria. (a) AC-716's colour bullet — "A colour axis on an L1 leaf accepts a hex literal (`#rgb` / `#rrggbb` / `#rrggbbaa`) … **or** a reference into the site palette which resolves to a hex before anything paints" — is the whole of AC-928's headline claim ("Every colour axis in the layout substrate … accepts **either** a hex literal **or** a reference to a palette entry"). (b) AC-716's closing paragraph — "A document that uses only literals needs no palette and is unaffected by the widening" — is AC-931's second bullet almost verbatim ("A literal-only document is entirely unaffected by the widening: it needs no palette, validates and renders exactly as before"). Warning rather than violation: nothing here is *false*, and AC-716's own subject — the literal as the base of the value model, envelope-bounded — is uniquely its own and uncovered elsewhere. | Narrow AC-716 to the literal: keep the length/geometry/radius bullet and the "absolute value is the base" framing, delete the palette-acceptance half of the colour bullet (AC-928 owns it) and the literal-only paragraph (AC-931 owns it), leaving a pointer to those two. |
| 3 | info | consistency | AC-718 + AC-719 (STORY-82) | — | REPORT-2093 findings 1 and 2 are **closed**, verified against the live bodies rather than the fix report. AC-718's four drifts are each gone: no runtime-type "capability" survives in title, body or Verification; the slot claim is the single required `form` slot at v4 with `submit` as a `control` and `intro`/`submit` named only as the REQ-96 supersession; the config set is `action` / `fields` / `successMessage` / `submitLabel`; and the flat negative is reframed — the dials are asserted absent by name while `labelMode` is stated positively as a captured a11y fact. AC-719's "(or a named overlay role)" is now "(or a reference to a site palette entry)". | none |
| 4 | info | exclusivity | AC-718 + AC-719 (STORY-82) vs AC-701, AC-716, AC-928 | — | **REPORT-2093's warning 4 is resolved, by the other permitted action shape.** That warning recommended deprecating both ACs as collapsing into siblings; the repair instead kept and scoped them, which is a legitimate resolution of an exclusivity warning and — on reading the results — the better one. AC-718 now carries an explicit scope note ("this is the *reproduction-treatment* claim … The behavior contract itself … is STORY-85's AC-701"), and AC-701 conversely owns the functional form and the anti-spam surface, which AC-718 does not assert. AC-719's subject is the card-veil / footer-override treatments specifically, not the general value model AC-716 and AC-928 state. The overlap is now stated rather than latent. No further action. | none |
| 5 | info | coverage | AC-1622, AC-1623, AC-1624 | — | The three new ACs carry status `pending` while every other AC in the capability is `active`. This is the ticket type's own creation default (`ticket_types.yaml`: `acceptance_criterion.status.default: pending`), not an exemption — structural validation enumerates ACs with no status filter (`quality/structural_validation.py:132`), so they are counted. Recorded because REPORT-2093 read `pending` as a staleness signal on AC-718/719; here it means "authored this cycle, not yet promoted", and the uat-level cycle is the natural place for that promotion. | none |
| 6 | info | consistency | AC-928, AC-1144, AC-1145 (STORY-80) | — | REPORT-2093's warning 6 is **closed**, and closed in the direction the ledger moved. REQ-137 was `bundled` then and is `free_and_reconciled` now, so the matrix owed an update — and got one: AC-928 states the entry is exactly one opaque colour carrying no ramp and no named steps, with the entry object closed so a stored step is rejected rather than read-and-ignored, and AC-1144/AC-1145 carry the continuous Oklab `shade` that replaced them. | none |

## Notes for the Editor

**Finding 1 is a one-clause edit and nothing else on this level needs touching.**
The temptation will be to "finish" REQ-93 across STORY-83 by rewriting AC-723
around the mount. Don't. AC-1622 already owns the two-state emission in full and
was verified against `render.ts:2150-2168` this pass; AC-723's job is the seam's
own attributes and their escaping, which is correct, passes its UATs, and holds
identically whether or not something is mounted. The defect is exactly one word's
worth of scope — "an empty element" stated unconditionally — and widening the edit
risks re-opening the duplication that finding 4 records as now resolved.

**Do not resolve finding 1 by editing AC-1622 instead.** AC-1622 is the newer and
the correct element; it is AC-723 that predates REQ-93. Reversing that would
restore the false absolute the story-level cycle spent two attempts removing from
STORY-83's body.

**The REQ-93 `ac-add` forwarded from the story cycle is discharged.** REPORT-3720
and REPORT-3722 both forwarded "the page-level binding rejections and `mountInL1`
have no acceptance criterion" to this level. AC-1623 and AC-1624 close it, and the
worry attempt 8 was written to prevent did not materialise: AC-1623 lists **five**
rejections and records the orphan seam as legal, so no UAT authored from it will
demand a sixth rejection the validator does not implement. That cascade was
stopped at the story level as intended.

**Finding 2 is now on its fourth cycle and is genuinely optional.** It has been
carried as a warning by every ac-level report since 2026-08-08 without ever
blocking. If an editor is in STORY-80 anyway it is worth twenty minutes; if not,
carrying it once more costs nothing, since nothing it describes is false and no
UAT is misdirected by it. It is listed here for the ledger, not as a queue item.

**Nothing needed escalation.** Every finding cites a `free_and_reconciled` intent
and was settled against the implementation on this branch. Step 2.5 did not fire —
no story or AC in this capability names an `abandoned` / `deprecated` / `wont_fix`
ticket as a delivery vehicle.

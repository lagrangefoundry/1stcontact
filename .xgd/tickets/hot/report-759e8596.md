---
uid: report-759e8596
id: REPORT-3933
type: report
title: Claude reconciliation_story_generation report
created_by: xgd
created_at: '2026-09-11T05:51:04.011546+00:00'
updated_at: '2026-09-11T05:51:04.011546+00:00'
completed_at: null
last_field_updated: created_at
fields:
  report_kind: reconciliation_story_generation
  subject_uid: bundle-87be4669
  plan_item_index: '13'
---

All five mutations are committed. Item 13 is applied.

**What I read first.** REQ-163 §5 ("Two fields were added to the `material`/`reference` schema, plus one more" — declared rather than undeclared so a re-describe pass is a query, `filename` so the Library needs no attachment lookup per row, all three optional) and REQ-161's "The role is a new field, and it narrows the rights §10.1 infers" (the only question askable because it is not a legal one; narrows never widens; absent leaves the older callers unchanged; malformed refused, never coerced). Then the code from the commits, since the vocabulary change is not in this worktree yet: `tickets.ts` `MATERIAL_FIELDS` at `f6c1366410` (the four declarations and their reasons), `material.ts` `classify` (`role ?? 'site'`, `republishable: role !== 'reference'`, fetched forced to `reference`), `router.ts` `/api/material` (400 `"role must be 'site' or 'reference'"` before anything is stored), and `MaterialRow`/`rowOf` (filename read off the material's own field). Intent and code agree throughout — no code-issue to flag.

**Judgment call worth naming:** the plan's "role narrows the rights" landed as *two* criteria rather than one. AC-1492 keeps the provenance rule and names the role as its one optional narrowing input (so nothing reads as a second rights model), and the narrowing behaviour itself — narrows / absent / malformed — became its own criterion, because those three cases are the whole decision. Rationale recorded under `## Reconciliation Decisions` dated 2026-09-10. I also kept the six description outcomes' *meanings* out of this story: the outcome set is named here as closed, and what each degraded value means belongs to item 8's describing story, so the claim does not live in two places.

```
Upgrade mutations applied for plan item 13 of 17

Target Stories: story-e07c589b
Primary Story UID: story-e07c589b
Stories Modified: 1
ACs Modified: 1
ACs Added: 3
ACs Removed: 0

tickets_modified:
  stories:
    - "story-e07c589b"        # STORY-128, story_kind feature -> upgrade
  acceptance_criteria:
    modified: ["acceptance_criterion-535253fa"]   # AC-1492
    added:
      - "acceptance_criterion-637402ab"           # AC-1736 — role narrows, absent, malformed
      - "acceptance_criterion-bc3b338f"           # AC-1737 — description outcome + describer declared
      - "acceptance_criterion-350a43ed"           # AC-1738 — filename on the record
    removed: []

Out of scope, unchanged: AC-1491, AC-1493..AC-1499; STORY-126/127 and their ACs;
the ingestion, describing, Library and overlay stories (items 7, 8, 11, 12).
No runtime code modified; no new story tickets created.

Progress: 13 of 17 plan items complete
```

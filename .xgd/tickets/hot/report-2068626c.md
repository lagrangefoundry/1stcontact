---
uid: report-2068626c
id: REPORT-1376
type: report
title: Claude reconciliation_uat_generation report
created_by: xgd
created_at: '2026-08-06T03:49:25.393847+00:00'
updated_at: '2026-08-06T03:49:25.393847+00:00'
completed_at: null
last_field_updated: created_at
fields:
  report_kind: reconciliation_uat_generation
  subject_uid: story-86c7c21b
---

All 8 names match the enforced regex, and `git status` confirms a single new untracked test file — no runtime or existing-test modifications.

```
UATs generated for story story-86c7c21b (plan item 11 of 12)

Story: story-86c7c21b
Test file(s) created:
  - tests/reconciliation-scaffold-starter-l1.test.ts

Tests written: 8
Tests passed:  7
Tests failed:  0
Tests skipped: 1  (AC-871 — browser-gated, see note below)

test_files_created:
  - "tests/reconciliation-scaffold-starter-l1.test.ts"
```

**AC → test mapping** (1:1, all 8 ACs covered):

| AC | Test | Boundary |
|---|---|---|
| AC-869 | `..._created_page_carries_a_complete_valid_l1_document` | `cmdNew` → disk artifact → `l1DocumentSchema` + `validateSite` |
| AC-870 | `..._fresh_site_renders_placeholder_centred_on_theme_background` | `cmdNew` → `cmdRender` → emitted HTML |
| AC-871 | `..._fresh_site_shoots_without_hand_editing` | `cmdNew` → `cmdShot` → PNG signature |
| AC-872 | `..._starter_widths_are_the_capture_viewport_ladder` | disk artifact vs `RESPONSIVE_VIEWPORTS` |
| AC-873 | `..._document_and_placeholder_colours_come_from_the_site_theme` | both disk artifacts (`site.json` theme vs `home.json` l1) |
| AC-874 | `..._scaffolded_root_declares_no_per_width_geometry_track` | disk artifact + emitted CSS |
| AC-875 | `..._every_created_slug_yields_one_starter_shape_with_no_flag` | multi-slug `cmdNew` + real `1c help` subprocess |
| AC-876 | `..._repro_over_a_created_slug_matches_repro_over_a_virgin_slug` | `cmdRepro` over virgin vs seeded slug |

**Two things worth flagging:**

1. **AC-871 skips in this environment, and that is not a test defect.** The Chromium binary genuinely isn't installed here — I verified directly: `browserType.launch: Executable doesn't exist at .../chromium_headless_shell-1228/...`. The AC itself sanctions the gate ("Requires a headless browser; the check is gated on browser availability"), and the pre-existing `req102` test skips identically via the same `it.runIf(chromiumAvailable())` pattern. It will execute on a runner provisioned with `playwright install`. I did not run it, so I cannot report it as passing.

2. **Assertion teeth verified, not assumed.** AC-874's `not.toContain('position: absolute')` is meaningful because `position: absolute` is emitted at exactly one site in the renderer (`packages/framework/src/l1/render.ts:1004`) and only for geometry-carrying nodes — so seeding a geometry track would fail the test.

I also avoided adding to a pre-existing typecheck wart: importing L1 types via the package specifier `@1stcontact/site-schema` produces `TS2307` under `tsc -p tsconfig.base.json` (3 existing test files, incl. `req102`, hit this). I import types from the relative path instead, so this file typechecks clean.

Regression scope run (7 files, incl. `naming.test.ts`, `generate.test.ts`, `site-schema.test.ts`, repro and envelope suites): **50 passed, 2 skipped, 0 failed**.

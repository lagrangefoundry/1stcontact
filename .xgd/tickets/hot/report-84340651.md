---
uid: report-84340651
id: REPORT-3823
type: report
title: 'Reconcile resolve conflicts: reconcile-BUNDLE-26'
created_by: xgd
created_at: '2026-09-11T00:37:21.672304+00:00'
updated_at: '2026-09-11T00:37:21.672304+00:00'
completed_at: null
last_field_updated: created_at
result: pass
fields:
  report_kind: resolve_conflicts
  subject_uid: reconcile-BUNDLE-26
---

## Files resolved

- `.xgd/tickets/hot/doc-e70ef718.md` — AA (both added), intent/bookkeeping doc
  ticket. Rule 2e (per-fact, later intent wins) with the enrichment's
  "unknown intent → more recent commit by timestamp" tiebreak. Resolved to
  **ours (HEAD)**, staged via `git checkout --ours` + `git add --sparse`
  (path is outside the sparse cone, DOC-986 §2/§4.1).

  Facts in conflict were frontmatter-only — `git diff :2: :3:` is 3
  insertions / 2 deletions, all inside the YAML header:
  `updated_at`, `last_field_updated`, and `fields.system_kb`.
  - Theirs (incoming `89e17d73`, 2026-08-30 17:50:28): `system_kb: true`,
    `last_field_updated: body`.
  - Ours (HEAD, via `93713dfb2a`, 2026-08-31 12:42:51, message
    "field: retire system_kb boolean; membership moves to doc_kind
    (DOC-39 3.3)"): `system_kb` removed, `last_field_updated: system_kb`.

  Same fact (`fields.system_kb`) changed differently on each side →
  genuine conflict, resolved to the later-positioned side. HEAD's
  retirement of the flag postdates the incoming commit by ~19 hours and
  is an explicit, narrated field decision, so it governs.

## Incoming changes preserved

The incoming commit's actual payload is a body edit to §6.3 ("correct 6.3
— the corpus was seed material for testing, never a curation policy; cite
DOC-39 for what the KB should hold"). That edit is **already present in
HEAD**: commit `fcf96a0a71` on this branch carries the identical message,
the identical author timestamp (2026-08-30 17:50:28) and the identical
body diff — it landed through a prior sync ahead of this cherry-pick.

Verified in the resolved (ours) blob: the new §6.3 prose is present in
full — "**Seed material, not a policy.**" (line 238), the
"[[DOC-39]] §3" citation (line 255), and "Seed first, verify, then
author." (line 263).

No hunk was dropped under the BUG-1301 precedence exception; no test
files were involved.

## Net effect

The staged tree has no diff versus HEAD (`git diff --cached HEAD` is
empty). This is the redundant-commit case (BUG-1109/BUG-1122), not a
discard: STEP 3's check confirms the incoming commit's content change is
present in HEAD via `fcf96a0a71`, and the only residual delta was
frontmatter that a later HEAD-side commit deliberately superseded.
`cherry-pick --continue/--skip` was not invoked; CHERRY_PICK_HEAD is
intact for `cherry_pick_finalize_resolution`.

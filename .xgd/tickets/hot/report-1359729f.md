---
uid: report-1359729f
id: REPORT-3697
type: report
title: 'Fix Draft Change Journal: What Changed On The Draft, And Who Changed It (story)
  — attempt 1'
created_by: xgd
created_at: '2026-09-10T08:14:18.441935+00:00'
updated_at: '2026-09-10T08:14:18.441935+00:00'
completed_at: null
last_field_updated: created_at
fields:
  report_kind: fix_structural_validation
  subject_uid: capability-702b7c02
  level: story
  fixes_applied: 1
  progress_made: true
  needs_more_work: false
  violations_remaining: 0
  anchor_report_uid: report-e37a6b4a
---

# Fix Summary — Draft Change Journal: What Changed On The Draft, And Who Changed It (story)

**Attempt**: 1
**Fixes applied this call**: 1 ticket mutation, resolving 2 findings (1 violation + 1 warning)
**Violations remaining**: 0 at level=story
**Needs more work**: false

Both story-level findings in report-642edd91 were `story-body-edit`, on the same
ticket. They were applied as a single `xgd ticket update --body-file` on
STORY-115 (`story-6cd17452`) so the matrix is consistent at the call boundary.
Fields and status survived the write (`intent_uid: bundle-77b28def`,
`capability_uid: capability-702b7c02`, `story_kind: feature`, `story_points: 3`,
`status: completed` — verified after the update).

## Actions Taken — by Resolution Category

| # | Category | Element | Action |
|---|---|---|---|
| 1a | story-body-edit | STORY-115 (`story-6cd17452`) — In scope | **Finding 1 (violation, coverage).** Added a sixth in-scope bullet, *"The manual guidance that tells the assistant what to do when the signal fires"*, naming both previously-unexpressed REQ-131 Part 2 deliverables: the **overview** paragraph carrying the cross-cutting rule (your user edits the same page; you are told at the start of a turn; look before you act; never write over a change you have not read) and the named **sequence** walking signal → read the changes → act. Placed immediately after the existing "The push signal" bullet, where the related content lives. |
| 1b | story-body-edit | STORY-115 — Technical Context | Companion to finding 1: added a bullet recording *why* the rule lives in the overview rather than being repeated per-operation, and that the read operation's own description stays about the read. This is the rationale REQ-131 Part 2 cites ([[DOC-30]]'s stated reason for having an overview at all); without it the new in-scope bullet reads as an arbitrary placement choice. |
| 2 | story-body-edit | STORY-115 — Technical Context | **Finding 2 (warning, consistency).** Added *"A no-op advances nothing"* immediately beside *"Records are written at the return of a mutating command"*, per the finding's suggested edit: a write that changes no field — an unchanged copy save, a dry-run gap fix — returns the current count and appends no record, because otherwise every no-op save from the editor's modal would look to the assistant exactly like the operator rewriting a heading. |

Nothing was removed or rewritten. Both edits are additive, as the report's Notes
for the Editor anticipated ("The single repair is small and additive… Nothing in
the story body needs to be removed or rewritten"). Findings 3, 4 and 5 are `info`
with resolution `none` and were correctly left alone — in particular the
"Divergence from the intent, recorded not absorbed" bullet (finding 3) is
preserved verbatim.

## Evidence That This Is Matrix Drift, Not Code Drift

Re-verified before editing, per the report's instruction not to read finding 1 as
a code gap. All three REQ-131 Part 2 artifacts are present in
`tools/generate/src/cli/ai/l1-surface.json`:

| Artifact | Present |
|---|---|
| `overview` paragraph | *"**Your user can change the site themselves, while you are working on it.** … you are told, at the start of a turn, when something has changed since your last one, and how much. When you are told, look at what changed before you act…"* |
| `sequences` entry | `"Pick up after your user has been editing"` — steps `list_changes` → `describe_page` → `get_l1`, with a note to start there rather than with a re-read |
| `absences` on undo | `"Undoing a change"` — leans on `list_changes` carrying before/after rather than instructing the assistant to narrate old values (already expressed in the story's Out-of-scope/Undo bullet; not re-expressed, to avoid duplication) |

Resolution was therefore `story-body-edit` throughout. **No code was edited.**

## Code Edits (if any)

None this call.

## Downstream Work Handed to the `ac` Level (not a needs_review item)

Finding 1's suggested edit ends *"An AC for the projected overview/sequence
follows at `ac` level"*, and the report's Notes say the resolution is
`story-body-edit` **then `ac-add` downstream**. That `ac-add` was deliberately
**not** performed in this story-level call, to honor the assessor's categorization
and level priority. Handing forward two facts the ac-level pass will need:

1. **The gap is real at AC level.** AC-1266 covers the reminder line only; AC-1264
   and AC-1265 cover the operation's projection and its `untrusted` marking. No
   existing AC covers the overview paragraph or the sequence entry.
2. **The AC will need a paired `uat-add`, not just a `uat-edit`.** All 13 UATs in
   `tests/test_UAT_FC_REQ-131_change_journal.test.ts` were scanned: none asserts
   the overview paragraph or the sequence entry.
   `test_UAT_FC_REQ_131_the_operation_is_in_the_manual_of_a_session_granted_ReadSite`
   is the nearest neighbour and the natural template — it already projects the
   manual for a granted session, so the new UAT can assert the overview and
   sequence content off the same projection.

## needs_review Items Forwarded

None. Neither story-level finding was ambiguous; both named the target section
and the text to add.

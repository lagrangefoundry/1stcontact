---
uid: report-07966615
id: REPORT-352
type: report
title: 'Code Review: bundle-f39884d2'
created_by: xgd
created_at: '2026-07-09T21:40:39.958563+00:00'
updated_at: '2026-07-09T21:40:39.958563+00:00'
completed_at: null
last_field_updated: created_at
result: pass
fields:
  report_kind: code_review
  subject_uid: bundle-f39884d2
  anchor_uid: bundle-f39884d2
---

# Code Review

**Result**: PASS

## Summary
Bundle of 11 free-coded commits (BUG-1 + REQ-12/13/14/15/16/22/23/24/25) landing the reference-capture pipeline, the `1c shot` screenshot primitive, four framework language-growth primitives (background, layer, motion, display-font), the header-over-hero compositing variant, the storage/ consolidation, and the structured-content schema fix. Code is uniformly high quality: every new capability follows the same `wrapWithX(html, …)` + static-CSS-folded-into-`theme.css` pattern, and the structured-only "no raw instance CSS" security line (DOC-7 §6.2) is held consistently via `.strict()` schemas and framework-computed inline styles. All quality gates pass and the substance was verified by running the tools.

## Quality Gates
Latest quality report REPORT-350 (report-ade142b7): **Lint** success (0 errors, 0 warnings), **Build** success, **Preflight** pass, **Tests** 63/63 pass (0 failed, 0 skipped), 142 deselected. Suite runs in 10.7s. All gates PASS.

## External Interface Accessibility
New entry points wired in: **yes**.
- `1c capture page <url>` — dispatched in `tools/generate/src/cli/index.ts:190` (case 'capture'), exported via `./capture`.
- `1c shot <slug> | --url <url>` — dispatched at `index.ts:205`, `cmdShot` exported.
- Structured-edit `page`/`config`/`asset`/`status` — dispatched at `index.ts:222`.
- Framework primitives exported from `packages/framework/src/index.ts` and consumed in `render.ts` (`wrapWithBackground`/`wrapWithLayer`/`wrapWithMotion`/`composeOverlayHeader`/`getModuleCss` + `SECTION_CSS`/`LAYER_CSS`/`MOTION_CSS`/`OVERLAY_BAND_CSS` folded into `theme.css`).
- `header` `overlay` variant + `logoFont`/`logoTreatment` dials registered in `header/meta.ts`.
No dead/unwired modules found.

## Code Quality
| File | Finding | Severity |
|------|---------|----------|
| framework/src/modules/{background,layer,motion}.ts | `escapeAttr` (1-line helper) duplicated in 3 modules; could live in a shared util | Minor |
| tools/generate/bin/1c.mjs (startup) | `1c help` prints Astro/vite warnings ("Missing pages directory", "WebSocket port in use") — side effect of eagerly importing `astro/container`; noise only, exit 0 | Minor |
| render/render.ts, styles.ts | BUG-1 fix (fold module `<style>` blocks into theme.css) clean and well-documented; verified end-to-end | Good |
| site-schema/src/schema.ts | Structured, thoroughly documented; `.strict()` security line preserved while widening content values to object/scalar forms | Good |

## Smoke Test
Entry points tested:
- `1c help` → exit 0, full usage printed (all commands listed).
- `1c render 1stcontact --source draft` → exit 0, 3 files rendered under `storage/dist/sites/1stcontact/draft` (confirms REQ-22 storage paths).
- Verified BUG-1 fix: rendered `theme.css` carries **111 class-selector rules** (`.hero`, `.hero.size-lg`, `.header__inner`, `.footer__tagline`, `.fc-bg-section*`, …) matching emitted module classes — not just `:root` vars. The unstyled-output bug is genuinely fixed.

## Checklist Compliance
No architecture, security, or design checklist reports exist for this anchor (queried all three `report_kind` filters → 0 items). Sections omitted per instructions.

## Issues Found
**Critical (must fix)**: none.

**Warnings (should fix)**:
- Minor DRY: the identical `escapeAttr` helper is copied into background.ts / layer.ts / motion.ts. Consider a shared `packages/framework/src/modules/escape.ts`.
- `1c help` (and any 1c invocation) emits Astro/vite startup warnings because `render.ts` imports `astro/container` at module load. Cosmetic, but pollutes CLI output; consider lazy-importing the container only inside the render path.

Neither warning affects correctness, gates, or user-facing behavior — recorded for future cleanup, not blocking.

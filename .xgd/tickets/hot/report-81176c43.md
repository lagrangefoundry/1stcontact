---
uid: report-81176c43
id: REPORT-1018
type: report
title: 'Resync resolve conflicts: d3d689184dbc45f44b278bad79f1c82fb57525b9'
created_by: xgd
created_at: '2026-07-29T04:04:20.276678+00:00'
updated_at: '2026-07-29T04:04:20.276678+00:00'
completed_at: null
last_field_updated: created_at
result: pass
fields:
  report_kind: resolve_conflicts
  subject_uid: report-510318c4
---

## Files resolved

- `package.json` — class **UU**, rule **2g (config file, scalar conflict)**.
  Sole conflict in the cherry-pick of `47fd0ae90` ("feat(l1): add `control`
  nodes so behavior modules stop painting [FREE-CODED]"). The hunk was a pure
  version-bump collision: ours (main-rooted resync branch, `sync_working_to_main`
  from xgd-working d3562e3b8285) at `0.0.225`; theirs (incoming free-coded
  commit) at `0.0.214`.
  Resolution: kept `0.0.225`. Resync picks carry code only — the incoming
  commit's stale bump is a bookkeeping artifact of its original position on
  xgd-working, not developer intent. Taking `0.0.214` would have rewound main's
  version counter. Verified the file re-parses as valid JSON.

No other conflicted paths. No deletion (DU/UD), AA, spec-ticket, UAT, or
intent/bookkeeping-ticket conflicts arose.

## Incoming changes preserved

Verified by tree-diffing the incoming commit against the landed commit:

    git diff 47fd0ae90 f4dff5057 --stat -- packages/ tests/ tools/ apps/ storage/
    -> empty

All 45 code files from the incoming commit (2408 insertions, 482 deletions)
are byte-identical in the resolved result, including the three new files:
`packages/framework/src/l2/contact-form.ts`,
`packages/framework/src/modules/contact-form/controls.ts`,
`tests/req96-control-composition.test.ts`.

The only deltas between incoming and landed are `package.json` (the intended
version resolution above) and `.xgd/tickets/**` (main-side ticket drift, which
is expected since the landed commit is rooted on main).

Build verification: `pnpm -r build` clean across all 7 workspace projects
(`tsc --noEmit` green on both apps; site-schema emits).

## DEVIATION — cherry-pick was advanced before these instructions arrived

In the turn preceding this prompt, `git cherry-pick --continue` was run,
committing the resolution as `f4dff5057`. This workflow's instructions
reserve `--continue/--skip/--quit/--abort` for the Python step that follows.

Current state: tree clean, no conflicts, CHERRY_PICK_HEAD ABSENT.

The resolution itself is correct and fully verified above, but the downstream
Python step depends on CHERRY_PICK_HEAD still being present. It will likely
report "no cherry-pick in progress" and needs to treat commit `f4dff5057` as
the already-completed result of attempt 38 rather than re-driving it.

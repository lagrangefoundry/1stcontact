---
uid: report-5746c198
id: REPORT-1610
type: report
title: 'Capability-Intent Alignment: Site Asset Store: What This Site Can Reference
  (level=ac)'
created_by: xgd
created_at: '2026-08-07T19:04:55.312097+00:00'
updated_at: '2026-08-07T19:04:55.312097+00:00'
completed_at: null
last_field_updated: created_at
result: pass
fields:
  report_kind: capability_validation
  subject_uid: capability-105cfacf
  level: ac
  violations: 0
  warnings: 1
  needs_review_count: 0
---

# Capability-Intent Alignment: Site Asset Store: What This Site Can Reference
# Level: ac

**Result**: PASS
**Violations**: 0
**Warnings**: 1
**Needs review**: 0

## Headline

**CAP-88 has no AC tree to check, because it has no story tree, because it is a
retired capability.** The ac-level check set is empty, so consistency, coverage and
exclusivity hold vacuously. This is the correct post-retirement end state, not drift.

Authoritative state, read off the ticket (not the index), `updated_at 2026-08-07T18:53:49`:

```
capability-105cfacf  status: superseded
                     superseded_by_uid: capability-b4ac88fc   (CAP-89)
                     stories: 0    acceptance criteria: 0
```

The story-level cycle for this capability already reached the same conclusion and
passed (REPORT-1609 `report-962c67e7`, 0 violations). Per the level cascade, that
story-level result is my working reference; nothing at ac level forces me to reopen it.

## Verification performed (index deliberately distrusted, re-derived this run)

REPORT-1599 finding 4 / REPORT-1609 warning 1 established that the canonical search
index is stale for this capability. I therefore did **not** build the check set from
`xgd ticket list --filter`. Every claim below is re-derived from `xgd ticket get --json`:

- **All 25 stories fetched individually**; `fields.capability_uid` read off each ticket.
  **Zero** point at `capability-105cfacf`. Four point at `capability-b4ac88fc`
  (STORY-92, 93, 97, 102). The remaining 21 distribute across CAP-12fee326, f753cecd,
  ae9d65d6, a994b8f3, aa030c83, a12e557f, 2049c9ec. Full accounting, no story unassigned.
- **All 50 acceptance criteria fetched individually**; every `fields.story_uid` resolves
  to a live story in that set — **zero orphans**. They parent as: STORY-92 (12),
  STORY-93 (8), STORY-97 (9), STORY-98 (13), STORY-102 (6), STORY-101 (2).
  **No AC hangs off CAP-88, and no AC body or field string mentions `capability-105cfacf`
  or `CAP-88`.**
- **CAP-88's own status and retirement field** confirmed directly, as quoted above.

So the ac-level check set for `capability-105cfacf` is empty by direct enumeration, not
by absence of a query result.

## Cumulative Intent Considered

Both intents that ever touched this capability's (now migrated) tree, re-verified this
run by `xgd ticket get --json`:

| Intent ID | Status | When | Asked / changed | Counts? |
|---|---|---|---|---|
| REQ-11 (`request-53c276dd`) | free_and_reconciled | 2026-06-30 | `1c` structured-edit CRUD, including the original **registry-only** `asset list` | YES — superseded in place by REQ-118 |
| REQ-118 (`request-66e4c630`) | free_and_reconciled | 2026-07-31 (merged `b2b9208c`) | Replaced the registry-only listing with the **union** of registry + `draft/assets/`, per-entry provenance, one `/assets/<name>` handle vocabulary, derived usage `kind`, and the independent `GET /api/assets` origin route. Explicitly rejected a second "pickable" listing. | YES — sole originating intent for this scope |

No later intent retires, narrows or re-homes the asset store. CAP-88's consolidation into
CAP-89 was process bookkeeping from this regression run's own structural pass
(REPORT-1573 → REPORT-1576 cluster 2 → REPORT-1599 finding 3 → REPORT-1608 fix), not a
change to product intent — which is why no intent ledger entry retires REQ-118's asks.

## Alignment Ledger

| Element | Intents aligned to | Outcome |
|---|---|---|
| CAP-88 (`capability-105cfacf`) | REQ-11, REQ-118 (both via STORY-102, migrated) | **retired** — 0 stories, 0 ACs. Empty ac-level check set. Correct end state. |
| — (no stories under CAP-88) | — | nothing to assess at this level |

### Nothing stranded at AC level by the retirement

REQ-118's asks are expressed as six **active** ACs, all `uat_coverage=pass`, all parented
to STORY-102 — which now sits under **CAP-89**, where they have already been assessed at
this exact level:

| CAP-88 scope statement (retired body) | AC now carrying it | Assessed under |
|---|---|---|
| Union of the two disagreeing sources, with provenance | AC-1018 (undeclared file still listed), AC-1019 (declared asset with no file still listed) | CAP-89 |
| One handle vocabulary | AC-1020 | CAP-89 |
| Usage kind, so a caller can narrow | AC-1021 | CAP-89 |
| Reachable without any editing gesture | AC-1022 (command line), AC-1023 (builder origin, same list; missing site refused as caller fault) | CAP-89 |

CAP-89's own ac-level check has **already passed**: REPORT-1600 (fail) → REPORT-1601
(fix attempt 1) → **REPORT-1602 `report-aa73bcbf` PASS**, followed by uat-level PASS
(REPORT-1605) and UAT coverage PASS (REPORT-1606). Re-assessing AC-1018…AC-1023 here
would be a duplicate assessment filed against the wrong subject.

## Findings

| # | Severity | Property | Element | Resolution category | Issue | Suggested edit |
|---|---|---|---|---|---|---|
| 1 | warning | consistency | canonical ticket search index | — (XGD tooling, not a matrix edit) | `xgd ticket list --type story --filter fields.capability_uid=capability-105cfacf` still returns STORY-102, whose authoritative `fields.capability_uid` is `capability-b4ac88fc`. Reproduced again this run. **At ac level the blast radius is larger than at story level**: a walker that trusts the filter sees a phantom story under a retired capability *and then resolves its six ACs* (AC-1018…AC-1023) — so AC-1018…AC-1023 get double-counted, once under CAP-89 where they belong and once under retired CAP-88. Raised as REPORT-1599 finding 4, forwarded by REPORT-1608, recorded by REPORT-1609; still unresolved. | `xgd ticket rebuild-index` from `xgd-working`. It refuses to run from a branch worktree by design, so it cannot be repaired from this regression worktree. No ticket-body change required. |
| 2 | info | coverage | CAP-88 (`capability-105cfacf`) | — | Empty AC tree under a `superseded` capability is correct, not a coverage gap. Only `feature`/`upgrade` stories are expected to carry ACs, and CAP-88 has no stories of any kind. The attempt-1 fix correctly declined to author a replacement story or AC. | none |
| 3 | info | exclusivity | AC-1018…AC-1023 | — | The six ACs appear under CAP-88 **only** via the stale index (finding 1). On authoritative state they exist exactly once, under STORY-102 → CAP-89. No genuine duplication between two live elements. | none |
| 4 | info | consistency | CAP-88 body | — | Body still reads as an active capability description with no prose "superseded by CAP-89" note. Consistent with all sibling retirements in this consolidation (CAP-80/81/83/84), which likewise carry only `superseded_by_uid` — the schema's single declared retirement field (`ticket_types.yaml`, `required_when: status == superseded`). Convention, not omission. | none — do not invent a prose convention |

## Notes for the Editor

**No edits are required at this level. Do not re-open CAP-88, and do not author ACs for it.**
A retired capability with an empty story tree cannot have an AC-level defect; authoring
anything under it would resurrect a capability the consolidation deliberately closed.

**Finding 1 is deliberately a warning, not `needs_review`.** It is a defect in the XGD
tool (a stale search index), not drift between the matrix and product intent, and it is
unfixable from a regression worktree by design. Classifying it as `needs_review` would
fail this level and spin a fix loop with no available action. It has now been escalated
to the operator three times (REPORT-1599, REPORT-1608, REPORT-1609) and is carried
forward here unchanged. It is load-bearing for any future capability→story→AC walk, so it
should be cleared from `xgd-working` before the next structural pass.

**Where the live ac-level work sits.** CAP-88's subject matter is governed by CAP-89,
whose ac-level check has already passed (REPORT-1602). One stale pointer noted at story
level still stands and belongs to CAP-89's ledger rather than this one: STORY-102's body
twice defers licence/provenance to "CAP-80" as a *neighbouring capability*, when CAP-80 is
now a retired sibling section inside CAP-89. Stale pointer, correct substance — cosmetic,
and out of scope for a check filed against `capability-105cfacf`.

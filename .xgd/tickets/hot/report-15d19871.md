---
uid: report-15d19871
id: REPORT-3873
type: report
title: 'Capability-Intent Alignment: Page Authoring Through The Control Surface: Read
  & Replace The Element Tree (level=ac)'
created_by: xgd
created_at: '2026-09-11T02:28:21.227360+00:00'
updated_at: '2026-09-11T02:28:21.227360+00:00'
completed_at: null
last_field_updated: created_at
result: pass
fields:
  report_kind: capability_validation
  subject_uid: capability-fe236246
  level: ac
  violations: 0
  warnings: 1
  needs_review_count: 0
---

# Capability-Intent Alignment: Page Authoring Through The Control Surface: Read & Replace The Element Tree
# Level: ac

**Result**: PASS
**Violations**: 0
**Warnings**: 1
**Needs review**: 0

Anchor report: report-e37a6b4a. Attempt 3 of this level (previous_attempt_count = 2).

CAP-93 (`capability-fe236246`) has exactly one story — STORY-106 (`story-189fc1ac`,
`story_kind=feature`, status `completed`) — carrying **12 active ACs** (AC-1083 … AC-1094),
all `kind=behavior`, `regression_only=false`, `uat_coverage=pass`. None deprecated. The
ticket store was re-read from disk this cycle (`.xgd/tickets/hot/`), and no AC for this
story exists in `cold/`.

**The prior cycle's single violation is repaired, verified independently rather than
accepted.** REPORT-3870 (`report-cac55785`, 02:16) failed this level on one `ac-edit`:
AC-1090 still carried a *"Known limit, deliberately recorded"* paragraph asserting that the
offending field never reaches this caller, after STORY-106's body had been corrected to say
that limit closed upstream. REPORT-3872 (`report-9b910172`, 02:20) applied four mutations.
All four were re-read this cycle, and the behavioural claim behind them was re-derived from
source and from an executed test rather than carried forward.

## Cumulative Intent Considered

STORY-106's `fields.intent_uid` is **BUNDLE-17** (`bundle-e59210c5`, `free_and_reconciled`,
`merged_at_commit 0198704b7e29db3c53cf569070042cec0eb467bc`), bundling eight requests, of
which **REQ-129** alone governs this capability. Statuses below were re-read from the ticket
store this cycle.

| Intent ID | Status | When | Asked / changed | Counts? |
|---|---|---|---|---|
| REQ-126 (`request-d9407f80`) | free_and_reconciled | 2026-08-08 | Declared the control surface: operations, error taxonomy, addressing contract. **Owned by CAP-92** | YES (dependency) |
| REQ-127 (`request-22a6521a`) | free_and_reconciled | 2026-08-08 | Tooling configuration over the surface. **Owned by CAP-92** | YES (dependency) |
| REQ-129 (`request-b1300473`) | free_and_reconciled | 2026-08-09 | **The governing intent.** `describe_page` widened to every node; verbatim `get_l1`; subtree-replacing `set_l1`; `get_copy`/`set_copy` retired; the security guarantee relocated to L1's closed schema; the operator modal left unchanged; refusal-specificity recorded as an upstream limit | YES — primary |
| REQ-130 (`request-ed6ba145`) | free_and_reconciled | 2026-08-09 | Beyond the element tree: config, components, page metadata, generated assets. **Owned by CAP-94** | YES (sibling, out of scope here) |
| REQ-119 / REQ-121 / REQ-122 / REQ-128 | free_and_reconciled | 2026-07-31 … 08-08 | The rest of BUNDLE-17; no reach into this AC surface | YES (unrelated here) |
| REQ-131 (`request-5d3bf630`) | free_and_reconciled | 2026-08-11 | Draft change journal; every mutating op returns the resulting draft count | YES — expressed in CAP-99 (finding 2) |
| REQ-135 (`request-a8ccd0dd`) | free_and_reconciled | 2026-08-12 | Operator modal gains typography controls — **the first editor surface that writes `axes`** (§8) | YES — bears on AC-1093 (finding 3) |
| REQ-137 (`request-d2980a95`) | free_and_reconciled | 2026-08-12 | L1 palette: `shade` on the reference replaces named `steps` | YES — no AC edit needed (finding 4) |
| REQ-139 (`request-3f57cd0c`) | free_and_reconciled | 2026-08-12 | The operator modal locks a control that cannot express what the element holds | YES — no AC edit needed (finding 5) |
| REQ-145 (`request-b474390f`) / REQ-146 (`request-0cdfdc5b`) | free_and_reconciled | 2026-08-15 | control-app becomes the builder; the AI host moves into workerd | YES — relocation of the host, not of the operations' semantics; no AC claim touched |
| REQ-134 (`request-ba3e3fba`) | abandoned | 2026-08-12 | Image-generation component | NO |
| REQ-157 (`request-3f8737f2`) / REQ-160 (`request-bbff35c7`) | draft | 2026-08-20 / 08-30 | Fidelity surface; session seeding | NO (not active) |

Widened beyond the prior cycle's ledger by sweeping every `request-*` ticket naming
`set_l1` / `get_l1` / `describe_page` / `editL1Set` / `copyFieldsOf` / `AuthorPages` /
`l1-surface`, which is what surfaced REQ-135, REQ-145 and REQ-146. None of the three
falsifies an AC.

## Alignment Ledger

| Element | Intents aligned to | Outcome |
|---|---|---|
| AC-1083 (`acceptance_criterion-da12ce4c`) — map returns every element | REQ-129 | aligned — matches the story's "Where is everything" bullet; `pageSegments`/`walkSegments` (`tools/generate/src/cli/segments.ts:72-118`) emits every node and both spaces, carrying `{module, slot}` only when scoped |
| AC-1084 (`acceptance_criterion-f1e7490c`) — label recognises, no styling in the map | REQ-129 | aligned — every label rule in the AC is the rule in `labelOf` (`segments.ts:42-53`): text truncated at 60 with an ellipsis, image alt-or-src, control name, slot name (+ behaviour), container `layout, N children`; no axes emitted |
| AC-1085 (`acceptance_criterion-aa3322ea`) — verbatim read | REQ-129, REQ-137 | aligned, durably — phrased model-agnostically ("a reference … still a reference"), so REQ-137's `steps`→`shade` change needs no edit |
| AC-1086 (`acceptance_criterion-9a249134`) — read→write round trip is **accepted** | REQ-129 | aligned — and correctly notes a refusal also leaves the page unchanged, so acceptance must be asserted |
| AC-1087 (`acceptance_criterion-277dfb8a`) — replace is whole-subtree, siblings untouched | REQ-129, REQ-131 | aligned — `editL1Set` returns `target {pageId, module, slot, path}` + `changed: [rawPath]` (`tools/generate/src/cli/edit.ts:864-874`), so "the reply names the address" still holds after REQ-131 added the draft count |
| AC-1088 (`acceptance_criterion-d75d0764`) — add/remove as group replace, and it renders | REQ-129 | aligned — carries the story's "no separate insert or delete", the sequences claim, and REQ-129's nav-bar acceptance case |
| AC-1089 (`acceptance_criterion-4d4ac81f`) — off-vocabulary refused whole, draft byte-unchanged | REQ-129, DOC-2 | aligned — all six measured security cases present |
| AC-1090 (`acceptance_criterion-4bd36a69`) — a refusal names the field **and** the strategy | REQ-129 (+ upstream BUG-39) | **aligned this cycle** — the drift REPORT-3870 found is repaired; re-verified at source and by running the test (finding 1) |
| AC-1091 (`acceptance_criterion-bad505ff`) — bad address refused, nothing written | REQ-129, REQ-126 (addressing) | aligned; one uat-level gap in its second half (finding 6) |
| AC-1092 (`acceptance_criterion-fbda4a6e`) — exactly one way to change a page; declared ≡ implemented | REQ-129 | aligned — `AuthorPages` is declared (`l1-surface.json:790`) and granted (`instances.json:6`), which is the AC's last clause |
| AC-1093 (`acceptance_criterion-d1bda2c2`) — operator modal opens/saves on assistant-composed elements | REQ-129, REQ-135, REQ-139 | aligned — kind-relative phrasing survives both widenings (finding 3) |
| AC-1094 (`acceptance_criterion-84b87d8a`) — no fields on a kind the form does not edit | REQ-129, REQ-139 | aligned — an empty list cannot be made non-empty by a never-hide-a-row rule |

### Consistency — each AC against the story body

All twelve now trace cleanly to a bullet of STORY-106's body. The one contradiction the
prior cycle found is gone: AC-1090's criterion now states the failure code, the recovery
strategy and the offending field as three things carried **together**, with an explicit
"complementary, not alternatives" sentence, and its title was widened to match
("…tells the caller nothing was written, **which field was wrong**, and what to do…").
STORY-106's divergence bullet now reads as history ("the acceptance criterion below now
asserts both halves, the field and the strategy, so nothing here is outstanding"). AC and
story body agree in both directions, which is what the two-sided edit was for.

### Coverage — the story's behavioural surface against the AC set

| Story bullet | Covered by |
|---|---|
| Where is everything (the map) | AC-1083, AC-1084 |
| Read one element as it stands | AC-1085 |
| Replace one element (+ the read/write pair) | AC-1086, AC-1087, AC-1091 |
| Adding and removing | AC-1088 |
| One way to change a page | AC-1092 |
| Where the security guarantee now lives | AC-1089, AC-1090 |
| The operator's editing gesture is untouched | AC-1093, AC-1094 |
| Divergence 1 — refusal specificity, closed upstream | AC-1090 (now asserts the closed behaviour) |
| Divergence 2 — vestigial `nav` settings key | correctly no AC; the body states it is not addressed here |

No residue in either direction: no AC states behaviour the body does not, and no in-scope
bullet is unaddressed.

### Exclusivity

No two ACs state the same criterion. The near neighbours were re-checked individually:
AC-1086 (a faithful round trip is *accepted*) vs AC-1087 (a *different* element replaces the
subtree) differ in what is sent and what is asserted; AC-1089 (schema-invalid, six payload
shapes, draft byte-unchanged) vs AC-1090 (what the refusal *says*) vs AC-1091 (not-found /
malformed **address**) are three different assertions over two different refusal classes;
AC-1093 and AC-1094 are complementary halves of one invariant.

## Findings

| # | Severity | Property | Element | Resolution category | Issue | Suggested edit |
|---|---|---|---|---|---|---|
| 1 | info | consistency | AC-1090 (`acceptance_criterion-4bd36a69`) | — | The prior violation is repaired and the repair is correct. Re-derived independently this cycle rather than accepted from REPORT-3872: `node_modules/@lagrangefoundry/ai/src/toolbox/declaration.js:448-463` makes `host_detail` opt-*out*, default true, citing upstream BUG-39; `runtime.js:493-495` renders `… (<code>). <declared meaning> The host reports: <detail>` — appended, not swapped; `runtime.js:549-558` (`usableDetail`) returns the pointer even when the message text adds nothing ("a useless message must not take one with it"); `tools/generate/src/cli/ai/l1-surface.json:124-126` declares `SCHEMA_INVALID` with a `message` only and no `host_detail` key, so this surface does not opt out; `tools/generate/src/cli/edit.ts:215-226` throws the JSON pointer as `path`. Empirically confirmed, not only read: `npm test -- tests/test_UAT_FC_REQ-129_l1_authoring.test.ts` was executed this cycle — **11 passed, 2 skipped**, and `test_UAT_FC_REQ_129_a_refusal_is_correctable_within_the_turn` (with the new `expect(answer).toMatch(/fontSizePx/)` at `:397`) is among the passes. The pointer reaches a Toolbox caller today. (The 2 skips are the modal block at `:488`, whose `beforeAll` calls `startBuilder` — `listen EPERM: operation not permitted 0.0.0.0`, `tools/generate/src/cli/builder.ts:363`. That is this session's sandbox refusing a socket, not a defect; those two tests are AC-1093/AC-1094's and passed at the `uat` level on 2026-08-16, REPORT `report-97baa7a9`) | none — resolved |
| 2 | info | coverage | STORY-106 AC set | — | REQ-131 made every mutating op return the resulting draft count. No AC here states a result contract for `set_l1` beyond "the reply names the address" (AC-1087), which `edit.ts:864-874` still satisfies. The count is claimed once for every write on the path by STORY-115 (`story-6cd17452`, CAP-99). Adding an AC here would duplicate a cross-cutting claim | none — coverage sits in CAP-99 by design |
| 3 | info | consistency | AC-1093 (`acceptance_criterion-d1bda2c2`) | — | **New to this cycle's ledger.** REQ-135 §8 makes the operator modal "the first editor surface that writes `axes`" — the same `node.axes` AC-1093 requires survive the operator's save byte-identical. Checked rather than assumed: the V1 field set (§6) writes only the axis a control was operated on, `applyCopyFields` writes into `node.axes` per field, and the UAT's own assertion is the untouched case — a text save leaves `{fontSizePx: 20}` intact (`tests/test_UAT_FC_REQ-129_l1_authoring.test.ts:557-558`). AC-1093 is kind-relative ("the fields that element's **kind** exposes"), so a widened row set is still the right answer. Not a violation; recorded because a future cycle reading REQ-135 §8 cold will reach for one | none |
| 4 | info | consistency | AC-1085 | — | REQ-137 replaced the palette's named `steps` with `shade` on the reference — a change to the vocabulary `get_l1` returns verbatim. AC-1085 never names the entry model, so it survives untouched | none |
| 5 | info | consistency | AC-1093 + AC-1094 | — | REQ-139 shows a control that cannot express what the element holds as locked-with-a-reason rather than dropped. Both ACs survive, for the reasons the two prior cycles recorded; re-confirmed, not re-derived | none |
| 6 | warning | coverage | AC-1091 (`acceptance_criterion-bad505ff`) | `uat-add` | The AC's second half — "an address that is not well-formed … is refused as malformed rather than resolved to some nearby element", with a Verification step that sends one — has **no UAT through this surface**. `tests/test_UAT_FC_REQ-129_l1_authoring.test.ts:400-412` covers only the out-of-range address (`9.9.9` → `NOT_FOUND`). The malformed path is implemented (`tools/generate/src/cli/edit.ts:835-843`, `parseL1Path` → `SCHEMA_INVALID` "'X' is not a segment address") and is exercised elsewhere for *other* callers — `tests/req117-edit-loop.test.ts:142` over `/api/copy`, `tests/reconciliation-edit-render-channel.test.ts:1022` on `parseL1Path` directly — but never through `set_l1`. Strictly a `uat`-level repair; recorded here because it is an AC whose stated verification is half-unperformed | Add a `set_l1` case at a malformed address (e.g. `'nope'`) asserting the refusal and the byte-unchanged draft, beside the existing `9.9.9` case. Note the code is `SCHEMA_INVALID`, not a distinct malformed-address code — assert the refusal, not a code AC-1091 never names |

## Notes for the Editor

**Nothing blocks this level.** The one violation from REPORT-3870 is repaired in both
directions (AC body + title, and the story sentence that would otherwise have gone stale
the moment the AC changed), and the paired `uat-edit` cascade landed in the test at
`:397`. Finding 6 is the only open item and it is a warning at the wrong level for this
cycle to fix.

**Dependency drift still has no commit here.** `@lagrangefoundry/ai` is unpinned and its
installed `package.json` declares `"version": "0.0.0"`, so the behaviour AC-1090 now asserts
arrived with no commit, no intent ticket and no red test in this repository. Both the
evidence chain *and* an execution of the suite were re-done this cycle for that reason. A
future run against a different installed copy must re-read
`node_modules/@lagrangefoundry/ai/src/toolbox/runtime.js` rather than trust this report.

**Ledger widened, deliberately.** The prior two cycles' ledgers omitted REQ-135, REQ-145 and
REQ-146. All three touch code this capability's ACs describe; none falsifies an AC, and
finding 3 records why REQ-135 in particular looks more dangerous than it is.

**Method.** Static analysis of the ticket store and working tree, plus one executed test
run (`npm test -- tests/test_UAT_FC_REQ-129_l1_authoring.test.ts` → 11 passed, 2 skipped,
180.8s; the file reports FAIL because the modal block's `beforeAll` hook timed out on a
sandbox-denied `listen`). Reported as it ran rather than as "green": the two skipped tests
are AC-1093's and AC-1094's, and this session produced no fresh execution evidence for
them. REPORT-3872 recorded "27 passed (27)" for a superset run; that is not reproducible
here, and the difference is the socket, not the code. Read-only: no ticket, test or source
file was modified.

**Traceability friction, unchanged and unrepaired.** CAP-93 carries no `intent_uid` and
STORY-106's points at BUNDLE-17, so establishing that REQ-129 governs still costs a
dereference of eight bundled requests. Tooling-level, not repairable at this level.

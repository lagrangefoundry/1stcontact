---
uid: report-989c8619
id: REPORT-3090
type: report
title: 'Reconcile resolve conflicts: reconcile-BUNDLE-22'
created_by: xgd
created_at: '2026-08-31T21:43:03.227748+00:00'
updated_at: '2026-08-31T21:43:03.227748+00:00'
completed_at: null
last_field_updated: created_at
result: pass
fields:
  report_kind: resolve_conflicts
  subject_uid: reconcile-BUNDLE-22
---

## Files resolved

- `.xgd/tickets/hot/bug-a98fb3b0.md` — UU, intent/bookkeeping ticket (rule 2e).
  Resolved to the HEAD side via `git checkout --ours` + `git add --sparse`
  (path is outside the sparse-checkout cone, DOC-986 §2/§4.1).

  Incoming commit `2c208ef3` ("xgd(ticket): update bug bug-a98fb3b0",
  2026-08-24 22:19:54 UTC) is the fourth step in this ticket's free-coding
  chain (after `1c5985f8`, `82518d60`, `04957574`, resolved in the three
  preceding iterations). It adds `story_points: 2` and, as a consequence,
  moves the derived marker `last_field_updated` from `status` to
  `story_points`.

  Per-fact comparison against HEAD (`git diff <theirs-blob> <ours-blob>`):
  - `story_points: 2` — the incoming commit's only substantive addition;
    already present byte-identically on HEAD. No conflict.
  - `title`, `severity`, `commits`, `version`, and the whole body — unchanged
    by this commit and identical on both sides. No conflict.
  - `status` — differs: incoming `free_coded`, HEAD `bundled`
    (updated_at 2026-08-26T17:36:27 vs the incoming's 2026-08-24T22:19:54).
    Timeline rule per fact: HEAD is the later position and `bundled` is the
    downstream lifecycle successor of `free_coded` for this same ticket. Kept
    HEAD's value, consistent with the auto-enrichment guidance ("take the more
    recent commit by timestamp").
  - `last_field_updated` — incoming `story_points`, HEAD `status`. This is a
    derived marker naming whichever field that side wrote last; it is not an
    independent fact. HEAD's later edit was `status: bundled`, so `status` is
    the correct value for the resolved file. Setting it to `story_points`
    would misdescribe HEAD's own most recent edit.
  - `bundled_in: bundle-78f4e2fe` — present only on HEAD, written by the
    bundling workflow after the incoming commit. Kept.

  HEAD is a strict superset of every substantive fact the incoming commit
  writes; taking it loses nothing from either side. No content was invented.

## Incoming changes preserved

- `.xgd/tickets/hot/bug-a98fb3b0.md`: the incoming commit's substantive
  addition, `story_points: 2`, is present in the resolved file (line 24). The
  `last_field_updated` / `status` / `updated_at` values are HEAD's later ones
  (lines 11-12) per the per-fact timeline rule above — a deliberate
  supersession of stale lifecycle bookkeeping, not a discard of developer
  content.

No code/implementation files were involved in this conflict. No hunks were
dropped under the BUG-1301 precedence exception.

## Note on the staged diff

`git diff --cached --stat HEAD` is empty: this commit's effect already reached
HEAD through a later route (the seed_local_overlay / bundling side, which
carries the identical `story_points: 2` plus the advanced status). This is the
redundant-commit case (BUG-1109/BUG-1122), not a discard — STEP 3's check
confirms the incoming change is *present* in HEAD rather than absent. `--skip`
was not called; the file is staged and the cherry-pick sequencer state
(CHERRY_PICK_HEAD = 2c208ef3) is left intact for
`cherry_pick_finalize_resolution`.

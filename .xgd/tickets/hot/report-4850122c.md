---
uid: report-4850122c
id: REPORT-4311
type: report
title: 'Reconcile resolve conflicts: reconcile-BUNDLE-27'
created_by: xgd
created_at: '2026-09-18T05:43:45.524413+00:00'
updated_at: '2026-09-18T05:43:45.524413+00:00'
completed_at: null
last_field_updated: created_at
result: pass
fields:
  report_kind: resolve_conflicts
  subject_uid: reconcile-BUNDLE-27
---

## Files resolved

- `.xgd/tickets/hot/bug-db356ff8.md` (BUG-36) — UU, intent/bookkeeping ticket
  (rule 2e). Incoming commit `5af1ff949d` ("xgd(ticket): update bug
  bug-db356ff8", 2026-08-23) vs HEAD. Applied the **strict-superset** branch of
  2e: kept the HEAD side wholesale, because a full three-way diff of the index
  stages showed HEAD contains every line the incoming commit added, plus
  strictly more.

  Two conflict hunks, both in the same file:

  1. **Frontmatter lifecycle block** — genuine same-field conflict. Incoming:
     `status: draft`, `completed_at: null`, `last_field_updated: body`,
     `updated_at: 2026-08-24T01:48:23Z`. HEAD: `status: free_and_reconciled`,
     `completed_at: 2026-08-31T19:19:38Z`, `last_field_updated: status`,
     `updated_at: 2026-08-31T19:19:38Z`, plus `fields.story_points`,
     `fields.commits` (working_sha `ea48502d0d`), `fields.version: 0.2.10`,
     `fields.bundled_in: bundle-78f4e2fe` — none of which the incoming side
     has. HEAD's is the later-positioned write (2026-08-31 vs 2026-08-24) and
     is the operator/workflow-owned lifecycle status; taking the incoming side
     would have demoted a `free_and_reconciled` ticket back to `draft` and
     dropped its bundle/version bookkeeping.
  2. **Final line of the body** — byte-identical text on both sides; the only
     difference is that HEAD's later write has no trailing newline at EOF.
     No content decision involved.

## Incoming changes preserved

Confirmed by diffing index stage 3 (incoming) against stage 2 (HEAD) directly:
the ONLY differences are the frontmatter lifecycle block and the EOF newline
described above. The incoming commit's entire substantive payload — 112 added
lines — is already present verbatim in HEAD:

- the rewritten `## Status` section ("Both halves landed and verified
  (2026-08-23)…", replacing "Scope drafted, awaiting operator confirmation");
- the whole `# Implementation — the tenant fix` section: the changed-files
  table (`store.ts` / `router.ts` / `builder.ts` / `d1r2-store.ts`), the
  `storeFor` cold-path snippet, "Why `reason` had to be exposed", "Scope of
  what the bootstrap may create", the five-UAT `## Tests` listing for
  `tests/test_UAT_FC_BUG-36_tenant_bootstrap.workers.test.ts`, the REQ-149
  AC-10 supersession note, `## Verified`, and `## Still open, and NOT this
  ticket` (the Error 1102 edit-mode note).

So nothing was discarded: the body arrived on this branch through an earlier
route (post-watermark sync), and the incoming commit's text is what HEAD
already carries. No code/implementation files were in conflict, so STEP 3's
per-code-file check has no other subject, and no hunk was dropped under the
BUG-1301 precedence exception.

Net effect: the staged resolution is byte-identical to HEAD, i.e. this
cherry-pick has no residual diff. Per STEP 4 that is not a failure and
`--skip` was NOT invoked — the tree is staged with `CHERRY_PICK_HEAD` intact
for `cherry_pick_finalize_resolution` to handle.

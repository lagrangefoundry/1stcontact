---
uid: report-0929694a
id: REPORT-4399
type: report
title: 'Reconcile resolve conflicts: reconcile-BUNDLE-27'
created_by: xgd
created_at: '2026-09-19T11:19:30.220067+00:00'
updated_at: '2026-09-19T11:19:30.220067+00:00'
completed_at: null
last_field_updated: created_at
result: pass
fields:
  report_kind: resolve_conflicts
  subject_uid: reconcile-BUNDLE-27
---

## Files resolved

- `.xgd/tickets/hot/request-bbff35c7.md` — **UU**, class **2e** (intent/bookkeeping
  ticket: `request-*`, user-authored content, not matrix state). Resolved to the
  **ours/HEAD** side under 2e's per-fact timeline rule. Out of the sparse-checkout
  cone, so resolved via `git checkout --ours` + `git add --sparse` (DOC-986 §2/§4.1).

  Index stages: base `8d17adefbb`, ours `c1046591bd`, theirs `e26fd6afb7`.
  Theirs is exactly the post-image of the incoming commit `a61029bbb8`
  (2026-09-01, `xgd(ticket): update request request-bbff35c7`).

  **Why ours, despite incoming being the free_coded side.** Both sides are the
  same ticket on the same working timeline, and ours is the strictly later
  position on it. `git diff ab253e514e:<path> <ours blob>` is **empty** — HEAD's
  version is byte-identical to `ab253e514e` (2026-09-15), the newest working-side
  revision of this ticket. The incoming commit `a61029bbb8` is an **ancestor** of
  `ab253e514e` in that file's history, nine revisions back. HEAD received it via
  `d9781fb2c7` (2026-09-17, `seed_local_overlay`), which carries the accumulated
  later state. This is the post-watermark case: the incoming work already landed,
  refined, so the conflict resolves toward HEAD and the pick nets empty.

## Incoming changes preserved

Verified by diffing theirs against the resolved file. Every prose block the
incoming commit added is present **verbatim** in the resolution:

- the "Except that in the Worker it is not one yet" section (`R2TranscriptArchive`
  → `TicketSessionArchive`, the drop-in argument, the three consequences, the
  named costs) — identical;
- the "And on the `knowledge` component, through lagrange-framework REQ-112"
  section and its two bullets — identical;
- the three **Out of scope** additions (chat ticket's AI-maintained summary, the
  audit trail, Node's host) — identical, with ours appending a `[[REQ-171]]`
  ownership sentence;
- the two **Success criteria** additions (chat ticket carrying `session_id`; the
  cursor as a field on it) — identical;
- the whole **Decided** section replacing **Open questions** (cursor start at the
  awareness map's build timestamp; ~400-character cap with an always-exact count;
  resumption needs no separate report) — identical.

`git diff <theirs> <resolved>` removes only six lines relative to theirs:

1. Four frontmatter bookkeeping scalars — `updated_at`, `completed_at`,
   `last_field_updated`, `status`. Theirs carries `status: draft` /
   `updated_at: 2026-09-01`; ours carries `status: bundled`,
   `completed_at: 2026-09-14`, `updated_at: 2026-09-16`, plus the
   `fields.commits` list, `fields.version: 0.2.34` and
   `fields.bundled_in: bundle-8e1807f6` this bundle itself wrote. Taking theirs
   here would demote the ticket back to `draft` and drop this bundle's own
   tracking state.
2. One two-line sentence — *"Until they land, seeding and the delta channel are
   deliverable and search remains single-index."* Same fact, changed differently
   on each side, so 2e's per-fact timeline rule applies: the later working-timeline
   position supersedes it with *"The second is genuinely blocked and waits. The
   first is not, and is delivered here in the only place available today…"* plus
   the fan-out/co-ranking paragraphs. Restoring the incoming sentence would
   contradict the co-ranked-search delivery that the same author wrote two weeks
   later into the same paragraph, and would contradict the ours-side success
   criterion *"A search reaching both knowledge bases returns one list ordered by
   the component's own scores."*

No content was invented; nothing is present that was not on one side. No
`fields.intent_uid` / `story_uid` / `capability_uid` was touched.

**Net effect:** `git diff --cached HEAD` is empty. Per STEP 4 this is the
redundant-commit case (BUG-1109/BUG-1122), not a discard — STEP 3's test
distinguishes them, and it lands on "redundant": the incoming commit's key
changes are demonstrably *present* in HEAD (verbatim, listed above), not absent.
`--skip` was not called; the cherry-pick sequencer state (`CHERRY_PICK_HEAD` =
`a61029bbb8`) is left intact for `cherry_pick_finalize_resolution`.

No code, test, or UAT files were in conflict, so no BUG-1301 precedence
exception was invoked and no test function was deleted.

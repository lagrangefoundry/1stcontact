---
uid: report-b6193852
id: REPORT-3477
type: report
title: Claude reconciliation_story_generation report
created_by: xgd
created_at: '2026-09-04T03:20:51.017717+00:00'
updated_at: '2026-09-04T03:20:51.017717+00:00'
completed_at: null
last_field_updated: created_at
fields:
  report_kind: reconciliation_story_generation
  subject_uid: bundle-203b1dc2
  plan_item_index: '5'
---

Story and criteria are in place, grounded in the intent body's "What landed" half (the later, followed half) plus `apps/control-app/src/knowledge.ts`, `kb/knowledge_bases.json`, `bindKb`, and the two REQ-159 FC suites.

Scope split honoured: this story covers the corpus, tenancy, index residency, incrementality, the declaration and the landscape record; the two triggers and the enumeration floor are left to plan item 6. No code was modified.

```
Story #5 created for reconciliation bundle-203b1dc2

Story UID: story-bb91191c (STORY-130)
Title: The client's own knowledge: a private, tenant-scoped corpus the assistant can search
Type: feature
Acceptance Criteria: 9 created

Progress: 5 of 15 plan items complete
```

Details worth flagging:

- **New capability created**: CAP-107 (`capability-7cf24564`) "Project Knowledge Base: The Client's Own Corpus, Index & Landscape". No existing capability fit — CAP-100 is the shipped design-document corpus (different source, residency, lifetime and isolation rule), and CAP-106 covers where material lives, not that it is searchable. Plan item 6 should attach to CAP-107 too.
- **ACs**: AC-1518 (corpus membership, client-wide), AC-1519 (tenant isolation of rows *and* vectors, unreachable-by-argument), AC-1520 (index residency: private store, per-account prefix, absent from the public-serving store and outside the attachment key space), AC-1521 (incremental embedding, 2→1→0), AC-1522 (each host serves only KBs it can resolve — the release build no longer reports a tenant corpus as searchable and empty), AC-1523 (one declaration, parsed not paraphrased, scaffold pinned to the shipped file), AC-1524 (landscape record accepted by the store from the first build, recycled in place), AC-1525 (never-indexed account is an ordinary starting state), AC-1526 (missing embedder/private store refuses by name).
- **Three reconciliation decisions recorded** under that heading in the story body: the empty-index starting state, named refusal on missing host configuration, and one-landscape-record-recycled-in-place. All three are behaviours the intent is silent on; I formalized rather than deferred them. No intent/code contradiction was found for this item — the intent's one supersession (its own ~200-char enumeration budget, overruled by DOC-39 §7) governs the floor, which is item 6.
- **Environment note**: every `xgd ticket create` printed `Push failed (may be offline) … This proxy requires authentication`. The tickets are created and committed locally; the push to the remote did not happen.

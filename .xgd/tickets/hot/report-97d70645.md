---
uid: report-97d70645
id: REPORT-1485
type: report
title: Claude reconciliation_uat_generation report
created_by: xgd
created_at: '2026-08-06T21:38:56.013889+00:00'
updated_at: '2026-08-06T21:38:56.013889+00:00'
completed_at: null
last_field_updated: created_at
fields:
  report_kind: reconciliation_uat_generation
  subject_uid: story-af36c2cb
---

That last `BAD .` is a false positive from my extraction regex matching `split('.')` in the helper — not a test name. All 11 test functions match the enforced pattern.

## Completion Report

```
UATs generated for story story-af36c2cb (plan item 6 of 6)

Story: story-af36c2cb
Test file(s) created:
  - tests/reconciliation-edit-render-channel.test.ts

Tests written: 11
Tests passed: 11
Tests failed: 0

test_files_created:
  - "tests/reconciliation-edit-render-channel.test.ts"
```

**AC → test mapping** (1:1, all 11 ACs covered):

| AC | Test |
|---|---|
| AC-948 | same content as preview; no link target, no form action/verb, no behaviour bundle referenced *or written* to the edit dir |
| AC-949 | preview carries `opacity: 0` + `l1-rv`; edit carries neither, and the copy is a stamped `copy` region |
| AC-950 | edit sets `<body data-fc-edit>`, arming the carousel's *own* `[data-fc-edit] .carousel__track` rule (wrap + `scroll-snap-type: none`); the identical rule sits inert in preview's shared `theme.css` |
| AC-951 | derived segmentation — 5 copy, 1 image, exactly 1 container (the painted one), 2 module seams; empty seam, unpainted container and control leaf carry no stamp |
| AC-952 | exactly one `[data-l1-segment]` treatment, selected on the stamp, using `outline`/`outline-offset` and declaring no box-model property; absent from preview |
| AC-953 | all 7 page-namespace addresses resolve, kinds agree (`copy`→text, `image`→image, `container`→container, `module`→slot), all distinct |
| AC-954 | slides carry instance-rooted `0`/`1` under `data-fc-module="gallery"` + `data-l1-slot="slide"`; page namespace excludes them |
| AC-955 | siblings swapped → addresses swap with the nodes and resolve against the changed definition |
| AC-956 | preview bytes identical before/after edit renders; preview and published carry no address/stamp/marker/outline and keep href, action, `method="post"`, bundle |
| AC-957 | `id="root"` / `id="band"` emitted identically in both channels; root has the id and no stamp, band has id **and** address |
| AC-958 | own `…/acme/edit` dir; `--edit` with `source: 'latest'` settles on the draft; revision history unchanged across three edit renders |

**Two notes for the downstream review:**

1. **FC orphans remain.** `tests/req116-edit-render.test.ts` still holds 7 `test_UAT_FC_REQ-116_*` tests covering the same behaviour. I was constrained not to modify existing test files, so renaming/deleting them is the reconciliation-review step's job (the `check_fc_orphans` gate will surface them).

2. **9 pre-existing failures in the full suite**, in 5 unrelated files (`reconciliation-behavior-l1-composition`, `reconciliation-clean-page-urls`, `reconciliation-l1-control-and-texture`, `reconciliation-scaffold-starter-l1`, `reconciliation-serve-deployed-snapshot`). These are not caused by this work — my only change is one new untracked file, and `reconciliation-scaffold-starter-l1` fails identically when run in isolation. Full suite: **1131 passed, 9 failed, 9 skipped**.

No runtime code was changed; no existing test file was modified.

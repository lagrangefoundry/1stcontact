---
uid: report-3fa8537f
id: REPORT-1023
type: report
title: 'Resync resolve conflicts: d3d689184dbc45f44b278bad79f1c82fb57525b9'
created_by: xgd
created_at: '2026-07-29T04:15:33.916430+00:00'
updated_at: '2026-07-29T04:15:33.916430+00:00'
completed_at: null
last_field_updated: created_at
result: pass
fields:
  report_kind: resolve_conflicts
  subject_uid: report-510318c4
---

## Files resolved

- `package.json` — **UU**, config file, version scalar only. Kept **ours (`0.0.225`)**; discarded incoming `0.0.216`.

  Rule applied: enrichment's "take the more recent commit by timestamp", evaluated on **author** date. The committer dates are rewrite-stamped by the resync machinery itself (ours 2026-07-28 20:38:31, incoming 2026-07-28 20:43:43 — 5 min apart, both "now"), so committer-date comparison is degenerate here and must not drive the decision. On the meaningful timeline, ours (author 2026-07-28 20:38:31) postdates incoming (author 2026-07-27 12:35:36).

  This is a deliberate departure from rule 2g ("config scalar conflicts: incoming wins"). `0.0.216` is a stale intermediate from the working branch's own history: `main`, `xgd-working`, `HEAD` and the publish target `d3d68918` **all read `0.0.225`**. No branch tip holds `0.0.216`. Taking incoming would rewind a monotonic bookkeeping counter — generated by `bin/project/xgd_version_bump`, not authored developer content — and leave the tree at a version no ref agrees with while 27 picks remain. 2g exists to protect deliberate developer config; it does not apply to a counter that is strictly behind.

  No code content was at stake: `package.json` is now byte-identical to HEAD.

The remaining 7 paths were **not conflicted** — git auto-merged them and they were already staged on entry. They are listed for completeness:

- `packages/framework/src/l1/render.ts` — M
- `packages/site-schema/src/l1/schema.ts` — M
- `packages/site-schema/src/l1/types.ts` — M
- `packages/site-schema/src/l1/validate.ts` — M
- `tools/generate/src/l1/assets.ts` — M
- `tools/generate/src/l1/fold.ts` — M
- `tests/req98-uniform-surface-axes.test.ts` — A

## Incoming changes preserved

All 7 code files verified **blob-identical** to `CHERRY_PICK_HEAD` (f221aa967) via `git rev-parse "$CPHEAD:<path>"` vs `git rev-parse ":<path>"`. Every incoming change is present.

Blob-identical resolution is also the signature of silently reverting an earlier pick that touched the same file, so that was checked explicitly rather than assumed. HEAD already carries two prior picks overlapping `schema.ts` / `render.ts`:

- f4dff5057 REQ-96 `control` nodes (author 2026-07-25 22:55)
- 1b029b4a8 REQ-97 text measure (author 2026-07-27 10:07)
- f221aa967 REQ-98 surface axes (author 2026-07-27 12:35) — incoming, authored last

Incoming is authored after both, so its blobs already subsume them. Confirmed in the staged `schema.ts`, where all three generations coexist:

- L639 `l1ControlSchema` / `kind: z.literal('control')` — REQ-96 intact
- L572 REQ-97 "a text run's own **measure**" — REQ-97 intact
- L470 `l1SurfaceAxesSchema` — REQ-98, the incoming change

The earlier picks' non-overlapping files (`tools/generate/src/l1/probes.ts`, `tests/req97-text-measure.test.ts`, `tests/req96-control-composition.test.ts`) are untouched by this pick (`git diff --stat HEAD` empty). No prior pick was reverted.

## Verification

- No unmerged paths; no residual conflict markers in any tracked file.
- Net change vs HEAD is non-empty: 7 files, 609 insertions, 173 deletions.
- `CHERRY_PICK_HEAD` still present — no `--continue` / `--skip` / `--quit` / `--abort` was run.
- `package.json` parses as valid JSON.
- Targeted tests: `req98` (the incoming commit's own test) and `req97` **pass**; `req96` 21 passed / 2 failed.

  The 2 failures are **environmental and pre-existing**, not resolution-induced: `ENOENT storage/references/gigabytealchemy.ai/index/multistate.json`. That directory does not exist in this worktree and is untracked (`git ls-files` empty) — a gitignored capture bundle a fresh resync worktree never receives. The `req96` test file is not touched by the incoming commit.

## Flagged for post-merge review

Per the enrichment rule, `package.json` is flagged. The dropped `0.0.216` bump means this pick lands without a version increment. If the free-coding gate (`bin/project/xgd_version_bump --check`) is enforced against the resync branch, it may want a bump to `0.0.226` at the end of the run rather than per replayed commit.

Separately, `packages/site-schema/dist/` holds stale compiled output still exporting the removed `l1BoxAxesSchema` / `L1BoxAxes`. Source is clean of both. A typecheck run before `pnpm -r build` will report phantom errors against those names — rebuild, do not revert.

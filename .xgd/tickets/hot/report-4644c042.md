---
uid: report-4644c042
id: REPORT-4163
type: report
title: 'Reconcile resolve conflicts: reconcile-BUNDLE-27'
created_by: xgd
created_at: '2026-09-13T22:49:37.900187+00:00'
updated_at: '2026-09-13T22:49:37.900187+00:00'
completed_at: null
last_field_updated: created_at
result: pass
fields:
  report_kind: resolve_conflicts
  subject_uid: reconcile-BUNDLE-27
---

## Files resolved

- `.xgd/tickets/hot/request-26dafd83.md` (REQ-165) — **UU**, class 2e
  (intent/bookkeeping ticket: `request-*`). Single conflicted region: the
  frontmatter status block. Ours (`2cf37792fd`, *seed_local_overlay request
  request-26dafd83*, 2026-09-09) has `status: bundled` /
  `updated_at: 2026-09-09T21:32:49`; incoming (`048fba1a9d`, *update request
  request-26dafd83*, 2026-08-31) has `status: free_coding` /
  `updated_at: 2026-08-31T21:57:01`. Same fact (`status`, `updated_at`,
  `last_field_updated`) changed differently on each side, so 2e's per-fact
  later-intent rule applies: kept ours. Resolved by editing the markers out in
  place rather than `git checkout --ours`, so that any auto-merged hunk
  elsewhere in the file would have survived; staged with `git add --sparse`
  (`.xgd/tickets/` is outside the sparse-checkout cone on reconcile branches,
  DOC-986 §2/§4.1). Body of the ticket had no conflict.

## Incoming changes preserved

Yes — via the redundancy route (BUG-1109/BUG-1122), not by discard. The incoming
commit `048fba1a9d` touches this file only, and its entire diff is one
transition:

    -updated_at: '2026-08-31T21:38:31.838491+00:00'
    +updated_at: '2026-08-31T21:57:01.925843+00:00'
    -last_field_updated: created_at
    -status: draft
    +last_field_updated: status
    +status: free_coding

i.e. `status: draft -> free_coding`, nothing else.

HEAD already carries that transition's effect and everything downstream of it.
The HEAD-side commit `2cf37792fd` (`seed_local_overlay`) replays the ticket's
full lifecycle from xgd-working in one step, taking it from `draft` directly to
`bundled` and bringing with it the artifacts that only exist *because* free
coding ran and completed: `fields.commits` with three `working_sha` entries
(`52fd6302cc`, `9ae7338430`, `c2f6c582ad`), `fields.bundled_in:
bundle-87be4669`, `fields.chat_comment`, `fields.version: 0.2.31`, and the whole
`## What landed` post-implementation body section. `bundled` is strictly
downstream of `free_coding` on this ticket's status path, so replaying the
incoming hunk would not add a missing fact — it would regress `status` by nine
days and drop the ticket out of its bundle.

No code/implementation files were in this conflict, and no hunk was dropped
under the BUG-1301 precedence exception.

Staged resolution is byte-identical to HEAD (`git diff --cached HEAD` empty).
Per STEP 4 this is expected for a redundant commit and is not a @fail; the
finalize step will detect the clean staged diff and skip the commit. I did not
call `cherry-pick --continue/--skip/--quit/--abort`; CHERRY_PICK_HEAD is still
present at `048fba1a9d`.

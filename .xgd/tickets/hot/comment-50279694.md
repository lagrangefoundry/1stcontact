---
uid: comment-50279694
id: COMMENT-1315
type: comment
title: Claude response for fix_structural_validation report
created_by: xgd
created_at: '2026-08-20T14:00:51.065881+00:00'
updated_at: '2026-08-20T14:00:51.065881+00:00'
completed_at: null
last_field_updated: created_at
status: null
fields:
  subject_uid: report-9ad1c893
  kind: note
---

Report **report-9ad1c893** (REPORT-2450) filed with `needs_more_work: false`, `violations_remaining: 0`.

## This call

Closed the last four `uat-add` findings:

| AC | Test | Home |
|---|---|---|
| AC-1350 | `test_UAT_AC1350_column_is_fitted_from_content_and_rejected_unless_every_sample_reproduces` | measured-axes suite |
| AC-1351 | `test_UAT_AC1351_column_anchors_are_fitted_per_axis_with_cap_track_and_refusals` | measured-axes suite |
| AC-1348 | `test_UAT_AC1348_seam_config_derives_the_six_facts_and_records_every_gap` | new `reconciliation-l1-seam-config-and-repro.test.ts` |
| AC-1349 | `test_UAT_AC1349_repro_materializes_seams_and_localizes_every_handle` | same new file |

Each paired with its `uat_coverage` field update — 8 mutations. All 14 violations and the 1 warning from the validation report are now addressed across the three calls.

## Verification

The nine AC-named reconciliation suites: **42 passed** (the assessor's original 34 plus the 8 added). Both new files typecheck clean.

**Two failures I need to flag rather than bury.** A wider sweep including the FC source suites came back `2 failed | 106 passed | 3 skipped`. Both failures are in `tests/req88-form-labelling-and-submit.test.ts` — a file this work never touched — and both fail with `listen EPERM: operation not permitted 0.0.0.0` from `tools/generate/src/cli/serve.ts:53`. This session's sandbox denies binding to `0.0.0.0`; suites that bind `127.0.0.1` explicitly all pass in the same run. It's an environment limitation I can't resolve from inside the sandbox, not a code or test defect — but the sweep isn't green, so I'm not calling it green.

## On non-vacuity

Several of these clauses pass trivially if written carelessly, so each got a discriminating contrast — notably AC-1350's "a column nothing refers to is not emitted" (the same page whose run keeps its identity *does* carry the column) and AC-1351's three width refusals (each also asserts the `x` anchor survives, so "no width anchor" can't pass by there being no anchor at all).

One assertion I wrote was genuinely vacuous and I fixed it before finalizing: the channel-separation check filtered residuals on `kind === 'control'`, which isn't a member of `FoldResidual['kind']`, so it could never have failed. It now filters on `'field'` — the kind an un-foldable control actually produces.

No code edits across any of the three calls; no `needs_review` items.

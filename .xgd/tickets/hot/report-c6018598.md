---
uid: report-c6018598
id: REPORT-1287
type: report
title: 'Capability-Intent Alignment: reproduction-module-treatments (level=ac)'
created_by: xgd
created_at: '2026-08-05T18:32:52.430542+00:00'
updated_at: '2026-08-05T18:32:52.430542+00:00'
completed_at: null
last_field_updated: created_at
result: pass
fields:
  report_kind: capability_validation
  subject_uid: capability-938f26ec
  level: ac
  violations: 0
  warnings: 3
  needs_review_count: 0
---

# Capability-Intent Alignment: reproduction-module-treatments
# Level: ac

**Result**: PASS
**Violations**: 0
**Warnings**: 3
**Needs review**: 0

## Headline

CAP-69 (`capability-938f26ec`) was **absorbed into CAP `capability-ae9d65d6`
(Framework Substrate: L1 Layout, Values & Behavior Modules) on 2026-08-05** by the
structural rebalance recorded in REPORT-1266 (`report-bdaf6840`, subject =
this run's anchor `report-31234d67`). Its single story STORY-82 was reassigned;
the ticket's own frontmatter now reads `capability_uid: capability-ae9d65d6`
(`last_field_updated: capability_uid`, 2026-08-05T17:24Z).

**CAP-69 therefore holds zero in-scope stories and, transitively, zero in-scope
ACs.** The ac-level consistency / coverage / exclusivity checks are vacuously
satisfied for this capability. No intent is orphaned: the reproduction-treatment
behaviours BUNDLE-6 and BUNDLE-7 asked for are expressed by STORY-82 +
AC-718/AC-719, which now live under `capability-ae9d65d6` and will be validated
in that capability's cycle.

The `xgd ticket list --filter fields.capability_uid=capability-938f26ec` query
still returns STORY-82. That is the **stale-index defect** REPORT-1266 documents
as its blocking issue, not live matrix drift — see Warning 1.

## Cumulative Intent Considered

| Intent ID | Status | When | Asked / changed | Counts? |
|---|---|---|---|---|
| BUNDLE-6 (`bundle-ab9e0cb6`) — REQ-58+59+62+61 | free_and_reconciled | 2026-07-17 | gigabytealchemy reproduction pass 3; forced the card veil/border, placeholder/inline contact-form, and footer copyright/colour treatments onto the framework as per-module dials on `services-grid` / `contact-form` / `footer`. Created CAP-69 + STORY-82 + AC-674..681. | YES |
| BUNDLE-7 (`bundle-31e474b9`) — REQ-63+79+82+83+84+2 more | free_and_reconciled | 2026-07-22 | The REQ-79 framework pivot. **REQ-84** deleted `services-grid`/`footer` (+ `header`/`hero`/`text-block`/`layer` and ~20 dials), re-homing their visual look onto L1 leaf axes. **REQ-85** reframed `contact-form` into a capability module (typed config + named L1 slots; field labelling demoted from dial to core a11y obligation). Superseded AC-674..681; authored AC-718/AC-719 in their place. | YES (supersedes) |

Both intents are `free_and_reconciled` and count. BUNDLE-7 supersedes BUNDLE-6's
*delivery mechanism* while preserving the *treatments* — the capability and story
bodies both state this correctly.

The 2026-08-05 rebalance (REPORT-1266) is a **structural** operation, not an
intent: it moved stories between capabilities without altering story content
("No story content was modified — only `capability_uid`"). It adds no behaviour
to the ledger.

## Alignment Ledger

| Element | Intents aligned to | Outcome |
|---|---|---|
| CAP-69 `capability-938f26ec` | BUNDLE-6 (origin), BUNDLE-7 (`updated_by`) | **empty by design** — absorbed 2026-08-05 into `capability-ae9d65d6`; body carries an accurate ABSORBED banner and `merged_into` field. Zero stories → zero ACs in scope. |
| STORY-82 `story-46e3b3c7` | BUNDLE-6, BUNDLE-7 | **no longer in this capability** — reassigned to `capability-ae9d65d6`. Body correctly describes the REQ-84/REQ-85 supersession. |
| AC-719 `acceptance_criterion-da7c62ec` | BUNDLE-7 (REQ-84) | out of scope here (follows STORY-82). Courtesy read: covers card/band veil + border and footer copyright/text/link colour via L1 leaf axes; matches STORY-82 surface (a). `uat_coverage: pass`. |
| AC-718 `acceptance_criterion-f3328e22` | BUNDLE-7 (REQ-85) | out of scope here (follows STORY-82). Courtesy read: covers contact-form presentation via capability config + `submit`/`intro` L1 slots, labelling as core obligation; matches STORY-82 surface (b). `uat_coverage: pass`. |
| AC-674..681 (8 ACs) | BUNDLE-6, retired by BUNDLE-7 | correctly **archived** (absent from the live AC query; retrievable only with `--archived`). Describe the deleted module dials (`cardVeil`, `cardBorder`, `fieldLabels=placeholder`, `submitInline`, `submitColor`, footer `copyright`/`textColor`/`linkColor`). See Warning 3 on their residual status string. |

## Findings

| # | Severity | Property | Element | Resolution category | Issue | Suggested edit |
|---|---|---|---|---|---|---|
| 1 | warning | consistency | index / `capability-938f26ec` | code-issue | `xgd ticket list --type story --filter fields.capability_uid=capability-938f26ec` returns STORY-82, but STORY-82's frontmatter reads `capability_uid: capability-ae9d65d6`. STORY-82 is simultaneously returned for **both** capability keys — a leftover index entry, not a live link. REPORT-1266 names the mechanism: on a branch worktree the ticket index resolves to the canonical main store, which still holds pre-merge `capability_uid` values; `xgd ticket rebuild-index` refuses on branch worktrees by design. | No matrix edit. Any validation scoped by index-derived capability membership is unreliable on this worktree; re-derive membership from ticket frontmatter (as this check did) until the index is rebuilt on main. |
| 2 | warning | consistency | CAP-69 `capability-938f26ec` | — | Capability `status` is still `active` despite holding zero stories and carrying `merged_into: capability-ae9d65d6`. REPORT-1266 documents this as a known blocked step: `reject_deprecation_if_capability_has_stories` → `attached_story_ids()` reads the same stale index and reports phantom attached stories. | Flip to `status: deprecated` in a later run once the index is correct on main. The body banner + `merged_into` field already make the state unambiguous in the interim. |
| 3 | warning | consistency | AC-674..681 (archived) | — | The eight superseded module-dial ACs are correctly archived, but each retains `status: active` in its frontmatter. STORY-82's body describes them as "archived as superseded". Archival already removes them from the live matrix, so this is a stale status string, not live drift. | Opportunistic: set the archived ACs to a terminal status (e.g. `deprecated`) so status agrees with archival. Not required for this level to pass. |
| 4 | info | coverage | CAP-69 | — | Zero in-scope stories/ACs. Both ledger intents' behaviours remain expressed under `capability-ae9d65d6` (STORY-82 + AC-718/AC-719), so absorption created no intent gap. | none |
| 5 | info | exclusivity | AC-718 vs AC-719 | — | The two surviving ACs partition STORY-82's surface cleanly — AC-719 = L1 leaf axes (card/band + footer), AC-718 = contact-form config + L1 slots. No overlap. | none |

## Notes for the Editor

**This level passes vacuously, and that is the correct result — but it means the
real ac-level validation for these behaviours has not happened here.** AC-718 and
AC-719 must be validated as part of `capability-ae9d65d6`'s cycle, alongside the
four other stories now under it (STORY-80, STORY-81, STORY-83, STORY-85). If the
run plan was generated from a pre-rebalance snapshot, it may still be scoped to
the eight absorbed capabilities; those will all pass vacuously while the three
surviving capabilities carry the entire matrix.

**The stale-index defect (Warning 1) is the single most consequential item
here.** REPORT-1266 additionally reports that `xgd ticket list --type capability`
returns 22 entries for 11 capabilities on this worktree, so
`assemble_capability_tree()` double-counts. Any structural health check re-run on
this branch is unreliable regardless of what this alignment check concludes. The
fix is not a matrix edit — it is rebuilding the index on main.

Two courtesy observations for whoever validates `capability-ae9d65d6`:

- STORY-82's `## Story Points` section reads `2` while its `fields.story_points`
  is `3`. Cosmetic, but the body and field disagree.
- STORY-82 is `story_kind: upgrade`, so it is correctly expected to carry ACs
  (it does, two of them, both `uat_coverage: pass`).

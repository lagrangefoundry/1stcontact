---
uid: comment-30958191
id: COMMENT-834
type: comment
title: Claude response for capability_validation report
created_by: xgd
created_at: '2026-08-07T23:47:03.236611+00:00'
updated_at: '2026-08-07T23:47:03.236611+00:00'
completed_at: null
last_field_updated: created_at
status: null
fields:
  subject_uid: report-23c1e31b
  kind: note
---

**REPORT-1662** (`report-23c1e31b`) — **FAIL**: 2 violations, 2 warnings, 0 needs_review.

## What I checked

CAP-71 now holds **32 ACs** across STORY-84 (fold) and STORY-86 (gate + cross-gate) — eight more than the last uat cycle saw. Every one has exactly one `test_UAT_AC<n>_*` test driving a real entry point. I ran all 32: **6 files, 32 passed, 1.28s**. There is no `uat-add` gap and no duplicate coverage.

## The two violations — both re-raises, both never repaired

1. **AC-691 / `test_UAT_AC691`** — the AC's height split ("a box/image leaf pins its height; a text leaf's keyframes carry no height") has **no assertion at all** in its UAT. Grepping the tree, the text-leaf "no height" invariant has zero executable evidence anywhere. That invariant is what makes AC-707's content-robustness probe meaningful — if a regression started pinning text heights, all 32 UATs would still pass.
2. **AC-689 / `test_UAT_AC689`** — the full-language clause (REQ-92) is unexercised: `FakeDriver`'s signals carry one text run and `images: []`, so the folded document has exactly one leaf kind.

Both were filed as REPORT-1320 violations 1–2 on 2026-08-05; `tests/reconciliation-l1-fold.test.ts` has not been touched since 2026-07-22, so no repair was attempted. Both fixes land in one file.

**Warnings**: AC-694's substantive sidecar assertions still sit behind a chromium skip that genuinely skips (verified: playwright wants build 1228, cache holds 1234) — fix by moving the *contract* assertions onto the canned-driver path, not by deleting the skip. AC-812 proves the backdrop paints behind text but never asserts it sits *after* its band boxes, the mirror-image failure.

## Cascade, recorded but not filed as UAT work

The ac-level cycle failed this morning (REPORT-1658) and closed without repair, so AC-691's typography sentence, AC-731's per-run surface model, AC-736's over-broad wording and the missing pinned-box-overflow AC are all still stale. I escalated to intent + code for those rather than grading tests against known-bad bodies: **AC-731's UAT is right and its AC is wrong** (the test was updated for BUG-14; the AC still describes what BUG-14 removed). Authoring UAT work there now would encode fixed bugs or trace to no matrix element.

Also resolved REPORT-1658's finding 12: the eight ACs missing a `uat_coverage` field all do have substantive, passing UATs — that's bookkeeping, not a coverage gap.

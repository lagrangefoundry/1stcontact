---
uid: report-06793c20
id: REPORT-3388
type: report
title: 'Reconcile resolve conflicts: reconcile-BUNDLE-23'
created_by: xgd
created_at: '2026-09-03T22:51:25.927023+00:00'
updated_at: '2026-09-03T22:51:25.927023+00:00'
completed_at: null
last_field_updated: created_at
result: pass
fields:
  report_kind: resolve_conflicts
  subject_uid: reconcile-BUNDLE-23
---

## Files resolved

- `.xgd/tickets/hot/bundle-8eef3846.md` — class **AA** (both added); intent/bookkeeping ticket (bundle-*), so rules **2b** (both added) + **2e** (intent/bookkeeping ticket) apply, plus the auto-enrichment rule "intent unknown on one or both sides → take the more recent commit by timestamp and flag for post-merge review".
  - **Resolution: kept OURS (HEAD side)**, staged via `git checkout --ours` + `git add --sparse`.
  - **Why:** the two sides are the same ticket at two points on its own lifecycle, not competing edits.
    - Incoming (`c2efcb99cb5d64cd96b21d45966ce25fa48d6e58`, `xgd(ticket): create bundle bundle-8eef3846`, 2026-08-30 22:05:09 -0700) is the *creation* snapshot: 369 lines, `status: ready_to_reconcile`, `last_field_updated: created_at`, `completed_at: null`, two `commits` entries still carrying un-reconciled `working_sha`s.
    - Ours (`2ca3de8c4964d32a23287ce38381e53b2267a3e8`, `xgd(ticket): update bundle bundle-8eef3846`, 2026-08-31 17:00:08 -0700) is the *completed* state: 692 lines, `status: free_and_reconciled`, `result: pass`, `completed_at` set, `merged_at_commit: 90527353c0fa4b9fd3ae91ba6285c7d791a25c53`, working shas remapped into `main_sha` plus the full `orphan_commits` remap table.
  - Ours is later by ~19h **and** a strict content superset. Taking the incoming side would have reverted operator-owned lifecycle state (`free_and_reconciled` → `ready_to_reconcile`, dropping `result: pass`, `merged_at_commit`, and the entire `orphan_commits` remap table) — a silent revert, not a merge.
  - No hand-composition was performed: no field, section, or fact exists on the incoming side that is absent from ours. Nothing was invented.

## Incoming changes preserved

- `.xgd/tickets/hot/bundle-8eef3846.md`: **fully preserved.** `git show c2efcb99 -- <file>` shows the commit adding the file whole (369 insertions, 0 deletions). Diffing the incoming blob against the resolved file, the **entire markdown body is byte-identical** — every line from `# Bundle` through the closing `Done, three UATs` is present in the resolution. The only differences are 11 frontmatter lines, and in each case ours holds a strictly *later* value for the same field along the same lifecycle (`updated_at`, `completed_at`, `last_field_updated`, `status`, and the `commits` working_sha→main_sha remap), plus a trailing-newline artifact.
- This is the redundant-commit case described in STEP 4 (BUG-1109 / BUG-1122), **not** a STEP 3 discard: the incoming commit's key change (the ticket body) is present in HEAD via a later route — the same create followed by its own subsequent updates already landed on this branch. Applying STEP 3's discriminator, the content is *present*, not *absent*. The staged diff vs HEAD is therefore empty; per STEP 4 this is staged and exited @done without calling `--skip`, leaving the skip decision to `cherry_pick_finalize_resolution`.
- No BUG-1301 precedence exception was invoked; no hunk was dropped.
- No code, test, or UAT files were involved in this conflict.

## Flagged for post-merge review

Per the enrichment rule for unknown-intent sides: `.xgd/tickets/hot/bundle-8eef3846.md` (BUNDLE-22) is flagged. Specifically, ours collapses the incoming side's two `commits` entries (`working_sha: 759cd87405a4b50f81995b2c9b510bf23be54fbd`, `working_sha: 29c0e86dd321b509e06f0dd9e531392ee9190b0e`) into a single entry with `working_sha: null` / `main_sha: 90527353c0fa4b9fd3ae91ba6285c7d791a25c53`. This is xgd's own reconcile bookkeeping for an already-merged bundle and was written by the HEAD-side `xgd ticket update`, so it was left as-is rather than hand-edited — but a reviewer may want to confirm those two working shas were intentionally consumed by the remap.

## Git state

Cherry-pick left in progress as required: `CHERRY_PICK_HEAD` still present. No `--continue` / `--skip` / `--quit` / `--abort` / `reset` was run. `git diff --name-only --diff-filter=U` is empty; no conflict-class lines remain in `git status --porcelain`.

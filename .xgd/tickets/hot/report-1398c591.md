---
uid: report-1398c591
id: REPORT-2733
type: report
title: 'Reconcile resolve conflicts: reconcile-BUNDLE-20'
created_by: xgd
created_at: '2026-08-31T06:04:50.143985+00:00'
updated_at: '2026-08-31T06:04:50.143985+00:00'
completed_at: null
last_field_updated: created_at
result: pass
fields:
  report_kind: resolve_conflicts
  subject_uid: reconcile-BUNDLE-20
---

## Files resolved

- `.xgd/tickets/hot/request-90edd177.md` — **AA** (both added), intent/bookkeeping
  ticket (`request-*`, REQ-62). Rule **2e** — "one side is a strict superset of
  the other: keep the superset." Resolved via `git checkout --theirs` +
  `git add --sparse` (path is under `.xgd/tickets/`, outside the sparse-checkout
  cone on a reconcile branch — DOC-986 §2/§4.1).

  The two sides are byte-identical except for a single added line on the incoming
  (free_coded) side inside `fields:`:

  ```
  +  chat_comment: comment-16619544
  ```

  This is a field the HEAD side never touched — no competing fact, so no
  per-fact timeline lookup (`xgd working-timeline`) was required. The enrichment
  metadata's "intent unknown on one or both sides → take the more recent commit
  and flag for review" fallback did not need to be exercised: the sides do not
  actually compete. Flagging for post-merge review anyway, per that rule, though
  the delta is a single bookkeeping field.

  No `fields.intent_uid` / `story_uid` / `capability_uid` were modified, and no
  content absent from both sides was introduced.

## Incoming changes preserved

- `.xgd/tickets/hot/request-90edd177.md` — **fully preserved, byte-exact.** The
  staged blob is `f551f18925fc3055fb5cbac523724fdc20a03391`, which is precisely
  stage-3 (theirs) from the index. Incoming commit
  `6fa40531790ead92dc42d0a2799a90b4f859e77b` ("xgd(ticket): update request
  request-90edd177") touches only this one file; its entire content is present in
  the resolution verbatim. Nothing from the incoming side was dropped.

  The HEAD side contributed no content the incoming side lacks (the incoming
  version is a strict superset), so combining lost nothing from `ours` either.

No code/implementation files were in conflict — the cherry-picked commit is a
ticket-only update. No hunks were dropped, so the BUG-1301 precedence exception
was not invoked. No UAT test functions were touched or deleted.

Cherry-pick sequencer state left intact (`CHERRY_PICK_HEAD` =
`6fa40531790ead92dc42d0a2799a90b4f859e77b`) for
`cherry_pick_finalize_resolution`. No `--continue`/`--skip`/`--quit`/`--abort`,
`reset`, or `checkout <branch>` was run.

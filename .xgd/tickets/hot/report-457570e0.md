---
uid: report-457570e0
id: REPORT-798
type: report
title: 'Capability-Intent Alignment: L1 Layout Substrate + Safety Envelope (level=uat)'
created_by: xgd
created_at: '2026-07-23T07:50:45.605259+00:00'
updated_at: '2026-07-23T07:50:45.605259+00:00'
completed_at: null
last_field_updated: created_at
result: pass
fields:
  report_kind: capability_validation
  subject_uid: capability-ae9d65d6
  level: uat
  violations: 0
  warnings: 0
  needs_review_count: 0
---

# Capability-Intent Alignment: L1 Layout Substrate + Safety Envelope
# Level: uat

**Result**: PASS
**Violations**: 0
**Warnings**: 0
**Needs review**: 0

## Cumulative Intent Considered

The capability (CAP-70) has a single story, STORY-83 (feature), whose
`intent_uid` is **BUNDLE-7 (bundle-31e474b9)**. The bundle reconciles
REQ-63 + REQ-79 + REQ-82 + REQ-83 + REQ-84 (+2 more) and is
`free_and_reconciled` (`merged_at_commit`
edeb1c2c00d0c194a45cf590bed3641a6cf82d60) — it counts fully toward
cumulative intent. REQ-82 is the load-bearing intent for this
capability's UAT surface (the L1 substrate: typed shape, envelope
validator, safe emitter, geometry keyframes, round-trip + cross-browser
gates).

| Intent ID | Status | When | Asked / changed | Counts? |
|---|---|---|---|---|
| BUNDLE-7 (REQ-82 et al.) | free_and_reconciled | merged @edeb1c2c | L1 substrate: typed tree, envelope validator, safe renderer, geometry keyframes, round-trip + cross-browser fidelity gates | YES |

Retired-behaviour context (from STORY-83 body, already reconciled): the
former per-breakpoint module length dials and header `navCollapse` dial
were deleted by REQ-79; responsive-across-widths intent is re-homed onto
L1 geometry keyframes. STORY-81 (archived) and its sole AC-717 were
merged here; per REPORT-795 the AC-level dedup collapsed AC-717 into
AC-684, archived it, and retired the duplicate test file
`tests/reconciliation-responsive-keyframes.test.ts`. Confirmed: that file
no longer exists; the behaviour is covered by
`tests/reconciliation-l1-substrate.test.ts`.

## Alignment Ledger

Level = uat, so AC bodies are the working reference. Each active AC and
its UAT:

| Element (AC) | UAT | Outcome |
|---|---|---|
| AC-682 well-formed doc accepted | test_UAT_AC682_valid_document_and_optional_primitives_accepted | aligned — calls real `validateL1`; asserts the hero validates and each optional primitive (hug/fluid/fixed sizing, row/grid + distribution/align, slot, relative-src image, viewport visibility, snap segment) is accepted |
| AC-683 authored axes round-trip at all widths | test_UAT_AC683_type_a_axes_reproduced_and_text_present_at_all_widths | aligned — real-browser `captureL1` over the 6-width ladder; asserts empty Type-A delta set + both text runs present at every width; skips cleanly without an engine |
| AC-684 geometry keyframes interpolate/snap | test_UAT_AC684_interpolate_varies_continuously_and_snap_holds | aligned — real `renderL1Document`; asserts the emitted interpolate `calc()` formula (continuous variation), pinned endpoints, media-query breakpoints, and that a snap track holds the lower keyframe with no `calc()` |
| AC-685 injection payloads inert | test_UAT_AC685_text_url_alt_and_fontfamily_payloads_are_neutralised | aligned — real renderer; asserts escaped `<script>`, escaped alt attribute-breakout, dropped `javascript:` src, and CSS-syntax font-family reduced to inert tokens (no `@import`/`body{…}`) |
| AC-686 out-of-range/oversize/freeform rejected | test_UAT_AC686_envelope_boundary_is_the_range_not_the_property | aligned — positive control + 12 rejection cases matching every enumerated envelope rule (font-size/weight range, geometry coord ±100k, non-finite, non-hex colour, disallowed image scheme, unknown-key `style`, undeclared/non-ascending keyframe widths, non-ascending ladder, depth cap 32, node cap 2000) |
| AC-687 full list of per-field errors | test_UAT_AC687_multiple_violations_all_reported_with_path_and_message | aligned — submits 3 simultaneous violations; asserts an errors array of ≥3, each with a `/`-rooted path + non-empty message, and the three expected paths present |
| AC-688 cross-browser equivalence | test_UAT_AC688_no_layout_divergence_across_three_engines | aligned — real `captureL1` on chromium/webkit/firefox; asserts a projection per engine, wordmark on all three, position/width spread within `X_BROWSER_TOLERANCE`, and one identical font-size; skips cleanly unless all three engines are installed |

## Findings

| # | Severity | Property | Element | Resolution category | Issue | Suggested edit |
|---|---|---|---|---|---|---|
| 1 | info | coverage | all 7 ACs (AC-682…AC-688) | — | Every active AC has exactly one substantive UAT exercising a real entry point (`validateL1` / `renderL1Document` / real-browser `captureL1`), never an AST/structural stand-in. Full file run: 7 passed, 0 failed, 0 unexpected skips. | none |
| 2 | info | exclusivity | AC-683 vs AC-688 | — | Both use real-browser capture but verify distinct properties (single-engine round-trip fidelity vs cross-engine agreement) — different test shapes, not duplicates. | none |
| 3 | info | consistency | AC-684 | — | The AC's "confirm with a real-browser capture (~2px)" clause is not asserted as a separate captured probe; the UAT proves continuous-variation/snap behaviour via the actual emitted `calc()` formula (a real-renderer entry point, not a structural check), and cross-engine captured positioning is independently covered by AC-688. Behavioural claim is substantively covered. | none |

## Notes for the Editor

Clean UAT level for this capability. Single feature story, seven active
behavior ACs, seven substantive UATs, all green. The STORY-81/AC-717
overlap-cluster dedup (REPORT-795) has been fully carried out — archived
AC, retired duplicate test file, provenance folded into AC-684 — with no
residual stale references at the UAT layer. The engine-gated probes
(AC-683, AC-688) use `it.runIf(engineAvailable)` and skip cleanly rather
than fail on an engine-less runner, matching each AC's stated verification
contract.

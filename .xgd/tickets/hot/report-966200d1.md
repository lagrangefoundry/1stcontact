---
uid: report-966200d1
id: REPORT-2878
type: report
title: 'Reconcile resolve conflicts: reconcile-BUNDLE-20'
created_by: xgd
created_at: '2026-08-31T09:02:17.182795+00:00'
updated_at: '2026-08-31T09:02:17.182795+00:00'
completed_at: null
last_field_updated: created_at
result: pass
fields:
  report_kind: resolve_conflicts
  subject_uid: reconcile-BUNDLE-20
---

## Files resolved

- `.xgd/tickets/hot/request-70217652.md` — class **AA** (both added, no merge base:
  index had stages 2 and 3 only). Intent/bookkeeping ticket (`request-*`), so rule
  **2e** applies, via 2b's superset clause. Both sides are 284/285 lines and differ
  by exactly one line: the incoming (`free_coded`, d4caffdb) side adds
  `fields.chat_comment: comment-940eb976` inside the frontmatter. HEAD touches no
  field the incoming side lacks, so incoming is a strict superset — kept it whole.
  Resolved with `git checkout --theirs` + `git add --sparse` (path is outside the
  sparse-checkout cone, DOC-986 §2/§4.1).

  Note on the enrichment metadata: it flagged intent as unknown on one or both
  sides and prescribed "take the more recent commit by timestamp, flag for
  post-merge review." That rule was not needed here — the two sides are not
  competing on any fact, so there is nothing for a timeline tiebreak to decide.
  The superset resolution keeps every fact from both sides.

## Incoming changes preserved

- `.xgd/tickets/hot/request-70217652.md` — the incoming commit d4caffdb is a
  whole-file add (286 insertions, 1 file changed). The staged blob is
  `cbb47104ce2deff69053ebbe1f94ddd70dffcf59`, which is byte-identical to
  `d4caffdb:.xgd/tickets/hot/request-70217652.md`. Every incoming line is present,
  including the sole distinguishing field `chat_comment: comment-940eb976`.
  Working tree contains zero conflict markers.

No hunks were dropped; the BUG-1301 precedence exception was not invoked. No code,
test, or UAT files were involved in this conflict.

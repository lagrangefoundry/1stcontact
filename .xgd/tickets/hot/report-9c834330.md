---
uid: report-9c834330
id: REPORT-3713
type: report
title: 'Capability-Intent Alignment: Builder Workspace: Chrome, Origin & Display Panel
  (level=ac)'
created_by: xgd
created_at: '2026-09-10T10:10:49.131487+00:00'
updated_at: '2026-09-10T10:10:49.131487+00:00'
completed_at: null
last_field_updated: created_at
result: pass
fields:
  report_kind: capability_validation
  subject_uid: capability-a994b8f3
  level: ac
  violations: 0
  warnings: 1
  needs_review_count: 0
---

# Capability-Intent Alignment: Builder Workspace: Chrome, Origin & Display Panel
# Level: ac

**Result**: PASS
**Violations**: 0
**Warnings**: 1
**Needs review**: 0

Attempt 6 (five prior fix attempts). The single violation and both warnings of
`report-dcfe6d0a` — AC-978's stale served-tree enumeration, AC-964 vs AC-1400,
AC-972 vs AC-1035 — were verified as repaired against the current ticket bodies
and against the code, not taken from `report-c47daafa`'s claim. None is
re-raised. Coverage of the STORY-99 body was re-derived bullet by bullet and is
complete. One warning is new and does not gate the level.

## Cumulative Intent Considered

Story-level alignment was validated today (`report-e9695d41`, PASS), so per the
level cascade the STORY-99 body is the working reference below. The ledger is
carried forward from `report-dcfe6d0a`; statuses were re-read this run and are
unchanged.

| Intent ID | Status | When | Asked / changed | Counts? |
|---|---|---|---|---|
| REQ-119 (`request-64864801`) | free_and_reconciled | 2026-07-31 | Request-time draft and edit renders; the draft-side channels stop being artifacts read off a shelf | YES |
| BUNDLE-16 (`bundle-15c1f647`) = REQ-117 + REQ-115 + REQ-44 | free_and_reconciled | 2026-08-07 | Originating intent: the workspace chrome, the display panel, the component-consumption route | YES |
| BUG-33 (`bug-ede1fb8c`) | free_and_reconciled | 2026-08-08 | Toolbar re-derivation; a replaced control is a detached survivor (→ AC-1110) | YES |
| REQ-145 (`request-b474390f`) | free_and_reconciled | 2026-08-15 | control-app becomes the builder. Proxy deleted; the Worker *is* the origin. `/builder/*`, `/webui/*`, `/framework/*.js` become build artifacts | YES |
| REQ-146 (`request-0cdfdc5b`) | free_and_reconciled | 2026-08-15 | The AI host moves into workerd; `/api/ai/*` graduates from its 501 | YES |
| REQ-147 (`request-23fd6e61`) | free_and_reconciled | 2026-08-15 | Cloudflare Access in front of the deployed origin — the admission qualifier | YES |
| REQ-149 (`request-554ac441`) | free_and_reconciled | 2026-08-17 | Publish in the cloud: revisions and rendered output without a filesystem | YES |
| BUNDLE-21 (`bundle-78f4e2fe`) = BUG-36 + BUG-37 + BUG-38 | free_and_reconciled | 2026-08-26 | One store opener and cold-start registration (→ AC-1449, narrowed AC-965, AC-1402); render cache dead at the deployed origin as an accepted trade (→ AC-1033) | YES |

**Provenance caveat, fifth time recorded.** Neither CAP-85 nor any of its 36 ACs
carries an `intent_uid`; `updated_by` on STORY-99 is a scalar holding only
`bundle-78f4e2fe`. This ledger is reconstructed each cycle from creation/update
windows, the story's own Reconciliation Decisions blocks, and the intent bodies.
Backfilling those fields is an operator decision, not an ac-level fix.

## Alignment Ledger

36 active ACs on STORY-99; no archived or deprecated ACs exist for this story
(checked with `--archived`, which returns the same 36). STORY-99 is
`story_kind: upgrade`, so it is expected to carry ACs.

| Element | Intents aligned to | Outcome |
|---|---|---|
| AC-959, AC-975, AC-976 — one tab, filling the window, every declared tab option honoured | REQ-115 | aligned |
| AC-960 — one definition site per shown name, and for the component scope | REQ-115, REQ-145 | aligned; the bounded browser-source exception matches STORY-99 verbatim |
| AC-961 — components byte-identical to the installed copy, never vendored | REQ-115, REQ-145 | aligned; the verbatim build copy preserves the byte-identity claim |
| AC-962 — a missing component names itself and the install command | REQ-115, REQ-145 | aligned in substance; opening clause names a superseded trigger — warning 1 |
| AC-963 — references through each component's declared entry points | REQ-115, REQ-145 | aligned |
| AC-964 — one origin for an admitted caller, nothing reinterpreted | REQ-115, REQ-145, REQ-147 | aligned — deferral to AC-1400 landed, see finding 2 |
| AC-965 — no account named / account deactivated, as distinct failures | REQ-145, BUG-36 (narrowed) | aligned |
| AC-966 — the ordinary mode displays the selected site's own rendering | REQ-115 | aligned |
| AC-967, AC-968, AC-969, AC-1029 — selector, mode switching, mode registration, the editable mode | REQ-115, REQ-117 | aligned |
| AC-970, AC-1110 — the strip is derived state; a replaced control is inert | REQ-115, BUG-33 | aligned |
| AC-971 — open-in-a-new-tab targets the displayed document | REQ-115 | aligned |
| AC-972 — publish through the existing path, for the displayed site | REQ-115, REQ-149 | aligned — narrowed to the invocation half, see finding 3 |
| AC-973, AC-974 — the split, and what it remembers | REQ-115 | aligned |
| AC-977 — every response non-cacheable, stamped by the route table | REQ-119, REQ-145 | aligned |
| AC-978 — no request escapes any served tree | REQ-115, REQ-145 | aligned — enumeration repaired, see finding 1 |
| AC-979 — unknown channel / unknown component answered as not found | REQ-115 | aligned; speaks of channels and components as request targets, not as trees, so REQ-145 did not invalidate it |
| AC-1030 — the components consumed are the repository's own, from any working tree | REQ-115 | aligned |
| AC-1031, AC-1032 — channels answer with no artifact on disk; one render backs writer and reader | REQ-119 | aligned |
| AC-1033 — a definition changed outside shows on the next request; two workspaces never share a rendering | REQ-119, amended by BUG-37 | aligned; the withdrawn equivalence claim is gone and the accepted trade is stated |
| AC-1034, AC-1035 — invalid draft reported in the pane; `published` from publish time only | REQ-119, REQ-149 | aligned |
| AC-1036 — the same addresses resolve; nothing outside the channel | REQ-119, REQ-145 | aligned — deferral text now matches the repaired AC-978, see finding 4 |
| AC-1399, AC-1400, AC-1401, AC-1402, AC-1403 — no local process; build artifacts behind the gate; the transport; copy-up; the boot guard | REQ-145, REQ-147, BUG-36 | aligned |
| AC-1449 — cold start registers the one configured account | BUG-36 | aligned |

**Coverage of the STORY-99 body, re-derived bullet by bullet this run — complete.**
Single origin at the edge runtime → AC-964, AC-971, AC-1399; admitted caller
only → AC-964, AC-1400; components consumed not copied, and assembled as build
artifacts → AC-961, AC-962, AC-963, AC-1030, AC-1400; scope written once →
AC-960; one tab filling the window → AC-959, AC-975, AC-976; panel modes →
AC-966, AC-968, AC-969, AC-1029; toolbar as derived state → AC-970, AC-967,
AC-971, AC-972, AC-1110; draft-side channels produced on request → AC-1031,
AC-1032, AC-1033, AC-1034; published not derived from the draft → AC-1035;
two front doors → AC-1401; copy-up → AC-1402; the split and its persistence →
AC-973, AC-974; freshness → AC-977; confinement → AC-978; the boot guard →
AC-1403; configuration failures → AC-965, AC-1449; checkout anchoring → AC-1030;
channel addressing → AC-1036, AC-979.

**The story's deliberate non-criteria were also checked, and no AC asserts one.**
STORY-99's Reconciliation Decisions record five things that must *not* appear in
the matrix: the 501-shaped deferral (no live instance), the proxy and its
forwarding variable (never had a matrix subject), a deploy-time account seed
(COMMENT-1435, declined), the interim production `INSERT`, and BUG-37's
assembled-definition memo (owned by AC-1447/AC-1448). None is asserted by any of
the 36 ACs; AC-1033 and AC-1449 each explicitly point the reader elsewhere.

## Findings

| # | Severity | Property | Element | Resolution category | Issue | Suggested edit |
|---|---|---|---|---|---|---|
| 1 | info | consistency | AC-978 (`acceptance_criterion-53c66f17`) | — | **Violation 1 of `report-dcfe6d0a` is confirmed repaired, verified against the code rather than against the fix report.** AC-978 (updated 2026-09-10T10:01:36) now enumerates two served classes — the rendered channels, and the built-artifact tree under its three prefixes `/builder/*`, `/webui/*`, `/framework/*.js` — and names `/framework/*.js` as the prefix carrying the edit client. That matches what ships: `apps/control-app/src/router.ts:45-46` declares the three prefixes as build artifacts served by the assets binding; `tools/generate/src/cli/builder.ts:148-168` resolves every one of them through a single `resolveStaticFile(dir, url.pathname)` over one `assetsDirOf(ctx)` root (`:181-185`). The identical-outcome clause was kept and correctly re-pointed from "all three trees" to the channels plus every prefix of the artifact tree, and the criterion's note that the three prefixes share one resolution point today is accurate. Verification now demands a `/framework/` traversal probe in plain and percent-encoded form | none |
| 2 | info | exclusivity | AC-964 (`acceptance_criterion-46d5804e`) | — | **Warning 2 of `report-dcfe6d0a` is confirmed repaired.** AC-964 (updated 2026-09-10T10:01:44) now carries the explicit deferral: the fall-through ordering, the behind-the-gate property and the no-account artifact case are AC-1400's in full, and "This criterion asserts nothing about ordering." The artifact survives in AC-964 only as one member of the route-class sweep, which is the correct residue. AC-1400 was left untouched, as the report directed | none |
| 3 | info | exclusivity | AC-972 (`acceptance_criterion-285b8c08`) | — | **Warning 3 of `report-dcfe6d0a` is confirmed repaired.** AC-972 (updated 2026-09-10T10:01:40) is narrowed to the half that is the workspace's own — publish is invoked for the *displayed* site, a revision is appended and locked in the same form a command-line publish produces, and no publish semantics exist only in the workspace. The serving clause is deferred to AC-1035 by name, citing the STORY-99 out-of-scope line, and the Verification ends "Do not restate the serving assertion". The title lost its serving half. AC-1035 was not weakened to compensate | none |
| 4 | info | consistency | AC-1036 (`acceptance_criterion-46e9debf`) | — | **Finding 5's carry-through is applied.** AC-1036's deferral sentence no longer inherits AC-978's stale "all three trees"; it reads "across every tree the origin serves — the rendered channels among them, alongside each prefix of the built-artifact tree". A sweep of all 36 AC bodies for `three trees` / `all three trees` / the old `the installed components, and the …` enumeration returns zero hits, so the matrix no longer contradicts itself about what this origin serves | none |
| 5 | warning | consistency | AC-962 (`acceptance_criterion-65d0dd94`) | ac-edit | **AC-962's opening clause names a trigger moment REQ-145 superseded, while its substance is correct.** It reads: *"When the workspace is **started** on a machine where one of the shared UI components has not been installed, the failure names the specific missing component and states the literal command that installs it."* Component resolution is no longer reached by starting the workspace. STORY-99's Technical Context: *"The resolution now happens at build time rather than per request, which moves* when *an upstream move is reported but not* whether". The code agrees: the single resolution point is `tools/generate/src/cli/webui.ts:205-220` (`webuiPackageDir`), raising `MissingWebuiComponentError` (`:223-233`) with the component name and `bin/install --lang js --component <name>`; its only production callers are `tools/generate/src/cli/assets.ts:294-295` (the `1c assets` build) and `shared-store.ts:62`. Starting the workspace against an unbuilt tree produces the *other* diagnostic — `builder.ts:153-157`'s 503 naming `1c assets`, which is AC-1400's subject. So the asserted outcome is real and reached at the single resolution point, exactly as the criterion's own Verification checks; only the sentence that says *when* is stale. This is the last AC on this story whose text still describes the pre-REQ-145 moment. AC-961 was checked for the same defect and does not have it: its operative claim is byte-identity with the installed copy, which the verbatim build copy preserves, and it asserts no moment | Replace the trigger clause with the build moment — e.g. "When the workspace's artifacts are built on a machine where one of the shared UI components has not been installed" — and keep every other sentence, including *"it does not serve a workspace document whose components 404 in the browser"*, which is the guarantee and is unchanged. Do **not** repair this by adding a parallel build-time criterion beside AC-962: STORY-99's Reconciliation Decisions rule that out explicitly (*"Adding parallel build-time criteria beside the request-time ones would state one guarantee twice"*). Do not touch AC-1400, which correctly owns the unbuilt-tree diagnostic |
| 6 | info | exclusivity | AC-965 (`acceptance_criterion-5286c04b`) vs AC-1449 (`acceptance_criterion-2180afc8`) | — | AC-1449's Verification re-drives AC-965's two refusals ("Assert the two refusals are untouched by it"). Re-confirmed **not** a duplicate: AC-1449's third bounded property is *"It resolves* not yet*, never* no", for which the two refusals are the controls, and STORY-99's 2026-08-31 Reconciliation Decisions rule the split explicitly (*"they are opposite outcomes with different evidence … combining them would make a single criterion whose two halves cannot both be read as its subject"*). Same shape as the AC-1402/AC-1449 overlap recorded as deliberate in `report-dcfe6d0a` finding 7. Recorded so it stays on the record as intended rather than being re-raised next cycle | none |
| 7 | info | exclusivity | AC-1031 (`acceptance_criterion-e9a9ba3b`) vs AC-1399 (`acceptance_criterion-4b9f7f0c`) | — | Both request a channel's referenced stylesheet and assert it resolves. Judged acceptable rather than redundant: they are different shapes over different arrangements — AC-1031 probes a local origin with every rendered artifact deleted from disk (its subject is that nothing was materialised), AC-1399 probes the deployed runtime with no local process anywhere (its subject is that the presentation was assembled somewhere the runtime can reach). Neither can stand in for the other | none |
| 8 | info | coverage | AC-1399 — the edit read-back | — | AC-1399 asserts an edit written through the workspace's own operation is still there when read back by a second request, and STORY-99 marks editing out of scope. Not a violation: the CAP-85 body reserves *what* the write path reads, writes and refuses to CAP-86 (AC-992) and keeps *that the operations are reachable here* for itself, and AC-1399's Verification asserts only reachability and persistence, never a refusal shape or a validation rule. Same conclusion `report-dcfe6d0a` finding 6 reached from the other direction | none |
| 9 | info | exclusivity | AC-960 (`acceptance_criterion-13d252a9`) vs AC-963 (`acceptance_criterion-78436279`) | — | Both assert scope-correctness of component references; disjoint surfaces. AC-960 sweeps every **tracked** text file, and the workspace document is generated and never committed — AC-963 says so in terms (*"the one produced now, never a copy of it committed to the repository"*). Unchanged from the same note in three prior reports | none |

## Notes for the Editor

- **The level passes. The one warning does not gate it** and can be taken
  opportunistically as a single-sentence edit to AC-962. If it is taken, the
  constraint in the Suggested edit column is the load-bearing part: repair the
  trigger clause in place, never by adding a second component criterion about
  build time.

- **The AC-978-shaped sweep is now exhausted.** `report-dcfe6d0a` asked whether
  any *other* AC stamped 2026-08-16 still describes a pre-REQ-145 topology. All
  22 such ACs were re-read this run against the current serving code. AC-962 is
  the one hit, and it is a trigger-moment slip rather than a wrong enumeration —
  a materially smaller defect than AC-966's or AC-978's were. The other
  topology-adjacent members (AC-961, AC-963, AC-979, AC-1030) were checked
  individually and are correct as written; AC-979 speaks of channels and
  components as request targets rather than as served trees, so REQ-145 left it
  untouched.

- **Two `uat-edit`s remain carried forward and are still not ac-level.** Both
  were raised by `report-dcfe6d0a` and re-stated by `report-c47daafa`; neither is
  counted here. (a) AC-978's evidence,
  `tests/reconciliation-builder-workspace-origin.test.ts:202-235`, probes the
  channels and `/builder/` plus a `WEBUI_INSTALLED`-gated `/webui/`, and has no
  `/framework/` probe group — which the repaired criterion now requires.
  (b) AC-964's Verification demands the unadmitted probe include a build
  artifact explicitly, while its evidence at `:688-748` makes that probe against
  `/preview/...` only. Both are the uat cycle's to close.

- **Six ACs carry no `uat_coverage` value at all** — AC-1399, AC-1400, AC-1401,
  AC-1402, AC-1403 and AC-1449, the whole 2026-08-31 REQ-145/BUG-36 batch. That
  field is owned by check/fix_uat_coverage and is deliberately not written here;
  it is flagged because the capability's own `uat_coverage` is `fail` and an
  unset value is not the same signal as a failing one.

- **Method note, still applicable.** `tools/generate/src/cli/builder.ts` contains
  NUL bytes and is treated as binary by `grep`; the serving path cited in
  finding 1 was read through `grep -an … | tr -d '\000'`.

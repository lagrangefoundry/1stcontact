---
uid: report-552e1877
id: REPORT-2388
type: report
title: Fix 1c Capture & Diff Fidelity (uat) — attempt 8
created_by: xgd
created_at: '2026-08-20T06:07:53.259255+00:00'
updated_at: '2026-08-20T06:07:53.259255+00:00'
completed_at: null
last_field_updated: created_at
fields:
  report_kind: fix_structural_validation
  subject_uid: capability-aa030c83
  level: uat
  fixes_applied: 9
  progress_made: true
  needs_more_work: false
  violations_remaining: 0
  anchor_report_uid: report-2485c83c
---

# Fix Summary — 1c Capture & Diff Fidelity (uat)

**Attempt**: 8
**Fixes applied this call**: 9
**Violations remaining**: 0
**Needs more work**: false

Both violations of `report-60445a46` are addressed, each as the pairing the report
prescribed (AC edit + parent story body + the code and test it made stale). The one
warning (W1) needed no action — see below.

## Actions Taken — by Resolution Category

| # | Category | Element | Action |
|---|---|---|---|
| 1 | uat-edit (F1) | `tools/generate/src/cli/fidelity.ts` | Added exported `selectMultiViewportPayload(cells, {clusters, collapse, json})` — the view choice + serialisation lifted out of the command switch as a pure function returning `{view, output}` |
| 2 | code-issue (F1) | `tools/generate/src/cli/index.ts:794-802` | Dispatcher now `console.log`s `selectMultiViewportPayload(...).output`; the inline double ternary is gone. Import list narrowed; helper re-exported from the CLI barrel |
| 3 | uat-edit (F1) | `tests/req63-values-diff-coverage.test.ts` | New `test_UAT_AC1289_clusters_takes_precedence_over_collapse_in_both_serialisations`: with **both** flags set, `--json` emits the ranked causes (dispositions, no `repairClass`) byte-identical to `clusterDefects(collapseMultiViewport(cells))`, text mode emits `formatClusterReport`, the losing collapsed view is asserted different, and the no-flag case still yields the raw per-cell view |
| 4 | ac-edit (F2) | AC-1288 `acceptance_criterion-88661be9` | Section spacing removed as a structural trigger from the Criterion; a paragraph records the REQ-73 retirement and why the rule must not key on a `§`+`padding` row. Verification dropped from five cases to four and now asserts *no* collapsed defect carries `§` text |
| 5 | story-body-edit (F2, paired) | STORY-116 `story-aaddb221` | Item 4 no longer lists section spacing as a third structural trigger; step 2 of the printed order drops "or the spacing model"; **In scope** names the two surviving triggers; two Technical Context bullets added — the cumulative-intent argument for the deletion, and the `selectMultiViewportPayload` precedence seam |
| 6 | code-issue (F2) | `fidelity.ts` `collapseMultiViewport` | Deleted `(g.text.startsWith('§') && g.property.startsWith('padding'))` from the structural predicate; comment records the REQ-73 retirement |
| 7 | code-issue (F2) | `fidelity.ts` `repairPlanLines` | Deleted the duplicated condition from `isStructural`; stale trailing comment sentence replaced |
| 8 | code-issue (F2) | `fidelity.ts` `formatCollapsedReport` | Group heading `'Type-A structural - author the responsive ladder / section spacing'` → `'… responsive ladder'`, so the printed report stops advertising a retired input |
| 9 | uat-edit (F2) | `tests/req63-values-diff-coverage.test.ts` | `test_UAT_AC1288_five_classification_cases_…`: the synthetic `report.deltas.push({…'§0'…'paddingTopPx'…})` block is gone, `expect(cls('§0'))` replaced with `expect(defects.some(d => d.text.startsWith('§'))).toBe(false)`, repair-order baseline re-cut to `A-flat 1 -> A-structural 2 -> B 1`, and the reference-value assertion now checks the ladder range `36 .. 72` rather than the synthetic row's `96` |

## Code Edits

| File | Lines | Evidence chain |
|---|---|---|
| `tools/generate/src/cli/fidelity.ts` | `collapseMultiViewport` structural predicate (was :354), `repairPlanLines` `isStructural` (was :557), `formatCollapsedReport` HEAD.structural | REQ-73 (`request-859652ae`, free_and_reconciled 2026-07-18) change 2 drops the band-padding deltas; verified at source — `cli/capture/values-diff.ts:2550-2583` emits `§<n>` rows carrying only `overlay` / `contentAnchor` / `textAlign`, with an in-code REQ-73 comment stating the retirement, and the surviving `padding*` deltas are element-scoped. The predicate was therefore unreachable; CLAUDE.md ("delete old code; don't hide it behind flags") settles the disposition |
| `tools/generate/src/cli/fidelity.ts` | new `selectMultiViewportPayload` before `repairPlanLines` | AC-1289's final Verification clause (`--clusters --collapse --json` emits causes, not collapsed rows) was unprovable because the branch sat inside the `--multi-viewport` block after a real render. Extraction is the refactor-to-test shape the report named and the one AC-1287/1288 already use against `collapseMultiViewport` |
| `tools/generate/src/cli/index.ts` | dispatcher `--multi-viewport` output block; import + export lines | Behaviour-preserving: the helper reproduces the previous ternaries exactly (clusters → collapse → cells; `--json` selects serialisation) |

**Red-check on the new test.** Flipped the precedence in the helper to
`collapse ? 'collapsed' : clusters ? 'clusters' : 'cells'` and re-ran: exactly one
test failed — `test_UAT_AC1289_clusters_takes_precedence_over_collapse_in_both_serialisations`
(1 failed | 36 passed) — then reverted. The clause is now genuinely defended.

## Verification

- `tests/req63-values-diff-coverage.test.ts`: **37 passed, 0 failed** (36 before; +1 new).
- Batch of 13 fidelity / values-diff / CLI-hygiene files: **180 passed, 11 skipped, 1 failed**.
  The single failure is `test_UAT_FC_REQ-48_engine_driver_factory_and_availability`
  (`req48-fidelity-axes.test.ts:539`), which times out at 60s calling
  `engineAvailable('webkit')` — a browser probe, no fidelity code in its path — and
  `req47-fidelity-structural.test.ts` reports the same `EPERM: listen 127.0.0.1`
  suite error the last two attempts recorded as environmental. Neither touches the
  edited code paths; both are the sandbox's missing browser/socket permission.
- `npx tsc --noEmit -p tools/generate/tsconfig.json`: one error, `builder.ts(337,29)`
  (`body.sessionId` possibly undefined) — pre-existing, in a file this call did not
  touch and which imports nothing from `fidelity.ts`.

## W1 (warning) — no action required

The report's suggestion was to "record the gating on AC-1307 the way attempt 7 did
for AC-1314/AC-1316". Read AC-1307 (`acceptance_criterion-4ecfd679`) in full: it
**already carries** an `**Evidence gating.**` paragraph in its Verification stating
that the oklch/`color-mix()` case is browser-gated, why (`getComputedStyle` under
jsdom returns the token verbatim), and which half runs headlessly. The obligation is
discharged; the residual — confirming on a Chromium-provisioned runner — is a runner
question, not a matrix or test edit, and no runner here can satisfy it.

## needs_review Items Forwarded

None. Finding 2 was decidable on the intent ledger (REQ-73 later than REQ-64 and
explicit), so it was applied in the "retire" direction rather than forwarded as an
operator call on keeping the rule as forward contract.

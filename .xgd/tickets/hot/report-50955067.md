---
uid: report-50955067
id: REPORT-3862
type: report
title: 'Reconcile resolve conflicts: reconcile-BUNDLE-26'
created_by: xgd
created_at: '2026-09-11T01:56:07.328113+00:00'
updated_at: '2026-09-11T01:56:07.328113+00:00'
completed_at: null
last_field_updated: created_at
result: pass
fields:
  report_kind: resolve_conflicts
  subject_uid: reconcile-BUNDLE-26
---

Cherry-pick 53374730fa ("Merge branch 'free-REQ-165' into xgd-working") —
four `UU` conflicts, all resolved. CHERRY_PICK_HEAD left in place.

## Files resolved

- **package.json** — config scalar (2g). HEAD `0.2.20` vs incoming `0.2.30`
  (the incoming's own diff is `0.2.29 → 0.2.30`). Took incoming: it is both
  the authoritative free-coded side and the higher number, so the branch's
  version advances monotonically.
- **tools/generate/src/cli/kb.ts** — implementation, UU (2c). Three hunks:
  - *imports*: kept the incoming's `./kb-projection` import; dropped its
    `import { CORPUS_TYPE, SHIPPED_SOURCE, SYSTEM_KB } from './kb-model'`.
    `kb-model.ts` (REQ-158) does not exist on this branch and is not added by
    this commit — those three constants are still declared locally in `kb.ts`
    here (lines 64/74/77), so taking the incoming import verbatim would have
    been an unresolvable module plus three duplicate declarations. Nothing is
    lost: the values are identical.
  - *`KbStatus`*: added the incoming's `projected: number` (REQ-165). Did NOT
    take the `tickets: number | null` field that sat beside it — `tickets` is
    pre-existing context on the working side, not REQ-165 content, and its
    producer `countMemberTickets()` does not exist on this branch.
  - *`kbStatus()`*: added `projected: files.filter((f) => isProjected(f)).length`;
    dropped `tickets: countMemberTickets()` for the same reason.
- **tools/generate/src/cli/index.ts** — implementation, UU (2c). Two hunks:
  - *imports*: kept HEAD's single-line form plus the incoming's
    `writeProjections`. Dropped `DOC_KIND_FIELD` / `MEMBER_KIND` from the
    import because the only thing that used them here was the ticket-count
    comparison (see below), which this branch has no `s.tickets` for; an
    unused import would not compile under `noUnusedLocals`.
  - *`kb status` output*: integrated both intents (2c.3b). Kept the incoming's
    REQ-165 change — the corpus line now reads
    `corpus: N exported + M projected` off `s.corpus - s.projected` — and
    dropped the `s.tickets`/`DOC_KIND_FIELD` staleness clause it was wrapped
    in, which belongs to a commit this bundle has not reached. Comment rewritten
    to state the REQ-165 rationale that still holds here.
- **tests/reconciliation-system-knowledge-base.test.ts** — test file, UU.
  Both hunks are the same shape: the incoming rewrote these assertions to add
  REQ-165's `projected: 0` *and* the `tickets:` field / `withStore` wrapping
  that comes with the ticket-count feature. Kept HEAD's structure
  (`withRoot((root) => …)`, `await withStore(CORPUS, () => exportCorpus(root))`,
  `toEqual`) and added `projected: 0` with the incoming's explanatory comments.
  No test function was added, removed or renamed on either side.

## Bridging edit outside the conflict hunks

`kb.ts` gained two constants, `DOC_KIND_FIELD = 'doc_kind'` and
`MEMBER_KIND = 'system_kb'` (the incoming file's own values, verbatim from
`53374730fa:tools/generate/src/cli/kb.ts:245-246`). The incoming's
`projectedDocument()` merged in CLEANLY — no conflict marker — and references
both, so without them the file did not compile. They are declared beside
`INCLUDE_FIELD` and used only by `projectedDocument`; this branch's membership
predicate is still `fields.system_kb === true` (`optedIn`), unchanged. This is
the case the incoming UAT
`test_UAT_FC_REQ-165_a_projection_declares_itself_a_member_of_the_knowledge_base`
asserts (`fields.doc_kind === 'system_kb'` alongside `fields.system_kb === 'true'`),
so porting the constants is what keeps that developer test meaningful rather
than merely compiling.

## Incoming changes preserved

Verified against `git diff f034eeee18 53374730fa -- <file>`:

- `package.json` — version bump present (`0.2.30`).
- `kb.ts` — all five REQ-165 hunks present: the `kb-projection` import; the
  `!isProjected(name)` guard in `exportCorpus`'s sweep; `writeProjections`,
  `corpusMembership` and `projectedDocument` in full and unmodified; the
  "two producers" paragraph in `KB_USAGE`; `projected` on `KbStatus` and in
  `kbStatus()`.
- `index.ts` — all five hunks present: `writeProjections` imported; the usage
  text naming both producers; `ensureConfig()` + `writeProjections()` before
  the export with the `projected: …` report line; `ensureConfig()` +
  `writeProjections()` before the build; the exported/projected split on the
  status line.
- `tests/reconciliation-system-knowledge-base.test.ts` — `projected: 0` on all
  four `kbStatus` assertions, and the bare-command assertion on
  `corpus: N exported + M projected`.
- `kb-projection.ts` (865 lines) and
  `tests/test_UAT_FC_REQ-165_projected_reference.test.ts` (379 lines) applied
  clean and are staged as adds; `packages/framework/src/modules/index.ts`
  merged clean.

Nothing dropped under the BUG-1301 precedence exception. The two things not
carried over (`tickets` on `KbStatus`, the `DOC_KIND_FIELD` staleness clause in
`kb status`) are not this commit's content — they are context lines from an
earlier working-branch commit that this bundle has not yet replayed, and their
supporting code (`countMemberTickets`, `kb-model.ts`) is absent from the tree.

## Verification run

- `tsc --noEmit -p tools/generate/tsconfig.json`: no errors in `kb.ts`,
  `kb-projection.ts` or `index.ts`. The only errors reported are the four
  known worktree artefacts (`apps/control-app/src/generated/*` absent from a
  git worktree).
- `npm test -- tests/test_UAT_FC_REQ-165_projected_reference.test.ts`:
  **12 of 13 pass.** The one failure,
  `test_UAT_FC_REQ-165_asking_what_a_component_supports_returns_the_projection`,
  throws `KnowledgeConfigError: knowledge base 'system' reads from source
  'shipped', which this host has no index for (available: none)` from inside
  `@lagrangefoundry/knowledge/src/index_store.js` — the unpinned shared store.
  Pre-existing and environmental: the untouched
  `tests/test_UAT_FC_REQ-123_system_kb.test.ts` fails with the identical error
  at the identical library frame on this tree.
- `npm test -- tests/reconciliation-system-knowledge-base.test.ts`: 6 pass,
  5 fail. Every failure is one of the two known environmental faults — the
  `KnowledgeConfigError` above (AC1293, AC1299, AC1306) or the shared store's
  `prompt` → `description` field rename (AC1305, AC1295). None is an assertion
  on `projected`: AC1293 reaches and passes both `projected: 0` blocks and then
  throws out of `buildKb` in the shared library.
- `./bin/1c kb status` prints the new line shape:
  `corpus: 0 exported + 0 projected`.

## Flagged for post-merge review

`package.json` — version taken from the incoming side per the enrichment's
"intent unknown on both sides" rule; the branch jumps 0.2.20 → 0.2.30.

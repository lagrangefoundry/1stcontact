---
uid: report-2f341248
id: REPORT-1131
type: report
title: Claude reconciliation_uat_generation report
created_by: xgd
created_at: '2026-08-03T03:30:06.234851+00:00'
updated_at: '2026-08-03T03:30:06.234851+00:00'
completed_at: null
last_field_updated: created_at
fields:
  report_kind: reconciliation_uat_generation
  subject_uid: story-02f21b8a
---

Only the new test file is added — no runtime code and no existing tests were touched.

```
UATs generated for story story-02f21b8a (plan item 7 of 9)

Story: story-02f21b8a
Test file(s) created:
  - tests/reconciliation-l1-seam-mounted-behaviors.test.ts

Tests written: 8
Tests passed: 8
Tests failed: 0

test_files_created:
  - "tests/reconciliation-l1-seam-mounted-behaviors.test.ts"
```

**AC → UAT mapping (1:1, all 8 ACs covered):**

| AC | Test | Boundary exercised |
|---|---|---|
| AC-782 | `test_UAT_AC782_l1_body_with_seam_bound_instances_validates_as_one_page` | `validateSite` — composed page, two-seam page, and both single-half shapes |
| AC-783 | `test_UAT_AC783_unresolvable_bindings_fail_with_a_machine_readable_path` | `validateSite` — 5 parameterized rejection cases, each asserting message **and** anchored error path (`/pages/0/modules/N/slot`, `/pages/0/l1`) |
| AC-784 | `test_UAT_AC784_controls_fold_to_one_seam_per_form_at_the_forms_union_rect` | `foldToL1` — seam names/behaviour, keyframes cover the full ladder, rect equals the form's own union at **every** width, one binding per seam in document order; geometry-less control → named gap |
| AC-785 | `test_UAT_AC785_clustering_separates_side_by_side_forms_at_the_widest_width` | `clusterControls` — 3+1 split with per-group membership and ordering, zero vertical gap proven, same split at 8× scale (size-derived, not a fixed constant), plus the real capture when present |
| AC-786 | `test_UAT_AC786_a_button_beside_a_form_becomes_that_forms_submit_control` | `foldToL1` — both submits claimed with appearance axes but **no** geometry/visibility, each seam contains its button at every width, neither button left in the page body, distinct buttons per form, far-off CTA unclaimed |
| AC-787 | `test_UAT_AC787_a_bound_seam_hosts_the_fragment_and_an_unbound_seam_stays_inert` | `renderL1Document` three ways + end-to-end `cmdRepro` → `cmdRender` |
| AC-788 | `test_UAT_AC788_two_instances_of_one_behaviour_mount_independently_per_seam` | Full pipeline render — per-instance `data-fc-module`, per-seam containment, per-instance CSS namespace (`form-0-submit-l1-*`), no field cross-talk |
| AC-789 | `test_UAT_AC789_universal_obligations_are_checkable_in_the_mounted_position` | `serveOneModulePage({ mountInL1: true })` — served page re-validated, broken binding refused, seam spans all six probed widths, module markup nested in the seam |

Two notes on what I grounded rather than guessed:

- Seam containment in AC-787/788/789 uses a depth-matched `</div>` extraction of the seam's own box, not a string slice — so nested module markup provably cannot leak between two seams.
- Four self-corrections were needed and all were **test** bugs, not code divergence: the error path carries a leading `/`, padding axes are `topPx`-style keys, the L1 fragment namespace is `<instance>-submit-l1-<n>`, and my vertical-overlap sanity expression was off by the touching-edge case. No AC was weakened.

Regression scope run alongside (7 files, 57 tests, all passing): `naming`, `req93-l1-slot-mounted-behaviors`, `req88-form-labelling-and-submit`, `reconciliation-behavior-modules`, `site-schema`, `reconciliation-l1-substrate`. `tsc` over the workspace reports only pre-existing errors in unrelated files (`req92-image-box-fold`, `site-schema.test`, `playwright-driver`, `aligned-crops`, `vitest.config.mts`) — none in the new file. ESLint has no flat config at the repo root, so it could not be run directly; the configured quality plugin drives it.

---
uid: report-4648744c
id: REPORT-2689
type: report
title: 'Reconcile resolve conflicts: reconcile-BUNDLE-20'
created_by: xgd
created_at: '2026-08-31T05:18:18.993893+00:00'
updated_at: '2026-08-31T05:18:18.993893+00:00'
completed_at: null
last_field_updated: created_at
result: pass
fields:
  report_kind: resolve_conflicts
  subject_uid: reconcile-BUNDLE-20
---

## Files resolved

- `.xgd/tickets/hot/request-a03967f2.md` — **AA (both added)**, intent/bookkeeping ticket
  (rule 2e + 2b, superset case). Path is outside the sparse-checkout cone
  (`!/.xgd/tickets/**`), so the conflict existed index-only with no working-tree
  markers; resolved with `git checkout --ours` + `git add --sparse` per DOC-986 §2/§4.1.

  Comparison of the two index stages showed the **document bodies are byte-identical**.
  The only differences are three frontmatter facts, all of which HEAD holds in a strictly
  more advanced state:

  | field | ours (HEAD) | theirs (incoming `c5752ee`) |
  |---|---|---|
  | `updated_at` | `2026-08-24T02:10:41Z` | `2026-08-23T03:29:49Z` |
  | `status` | `bundled` | `ready_to_reconcile` |
  | `fields.chat_comment` | `comment-869ded75` | *(absent)* |
  | `fields.bundled_in` | `bundle-b3b7c399` | *(absent)* |

  HEAD is a **strict superset**: same content, status advanced one step further along the
  same lifecycle (`ready_to_reconcile` → `bundled`), plus two fields the incoming side
  never carried. Under 2e's superset clause the superset is kept; the enrichment metadata's
  fallback rule ("take the more recent commit by timestamp") points the same way, HEAD being
  ~23h later. No field was invented and no field was set beyond what one side already
  declared.

## Incoming changes preserved

The incoming commit `c5752ee5cc91b01c14e40da43c6e14cd233931bf`
(*"xgd(ticket): update request request-a03967f2"*) touches exactly one file — this ticket,
164 insertions, no code. Its entire content is present verbatim in the resolved version:
the `diff` between the two stages shows **zero body differences**, and the frontmatter facts
it carries are all present in HEAD at an equal-or-later state. Nothing from the incoming
side was discarded.

Consequently the staged tree has **no net diff against HEAD** (`git diff --cached --stat HEAD`
is empty). This is the redundant-commit case of BUG-1109/BUG-1122, not a STEP 3 discard: the
incoming commit's effect is already in HEAD via the later `bundled` update, rather than
merely absent. Per STEP 4 no `--skip` was issued — `CHERRY_PICK_HEAD` is intact and
`cherry_pick_finalize_resolution` will detect the clean staged diff and skip the commit.

No code, UAT, or spec-ticket files were involved, so no BUG-1301 precedence exception was
needed and no hunk was dropped.

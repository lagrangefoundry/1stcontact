---
uid: report-28150a88
id: REPORT-3392
type: report
title: 'Reconcile resolve conflicts: reconcile-BUNDLE-23'
created_by: xgd
created_at: '2026-09-03T23:00:32.406616+00:00'
updated_at: '2026-09-03T23:00:32.406616+00:00'
completed_at: null
last_field_updated: created_at
result: pass
fields:
  report_kind: resolve_conflicts
  subject_uid: reconcile-BUNDLE-23
---

## Files resolved

- `.xgd/tickets/hot/bundle-78f4e2fe.md` — class **UU**, rule **2e** (intent/bookkeeping ticket; `bundle-*.md`, not a matrix-defining spec ticket). Incoming commit `2ff73d0d1f` (2026-08-31 19:19:32Z).

  The conflict region was narrower than the previous attempt: `status: free_and_reconciled` sits **outside** the markers — both sides agree on it, so it merged cleanly. Only three bookkeeping lines conflicted, and both sides changed the same three facts, so the per-fact timeline rule applies:

  | fact | HEAD (`4b197af0eb`, 19:19:50Z) | Incoming (`2ff73d0d1f`, 19:19:32Z) |
  |---|---|---|
  | `updated_at` | `19:19:50.607800` | `19:19:32.730241` |
  | `completed_at` | `19:19:32.487153` | `19:19:32.730241` |
  | `last_field_updated` | `result` | `status` |

  HEAD is the later-positioned side on every one of the three (18s later by commit timestamp, and later by the ticket's own `updated_at`). HEAD's `last_field_updated: result` records a strictly subsequent operation — the write of `result: pass` — that post-dates incoming's status transition. The two `completed_at` values are the same semantic fact recorded 243ms apart by parallel timelines; the timeline rule keeps HEAD's. Kept HEAD's three lines.

  Resolved by editing the marker region in place rather than `checkout --ours`, so the cleanly-merged `status` line and all other regions were preserved untouched. No fields invented; no `intent_uid`/`story_uid`/`capability_uid` touched.

## Incoming changes preserved

Incoming `2ff73d0d1f` touched only this ticket file, in one hunk, whose substantive intent was to complete the bundle: `status: reconciling` → `free_and_reconciled` and `completed_at: null` → a real timestamp. Both are present in HEAD:

1. **`status: free_and_reconciled`** — present verbatim, and not even contested; it merged clean outside the conflict markers.
2. **`completed_at` non-null** — present in HEAD as `2026-08-31T19:19:32.487153+00:00`. The value differs from incoming's by 243ms only because the same completion was stamped independently on each timeline; the fact the developer's commit asserted (bundle is completed, with a completion time) holds in the resolved file.

The remaining differences (`updated_at`, `last_field_updated`) are xgd's own write-bookkeeping, not developer intent, and HEAD holds the later value for each.

`git diff --cached HEAD` is therefore empty. This is BUG-1109/BUG-1122 redundancy, not a STEP 3 discard — the distinguishing test passes because the incoming commit's key changes are demonstrably present in HEAD rather than absent. Per STEP 4 the file is staged and this exits `@done`; `--skip` was not called and the cherry-pick sequencer state (`CHERRY_PICK_HEAD` = `2ff73d0d1f04115870687bb9593207260877c197`) is left intact for `cherry_pick_finalize_resolution`.

No code, UAT test, or config files were in this conflict set, so rules 2c/2f/2g and the BUG-1301 precedence exception did not apply.

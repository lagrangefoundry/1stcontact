---
uid: report-0e840702
id: REPORT-3752
type: report
title: 'UAT Coverage: L1 Reproduction Pipeline: Fold & Acceptance Gate'
created_by: xgd
created_at: '2026-09-10T15:38:56.446717+00:00'
updated_at: '2026-09-10T15:38:56.446717+00:00'
completed_at: null
last_field_updated: created_at
result: fail
fields:
  report_kind: uat_coverage_check
  subject_uid: capability-2049c9ec
  violations: 3
  warnings: 1
  needs_review_count: 0
---

# UAT Coverage Assessment: L1 Reproduction Pipeline: Fold & Acceptance Gate

**Result**: FAIL
**AC verdicts**: 39 pass, 2 fail, 0 deprecated, 0 needs_review
**Story verdicts**: 1 pass, 1 fail, 0 stale, 0 needs_review
**Capability verdict**: fail

CAP-71 (`capability-2049c9ec`) holds two `upgrade` stories: STORY-84
(`story-8acc338d`, the fold) with **24** ACs and STORY-86 (`story-24098299`, the
3-probe gate + cross-gate reconciliation) with **17** — 41 in total, up from 34 at
the last cycle. Seven of the eight new ACs (AC-1625…AC-1631, AC-1630) were created
today by the structural-validation cycle to close prior findings 4–8. This is
attempt **7**.

## Method — executed, not derived

Unlike the previous six cycles, **the suite ran this session**. All nine
reconciliation test files for this capability were executed with
`npm test -- <files>`:

```
tests/reconciliation-l1-fold.test.ts
tests/reconciliation-l1-fold-responsive-axes.test.ts
tests/reconciliation-l1-bundle-materialization.test.ts
  → 3 files passed, 13 tests passed (233ms)

tests/reconciliation-l1-fold-full-language.test.ts
tests/reconciliation-l1-fold-seams-and-refold.test.ts
tests/reconciliation-l1-fold-framing-and-adjustment.test.ts
tests/reconciliation-3probe-gate.test.ts
tests/reconciliation-3probe-gate-evaluator.test.ts
tests/reconciliation-cross-gate-reconciliation.test.ts
  → 6 files passed, 28 tests passed (386ms)
```

41 tests for 41 ACs — **every AC has exactly one `test_UAT_AC<n>_*` test and all
41 pass**. The verdicts below are therefore about *what the tests assert*, on top
of a confirmed-green run, rather than static reading alone.

The 233ms/386ms test durations are themselves evidence for finding 3: AC-694's
real-engine branch launches Chromium and an HTTP server, which cannot complete in
that budget. It silently did not run.

## Cumulative Intent Considered

Statuses re-read from the live tickets this session; **every** intent below is
`free_and_reconciled`, so every one counts. No intent has touched this capability
since REQ-136 (2026-08-12), and nothing retires an AC.

| Intent ID | Status | When | Asked / changed | Counts? |
|---|---|---|---|---|
| REQ-79 / REQ-82 | free_and_reconciled | 2026-07-19/20 | L1 substrate + safety envelope (the fold's target language) | YES |
| REQ-83 | free_and_reconciled | 2026-07-20 | Capture→L1 fold: keyframes + `interpolate\|snap` + visibility, oracle retention, advisory hint sidecar; dissolves `adopt-values` — origin of STORY-84 | YES |
| REQ-86 | free_and_reconciled | 2026-07-20 | End-to-end 3-probe gate + demand-driven promotion — origin of STORY-86 | YES |
| REQ-88 | free_and_reconciled | 2026-07-21 | `1c repro` / `1c l1-gate`; responsive padding tracks; viewport-height probe; `nowrapFromPx`; captured **surface rect** as card geometry (round-5) | YES |
| BUG-5…BUG-9 | free_and_reconciled | 2026-07-23 | Occurrence pairing; typed residual; row main-axis tiling; half-open intervals; recursive region-aware promotion | YES |
| REQ-92 | free_and_reconciled | 2026-07-23 | Rebuild `foldToL1` to populate the **full** L1 language | YES |
| BUG-11…BUG-14 | free_and_reconciled | 2026-07-23 | Surface fills, font faces, section background images; section-band → card hierarchy | YES |
| BUG-17 | free_and_reconciled | 2026-07-23 | Fold a captured element's per-side padding | YES → **AC-1626 (new)** |
| BUG-18 | free_and_reconciled | 2026-07-23 | Varying numeric text axes keyframed per width | YES → **AC-1625 (new)** |
| BUG-19 | free_and_reconciled | 2026-07-23 | Full-bleed **bar** seeds a band, not one card per run | YES (finding 1) |
| BUG-20 / BUG-21 | free_and_reconciled | 2026-07-23/24 | Box treatments; the **self-painting** pill/chip and padded control | YES (finding 1) |
| BUG-23 | free_and_reconciled | 2026-07-24 | Reproduction must not hotlink the captured origin | YES → **AC-1628 (new)** |
| BUG-24 | free_and_reconciled | 2026-07-24 | A band's translucent **scrim** must round-trip capture → fold → render | YES → **AC-1629 (new)** |
| BUG-27 | free_and_reconciled | 2026-07-25 | CSS background images / lazy media captured; backdrops count toward the **page-base** extent | YES (finding 2) |
| REQ-94 | free_and_reconciled | 2026-07-25 | Cross-gate reconciliation, perceptual floor, reference coverage, named causes | YES |
| REQ-96 | free_and_reconciled | 2026-07-26 | L1 `control` node — a captured control binds instead of being dropped | YES |
| REQ-136 | free_and_reconciled | 2026-08-12 | Non-destructive framing + colour adjustment as typed L1 axes | YES |

No `needs_review` anywhere: every behaviour either story body describes traces to
a reconciled intent above. In particular the four clause-clusters added to
STORY-84's body since the last cycle — scrim (BUG-24), nowrap threshold (REQ-88),
height probes (REQ-88), `1c repro` materialization (REQ-88 / BUG-23) — are all
intent-grounded, as are the fold clauses AC-731 absorbed today (BUG-19,
BUG-20/21, BUG-27, REQ-88 round-5).

## Alignment Ledger

| Story | Intents aligned to | Outcome | Notes |
|---|---|---|---|
| STORY-84 (fold) | REQ-83, REQ-88, REQ-92, REQ-96, REQ-136, BUG-6, BUG-11…BUG-14, BUG-17…BUG-21, BUG-23, BUG-24, BUG-27 | **fail** | Body is now fully aligned — the four gaps the last cycle reported as story-body drift (padding, responsive tracks, viewport-height response, self-painting run) are all described, and each has an AC. The remaining defect is one AC (AC-731) whose body grew today faster than its UAT (findings 1–2), plus AC-694 (finding 3) |
| STORY-86 (gate) | REQ-86, REQ-88, REQ-94, BUG-5, BUG-7, BUG-8, BUG-9 | **pass** | Body faithful to intent; all 17 ACs substantively covered. The last cycle's only STORY-86 gap — the third envelope violation (pinned-box content overflow), uncovered for four consecutive cycles — is now AC-1630 with a thorough UAT, and AC-706/707/710 were widened to reference it |

## What closed since the last cycle

Recorded so the editor does not re-open settled work:

| Prior finding | Status now |
|---|---|
| 1 — AC-689's fixture too thin for the "full language" clause | **closed**: `signalsFor()` carries a media element and a painted panel; the test asserts `kinds` contains `text`, `image` and `box` off the `l1.json` read back from the bundle |
| 2 — AC-691 made no height assertion | **closed**: asserts `kfs.every(k => k.height === undefined)` for the text leaf and a pinned height per keyframe for the image and box leaves; the AC's widest-sample clause was also correctly rescoped to *static* axes (prior warning 9) |
| 4 — pinned-box content overflow uncovered | **closed**: AC-1630 + `test_UAT_AC1630_*` covers the finding shape, the epsilon boundary (63/64/65px), the off-sample probe at an unsampled width, and the robustness probe at every captured width |
| 5 — padding fold unstated | **closed**: AC-1626 + `test_UAT_AC1626_*` (four sides, absent/zero negatives, image leaf, render inset) |
| 6 — responsive scalar track unstated | **closed**: AC-1625 + `test_UAT_AC1625_*` (three type axes, per-side padding, constant-axis negative, narrow-vs-desktop CSS) |
| 7 — viewport-height response unstated | **closed**: AC-1627 + `test_UAT_AC1627_*` (measured factors, probe-is-not-a-keyframe, no-probe negative with a byte-for-byte document comparison) |
| 10 — AC-731's per-run backing-box text was stale | **closed**: rewritten today to the band → card model |
| 11 — AC-729's framing clause delegated to siblings | **closed**: `test_UAT_AC729_*` now asserts the closed union `objectPosition` + `filter` + finishing axes on one leaf |

Prior finding 8 (self-painting run) is **half** closed — AC-731 now states it, but
no AC-named UAT asserts it. That is finding 1 below.

## Findings — Categorized by Editor Action

| # | Severity | Level | Element | Resolution category | Issue | Suggested edit |
|---|---|---|---|---|---|---|
| 1 | violation | ac | AC-731 | uat-edit | AC-731's body was widened today (14:41) with three clause-clusters its UAT does not reach. `test_UAT_AC731_*` (`tests/reconciliation-l1-fold-full-language.test.ts:317-392`) asserts only the original claim — dominant fill → band, differing/gradient surfaces → cards, boxes ordered ahead of content. It does **not** assert: (a) the **self-painting run** (pill radius ≥ half painted height, authored vertical inset → surface on the text leaf, **no** backing box; gradient / left-accent keeps the backing box) — BUG-20/BUG-21; (b) the **full-bleed bar** rule (edge-hugging same-fill runs → one band; evenly-tiled grid → cards) — BUG-19; (c) the **captured surface rect** as the backing box's geometry, radius and grouping identity (two runs on one rect merge, two on different rects never do; a viewport-wide surface is the band) — REQ-88 round-5. A regression in any of the three leaves `test_UAT_AC731_*` green. | Extend `test_UAT_AC731_*` to the clauses the AC now states, or re-attribute the existing free-coded suites that already prove them: `tests/bug20-chip-self-surface.test.ts`, `tests/bug21-control-surface-outset.test.ts`, `tests/bug19-fold-bar-band-fill.test.ts`, `tests/req88-surface-shape-and-fontface.test.ts`. Prefer re-attribution over a second copy (one-authoritative-location rule) |
| 2 | violation | ac | AC-731 | uat-add | AC-731's fourth new clause — **the page base is chosen by painted extent, not run count** — has evidence **nowhere in the repo**. `fold.ts:2107-2140` sums `bandHeightByFill` over the reconstructed bands **and** the captured backdrops (BUG-27), takes the greatest total height, and falls back to the most-common run fill, then to the captured canvas fill. The only assertion on `doc.background` anywhere is `tests/bug14-fold-surface-hierarchy.test.ts:96` — `expect([BAND1, BAND2]).toContain(doc.background)` — which cannot distinguish extent from count. `test_UAT_AC731_*`'s own fixture makes the tallest band and the most-common run fill the **same** colour (`BAND`, 3 runs), so it cannot discriminate either. The AC's Verification demands all three rungs of the chain. | Author the three fixtures the AC names: (i) a fold whose tallest reconstructed band is *not* the most-common run fill → assert the taller band wins; (ii) a fold that reconstructs no full-bleed band → assert the most-common run fill; (iii) a fold with neither → assert the captured canvas fill. Add a backdrop-only page to prove backdrops count toward the extent (BUG-27) |
| 3 | violation | ac | AC-694 | uat-edit | The extraction AC-694 claims is still unproven, and the run confirms it. On the always-run path all six sidecar dimensions are now asserted — but against `CANNED_HINTS` (`tests/reconciliation-l1-fold.test.ts:196-252`), a constant **the test itself hands `FakeDriver`**, so `HINTS_SCRIPT` never executes and the assertions prove the capture→`hints.json`→`readHints` plumbing, not the derivation. The real-engine half sits behind `if (!(await chromiumAvailable())) return` (`:624`) — a **silent** early return: the test reports green having asserted nothing about extraction. It did not run this session (233ms for three files; a Chromium launch cannot fit). Even when it does run it asserts only 3 of the 6 dimensions — ancestry, position mode and sibling-repetition count are asserted on **neither** path. Reported for a second consecutive cycle. | Two edits, both needed: (a) make the skip **visible** — hoist the engine half into its own `it.skipIf(!chromiumAvailable)` case so an unrun branch reports as skipped rather than passed; (b) extend the engine assertions to `parentId` (the `.col` children name the `.row` section), `position` (`static`) and `repeatCount` (2) — all already derivable from the existing fixture HTML at `:625-631`. Optionally add always-run extraction evidence by driving `HINTS_SCRIPT` against an in-process DOM so the dimension is not wholly environment-gated |
| 4 | warning | ac | AC-1625…AC-1631, AC-1630 | — (informational) | The eight ACs created today carry `status: pending` rather than `active`. Coverage is unaffected — all eight have substantive passing UATs and are graded `pass` here — but a `pending` AC may not be counted by downstream matrix aggregates. | Not this workflow's field; flagged for whichever stage owns AC `status` promotion |

## Notes for the Editor

**Findings 1 and 2 are one AC and one root cause.** AC-731 absorbed four
behaviour clusters today so that STORY-84's body would stop under-describing the
fold — which was the right repair for the *matrix*, but it moved the gap rather
than closing it: the AC now claims four things its UAT does not test. Finding 1 is
cheap (the evidence exists; re-attribute or extend), finding 2 is real authoring
(no test anywhere asserts the page-base ordering). Consider whether AC-731 should
be **split** — it is now the largest AC in the capability by a wide margin, and a
single `test_UAT_AC731_*` proving band reconstruction, self-painting
discrimination, bar detection, surface-rect adoption *and* page-base inference
would be one test carrying five unrelated failure modes.

**Finding 3 is the only carry-over.** Every other prior finding closed. The
`chromiumAvailable()` silent return is the last instance of the pattern in this
capability's suites — a grep across all nine files shows one occurrence
(`reconciliation-l1-fold.test.ts:624`) and no other conditional skips, no
`vi.mock` of internal modules, and no source-text/structural assertions. The one
`vi.spyOn(globalThis, 'fetch')` (`reconciliation-l1-fold-seams-and-refold.test.ts:540`)
is legitimate: it is AC-814's *assertion* that an offline re-fold does not re-hit
the captured origin, not a mock of the thing under test.

**The suite is green and fast.** 41 tests, 41 ACs, ~0.6s of test time total. That
makes it cheap for the fix loop to run these files after each edit rather than
reasoning statically, which is how the last six cycles had to work.

**No `needs_review`, and no BUG-1306 defaults were needed.** Every behaviour in
both story bodies traces to a reconciled intent; the ledger above names the intent
for each. Nothing in this capability is waiting on an operator decision.

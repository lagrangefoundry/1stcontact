---
uid: report-0130303d
id: REPORT-1807
type: report
title: Claude reconciliation_story_generation report
created_by: xgd
created_at: '2026-08-10T11:21:20.334095+00:00'
updated_at: '2026-08-10T11:21:20.334095+00:00'
completed_at: null
last_field_updated: created_at
fields:
  report_kind: reconciliation_story_generation
  subject_uid: bug-ede1fb8c
  plan_item_index: '1'
---

All three mutations committed, each touching only its own ticket file — no runtime code in any of them.

## Stage 1 — Targets validated

**In scope (changed):** `story-e674c60a` (STORY-99), `acceptance_criterion-60683857` (AC-970), plus one new AC under that same story.

**Explicitly out of scope (unchanged, verified):** AC-971 (open-in-new-tab target — the plan says it stands as written, and it does); AC-967/AC-968 (checked for overlap — AC-967 is selector→panel, the new AC-970 clause is panel→selector; AC-968 is about the *pane* not being rebuilt, which the toolbar's persisting root element keeps consistent with); every other STORY-99 AC; STORY-101 / `story-3bf94bd4` and its `openLoneControl` criteria (AC-1044, AC-1004, AC-997–1000), which the plan correctly declined to touch; all runtime code.

## What the code actually showed

Every claim in the plan item held up against `toolbar.js` and `panel.js`:
- `offPanel = [panel.on('mode', render), panel.on('site', render)]` — the strip re-derives on both.
- `render()` → `disposeActions()` runs every `actionCleanups` entry, clears `mounted`, `replaceChildren()`s — replaced controls are released.
- `siteSelectorAction.create` reads `panel.getSite()` at construction, so the rebuilt selector follows the site whatever set it — including `restore()`, which routes through `setSite`.
- Both `setMode` and `setSite` early-return on an unchanged value, so the trigger is genuinely *a change*, which I made explicit in the criterion rather than leaving "whenever what is displayed changes" to be read as "on every call".

## Mutations applied

**AC-970 modified** — title and body widened: trigger is now a mode *or* site change; each control is built against the mode and site current at that moment; the strip persists while its contents are replaced; a no-op set re-derives nothing. Verification gained the site-change half, including site changes made *without* the selector, so the property can't pass by the selector merely updating itself.

**AC-1110 added** (`acceptance_criterion-8cc0c9f2`) — control lifetime: a replaced control stops reacting, re-derivation leaves one live updater per on-screen control and none for its predecessors, and tearing the chrome down releases the strip the same way. I kept the criterion observable ("stops reacting") and put subscription-counting in Verification only, since counting at the panel is the mechanism, not the outcome. I folded the teardown clause into this one criterion rather than adding a second AC — it's the same property (lifetime = element lifetime) at the other end, and the plan was deliberate about one item.

**STORY-99 modified** — the "A toolbar the active mode declares" scope bullet now states the wider trigger and control release; two Technical Context bullets added, one explaining why the *whole* strip re-derives and what the detached-survivor consequence cost, one recording the plan's stale-comment observation (`// Re-render on every mode change` sits above a line subscribing to both) as a commentary divergence with no behavioural difference.

## Two things to flag

- **AC-970 still carries `uat_coverage: pass` from before the widening.** I deliberately did not reset it: `validator.py:153` defines `uat_coverage` as a verdict-only field owned by the `check_uat_coverage` / `fix_uat_coverage` loop, and this prompt's contract forbids touching UATs. The widened clauses get their evidence from the downstream reconciliation UAT step. Worth watching that the coverage loop does re-evaluate it — a stale `pass` on a widened criterion is precisely the silent-green shape this bug family is about.
- **`xgd ticket children story-e674c60a` returns AC-970 twice** (31 rows, 30 distinct uids). Only one file exists on disk (`.xgd/tickets/hot/acceptance_criterion-60683857.md`), so this is an index listing artifact — the updated ticket appearing in both the hot and cold shards — not a duplicated ticket. I did not run `rebuild-index`, since that mutates shared state under a live reconcile run.

```
Upgrade mutations applied for plan item 1 of 1

Target Stories: story-e674c60a
Primary Story UID: story-e674c60a
Stories Modified: 1
ACs Modified: 1
ACs Added: 1
ACs Removed: 0

tickets_modified:
  stories:
    - "story-e674c60a"
  acceptance_criteria:
    modified: ["acceptance_criterion-60683857"]
    added: ["acceptance_criterion-8cc0c9f2"]
    removed: []

Progress: 1 of 1 plan items complete
```

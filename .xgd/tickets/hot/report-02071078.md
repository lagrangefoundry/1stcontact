---
uid: report-02071078
id: REPORT-1284
type: report
title: 'Capability-Intent Alignment: capability-modules (level=uat)'
created_by: xgd
created_at: '2026-08-05T18:19:54.624317+00:00'
updated_at: '2026-08-05T18:19:54.624317+00:00'
completed_at: null
last_field_updated: created_at
result: pass
fields:
  report_kind: capability_validation
  subject_uid: capability-ce902be4
  level: uat
  violations: 0
  warnings: 2
  needs_review_count: 0
---

# Capability-Intent Alignment: capability-modules
# Level: uat

**Result**: PASS
**Violations**: 0
**Warnings**: 2
**Needs review**: 0

## Headline

`capability-ce902be4` (CAP-72) holds **zero stories**, therefore **zero ACs**,
therefore **zero UATs**. The UAT-level check is vacuously satisfied: there is no
test in this capability's tree that could fail to exercise its AC, leave an AC
uncovered, or duplicate another test's scenario.

This is the intended outcome of the structural rebalance earlier in this same
regression (`report-bdaf6840`, 2026-08-05), which absorbed CAP-72 into
**`capability-ae9d65d6`** (CAP-70, "Framework Substrate: L1 Layout, Values &
Behavior Modules"). The capability body documents the absorption and carries
`merged_into: capability-ae9d65d6`.

Per the level cascade, the story-level pass (`report-248c0b33`, PASS, 0
violations — transfer was lossless) and the AC-level pass (`report-666132a3`,
PASS, 0 violations — AC tree empty by design) are my working reference. I found
no evidence forcing me to reopen either.

The UAT-level question that *is* live here is the same trap the AC level named,
one layer down: **do not audit the nine tests in
`tests/reconciliation-behavior-modules.test.ts` against this capability.** The
ticket index says they belong here. They do not. Details in warning 2.

## Verification Method

The ticket index on this branch worktree is unreliable (warning 2), so I did not
trust `--filter fields.capability_uid=...`. UAT-level state was established from
four independent sources, all agreeing:

1. **Per-ticket read.** `xgd ticket get story-179b8c06` (STORY-85, the only story
   the stale filter attributes here) reads `capability_uid: capability-ae9d65d6`,
   `updated_at 2026-08-05`. The filtered list serves a `2026-07-24` snapshot.
2. **Direct ticket-type sweep.** `--filter fields.capability_uid=capability-ce902be4`
   for types `acceptance_criterion`, `uat`, `task`, `report` returns **empty** in
   every case. No UAT ticket claims this capability.
3. **UAT index.** `.xgd/uat_index.json` (rebuilt by the rebalance run; 86 ACs, 87
   UATs) contains **zero** references to `capability-ce902be4`. Index entries are
   keyed by AC (`ac697`…), not capability, so no UAT is attributable here.
4. **Source grep.** No file under `packages/`, `tools/`, `tests/`, or `src/`
   references `ce902be4` or `capability-modules`. No test is tagged to this
   capability by name or UID.

## Cumulative Intent Considered

| Intent ID | UID | Status | When | Asked / changed | Counts? |
|---|---|---|---|---|---|
| REQ-79 | request-87b26bca | free_and_reconciled | 2026-07-19 | Framework pivot: L1 layout substrate + capability modules (safety envelope) | YES |
| REQ-84 | request-f243b6b9 | free_and_reconciled | 2026-07-20 | Pivot C: strip layout modules to L1; retires "module = bundle of aesthetic dials" | YES (retired prior model) |
| REQ-85 | request-015e42ac | free_and_reconciled | 2026-07-20 | Pivot D: module contract (config/slots/conformance) + reframe carousel & contact-form — **founding intent for this capability** | YES |
| REQ-87 | request-84af044b | free_and_reconciled | 2026-07-21 | Mechanical rename `capability module` → **behavior module**; `kind: 'behavior'` discriminant; no back-compat alias | YES |
| BUNDLE-7 | bundle-31e474b9 | free_and_reconciled | 2026-07-22 | Reconciliation vehicle for REQ-63/79/82/83/84 +2; STORY-85's `intent_uid` | YES |
| REQ-96 | request-3a064234 | ready_to_reconcile | 2026-07-26 | Behavior modules layout-agnostic by construction (L1 `control` node; module ships zero CSS) | imminent — scope now under capability-ae9d65d6 |

**Coverage consequence at UAT level.** Every reconciled intent above is expressed
through STORY-85's nine ACs, and each of those nine ACs carries exactly one named,
existing UAT — all under `capability-ae9d65d6`. Nothing is orphaned; nothing is
expressible here, because this capability has no AC to hang a test from. There is
no `uat-add` repair available or appropriate against this subject.

## Alignment Ledger

| Element | Intents aligned to | Outcome |
|---|---|---|
| capability-ce902be4 (UAT tree) | — | **Empty by design.** Zero stories ⇒ zero ACs ⇒ zero UATs. Vacuously consistent, complete, and exclusive. |
| `tests/reconciliation-behavior-modules.test.ts` — 9 UATs (AC697–AC704, AC722) | REQ-79, REQ-84, REQ-85, REQ-87, BUNDLE-7 | **Out of subject scope.** Attached to STORY-85 (story-179b8c06), whose capability is `capability-ae9d65d6`. Verified present in source (1 file per AC, 1:1 AC→test); UAT-level alignment is owned by CAP-70's pass, not this one. Listed only so a reader who sees them via the stale index knows why they are excluded. |

## Findings

| # | Severity | Property | Element | Resolution category | Issue | Suggested edit |
|---|---|---|---|---|---|---|
| 1 | warning | consistency | capability-ce902be4 | *(none — system defect)* | Capability remains `status: active` and — squarely a **UAT-level** field — still carries `uat_coverage: pass` while holding zero stories, zero ACs and zero UATs. The attestation is now vacuous: it certifies UAT coverage of a tree that no longer exists, and a consumer reading it as evidence would be misled. Deprecation was **blocked, not skipped**: `reject_deprecation_if_capability_has_stories` → `attached_story_ids()` reads the canonical main store, which still holds pre-merge `capability_uid` values, so it reports phantom attached stories (`report-bdaf6840`). Unchanged since the story- and AC-level passes. | No matrix edit. Flip `status: deprecated` (which moots `uat_coverage`) in a later run once the index resolves correctly — an xgd-repo fix, not a matrix repair. |
| 2 | warning | exclusivity | capability-ce902be4 (index) | *(none — system defect)* | **UAT-level manifestation of the index defect.** `xgd ticket list --type story --filter fields.capability_uid=capability-ce902be4` returns STORY-85 (`updated_at 2026-07-24`) while the ticket reads `capability-ae9d65d6` / `2026-08-05`. A naive UAT-level pass that trusts this filter would walk STORY-85 → its nine ACs → their nine UATs in `tests/reconciliation-behavior-modules.test.ts` and audit them against a capability that does not own them — **double-auditing the same nine tests alongside CAP-70's own UAT pass**, which is precisely the exclusivity failure this property exists to catch. Same root defect duplicates the story list (21 index entries for 12 distinct stories) and breaks human-ID resolution outright (`xgd ticket get STORY-85` → "not found"; UID lookup succeeds). | None here. Any re-run of a UAT-level check on this branch must resolve stories via per-ticket UID reads, as this report did — never via filtered lists or human IDs. |

No violations. No `needs_review`: the intent ledger is explicit at every point
where this capability's UAT-level state could have looked ambiguous, and the
empty UAT tree is a documented, verified consequence of a sanctioned rebalance.

## Notes for the Editor

**Do not "fix" the empty UAT tree.** There is no `uat-add` work here. The
tempting repair — authoring UATs against CAP-72 because the index shows it owning
a story, or re-attaching STORY-85 — would undo a verified rebalance and
reintroduce the imbalance it corrected. The ticket file is authoritative; the
index entry is stale.

**Both warnings are one xgd-repo defect, already filed.** On a branch worktree
the ticket index resolves to the canonical main store and serves pre-merge
`capability_uid` values. `xgd ticket rebuild-index` refuses on branch worktrees
by design, so this cannot be cleared from here. `report-bdaf6840` already raised
it (with the `.xgd/uat_index.json` all-zero-counts defect) against
`/Users/martin/lagrangefoundry/xgd`. No matrix edit repairs either; none should
be attempted. This is the third consecutive level (story → ac → uat) to report
the same pair unchanged — it is stable, not degrading.

**One UAT-level observation for the CAP-70 pass, not for this subject.** All nine
of STORY-85's ACs read `status: missing` in `.xgd/uat_index.json` — meaning *not
yet run in this index*, not *absent*. I verified all nine test functions exist in
`tests/reconciliation-behavior-modules.test.ts` (1:1 AC→test), so this is a
run-status artifact of the freshly rebuilt index, not a coverage gap. Flagged so
CAP-70's UAT pass does not misread `missing` as `uat-add` work.

**Handoff items already routed to CAP-70** (recorded at story/AC level, restated
so nothing is lost in the level chain): the REQ-96 coverage gap (`ready_to_reconcile`,
needs AC + UAT expression under CAP-70 when it reconciles), and the stale
post-rebalance "Out of scope" cross-references in STORY-85's body (cosmetic
`story-body-edit`, likely recurring across all absorbed stories — worth one sweep).

**The REQ-87 rename is settled — don't re-open it at UAT level.** Two deliberate
non-changes read like incomplete renames and must not be "corrected" in test
fixtures or snapshots: the emitted asset filename stays `capabilities.js` (a
plural bundle-output name, not a type or discriminant — renaming it breaks the
page reference), and the English-word uses of "capability" in the capture layer
(driver capability negotiation) and site schema ("schema-only capability") are
correct English, not the renamed type. `test_UAT_AC722_*` encodes the rename
contract correctly as-is.

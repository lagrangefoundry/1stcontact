---
uid: report-6125630f
id: REPORT-4125
type: report
title: 'Reconcile resolve conflicts: reconcile-BUNDLE-27'
created_by: xgd
created_at: '2026-09-12T20:18:05.662492+00:00'
updated_at: '2026-09-12T20:18:05.662492+00:00'
completed_at: null
last_field_updated: created_at
result: pass
fields:
  report_kind: resolve_conflicts
  subject_uid: reconcile-BUNDLE-27
---

## Files resolved

- `.xgd/tickets/hot/bundle-78f4e2fe.md` — class **AA** (both added), intent/bookkeeping
  ticket (STEP 2 rule 2b + 2e). Resolved to **ours** (`git checkout --ours` +
  `git add --sparse`).

  Rationale — this is the seeded-overlay artifact, not a competing edit:
  - HEAD-side history for this path (`git log --oneline -- <path>`) roots at
    `06ba736f57 xgd(ticket): seed_local_overlay bundle bundle-78f4e2fe` followed by 24
    `update` commits, and contains **no** `create` commit. Git therefore has no merge
    base for the path and reports AA rather than UU.
  - The seeded overlay (`06ba736f57:<path>`) is byte-identical to the incoming create
    (`efdc5802db`) across all 622 lines except `updated_at`/`last_field_updated`/`status`
    — i.e. ours is the lineal descendant of the very commit being applied.
  - Immutable fields are identical on both sides: `uid`, `id`, `type: bundle`,
    `title: BUG-36 + BUG-37 + BUG-38`, `created_by: xgd`,
    `created_at: 2026-08-26T17:36:26.915596+00:00`.
  - Ours is the later-positioned intent for every contested fact:
    `status: free_and_reconciled` vs incoming `ready_to_reconcile`;
    `updated_at 2026-08-31T19:19:50` vs incoming `2026-08-26T17:36:26` (== its
    `created_at`); ours additionally carries `completed_at`, `result: pass`,
    `orphan_commits` (21 entries), `merged_at_commit`, and the `commits` list advanced
    from five `working_sha` entries to the resolved
    `main_sha: 96a76934e010d272feb2d2bfc2b5d9645db10fe8`.
  - The only other conflicting hunk was the trailing newline on the final line of the
    body (`tests/test_UAT_FC_REQ-122_chat_host.test.ts`) — identical text on both sides.

  Safety check before `--ours`: `git diff HEAD -- <path>` showed **every** hunk bounded
  by `<<<<<<<` / `=======` / `>>>>>>>`. No hunk sat outside a conflict region, so no
  cleanly auto-merged incoming content was discarded by restoring stage 2.

## Incoming changes preserved

No code/implementation files were conflicted — the only conflicted path is a bundle
bookkeeping ticket.

The incoming commit `efdc5802db` is a pure `create` of this ticket (622 insertions, one
file). Its entire content is present in HEAD via the `seed_local_overlay` commit
`06ba736f57` and the subsequent lifecycle updates; nothing from the incoming side is
absent. This is the redundant case (BUG-1109/BUG-1122), not a discard: the incoming
commit's key changes are *present in HEAD*, merely arrived by a different route.

Accordingly `git diff --cached HEAD` is empty. Per STEP 4 no `git cherry-pick --skip`
was issued — the finalize step detects the clean staged diff. `CHERRY_PICK_HEAD` is
still present (`efdc5802dbeffa2254dcc3fc00ec12a873e64560`).

No hunks were dropped under the BUG-1301 precedence exception.

`git status --porcelain` is empty — no conflict-class lines remain.

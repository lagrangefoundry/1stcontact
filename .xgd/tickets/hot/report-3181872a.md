---
uid: report-3181872a
id: REPORT-3222
type: report
title: 'Reconcile resolve conflicts: reconcile-REQ-162'
created_by: xgd
created_at: '2026-09-01T21:50:22.133682+00:00'
updated_at: '2026-09-01T21:50:22.133682+00:00'
completed_at: null
last_field_updated: created_at
result: pass
fields:
  report_kind: resolve_conflicts
  subject_uid: reconcile-REQ-162
---

## Files resolved

- `.xgd/tickets/hot/doc-e70ef718.md` — **AA** (both added; no merge base, so every
  difference surfaced as a conflict). Class 2b/2e per-fact resolution → took the
  INCOMING side in full, after proving that is lossless.

  Two conflicted hunks, resolved per fact:

  1. **Frontmatter bookkeeping** (`updated_at`, `last_field_updated`).
     Ours: `2026-08-16T01:19:16Z` / `system_kb`. Incoming: `2026-08-31T00:50:27Z`
     / `body`. Incoming is the later intent and its metadata correctly describes
     the newer body edit → incoming.
  2. **§6.3 "What is in it"**. Ours held the ORIGINAL prose ("Every design
     document, in full… deliberately no curation pass"). Incoming is a deliberate
     correction of exactly that paragraph — commit 89e17d7344, message: *"content
     edit: correct 6.3 — the corpus was seed material for testing, never a
     curation policy; cite DOC-39 for what the KB should hold"*. Not two competing
     edits; incoming supersedes the text it names → incoming.

  **Lossless proof for taking theirs.** `git diff :2:<path> :3:<path>` shows the
  two hunks above are the ONLY differences between the sides. HEAD's sole
  substantive edit on this file (40a5a974c7, 2026-08-15, adding
  `fields.system_kb: true`) is already present in the incoming version at line 14,
  outside any conflict region — the incoming commit was authored on top of it. So
  no HEAD-side fact is discarded by taking theirs.

  Staged with `git add --sparse` (path is outside the sparse-checkout cone on this
  reconcile branch, DOC-986 §2/§4.1).

## Incoming changes preserved

Resolved file is byte-identical to stage 3 (verified via `git diff --no-index`
against `git show :3:<path>`). All incoming markers confirmed present:

- `updated_at: '2026-08-31T00:50:27.937839+00:00'` (line 8)
- `last_field_updated: body` (line 10)
- `**Seed material, not a policy.**` (line 239)
- `[[DOC-39]] §3` citation (line 256)
- `**The seed set stays until the machinery is proven.**` (line 261)

HEAD-side field `system_kb: true` also retained (line 14).

Zero conflict markers remain; `git ls-files -u` returns 0 unmerged entries.
Staged diff vs HEAD: 28 insertions, 8 deletions — the incoming content edit.
No hunks were dropped, so the BUG-1301 precedence exception does not apply here.

## Note for post-merge review

The auto-enriched intent metadata flagged this file as "intent unknown on one or
both sides." The resolution did not rest on that fallback — the incoming commit
message states its operation explicitly (content edit to §6.3), and the HEAD-side
edit was verified to be a disjoint frontmatter field already carried by the
incoming version. No timeline tiebreak was needed.

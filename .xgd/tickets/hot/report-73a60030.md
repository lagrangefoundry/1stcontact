---
uid: report-73a60030
id: REPORT-3104
type: report
title: 'Reconcile resolve conflicts: reconcile-BUNDLE-22'
created_by: xgd
created_at: '2026-08-31T22:15:59.036529+00:00'
updated_at: '2026-08-31T22:15:59.036529+00:00'
completed_at: null
last_field_updated: created_at
result: pass
fields:
  report_kind: resolve_conflicts
  subject_uid: reconcile-BUNDLE-22
---

## Files resolved

- `.xgd/tickets/hot/bug-23d1ec27.md` — **UU**, class 2e (intent/bookkeeping
  ticket). Index-only conflict: the path is outside the sparse-checkout cone
  (DOC-986 §2/§4.1), so there were no working-tree markers. Resolved with
  `git checkout --ours` + `git add --sparse`, each issued as its own call.

  Incoming commit: `7f1350e9a56fc0c6292aa72c851b34a80dda8aae`
  ("xgd(ticket): update bug bug-23d1ec27", 2026-08-25 16:28:10 -0700).
  Its whole diff is a `story_points` bump: adds `fields.story_points: 3`,
  sets `last_field_updated: story_points`, advances `updated_at`.

  **Rule applied — 2e "one side is a strict superset", with the per-fact
  timeline rule for the facts both sides set differently:**

  | fact | ours (HEAD) | theirs (incoming) | resolution |
  |---|---|---|---|
  | `fields.story_points` | `3` | `3` | identical — incoming's contribution already present |
  | `fields.commits[].working_sha` | `759cd874…` | `759cd874…` | identical |
  | `fields.version` | `0.2.15` | `0.2.15` | identical |
  | `updated_at` | `2026-08-31T05:05:09` | `2026-08-25T23:28:10` | **ours** — later |
  | `status` | `bundled` | `free_coded` | **ours** — later timeline position |
  | `last_field_updated` | `status` | `story_points` | **ours** — derived trace of the later edit |
  | `fields.bundled_in` | `bundle-8eef3846` | *(absent)* | **ours** — fact only on our side |
  | trailing newline | present | absent | **ours** — normalized form |

  `story_points` — the entire point of this commit — is identical on both
  sides, so there is nothing to compose there. The facts that do differ all
  resolve to ours on the timeline rule: HEAD's `bundled` / `bundled_in:
  bundle-8eef3846` is this reconcile bundle's own bundling operation, which by
  construction postdates the `free_coded` commit being picked, and the
  `updated_at` stamps agree (08-31 vs 08-25). `last_field_updated` is a derived
  trace field naming whichever field was written most recently; ours correctly
  names `status`, the later write. Taking theirs would regress the ticket from
  `bundled` to `free_coded` and drop `bundled_in`.

  No `fields.intent_uid` / `story_uid` / `capability_uid` were touched, and no
  content was invented that is not on one of the two sides.

## Incoming changes preserved

Verified against `git show 7f1350e9a56fc0c6292aa72c851b34a80dda8aae --
.xgd/tickets/hot/bug-23d1ec27.md`. The incoming commit's sole substantive
addition is `fields.story_points: 3`, and it is present in the resolved file
(`git show HEAD:.xgd/tickets/hot/bug-23d1ec27.md`):

```
24:  story_points: 3
```

alongside the rest of the ticket's field block unchanged (`working_sha`
`759cd874…` at line 20, `version: 0.2.15` at line 23). Nothing from the
incoming diff is absent.

No hunk was dropped under the BUG-1301 precedence exception; none needed it.

## Note for the finalize step

The staged diff nets to **no change vs HEAD** (`git diff --cached --stat HEAD`
is empty). This is the redundant-commit case (BUG-1109/BUG-1122): a
post-watermark sync already landed this commit's effect through a different
route, and HEAD then advanced the ticket past it. This is the second
consecutive commit in this bundle to land that way — attempt 236's
`0941885b` (the `commits`/`version` bump) resolved identically and was skipped
by finalize, leaving HEAD at `fda1aad1f8`, unchanged.

This is *not* a STEP 3 discard: the incoming commit's key change is
demonstrably **present** in HEAD (quoted above), not absent.

Per STEP 4, `--skip` was NOT called and the cherry-pick sequencer state is
untouched — `CHERRY_PICK_HEAD` remains at
`7f1350e9a56fc0c6292aa72c851b34a80dda8aae`.
`cherry_pick_finalize_resolution` should detect the clean staged diff and skip
the commit itself.

`git status --porcelain` shows zero non-untracked lines: no conflict-class
(UU/AA/DU/UD) entries remain.

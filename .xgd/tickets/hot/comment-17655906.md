---
uid: comment-17655906
id: COMMENT-2607
type: comment
title: Comment on request REQ-215
created_by: xgd
created_at: '2026-09-10T21:24:50.762228+00:00'
updated_at: '2026-09-10T22:08:52.950980+00:00'
completed_at: null
last_field_updated: body
status: null
fields:
  subject_uid: request-8a5f499e
  kind: chat_transcript
---

<!-- xgd-turn id="8d3fc848-ecc6-4695-bf52-94bdd3a0d00d-user" -->

<!-- xgd-chat role="user" ts="2026-09-10T21:24:31.289152+00:00" -->
#### You
Please implement this ticket following the FREE CODING protocol. If you have any questions that you need to answer before implementation please ask them here

Project note (additive — does not override the above):
Please make sure you have clarity on the user interaction model and the core design. That you understand the API contracts, what you should be built from scratch and when code must be reused. We try to minimise external dependencies and maximise code reuse because the cost of code is not just in its creation but its maintenance.
Please do make implementation decisions and low level UI decisions yourself. It is generally cheaper to fix the details in a working system than it is to try to get them all right upfront, provided the general shape is correct

<!-- xgd-turn id="8d3fc848-ecc6-4695-bf52-94bdd3a0d00d-assistant" -->

<!-- xgd-chat role="assistant" ts="2026-09-10T21:53:00.894141+00:00" -->
#### Claude
I have the full picture. Let me record the design on the ticket before writing any code.

Now let me create the dialog vocabulary module.

Now update `render.ts` to use them and make the shell channel-independent.

Now register the browser asset entry.

Now the carry module.

Now the panel hooks.

Now wire it into `app.js`.

Now let me write the UATs.

Now merging back to `xgd-working` under the branch lock.

Done — REQ-215 is implemented, merged into `xgd-working`, and the ticket is at `free_coded`.

## What I built

**The rule:** switching View↔Edit now shows the same page in the same state — same page path, same scroll offset, same panels open.

**Three pieces:**

1. **`packages/framework/src/l1/render.ts`** — the edit render now emits the modal apparatus in full (covering shell, `data-l1-dialog` id, `role="dialog"`, both halves of the overlay stylesheet). It used to drop all of it along with the script, which left nothing in the edit document that a channel switch could *tell* anything. Emitting it costs that channel nothing: every overlay rule is gated on a marker only a script — or the builder — sets, so an edit render nobody has told anything still lays the panel out in flow and settled, exactly as REQ-116 wants. The one line that keeps it inert is now the script gate, and it's the only one that has to be.

2. **`packages/framework/src/l1/page-state.ts`** (new, plus `dialog.ts` for the shared attribute vocabulary) — reads a page's state off a window and puts a window into one. The two channels differ deliberately: **Edit is told** (markers set directly — no script, none wanted), **View is driven** (the opener is *activated*, so the page's own script does the opening and focus, the scroll lock and `aria-expanded` are all true afterwards). It's the same handle REQ-216 needs.

3. **The builder** — `carry.js` holds the state across the swap that destroys the document; `panel.js` announces when a document is going and when one has arrived; `previewUrl` gained the path within the channel. Edit also gains a **panel selector**, because a modal reproduced there covers the page and its own Close is inert — it's the escape hatch and the way to edit copy inside a modal you never opened in View.

**Deliberate scope:** L1 dialogs only. A behaviour module's panel (`account-chrome`'s sign-in dialog) keeps its settled state — reaching into one would mean this module knowing what an `account-chrome` is, which is what the carousel's own rule forbids. So the ticket's open question is answered: `data-fc-edit` does *not* grow to carry which state; it stays the fallback for "nobody said".

**Supersession, stated explicitly on the ticket:** REQ-212 §5.7 / AC-12's "no shell, no role in edit" becomes "the shell and the role, always". I updated that UAT rather than leaving reconcile to discover the conflict.

## Verification

- **22 new UATs** across three files (renderer, page-state driven end-to-end in jsdom with the real inline script, the builder carry, and the real toolbar/panel composition) — all green.
- Full suite: 3438 tests, 19 failures. I checked every one: all pre-existing (the priming/KB prompt-text family, `points.js`, a missing `:has(.builder-modal__box` CSS rule, an unprobed `/api/material/changes` route) or parallel-load flakes that pass in isolation. `git show HEAD:` confirms the source-level ones predate this branch.
- Typecheck clean in `packages/framework` and `tools/generate`; `apps/control-app` has its usual 8 pre-existing `TS7016` errors, none in changed files.
- `./bin/1c assets` rebuilt in the working checkout, so your dev server serves `/framework/page-state.js` — reload the builder and it's live.

## Two things worth knowing

- **There's a brief flash on switching to Edit with a panel open**: the edit render paints the panel in flow (the settled fallback) for a frame or two before the builder marks it open. I left it rather than hiding the frame during every navigation, which trades one artifact for a worse one. Easy to revisit.
- **REQ-216 is being worked in parallel** and claimed version `0.2.155` mid-merge; I re-bumped to `0.2.156`. Both tickets want the same "which panel is open" handle, and `page-state.ts` is it — worth pointing that session at it rather than having it grow a second one.

<!-- xgd-turn id="879c7905-bc60-4710-a625-82aed2bbc549-user" -->

<!-- xgd-chat role="user" ts="2026-09-10T22:08:51.711036+00:00" -->
#### You
please complete the free-coding of this ticket

Project note (additive — does not override the above):
Please be aware that in this current chat environment, background jobs die. Please be sure to run any tests in the foreground.

<!-- xgd-chat-end -->
---
uid: report-506d1389
id: REPORT-2319
type: report
title: 'Resync resolve conflicts: 7292af8f63220275216b82416fc441658a744e02'
created_by: xgd
created_at: '2026-08-20T02:33:22.303364+00:00'
updated_at: '2026-08-20T02:33:22.303364+00:00'
completed_at: null
last_field_updated: created_at
result: pass
fields:
  report_kind: resolve_conflicts
  subject_uid: report-db1949d9
---

## Files resolved

- `package.json` — class **UU**, scalar conflict on the `version` field only.
  Enrichment gave no xgd-kind for the incoming side, so the stated rule applied:
  *take the more recent commit by timestamp and flag for post-merge review*.
  - Ours: `0f44ef1ba06d0e071fbe726db099d5908cc425e4` — `xgd: sync from xgd-working 097e8bc90814 (post-watermark)`, committed 2026-08-19 17:43:02 -0700, `"version": "0.1.59"`.
  - Theirs: `87306fa43ea10900fecabea6d00f47b11184a3e2` — `fix(test): enumerate stored sites by directory, not raw dirent [FREE-CODED]`, committed 2026-08-13 09:40:08 -0700, `"version": "0.1.42"`.
  - Ours is the later commit by six days, so **`0.1.59` was kept**. This is also the
    only non-regressing choice: the version scalar is a monotonic counter and main
    has already published through 0.1.59, so taking the incoming `0.1.42` would have
    rewound it onto a value later tickets have already consumed.
  - No other hunk in `package.json` conflicted; the rest of the file is untouched.

**Flagged for post-merge review** (per the rule's second clause): `package.json`.
The flag is low-risk here — the whole disputed region is the one version scalar,
and the incoming side carried no other edit to this file.

## Incoming changes preserved

The incoming commit touches two files. Both were checked against the resolved tree:

- `tests/test_UAT_FC_REQ-137_palette_shade.test.ts` (+19/-1, the substantive fix —
  filter `storage/sites/` enumeration to directories so a stray `.DS_Store` cannot
  cause ENOTDIR, plus a guard asserting the 22 stored entries were actually examined).
  Did **not** conflict. `git diff 87306fa43 HEAD -- <path>` is empty, i.e. HEAD's copy
  is byte-identical to the incoming version — the post-watermark sync commit
  `0f44ef1ba` had already landed this change verbatim. No test function was
  removed or altered.

- `package.json` (+1/-1) — the incoming edit is *solely* the version bump
  `0.1.41` → `0.1.42`. Nothing substantive was discarded by keeping `0.1.59`;
  the bump is superseded bookkeeping, not developer code.

**Net effect:** the resolution leaves no change from HEAD, because the incoming
commit's real content was already present through the post-watermark sync. Per the
stage instructions this was staged and reported as normal — `--skip` was **not**
called. `CHERRY_PICK_HEAD` is intact at `87306fa43ea10900fecabea6d00f47b11184a3e2`
for the finalize step, which is expected to detect the clean staged diff and skip
the commit itself.

## Verification

- `git ls-files -u` → empty (no unmerged index entries).
- `git status --porcelain` → empty (no UU/AA/DU/UD lines; no residual conflict markers).
- `git rev-parse CHERRY_PICK_HEAD` → `87306fa43ea10900fecabea6d00f47b11184a3e2` (preserved).

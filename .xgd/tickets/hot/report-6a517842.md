---
uid: report-6a517842
id: REPORT-3792
type: report
title: 'Capability-Intent Alignment: Site Materials & Starting Point: Scaffold, Assets,
  Provenance & Palette (level=uat)'
created_by: xgd
created_at: '2026-09-10T20:31:12.953954+00:00'
updated_at: '2026-09-10T20:31:12.953954+00:00'
completed_at: null
last_field_updated: created_at
result: pass
fields:
  report_kind: capability_validation
  subject_uid: capability-b4ac88fc
  level: uat
  violations: 0
  warnings: 1
  needs_review_count: 0
---

# Capability-Intent Alignment: Site Materials & Starting Point: Scaffold, Assets, Provenance & Palette
# Level: uat

**Result**: PASS
**Violations**: 0
**Warnings**: 1
**Needs review**: 0

> **This is attempt 6, re-checking after `report-f1aed64a` (fix, 20:22Z).** All three
> violations and both warnings from the previous uat-level check (`report-dde93f42`,
> 20:17Z) are discharged at HEAD `4e1093610a`, verified from the tree rather than
> from the fix report's own account. The story level passed at 19:49Z
> (`report-d9b5dd9e`) and the ac level at 20:09Z (`report-6c22789e`); per the
> level-priority rule the 38 AC bodies are this check's working reference and were
> not re-litigated, except where the implementation contradicts one (warning 1).
>
> **Coverage is complete and exclusive.** All 38 active ACs across the four stories
> have exactly one substantive UAT each — 38 `test_UAT_AC<n>_*` functions, no AC
> uncovered, no AC claimed twice anywhere in the repo (swept by a full-tree scan of
> every `.ts`/`.tsx`/`.js`/`.mts` file, not a grep of `tests/`, because two files in
> this repo carry NUL bytes and read as binary to grep).

## Cumulative Intent Considered

Unchanged from `report-6c22789e` (ac level, 20:09Z) and `report-dde93f42` (uat level,
20:17Z). Re-derived here only for the intents a finding or a verification decision
turns on.

| Intent ID | Status | When | Asked / changed | Counts? |
|---|---|---|---|---|
| REQ-101 (`request-b63bbed5`) | free_and_reconciled | 2026-07-26 | Font provenance index, three-state redistribution, `distribution` marker, on-disk scan | YES |
| REQ-102 (`request-56cb1897`) | free_and_reconciled | 2026-07-26 | `1c new` seeds a complete L1 document; renders/shots unedited; `1c repro` overwrites wholesale | YES |
| REQ-114 (`request-3cd338cd`) | free_and_reconciled | 2026-07-31 | Census + retrofit; retired the theme colour token group | YES (AC3 superseded by REQ-137) |
| REQ-118 (`request-66e4c630`) | free_and_reconciled | 2026-07-31 | `listSiteAssets` — registry ∪ store merged by handle, provenance flags, one handle vocabulary, CLI + `/api/assets` | YES |
| REQ-137 (`request-d2980a95`) | free_and_reconciled | 2026-08-12 | Deletes entry `steps`, adds continuous `shade`; replaces byte-identity with a measured ≤8/255 bound | YES |
| REQ-140 | free_and_reconciled | — | Deleted the `1stcontact` and `harbor-cafe` example sites as dead | YES — drives warning 1 |
| REQ-142 (`request-0dd62a5d`) | free_and_reconciled | 2026-08-15 | Async `SiteStore` port, filesystem moved behind it | YES |
| REQ-143 (`request-18a48d63`) | free_and_reconciled | 2026-08-15 | D1/R2 adapter for the same port | YES |
| REQ-145 (`request-b474390f`) | free_and_reconciled | 2026-08-15 | control-app becomes the builder origin | YES |

**Step 2.5 checked, does not apply.** No AC in this capability names a ticket as its
delivery vehicle, and no test in the five files below cites an `abandoned` /
`deprecated` / `wont_fix` ticket. The stale-citation case cannot arise.

**Test discovery.** `vitest.node.config.mts` includes `tests/**/*.test.ts` and
excludes only `*.workers.test.ts`; all five files below are live and were executed
this run.

## Alignment Ledger

Line numbers are HEAD `4e1093610a`.

### STORY-92 (`story-8685be2d`) — font provenance & licence — 12 ACs — **aligned**

`tests/reconciliation-font-provenance.test.ts`, driving the real CLI entry point
(`run(['fonts','check'])`), `cmdFontsCheck`, `formatFontsReport` and the exported
schema validators against on-disk trees in throwaway workspaces. Unchanged since
2026-08-10; spot-verified AC-861 (`:396`, the product gate — varies only the
distribution marker and the three-state answer, and asserts the unresolved state is
refused *as unresolved*), AC-862 (`:444`) and AC-863 (`:476`, advisory-not-blocking).
12 ACs → 12 tests at `:205 :299 :325 :354 :396 :444 :476 :509 :560 :596 :625 :662`.
**Executed: 12 passed.**

### STORY-93 (`story-86c7c21b`) — the authoring start point — 8 ACs — **aligned**

`tests/reconciliation-scaffold-starter-l1.test.ts`, driving `cmdNew`, `cmdRender`,
`cmdShot`, `cmdRepro` and the shipped `1c` launcher against real temp trees.
Re-verified AC-875 (`:304` — reads `1c new <slug> [--sandbox]` out of the launcher's
own help and asserts no `--l1` / `--empty` mode exists, matching the AC's "a slug
plus the shared workspace-root selector") and AC-876 (`:338` — byte-identical page
documents modulo slug, plus the bundle's own widths/background and the absence of the
`placeholder` node). 8 ACs → 8 tests at `:110 :145 :178 :199 :219 :266 :304 :338`.
**Executed: 7 passed, 1 skipped** (AC-871, browser-gated — info 2).

### STORY-97 (`story-5e7eb0c5`) — colour census & palette retrofit — 12 ACs — **aligned**

`tests/reconciliation-colour-retrofit-shade-model.test.ts` is now STORY-97's single
UAT file. The superseded `tests/reconciliation-colour-census-and-retrofit.test.ts` is
gone from the tree, and a repo-wide grep over `tests/ tools/ packages/ apps/ docs/`,
`vitest.node.config.mts` and `package.json` finds no reference to it — no orphaned
import, config entry or doc pointer.

| AC | Test | Outcome |
|---|---|---|
| AC-932 (`9f1e7baf`) | `:988 …a_colourless_site_retrofits_to_an_empty_palette_and_still_validates` | aligned on all three bullets — palette written and empty (`:1009-1011`), no page reference (`:1016`), `validateSite` clean (`:1021`). See warning 1 on the AC's own prose |
| AC-939 (`681fa4dd`) | `:310 …census_reports_literals_counts_alpha_families_and_writes_nothing` | aligned — counts, ordering, α annotation, families section present/absent, and both trees hashed before/after |
| AC-940 (`63d8463e`) | `:383 …census_json_is_one_parseable_document_agreeing_with_the_human_form` | aligned — whole stream parses, every field shape-checked, header reconciled against the human form |
| AC-941 (`48360aec`) | `:449 …assign_writes_palette_rewrites_pages_and_reports_entries_drift_and_files` | aligned — "materially smaller" is **measured** (censuses first, then `entryCount < literalCount / 2`), entries reported by name, files named not counted, changed-set equals reported-set, `--json` palette equals the stored one |
| AC-942 (`62c0b208`) | `:527 …one_rgb_at_three_opacities_becomes_one_entry_carrying_opacity_on_each_reference` | aligned — one carrier entry, every entry value opaque, three authored alphas ride the references |
| AC-943 (`3f7e1894`) | `:588 …families_collapse_onto_the_most_reaching_member_and_refused_fits_stand_alone` | aligned — all seven scenarios the AC's Verification prescribes (see info 4 on the one Criterion clause the Verification does not prescribe) |
| AC-944 (`3127e56f`) | `:679 …unshaded_references_are_exact_shaded_stay_within_the_bound_and_drift_is_reported` | aligned on all five clauses — δ=0 unshaded, ≤`SHADE_FIT_TOLERANCE` shaded, alpha byte-identical, slot count unchanged, drift set equals the measured inexact set, worst-first, and the write aborts on an over-bound fit |
| AC-945 (`66e919f9`) | `:762 …unprovable_retrofit_exits_nonzero_diagnoses_and_writes_nothing` | aligned — all three causes (no draft / over-bound / invalid definition), each with its own diagnostic and a `hashTree` equality |
| AC-946 (`c9cc59fc`) | `:819 …derived_names_describe_colours_and_rename_to_role_vocabulary` | aligned — descriptive vocabulary, kebab rule, uniqueness, rename changes names only |
| AC-947 (`e7d18852`) | `:938 …reproduced_sites_carry_literals_and_re_assignment_is_a_byte_identical_fixpoint` | aligned — all three bullets, fixpoint asserted by `hashTree` on a stored site plus identical stdout |
| AC-1146 (`3dc77086`) | `:1028 …a_more_saturated_member_earns_its_own_byte_exact_entry_rather_than_a_shade` | aligned |
| AC-1147 (`b80e8a70`) | `:1081 …reported_drift_is_reproduced_exactly_by_the_palette_models_own_resolution` | aligned |

Docblock (`:2-4`, `:32`) now carries STORY-97's current title ("within a proven
per-channel bound") and AC-945's current wording ("cannot be proved within the
bound") — previous warning 4 discharged. **Executed: 12 passed (29.9s).**

### STORY-102 (`story-c46abfa6`) — the site asset store — 6 ACs — **aligned**

`tests/reconciliation-site-asset-listing.test.ts`, driving both real entry points —
`run(argv)` for the command line and `startBuilder` over HTTP for the builder origin,
plus the control-app's own `fetchAssets` client. Docblock (`:8-9`) and the AC-1018 /
AC-1019 inline comments now say "asset **store**", matching the ACs reworded after
REQ-142 — previous warning 5 discharged. 6 ACs → 6 tests at
`:218 :246 :274 :314 :340 :376`. **Executed: 5 passed; AC-1023 did not run in this
sandbox** — info 3.

### Element → intent alignment

| Element | Intents aligned to | Outcome |
|---|---|---|
| STORY-92 UATs (12) | REQ-101 | aligned |
| STORY-93 UATs (8) | REQ-102, REQ-114 | aligned |
| STORY-97 UATs (12) | REQ-114, REQ-137 | aligned — the pre-REQ-137 duplicate file is gone |
| STORY-102 UATs (6) | REQ-118, REQ-142, REQ-143, REQ-145 | aligned |
| `…colour-palette-overlay.test.ts` (`story-c490f1cf`) | REQ-114, REQ-137 | aligned — the AC-932 block and its docblock entry are removed; the docblock now points the retrofit at STORY-97 and names AC-941 / AC-944 as its owners |

## Findings

| # | Severity | Property | Element | Resolution category | Issue | Suggested edit |
|---|---|---|---|---|---|---|
| 1 | warning | consistency | AC-932 (`acceptance_criterion-9f1e7baf`) vs `tests/reconciliation-colour-retrofit-shade-model.test.ts:988-998` | ac-edit | The AC's rationale asserts evidence that no longer exists. Commit `7dd177abe1` (2026-09-10 19:59Z, the ac-level fix) added the sentence "**Stored sites in this state exist** — the ones whose pages carry no colour axes census at zero literals — so the case is the ordinary outcome for them rather than a contrived one", and rewrote Verification to "Census a **stored site** whose pages declare no colour axes". `storage/sites/` holds exactly two sites, `xgd` and `gigabytealchemy`, and both are colourful — AC-944's own body records them at 210 and 91 painted slots, and AC-939's UAT asserts `xgd`'s literal count is `> 0`. The UAT therefore **synthesises** its specimen (`paintedSite(cwd, 'small-bare', [])`) and its comment states the contradiction outright: "SYNTHESISED, not stored (REQ-140) … This claim used to be made against the `1stcontact` and `harbor-cafe` example sites, which were deleted as dead". Not a violation: the test proves all three of the AC's Criterion bullets, and a synthesised site written into a real store and censused through the real command does satisfy Verification under its ordinary reading. The defect is the factual aside, which will keep re-surfacing at this level | In AC-932, replace the "Stored sites in this state exist…" sentence with the true statement — that the specimen is a page painted with no colour axes, the example sites that used to be in this state having been removed by REQ-140 — and reword Verification from "Census a stored site whose pages declare no colour axes" to "Census a site whose pages declare no colour axes". Do **not** change the test: it cannot census a stored colourless site, because none exists |
| 2 | info | coverage | AC-871 (`b17420aa`) / `…scaffold-starter-l1.test.ts:178` | — | Gated `it.runIf(await chromiumAvailable())` (`:47-48`) and skipped in this sandbox, where Chromium is blocked by the Mach bootstrap policy rather than absent. The test is substantive — it drives `cmdShot` end to end and asserts PNG magic bytes. Environment property, unchanged from the previous run | none |
| 3 | info | coverage | AC-1023 (`07e381ca`) / `…site-asset-listing.test.ts:376` | — | The builder-origin `describe` failed to start this run: `startBuilder` threw `Error: listen EPERM: operation not permitted 0.0.0.0` at `tools/generate/src/cli/builder.ts:363`, so `beforeAll` aborted and AC-1023 did not execute (`1 failed | 3 passed` files; `28 passed | 2 skipped` tests). This is the sandbox refusing a socket bind, not a defect in the test or the code — the same file's other five tests passed, and `report-f1aed64a` executed this suite green 20 minutes earlier at the same commit. Unlike AC-871's browser gate the network dependency is ungated, so the environment turns it into a suite failure rather than a skip. Recorded so a downstream reader does not mistake a red suite for drift | none — if it recurs outside a sandbox, that would be a real regression |
| 4 | info | consistency | AC-943 (`3f7e1894`) / `…shade-model.test.ts:588` | — | The Criterion's third bullet ("grouping proceeds in **rounds**: whatever a family's entry cannot reach is re-offered for grouping rather than dropped straight to a singleton") has no dedicated assertion in AC-943's UAT. It is not an omission against the AC's own Verification, which prescribes seven scenarios and all seven are covered; and the behaviour is incidentally evidenced in AC-946's UAT at `:856` (`expect(names.some((n) => /-\d+$/.test(n))).toBe(true)` — a suffixed name exists only because a later grouping round derived a name an earlier one had taken). Noted so a future check does not read it as a fresh gap | none |
| 5 | info | exclusivity | `tests/req101-font-registry.test.ts`, `tests/req102-scaffold-l1.test.ts`, `tests/req114-palette-model.test.ts`, `tests/req118-image-selection.test.ts`, `tests/test_UAT_FC_REQ-137_palette_shade.test.ts` | — | These carry `test_UAT_FC_REQ-*` tests over the same subject matter as STORY-92/93/97/102. They are free-coded UATs traceable to the REQ rather than to an AC — a separate lineage from the AC-numbered reconciliation UATs — and are not exclusivity duplicates. Carried forward from `report-dde93f42` info 7 | none |

## Notes for the Editor

**Nothing blocks this level.** The one warning is an AC-prose repair that can be
taken opportunistically at the next ac-level pass; it does not touch a test.

**What was verified from the tree, not from the fix report.** Each of the previous
run's three violations was re-checked against HEAD directly:

- `tests/reconciliation-colour-census-and-retrofit.test.ts` is absent from the
  working tree, and no file under `tests/`, `tools/`, `packages/`, `apps/`, `docs/`,
  `vitest.node.config.mts` or `package.json` still names it. The only surviving
  mentions are in archival tickets (prior reports, comments, and REQ-137's body),
  which are historical records rather than live claims.
- `…colour-palette-overlay.test.ts` no longer defines an `AC932` test; its docblock
  (`:22-26`) now states that the retrofit belongs to STORY-97 and names AC-941 and
  AC-944 as the owners of the two claims that block used to assert, and the imports
  the block needed (`cmdColors`, `cmdColorsAssign`, `collectColorLiterals`, `cpSync`,
  `fileURLToPath`) are gone. The file runs green.
- A full-tree scan for `test_UAT_AC<n>` returns exactly 38 functions for this
  capability's 38 active ACs — one each, zero duplicates, zero uncovered.

**A green test is still not evidence of alignment, and a red suite is still not
evidence of drift.** Both halves showed up this run: the deleted duplicates passed
right up until they were removed, and AC-1023's suite is red here purely because the
sandbox refuses `listen(0.0.0.0)`. Read what a test asserts against the AC body, and
read *why* a suite failed before treating it as a finding.

**Field state, for the check that follows this one.** The capability carries
`uat_coverage: fail` and STORY-93 / STORY-102 carry `uat_coverage: stale` while all
38 ACs carry `pass`. That aggregate is the `uat_coverage` check's field to own and
was deliberately not touched here; on the evidence above the underlying coverage is
complete.

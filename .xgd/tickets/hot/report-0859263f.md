---
uid: report-0859263f
id: REPORT-502
type: report
title: Claude reconciliation_uat_generation report
created_by: xgd
created_at: '2026-07-13T20:20:42.634415+00:00'
updated_at: '2026-07-13T20:20:42.634415+00:00'
completed_at: null
last_field_updated: created_at
fields:
  report_kind: reconciliation_uat_generation
  subject_uid: story-79e068e5
---

All five test names conform to the enforced `^test_UAT_AC\d+_\w+$` regex. (The eslint error is just environmental noise — `npx` pulled a global eslint v10 with no config because there's no local eslint at that invocation path; the project lints via its own vitest-plugin toolchain, and my file mirrors the exact import/fixture/JSDOM patterns of the existing `req52-*` and `req47-*` test files.)

## Summary

I wrote reconciliation UATs for story **story-79e068e5** — one per acceptance criterion — verifying the two REQ-52 blind-spot fixes against the existing code. All pass.

**Grounding:** the fixes live in `tools/generate/src/cli/capture/extract.ts` (canvas-based colour resolver in `EXTRACT_SCRIPT`, with an rgb()/rgba() regex fallback when no 2D canvas exists), `capture/values-diff.ts` (`buildObjectCard` one-sided-geometry skew flag), and `fidelity.ts` (`formatReport` STALE-REFERENCE summary). The tests hit real boundaries: real headless Chromium against the committed `req52-oklch.html` fixture for colour resolution, a JSDOM `EXTRACT_SCRIPT` eval for the canvas-less fallback, and the pure `diffManifests`/`formatReport` pipeline for the geometry/report checks — matching the existing `req52-*`/`req47-*` patterns exactly.

```
UATs generated for story story-79e068e5 (plan item 3 of 8)

Story: story-79e068e5
Test file(s) created:
  - tests/reconciliation-colour-resolution-stale-geometry.test.ts

Tests written: 5
Tests passed: 5
Tests failed: 0

test_files_created:
  - "tests/reconciliation-colour-resolution-stale-geometry.test.ts"
```

Per-AC coverage:
- **AC-589** — `test_UAT_AC589_modern_css_colours_resolve_to_srgb_hex_not_inferred`: oklch runs resolve to accurate `#rrggbb` (blue-dominant / dark-slate), not flagged inferred, and a resolved reference now surfaces a `color` delta the inferred sentinel used to suppress.
- **AC-590** — `test_UAT_AC590_transparent_colour_falls_back_to_sentinel_and_is_inferred`: transparent `color` → `#000000` sentinel + `colorInferred:true`, contrasted with a resolvable neighbour.
- **AC-591** — `test_UAT_AC591_rgb_colours_resolve_to_hex_without_a_rendering_surface`: in canvas-less JSDOM, `rgb(49,65,88)` → `#314158`; zero-alpha `rgba()` → `#000000` sentinel/inferred.
- **AC-592** — `test_UAT_AC592_one_sided_box_geometry_is_flagged_not_silently_passed`: box present on exactly one side (both skew directions) flags mismatch; matching both-sided boxes do not false-flag.
- **AC-593** — `test_UAT_AC593_report_prints_loud_stale_reference_warning_with_count`: report prints the STALE-REFERENCE warning with count + re-capture guidance; absent when all objects carry geometry.

No runtime code was modified and no existing test files were touched.

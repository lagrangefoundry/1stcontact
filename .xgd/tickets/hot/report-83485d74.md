---
uid: report-83485d74
id: REPORT-1703
type: report
title: 'Resync resolve conflicts: 0a409cf4fa5ab3667cbc9c1b94c0ff8b57b791be'
created_by: xgd
created_at: '2026-08-08T02:18:16.651437+00:00'
updated_at: '2026-08-08T02:18:16.651437+00:00'
completed_at: null
last_field_updated: created_at
result: pass
fields:
  report_kind: resolve_conflicts
  subject_uid: report-f53a5001
---

## Summary

Incoming commit `92d1ab5ee` (`fix(webui): move component scope to @lagrangefoundry [FREE-CODED]`, 2026-08-07 09:51) is a free-coded fix for BUG-32. Main already contains a **strict superset** of it: the develop work for `story-e674c60a` / `bug-5cabb340` (notably `0f308ea96`, 2026-08-07, which touches the same six files) redid the same rename under AC-960's one-definition rule and went further.

Every conflict therefore resolved to the **HEAD (ours)** side. All ten resolved files are byte-identical to HEAD; the net change carried by the cherry-pick is the `package.json` version bump `0.1.24 → 0.1.25`, which merged cleanly and is a genuine forward move (main had not bumped).

## Files resolved

- `index.html` — **DU** (main deleted, incoming modified). Deletion is a legitimate refactor, not churn: `0f308ea96` (same story, same intent as the incoming fix) removed it because a checked-in copy of `chromeHtml()`'s output is a second definition site for the scope. The surviving `tests/bug32-webui-scope-rebrand.test.ts` records this verbatim ("`index.html` … has since been deleted (nothing read it; a committed copy of the generator's output is itself a second definition site)"). Incoming's only edit to it was the import-map scope rename, which the generator now emits. → `git rm index.html`.
- `tests/bug32-webui-scope-rebrand.test.ts` — **AA** (both added). Ours (252 lines) vs incoming (122 lines); ours is the superset. Took ours. UAT coverage mapping below — no test function's coverage is lost.
- `tools/generate/src/cli/webui.ts` — **UU**, 2 hunks. (1) doc prose; (2) ours' `walkOrigin()` + `createRequire(mainCheckout(...))` vs incoming's empty side. Took HEAD on both. Both sides independently produce the identical line `export const WEBUI_SCOPE = '@lagrangefoundry'` — git merged it cleanly, so incoming's headline change is present.
- `tools/generate/src/cli/builder.ts` — **UU**, 2 hunks. (1) ours adds `NO_STORE` to the `./serve` import; (2) ours adds an explanatory comment. Took HEAD on both — these are ours-only additions, incoming had nothing to lose there. Incoming's `WEBUI_SCOPE` import and both composed import-map keys merged cleanly (lines 12, 72, 74).
- `tests/reconciliation-builder-workspace-chrome.test.ts` — **UU**, doc prose. Took HEAD.
- `tests/reconciliation-builder-workspace-origin.test.ts` — **UU**, doc prose. Took HEAD. Incoming's `WEBUI_SCOPE` import and `map.imports[\`${WEBUI_SCOPE}/${name}\`]` assertion merged cleanly and survive (lines 58, 467).
- `tests/reconciliation-copy-edit-gesture-modal.test.ts` — **UU**, doc prose. Took HEAD.
- `tests/reconciliation-copy-edit-gesture.test.ts` — **UU**, doc prose. Took HEAD.
- `tests/req115-builder-composition.test.ts` — **UU**, doc prose. Took HEAD.
- `tests/req115-builder-shell.test.ts` — **UU**, 2 hunks (doc prose + an ours-only comment). Took HEAD. Incoming's `WEBUI_SCOPE` import and both composed assertions merged cleanly (lines 26, 114, 116).

## Why "incoming wins" was NOT applied to the prose hunks

Taking incoming's prose is not a stylistic preference here — it is a **red-test regression**. Ours' guard `test_UAT_AC960_component_scope_is_written_in_exactly_one_place` asserts (b) that the scope in use is written in **no tracked text file** except `tools/generate/src/cli/webui.ts` and `apps/control-app/src/builder/*.js`, and its scan is `git grep -F` over the whole tracked tree — comments included. Incoming's prose writes the literal `@lagrangefoundry` into seven test/support files. Applying it makes the tree's own suite fail.

Incoming's *intent* — no legacy scope literal anywhere, scope declared once, references composed from the declaration — is fully satisfied and exceeded by ours, which additionally bans the literal from prose.

## Incoming changes preserved

Verified present in the resolved tree:

- `tools/generate/src/cli/webui.ts:104` — `export const WEBUI_SCOPE = '@lagrangefoundry'` (incoming's core change; agreed by both sides).
- `tools/generate/src/cli/builder.ts:12,72,74` — `chromeHtml()` composes **both** import-map keys from `WEBUI_SCOPE`. This was incoming's headline de-duplication ("chromeHtml() hardcoded the scope in two places"); it is present.
- `tests/reconciliation-builder-workspace-origin.test.ts:58,467` and `tests/req115-builder-shell.test.ts:26,114,116` — `WEBUI_SCOPE` imported and composed, replacing the former literals.
- `apps/control-app/src/builder/{app,editor}.js` — bare specifiers under `@lagrangefoundry` (merged clean, no conflict).
- `package.json:3` — version `0.1.25`.
- Repo-wide: `grep -rn gendevlabs tests/ tools/ apps/ packages/` returns **no hits**.

### Incoming UAT coverage mapping (rule 2f — no test function's coverage deleted)

| incoming UAT | covered on ours by |
|---|---|
| `test_UAT_FC_BUG-32_every_webui_component_actually_resolves` | `test_UAT_AC961_components_are_served_byte_identical_from_outside_this_repo` (origin test) — asserts package self-identity, resolution outside the repo, and `WEBUI_INSTALLED === true` |
| `test_UAT_FC_BUG-32_chrome_import_map_keys_are_all_under_the_current_scope` | `test_UAT_AC960_browser_source_specifiers_are_declared_by_the_generated_document` + origin test lines 492/499 |
| `test_UAT_FC_BUG-32_builder_browser_imports_match_the_import_map` | `test_UAT_AC960_browser_source_specifiers_are_declared_by_the_generated_document` (stricter: matches both quote styles, asserts against a freshly generated document) |
| `test_UAT_FC_BUG-32_webui_scope_is_defined_in_exactly_one_place` | `test_UAT_AC960_component_scope_is_written_in_exactly_one_place` (whole tracked tree vs three source roots; adds the superseded-scope scan and an exactly-one-quoted-literal check) |

## Evidence

- `npx vitest run tests/bug32-webui-scope-rebrand.test.ts` → **2 passed**, confirming the resolved tree satisfies the AC-960 guard that the prose decision rests on.
- Pre-existing, NOT caused by this resolution: `tests/reconciliation-copy-edit-gesture-modal.test.ts` (5 failures) and `tests/req115-builder-composition.test.ts` (1 failure: `test_UAT_FC_REQ-115_open_in_new_tab_matches_the_iframe_exactly`) fail on main. Both files are byte-identical to HEAD after resolution, so that run is a HEAD run. Flagged for the outer workflow, not actionable in this step.
- `tests/req115-builder-shell.test.ts > test_UAT_FC_REQ-115_control_app_fronts_the_builder_same_origin` timed out (60s) on one run and passed on a rerun — flaky, also HEAD-side.

## Post-merge review flags

Per the enrichment rule (intent unknown on the incoming side), all ten files are flagged for post-merge review. The substantive flag is that this cherry-pick contributes **only** a version bump — its code content was already reconciled into main by `story-e674c60a`.

---
uid: report-86157e60
id: REPORT-4284
type: report
title: 'Reconcile resolve conflicts: reconcile-BUNDLE-27'
created_by: xgd
created_at: '2026-09-18T04:29:10.017149+00:00'
updated_at: '2026-09-18T04:29:10.017149+00:00'
completed_at: null
last_field_updated: created_at
result: pass
fields:
  report_kind: resolve_conflicts
  subject_uid: reconcile-BUNDLE-27
---

## Files resolved

- `.xgd/tickets/hot/request-554ac441.md` (REQ-149) — class **UU**, rule **2e (intent/bookkeeping ticket)**, sub-rule "one side is a strict superset of the other: keep the superset". Resolved to the **HEAD/ours** side on both hunks via `git checkout --ours` + `git add --sparse` (`.xgd/tickets/` is outside the sparse-checkout cone on this reconcile branch, DOC-986 §2/§4.1).

  Incoming commit: `a74ac03993024fbbffb13bf95ca29a0605894043` ("xgd(ticket): update request request-554ac441", 2026-08-22 20:24 -0700, `last_field_updated: body`). It touches exactly one file. Unlike the previous pick in this bundle (`7782255e`, frontmatter only), this one is a genuine **content** change: it appends a ~50-line follow-up section to the ticket body.

  Two conflict hunks:

  **Hunk 1 — frontmatter scalars (lines 8–18).** Same fields changed differently on each side, so the per-fact timeline rule applies. HEAD's facts are strictly later on every one:

  | fact | incoming (Aug 22) | HEAD (Aug 31) — kept |
  |---|---|---|
  | `updated_at` | `2026-08-23T03:24:38Z` | `2026-08-31T14:22:34Z` |
  | `completed_at` | `null` | `2026-08-31T14:22:34Z` |
  | `last_field_updated` | `body` | `status` |
  | `status` | `free_coded` | `free_and_reconciled` |
  | `fields.version` | `0.2.7` | `0.2.9` |

  `free_and_reconciled` is the later, operator-owned status; `last_field_updated: status` records HEAD's own Aug 31 status transition, which post-dates the incoming `body` edit. No `working-timeline` tie-break was needed — this is one monotonic bookkeeping sequence observed at two points in time, not competing intent.

  **Hunk 2 — end of body (lines 459–545).** Asymmetric: the HEAD side carries a further whole section ("## Follow-up: the deploy secret guard asked the wrong question", ACs 13–16, its test-changes and version-bookkeeping subsections, ~85 lines) where the **incoming side carries nothing at all**. Keeping ours preserves that section; the incoming side had no content here to lose. The hunk conflicts only because the shared base ended with a no-newline-at-EOF boundary that both sides appended past.

  No `intent_uid` / `story_uid` / `capability_uid` field was touched. No content absent from both sides was introduced.

## Incoming changes preserved

No code or implementation file was in this conflict, so STEP 3's code-discard guard has no code target. For the one ticket file, the incoming commit's substantive change is **present verbatim** in the resolved file — HEAD had already received it through an earlier route, and then added more on top.

Mechanical check — `git diff HEAD:<path> a74ac039:<path>` reports that the ONLY lines existing on the incoming side and not in the resolved file are these five bookkeeping scalars, every one of them superseded by a strictly later HEAD value:

```
+updated_at: '2026-08-23T03:24:38.640592+00:00'   -> HEAD 2026-08-31T14:22:34
+completed_at: null                                -> HEAD 2026-08-31T14:22:34
+last_field_updated: body                          -> HEAD status
+status: free_coded                                -> HEAD free_and_reconciled
+  version: 0.2.7                                  -> HEAD 0.2.9
```

The entire prose payload of the incoming commit is byte-identical in the resolved file:

- `## Follow-up: \`bin/build\` failed on a type-only reach into node` — resolved file line 405
- its Cause / "Why no test caught it" narrative (the `render.ts` → `../store/loadSite` type-only specifier reaching `node:path`, and REQ-146's runtime-only import guard being silent about it) — lines 405–445
- `12. No module reachable from a Worker entrypoint imports a node-only module, including through a type-only import.` — line 442
- its `### Version bookkeeping` paragraph ending "Ticket version is now 0.2.7." — line 451

Taking the incoming side instead would have deleted the ACs 13–16 follow-up section, reverted the ticket from `free_and_reconciled` to mid-flight `free_coded`, and rolled the version back from 0.2.9 to 0.2.7.

The BUG-1301 precedence exception was not invoked; no hunk was dropped as obsolete-refactor, and no test function on either side was deleted.

## Net effect

The staged tree is byte-identical to HEAD (`git status --porcelain` empty, `git ls-files -u` empty, no conflict markers in the file) — this cherry-pick is redundant, its body content and bookkeeping having already been carried forward. Per STEP 4 this is not a failure and `--skip` was **not** called; the sequencer is untouched and `CHERRY_PICK_HEAD` (`a74ac03993024fbbffb13bf95ca29a0605894043`) is still present for `cherry_pick_finalize_resolution`.

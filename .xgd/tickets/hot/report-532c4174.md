---
uid: report-532c4174
id: REPORT-911
type: report
title: 'Capability-Intent Alignment: Framework Absolute-or-Overlay Value System (level=story)'
created_by: xgd
created_at: '2026-07-24T08:24:02.626330+00:00'
updated_at: '2026-07-24T08:24:02.626330+00:00'
completed_at: null
last_field_updated: created_at
result: pass
fields:
  report_kind: capability_validation
  subject_uid: capability-6e088083
  level: story
  violations: 0
  warnings: 0
  needs_review_count: 0
---

# Capability-Intent Alignment: Framework Absolute-or-Overlay Value System
# Level: story

**Result**: PASS
**Violations**: 0
**Warnings**: 0
**Needs review**: 0

## Cumulative Intent Considered

Chronological ledger of intents that touched this capability (via STORY-80's
`intent_uid` = BUNDLE-6 and `updated_by` = BUNDLE-7, plus the substantive REQs
those bundles carry that shaped this capability):

| Intent ID | Status | When | Asked / changed | Counts? |
|---|---|---|---|---|
| BUNDLE-6 (REQ-58/59/62/61) | free_and_reconciled | 2026-07-17 | Originating intent: the absolute-or-overlay value system, originally delivered via layout-module dials (colour hex/role, absolute length, named step, radius — AC-660..665) | YES (originating; dial delivery later superseded) |
| REQ-79 (request-87b26bca) | free_and_reconciled | 2026-07-19 | Framework pivot: absolute base re-homed on L1 leaf literals; decision #2 "one value = one literal field — no theme-role indirection IN L1"; decision #4 "L2 (design library) PARKED, possibly never needed" | YES |
| REQ-84 (request-f243b6b9) | bundled (in free_and_reconciled BUNDLE-7) | 2026-07-20 | Strip layout modules to L1 — deleted header/hero/footer/text-block/services-grid/layer + ~20 dials | YES |
| REQ-85 (request-015e42ac) | free_and_reconciled | 2026-07-20 | Capability-module contract; explicit superseded-AC list retiring the module-dial delivery (AC-637, AC-660..665, AC-666..670, AC-676..678) | YES (retired the dial delivery) |
| BUNDLE-7 (REQ-63/79/82/83/84 + 2 more) | free_and_reconciled | 2026-07-22 | The pivot bundle carrying REQ-79/82/83/84/85 | YES |

**Cumulative picture:** the absolute-or-overlay capability was originated by
BUNDLE-6 and delivered through layout-module dials. The REQ-79 pivot (executed by
REQ-84 deletion + REQ-82 L1 substrate/envelope + REQ-85 capability contract)
retired the dial delivery and re-homed the **absolute base** on L1 leaf literals
(colour = hex literal, length/geometry = finite px literal, radius = px literal),
while **parking the named-overlay half in L2** (REQ-79 #4). Current cumulative
intent: L1 carries only the validated absolute literal — never an `absolute OR
role` union — and the overlay is a parked, possibly-never-built L2 convenience.

## Alignment Ledger

| Element | Intents aligned to | Outcome |
|---|---|---|
| STORY-80 (story-c490f1cf, upgrade, status=updated) | BUNDLE-6 (originating); REQ-79 / REQ-84 / REQ-85 (via updated_by=BUNDLE-7) | aligned — story body describes the absolute base carried as validated L1 leaf literals for colour/length/radius; explicitly frames the module-dial delivery (AC-660..665) as *superseded, not lost* per the REQ-79 reconciliation note and REQ-85 superseded-AC list; explicitly parks the named overlay in L2 per REQ-79 #4 |

## Findings

| # | Severity | Property | Element | Resolution category | Issue | Suggested edit |
|---|---|---|---|---|---|---|
| 1 | info | consistency | STORY-80 | — | Story body's claim "no named-role indirection lives in the substrate — L1 carries only the absolute literal, not an `absolute OR role` union" is directly supported by REQ-79 decision #2 ("one value = one literal field — no theme-role indirection IN L1") and #4 (L2 overlay parked). Correctly aligned. | none |
| 2 | info | consistency | STORY-80 | — | Story frames the module-dial delivery (AC-660..665) as superseded by the pivot, matching REQ-85's explicit superseded-AC list and REQ-79's "intentional supersession, not lost free-coded work" reconciliation instruction. No retired behavior is described as active. | none |
| 3 | info | coverage | STORY-80 | — | The overlay half (palette role / named step / named shape) is correctly absent from active behavior — REQ-79 #4 parks it in L2 ("possibly never needed"). Its absence is intent-correct, not a coverage gap. | none |

## Notes for the Editor

- **Single story capability.** STORY-80 is the only story under CAP-67; exclusivity is trivially satisfied. It carries one AC (AC-716, "L1 leaf axes carry the absolute (literal) value, validated by the envelope"), so the upgrade story is not orphaned. AC/UAT-level fitness is out of scope for this story-level cycle.

- **Recorded intent tension (not a finding, no action needed at story level).** REQ-79 contains two statements that read in slight tension: the top-of-ticket *reconciliation note* says the "'absolute value OR role' concept carries into L1 leaf axes," whereas *decision #2* ("no theme-role indirection IN L1") and *decision #4* (L2 overlay parked) say L1 carries only the literal. STORY-80 resolves this correctly and explicitly (Technical Notes: "the substrate carries the literal, not an `absolute OR role` union"), following the authoritative decision sections. The reconciliation-note phrase is reasonably read as "the *capability* of expressing that value survives (now as a literal)," not "the union type survives." Intent is therefore not genuinely ambiguous about the active behavior — hence info, not needs_review. Flagging only so a future cycle at ac/uat level does not re-litigate it as drift.

- **Story kind = upgrade** is correct: the pivot evolved an existing capability bucket (absolute-or-overlay values) in place rather than introducing a new one.

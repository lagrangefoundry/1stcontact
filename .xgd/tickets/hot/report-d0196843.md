---
uid: report-d0196843
id: REPORT-2093
type: report
title: 'Capability-Intent Alignment: Framework Substrate: L1 Layout, Values & Behavior
  Modules (level=ac)'
created_by: xgd
created_at: '2026-08-16T08:24:35.998266+00:00'
updated_at: '2026-08-16T08:24:35.998266+00:00'
completed_at: null
last_field_updated: created_at
result: fail
fields:
  report_kind: capability_validation
  subject_uid: capability-ae9d65d6
  level: ac
  violations: 3
  warnings: 3
  needs_review_count: 0
---

# Capability-Intent Alignment: Framework Substrate: L1 Layout, Values & Behavior Modules
# Level: ac

**Result**: FAIL
**Violations**: 3
**Warnings**: 3
**Needs review**: 0

Anchor report: report-7ef6a9ea · Capability: capability-ae9d65d6 (CAP-70) · Previous attempts: 6

## Cascade precondition — NOT met

The `ac` level is defined to assume story bodies are already aligned. **That
assumption does not hold on this run.** The story-level cycle ran today
(`report-6b02087e`, 2026-08-16T08:14) and returned **FAIL / 4 violations**, and
no story in this capability has been edited since:

| Story | last `updated_at` | vs. story report at 08:14 today |
|---|---|---|
| STORY-83 | 2026-08-12T21:22 | unfixed |
| STORY-82 | 2026-08-09T05:42 | unfixed |
| STORY-85 | 2026-08-09T05:42 | unfixed |
| STORY-80 / 81 / 90 / 91 | 2026-08-09T05:42 | — |

Three of that report's four violations (its findings 2, 3, 4) are the *same*
REQ-93 / REQ-96 drift this level re-detects in the ACs below. Where the story
body is the demonstrated source of the error, I have consulted the intent
directly rather than inheriting the error — as the level rules permit when the
upper layer is itself unsound. Every finding below is sequenced accordingly.

## Cumulative Intent Considered

Ledger carried forward from the story-level cycle; the rows that bear on the
findings were re-verified against code in this pass (citations in Findings).

| Intent ID | Status | When | Asked / changed | Counts? |
|---|---|---|---|---|
| REQ-79 / REQ-82 / REQ-84 (bundle-31e474b9) | free_and_reconciled | 2026-07-22 | Framework pivot: L1 typed substrate + envelope + sole renderer; delete semantic layout modules and their ~20 dials | YES |
| REQ-85 (bundle-31e474b9) | free_and_reconciled | 2026-07-22 | Reframe carousel / contact-form as vetted modules: typed config + named L1 slots | YES |
| REQ-87 | free_and_reconciled | 2026-07-24 | Rename runtime type `capability module` → **behavior module**; **no back-compat alias** | YES |
| **REQ-93** | **free_and_reconciled** | **2026-07-25** | **Page-level slot binding (5 rejections), renderer mounts the module fragment into the slot, `contact-form` config derived from capture incl. `labelMode`, `mountInL1` conformance mode** | **YES** |
| REQ-90 / REQ-91 | free_and_reconciled | 2026-07-29 | Document resource table + `@font-face`; extend L1 axes to cover captured pixel-movers | YES |
| **REQ-96** | **free_and_reconciled** | **2026-08-06** | **`control` leaf; delete `carousel.config.view`; replace contact-form's `intro`/`submit` slots with one required `form` slot; zero-CSS module obligation** | **YES** |
| REQ-97 / REQ-98 / REQ-105 | free_and_reconciled | 2026-08-06 | Shared surface + node-level axis groups across every kind; text measure; slot sizing | YES |
| REQ-99 / REQ-100 / REQ-108 | free_and_reconciled | 2026-08-06 | Typed interaction state, scroll reveal + stagger, pointer accent; renderer safety floor | YES |
| REQ-103 / REQ-104 / REQ-106 / REQ-107 | free_and_reconciled | 2026-08-06 | Texture axis; per-width layout track; link role + DOM ids; `validateL1` on the authoring path | YES |
| BUG-28 / REQ-109 / BUG-30 | free_and_reconciled | 2026-08-06 | contact-form enhancement isolation; relocatable document-relative URLs | YES |
| **REQ-114** | **free_and_reconciled** | **2026-07-31** | **Deletes the closed colour-role vocabulary; overlay becomes a free-form kebab-case palette reference** | **YES** |
| REQ-128 / REQ-136 | free_and_reconciled | 2026-08-08 / 08-12 | Background image selection; non-destructive image framing + colour adjustment | YES (covered — AC-805, AC-1124/1125/1126) |
| REQ-121/122/125/126/127/129/130/132/135/138 | free_and_reconciled / legacy_done | 2026-08-07 → 08-12 | Builder chat, L1 control-surface API, editor surfaces | YES — but land on the **control-surface / builder** capability, not CAP-70 |
| **REQ-137** | **bundled** (bundle-d9226698) | **2026-08-12** | **Deletes palette `steps`; light↔dark becomes an Oklab `shade` on the reference** | **imminent — not yet enforced** |
| REQ-131/133/139/140/141 | ready_to_reconcile | 2026-08-11 → 08-15 | Change journal, palette popup, editor locks, workers-runtime UATs | imminent — builder capability, not CAP-70 |

**Intent scan since the last ac cycle (2026-08-09).** 18 intents created on or
after 2026-08-07 reached a counting status. All but REQ-136/REQ-128 (already
covered) and REQ-137 (imminent, warning 2) belong to the builder / control-surface
capability. **No new ac-level coverage gap arises from them.**

## Alignment Ledger

100 ACs across 7 stories (all `feature`/`upgrade`, so all are in scope for AC
coverage). Grouped; flagged elements itemised.

| Element | Intents aligned to | Outcome |
|---|---|---|
| STORY-80 · AC-716, 928, 929, 930, 931 (5) | REQ-84, REQ-114 | aligned on the colour-role cut; **3 ACs overlap** (warning 1); AC-928/716 carry `steps`, superseded by imminent REQ-137 (warning 2) |
| STORY-81 · AC-833–838 (6) | REQ-104 | aligned |
| **STORY-82 · AC-718, AC-719 (2)** | REQ-84 only — **never re-aligned to REQ-87, REQ-93, REQ-96, REQ-114** | **violations 1 & 2**; also the only 2 non-`active` ACs in the whole 424-AC project |
| STORY-83 · AC-682–688, 723, 725–728, 801–807, 829–832, 849–851, 888–891, 933–936, 1009–1012, 1124–1128 (43) | REQ-82, 90, 91, 97, 98, 103, 105, 106, 107, 109, 114, 136 | aligned, **except AC-723** — states the inert placeholder as the whole truth about a slot; REQ-93's mount contradicts it (violation 3) |
| STORY-85 · AC-697–704, 722, 808–811, 877, 878 (15) | REQ-85, 87, 93 (*partly*), 96, BUG-28 | aligned — AC-701 and AC-722 already carry the post-REQ-96/REQ-87 truth; **but no AC covers REQ-93's page-level binding** (violation 3) |
| STORY-90 · AC-819–828, 879–887 (19) | REQ-99, REQ-100, REQ-108 | aligned |
| STORY-91 · AC-839–848 (10) | REQ-106 | aligned |

## Findings

| # | Severity | Property | Element | Resolution category | Issue | Suggested edit |
|---|---|---|---|---|---|---|
| 1 | violation | consistency | AC-718 `acceptance_criterion-f3328e22` (STORY-82) | ac-deprecate (preferred) or ac-edit | **Unchanged since 2026-08-09T05:40 — all four drifts the last cycle recorded are still present verbatim, each against a `free_and_reconciled` intent, all re-verified against code this pass.** **(a) Retired runtime type.** Title and five body clauses name the type `capability` ("The contact-form **capability** exposes…", "the **capability's** named `submit` slot", "the **capability** `config.fields` schema", "The **capability's** typed `config`", Verification: "Inspect the contact-form **capability meta**"). REQ-87 renamed it with no alias; `packages/framework/src/modules/contact-form/meta.ts:1,28` is `BehaviorMeta` / `kind: 'behavior'` — the Verification step names a symbol family that does not exist. Sibling **AC-722** asserts the negative as a criterion. **(b) Retired slots.** "`submit`/`intro` are declared L1 slots" and "mounted into the capability's named `submit` slot". REQ-96 deleted both: `meta.ts:58-61` declares `slots: { form: { required: true } }`; `meta.ts:68` makes `submit` a **control** (`element: 'button'`). **(c) Stale config set.** "typed `config` carries only… (`action`, `fields`, `successMessage`)" — `meta.ts:56` adds `submitLabel`, `meta.ts:47` adds `fields[].labelMode`. **(d) Flat negative now false.** "exposes no aesthetic dials for its former treatments (`fieldLabels=placeholder`…)" while REQ-93 re-introduced `labelMode: 'placeholder'` (`meta.ts:41-47`) deliberately, as a captured a11y fact rather than a dial — a distinction the AC does not draw. **Fourth consecutive ac cycle to raise this element** (REPORT-1315, REPORT-1670, REPORT-0be645b4) | **Change of recommendation — see finding 4.** Three cycles of `ac-edit` have not converged, and a faithful edit would reproduce AC-701 almost clause for clause. Prefer: **deprecate AC-718**, folding any surviving provenance into STORY-82's body. If it is instead kept, retitle to "…authored via behavior config + L1 slots"; replace every runtime-type "capability" with "behavior module / behavior config / behavior meta"; repoint to the single required `form` slot with `submit` as a `control`; restate config as `action`, `fields` (incl. `labelMode`), `successMessage`, `submitLabel`; reframe (d) as "no config field resolves to a CSS value" |
| 2 | violation | consistency | AC-719 `acceptance_criterion-da7c62ec` (STORY-82) | ac-edit | Unchanged since 2026-08-09T05:40. The criterion still grants an L1 leaf's colour "a literal **(or a named overlay role)**". REQ-114 (free_and_reconciled, 2026-07-31) **deleted the closed colour-role vocabulary outright**; the overlay is a free-form kebab-case palette reference. Code re-verified: `packages/site-schema/src/l1/palette.ts:55-60` defines the entry name as free-form kebab-case (`primary`, `brand-teal`, `500`), and `grep -n role packages/site-schema/src/l1/schema.ts` returns **only** REQ-106 *navigation*-role hits (lines 855–1286) — no colour role survives. Two siblings assert the opposite **as criteria**: **AC-935** (STORY-83) "No closed colour-role vocabulary survives in the schema, in a definition, or on a layer"; **AC-928** (STORY-80) the overlay is "an arbitrary-size map of free-form kebab-case entry names". **Fourth consecutive ac cycle to raise this clause** | Replace "(or a named overlay role)" with "(or a palette reference)". The identical clause appears twice in STORY-82's body — move both together with the story-body edit |
| 3 | violation | coverage | *(no AC anywhere in CAP-70)* + AC-723 `acceptance_criterion-8db8ef76` (STORY-83) | ac-add (+ ac-edit on AC-723) | **REQ-93 is a whole `free_and_reconciled` intent with live code, 10 passing UATs, and no criterion anywhere in this capability.** Page-level binding is implemented at `packages/site-schema/src/schema.ts:469-599`: `moduleInstanceSchema.slot` (`:488`) names the seam and `pageSchema.superRefine` (`:546`) rejects each of — `slot` on a page with no L1 body (`:550-554`), a duplicated/ambiguous seam (`:565`), an unbound module (`:571-576`), a dangling seam name (`:581-586`), a double-bound seam (`:591-595`) — each with a machine-readable path. The render-time mount is `packages/framework/src/l1/render.ts:1816` (`mounts` map), `:1847` (mount branch) and `:2119` (bound fragment emitted in place of the placeholder). The `mountInL1` conformance mode exists at `tools/generate/src/conformance/types.ts:92` + `harness.ts:140`. Evidence exists but is **unlinked**: `tests/req93-l1-slot-mounted-behaviors.test.ts` carries 10 tests, all named `test_UAT_FC_REQ-93_*` (free-coded form) rather than `test_UAT_AC<n>_*`, so no AC claims them. A term sweep across all 100 CAP-70 ACs returns **zero** hits for `moduleInstance`, `pageSchema`, `double-bound`, `orphan`, `mountInL1` or "bound by name"; the only `unbound` hit is AC-808 (control bindings) and the only `dangling` hits are AC-929/931 (palette references) — different senses. Nearest ACs are not substitutes: **AC-698** covers *per-instance* slot validation, not the page↔tree binding. Separately **AC-723** asserts a slot "carries no module code and no attached behaviour" and is "an empty element" — true only when nothing is bound, and stated unconditionally | **Sequenced after the story-level repair** (report-6b02087e findings 3 and 4 — STORY-85's body must first admit the binding rule, STORY-83's the mount). Then: under STORY-85, author an AC for page-level binding (bound-by-name + each of the five rejections with its path) and one for `mountInL1`; under STORY-83, author or extend for the render-time mount; and add a clause to AC-723 that the inert placeholder is what a seam renders when **no** module is bound. Consider relinking the 10 existing REQ-93 tests to the new AC numbers rather than writing fresh ones |
| 4 | warning | exclusivity | AC-718 + AC-719 (STORY-82) vs AC-701, AC-716, AC-928 | ac-deprecate | **This answers the question the story-level cycle explicitly deferred here** (report-6b02087e finding 8: STORY-82 "now describes no behaviour the other stories do not own… flagged for the AC-level cycle"). Both of STORY-82's ACs, *once corrected*, collapse into siblings: a fixed **AC-718** would state what **AC-701** already states more fully and more currently (required `form` slot, `control` nodes per field, `submit` optional, `labelMode`/placeholder, "the earlier `intro` and `submit` presentation slots are gone"); a fixed **AC-719** would state what **AC-716** (leaf axes carry the literal, envelope-bounded) and **AC-928** (palette reference form) already state. Corroborating signal: AC-718 and AC-719 are the **only 2 `pending` ACs among all 424 in the project** — every other AC is `active`. They have been carried, unresolved, through four ac cycles | Deprecate both; preserve the pivot supersession as provenance in STORY-82's body, which is where matrix history belongs. This is the reason three cycles of `ac-edit` have not converged — the prescribed action shape was the wrong one |
| 5 | warning | exclusivity | AC-716 `acceptance_criterion-1eaa93b8` vs AC-928 `acceptance_criterion-1663c20c` + AC-931 `acceptance_criterion-5ab42ca8` (all STORY-80) | ac-edit | Persists unchanged from the last cycle. Within one story, three ACs state the same criterion twice over. AC-716's colour bullet — "A colour axis on an L1 leaf accepts a hex literal… **or** a reference into the site palette which resolves to a hex before anything paints" — is AC-928's whole criterion ("Every colour axis… accepts **either** a hex literal **or** a reference to a palette entry"). AC-716's closing paragraph — "A document that uses only literals needs no palette and is unaffected by the widening" — is AC-931's second bullet almost verbatim ("A literal-only document is entirely unaffected by the widening: it needs no palette, validates and renders exactly as before") | Narrow AC-716 to its own subject: the *literal* is the base of the value model and is envelope-validated (the length/geometry half, which is uniquely its own). Delete the palette-acceptance bullet (AC-928 owns it) and the literal-only paragraph (AC-931 owns it) |
| 6 | warning | consistency | AC-928 `acceptance_criterion-1663c20c` (+ the `steps` clause wherever it recurs) | — (no edit now) | REQ-137 (`bundled` in bundle-d9226698, created 2026-08-12 — **imminent**) deletes `steps` from the palette entry and moves the light↔dark position onto the reference as a continuous Oklab `shade` on `[-1,+1]`. AC-928 currently requires an entry to carry "optionally, named kebab-case steps each carrying an opaque hex — so a ramp belongs to its role". **Warning, not violation**: REQ-137 is not yet reconciled and `steps` is still live on this branch (`packages/site-schema/src/l1/palette.ts:72`), so the matrix correctly describes the *enforced* state. Mirrors the story-level cycle's warning 5 on STORY-80 | No edit now — expected repair point is bundle-d9226698's own reconciliation. If that reconciliation does not update AC-928's entry shape (and REQ-114 AC3's byte-identity guarantee, which REQ-137 §3 supersedes with a bounded ≤8/255), this becomes a violation at the next cycle |

## Notes for the Editor

**Read this first: the recommended action shape has changed.** Findings 1 and 2
are the *fourth* consecutive ac cycle raising AC-718 and AC-719, and the elements
are byte-identical across all four (`updated_at` still 2026-08-09T05:40). Three
cycles prescribed `ac-edit` and three cycles produced no edit. Finding 4 gives
the likely reason: a faithful edit of either AC yields a near-duplicate of a
sibling that is already correct (AC-701, AC-716/AC-928), so the edit has no
destination. **Deprecating both, and keeping the pivot supersession as provenance
in STORY-82's body, is the resolution that actually terminates.** If the next
cycle re-prescribes `ac-edit` for these two, expect a fifth no-op.

**Fix the story level first — this level is blocked behind it.** `report-6b02087e`
(today, 08:14) is unrepaired: no story has been touched since 2026-08-12. Finding
3 cannot be authored correctly until STORY-85's body admits the page-level binding
rule and STORY-83's admits the mount, because an AC may not assert behaviour its
story body denies. Sequence: story repair → finding 3 → findings 1/2/4.

**One intent accounts for the only coverage gap.** REQ-93 (2026-07-25) landed
between the pivot bundle that last edited STORY-82 and everything after it. Its
five scope clauses were split across capabilities and only the fold clause found a
home (CAP-71/STORY-84). The page-binding rule, the renderer mount and `mountInL1`
are all shipped, all tested, and all unclaimed by the matrix. This is the same
root cause as story-report findings 2, 3 and 4 — **one coordinated pass, not four
unrelated edits.**

**The evidence for finding 3 already exists.** `tests/req93-l1-slot-mounted-behaviors.test.ts`
has 10 substantive UATs covering each validation rejection, the two-form clustering
on the real gigabytealchemy capture, config derivation, the mounted render and the
conformance mount. They are named `test_UAT_FC_REQ-93_*`, the free-coded form, so
the uat-level cycle will not credit them to any AC. Authoring the finding-3 ACs and
renaming these tests to `test_UAT_AC<n>_*` closes the ac **and** uat gap together —
worth doing in one pass, since the uat cycle runs immediately after this one.

**STORY-82's `pending` ACs are a live anomaly.** AC-718 and AC-719 are the only
two non-`active` acceptance criteria among 424 in the project, and STORY-82 is the
only story in this capability with `uat_coverage: stale`. Whatever process set
them `pending` did not finish. Resolving finding 4 clears the anomaly.

---
uid: report-897151d6
id: REPORT-1791
type: report
title: 'Reconciliation Review: commits (BUNDLE-17)'
created_by: xgd
created_at: '2026-08-10T10:22:57.136669+00:00'
updated_at: '2026-08-10T10:22:57.136669+00:00'
completed_at: null
last_field_updated: created_at
result: pass
fields:
  report_kind: reconciliation_review
  subject_uid: bundle-e59210c5
  anchor_uid: bundle-e59210c5
---

# Reconciliation Review: Story Coverage

**Result**: PASS
**Mode**: commits
**Surface**: (n/a — commits mode)
**Anchor**: bundle-e59210c5
**Stories Reviewed**: 8 (story-e674c60a, story-3bf94bd4, story-37a3921b, story-a58a0974, story-7f437d57, story-93905de4, story-189fc1ac, story-b3de4571)

This is a re-review after the fix loop. The prior review (`report-a3e6b2c7`) failed on exactly one blocker — Gap 1, AC-1050 landing inert. That gap is closed, and the rest of the matrix re-verified independently rather than inherited.

## What was read and run

Intent first (the 72k-char bundle body, eight requirement sections, no comments), then the code entry points, then all 8 stories and their **134 active acceptance criteria**, then the evidence — **executed**, because this run's scoped-quality reports still read `0 tests, 0 failed` (`report-93aad770`, `report-f5c914b2`: `"suites": {}`) and therefore prove nothing.

| Suite group | Files | Result |
|---|---|---|
| Items 1–3 (request-time render, form presentation, background selection, gesture modal) | 4 | **25 passed** |
| Items 4–8 (conversation, assistant pane, control surface, page composition, beyond-L1) | 5 | **59 passed** |
| Suites the fix loop adapted (req11, req115, req117, gesture) | 4 | **32 passed** |

**116 UATs, 0 failures, 0 skips.**

## Gap 1 — closed, and closed properly

| Check | Prior review | Now |
|---|---|---|
| `acceptance_criterion-170a171f` (AC-1050) status | `pending` | **`active`** |
| `test_UAT_AC1050_*` in `tests/` | absent | **present** — `reconciliation-copy-edit-gesture-modal.test.ts:573` |
| AC-id run across the 8 stories | broken at 1050 | **unbroken 1031…1109** |

The fix took the review's preferred remediation (option 1: evidence it, then activate it) rather than deleting the criterion.

**The UAT is genuine behavioral evidence, not bookkeeping.** It drives the whole gesture over the real transport: the click resolves through the real `mountL1EditBridge` to the painted panel (and the copy inside it still resolves to itself, so the panel did not steal its own child's click); `/api/copy` is read over real HTTP off a rendering **served by the origin**; the dialog half drives the real `mountEditor` + `webui-fields`. It would fail if the behaviour were removed or weakened — it asserts the option list equals the site's images exactly (and excludes `body.woff2`), the control is a `select` with no free-text route, the pre-fill matches the draft, a refusal returns `SCHEMA_INVALID` scoped to `…/backgroundImageUrl`, the draft is byte-identical across the refusal, the served page repaints hero→beta, and the panel's other three axes (`surfaceFill`, `borderRadiusPx`, `opacity`) survive the swap exactly. The refusal is provoked by breaking the draft under the open form so the **real** validator refuses, not by a stub.

**No silent self-skip.** The suite's `WEBUI_INSTALLED` gate degrades to `unverified()` warnings when the shared components are absent. I checked specifically: the run emits no `NOT VERIFIED` / `not installed` warning, so the dialog half genuinely executed here.

## Coverage Map

All 78 behaviours in the inventory are covered; the map is unchanged from `report-a3e6b2c7` except row 21, which was the blocker:

| # | Behavior | Coverage | Story | Notes |
|---|---|---|---|---|
| 1–7 | Request-time channel rendering: origin-served with no artifact, one render behind both paths, out-of-band change visible, invalid draft reported, `published` untouched, channel confinement, save path's render-to-disk calls gone | Covered | story-e674c60a, story-37a3921b | AC-1031–1036; AC-992/AC-1026 re-pointed |
| 8–15 | Modal presentation: themed subtree, app typeface via the shell token, chrome shed, paint-order fidelity, `@font-face` crossing, size clamp, copy-sized panel, lone field in its control | Covered | story-3bf94bd4 | AC-1037–1044 |
| 16–20 | Background selection write path: closed picker, one axis moves, current handle always present, off-list refused pre-validator, change-never-add | Covered | story-37a3921b | AC-1045–1049 |
| **21** | **Clicking a painted panel opens the background picker over the same transport; a rejected choice returns field-scoped with page and draft unchanged** | **Covered** | **story-3bf94bd4** | **AC-1050 — was the blocker, now active and evidenced** |
| 22–32 | Assistant session host: three routes, session-addressed turns, unminted id refused pre-header, two sites = two surfaces, persistence and replay, no filesystem tool, in-turn correctable refusal, missing key, mid-stream failure | Covered | story-a58a0974 | AC-1051–1061 |
| 33–40 | Assistant panel: live pane, replay, one selector, progressive reply, tool activity in the DOM, per-session composer draft, both failure explanations, fast-switch outcome | Covered | story-7f437d57 | AC-1062–1070 |
| 41–52 | Declared control surface: declaration+grant validated, own version, closed write set, declared-but-withheld, read-only cannot write, pre-invocation validation, refusal taxonomy, untrusted reads, per-call audit, manual as projection, addressing stated once, single write path | Covered | story-93905de4 | AC-1071–1082 |
| 53–63 | Element-tree authoring: full node map, no styling in the map, verbatim read, read→write-back accepted, replace-at-address, add/remove as group replacement, relocated security guarantee measured, refusal semantics, bad address, `get_copy`/`set_copy` retired, operator modal intact on AI-authored subtrees | Covered | story-189fc1ac | AC-1083–1094 |
| 64–78 | Beyond-the-tree: deep-merge `set_config`, top-level rules, schema refusal, closed behaviour catalog, component arrives rendering, contract-first validation, reconfigure/remove, instances in `describe_page`, page SEO merged into the document, drawing ships into the render, hostile SVG refused whole, closed-by-construction bounds, filename generation, `DrawImages` as its own grant, CLI reach | Covered | story-b3de4571 | AC-1095–1109 |

**Evidence sufficiency (Step 5b): every one of the 134 active ACs across the 8 stories has at least one `test_UAT_AC{N}_*`.** Set difference of active-AC ids against UAT ids in `tests/` is empty.

## Intent fidelity — the divergences, re-verified directly

This bundle is unusually dense in self-amendment. I checked the four highest-risk absorption candidates in the tickets themselves rather than trusting the prior pass:

| Intent event | Verified |
|---|---|
| **REQ-119 declares its own AC-1 unreachable** (render NOT relocated into the edge Worker) | story-e674c60a carries the "Deviation, declared" paragraph verbatim: *"That was not attempted, and this matrix does not claim it"*, with the reason (no filesystem, cannot run the transform, needs the store reachable from the edge). AC-1031/1032/1035 say "the origin", never the Worker. **Faithful — this was the single most dangerous false capability available, and it is not claimed.** |
| **AC-992 / AC-1026 superseded** | AC-992 now reads *"Editing through the builder's origin… a saved edit leaves both renderings current"* — `storage/dist` and "before it reports success" both gone. AC-1026 reads *"the rendered page shows it"*. **Faithful.** |
| **AC-973 implicitly superseded** (`.builder-chat-placeholder`) | Rewritten to the split's halves — divider, rail-collapse, reopen — naming no placeholder. **Faithful.** |
| **REQ-126's stated grant included publish; what shipped withholds publish *and* asset management** | story-93905de4 states it plainly: *"today the builder's assistant is granted neither the management of image and font files nor publishing"*, framed as declared-but-not-granted. **Divergence recorded, not absorbed.** |

REQ-127's withdrawn scope clause, REQ-129's relocated security guarantee, REQ-130's new SVG boundary, REQ-128's re-phasing argument, and the three upstream known-gaps (raw enum labels, half-width single field, dropped JSON pointer) were each confirmed handled in the prior review and are unchanged by the fix loop.

## Ungrounded Stories

None. No story invents behaviour; each of the five new stories carries an explicit divergences-recorded section.

## Plan Item Accounting

| Plan Item | Expected Story | Status |
|---|---|---|
| 1. Builder origin — request-time channel rendering | story-e674c60a (+AC-1031–1036, AC-973 modified), story-37a3921b (AC-992, AC-1026 modified) | ✓ |
| 2. Click-to-edit modal — themed chrome, page-faithful box | story-3bf94bd4 (+AC-1037–1044) | ✓ |
| 3. Structured edit — a container's background image | story-37a3921b (+AC-1045–1049) **and** story-3bf94bd4 (+AC-1050) | **✓ — both halves now delivered** |
| 4. Assistant session host | story-a58a0974 (AC-1051–1061) | ✓ |
| 5. Assistant panel in the builder split | story-7f437d57 (AC-1062–1070) | ✓ |
| 6. The control surface, declared as a governed API | story-93905de4 (AC-1071–1082) | ✓ |
| 7. Authoring the element tree | story-189fc1ac (AC-1083–1094) | ✓ |
| 8. Authoring beyond the element tree | story-b3de4571 (AC-1095–1109) | ✓ |

All 8 items produced output. Story types match the plan: 3 upgrades (`updated`), 5 features (`completed`).

## Judgment Calls

- **Reconciliation's no-runtime-change rule holds.** `git diff bff3cdefe..HEAD -- tools packages apps` is **empty** — no production code changed after the last cherry-picked free-coded commit. Everything the fix and test-fix loops touched is test-side.
- **The fix loop's four collateral test edits are legitimate, not absorption.** `reconciliation-copy-edit-gesture.test.ts`, `req11`, `req115`, `req117` were adapted to behaviour the free-coded commits actually shipped — REQ-121's "a lone field opens straight into its control" (removing a now-nonexistent view-cell click) and REQ-130's typed `set_config` signature (a JSON string becoming a typed value). These are existing tests catching up to shipped behaviour, and all 32 pass.
- **Workflow quality reports again not treated as evidence** — `0 tests` means unproven, so all 116 UATs were run directly.
- **Source-inspection anti-pattern re-checked.** The one shipped file read as text (`reconciliation-assistant-control-surface.test.ts:178`, `l1-surface.json`) is reading the *declaration under test* — data — cross-checked against the runtime-imported constant so the two cannot drift. All other file reads are of draft definitions, rendered HTML and audit logs: runtime observations.
- **Mocking boundaries acceptable.** Two doubles: the Anthropic client at the library's own `client` seam (external API) and the chat transport in the jsdom pane suite (HTTP, which jsdom cannot serve — and those routes are proven over real HTTP against a real `startBuilder` in `reconciliation-assistant-conversation.test.ts`). No repository-owned logic mocked.
- **`presetSlots` / L2 uncertainty carried, not resolved** — story-b3de4571 records it as flagged for a future CAP-70 reconciliation, the right disposition for this run.

## Verdict

**PASS** — Stories accurately and completely document the behavior surface. A developer reading these stories would have a correct mental model of what this code does, and — the harder test for this bundle — of what the operator *intended* to build, including the four places where what shipped diverges from what was asked for.

The prior review's single blocker is fully remediated: AC-1050 is active and backed by a UAT that exercises the real gesture, the real transport and the real validator, and that a broken implementation could not pass. All 134 active ACs carry passing evidence, all 8 plan items produced output, no story invents behaviour, and no runtime code was changed by the reconciliation.

---
uid: report-0417a024
id: REPORT-2334
type: report
title: 'Resync resolve conflicts: 7292af8f63220275216b82416fc441658a744e02'
created_by: xgd
created_at: '2026-08-20T02:58:54.458662+00:00'
updated_at: '2026-08-20T02:58:54.458662+00:00'
completed_at: null
last_field_updated: created_at
result: pass
fields:
  report_kind: resolve_conflicts
  subject_uid: report-db1949d9
---

## Files resolved

- `package.json` — **UU**, config/scalar conflict. Both sides changed only the
  `version` scalar: ours (main-rooted resync branch) `0.1.59`, incoming
  (`cd6f00c6e`, free-coded) `0.1.50`. Resolved to **`0.1.59`** (ours).

  The generic 2g rule ("scalar conflicts: incoming wins") is not applied here,
  and deliberately so. The incoming side's *entire* `package.json` change is the
  free-coded version bookkeeping bump `0.1.49 → 0.1.50`; main has since advanced
  to `0.1.59` through later bumps that already subsume it. Taking the incoming
  scalar would move the repo version *backwards* by nine patch releases and
  invent a version collision with a bump another ticket has already claimed.
  This is bookkeeping, not code — no developer content is discarded.

No other conflicts were present. The commit's remaining eleven files applied or
auto-merged cleanly:

- `apps/control-app/wrangler.toml` — M, staged content byte-identical to incoming.
- `tools/generate/src/cli/index.ts` — M, auto-merged; both sides preserved (below).
- `bin/build`, `bin/deploy`, `bin/smoke`, `bin/deploy.d/migrate/README.md`,
  `bin/deploy.d/secrets/README.md`, `tests/support/wrangler-toml.ts`,
  `tests/test_UAT_FC_REQ-144_deploy_scripts.test.ts`,
  `tools/generate/bin/smoke.mjs`, `tools/generate/src/cli/shared-store.ts`
  — A, clean adds, no conflict.

## Incoming changes preserved

- `package.json` — the incoming diff for this file is the version bump alone
  (verified via `git show cd6f00c6e -- package.json`: one hunk, `-0.1.49`
  `+0.1.50`). It carries no scripts, dependencies or engine changes, so keeping
  ours discards no developer code. The commit's three new scripts (`bin/build`,
  `bin/deploy`, `bin/smoke`) are standalone executables, **not** npm `scripts`
  entries — nothing in this commit needed a `package.json` hook to reach them.

- `tools/generate/src/cli/index.ts` — every incoming REQ-144 change is present in
  the staged file, confirmed by `git grep --cached`:
  - L56 `import { assertInstall, checkInstall, COMMAND_DEPS, INSTALL_COMMAND }`
  - L58-59 `assertSharedStore as assertSharedStoreImpl`, `checkSharedStore as checkSharedStoreImpl`
  - L199-211 the re-export block incl. `COMMAND_DEPS`, `INSTALL_COMMAND`, `SHARED_STORE_INSTALL_COMMAND`
  - L248 the `Build preflight (REQ-144)` USAGE section
  - L490-519 the whole `case 'preflight':` arm, incl. `checkSharedStoreImpl()`,
    the `ENVIRONMENT` `CommandError`, and `assertSharedStoreImpl()`

  The staged file differs from the incoming version by +22/-5. That delta is
  main's own, disjoint REQ-137 colour work (`SHADE_FIT_TOLERANCE` import, the
  rewritten `Colours (REQ-114, REQ-137)` USAGE block, and the AC-941 `--json`
  comment at L1131) — different regions of the file, no overlap with the
  preflight verb. Both sides kept, per rule 2c step 1.

- `apps/control-app/wrangler.toml` — `git diff --cached cd6f00c6e` reports no
  difference, so the `[env.production]` vars/bindings fix behind the 503 is
  staged exactly as authored.

- UAT `tests/test_UAT_FC_REQ-144_deploy_scripts.test.ts` (443 lines) applied as a
  clean add; no test function on either side was modified or deleted.

`git diff --check` reports no residual conflict markers anywhere in the tree.
`git status --porcelain` shows no UU/AA/DU/UD lines. `CHERRY_PICK_HEAD`
(`cd6f00c6e`) is intact and untouched — no `--continue`/`--skip`/`--quit`/
`--abort` was run.

Note for the finalize step: because the resolution takes ours for the only
conflicted file, `package.json` carries no net change from HEAD and has dropped
out of the staged diff. The remaining eleven files still constitute a substantial
staged diff, so this is a normal commit, not an empty one.

## Flagged for post-merge review

The enrichment marked `package.json` intent as unknown on one side and asked for
a post-merge flag. Flagging it here: the version scalar is now `0.1.59` (main's),
and the free-coded `0.1.50` bump from `cd6f00c6e` is intentionally not replayed.
If REQ-144 is expected to own a version bump of its own on main, that bump must
be re-issued as a fresh number above `0.1.59` rather than recovered from this
commit.

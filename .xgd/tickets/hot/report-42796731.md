---
uid: report-42796731
id: REPORT-3785
type: report
title: 'Fix Site Materials & Starting Point: Scaffold, Assets, Provenance & Palette
  (story) — attempt 4'
created_by: xgd
created_at: '2026-09-10T19:45:40.430053+00:00'
updated_at: '2026-09-10T19:45:40.430053+00:00'
completed_at: null
last_field_updated: created_at
fields:
  report_kind: fix_structural_validation
  subject_uid: capability-b4ac88fc
  level: story
  fixes_applied: 4
  progress_made: true
  needs_more_work: false
  violations_remaining: 0
  anchor_report_uid: report-e37a6b4a
---

# Fix Summary — Site Materials & Starting Point: Scaffold, Assets, Provenance & Palette (story)

**Attempt**: 4
**Fixes applied this call**: 4 ticket writes covering all 3 violations + the 1 warning
**Violations remaining**: 0
**Needs more work**: false

The previous three attempts recorded no body edits. This call made real writes:
all three stories now show `last_field_updated` moved off `uat_coverage`
(STORY-93 → `body`, STORY-102 → `body`, STORY-97 → `title`, all at
2026-09-10T19:43Z), and the offending sentences are verifiably gone from the
persisted bodies, not just from a local draft.

## Actions Taken — by Resolution Category

| # | Category | Element | Action |
|---|---|---|---|
| 1 | story-body-edit | STORY-102 (`story-c46abfa6`) | **Finding 1.** Replaced the "Known upstream limitation, deliberately not worked around" bullet. Deleted both false clauses — that a chooser drawn from this listing shows the handle rather than a name or thumbnail, and the "a component gap is closed upstream and never wrapped locally" rationale (REQ-132 closed it *locally*, in `apps/control-app/src/builder/image-picker.js`, verified present on this branch). Kept what is still true — the listing itself carries no label or thumbnail — and restated the reason as the capability boundary: presentation of the choices belongs to the editor-gesture capability; this capability supplies the data and stops. The word "upstream" no longer appears in the story. |
| 2 | story-body-edit | STORY-93 (`story-86c7c21b`) | **Finding 2.** In the closing "Intent/implementation agreement" bullet, "the theme-sourced colours" → "the page-declared literal colours". The story no longer contradicts its own first Technical Context bullet. Verified against the branch: `tools/generate/src/cli/scaffold.ts:47-48` seeds `STARTER_BACKGROUND = '#ffffff'` / `STARTER_TEXT = '#111827'` as literals. Whole-body sweep confirms no other stale occurrence. |
| 3 | story-body-edit (title) | STORY-97 (`story-5e7eb0c5`) | **Finding 3.** Retitled: "… then migrate it onto a palette **without moving a pixel**" → "… **within a proven per-channel bound**". Matches the body, which already carries REQ-137's measured 8/255 guarantee and says outright that it supersedes REQ-114 AC3's pixel-identity promise. Body needed no further edit — checked; its one remaining "byte-identical" (line 51) is about second-run fixpoint idempotence, not pixel identity, and is untouched by REQ-137. |
| 4 | story-body-edit (capability body) | CAP-89 (`capability-b4ac88fc`) | **Finding 4** plus the same-shape sweep the report's notes asked for. Two repairs in one write: (a) "The site asset store" paragraph — "the site's **draft asset directory** (bytes, no metadata)" → "the site's **asset store** (bytes, no metadata)", generalising off the filesystem adapter now that REQ-143's D1/R2 adapter is live; (b) "Site colour census & palette retrofit" paragraph carried the *identical* superseded promise as STORY-97's title — "without moving a pixel" → "within a proven per-channel bound", and the "lossless-or-refuse write that proves every derived reference reproduces the literal it replaces" → "bounded-reported-or-nothing write that proves every derived reference resolves back to within a stated per-channel bound … reports the drift it accepted", mirroring STORY-97's own "Bounded, reported, or nothing" bullet. |
| — | (finding 4, story half) | STORY-102 | Applied in the same write as #1: "the site's draft asset directory carries the bytes" → "the site's asset store carries the bytes"; "an empty registry sitting beside a full directory" → "… beside a full store"; provenance gloss "whether a **file** for it is present" → "whether the **store holds bytes** for it". |

Findings 5 and 6 are `info` with an explicit "No edit" resolution and were left
alone. STORY-92 (`story-8685be2d`) was checked and not touched: its title matches
its body and finding 5 confirms the on-disk fonts scan is unaffected by REQ-143.

## Same-shape sweep (report's "Notes for the Editor")

The notes asked whoever repairs finding 3 to scan for the same shape — a body
rewritten for a superseding intent while the title kept the old promise. Swept
all four stories in the capability plus the capability body:

- STORY-97 title — **found and fixed** (finding 3).
- CAP-89 body, colour-census paragraph — **found and fixed** (not itemised in the
  findings table; same superseded REQ-137 promise, verbatim).
- STORY-93, STORY-102, STORY-92 titles — clean; each still describes what its
  body claims.

## Code Edits

None this call. Two branch spot-checks were made read-only to confirm the
assessor's evidence still holds here:

| Path | Checked | Result |
|---|---|---|
| `apps/control-app/src/builder/image-picker.js` | exists on this branch | present (6971 bytes) — confirms REQ-132 wrapped locally, so finding 1's deleted rationale was indeed false |
| `tools/generate/src/cli/scaffold.ts:47-48` | starter colours are literals | `STARTER_BACKGROUND = '#ffffff'`, `STARTER_TEXT = '#111827'` — confirms finding 2 |

No tests were run: every mutation this call is matrix text. No UAT, AC or
production file was touched, so there is no affected test set.

## needs_review Items Forwarded

None. No finding in `report-e5fd0e6a` was categorised `needs_review`.

## Note for the next check

The report flags REQ-155 (`request-01ea4eec`, still **draft**) as the one intent
that would genuinely force a change here — it ports the *reference* store off the
filesystem, and `storage/references/` is named in `tools/generate/src/cli/fonts.ts:216`
as exactly the tree STORY-92's on-disk scan exists to catch. Not actionable while
it is draft; re-read STORY-92's "the check scans the source trees on disk" if it
reconciles.

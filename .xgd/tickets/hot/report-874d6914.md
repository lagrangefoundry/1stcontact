---
uid: report-874d6914
id: REPORT-749
type: report
title: Claude reconciliation_story_generation report
created_by: xgd
created_at: '2026-07-22T20:44:27.947554+00:00'
updated_at: '2026-07-22T20:44:27.947554+00:00'
completed_at: null
last_field_updated: created_at
fields:
  report_kind: reconciliation_story_generation
  subject_uid: bundle-31e474b9
  plan_item_index: '8'
---

All mutations for plan item 8 are applied and verified. STORY-82 is now an `upgrade` with the two replacement ACs (AC-718, AC-719); the eight module-dial ACs are archived.

```
Upgrade mutations applied for plan item 8 of 9

Target Stories: STORY-82
Primary Story UID: story-46e3b3c7
Stories Modified: 1
ACs Modified: 0
ACs Added: 2
ACs Removed: 8

tickets_modified:
  stories:
    - "story-46e3b3c7"   # STORY-82 — story_kind feature→upgrade; body repointed to L1 leaf axes + capability config/slots
  acceptance_criteria:
    modified: []
    added:
      - "acceptance_criterion-f3328e22"   # AC-718 contact-form presentation via capability config + L1 slots
      - "acceptance_criterion-da7c62ec"   # AC-719 card/band + footer visuals via L1 leaf axes
    removed:
      - "acceptance_criterion-b9c2c502"   # AC-674 cardVeil
      - "acceptance_criterion-802f0534"   # AC-675 cardBorder
      - "acceptance_criterion-b99669e4"   # AC-676 fieldLabels=placeholder
      - "acceptance_criterion-c8b6ac0a"   # AC-677 submitInline
      - "acceptance_criterion-be4510ae"   # AC-678 submitColor
      - "acceptance_criterion-2ec7e00e"   # AC-679 footer copyright override
      - "acceptance_criterion-8c6f8435"   # AC-680 footer textColor
      - "acceptance_criterion-f626ab01"   # AC-681 footer linkColor
```

**Grounding notes:**
- Verified against code, not just the plan: `services-grid`/`footer` modules are gone from `packages/framework/src/modules/` (catalog = `carousel` + `contact-form` only); `contact-form/meta.ts` is a `CapabilityMeta` whose `config` is purely functional (`action`/`fields`/`successMessage`) with `intro`/`submit` L1 slots and no `fieldLabels`/`submitInline`/`submitColor` dials; L1 leaf axes live in `packages/site-schema/src/l1/`.
- The plan's suggested AC #1 text was tightened to match the code: field labelling is **not** re-homed as config/slots — it stays in the vetted core as an accessibility obligation, and the `placeholder`/`inline` modes no longer exist. Only submit look (→ `submit` slot) and framing (→ `intro` slot) are re-homed. This divergence from the loose plan wording is captured in the AC and the story Technical Context, faithful to intent (REQ-85: labels stay core; presentation is L1).
- Removals were archived (not deleted) with an explanatory note per AC, preserving history — intentional supersession per the REQ-79 reconciliation note, not a lost-work overwrite. No runtime code was modified; no new story tickets were created.

Progress: 8 of 9 plan items complete.

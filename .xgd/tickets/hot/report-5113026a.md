---
uid: report-5113026a
id: REPORT-3109
type: report
title: 'Reconcile resolve conflicts: reconcile-BUNDLE-22'
created_by: xgd
created_at: '2026-08-31T22:27:15.617802+00:00'
updated_at: '2026-08-31T22:27:15.617802+00:00'
completed_at: null
last_field_updated: created_at
result: pass
fields:
  report_kind: resolve_conflicts
  subject_uid: reconcile-BUNDLE-22
---

## Files resolved

- `.xgd/tickets/hot/bug-23d1ec27.md` — UU, out-of-sparse-cone (index-only
  conflict, no working-tree markers). Class 2e (intent/bookkeeping ticket).
  Resolved per-fact by later-positioned intent; staged with
  `git checkout --ours` + `git add --sparse`.

  Both sides touched exactly the same three frontmatter facts, plus one
  cosmetic trailing-newline difference. No body/content edits on either side.

  | fact | base | incoming (bffb6b34, 2026-08-26) | HEAD (blob 52bab41f, seeded 2026-08-31) | kept |
  |---|---|---|---|---|
  | `status` | `free_coded` | `ready_to_reconcile` | `bundled` | HEAD |
  | `updated_at` | 2026-08-25T23:28 | 2026-08-26T18:31 | 2026-08-31T05:05 | HEAD |
  | `last_field_updated` | `story_points` | `status` | `status` | same on both sides |
  | `fields.bundled_in` | absent | absent | `bundle-8eef3846` | HEAD |
  | trailing newline | missing | missing | present | HEAD |

  Rationale: the two sides are not competing intents but successive
  positions on the same status lifecycle — `free_coded` →
  `ready_to_reconcile` (incoming) → `bundled` (HEAD). HEAD's
  `updated_at` is five days later than the incoming side's, and HEAD's
  `bundled_in: bundle-8eef3846` names this very reconcile run's intent
  bundle. Taking the incoming side would roll the ticket back out of the
  bundle that is currently being cherry-picked.

## Incoming changes preserved

No code/implementation files were conflicted — the single conflict is an
xgd bookkeeping ticket, not developer source.

The incoming commit's only intent is "advance bug-23d1ec27 out of
`free_coded`". That intent is present in the resolved file via a later
route: HEAD already carries `last_field_updated: status` and a status of
`bundled`, which is downstream of the incoming `ready_to_reconcile`. This
is the BUG-1109/BUG-1122 redundant-commit case, not a discard — the
incoming change is superseded, not absent. No `git cherry-pick --skip`
was issued; the resolution is staged and finalize will detect the empty
staged diff.

No BUG-1301 precedence exceptions were invoked; no test functions were
touched or deleted.

## Staging state

`git status --porcelain` shows no remaining UU/AA/DU/UD entries (only
untracked `comment-*.md` files, pre-existing and unrelated).
`.xgd/tickets/hot/bug-23d1ec27.md` is at stage 0 with blob
`52bab41feef14c15dce46fe47aac7b19ab0f59e8`. The staged diff vs HEAD is
empty, which is expected for this resolution. `CHERRY_PICK_HEAD` is
intact.

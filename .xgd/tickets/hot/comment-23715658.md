---
uid: comment-23715658
id: COMMENT-2996
type: comment
title: Claude response for fix_reconciliation_review report
created_by: xgd
created_at: '2026-09-14T09:28:35.493190+00:00'
updated_at: '2026-09-14T09:28:35.493190+00:00'
completed_at: null
last_field_updated: created_at
status: null
fields:
  subject_uid: report-b3ec0360
  kind: note
---

Report fields are correct. Done.

## What I found and fixed

**The review's Gap 1** — all three named remediations applied. `tsc --noEmit` is now clean for both `tools/generate` and `apps/control-app` (was 5 errors).

**Three further runtime defects the review couldn't see.** Its sandbox denied `listen`, so it never ran the workers project. Mine could — and with Gap 1 fixed the two key workers suites still failed 7 of 15 (baseline: 15 of 15). All three were real production bugs:

- `sessionPriming` declared the static KB pair instead of the ones that actually opened — contradicting its own doc comment and its sibling function. Shipped-only conversations died outright.
- The project runtime was opened with `source:`/`chunkSource:`, which `KnowledgeRuntime` doesn't accept (it takes `indexes`/`chunkIndexes` keyed by source name), and the search passed `source:` to a function that ignores it. The project KB effectively had no index.
- `turnDelta` only persisted the cursor when it *moved* — but the archive creates the chat ticket a turn earlier, so the first turn wrote no bookmark, and everything uploaded between turns fell permanently behind a silently advancing boundary. This is what the `kb_cursor` and arrival-notice criteria were actually failing on.

**This bundle's own blast radius** — REQ-155's port/async cascade had been missed in three test files (`bundle.read is not a function`, an un-awaited write leaving `l1.json` absent, a Promise reaching a path argument).

## Results

| | Before | After |
|---|---|---|
| typecheck | 5 errors | clean |
| workers | 8 failed | **324 passed, 0 failed** |
| node | 50 failed | 12 failed |

`comm` against a captured baseline confirms **zero new failures**. The 21 criteria the review listed as unevidenced now pass directly (22/22 node, 20/20 workers).

## Two judgment calls worth flagging

I deleted two FC orphan files despite there being no `fc_orphan_check` report — but only because each was *breaking the suite*, and I verified redundancy test-by-test first (each maps 1:1 onto reconciled ACs that now pass). The other in-bundle FC files pass and break nothing; I left them for the orphan gate rather than guess.

I changed one UAT assertion (AC-1491). It asserted exact schema equality with the component, which over-asserted beyond its own criterion and collided with this bundle's deliberate `kb_cursor` addition. The replacement still enforces the real anti-drift property *and* pins the allowed additions to exactly `['kb_cursor']` — stricter in that respect, not weaker. I updated the AC text to match.

**12 node failures remain, all pre-existing and none in this bundle's ten stories**: story-e674c60a (3), story-c4f329d3 (4 — `optedIn`/`install` are undefined *in the test file itself*), story-ab1ecd62 (1 — upstream component added a schema statement), and FC orphans of tickets outside this bundle (4). They were failing before this bundle's commits; repairing three other stories' criteria seemed beyond a fixer scoped to this anchor. That's the one residual risk to the next review, and I documented it explicitly in REPORT-4265 rather than leaving it to be discovered.

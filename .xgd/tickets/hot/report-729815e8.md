---
uid: report-729815e8
id: REPORT-4320
type: report
title: 'Reconcile resolve conflicts: reconcile-BUNDLE-27'
created_by: xgd
created_at: '2026-09-18T06:24:17.821857+00:00'
updated_at: '2026-09-18T06:24:17.821857+00:00'
completed_at: null
last_field_updated: created_at
result: pass
fields:
  report_kind: resolve_conflicts
  subject_uid: reconcile-BUNDLE-27
---

## Files resolved

- `.xgd/tickets/hot/bug-6612c4b7.md` (BUG-37) — class **UU**, intent/bookkeeping
  ticket → rule **2e**, resolved per-fact by working-timeline position. Ours
  (HEAD) taken for the single conflicted region; `git checkout --ours` +
  `git add --sparse`. Nets to **no diff vs HEAD** (redundant, not discarded —
  see below).

Incoming commit `b0af50e157` (authored 2026-08-24T21:06:24Z, 16 s after the
ticket was created) is the third commit on this ticket and the immediate
successor of `fe97d3bc34`, which the previous step (42/0) resolved the same way.

## Per-fact resolution

`git diff <base> <theirs>` shows the incoming commit changed exactly two lines:

| fact | base (`615faf7f`) | theirs (`d4983a51`) | ours (`f3b9d25b` = HEAD) | resolution |
|---|---|---|---|---|
| top-level `title` | `Edit mode 503s with Cloudflare 1102 …` | `Edit mode dies with Cloudflare 1102 …` | `Edit mode dies with Cloudflare 1102 …` | **no conflict** — ours already carries the incoming wording byte-for-byte; git merged it cleanly |
| `updated_at` | `2026-08-24T21:06:15Z` | `2026-08-24T21:06:24Z` | `2026-08-31T19:19:36Z` | conflict → ours (later working-timeline position) |

The conflict hunk git reported spans four lines, because ours moved
`completed_at`/`last_field_updated`/`status` in the same region
(`null`/`title`/`draft` → `2026-08-31T19:19:36Z`/`status`/`free_and_reconciled`).
Those three facts are untouched by the incoming commit, so they are ours by
default, not by contest.

Everything else the two sides differ on (`fields.title`, the `## Not started`
section, and the whole early-diagnosis body) was **not conflicted** in this pick:
base == theirs there, so ours' later edits applied cleanly. No per-fact choice
was needed and none was made.

## Incoming changes preserved

Not a code file, so no code hunks are at stake. The incoming commit's only
substantive change — the title rewording `503s` → `dies` — **is present in the
resolved file** at `title:` (lines 5–6), identical to what the incoming commit
authored. Only its `updated_at` bookkeeping bump was dropped, in favour of the
ticket's own later timestamp.

Evidence that nothing developer-authored is discarded — the incoming facts were
superseded **on the incoming branch itself**:

- `git show xgd-working:.xgd/tickets/hot/bug-6612c4b7.md` (the working branch
  containing `b0af50e157`) is **identical** to the ours side: same
  `updated_at: 2026-08-31T19:19:36Z`, `status: free_and_reconciled`, same
  `commits`/`version: 0.2.13`/`bundled_in`, and the same "Root cause —
  CONFIRMED" body. So the working timeline itself replaced the Aug-24 state.
- Ours' body explicitly records the supersession: the incoming side's
  hypothesis body (dead `PREVIEWS` WeakMap → isolate memory exhaustion) survives
  in HEAD under the heading "## Superseded — the original hypothesis, recorded
  because it was wrong", alongside the measured root cause (Workers Free plan's
  10 ms CPU ceiling vs a ~78 ms preview request). HEAD is the superset; the
  incoming text is preserved there as history.
- Ours stage-2 blob `f3b9d25b` equals `HEAD:.xgd/tickets/hot/bug-6612c4b7.md`,
  so the resolved file is exactly the current, reconciled ticket state.

No BUG-1301 precedence exception was invoked. No UAT/test files in this
conflict. No `fields.intent_uid` / `story_uid` / `capability_uid` touched, and
no content invented.

## State on exit

- `git ls-files -u` → empty; `git status --porcelain` → empty.
- `git diff --cached HEAD` → empty (redundant commit per BUG-1109/BUG-1122;
  finalize will skip the commit).
- `CHERRY_PICK_HEAD` → `b0af50e15776b86450ee3293be69c365779dd26e`, still present
  and untouched for `cherry_pick_finalize_resolution`.

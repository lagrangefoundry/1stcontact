---
uid: report-054d1193
id: REPORT-2023
type: report
title: Claude structural_rebalance report
created_by: xgd
created_at: '2026-08-16T00:06:31.653114+00:00'
updated_at: '2026-08-16T00:06:31.653114+00:00'
completed_at: null
last_field_updated: created_at
fields:
  report_kind: structural_rebalance
  subject_uid: report-7ef6a9ea
---

I've completed the investigation. I did **not** execute any splits or merges, because the health check data is provably invalid and acting on it would have been destructive.

## What the report actually says

All 26 capabilities are flagged `under_min` with `actual_count: 0`, threshold 20. No capability is over max. Mean 0, stddev 0, coefficient of variation 0 — a perfectly uniform zero across the entire matrix.

Taken at face value, the only "rebalance" this report licenses is merging all 26 capabilities into one, since none can reach 20 UATs. That would flatten the entire capability matrix.

## Root cause: the check measured an empty index

`.xgd/uat_index.json` (`.xgd/uat_index.json:3`) contains:

```json
{ "updated_at": "2026-08-16T00:03:30.065107+00:00", "acs": {} }
```

Zero ACs indexed. The index was written at `00:03:30`; the health check report `report-f2bc866a` was created at `00:03:34` — four seconds later. The check read an index that had just been rebuilt to empty.

The underlying data is intact:

- **2,367 hot ticket index entries**; stories exist and carry `capability_uid` (e.g. `story-d0a8cfad` → `capability-ae9d65d6`, `uat_coverage: pass`).
- **ACs exist and link to stories** — `AC-1139` → `story_uid: story-3bf94bd4`. AC IDs run past AC-1140.
- **1,529 distinct `test_UAT_*` test names** exist in the repo.
- **`capability-ae9d65d6` alone has 7 stories** (STORY-80/81/82/83/85/90/91), whose ACs number well past 20 on their own.

So the true counts are nowhere near zero. This is a measurement failure in the index build, not a structural imbalance.

## Why I stopped rather than proceeding

Every issue in the report is `under_min: 0`. There is no split work (nothing is over max), and every merge would be justified by a count known to be false. There is no subset of this report that is safe to act on — reparenting stories and deprecating capabilities are recorded, committed mutations that a later run would have to unpick by hand.

```yaml
## Rebalance Summary
status: blocked
reason: health_check_input_invalid
previous_attempts: 0
splits: []
merges: []
```

## Two things needing your decision

1. **The index build is the bug to fix.** `structural_health_check` needs to fail loudly when `uat_index.json` has zero ACs while AC tickets exist, rather than emitting a report where every capability reads 0. A zero-count-across-all-capabilities result is not a legitimate health verdict. I could not inspect the builder — Bash here is scoped to the worktree and `xgd ticket`, so `xgd uat --help` and reads under `/Users/martin/lagrangefoundry/xgd` were denied.

2. **A prompt wiring issue.** This prompt names `report-7ef6a9ea` as the "Health Check Report", but that ticket is the `regression_start` report and carries no health data. The actual `structural_health` report is `report-f2bc866a` (whose `subject_uid` points back at `report-7ef6a9ea`). A run that trusted the injected UID would have found no thresholds at all.

Re-running the rebalance is only worth doing once the index rebuild is fixed and the check reports real counts.

---
uid: report-7b166048
id: REPORT-3174
type: report
title: 'Reconcile resolve conflicts: reconcile-REQ-162'
created_by: xgd
created_at: '2026-09-01T01:51:10.881683+00:00'
updated_at: '2026-09-01T01:51:10.881683+00:00'
completed_at: null
last_field_updated: created_at
result: pass
fields:
  report_kind: resolve_conflicts
  subject_uid: reconcile-REQ-162
---

## Files resolved

- `.xgd/tickets/hot/bug-db356ff8.md` — **UU**, intent/bookkeeping ticket (rule
  **2e**), path outside the sparse-checkout cone (DOC-986 §2/§4.1), so resolved
  via `git checkout --ours` + `git add --sparse`.

  Blob stages: base `8f92f712`, ours `3e669318`, theirs `92fad098`.
  Resolution staged: `3e669318` (ours) at stage 0.

  Incoming commit `c1d2a2ff` (2026-08-23 18:48:30 -0700) is **frontmatter-only** —
  `git diff <ours> <theirs>` contains no body hunk at all. Per-fact:

  | Fact | Base | Ours (HEAD) | Theirs (incoming) | Resolution |
  |---|---|---|---|---|
  | `fields.story_points` | absent | `3` | `3` (added) | **already present in ours** — incoming change preserved verbatim |
  | `last_field_updated` | `body` | `status` | `status` (changed) | **already present in ours** — incoming change preserved verbatim |
  | trailing newline | present | absent | absent (removed) | **already present in ours** — incoming change preserved verbatim |
  | `status` | `draft` | `bundled` | `free_coding` | same fact, both changed → later-positioned wins → **ours** |
  | `updated_at` | `2026-08-24T01:48:23` | `2026-08-26T17:36:27` | `2026-08-24T01:48:29` | same fact, both changed → later-positioned wins → **ours** |
  | `fields.commits`, `fields.version`, `fields.bundled_in` | absent | added by bundling | never touched (absent on base *and* theirs) | only ours changed → **ours** |

  Ours is a strict superset. Three of the incoming commit's five facts are
  **already byte-identical in ours**; the remaining two are lifecycle fields where
  ours is strictly downstream. `free_coding` → `bundled` is the forward direction
  of the ticket lifecycle, so taking theirs would have rewound this ticket out of
  the very bundle this reconcile run is assembling, and dropped the three bundling
  fields with it — a state loss that recovers nothing, since the incoming facts are
  all retained.

  All three timeline signals agree with the superset: ours' `updated_at`
  (2026-08-26) is later than theirs (2026-08-24); the HEAD-side commit `7a8d0abd`
  (2026-08-31, `seed_local_overlay`) postdates the incoming `c1d2a2ff`
  (2026-08-23); and the enrichment block's own rule ("take the more recent commit
  by timestamp") selects ours.

## Incoming changes preserved

Confirmed. The incoming commit touched one file and no code. Of its frontmatter
edits, `story_points: 3`, `last_field_updated: status`, and the trailing-newline
removal are present verbatim in the staged resolution — they appear in the
base→theirs diff and are absent from the ours→theirs diff, which is exactly the
signature of a change already carried by HEAD. The two facts not taken from
theirs (`status`, `updated_at`) are not discards: both are fields ours advanced
*past* theirs, which is the per-fact timeline rule in 2e operating as intended,
not developer content being overwritten.

No hunk was dropped; the BUG-1301 precedence exception was not invoked, and no
code or UAT files were involved.

**Note for the finalize step:** this resolution nets to *no diff vs HEAD* — the
staged blob is HEAD's blob. This is the redundant-commit case
(BUG-1109/BUG-1122), not a discard: STEP 3's discriminator is satisfied because
the incoming commit's key changes are *present* in HEAD rather than absent. Per
STEP 4 this was staged and exited normally; `--skip` was not called, and the
sequencer state (`CHERRY_PICK_HEAD` = `c1d2a2ff`) is still present for
`cherry_pick_finalize_resolution`.

This is the second consecutive commit in this bundle to resolve this way
(previous: `5af1ff94`, REPORT-3173). Both are `xgd(ticket): update bug
bug-db356ff8` commits from 2026-08-23 whose effect the HEAD-side
`seed_local_overlay` commit already carries.

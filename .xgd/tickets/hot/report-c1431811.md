---
uid: report-c1431811
id: REPORT-3170
type: report
title: 'Reconcile resolve conflicts: reconcile-REQ-162'
created_by: xgd
created_at: '2026-09-01T01:41:14.081332+00:00'
updated_at: '2026-09-01T01:41:14.081332+00:00'
completed_at: null
last_field_updated: created_at
result: pass
fields:
  report_kind: resolve_conflicts
  subject_uid: reconcile-REQ-162
---

## Files resolved

- `.xgd/tickets/hot/request-18a48d63.md` — UU, intent/bookkeeping ticket (rule 2e).
  Conflict was confined to the frontmatter header block (`updated_at`,
  `last_field_updated`, `status`). Resolved to the HEAD side under 2e's
  "one side is a strict superset" clause; staged with `git add --sparse`
  (path is outside the sparse-checkout cone, DOC-986 §2/§4.1).

  Per-fact analysis:
  - `fields.commits` — the incoming commit's entire substantive change
    (BUG-1265 data fix: merge orphaned `working_sha 7ebc721b` into the
    surviving entry's `working_sha_history`, drop the orphan entry).
    Already present verbatim on the HEAD side; not in conflict, no
    decision needed.
  - `status` — HEAD `bundled` vs incoming `ready_to_reconcile`. HEAD wins:
    it is the later lifecycle position, and the HEAD side also carries
    `fields.bundled_in: bundle-b3b7c399` (untouched by incoming), so taking
    the incoming value would have regressed the ticket to a pre-bundle
    state inconsistent with `bundled_in`.
  - `updated_at` / `last_field_updated` — HEAD `2026-08-24T02:10:41` /
    `status` vs incoming `2026-08-24T01:14:03` / `commits`. HEAD is the
    later timestamp; these are write-stamp metadata for the winning fact.
    Enrichment reported intent unknown on one/both sides, so the
    more-recent-commit-by-timestamp rule applied: HEAD-side commit
    `209bea117b` (2026-08-30) vs incoming `6788b08404` (2026-08-23).

## Incoming changes preserved

No incoming change was discarded. The incoming commit `6788b08404`
("xgd(ticket): update request request-18a48d63", BUG-1265 data fix, no
code change) touched exactly two regions of one file:

1. The `fields.commits` data fix — **already present in HEAD**, landed by
   HEAD-side commit `209bea117b` ("seed_local_overlay request
   request-18a48d63"), whose diff contains the byte-identical hunk:
   `+ 7ebc721b83ab6202fdec600cd0493b69964bac39` added to the surviving
   entry's `working_sha_history`, and the orphan
   `- working_sha: 7ebc721b...` entry with `working_sha_history: []`
   removed. This is STEP 3's "present via a different route" case
   (BUG-1109/BUG-1122), not a discard.
2. The `updated_at`/`last_field_updated` write-stamp — bookkeeping for
   change (1), superseded by HEAD's later stamp for the same reason (1)
   is redundant.

Consequently the resolved file is byte-identical to HEAD (`git diff HEAD --
<path>` is empty), so this cherry-pick stages to no net diff. Per STEP 4
this was left for `cherry_pick_finalize_resolution` to skip; `--skip` was
not called and `CHERRY_PICK_HEAD` remains intact.

No code, test, or spec-ticket files were involved; the BUG-1301 precedence
exception was not invoked. `git status --porcelain` shows zero conflict-class
entries.

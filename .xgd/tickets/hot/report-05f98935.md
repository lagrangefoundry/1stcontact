---
uid: report-05f98935
id: REPORT-3453
type: report
title: 'Reconcile resolve conflicts: reconcile-BUNDLE-23'
created_by: xgd
created_at: '2026-09-04T01:28:55.314763+00:00'
updated_at: '2026-09-04T01:28:55.314763+00:00'
completed_at: null
last_field_updated: created_at
result: pass
fields:
  report_kind: resolve_conflicts
  subject_uid: reconcile-BUNDLE-23
---

## Files resolved

- `.xgd/tickets/hot/request-78370159.md` — **UU**, intent/bookkeeping ticket (STEP 2e).
  Sparse-checkout path (conflict existed in the index only, no working-tree
  markers), so resolved with `git checkout --ours` + `git add --sparse`.

  Both sides touched exactly one fact: the ticket's lifecycle status (plus its
  `updated_at` / `last_field_updated` bookkeeping companions).

  - Merge base `f8e0364`: `status: free_coded`, `updated_at 2026-09-01T00:54Z`.
  - Incoming (`0bba66b7`, `xgd(ticket): update request request-78370159`,
    2026-09-01 11:09 -0700): `free_coded` → `ready_to_reconcile`.
  - Ours / HEAD (`9b278972`, `xgd(ticket): seed_local_overlay request
    request-78370159`, 2026-09-02 10:50 -0700): `status: bundled`,
    `updated_at 2026-09-02T17:48Z`, plus a new field
    `fields.bundled_in: bundle-203b1dc2`.

  Same field changed differently on each side → per-fact timeline rule. The
  auto-enrichment classified intent as unknown on one/both sides and prescribed
  "take the more recent commit by timestamp": HEAD is the later commit
  (2026-09-02 vs 2026-09-01). HEAD is also a strict superset for this fact —
  `bundled` is the state *downstream* of `ready_to_reconcile` in the ticket
  lifecycle, and it carries the additional `bundled_in: bundle-203b1dc2` field
  that the incoming side never had. Taking the incoming side would have
  regressed the ticket's operator-owned status from `bundled` back to
  `ready_to_reconcile` and dropped `bundled_in`.

  Nothing outside that frontmatter hunk differed between the two sides — the
  ticket body is identical on both, so no other facts needed composing.

## Incoming changes preserved

No code/implementation files were conflicted in this cherry-pick; the sole
conflict was a bookkeeping ticket.

The incoming commit's only substantive change was advancing this request past
`free_coded` to `ready_to_reconcile`. That intent is present in HEAD via a
later route: HEAD already carries `status: bundled`, i.e. the request was
subsequently reconciled and bundled into `bundle-203b1dc2` — the very bundle
this reconcile run is replaying. This is the redundant case, not a discard:
the incoming key change is realized in HEAD in a superseding form, so STEP 3's
guard is satisfied.

No hunks were dropped under the BUG-1301 precedence exception; none applied.

Net effect: the staged tree is identical to HEAD for this path
(`git diff --cached HEAD` is empty for the file). Per STEP 4 this is left for
`cherry_pick_finalize_resolution` to detect and skip — `--skip` was not called
here, and the cherry-pick sequencer state (CHERRY_PICK_HEAD `0bba66b7`) is
left intact.

Flagged for post-merge review, as the enrichment's unknown-intent rule
requests: confirm `request-78370159` should remain at `status: bundled` with
`bundled_in: bundle-203b1dc2`.

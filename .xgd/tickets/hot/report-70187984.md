---
uid: report-70187984
id: REPORT-3033
type: report
title: 'Reconcile resolve conflicts: reconcile-BUNDLE-22'
created_by: xgd
created_at: '2026-08-31T19:34:57.832342+00:00'
updated_at: '2026-08-31T19:34:57.832342+00:00'
completed_at: null
last_field_updated: created_at
result: pass
fields:
  report_kind: resolve_conflicts
  subject_uid: reconcile-BUNDLE-22
---

## Context

Incoming commit `ced4356a6a` — *feat(schema): a site knows where it is, and both
renderers say so [FREE-CODED]* (REQ-151, authored 2026-08-21 13:29:54) — is a
duplicate of `bbce12ddd421f449812f77a18967b872dbc5debc`, which carries the
**identical subject and identical author date** and is **already an ancestor of
HEAD** (`git merge-base --is-ancestor bbce12dd HEAD` → true). REQ-151 landed on
this branch through the post-watermark sync; HEAD has since built two further
free-coded commits on top of it.

Both sides of every conflict are `free_coded`, so the working-timeline exception
applies: take the later-positioned side per fact. Every HEAD side is later than
the 2026-08-21 incoming, and in each case HEAD is additionally a **strict
superset** of the incoming's content — so the timeline rule and the superset rule
agree, and neither discards developer intent.

The incoming commit touches 8 files; 4 merged clean. `git diff HEAD ced4356a6a`
over those 4 — `packages/framework/src/l1/render.ts`,
`packages/framework/src/modules/behavior.ts`, `packages/site-schema/src/index.ts`,
`tests/test_UAT_FC_REQ-151_site_locale.test.ts` — is **empty**: they are
byte-identical between HEAD and the incoming commit.

## Files resolved

- **`package.json`** — UU, config scalar (2g / version-scalar precedent).
  Resolved to HEAD (`checkout --ours`). Two facts differ:
  - `version`: HEAD `0.2.14` vs incoming `0.2.2`. The incoming commit's *own*
    change here is version bookkeeping only (`git show ced4356a6a -- package.json`
    is the single hunk `0.2.1 -> 0.2.2`). HEAD's side (`97f4e4e5`, 2026-08-24) is
    later on the working timeline and has advanced a full minor series past it;
    taking the incoming would regress the package version.
  - `astro: ^7.0.4` in `devDependencies`: present on the incoming side, absent on
    HEAD. This is **not** the incoming commit's change — it is ambient history.
    `astro` was already in `package.json` at the incoming's timeline position and
    was removed later on the HEAD line by REQ-150, which took Astro off the render
    path. Re-adding it would reintroduce a dependency a later free-coded commit
    deliberately deleted.

- **`packages/site-schema/src/locale.ts`** — AA, both added (2b). Resolved to HEAD
  (`checkout --ours`). HEAD's file is the incoming's 279 lines **plus 89 lines**
  added by `6852ca13` (*feat(schema): reserve locale-shaped page slugs*,
  2026-08-22 15:08, REQ-153): `ISO_639_1_LANGUAGES`, the `LOCALE_SHAPED` regex,
  `isLocaleShapedSlug`, and `localeShapedSlugMessage`. `git diff HEAD ced4356a6a`
  on this path is deletions only — the incoming is a strict subset. 2b's
  "keep the superset" and the timeline rule select the same side.

- **`packages/site-schema/src/schema.ts`** — UU, code (2c). Resolved to HEAD
  (`checkout --ours`). The only region where the two sides differ is REQ-153's
  `pageSchema.slug` refinement: HEAD carries the `superRefine` that refuses
  locale-shaped slugs plus the `isLocaleShapedSlug` / `localeShapedSlugMessage`
  imports; the incoming has the bare `slug: z.string()` that predates it. Same
  commit `6852ca13`, later timeline position. Taking the incoming would delete
  REQ-153 outright.

- **`tools/generate/src/render/render.ts`** — UU, code (2c). Resolved to HEAD
  (`checkout --ours`). One region differs: the `LoadedSite` type import. HEAD
  imports it from `../store/assemble` (where it is declared) with the explanatory
  comment; the incoming imports it from `../store/loadSite`. HEAD's side is
  `94983a2b` (*fix(render): import a type from where it is declared*, 2026-08-22
  17:07, REQ-149) — later, and a deliberate fix: re-exporting through the
  filesystem module put `node:fs` / `node:path` into the Worker's *type* program
  and broke control-app's `tsc` build. Taking the incoming would reintroduce that
  build failure.

## Incoming changes preserved

Every one of the incoming commit's key changes is present in the resolved tree,
via `bbce12dd`:

- **`packages/site-schema/src/schema.ts`** — the incoming adds 63 lines (the four
  optional `siteConfigSchema` fields: `country`, `locale`, `currency`,
  `timezone`). `git diff HEAD ced4356a6a` on this file shows *only* the REQ-153
  slug hunk, which means all 63 of those lines are byte-identical on both sides
  and therefore present in the resolution.
- **`tools/generate/src/render/render.ts`** — the incoming changes 28 lines
  (`resolveSiteLocale` import, the `locale` parameter on `renderModuleInstances`,
  the `Component({…, locale})` prop). The HEAD↔incoming diff shows *only* the
  `LoadedSite` import line, so all of REQ-151's locale threading is present.
- **`packages/site-schema/src/locale.ts`** — all 281 incoming lines are present;
  HEAD adds 89 more on top.
- **`packages/site-schema/src/index.ts`**, **`packages/framework/src/l1/render.ts`**,
  **`packages/framework/src/modules/behavior.ts`** — merged clean and verified
  byte-identical to the incoming commit.
- **`tests/test_UAT_FC_REQ-151_site_locale.test.ts`** — merged clean and
  byte-identical to the incoming; present in HEAD with all 9 `test_UAT_*`
  functions intact. **No UAT function was deleted on either side**, so 2f is not
  engaged and the BUG-1301 precedence exception was not needed anywhere in this
  resolution.
- **`package.json`** — the incoming's only change was the `0.2.1 -> 0.2.2` version
  bump, deliberately not taken per the both-`free_coded` timeline rule above. A
  bookkeeping scalar, not developer code; no functional incoming change discarded.

## Net result

`git diff --cached HEAD` is **empty**, and `git status --porcelain` shows no
remaining conflict classes (only the six pre-existing untracked
`.xgd/tickets/hot/` files from earlier passes, which are not conflicts and were
left alone).

This is the redundant-commit case (STEP 4 / BUG-1109/BUG-1122), **not** a discard.
STEP 3's distinguishing check passes affirmatively: the incoming commit's changes
are verifiably *present* in HEAD via `bbce12dd`, not absent. I did not call
`--skip`; `CHERRY_PICK_HEAD` remains at
`ced4356a6a0fb88f2fb4f71c6d47060e65881499` for `cherry_pick_finalize_resolution`.

---
uid: report-e2775532
id: REPORT-4435
type: report
title: 'Reconcile resolve conflicts: reconcile-BUNDLE-27'
created_by: xgd
created_at: '2026-09-19T12:59:14.044823+00:00'
updated_at: '2026-09-19T12:59:14.044823+00:00'
completed_at: null
last_field_updated: created_at
result: pass
fields:
  report_kind: resolve_conflicts
  subject_uid: reconcile-BUNDLE-27
---

## Files resolved

- `.xgd/tickets/hot/request-8b33a146.md` — class **UU** (index-only; path is
  outside the sparse-checkout cone, so no working-tree markers existed).
  Rule applied: **2e (intent/bookkeeping ticket)**, per-fact, resolved to the
  later-positioned side. Staged with `git checkout --ours` + `git add --sparse`.

  The conflict is confined to YAML frontmatter; all three stages have a
  byte-identical body. Per-fact breakdown:

  | fact | base | ours (HEAD) | theirs (incoming) | taken |
  |---|---|---|---|---|
  | `status` | `free_coded` | `bundled` | `ready_to_reconcile` | ours |
  | `updated_at` | `2026-09-01T21:34:31` | `2026-09-16T01:48:39` | `2026-09-01T21:43:09` | ours |
  | `completed_at` | `null` | `2026-09-14T10:29:05` | `null` (untouched) | ours |
  | `fields.bundled_in` | absent | `bundle-8e1807f6` | absent (untouched) | ours |

  Both sides changed the same two facts (`status`, `updated_at`), so the
  timeline rule applies. The enrichment block reported intent unknown on one
  or both sides and directed "take the more recent commit by timestamp":

  - ours   — `ee0d0b9866` `xgd(ticket): seed_local_overlay request request-8b33a146`, 2026-09-17 13:23:47 -0700
  - theirs — `fc7b846d79` `xgd(ticket): update request request-8b33a146`,             2026-09-01 14:43:09 -0700

  Ours is 16 days later, so ours wins both contested facts. The remaining two
  facts (`completed_at`, `fields.bundled_in`) are ours-only additions that
  theirs never touched, so there is nothing to compose from the incoming side
  — taking ours loses no incoming field. No content was invented; the result
  is exactly the ours blob `c66d9bfb5e`.

  Substantively this is the same conclusion by a second route: the two sides
  are successive positions on one lifecycle, not competing edits. Incoming
  advances `free_coded → ready_to_reconcile`; ours has already carried the
  same ticket past that point to `bundled`, stamping `completed_at` and
  recording membership in this very bundle (`bundle-8e1807f6`). Taking
  incoming would regress the status two steps and drop the bundle membership.

## Incoming changes preserved

No code/implementation files were in conflict — the sole conflicted file is a
bookkeeping ticket under `.xgd/tickets/hot/`, so STEP 3's code-preservation
check does not bite, and no BUG-1301 precedence exception was invoked.

For completeness, on the one conflicted file the incoming commit's entire diff
is the status/`updated_at` pair shown above. Its intent — move this request off
`free_coded` and onward through the reconcile lifecycle — is present in HEAD
via a later route: HEAD sits at `bundled`, which is downstream of the
`ready_to_reconcile` that incoming was setting. This is STEP 4's *redundant*
case, not STEP 3's *discarded* case: the incoming change is absent as literal
text but present in effect, superseded by a strictly later position on the same
lifecycle.

Consequently the staged tree is byte-identical to HEAD and this cherry-pick
will finalize empty. Per STEP 4 I did not call `--skip`; the finalize step
detects the clean staged diff. Conflict stages are gone
(`git ls-files -u` empty, `git status --porcelain` empty) and `CHERRY_PICK_HEAD`
is intact at `fc7b846d79`. No sequencer-advancing command was run.

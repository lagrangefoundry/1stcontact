---
uid: report-84714675
id: REPORT-1973
type: report
title: 'Code Review: request-1ff09fab (REQ-138 — copy modal live preview)'
created_by: xgd
created_at: '2026-08-13T02:14:27.129656+00:00'
updated_at: '2026-08-13T02:14:27.129656+00:00'
completed_at: null
last_field_updated: created_at
result: pass
fields:
  report_kind: code_review
  subject_uid: request-1ff09fab
  anchor_uid: request-1ff09fab
---

# Code Review

**Result**: PASS

## Summary

The critical defect the previous review failed on is fixed, and the fix is proven
where it was previously disproven: capitalisation now reaches the words the
operator is editing, measured on `.builder-modal__box .fields-control` in a real
Chromium, and the UAT that used to pin the defect as intended behaviour now
asserts the opposite in both directions (`uppercase` arrives, `none` clears).
`page-style.js` and `editor.js` were left untouched as directed — the one-line
UA-reset gap in `builder.css` was the whole of it.

One item from the previous Fix-It prompt is outstanding and is NOT a code
defect: AC-1138 and story-3bf94bd4 still describe capitalisation as a recorded
divergence, which the code and its evidence now contradict. The fix loop is
forbidden from mutating the matrix and correctly declined, recording the verbatim
rewrite on both tickets instead. That is an operator/matrix-loop action, flagged
below as blocking-for-merge; failing the code review over it would spend the
remaining fix iterations against a prohibition and fix nothing.

## Quality Gates

| Gate | Status | Evidence |
|------|--------|----------|
| Lint | success (0 errors, 0 warnings) | report-13085166 |
| Build | success | report-13085166 |
| Tests | **not exercised by the gate** — run directly instead | report-13085166 `suites: {}` |
| Coverage | n/a (gate ran no suites) | — |

The gate proves less than it appears to (`suites: {}`, lint 0.0001s, build 0.0s),
so tests were run directly on this worktree:

- `tests/test_UAT_FC_REQ-138_live_preview.test.ts` — 6/6 pass
- `tests/reconciliation-copy-edit-live-preview.test.ts` — 3/3 pass, and the
  evidence is genuinely present rather than degraded: `WEBUI_INSTALLED === true`
  (verified independently), no `NOT VERIFIED` warning emitted, and the browser
  halves really ran (AC-1138 1775ms, AC-1140 1070ms)
- Editor/modal regression scope — `reconciliation-copy-edit-{form-presentation,
  parameter-sheet,typography,write-path}`, `req117-copy-editing`,
  `test_UAT_FC_REQ-135_text_properties`: 50/50 pass
- Full suite — **1544 passed, 13 failed, 4 skipped** across 218 files. The 13 are
  the pre-existing API-key failures in `reconciliation-assistant-conversation`,
  `test_UAT_FC_REQ-122_chat_host` and `test_UAT_FC_REQ-127_session_binding`
  (confirmed: `expect(opened.error).toContain('ANTHROPIC_API_KEY')`), and none of
  those three suites imports any file this change touches.

## External Interface Accessibility

Wired in — and the one gap the previous review found is closed.

| Symbol | Wiring | Evidence |
|--------|--------|----------|
| `previewScale` | exported, imported, called at modal open | `page-style.js:50`, `editor.js:4`, `editor.js:387` |
| `previewSizePx` | exported, called from the `fontSizePx` row | `page-style.js:72`, `page-style.js:96` |
| `previewVarFor` | exported, called on every `change` | `page-style.js:111`, `editor.js:389` |
| `properties.on('change')` | real component API — `EVENTS` includes `change`, payload is `{name, value}`, buffered mode emits it without calling `onCommit` | `webui-fields/src/index.js:18,66,265` |
| listener lifecycle | `destroy()` clears every listener array; `close()` calls `properties.destroy()` | `webui-fields/src/index.js:403-405`, `editor.js:250` |
| `fontSizePx` / `fontWeight` / `italic` | reach the words via `--fields-font-size` and `font: inherit` | `builder.css:252,256,257` |
| `textTransform` | **now reaches the words** | `builder.css:280-283` |

`--preview-text-transform` was landing on `.builder-modal__box`, whose only text
descendant is the component's `.fields-control`; `font: inherit` does not carry
`text-transform` and the UA stylesheet resets it on form controls. The new rule
re-declares that inheritance, scoped to the editing box.

## Code Quality

| File | Finding | Severity |
|------|---------|----------|
| `builder.css:280-283` | The fix, scoped to `.builder-modal__box`, applied exactly as directed | None (correct) |
| `builder.css:214-232` | The block comment claiming "nothing here targets a component class" was amended rather than left contradicting the new rule, and the amendment is honest about why naming the class is safe here (AC-1138 measures it in a browser, so a rename fails loudly) | None (good) |
| `builder.css:282` | `letter-spacing: inherit` is the previous review's optional warning, applied. Same UA reset one line apart; no test pinned it as not-arriving and none broke. It is a small behaviour change (the box now mirrors a tracked headline's tracking) landing with no criterion of its own | Warning — see below |
| `page-style.js:35-113` | `previewScale` / `previewSizePx` / `PREVIEW_PARAMETERS` unchanged from the previous review, where they were assessed clean: well-bounded, degrade to scale 1 on missing ends, reuse `PREVIEW_MIN_PX` rather than a magic number, and the table makes colour's absence legible rather than accidental | None (good) |
| `editor.js:243-246, 372-392` | `box` hoisted with the documented TDZ reasoning; `if (box)` is a real guard (a background segment has parameter fields and no box); scale measured once at open, so the subscription is O(1) per change; buffered commit untouched, so nothing posts | None (good) |
| `editor.js:387` | `spec.values.fontSizePx` is the one field name hardcoded outside the mapping table — acceptable, scale is inherently size-specific | Nit |
| tests | No mocking of internal components in either new suite; the modal is reached through `mountEditor`, both forms are the installed `webui-fields`, reads go over HTTP to a real builder origin | None (good) |
| both files | No debug code, no commented-out blocks, no TODO stubs, no duplicated helper | None |

Architecture: control-app builder chrome only. No raw-CSS hole in the
site-definition path, no instance data written, no origin reached. The written
values are a closed table of typed scalars set as CSS custom properties on a
dialog element — nothing touches L1, the validator, the renderer or the write
path (DOC-2 unaffected).

## Checklist Compliance

No `architecture_checklist`, `security_checklist` or `design_checklist` report
tickets exist (0 items returned for each). Sections omitted.

## Smoke Test

The user-facing entry point is the copy-edit modal, and it is driven for real in
Chromium by `reconciliation-copy-edit-live-preview.test.ts` over a real
`1c render --edit` page, through the gestures a user makes. Measured on the words
themselves, all four parameters now move them:

- weight 700 to 400: drawn lighter — pass
- italic ticked, then unticked: drawn italic, then upright — pass
- size 72 to 120: drawn bigger (scaled, not re-clamped to 32px) — pass
- capitalisation to `uppercase`, then back to `none`: **drawn uppercase, then
  cleared** — pass (this was the failing gesture)

No stack traces, no crashes.

## Issues Found

**Critical (must fix)**: none in the code.

**Blocking for merge — operator / matrix-loop action, not a code defect**:

- **AC-1138 (`acceptance_criterion-2d587432`) and story-3bf94bd4 are now false.**
  The criterion is titled "Size, weight and italic..." (three parameters), its
  body states "Capitalisation is written but does not arrive, and that is a
  recorded divergence from what was asked for", and its Verification section
  instructs a reader to "assert the words' own capitalisation is **unchanged**"
  — the exact opposite of what the code does and what
  `test_UAT_AC1138_...:513` now asserts. story-3bf94bd4 carries the same framing
  at body lines ~98-101, ~236-242 and ~257.

  Merging as-is writes a false claim into the capability matrix and leaves
  verification instructions that would drive a future regression or
  reconciliation session to re-assert the defect. The `fix_review_free_coded`
  loop is forbidden from mutating stories/capabilities/ACs and correctly declined
  (report-bb5eb085, "Not applied: Fix-It steps 3 and 4"), recording the verbatim
  rewrite on each ticket: **comment-08144f06** on the AC, **comment-9679cc1a** on
  the story. Applying those two comments is all that is required, and it must
  happen before or at merge.

**Warnings (should fix)**:

- `letter-spacing: inherit` (`builder.css:282`) is a correct fix to a real
  mirroring gap but sits outside REQ-138's declared scope and lands with no
  criterion or assertion of its own. Worth one line in the matrix so it is not
  invisible; not a condition of passing.
- The quality gate is a no-op for this project — every scoped quality report in
  this run records `suites: {}` with lint at ~0.0001s and build at 0.0s, so no
  test, lint or build command actually executes. Every review downstream inherits
  the blind spot and has to run the suite by hand, as this one did. Not a REQ-138
  finding; raised for the operator.

---
uid: report-25b51548
id: REPORT-3818
type: report
title: 'Capability-Intent Alignment: In-Page Copy Editing: The Editable Render & The
  Click-to-Edit Gesture (level=ac)'
created_by: xgd
created_at: '2026-09-10T23:53:07.231733+00:00'
updated_at: '2026-09-10T23:53:07.231733+00:00'
completed_at: null
last_field_updated: created_at
result: pass
fields:
  report_kind: capability_validation
  subject_uid: capability-12fee326
  level: ac
  violations: 0
  warnings: 0
  needs_review_count: 0
---

# Capability-Intent Alignment: In-Page Copy Editing: The Editable Render & The Click-to-Edit Gesture
# Level: ac

**Result**: PASS
**Violations**: 0
**Warnings**: 0
**Needs review**: 0

Attempt 7. Two stories, both `story_kind: upgrade`, **54 ACs** in scope:

- **STORY-98** (`story-af36c2cb`) — the edit render channel, 14 ACs. Re-verified
  bullet by bullet against the body; no finding.
- **STORY-101** (`story-3bf94bd4`) — the click-to-edit gesture, 40 ACs. All five
  edits attempt 6 applied are present, correct, and have not falsified a
  neighbour. No new violation survives.

**The one violation and three warnings of report-efb2ed3c are all closed.**
Attempt 6 (report-c0b1dda7, 2026-09-10T23:37Z) applied V1 to AC-997, W1 to
AC-1123, W2 to AC-1000 + AC-1043, and W3 to STORY-101's body. I re-read all five
bodies from the store and re-derived the code facts they turn on from
`packages/site-schema/src/l1/edit.ts` rather than adopting the citations. Every
repair checks out. See findings 1–3.

## Cumulative Intent Considered

Story level passed 2026-09-10T23:07Z (report-1321eb22, 0 violations). Per the
level cascade the story bodies are my working reference; I escalated to intent
history only where an AC contradicted its body — which, this cycle, nothing did.
No AC or story body under this capability cites a ticket by ID at all (grepped
all 54 AC bodies for `REQ-`/`BUG-`/`STORY-` citations — zero hits), so **Step
2.5's named-abandoned-vehicle case does not arise anywhere in this capability.**

Every status and date below was **re-read from the store this cycle**, not
adopted. Doing so corrected two errors that had been carried through the
previous reports' ledgers unchallenged — see the two ✱ rows.

| Intent ID | UID | Status | When | Asked / changed | Counts? |
|---|---|---|---|---|---|
| REQ-116 | `request-41796766` | free_and_reconciled | 2026-07-31 ✱ | The edit render: non-functional channel, derived segments. Origin of STORY-98 | YES |
| REQ-117 | `request-395b67e6` | free_and_reconciled | 2026-07-31 ✱ | Copy editing end-to-end: click segment → fields modal → validated diff → re-render. Origin of STORY-101 | YES |
| REQ-118 | `request-66e4c630` | free_and_reconciled | 2026-07-31 ✱ | Image selection through the same loop | YES |
| REQ-121 | `request-9707484c` | free_and_reconciled | 2026-08-07 | The copy-edit modal made elegant: themed chrome, app typeface, dressed box | YES |
| REQ-128 | `request-de67e1a1` | free_and_reconciled | 2026-08-08 | A painted panel's background image reaches the dialog | YES |
| **REQ-132** | `request-5946d045` ✱ | free_and_reconciled | 2026-08-12 | Page editor: image picker shows thumbnails with file names | YES |
| **REQ-133** | `request-8467b1a3` ✱ | free_and_reconciled | 2026-08-12 | Palette popup: display, pick and edit the site's colours — the surface the colour row opens | YES |
| REQ-135 | `request-a8ccd0dd` | free_and_reconciled | 2026-08-12 | Page editor: text properties — colour, size, weight, italic | YES |
| REQ-136 | `request-8a132869` | free_and_reconciled | 2026-08-12 | Image editor: non-destructive framing and colour adjustment — parameters beside picker and alt text | YES |
| REQ-138 | `request-1ff09fab` | free_and_reconciled | 2026-08-12 | Copy modal: parameter changes preview live in the editing box | YES |
| REQ-139 | `request-3f57cd0c` | free_and_reconciled | 2026-08-12 | Lock controls that cannot express what the element holds | YES |
| REQ-140 | `request-3c0fec69` | free_and_reconciled | 2026-08-15 | Page editor: colour — text colour and panel background from the palette | YES |
| BUG-34 | `bug-13082cb4` | free_and_reconciled | 2026-08-12 | Copy modal: gradient-filled text previews as invisible | YES |
| BUG-35 | `bug-1bde3bf9` | free_and_reconciled | 2026-08-13 | Copy modal: capitalisation never previews — UA reset blocks inheritance | YES |
| REQ-145 / REQ-147 / BUG-37 | — | free_and_reconciled | 2026-08-15 → 08-31 | Request-time edit render at the builder origin | YES (context) |
| REQ-134 | `request-ba3e3fba` | abandoned | 2026-08-12 | An image generation component | NO |
| REQ-154 | `request-b88b79fe` | bundled | 2026-08-20 | Browser Rendering driver | imminent, no ask here |

Carrier bundles, all `free_and_reconciled`: BUNDLE-16 (`bundle-15c1f647`,
STORY-101's `intent_uid`), BUNDLE-19 (`bundle-77b28def`, its `updated_by`),
BUNDLE-14 (`bundle-0385746c`, STORY-98's `intent_uid`).

✱ **Ledger corrections made this cycle.** (1) REQ-116/117/118 were dated
2026-08-06/08-07 in the adopted ledger; the store says all three were created
2026-07-31. (2) The adopted ledger listed `request-8467b1a3` and
`request-5946d045` as one REQ-132 row. They are two different intents:
`request-5946d045` is REQ-132 (the thumbnail picker) and `request-8467b1a3` is
**REQ-133** (the palette popup) — the surface AC-1279 and AC-1281 depend on.
Neither correction changes any finding, but the conflation had been reproduced
across at least three reports and would have hidden REQ-133 from a future
coverage check.

## Alignment Ledger

| Element | Intents aligned to | Outcome |
|---|---|---|
| STORY-98's 14 ACs (AC-948…AC-958, AC-1007, AC-1008, AC-1135) | REQ-116; REQ-117; REQ-136 (paint parity) | **aligned** — re-derived independently against the body. Every in-scope bullet has exactly one covering AC: third channel / render mode (AC-958), deliberate inertness (AC-948), settled state (AC-949, AC-950), derived segmentation (AC-951), addresses (AC-953, AC-954, AC-955), page stamp (AC-1007), the module's seam marker as a **catalog-wide obligation** (AC-954 ¶3, verification quantified "for each module in the catalog that exposes a presentation seam"), renderer-drawn outlines + hover (AC-952), published vocabulary (AC-1008), no leakage (AC-956), one emitter / paint parity (AC-1135), plus AC-957 for the element identifier from Technical Context. The per-request render named in the "third channel" bullet is explicitly deferred to the workspace capability by the same story's out-of-scope list, so it is not an ac-level gap |
| **AC-997** (`acceptance_criterion-e2413484`) | REQ-117, REQ-118, REQ-136 | **repaired and verified — finding 1.** The closed "two controls" enumeration is gone, replaced by the derivation, and the Verification's image leg now carries the framing-parameter step |
| **AC-1123** (`acceptance_criterion-35907074`) | REQ-135, REQ-136, REQ-140 | **repaired and verified — finding 2.** Criterion and Verification now agree about where a closed image list lands |
| **AC-1000** (`acceptance_criterion-43e5a016`), **AC-1043** (`acceptance_criterion-8acf277e`) | REQ-118, REQ-132, REQ-140 | **repaired and verified — finding 3.** Both now describe a witness region that exists |
| **STORY-101 body** | REQ-121 | **repaired — finding 4.** The dialog-chrome bullet is present (¶ "The dialog is dressed as the workspace's own"), so AC-1037 and AC-1038 now derive from the body. The W3 carried since report-657d1e9a is closed, and neither AC was touched — which is what the previous reports insisted on |
| AC-1050 (`acceptance_criterion-170a171f`) | REQ-128, REQ-140 | aligned, **and still do not narrow it** — finding 6 |
| AC-1279, AC-1280, AC-1281 | REQ-140, REQ-133 | aligned — colour row, read-only panel-behind-the-words row with save-then-navigate, empty-palette state |
| AC-1282, AC-1283 | REQ-139 | aligned — the two halves of the lock rule, over both control families |
| AC-1138, AC-1139, AC-1140, AC-1143, AC-1284, AC-1040, AC-1042 | REQ-138, REQ-135, BUG-34, BUG-35 | aligned — opening dressing, the scale rule, the untouched-axis rule, glyph paint, tracking |
| AC-1112…AC-1116 | REQ-132 | aligned — grid presentation, file-name label + tooltip, real bytes, placeholder tile, keyboard group |
| AC-1028 (`acceptance_criterion-26ffac6d`) | REQ-118, REQ-136 | aligned — states what an image region exposes by derivation and defers routing to AC-1123 |
| AC-1037, AC-1038, AC-1041, AC-1039, AC-1044 | REQ-121 items 1–6 | aligned, **and now derivable from the body** |
| AC-993…AC-996, AC-998…AC-1006 | REQ-117 | aligned — hover, innermost resolution, seam scoping, save / refuse / the two dead ends / stale rendering / viewing-is-not-editing / one address implementation. AC-994 carries one clause worth recording rather than editing — finding 5 |
| STORY-101 coverage vs body | — | **complete.** All sixteen in-scope bullets, including the newly added dialog-chrome bullet, have covering ACs; the out-of-scope list is consistent with what the AC set does not claim |
| STORY-101 exclusivity | — | **no duplicates.** The one-Save-one-change invariant is asserted by AC-997, AC-1000, AC-1123 and AC-1279 over four *different* control compositions, which is a distinction not a duplication; AC-1042 vs AC-1139 split cleanly on opening-dressing vs live change, each explicitly delimiting the other; AC-1116 vs AC-1044 overlap only on where the keyboard lands and assert different criteria (grid accessibility vs the auto-open scope) |

## Findings

| # | Severity | Property | Element | Resolution category | Issue | Suggested edit |
|---|---|---|---|---|---|---|
| 1 | info | consistency | AC-997 (`acceptance_criterion-e2413484`) | — | **report-efb2ed3c V1 is closed. Do not re-open.** The criterion no longer says "two controls": it now states the merge holds "across **however many controls the region's fields were spread across** — that count is derived from what the region exposes, never written down here", then illustrates with all three of an image region's controls (grid + editing box + parameter sheet) and cites AC-1123 for the routing rule instead of restating it. The Verification's image leg gained the missing step — alter thumbnail, alt text **and one framing parameter in the sheet**, assert one change and one re-rendering. The *only-what-was-touched* paragraph and its picker-re-report clause survive with the single forced change ("either control" → "any control"). Re-derived from source, not adopted: `copyFieldsOf`'s image branch (`packages/site-schema/src/l1/edit.ts:987-1005`) returns `src` (`type: 'enum'`, `format: 'image'`), `alt`, then `...framing.fields`, and `imageFramingFields` supplies the framing set — three routes, exactly as the AC now says | none |
| 2 | info | consistency | AC-1123 (`acceptance_criterion-35907074`) | — | **report-efb2ed3c W1 is closed.** The routing enumeration now carves the image-options descriptor out and defers the grid to AC-1112: "a field whose descriptor declares that its options are images is drawn as the grid this dialog draws itself (AC-1112); every other shape a region can expose … is drawn in the sheet." Criterion and Verification agree again. Note for future sweeps: the enumeration still lists a colour as landing "in the sheet" while STORY-101's Technical Context describes **four** controls with the colour row as its own. This is not drift — the colour row *sits inside the sheet* (STORY-101: "a **row in the parameter sheet**"; "It sits **first in the sheet**"; AC-1279: "The row is in the parameter sheet beneath the words"), and AC-1123's own next paragraph names the two drawing controls explicitly. Location and drawer are different questions and the AC answers both correctly | none |
| 3 | info | consistency | AC-1000 (`acceptance_criterion-43e5a016`) + AC-1043 (`acceptance_criterion-8acf277e`) | — | **report-efb2ed3c W2 is closed.** Both now name a witness region that exists: "a painted backdrop, whose controls are the thumbnail grid for its background image and the colour row for its fill" (AC-1000) and "a painted backdrop, whose controls are its thumbnail grid and its colour row" (AC-1043), with the untouched-confirm key restated as "no tile chosen and no colour picked" and the Verifications following. Confirmed against source: for a painted `box`/`container` (`edit.ts:1022-1046`) the derivation pushes `backgroundImageUrl` only where `backgroundHandleOf(node) !== undefined`, then **always** pushes `fill` — so no node kind yields an image field alone and "all thumbnails" described nothing. AC-1000's universal opening sentence and AC-1043's absence-of-an-editing-surface key are untouched, as instructed | none |
| 4 | info | coverage | STORY-101 (`story-3bf94bd4`) + AC-1037 (`acceptance_criterion-279f1f6d`) + AC-1038 (`acceptance_criterion-4e320828`) | — | **report-efb2ed3c W3 / report-1321eb22 finding 2 is closed at the body, not at the ACs.** STORY-101's in-scope list now carries "**The dialog is dressed as the workspace's own**" — themed subtree and palette resolution with no hardcoded stand-ins, a theme switch reaching an open form, one application typeface applied once at the themed root, the family as an application constant, self-hosted faces, Cancel/Save reading as the workspace's own controls, and the declared refusal-colour exception. AC-1037 and AC-1038 were correctly left alone. One sub-clause of the new bullet — "Cancel and Save read as the workspace's own controls" — has no AC of its own; it is covered in substance by AC-1037's chrome claim ("the panel, its border, its radius, its surface and its text all come from the theme's own values") and its Verification, and the clause was derived *from* AC-1037/AC-1038 rather than from new intent. **Recorded so a later cycle does not open an `ac-add` for it** | none |
| 5 | info | consistency | AC-994 (`acceptance_criterion-ce71a033`) | — | **Recorded, deliberately not raised — do not edit this AC.** Its second paragraph reads "The form is built from typed controls supplied by the shared component set and is confirmed as a whole (buffered), not field by field." Since REQ-140 a copy region's dialog holds three controls, one of which — the colour row — is drawn by the dialog itself precisely because the shared component's value is a hex string and this surface writes a palette reference. So read as a claim about the whole dialog the sentence is imprecise. It is nevertheless **not ac-level drift**: (a) it mirrors STORY-101's own Technical Context ("**The form is a shared component, not hand-rolled.** The intent is explicit that typed controls and the confirm/cancel model come from the shared UI component set"), which is the working reference at this level; (b) its Verification — "assert its form is the shared component's form (not a hand-built one) in whole-form confirm mode" — is true of the editing box and is what the test reads; (c) the composition claim is owned, correctly and in three places, by AC-997, AC-1123 and AC-1282. Editing AC-994 would restate a claim two other ACs already own and risks the neighbour-falsification the previous cycles kept warning about. If it is ever tightened, it belongs at **story level** as one clause on the body's Technical Context, not here | none |
| 6 | info | consistency | AC-1050 (`acceptance_criterion-170a171f`) | — | **Do not narrow this AC**, carried forward from report-efb2ed3c finding 8 and re-verified. Its closed enumeration — "it is the picker where there is one, the colour row, the footer, and nothing else" — is **exactly right**, not a stale one: `edit.ts:1022-1046` returns precisely `backgroundImageUrl` (conditional) and `surfaceFill` for a painted box/container, and nothing else. It is the one closed enumeration under this story that a sweep should leave alone | none |
| 7 | info | — | evidence execution | — | **No UAT was executed for this report and none of it rests on one.** The covering suites cannot run in this sandbox — `startBuilder` raises `listen EPERM: operation not permitted 0.0.0.0` (`tools/generate/src/cli/builder.ts:362-363`) from each suite's `beforeAll`, which is the sandbox refusing to bind a socket rather than a failing assertion (report-efb2ed3c finding 7, unchanged). Every code fact cited above was verified by reading `packages/site-schema/src/l1/edit.ts` directly | none |
| 8 | info | coverage | AC-997 (`acceptance_criterion-e2413484`) | — | **Handoff to the uat level, not actionable here.** AC-997's Verification now asks for a step its covering UAT does not perform: the three-way merge (thumbnail + alt text + one framing parameter → one change). `tests/reconciliation-copy-edit-image-picker.test.ts:657-693` still stops at thumbnail + alt text. This is the coverage consequence of closing V1 and it belongs to `check_uat_coverage` / `fix_uat_coverage`. Its `uat_coverage` field is deliberately left at `pass` here — that field is owned by the uat cycle, and setting it from a structural-validation pass would manufacture a verdict the uat level is about to re-derive | none |

## Notes for the Editor

- **Nothing to do at this level.** Zero violations, zero warnings, zero
  needs_review. Every finding above is an `info` ledger entry; four of them
  (1–4) record that a previously-open finding is closed, and three (4, 5, 6)
  exist specifically to stop a later cycle from re-deriving a non-issue or
  "repairing" a correct AC.

- **The closed-enumeration drift shape appears to be exhausted.** It recurred six
  times across attempts 1–6 and every instance is now repaired or affirmatively
  cleared. I re-read all 54 AC bodies and swept them for the shape independently
  of the previous sweeps (`nothing else`, `and no more`, `only its`, `two
  controls`, `both controls`, `exactly the fields`, `no other control`, plus
  `shared component`/`typed controls`). Seven hits; six are correct as written
  (AC-997 as repaired, AC-1000 and AC-1043 as repaired, AC-1041's "only the face
  declarations cross", AC-1050 — finding 6, AC-1112's "two controls answering the
  same question", which is about the removed dropdown), and the seventh is
  AC-994, recorded as finding 5 with a reasoned instruction not to edit it. The
  standing prescription still holds for anything new: **an AC that enumerates a
  region's fields or a dialog's controls should name the derivation, not copy its
  output.**

- **The provenance exposure is unchanged and still worth stating.** No AC under
  this capability carries an `intent_uid` or `updated_by` of its own, so an AC's
  alignment to an intent is always inferred through its story and nothing in the
  ticket graph links an AC to the intent that superseded it. That is the
  structural reason this level needed seven attempts. It is a matrix-shape
  observation, not a finding against this capability.

- **STORY-98 needed nothing again**, and was re-verified independently rather
  than adopted: all 14 ACs re-read against the body, every in-scope bullet
  covered exactly once, no overlap.

- **Two ACs carry `uat_coverage: fail` (AC-1039, AC-1138) and six carry no value
  at all (AC-1143, AC-1279…AC-1284).** That is the uat level's business and does
  not gate this one; noted only so the next cycle knows the ac level passed with
  those outstanding.

- **Environment note — the `xgd` CLI was down for part of this cycle and this
  report was filed through a shim.** Every `xgd` subcommand began failing
  mid-session with `CONFIGURATION_ERROR: [Errno 1] Operation not permitted:
  <...>/main/.xgd/config.yaml`. The file is readable; the EPERM is on a **write**
  — `validate_config_file()` (`xgd_source/config/validator.py:236`) opens main's
  `config.yaml` for writing whenever the packaged template holds a key the live
  config lacks, here `dashboard.prompts.reread_extra`, and a branch worktree may
  not write anything under `main/.xgd` except `_locks`. Report creation therefore
  ran through `.xgd/tmp/xgd_nowrite.py`, which replaces that one function with
  its own body minus the write-back and then calls the real CLI `main()`; the
  in-memory config is what the CLI would have used had the write succeeded, and
  no other code path is patched. **This is an environment defect, not a matrix
  one, and it will block every workflow in every branch worktree until someone
  with write access to main adds the missing key** (via `xgd quality config` or
  the dashboard — not by hand-editing `.xgd/config.yaml`, per the coding
  standards).

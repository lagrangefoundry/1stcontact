---
uid: report-a3131846
id: REPORT-4428
type: report
title: 'Reconcile resolve conflicts: reconcile-BUNDLE-27'
created_by: xgd
created_at: '2026-09-19T12:42:04.632147+00:00'
updated_at: '2026-09-19T12:42:04.632147+00:00'
completed_at: null
last_field_updated: created_at
result: pass
fields:
  report_kind: resolve_conflicts
  subject_uid: reconcile-BUNDLE-27
---

## Files resolved

- `.xgd/tickets/hot/request-8b33a146.md` — **UU**, index-only (path is outside
  the sparse-checkout cone, so there were no working-tree markers; resolved via
  `git checkout --ours` + `git add --sparse`). Class **2e** (intent/bookkeeping
  ticket), per-fact timeline rule → **ours** for every conflicting fact.

  Incoming commit `e558098f` (`xgd(ticket): update request request-8b33a146`,
  authored 2026-09-01T21:20:09) changes exactly four facts: `updated_at`
  21:16:28 → 21:20:09, `last_field_updated` title → status, `status`
  draft → free_coding, and removal of the trailing newline.

  Ours (`ee0d0b9866`, `xgd(ticket): seed_local_overlay`, 2026-09-17) carries the
  same ticket at `updated_at` 2026-09-16T01:48:39 with `completed_at`
  2026-09-14T10:29:05, `status: bundled`, `fields.version: 0.2.37`,
  `fields.bundled_in: bundle-8e1807f6`, a two-entry `fields.commits` list
  (`eb6655c3`, `324b55bf`), and the post-development body.

  Every fact the incoming side touches is also touched by ours, with a
  later-positioned value; `last_field_updated: status` and the trailing-newline
  removal are byte-identical on both sides. There is no field, section or
  paragraph present on the incoming side and absent from ours — the ours→theirs
  diff is purely subtractive. Taking incoming would have reverted the ticket
  from `bundled` back to `free_coding`, cleared `completed_at`, dropped
  `commits`/`version`/`bundled_in`, and reverted the body to its pre-development
  form. No content was invented, and no `intent_uid`/`story_uid`/
  `capability_uid` field was touched.

## Incoming changes preserved

No code or implementation files were in this conflict — the single conflicted
path is a bookkeeping (request) ticket, resolved under 2e.

The incoming commit's intent **is present in HEAD via a later route**, not
discarded: `e558098f` advances this request's own lifecycle `draft →
free_coding`, and HEAD already holds the same ticket at `status: bundled` with
`completed_at` set, `version: 0.2.37` and `bundled_in: bundle-8e1807f6` — i.e.
the free-coding run that commit was opening has since completed and been
bundled into this very bundle (bundle-8e1807f6). `free_coding` is upstream of
`bundled` in the same ticket's lifecycle, so re-applying it would move the
ticket backwards.

This is the BUG-1109/BUG-1122 shape, not STEP 3's discard case: the resolution
nets to no diff vs HEAD (`git diff --cached HEAD` is empty; the path is staged
at stage 0 with blob `c66d9bfb`). Per STEP 4, `--skip` was not called — the
cherry-pick sequencer is untouched and `CHERRY_PICK_HEAD` (`e558098f`) remains
present for `cherry_pick_finalize_resolution`, which will detect the clean
staged diff and skip the commit itself.

No hunk was dropped under the BUG-1301 precedence exception, and no UAT test
function was deleted.

## Flagged for post-merge review

The enrichment classed this file's intent as unknown on one or both sides and
asked that it be flagged. Flagging `request-8b33a146`: the ours side reached
this branch through overlay seeding (`seed_local_overlay`, the only two commits
touching the path on HEAD) rather than by replaying each lifecycle transition,
so the branch has no per-step ledger for this ticket to cross-check the
composed result against.

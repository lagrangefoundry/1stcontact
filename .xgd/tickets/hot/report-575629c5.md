---
uid: report-575629c5
id: REPORT-1601
type: report
title: Fix site-materials-and-start-point (ac) — attempt 1
created_by: xgd
created_at: '2026-08-07T18:19:44.239825+00:00'
updated_at: '2026-08-07T18:19:44.239825+00:00'
completed_at: null
last_field_updated: created_at
fields:
  report_kind: fix_structural_validation
  subject_uid: capability-b4ac88fc
  level: ac
  fixes_applied: 3
  progress_made: true
  needs_more_work: false
  violations_remaining: 0
  anchor_report_uid: report-17a279f7
---

# Fix Summary — site-materials-and-start-point (ac)

**Attempt**: 1
**Fixes applied this call**: 3
**Violations remaining**: 0
**Needs more work**: false

The validation report carried exactly one violation (finding 1, `ac-edit`) and
three `info` rows requiring no action. The violation is repaired, and the same
retired claim — REQ-114's deleted theme colour-token group — was swept out of the
two other places in this capability where it survived verbatim.

## Actions Taken — by Resolution Category

| # | Category | Element | Action |
|---|---|---|---|
| 1 | ac-edit | AC-873 (acceptance_criterion-56334082) | Retitled from "…come from the site's own **theme tokens, not from literals** invented by the scaffold" to "A newly created site states its document background and placeholder colour as hex literals in its own layout document, and declares no palette". Body, Verification and `fields` untouched, per the assessor's explicit instruction not to fix the body toward the title. |
| 2 | consistency sweep (story body) | STORY-93 (story-86c7c21b) | The `## Story` line still read "a complete layout document on the standard width ladder, **in the site's own theme colours**" — the identical pre-REQ-114 claim, and directly contradicted by its own Description bullet ("Colour is stated in the page's own document, as literals") and Technical Context. Replaced with "in colours the page's own document declares". Paragraph re-wrapped to the surrounding 80-column style. Nothing else changed. |
| 3 | consistency sweep (capability body) | CAP-89 (capability-b4ac88fc) | The Scope § "The authoring start point" carried the same phrase ("…on the same width ladder a reproduction keyframes at, **in the site's own theme colours**"). Replaced identically and re-wrapped. Nothing else changed. |

Edits 2 and 3 are strictly the same repair as the violation, applied to the two
surfaces the ac-level pass does not read. They were applied in the same call so
the matrix does not sit in a state where the corrected AC title contradicts its
own parent story and capability. Both were minimal phrase substitutions
(occurrence count asserted == 1 before writing); no other sentence, section or
field was touched.

## Verification

- **Drift sweep across the whole capability.** Fetched the titles of all 35
  active ACs across STORY-93, STORY-92, STORY-102 and STORY-97 and read them
  against the Alignment Ledger. AC-873 was the only title carrying a retired
  mechanism; no second instance of the pattern the assessor warned about exists
  in this capability.
- **AC-873's UAT already held the corrected position**, independently confirming
  the title was the sole stale surface: `tests/reconciliation-scaffold-starter-l1.test.ts`
  line 16 documents AC-873 as "colour is stated as literals in the page's own
  layout document, and creation declares no palette (REQ-114 retired the
  theme's)", and `test_UAT_AC873_document_and_placeholder_colours_are_literals_in_the_page_document`
  asserts exactly that.
- **Suite run**: `npx vitest run tests/reconciliation-scaffold-starter-l1.test.ts`
  → **7 passed, 1 skipped**. AC-873's UAT passes. No regression; no test file was
  modified this call.

## Code Edits (if any)

None this call. All three mutations are ticket-body/title edits made via
`xgd ticket update`; each is committed (`4bf93fe21`, `d54d9d868`, `fcbdc9c0f`,
`e7bf6bc4c`).

## needs_review Items Forwarded

None. No finding in report-f5bac919 was categorised `needs_review`, and no edit
this call required a judgement the intent ledger did not already settle.

## Observations for the uat-level cycle (not acted on here)

Recorded rather than fixed, because both are outside the ac-level remit:

| Element | Observation |
|---|---|
| AC-871 | `test_UAT_AC871_fresh_site_shoots_without_hand_editing` is **skipped**, not passing, in the suite run above — while STORY-93 carries `uat_coverage: pass`. The uat-level cycle should decide whether a skipped screenshot UAT constitutes coverage. |
| AC-870 | Its UAT is still named `test_UAT_AC870_fresh_site_renders_placeholder_centred_on_theme_background` — the same retired "theme" vocabulary, in a test *name*. The AC's own title correctly says "document background". Deliberately **not** renamed here: test names are referenced by evidence sets, so the rename belongs to a uat-level pass that can update those references together. |
| STORY-92, STORY-102 | Carry no `uat_coverage` field at all (STORY-93 and STORY-97 carry `pass`), as the assessor noted. Flagged so the uat-level cycle does not read the absence as a pass. |

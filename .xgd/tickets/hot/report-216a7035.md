---
uid: report-216a7035
id: REPORT-4208
type: report
title: 'Reconcile resolve conflicts: reconcile-BUNDLE-27'
created_by: xgd
created_at: '2026-09-14T03:19:20.227362+00:00'
updated_at: '2026-09-14T03:19:20.227362+00:00'
completed_at: null
last_field_updated: created_at
result: pass
fields:
  report_kind: resolve_conflicts
  subject_uid: reconcile-BUNDLE-27
---

## Files resolved

- `.xgd/tickets/hot/bug-034bf955.md` (BUG-42) — **UU**, intent/bookkeeping
  ticket (rule 2e). Resolved to **ours**.

  Incoming commit this attempt is `3df77005` *xgd(ticket): update bug
  bug-034bf955* (Tue Sep  1 14:16:53 2026) — the immediate successor of the
  previous attempt's `488962bd`. Index stages confirm the chain: base is now
  `040ee98c` (the previous attempt's incoming blob), ours is still `91ed8fa`
  (unchanged, since that attempt's resolution kept HEAD's blob and finalize
  skipped the empty commit), theirs is `db7db2f7`.

  Path is outside the sparse-checkout cone (skip-worktree, DOC-986 §2/§4.1), so
  the conflict existed only in the index with no working-tree markers. Resolved
  by pointing the index entry at the stage-2 blob (`91ed8fa`) via
  `git update-index --cacheinfo`, then restoring the `skip-worktree` bit that
  the index write clears (verified `S` via `git ls-files -v`).

  Conflict is frontmatter-only — the prose body is byte-identical across base,
  ours and theirs. Per-fact resolution:

  | fact | theirs (incoming) | ours (HEAD) | taken |
  |---|---|---|---|
  | `fields.story_points` | `3` | `3` | same value — incoming's change already present |
  | `status` | `free_coded` | `bundled` | ours |
  | `last_field_updated` | `story_points` | `status` | ours |
  | `fields.bundled_in` | absent | `bundle-8e1807f6` | ours |
  | `updated_at` | 2026-09-01T21:16:53 | 2026-09-11T18:53:54 | ours |

  On every genuinely competing fact, 2e's per-fact timeline rule and the
  auto-enrichment's "more recent commit by timestamp" rule agree and both select
  ours: HEAD-side `79ad55aa` *seed_local_overlay* (Fri Sep 11 14:08:30 2026)
  postdates incoming `3df77005` (Tue Sep  1 14:16:53 2026) by ten days. The
  lifecycle ordering agrees independently — `bundled` is downstream of
  `free_coded`. `last_field_updated` is a derived marker naming the most recently
  edited field; ours' most recent edit was `status` → `bundled`, so `status` is
  the self-consistent value for the ours state. Taking theirs would leave the
  file internally inconsistent.

## Incoming changes preserved

Incoming commit `3df77005` makes exactly three changes, all frontmatter:

1. `fields.story_points: 3` added — **present in HEAD byte-identically**,
   carried by the seed_local_overlay commit. This is the commit's only
   substantive change, and it does not appear in the ours-vs-theirs stage diff
   at all, which is direct evidence it is already in ours. Preserved.
2. `last_field_updated: status` → `story_points` — derived bookkeeping, resolved
   to ours as reasoned above.
3. `updated_at` bump — timestamp churn, no developer intent.

No prose/body hunks exist in the incoming diff, so nothing could be dropped
there. No code or test files were involved; no hunk was dropped under the
BUG-1301 precedence exception.

Net staged diff vs HEAD is empty — this commit's substantive effect
(`story_points: 3`) had already landed via the seed_local_overlay route. That is
the BUG-1109/BUG-1122 redundant-commit case, not a discard: STEP 3's test
distinguishes them by asking whether the incoming change is *present in HEAD*
(redundant) or *simply absent* (discarded), and here it is present and
byte-identical. Per STEP 4, staged and exiting normally; `--skip` was not called
and the cherry-pick sequencer state (CHERRY_PICK_HEAD `3df77005`) is intact.

---
uid: report-6952e624
id: REPORT-4190
type: report
title: 'Reconcile resolve conflicts: reconcile-BUNDLE-27'
created_by: xgd
created_at: '2026-09-14T00:41:53.765461+00:00'
updated_at: '2026-09-14T00:41:53.765461+00:00'
completed_at: null
last_field_updated: created_at
result: pass
fields:
  report_kind: resolve_conflicts
  subject_uid: reconcile-BUNDLE-27
---

## Files resolved

- `.xgd/tickets/hot/request-bbff35c7.md` — UU, sparse-excluded (index-only, no
  working-tree markers). Intent/bookkeeping ticket (`request-*`), so rule **2e**
  applies, resolved per-fact:
  - **Body — `[[REQ-171]] owns it, together with the session prompts and turn
    reminders it has to be written into.`** (the incoming commit's only
    substantive change): present **identically** on both sides. Ours already
    carries it verbatim, so there is nothing to compose — no loss.
  - **Body — everything else** (the three new delta bullets on cursor boundary /
    corrupt cursor / never-report-self, the rewritten *Depends on* fan-out
    paragraphs, the new Acceptance bullet, the "oversized title is clipped"
    Decided bullet): HEAD-side only, untouched by incoming. Kept — ours is a
    strict superset of theirs on the body.
  - **Frontmatter — `status: bundled`, `last_field_updated: status`,
    `fields.commits`, `fields.version: 0.2.34`,
    `fields.bundled_in: bundle-8e1807f6`**: HEAD-side only, incoming never
    touched them. Kept (superset rule).
  - **Frontmatter — `updated_at`**: the single genuinely conflicting fact.
    Ours `2026-09-11T18:53:54` vs theirs `2026-09-01T19:34:20`. Took the
    later-positioned side (ours), consistent with the auto-enrichment's
    "take the more recent commit by timestamp" rule for this file.

  Net effect: resolution equals ours exactly (`git checkout --ours
  --ignore-skip-worktree-bits`, then `git add --sparse`), because ours is a
  superset on every fact and later on the one contested fact.

## Incoming changes preserved

Incoming commit `20be4747de43b93ebc04b60f786538fbbe1ee237`
(*xgd(ticket): update request request-bbff35c7*) touches exactly one file, 3
insertions / 1 deletion. Diffed against the merge base, it consists of:

1. the two-line `[[REQ-171]] owns it …` addition to the *Out of scope* section's
   "chat ticket's AI-maintained summary" bullet, and
2. the `updated_at` bump.

Item 1 is present in the resolved file — verified directly against the staged
result: `git show HEAD:.xgd/tickets/hot/request-bbff35c7.md` matches it at line
216. It arrived on the HEAD side through the `seed_local_overlay` commit, which
snapshotted a later working-tree state that already included this edit. Item 2 is
superseded by the later HEAD-side timestamp, per the per-fact timeline rule.

**No hunk was dropped**, so the BUG-1301 precedence exception was not invoked and
does not apply here.

The staged diff against HEAD is therefore empty. Per STEP 4 this is the
*redundant-commit* case (BUG-1109/BUG-1122), not a discard, and STEP 3's test is
what distinguishes them: the incoming commit's key change is **present** in HEAD
via a different route (the seed overlay), rather than absent. Staged and left for
`cherry_pick_finalize_resolution` to skip; no `--skip`, `--continue`, or other
sequencer-state command was run, and `CHERRY_PICK_HEAD` is intact.

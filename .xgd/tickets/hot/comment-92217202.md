---
uid: comment-92217202
id: COMMENT-772
type: comment
title: Claude response for capability_validation report
created_by: xgd
created_at: '2026-08-07T17:21:27.040885+00:00'
updated_at: '2026-08-07T17:21:27.040885+00:00'
completed_at: null
last_field_updated: created_at
status: null
fields:
  subject_uid: report-9f7e2b43
  kind: note
---

Report created and schema-verified: **REPORT-1592** (`report-9f7e2b43`), `result: fail`, `report_kind: capability_validation`, `subject_uid: capability-12fee326`, `level: ac`, `violations: 1`, `warnings: 1`, `needs_review_count: 0`.

## Result: FAIL — 1 violation, 1 warning, 0 needs_review

Scope: 28 ACs (15 under STORY-101, 13 under STORY-98). Story cycle passed at 17:14 (report-afb07aaa), so story bodies were the working reference; intent was consulted only where a body was silent.

**Violation — AC-1028 (`acceptance_criterion-26ffac6d`), coverage, `ac-edit`.** The AC covering REQ-118's image-selection ask sits at `status: pending`, so it is not in the enforced matrix — `xgd_source/quality/ac_coverage_gate.py:14` queries only `status == active`, and 310 of 313 project ACs are active. REQ-118 is `free_and_reconciled` (merged `b2b9208c`) and STORY-101's body carries image selection as in-scope. AC-1028 is also the sole carrier of the kind-agnosticism property, so the gap is wider than the image kind. Fix is one field write: `xgd ticket update acceptance_criterion-26ffac6d --fields '{"status":"active"}'` — the body already matches the story and intent clause for clause. report-afb07aaa explicitly deferred this here as "an ac-level action".

**Warning — AC-1000, consistency, `story-body-edit`.** "Closing a form in which nothing was changed writes nothing and re-renders nothing" has no counterpart in STORY-101's in-scope list. It isn't implied by "one confirmed form is one change" (that's the n>1 case). The behaviour is genuine — `apps/control-app/src/builder/editor.js:222-227` guards the empty change map, and `tests/reconciliation-copy-edit-gesture-modal.test.ts:387` proves it — so the AC is right and the story body is short.

Three things worth flagging beyond the findings table:

- **Sequence the fix with the uat cycle.** Activating AC-1028 makes it visible to `check_active_ac_coverage`, which will then flag it as orphaned: there is no `test_UAT_AC1028_*` test. Its evidence exists as `test_UAT_FC_REQ-118_clicking_an_image_segment_offers_a_picker_of_the_sites_assets`, a name `extract_ac_id_from_test_name` cannot map. Activating alone converts a silent gap into a loud gate failure.
- **`pending` is a systemic trap.** It's the AC type's default, the story-generation prompts create ACs with it, and nothing in the prompt or workflow tree ever drains it — AC-718/AC-719 have sat there since 2026-08-05, one already carrying passing evidence. That's an XGD-tool concern, out of scope for this report, but future ac-level cycles should expect it.
- **STORY-98 is clean** — 13 ACs, one-to-one against every in-scope bullet, no orphans, no real overlap.

Also carried forward as info: report-afb07aaa's finding 1 (CAP-87 body still reads "the new words") is unrepaired. It's an upper-layer element the story cycle passed as a warning, and no AC inherits the narrowing, so I did not re-raise it as blocking.

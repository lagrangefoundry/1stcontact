---
uid: report-3e693963
id: REPORT-4328
type: report
title: 'Reconcile resolve conflicts: reconcile-BUNDLE-27'
created_by: xgd
created_at: '2026-09-18T06:45:05.230391+00:00'
updated_at: '2026-09-18T06:45:05.230391+00:00'
completed_at: null
last_field_updated: created_at
result: pass
fields:
  report_kind: resolve_conflicts
  subject_uid: reconcile-BUNDLE-27
---

## Files resolved

- `.xgd/tickets/hot/bug-6612c4b7.md` (BUG-37) — class **UU**, index-only
  (path is outside the sparse-checkout cone on this reconcile branch, DOC-986
  §2/§4.1, so there were no working-tree markers). Rule **2e** (intent /
  bookkeeping ticket: `bug-*`). Resolved to the **HEAD** side verbatim via
  `git checkout --ours` + `git add --sparse`.

  Incoming commit being applied: `a9248d67` *"xgd(ticket): update bug
  bug-6612c4b7"*, authored 2026-08-24 14:55 -0700.

  The conflict was confined entirely to frontmatter. A blob-to-blob diff
  (`git diff --unified=0 HEAD:<path> a9248d67:<path>`) yields exactly two
  hunks, both above the closing `---` at line 32, and **no body hunks at all**:

  | fact | HEAD (ours) | incoming (theirs) |
  |---|---|---|
  | `status` | `free_and_reconciled` | `free_coded` |
  | `completed_at` | `2026-08-31T19:19:36` | `null` |
  | `last_field_updated` | `status` | `body` |
  | `updated_at` | `2026-08-31T19:19:36` | `2026-08-24T21:55:26` |
  | `fields.version` | `0.2.13` | `0.2.11` |
  | `fields.commits` | 3 entries (+ `working_sha_history`) | 1 entry |
  | `fields.bundled_in` | `bundle-78f4e2fe` | *absent* |

  Per 2e this is the **strict-superset** case on every conflicting fact, not a
  per-fact split: HEAD carries a later lifecycle state, a higher version, two
  additional recorded `commits` entries, and a `bundled_in` assignment that
  incoming never had. There is no field on the incoming side that HEAD does not
  already cover, so no `working-timeline` tiebreak was needed and nothing from
  the incoming side was dropped in favour of an equal-standing competitor.
  Taking any incoming scalar would have rolled operator-owned status backwards
  from `free_and_reconciled` to `free_coded` and discarded `bundled_in` —
  i.e. reverting the bundle's own bookkeeping.

  The enrichment metadata's fallback rule ("take the more recent commit by
  timestamp") points the same way: HEAD's `updated_at` is 2026-08-31, seven
  days after incoming's 2026-08-24.

## Incoming changes preserved

Incoming's substantive change was a **body rewrite**: it replaced the
`## Still outstanding (not in this ticket)` section with the expanded
`## Observability — added here` and `## Deployment` sections (29 insertions,
8 deletions over the whole file).

That content is **already present in HEAD, byte-identical** — verified two
ways:

1. The blob-to-blob diff above shows zero hunks below the frontmatter, so the
   two sides' bodies do not differ by a single byte.
2. Positive spot-check of HEAD's blob: `## Observability — added here` at line
   142, `## Deployment` at line 165, and the
   `test_UAT_FC_BUG-37_the_production_route_survives_the_new_table` reference at
   line 160 are all present; the superseded `## Still outstanding` heading is
   absent.

So the incoming commit's intent is preserved in full — it arrived on this
branch earlier through the post-watermark sync of working into reconcile, which
is why only the stale lifecycle scalars were left to collide.

No hunk was dropped under the BUG-1301 precedence exception; that exception did
not apply here. No test functions were involved. No content was invented, and
`fields.intent_uid` / `story_uid` / `capability_uid` were not touched.

## Staging state

`git ls-files -u` is empty (no unmerged index entries) and
`git status --porcelain` is empty. The staged tree nets to **no diff vs HEAD**
— the expected BUG-1109/BUG-1122 redundant-commit shape, since the incoming
effect had already landed via a different route. Per STEP 4 this was staged and
left alone rather than skipped; `cherry_pick_finalize_resolution` will detect
the clean staged diff and skip the commit.

`CHERRY_PICK_HEAD` (`a9248d6756055f417085cb60c7ed117219ba4700`) is intact. No
`--continue`, `--skip`, `--quit`, `--abort`, `reset`, or branch checkout was
run; the only git writes were `checkout --ours` and `add --sparse` on the single
conflicted path.

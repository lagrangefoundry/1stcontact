---
uid: bug-358f076f
id: BUG-145
type: bug
title: Delegated spend is lost when the calling turn writes no spend row
created_by: EPIC-20
created_at: '2026-09-25T03:56:31.457999+00:00'
updated_at: '2026-09-25T22:23:08.908002+00:00'
completed_at: null
last_field_updated: body
status: free_coding
fields:
  epic_parent: epic-0923bb64
  auto_merge_back: true
  needs_review: false
  priority: medium
  chat_comment: comment-a9a74daa
---

## What is wrong

**A delegation is metered if and only if its enclosing turn wrote a `turn_spend`
row.** Where the turn wrote one, the worker is attributed correctly. Where the
turn wrote none, the worker's spend is discarded — and the delegation becomes
invisible to every reader of the meter.

Measured on the dev environment 2026-09-24 (Lagrange Foundry,
`biz_5b101742d436573a04a2512fb7ecdbb5`):

- **Eight worker sessions have ever existed.** Four appear in a
  `turn_spend.attributed` list. **Four do not.**
- The split is not random. All four attributed workers ran inside a turn that
  wrote a spend row (4 of 4). All four lost workers ran inside a turn that wrote
  no row at all (0 of 4).

## Why it happens

`turnSpendRecord` in `tools/generate/src/cli/ai/spend-core.ts`:

```
const usage = countersOf(spend.usage)
if (!measured(usage)) return null
```

The **caller's own** usage gates the whole row, and `attributed` is a field of
that row, so it is discarded with it. The rule *"a turn that measured nothing
writes no record"* is right about the caller and wrong about the second party:
a turn that died before its terminal meta arrived — an error, an abandoned
stream — may still have caused a worker to run to completion on a real bill.

REQ-295's accounting criterion holds for every **delegation** exit path
(`reported`, `stopped`, `silent`, `failed`) and does not hold for the
**caller's**. That is the gap; the delegation surface is not at fault.

Today's instance: the 11:49 local turn ended
`D1_ERROR: string or blob too big: SQLITE_TOOBIG` (the last casualty of REQ-309,
already fixed), so its worker — 70 tool calls, 27 site writes, zero failures — is
unmetered.

## Why it matters more than the money

The absolute loss is small: comparable workers priced at **$0.14–$0.57** each.
The damage is to the instrument. REQ-293's split-by-model report is the
designated answer to *"did delegation move any work"* — the one number EPIC-20
intends to steer by — and on the record to date it under-counts delegated spend
by half. Neither of the two motivation levers now under consideration can be
evaluated with it until this is fixed.

## What it should do instead

Write the row when **either** the caller's usage is measured **or** `attributed`
is non-empty.

This deliberately widens *nothing, never zero*, and the widening is the decision
in this ticket rather than an accident of it. `turn_spend` gains rows whose four
counters are absent but whose `attributed` is not. Such a row is not a turn
claimed to be free — it says **"this turn caused measured spend elsewhere, and
none of its own was observed"**, which is a fact and not a zero. A reader that
sums the four counters is unaffected; a reader of `attributed` stops losing half
its subject.

Consequences to settle while implementing:

- `cost_micros` for such a row is the caller's own cost and is therefore **null**,
  not `0` — the same rule, one column over.
- `requests` is `0` legitimately: none were observed.
- The four counter columns are `NOT NULL` today. Whether they take `0` — a
  measured absence the row's own shape already disclaims — or the schema widens
  to nullable, is the implementer's call; the first is cheaper and the row is
  already unambiguous because `cost_micros` is null.
- `outcome` carries the turn's real end (`error`, `aborted`), which is what makes
  the row legible rather than mysterious.

## How it will be known to work

A turn that delegates and then fails before its own terminal meta arrives leaves
a `turn_spend` row whose `attributed` names the worker, whose `cost_micros` is
null, and which REQ-293's split counts on the delegated side.


## What was built

The gate in `turnSpendRecord` now has two reasons for a row to exist rather than
one: the caller's own usage is measured, **or** `attributed` is a non-empty list.
Everything downstream of it — the port, the insert, the two readers — was already
able to carry such a row and is unchanged.

The decisions the ticket left open, settled:

- **The four counter columns stay `NOT NULL` and take `0`.** The schema is not
  widened. A migration would buy a second way to spell the same absence, and the
  row is already unambiguous without one: `cost_micros IS NULL` is the test, and
  that column was already nullable for the neighbouring reason (an unpriced
  `(backend, model)`).
- **`cost_micros` is `null` and not `0`** for such a row — the arithmetic over
  four zeros would otherwise settle at exactly `0` for any priced pair, and a
  stored `0` is the claim a meter must never make. What the *worker* cost is
  still priced from `attributed`, against the worker's own key.
- **`requests` is `0`**, honestly: none were observed.
- **`outcome` carries the turn's real end** (`error`, `aborted`), unchanged — it
  is what makes a row with no counters legible rather than mysterious.

## What deliberately did not widen

- **An empty `attributed` is not an attribution.** `[]` is what a host that
  composed the delegation surface and delegated nothing hands over — the ordinary
  turn, not an exceptional one — so it must not be a reason for a row. "Asked and
  found none" and "caused measured spend elsewhere" are different claims and only
  the second is worth a row. Neither is a non-array.
- **A turn with neither its own measurement nor an attribution still writes
  nothing.** *Nothing, never zero* is narrowed to the one case that was wrong, not
  repealed.
- **A caller that did measure its own spend still settles its own cost.** This is
  the regression the fix could have introduced — reaching for the new `null` one
  branch too early would have unpriced every delegating turn, breaking the same
  instrument the other way round.

## One consequence worth stating

REQ-293's `unpricedTurns` now counts these rows beside the genuinely unpriced
`(backend, model)` it was named for. That is a real widening of what that alarm
means. It is not given a column of its own: the shape only arises where a
delegating turn died before its terminal meta, which is rare, and the row carries
`requests = 0` and four zero counters for anyone who needs to tell the two apart.
The tenant's own report is otherwise untouched — a reader that sums the four
counters sees nothing new.

## Evidence

- `tests/test_UAT_FC_BUG-145_delegated_spend.test.ts` — the rule where it is
  decided, over no database: a row exists, its cost is `null`, nothing else
  widened, and a measured caller keeps its own settled cost.
- `tests/test_UAT_FC_BUG-145_delegated_spend.workers.test.ts` — the real route
  inside workerd. The caller delegates, the worker runs to completion, and the
  caller's next request fails with the `SQLITE_TOOBIG` that prompted this ticket.
  The stored row names the worker with the worker's own counters, its
  `cost_micros` is `NULL`, its `outcome` is `error`, and `tenantDelegatedSpend`
  counts it on the delegated side priced at the worker's backend.

Both cases were confirmed to fail against the unfixed `turnSpendRecord` — the
node case at "expected null not to be null", the workers case at "expected [] to
have a length of 1".

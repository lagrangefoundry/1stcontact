---
uid: report-82876d48
id: REPORT-760
type: report
title: 'Code Review: bundle-31e474b9'
created_by: xgd
created_at: '2026-07-22T21:11:59.079738+00:00'
updated_at: '2026-07-22T21:11:59.079738+00:00'
completed_at: null
last_field_updated: created_at
result: pass
fields:
  report_kind: code_review
  subject_uid: bundle-31e474b9
  anchor_uid: bundle-31e474b9
---

# Code Review — bundle-31e474b9 (framework pivot: L1 substrate + capability modules)

**Result**: PASS

## Summary
The bundle delivers the framework pivot (REQ-63/79/82/83/84/85/86): the L1 layout substrate + safety envelope, capture→L1 fold + structural hints, the strip of the semantic layout modules, the capability-module contract (carousel + contact-form reframed onto config/slots + shipped client JS), and the end-to-end 3-probe reproduction gate. All quality gates pass, the full test suite is green (567/567), a clean cross-package typecheck passes (the flagged subScales drift is resolved), and the security spine (envelope validator + single-emitter renderer) is well-structured and matches DOC-2. Findings are minor/latent and non-blocking; recorded as warnings for follow-up.

## Quality Gates
- **Lint**: success — 0 errors, 0 warnings (quality report report-985e4955).
- **Build**: success.
- **Tests (scoped quality report)**: 48 scoped UATs passed, 0 failed. Independently ran the **full suite: 81 files / 567 tests, all passing** (39s).
- **Typecheck (coherence-review requirement)**: rebuilt site-schema dist, then `tsc --noEmit` on framework AND tools/generate — both exit 0. The REQ-84 `subScales` responsive type-drift is resolved (no stale-dist masking).
- **Coverage**: met (gate green in report).

## External Interface Accessibility
New entry points wired in: **yes**.
- L1 renderer exported from framework barrel (`renderL1Document/Page/Fragment`); envelope validator (`validateL1`, `isSafeUrl`, `L1_ENVELOPE`) exported from site-schema barrel.
- Capability contract (`CapabilityMeta`/`CapabilityDefinition`, `validateCapabilityConfig/Slots/Instance`) exported via `modules/index.ts` + framework barrel; consumed by carousel/contact-form metas + registry.
- Registry correctly reduced to the two capability modules (carousel, contact-form); no dangling imports to the deleted layout modules (hero/footer/header/text-block/services-grid/layer + helpers) anywhere in src.
- `getModuleClientJs()` folds catalog `client.js` → render pipeline writes `capabilities.js` and references it once per page (render.ts:118,169-170).
- `1c aligned-crops` routed in the CLI switch; `adopt-values` removed as intended (REQ-83) — no surviving CLI case or exported symbol.

## Code Quality
| File | Finding | Severity |
|------|---------|----------|
| packages/site-schema/src/l1/validate.ts | Envelope validator: typed ranges, URL allowlist, geometry well-formedness, depth/node caps. Clean, well-documented. | OK |
| packages/framework/src/l1/render.ts | Single-emitter renderer: escaped text, hex-only colours, sanitised font-family, numeric lengths, unsafe src dropped. Enum axes (textAlign/textTransform/fontStyle/object-fit/distribution/align) are emitted trusting the closed schema enum rather than re-checked at emit; acceptable (real freeform vectors ARE re-checked) but a small defense-in-depth asymmetry vs the stated "every value re-checked here". | Nit |
| tools/generate/src/l1/probes.ts:223,226,232 | Analytic layout evaluator: the `row` container branch advances the cursor by `box.width` (parent width) and frames each child at full parent width — correct for `stack`/column flow (the real path) but wrong for a `row` of in-flow children. **Latent**: the pipeline never produces row-flow (captured nodes are pinned; `promoteToFlow` only emits `layout:'stack'`), so no probe/test exercises it. | Warning |
| packages/framework/src/modules/contact-form/client.js:61-62 | `success && 'content' in success ? success.innerHTML : success ? success.innerHTML : ''` — the `'content' in success` guard is dead (both truthy branches return `success.innerHTML`). Outcome is correct (SSR-origin content, not user input); just convoluted. | Nit |
| packages/framework/src/modules/contact-form/client.js:83-86 | `enhanceContactForm` lacks the outer try/catch that `enhanceCarousel` has. Low real risk (`form` always from querySelectorAll; `handleSubmit` is async so throws become voided rejections), but inconsistent with the stated isolation-by-defensiveness principle. | Warning |
| packages/framework/src/modules/capability.ts | `validateCapabilityConfig/Slots/Instance` are exported + unit-tested but not invoked in the render/load path. Not a security gap — schema (`moduleInstanceSchema`) validates config + re-parses slots as `l1NodeSchema` at load, and robustness against degenerate config is delivered by module-level defensive degradation (the isolation conformance dimension, render-level, always-run). Recommend wiring `validateCapabilityInstance` into the authoring/edit path for earlier, clearer errors. | Warning |

## Smoke Test
Entry points are driven by 567 passing UATs through the same Vite SSR transform the `1c` bin uses (including real-browser roundtrip/hints/conformance probes). Direct `node bin/1c.mjs <cmd>` hits a **pre-existing** repo issue — native Node ESM externalizes `@1stcontact/site-schema` to its `dist/index.js`, which uses extensionless re-exports (`export * from './types'`) present identically on `main`; not introduced by this bundle. The Vite-transformed path (tests + normal operation) resolves cleanly.

## Issues Found
**Critical (must fix)**: none.

**Warnings (should fix, follow-up)**:
- probes.ts row-flow layout math (`box.width` → child width) — latent, harden before any row-flow authoring lands.
- contact-form/client.js: add the outer try/catch to `enhanceContactForm` for isolation parity with carousel; simplify the dead `'content' in success` ternary.
- Wire `validateCapabilityInstance` into the load/authoring path so contract violations surface before render.
- render.ts: optionally re-check enum axes at emit for full defense-in-depth parity.

## Verdict
PASS. The pivot is coherent (code ↔ docs ↔ registry tell one story), quality gates are green including a clean full rebuild + cross-package typecheck, and the security envelope holds. The findings above are non-blocking quality/robustness follow-ups, not correctness failures against any acceptance criterion.

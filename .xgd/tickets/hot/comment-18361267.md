---
uid: comment-18361267
id: COMMENT-2403
type: comment
title: Comment on request REQ-212
created_by: xgd
created_at: '2026-09-10T00:22:11.502320+00:00'
updated_at: '2026-09-10T20:21:10.678541+00:00'
completed_at: null
last_field_updated: body
status: null
fields:
  subject_uid: request-37983f6e
  kind: chat_transcript
---

<!-- xgd-turn id="e518ba94-a5bd-481e-b784-73bb04f75dc2-user" -->

<!-- xgd-chat role="user" ts="2026-09-10T00:22:02.285820+00:00" -->
#### You
I would like to discuss if/how L1 can/should support modals.

L1 can describe a sign up/on form. I would like the sign in link on 1st Contact to trigger a modal with a sign in form that completes a flow in the modal.

>>>In the top right, I would like a "sign in" link that opens a sign in modal. "Please enter your email address [text field] // [Continue]" - clicking the continue will replace that content in the modal with a message - "Please check your email. If you are already a member we have just sent you a sign up link."

In this ticket I want to discuss extending L1 so that the AI could create an interaction like this

<!-- xgd-turn id="e518ba94-a5bd-481e-b784-73bb04f75dc2-assistant" -->

<!-- xgd-chat role="assistant" ts="2026-09-10T00:25:51.827460+00:00" -->
#### Claude
I've read the substrate. Here's what I found, and my position.

## The specific thing you asked for already exists

`account-chrome` ([[REQ-200]], `packages/framework/src/modules/account-chrome/`) is a shipped behaviour module that is *exactly* your flow: a `signIn` control opens a `dialog` slot holding an `email` input and a `submit`; on submit it POSTs the address and replaces the panel with `sentMessage` — defaulting to *"Check your email for a sign-in link."* It never reads the response, deliberately, so a known and an unknown address are indistinguishable ([[REQ-134]]).

So putting a sign-in modal on 1st Contact is a **site-definition** job — instantiate `account-chrome`, fill its four slots with L1 (there's an L2 preset at `l2/account-chrome.ts` to start from) — not an L1 extension. Worth separating that from this ticket.

## But the general question is real, and the code says so

`account-chrome/styles.css` ships this, and justifies it in a comment:

```css
.account-chrome[data-account-chrome-enhanced] .account-chrome__dialog {
  position: fixed; inset: 0; z-index: 2147483000;
  display: flex; align-items: center; justify-content: center;
  background: rgba(0, 0, 0, 0.5);
}
```

> *"Covering the page and centring on it is behavioural — a modal that did not would not be one — and no L1 axis can express `position: fixed`."*

DOC-25 §10.2 says a conforming behavior ships **zero CSS beyond §10.3 invariants**. A 50%-black backdrop no site can vary is not an obligation-pinned invariant, it's taste. That comment is an honest admission of a **substrate gap**, not a principled carve-out — and it's the strongest in-repo evidence that overlay presentation belongs in L1.

## Decomposing a modal

Applying DOC-25 §10.3's own line — *L1 owns everything the reference can legitimately vary; the module owns what an obligation pins*:

| part | today | belongs to |
|---|---|---|
| panel look (fill, radius, padding, width, type) | L1 already | **L1** — nothing needed |
| overlay presentation (fixed, cover, centre, backdrop, entry motion) | module CSS, unvariable | **L1** — this is the gap |
| open/close + what triggers it | module `client.js` + `data-*` | **split** — L1 says *what opens what*, renderer owns *how* |
| dialog obligations (`role`, focus trap, Escape, return-focus, scroll lock, inert background) | **mostly absent** | **renderer**, not the author |
| step transition ("swap for the message") | module, hardcoded per module | **module** — it's a server round-trip |

That fourth row is worth flagging on its own: today's dialog has `aria-expanded` on the opener and `.focus()` on the field, and that's all. No `role="dialog"`, no Escape, no focus trap, no return-focus, no scroll lock. It's a disclosure styled as an overlay. That's a defect in REQ-200 regardless of what we decide here — and it's the argument *against* letting every future modal be hand-rolled per module.

## My recommendation

**Two small L1 additions, both roles rather than node kinds** — following the `link`/`reveal` precedent exactly:

1. **`overlay` on a node** — "this subtree presents above the page rather than in flow." Carries only what a reference can legitimately vary (backdrop paint via existing surface axes, panel placement via existing geometry). The renderer emits `<dialog>` and owns everything the dialog contract obliges.
2. **`action` alongside `link`** — a *closed* verb set, initially `{ opens: <id> }` / `{ closes: <id> }`. Not an event system: an enumeration of what one node may do to another's disclosure state. Mutually exclusive with `link`, enforced by `.strict()` the way `control` already can't carry `link`.

The precedent for putting obligations in L1 rather than a module is already there: `l1LinkSchema` pins `rel="noopener noreferrer"` whenever `newTab` is set — *"there is deliberately no way to ask for `_blank` without its `rel`"* — and `href` goes through `isSafeUrl`. `l1FocusRingSchema` exists because DOC-24 holds focus above taste. The renderer being the sole sink is how L1 already carries non-negotiables.

The `reveal` doc comment argues this case for us almost verbatim: motion became an L1 *adjective* rather than a module partly because *"putting it in a module would make animated content unfoldable by construction — `fold` maps captured node axes onto L1 node axes and never authors a module."* **A captured reference site with a modal has the same problem.** If modal-ness only exists as a module, reproduction can never fold one.

**What stays in a module:** anything with a server round-trip or a policy obligation. The sign-in flow's "one message whatever the server answers" is REQ-134 policy and can never be an L1 axis. What changes for `account-chrome` is that its `dialog` slot's overlay presentation comes from L1, and it deletes the stylesheet block above.

**What I'd explicitly not build:** a general L1 state machine for multi-step flows. Every step transition anyone has actually needed is "swap when the behaviour says so", and the behaviour is always the thing that knows the submit succeeded. Both `contact-form` and `account-chrome` already reinvent this ad-hoc (`data-account-chrome='signed-out'` + module CSS hiding the other states). The cheaper, better-motivated move is to generalise *that*: module-declared named states, framework-owned visibility switching, one rule the framework emits instead of each module writing its own.

## Open questions I'd want your answer on

1. **Is `opens`/`closes` enough, or does `action` need to be extensible from day one?** A closed two-verb set is easy to validate and hard to abuse; but the next ask (a tab strip, an accordion, a "show more") is the same shape and would want in.
2. **Does the trigger need to reach across a module boundary?** Your case is trigger-in-header, panel-at-page-level. `action.opens: <id>` targets a node id, so it does — but that means an L1 node can reference a node inside a module's slot subtree, which nothing currently validates.
3. **Non-JS baseline.** `account-chrome` renders the form *open* and script folds it away, so script only ever subtracts. Should an L1 `overlay` inherit that discipline (render in flow, become an overlay on enhancement), or is a `<dialog>` that simply isn't reachable without JS acceptable?
4. **Is a modal a `<dialog>` or a positioned box?** `<dialog>` gets the platform's focus trap, Escape and inertness free, but constrains what the backdrop can be. Hand-rolling gets full paint control and owes all of it.

Shall I write this up into the ticket body as the design record, or do you want to push on any of these first?

<!-- xgd-turn id="47c3bd1b-44a5-4165-8739-cdddccca5840-user" -->

<!-- xgd-chat role="user" ts="2026-09-10T00:28:55.343645+00:00" -->
#### You
Lets see how far this gets us can you make the changes to add this and document it in the sytem kb?

<!-- xgd-turn id="47c3bd1b-44a5-4165-8739-cdddccca5840-assistant" -->

<!-- xgd-chat role="assistant" ts="2026-09-10T00:52:32.181329+00:00" -->
#### Claude
Starting the free-coding protocol. Scoping the ticket first.

Ticket scoped. Cutting the branch.

Now the renderer — the dialog CSS, the vetted script, and the emitter wiring.

Now the UAT suite.

33 UATs green. Now checking for regressions across the L1 and KB suites.

Now regenerating the projected KB reference so `REF-l1` picks up the new vocabulary.

Now the full node suite, to catch anything else that enumerates L1 node fields.

No regressions — those same 12 files fail identically on the main checkout. Now aligning the ticket body with everything the UATs assert.

Done and merged to `xgd-working`. REQ-212 is `free_coded` at `d64e82a` (+ the `0.2.143` bump), branch cleaned up.

## What L1 gained

**`dialog`** — a node-level role on `box` / `container`. Carries only what a reference can legitimately vary: `backdrop` (colour + opacity), `placement` (center/top/bottom), `dismissOnEscape`, `dismissOnBackdrop`, `ariaLabel`. The panel's look is its ordinary surface and sizing axes. Both dismissals default **on**, so a trap has to be asked for.

**`action`** — a node-level role on `box` / `container` / `text`. A closed set of two verbs, `opens` / `closes`, naming a panel's `id`. Not an event system: a document can change which of its own declared panels is open, and there is no third thing it can say. Mutually exclusive with `link`.

Four structural rules in `validate.ts`: a panel must declare an `id`; an action names exactly one verb; what it names must exist **and carry `dialog`**; no node carries both `link` and `action`.

## What the renderer owns

Action nodes **retag** to `<button type="button">` — REQ-106's discipline, so the class, paint axes and focus ring stay put. A panel is **wrapped** in a covering shell (the one place a wrap is the faithful shape: the panel keeps focus and every axis, and covering the page is a statement the panel's own element can't make).

One vetted script, emitted only when a document carries a modal and byte-identical across sites — I assert that in a UAT by rendering two different modals and comparing the scripts. It owns focus-in, focus-return, the Tab trap, Escape, the scrim click, and the scroll lock.

**Every overlay rule is gated on a marker only that script sets.** No script → the panel is an ordinary in-flow, open part of the page. Script only ever subtracts, which is `account-chrome`'s discipline generalised. The edit render ships none of it.

One thing that doesn't compose, so the emitter declines it: **`reveal` on a panel is not emitted**. A closed panel never intersects, so the entrance could never fire and REQ-100's pre-state rule would leave it at `opacity: 0` inside a modal that opens onto nothing.

## Verification

33 new UATs in `tests/test_UAT_FC_REQ-212_l1_modals.test.ts`, including the whole reader-facing half driven end-to-end in jsdom against the actual emitted script. `test_UAT_FC_BUG-48_*` gained fixtures for the four new rules (it enforces that every stated rule provokes a real refusal).

Full suite: 12 files fail, and **the same 12 fail identically on a clean tree** — builder/webui, KB and priming suites, matching what's already known-broken. No L1 or render regression.

## KB

`REF-l1` is projected from the schemas, so it picked up both roles, their shapes and all four rules from the declarations. For the judgement a field list can't carry, I amended two existing authored docs rather than filing a new DOC ticket (the session is scoped to REQ-212): **DOC-48 §14** — when a modal is right (three cases) and when it isn't (most of them), with "a modal is for something the reader *asked for*" as the deciding rule; **DOC-47** — records it's now buildable, and that it's ordinary layout rather than a new component.

## Two things I left out, deliberately

- **No multi-step vocabulary.** "Swap the form for the message" is always driven by something that knows the submit succeeded — a module. `account-chrome`'s "one message whatever the server answers" is REQ-134 policy and shouldn't be an L1 axis.
- **`account-chrome` is not migrated.** Its stylesheet still carries the `position: fixed` block quoted in the ticket. Moving its `dialog` slot onto the new role is the obvious follow-up, but doing it here would put a live authentication surface inside a substrate change.

So the 1st Contact sign-in modal is still best served by instantiating `account-chrome` — that flow's server contract is the part L1 can't own. What changed is that the AI can now author *any* modal without a module existing for it.

<!-- xgd-turn id="8f92aa3d-fa25-4c27-9b09-61ea83921ca3-user" -->

<!-- xgd-chat role="user" ts="2026-09-10T17:14:24.292432+00:00" -->
#### You
Is this documented? If you go back to my original ask (top of this chat) can the AI now implement that?

<!-- xgd-turn id="8f92aa3d-fa25-4c27-9b09-61ea83921ca3-assistant" -->

<!-- xgd-chat role="assistant" ts="2026-09-10T17:16:01.410173+00:00" -->
#### Claude
Good question — let me actually verify rather than assert. Two things to check: whether the AI's authoring surface exposes this, and whether the *whole* original flow is buildable.

Verified rather than assumed. Here's the honest answer.

## Can the AI implement your original ask?

**The shape, yes. The sign-in semantics, no — and that part was already true before this change.**

I probed it: a behaviour module mounts cleanly *inside* an L1 dialog. This validates and renders correctly:

```
text  "Sign in"      action: { opens: 'signin' }
container #signin    dialog: { backdrop: …, ariaLabel: 'Sign in' }
  └ slot             behavior: contact-form
  └ text "Close"     action: { closes: 'signin' }
```

The module's markup lands inside the panel, inside the shell. So all four beats of your ask are reachable: link top-right → modal opens → real field + Continue that submits → `contact-form`'s success message replaces the panel's contents in place.

What the AI **cannot** do is author a bare text field. `control` nodes only paint elements a *module* declared — there is no free-standing `<input>` in L1. So the field always comes from a mounted component, which means today it's `contact-form` (lead capture), not a sign-in.

For a genuine sign-in — magic link, "if you are already a member", the rule that a known and unknown address must be indistinguishable — that's `account-chrome`/REQ-134, and it works today. But it **cannot be re-housed in an L1 dialog**: its Sign In trigger is a module `control`, and `control` deliberately cannot carry `action`. So account-chrome keeps painting its own overlay, exactly as I flagged in the ticket's out-of-scope section.

## Is it documented?

Partially, and the gap is the important bit.

- `REF-l1` — every field, every shape, all four rules, projected from the declarations. ✓
- `DOC-48 §14` — when a modal is right and when it isn't. ✓
- **Missing:** that a component can go inside a modal. That single fact is what makes your ask buildable, and nothing says it. Also missing: that sign-in specifically is `account-chrome`'s job, not L1's — so an AI reading DOC-48 §14's "signing in is the canonical case" could reasonably try to build one out of L1 and hit the `control` wall.

That's a real gap in §7 of this ticket's own scope. It's a document change, so no branch needed — shall I close it now? It's about three paragraphs in DOC-48 §14: what can go inside a modal, and the one flow that is a module's rather than L1's.

<!-- xgd-turn id="a625e3cc-7b94-46ac-a7f2-69e0b2842e65-user" -->

<!-- xgd-chat role="user" ts="2026-09-10T19:32:37.237462+00:00" -->
#### You
Please review the last few turns of my chat transcript for 1stcontact and look at the site.

I am trying to get Claude to configure a modal for sign in and the results are confusing. I see the modal content appearing at the top of the page. Claude is telling me this is intended and is a "preview" mode

Lets discuss this design, its not working for me

<!-- xgd-turn id="a625e3cc-7b94-46ac-a7f2-69e0b2842e65-assistant" -->

<!-- xgd-chat role="assistant" ts="2026-09-10T19:36:08.932826+00:00" -->
#### Claude
Let me gather the evidence before opining — the transcript, the site, and what my own change actually does in edit mode.

Now let me verify the code — the preview render paths and what scripts they ship.

I traced it end to end. The short version: **the AI was wrong, and there's a real design problem underneath its mistake.**

## What actually happened

You were in **Edit** mode. The builder serves two channels (`app.js:353`/`359`):

| mode | channel | ships `capabilities.js`? |
|---|---|---|
| **View** | `draft` | **yes** |
| **Edit** | `edit` | **no** — `render.ts:204` gates it on `!edit` |

`account-chrome`'s dialog is inline-open in the served HTML by design, and its `client.js` is what folds it away and makes it an overlay. Edit mode ships no client JS at all, so the dialog just… stays as the server sent it. Same reason the carousel wraps all its slides in Edit and the account portal shows its erasure text open. It's REQ-116: the edit page must be inert so a click resolves to "edit this text" instead of navigating.

"Open in new tab" points at `panel.getSrc()` — **the URL the current mode is showing**. From Edit, that's the edit channel. So your new tab reproduced the inert render exactly. Your test was sound; it just tested the same thing twice.

**Switch the panel to View and click Sign in — it should work.** That's worth confirming before we design anything.

## Where the AI went wrong

Three distinct errors, and only the first is forgivable:

1. It screenshotted `kind: 'edit'` and reasoned about the picture as though it were the page.
2. It told you *"I only have the inline preview to work from"* and *"I can't personally click"* — **`kind: 'draft'` exists** and is one of five it's given. It never tried it.
3. It invented a mechanism: *"the preview shows components in an always-open state."* No such mode exists. It's not that components are forced open; it's that nothing runs.

Error 3 is the one that cost you the most, because a confident wrong mechanism is unfalsifiable in conversation.

## The design problem, which is ours not the AI's

**An inert render of a behavioural component is indistinguishable from a broken one** — to you and to the AI. The picture comes back as pixels with no annotation saying "behaviour is off in this channel." So the model does what models do: builds a story from what it can see.

And this isn't an `account-chrome` quirk. It's every behavioural surface, and it's **exactly what my REQ-212 change inherits** — I wrote *"the edit render ships none regardless (its panels stay in flow and settled)."* An L1 modal will look precisely as wrong in Edit as account-chrome does. Consistent, defensible, and it produces this conversation every time.

There's also a genuine gap the AI fumbled toward: **in View mode the modal is correctly closed, so nobody can see the panel to judge it.** The AI can't click. So there is currently no way to get a truthful picture of an open modal.

## What I'd fix — three things, ordered

1. **`kind: 'edit'` returns a labelled picture.** One sentence with the image: *behaviour is off in this channel; panels, carousels and disclosures show their settled state.* That alone ends the whole exchange above. Cheapest fix by a distance.
2. **Steer the AI to `draft` for "how does this look."** `edit` is for locating something to edit, not for judging design. Currently nothing says so.
3. **`kind: 'draft'` gains `open: '<panel-id>'`.** The draft channel has the live script, so the shot driver opens the named panel and photographs the *real* modal in its *real* state. Not a fake, not a settled stand-in. This is the actually-missing capability, and it's small.

## The architectural question for you

Should **Edit mode be able to show a panel as an overlay?** Two shapes:

- **No** (my lean). Edit stays inert; you review modals in View, and (3) gives the AI a truthful picture. Clean, but you can't edit the copy *inside* a modal while seeing it as a modal.
- **Yes, via a panel selector** in the Edit chrome — the document declares its panels, you pick one, it renders as a static overlay with the copy still clickable. Same control the AI would drive with `open:`. Costs a real UI addition, and every page with a modal needs the "none" state or it's permanently covered.

I'd not ship the third option — making Edit genuinely interactive — because a click on a Sign In button would have to mean either "edit this label" or "open the panel", and that ambiguity is the reason the channel is inert in the first place.

Which way do you want to go? Once you've picked, I'll write it into REQ-212 (§7 already owes a doc fix) and scope the code.

<!-- xgd-turn id="ebf03657-21b7-4eb6-b216-8774cb4d8e33-user" -->

<!-- xgd-chat role="user" ts="2026-09-10T19:38:54.173580+00:00" -->
#### You
Firstly I am not and was not in edit mode. I am in view mode, and I see the same thing with "Open in new tab"

Lets take a step back from what the product AI has said and talk about the actual experience.

<!-- xgd-turn id="cfb54f30-ae1e-4382-a9df-bd28e03437fd-user" -->

<!-- xgd-chat role="user" ts="2026-09-10T19:39:17.800393+00:00" -->
#### You
Firstly I am not and was not in edit mode. I am in view mode, and I see the same thing with "Open in new tab"

Lets take a step back from what the product AI has said and talk about the actual experience. Do you understand the experience I am trying to create on the web page first of all?

<!-- xgd-turn id="cfb54f30-ae1e-4382-a9df-bd28e03437fd-assistant" -->

<!-- xgd-chat role="assistant" ts="2026-09-10T19:39:53.595544+00:00" -->
#### Claude
You're right — View mode runs the script, so my Edit-mode explanation doesn't hold. Let me set the diagnosis aside entirely and say back what I think you're building.

## The experience

**On load, there is no sign-in content on the page.** The header carries a quiet "Sign In" link and nothing else. No box, no email field, nothing occupying space at the top or anywhere. A visitor who never clicks it never knows it exists.

**Clicking "Sign In" summons a panel over the page.** Not in the page — over it. The content behind dims, stays put, and doesn't scroll. The panel sits centred in the viewport. It holds a prompt, an email field, and a Continue button, and it looks like it belongs to 1st Contact — the coral accent, the rounded warmth, the same voice as the beta form — rather than a default browser dialog dropped in from elsewhere.

**Submitting replaces the panel's contents in place.** The field and button give way to *"Please check your email. If you are already a member we have just sent you a sign-in link."* The panel stays open around that sentence. You don't get bounced to another page.

**Dismissing returns you to exactly where you were.** Close, Escape, or a click on the dimmed area outside. The page is unchanged and the panel is gone as though it had never been there.

## The part that's failing

Only the first two matter for what you're seeing, and the first is the whole complaint: **the sign-in content should be absent from the page's layout until summoned.** Right now it's present, at the top, taking up space, on first paint — which is the one thing a modal must never do. Styling it better made it a nicer-looking box in the wrong place; it didn't move it.

So the test that separates "working" from "what you have" isn't about how the panel looks. It's: **load the page and don't touch anything — is the email field on screen?** Today it is. It shouldn't be visible at all.

Is that right, or have I missed something you want — particularly around what happens after submit, or whether the header link should look like a button rather than plain text?

If that's the experience, the next thing I'd do is stop reasoning about it and actually fetch what the draft channel is serving you — the HTML and whether `capabilities.js` is arriving and executing — because in View mode this should already work, and it doesn't. That's a bug to find, not a design to debate.

<!-- xgd-turn id="da53d2ce-1dd5-4ac8-b701-9db54b46945f-user" -->

<!-- xgd-chat role="user" ts="2026-09-10T19:47:37.772031+00:00" -->
#### You
You have the experience correct.

Now, lets talk about the experience of creating this experience, what the user sees, what the AI sees etc:

(1) In edit mode I should see the same thing that I see in view mode. If I am looking at the home page in view mode and I switch to edit - I see the home page. If I am in view mode looking at the sign-in modal and I switch to edit - I should now see, and be able to edit the sign-in modal.
(2) The AI experience for the AI is parallel, it said it "cannot click on the Sign In link" - can we fix that, it needs to be able to manipulate the page to get it into the right state before hitting the screenshot.

ok - if this makes sense - please go and find the bug?

<!-- xgd-turn id="da53d2ce-1dd5-4ac8-b701-9db54b46945f-assistant" -->

<!-- xgd-chat role="assistant" ts="2026-09-10T19:51:00.755628+00:00" -->
#### Claude
Both make sense — (1) is about mode switching preserving page state, (2) is a real capability gap in the picture tool. Let me go find the bug first.

Need auth for the live server. Let me instead check the Worker render path directly — that's what serves `/preview/`.

Found a strong candidate. Let me verify it decisively.

Confirmed — this is the bug.

Found it. It's not a design decision and it's not the modal.

## The bug

`capabilities.js` — the bundle carrying **every** module's client behaviour — is a **JavaScript syntax error**. It does not parse, so none of it runs:

```
.xgd/tmp/capabilities.mjs:281
const ERROR_SELECTOR = '[data-account-error]'
      ^
SyntaxError: Identifier 'ERROR_SELECTOR' has already been declared
```

Three modules each declare `ERROR_SELECTOR` at top level:

| behavior | value |
|---|---|
| `contact-form` | `'[data-contact-error]'` |
| `account-portal` | `'[data-account-error]'` |
| `account-chrome` | `'[data-account-chrome-error]'` |

`tools/generate/src/cli/module-assets.ts:93` composes the bundle as `jsParts.join('\n\n')` — a flat concatenation into **one module scope**. Two `const`s with the same name in one scope is a parse-time error, so the browser discards the entire file before executing a single line.

## Why it looked like a design choice

`account-chrome`'s no-JS baseline *is* "dialog inline, open, at the top of the page." That's deliberate — REQ-200's fail-visible discipline, so a broken script leaves an address you can still submit. The script is what sets `data-account-chrome-enhanced` and folds the panel away.

So the symptom you saw is precisely the documented degradation path. **Fail-visible worked exactly as designed — which is why five days passed without anyone noticing it was failing.** The product AI then reverse-engineered a plausible story for a page that was, in fact, simply broken.

## Blast radius

Wider than the modal. Nothing in `capabilities.js` has run since **2026-09-05** (`03f961b34c`, where `account-portal` landed the second `ERROR_SELECTOR` next to `contact-form`'s), on **every site, draft and published**:

- **account-chrome** — dialog never closes, never becomes an overlay ← what you hit
- **contact-form** — your beta form falls back to a native POST that navigates the visitor off the page
- **carousel** — no autoplay, no dot indicator
- **account-portal** — identity and holdings lines stay blank, erasure text stays open

## Why nothing caught it

Every module's `client.js` is unit-tested **individually**, by importing the source. The REQ-145 UAT checks that `module-assets.ts` still matches its sources — byte equality, not validity. Nothing anywhere parses the composed bundle. Each file is correct; the composition is not, and the composition is what ships.

## The fix

Renaming the constant would work today and re-break on the next module that picks a common name. The durable fix is to give each behavior its own scope during composition — strip `export ` (the bundle is self-wiring; exports exist only for the unit tests, which import sources directly) and wrap each block in `{ }`, which is enough because `const` is block-scoped. I prototyped it against the real bundle:

```
behaviors wrapped: 4
PARSES CLEAN
```

Plus a UAT that parses the composed bundle, so this class of failure can't return silently.

**REQ-212 is unaffected** — the L1 dialog emits its own inline `<script>` in the body (`render.ts:2794`) rather than riding in `capabilities.js`, so modals authored the new way were never touched by this.

## Your two points still stand

Neither is this bug, and both are still worth doing:

1. **Edit should show what View showed** — switching modes shouldn't reset page state. Today Edit ships no JS at all, so there's no state to carry. Real work, real design question.
2. **The AI should be able to drive the page before shooting** — click Sign In, then screenshot. It genuinely can't today, and that's the gap that let it guess instead of look.

## How I'd like to proceed

This is a defect in the asset composer, separate from REQ-212's modal work — it wants its own bug ticket. **Want me to file one?** I won't create it without you saying so.

Once you confirm, I'd suggest: fix the bug first (it's small, verified, and unblocks your sign-in modal immediately), then take (1) and (2) as their own scoped pieces.

<!-- xgd-turn id="1f6380a5-50aa-40aa-a9c9-0b0a3a3610a8-user" -->

<!-- xgd-chat role="user" ts="2026-09-10T20:21:10.624912+00:00" -->
#### You
yes please create two REQ tickets for the extended functionality and fix the bug here please

<!-- xgd-chat-end -->
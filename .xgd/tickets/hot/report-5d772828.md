---
uid: report-5d772828
id: REPORT-927
type: report
title: 'UAT Coverage: L1 Layout Substrate + Safety Envelope'
created_by: xgd
created_at: '2026-07-24T09:25:35.831706+00:00'
updated_at: '2026-07-24T09:25:35.831706+00:00'
completed_at: null
last_field_updated: created_at
result: pass
fields:
  report_kind: uat_coverage_check
  subject_uid: capability-ae9d65d6
  violations: 0
  warnings: 0
  needs_review_count: 0
---

# UAT Coverage Assessment: L1 Layout Substrate + Safety Envelope

**Result**: PASS
**AC verdicts**: 7 pass, 0 fail, 0 deprecated, 0 needs_review
**Story verdicts**: 1 pass, 0 fail, 0 stale, 0 needs_review
**Capability verdict**: pass

## Cumulative Intent Considered

Story-83's `intent_uid` is BUNDLE-7 (bundle-31e474b9), the framework-pivot bundle,
`free_and_reconciled` (merged at edeb1c2c). It carries the founding intents of the
L1 substrate. All count toward cumulative intent; none is retired by a later intent.

| Intent ID | Status | Asked / changed | Counts? |
|---|---|---|---|
| REQ-63 | free_and_reconciled (bundled) | Coverage audit: capture+diff every render-affecting CSS axis | YES |
| REQ-79 | free_and_reconciled (bundled) | Framework pivot: introduce L1 layout substrate + capability modules (safety envelope) | YES |
| REQ-82 | free_and_reconciled (bundled) | L1 substrate B1: schema, safe renderer, envelope validator, round-trip gate | YES |
| REQ-83 | free_and_reconciled (bundled) | B2: capture→L1 fold (keyframes + oracle) + structural-hint extractor | YES |
| REQ-84 | free_and_reconciled (bundled) | C: strip former semantic layout modules to L1 (delete hero/text-block/etc.) | YES |
| REQ-85 | free_and_reconciled (bundled) | D: capability-module contract; reframe carousel & contact-form | YES |
| REQ-86 | free_and_reconciled (bundled) | E: reproduce a site end-to-end in the new system (3-probe gate) | YES |

Result: current cumulative intent is exactly the L1 substrate the capability
describes — a typed element tree, an envelope validator, a single safe renderer,
per-viewport geometry keyframes, and a round-trip / cross-browser fidelity gate.
AC-684 records that it subsumes the retired AC-717 (per-breakpoint module dials,
removed post-REQ-79); that retirement is already reflected in the AC set.

## Alignment Ledger

| Story | Intents aligned to | Outcome | Notes |
|---|---|---|---|
| STORY-83 | REQ-79, REQ-82, REQ-83, REQ-84 (bundle-31e474b9) | aligned | Body describes the typed shape, validator, safe emitter, geometry keyframes, round-trip and cross-browser guarantees — every behavior supported by reconciled intent; nothing stale. |

## Findings — Categorized by Editor Action

None. Zero violations, zero warnings, zero needs_review.

Every AC is active per cumulative intent and is substantively covered by a passing
UAT that drives real entry points (`validateL1`, `renderL1Document`/`renderL1Page`,
and real-browser `captureL1` across chromium/webkit/firefox). No test is trivial,
over-mocked, or structural.

| AC | Behavioral claim | Covering UAT (real entry point) | Verdict |
|---|---|---|---|
| AC-682 | Well-formed typed doc accepted; each optional primitive accepted | `test_UAT_AC682_valid_document_and_optional_primitives_accepted` — validateL1 on hero + 5 primitive variants (row/hug, grid/slot/image/visibility, snap track) | pass |
| AC-683 | Authored (Type-A) axes round-trip at every width | `test_UAT_AC683_type_a_axes_reproduced_and_text_present_at_all_widths` — real Chromium capture, zero Type-A deltas across all 6 widths | pass |
| AC-684 | Geometry: interpolate varies continuously, snap holds | `test_UAT_AC684_interpolate_varies_continuously_and_snap_holds` — real renderer CSS: fluid calc() vs static hold (+ req82 browser confirmation) | pass |
| AC-685 | Injection payloads inert in rendered output | `test_UAT_AC685_text_url_alt_and_fontfamily_payloads_are_neutralised` — script/alt escaped, js: src dropped, font-family CSS neutralised | pass |
| AC-686 | Out-of-range/oversize/freeform rejected; boundary is the range | `test_UAT_AC686_envelope_boundary_is_the_range_not_the_property` — 12 distinct violations rejected + in-range positive control | pass |
| AC-687 | Rejected doc returns full per-field error list (path+message) | `test_UAT_AC687_multiple_violations_all_reported_with_path_and_message` — asserts ≥3 errors, each with `/`-path + message, specific paths present | pass |
| AC-688 | Renders equivalently across chromium/webkit/firefox | `test_UAT_AC688_no_layout_divergence_across_three_engines` — projection per engine, pos/width within tolerance, identical font-size | pass |

## Notes for the Editor

- **No action required.** Coverage is complete and green.
- **Index caveat (not a finding):** `.xgd/uat_index.json` reports these tests as
  `missing` and omits AC-683/AC-688 entirely. This is a phantom of a fresh
  regression worktree — the index was not rebuilt against the TypeScript test
  suite (`tests/reconciliation-l1-substrate.test.ts`, `tests/req82-l1-substrate.test.ts`).
  The tests exist, use the exact `test_UAT_AC<n>_*` naming, and were verified by
  running them: `vitest run` on both files = **15 passed, 0 skipped** (browser
  engines installed, so the round-trip and 3-engine probes genuinely executed).
  The verdict is grounded on the test files and their live execution, not the index.

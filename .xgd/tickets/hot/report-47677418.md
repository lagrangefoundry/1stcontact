---
uid: report-47677418
id: REPORT-2435
type: report
title: 'Capability-Intent Alignment: L1 Reproduction Pipeline: Fold & Acceptance Gate
  (level=story)'
created_by: xgd
created_at: '2026-08-20T12:33:14.216376+00:00'
updated_at: '2026-08-20T12:33:14.216376+00:00'
completed_at: null
last_field_updated: created_at
result: pass
fields:
  report_kind: capability_validation
  subject_uid: capability-2049c9ec
  level: story
  violations: 0
  warnings: 0
  needs_review_count: 0
---

# Capability-Intent Alignment: L1 Reproduction Pipeline: Fold & Acceptance Gate
# Level: story

**Result**: PASS
**Violations**: 0
**Warnings**: 0
**Needs review**: 0

Attempt 13. All six mutations of the previous cycle (`report-81daebe5`, answering
`report-3baffe05`) are **verified landed and verified code-true** — the first time
attempt 12's additions have been independently checked, since they were written
after the last assessment ran.

| Prior mutation (attempt 12) | Landed | Code-true |
|---|---|---|
| 1 — column paragraph rewritten: container + inset reproduce every **origin**, content cap reproduces the **extent** | yes | `fold.ts:393-398` (cap = extent wherever the column stopped growing), `:446-449` (`columnExtent` = `min(maxWidthPx, inner)`) |
| 2 — new paragraph "A column anchor is fitted per axis, not as one undivided thing" (+ nested cap, both admission conditions) | yes | `fold.ts:517-539` — `x = fitAxis(dxs, false)`, `width = fitAxis(…, true)`, then `if (x) anchor.x = x; if (width) anchor.width = width`; cap over-determined at `:508` (`below.length < 3`), sane fraction at `:442` (`|f| <= 2`) |
| 3 — the two no-closed-form rules (mode change keyframes only the residual inset and inherits `segments`; full-bleed never anchored) | yes | `fold.ts:519-533` — `if (segments) track.segments = segments`; full-bleed refusal is `frames.every((f) => f.box.width < f.at - 1)` |
| 4 — new paragraph "A reconstructed surface takes its rect from the element that painted it" (band guard, accent-bearer fallback, precedence, radius corollary, grouping identity) | yes | `fold.ts:1906-1921` — `shapeBoxAt` returns the surface only when `shape.width < at` (band guard); `shapeBoxAt(…) ?? (el.borderLeft ? el.accentBox : undefined)` (precedence); `surfShapeRadius` gated on `shapeBoxAt` (radius corollary). Grouping identity at `:1610-1630` — `skey` decides membership outright, proximity only arbitrates rows with no resolved surface |
| 5 — **In scope** clauses scrubbed to match both new paragraphs | yes | n/a (matrix text) |
| 6 — **Technical Context**: REQ-88 bullet extended with the two qualifiers; new bullet carrying the modal left edge + over-determined cap | yes | `fold.ts:369-373` — `modal(boxes.map((b) => b.x))`, with the comment stating "A real page has more than one gutter" |

No finding this cycle is a repeat, because there is no finding this cycle.

## Sweeps used this cycle

Four angles, three of which no prior cycle has run. Each is recorded with its
result so a future cycle can see what is spent.

1. **Intents newer than the entire prior ledger** (new — the prior ledger's newest
   entry was REQ-136, 2026-08-12, and today is 2026-08-20). Enumerated every
   `request` / `bug` / `bundle` ticket and read the section list of all three
   bundles created since. **Clean, no finding**, and this is the material negative
   result of the cycle: `bundle-77b28def` (2026-08-18) is status **`reconciling`**,
   i.e. *imminent* and therefore ledger-counting, so had it touched this capability
   it would have been a coverage gap by definition. It does not — its nine sections
   (REQ-133 palette popup, BUG-35 capitalisation preview, REQ-131 draft change
   journal, REQ-140 editor colour, REQ-139 locked controls, REQ-123 system KB,
   REQ-141 workerd UATs, REQ-144 build/deploy scripts, REQ-142 async SiteStore) are
   builder / editor / Cloudflare-runtime work with no fold, gate, oracle or
   reproduction surface. `bundle-d9226698` (BUG-34 + REQ-137, palette shade) and
   `bundle-e59210c5` (REQ-119…REQ-130, control-surface API and editor) are likewise
   off-capability. The capability's intent frontier genuinely stops at REQ-136.

2. **The operator dialogue on REQ-88** (new — the prompt names comments as the
   secondary source of truth in the chain of authority, and no prior cycle read
   them). `COMMENT-258` on `request-7ff1bacd` is **349,000 chars, 144 turns, 74 of
   them the operator's own**. Extracted and read every operator turn
   (`.xgd/tmp/users13.py`). **Clean, no finding** — every ask that shapes this
   capability is already expressed:
   - the idempotence identity, asked verbatim by the operator on 2026-07-23
     ("`value-render(X) == value-render(value-render(X))`" and "B1 … should include
     idempotency tests") → STORY-86's "The fidelity verdict rests on an
     **idempotence identity**";
   - "we need at least one hero capture at a different size so we can 'see' that
     connection. It was an important and deliberate design feature" (2026-07-25) →
     STORY-84's "The ladder has a second sampling axis: the viewport's HEIGHT";
   - "the module is making layout decisions which should not do … How do we make
     the capability module completely layout agnostic?" (2026-07-26) → became
     REQ-96, whose `control` node kind STORY-84 correctly defers to CAP-70;
   - the vocabulary turn (2026-07-23, "Target / Local / capture / rendered value
     set / fold / structural vs flat properties") and "I want you to create a
     glossary" resolve to a **doc** deliverable, not story behaviour — the operator
     ruled in the same session that "ALL docs should be doc tickets". Not a story
     coverage gap.
   Comments on REQ-94 (`COMMENT-524`) and REQ-96 (`COMMENT-525`) were also read:
   one and two operator turns respectively, both bare go-aheads ("Please free code
   a fix to this", "Please implement this change to modules"), carrying no nuance
   absent from the bodies.

3. **Mechanical identifier consistency sweep across both story bodies** (new).
   Extracted every backticked token from STORY-84 and STORY-86 and resolved each
   against a 110-file, 1.5M-char corpus of `tools/generate/src`,
   `packages/framework/src` and `packages/site-schema/src`, read as bytes and
   decoded lossily so the NUL-bearing sources are not silently skipped
   (`.xgd/tmp/idents13.py`). **19 tokens in STORY-84, 13 in STORY-86, 0 unresolved.**
   No story body names a flag, verb or symbol that no longer exists — the classic
   stale-matrix consistency failure is absent.

4. **Code verification of attempt 12's six additions** (new — they post-date the
   last assessment). Every cited site read directly; results in the table above.
   All six are code-true.

**Evidence run.** `npm test -- tests/req88-viewport-relative-and-nowrap.test.ts` →
**21 passed / 21**, 1.00s (vitest 4.1.9). Every behaviour attempt 12 newly described
is live and pinned.

## Cumulative Intent Considered

Statuses read live this cycle (`.xgd/tmp/st13.py`), not inherited from the prior
report.

| Intent ID | UID | Status | When | Asked / changed | Counts? |
|---|---|---|---|---|---|
| REQ-79 / REQ-82 / REQ-83 | `bundle-31e474b9` | free_and_reconciled | 2026-07-22 | Framework pivot; L1 substrate + safe renderer; the capture→L1 fold. Originating `intent_uid` of both stories | YES |
| REQ-86 | — | free_and_reconciled | 2026-07-22 | The 3-probe end-to-end reproduction gate | YES |
| **REQ-88** | `request-7ff1bacd` | free_and_reconciled | 2026-07-21 | The operator-facing pipeline: `repro`, `l1-gate`, padding/type tracks, no-wrap threshold, centred column, height probe, `mounted` channel, ladder-only oracle. Its Round-5…Round-8 column/anchor/surface-rect work was the previous cycle's finding set and is now expressed | YES — **expressed, no gap** |
| BUG-5 / BUG-6 / BUG-7 / BUG-9 | — | free_and_reconciled | 2026-07-23 | Occurrence-index pairing; typed residual signal; row tiling; region-aware recursive promotion | YES |
| BUG-11 / BUG-13 / BUG-17..BUG-22 | — | free_and_reconciled | 2026-07-23/24 | Surface fills; section-background boxes; padding; responsive type track; bar band; the two self-painting-run families; captured surface shape | YES |
| BUG-14 | `bug-29b55835` | free_and_reconciled | 2026-07-23 | Section-band → card → text reconstruction, and its consequence for the gate's non-text pairing queue | YES |
| BUG-23 | `bug-3bf390f7` | free_and_reconciled | 2026-07-24 | Asset localization: hard failure on an unmirrored handle **and** the unreferenced-mirror fold gap | YES |
| BUG-24 | — | free_and_reconciled | 2026-07-24 | Fold-side scrim | YES |
| **BUNDLE-10** | `bundle-4ff83a8b` | free_and_reconciled | 2026-07-29 | Reconciliation vehicle carrying BUG-12..BUG-24 and REQ-88's Round-5..GA-round-10 passes | YES — fully mined over cycles 11–12; nothing further this cycle |
| REQ-90 / REQ-91 / REQ-92 | — | free_and_reconciled | 2026-07-23 | Font resource table; text pixel-movers; full-language rebuild + non-text pairing key | YES |
| REQ-93 | `request-f26cbe32` | free_and_reconciled | 2026-07-25 | L1 pages host behavior modules in their slots; derive module config from the capture | YES |
| REQ-94 | `request-16253634` | free_and_reconciled | 2026-07-25 | Gate calibration — a clean value gate must not outvote a failing perceptual one; the cross-gate verdict | YES |
| REQ-96 | `request-3a064234` | free_and_reconciled | 2026-07-26 | Behavior modules layout-agnostic by construction: the L1 `control` node; offline re-fold; submit button's per-width position | YES |
| REQ-97 / REQ-98 / REQ-103 / REQ-114 | — | free_and_reconciled | 2026-07/08 | `sizing` + `constrainWidth`; uniform paint axis group; gradient branch; palette model | YES (axes owned by CAP-70) |
| REQ-104 | `request-d67ea520` | free_and_reconciled | 2026-07-27 | Layout track + wrapping row | YES (deferred to CAP-70, correctly) |
| BUNDLE-11 | `bundle-ee56a66e` | free_and_reconciled | 2026-08-05 | BUG-27 + REQ-94 + REQ-96 + REQ-97 + REQ-98 + 10 more; `updated_by` on STORY-86 | YES |
| REQ-136 | `request-8a132869` | free_and_reconciled | 2026-08-12 | Image framing + colour adjustment; `updated_by` on STORY-84 | YES |
| **BUNDLE-14** | `bundle-77b28def` | **reconciling** | **2026-08-18** | REQ-133/BUG-35/REQ-131/REQ-140/REQ-139/REQ-123/REQ-141/REQ-144/REQ-142 — palette popup, editor colour + locked controls, change journal, system KB, workerd UATs, build scripts, async SiteStore | **imminent — but off-capability (new check, no gap)** |
| BUNDLE-13 | `bundle-d9226698` | free_and_reconciled | 2026-08-13 | BUG-34 + REQ-137 (palette shade) | NO (off-capability) |
| BUNDLE-12 | `bundle-e59210c5` | free_and_reconciled | 2026-08-10 | REQ-119..REQ-130 (control-surface API, chat panel, copy modal) | NO (off-capability) |
| CAP-73 | `capability-8108afab` | deprecated | 2026-07-22 | Archived half of the 2026-08-05 consolidation; reconciled into CAP-71 last cycle | NO (retired, merged) |
| REQ-134 | `request-ba3e3fba` | abandoned | 2026-08-12 | Image-generation component | NO |
| BUG-15 / BUG-16 / BUG-25 | — | free_and_reconciled | 2026-07 | Capture-side / values-diff | NO (CAP-63) |

**Ledger note.** The prior cycle's note — that BUNDLE-10 was the last large unmined
region and that it did not expect a third pass to yield more — is discharged and
confirmed. This cycle deliberately did *not* re-mine BUNDLE-10; it went after the
two angles no cycle had touched (intents newer than the ledger, and the operator
comment threads) plus a mechanical stale-reference sweep. All three came back clean.
The capability's intent surface is, as far as this assessor can establish, fully
expressed.

## Alignment Ledger

| Element | Intents aligned to | Outcome |
|---|---|---|
| STORY-84 (`story-8acc338d`) | REQ-83, REQ-88, REQ-90/91/92/93, REQ-96, REQ-103, REQ-114, REQ-136, BUG-6, BUG-11, BUG-13, BUG-14, BUG-17..BUG-24 | **aligned.** Attempt 12's six mutations verified landed *and* verified code-true against `fold.ts` (table above). Body 33,728 chars. Both halves now complete: the behaviour-seam half (closed at attempt 11) and the geometry-derivation half (closed at attempt 12). Zero unresolved identifiers. First clean cycle for this story |
| STORY-86 (`story-24098299`) | REQ-86, REQ-88, REQ-92, REQ-94, REQ-97, REQ-104 (deferred), BUG-5, BUG-7, BUG-9, BUG-14 | **aligned.** Untouched by attempts 11 and 12 and re-checked this cycle against the newer-intent sweep, the REQ-94 comment thread and the identifier sweep — no finding. **Third consecutive clean cycle** |
| Capability body (CAP-71) | REQ-86, REQ-94 | **aligned.** The four Scope bullets (fold, 3-probe gate, demand-driven recovery, cross-gate verdict) each summarize a claim the stories make and introduce none they do not; Out-of-scope mirrors STORY-86's and correctly defers the perceptual and value eyes' own measurement contracts. CAP-73's archived body was reconciled in last cycle and is `deprecated` |

**Consistency**: clean — no story text references retired behaviour, and no
backticked identifier in either body fails to resolve in the codebase.
**Coverage**: clean — every reconciled and every imminent intent in the ledger has
its asked behaviour expressed somewhere in the two-story tree, including the
newly-checked `reconciling` BUNDLE-14, which asks nothing of this capability.
**Exclusivity**: clean — the fold/gate split holds. STORY-86's Out-of-scope defers
"the fold itself, including which residuals it emits" by name, and everything
attempt 12 added to STORY-84 is fold-side geometry derivation, so the new text
moved no boundary. Nothing crosses into CAP-70: the column/anchor *axis vocabulary*
is CAP-70's, while every rule STORY-84 states is about what the **fold derives**.

## Findings

| # | Severity | Property | Element | Resolution category | Issue | Suggested edit |
|---|---|---|---|---|---|---|
| 1 | info | consistency | `tools/generate/src/l1/fold.ts:451-458` | — | **Not a matrix finding — recorded so it stops costing cycles.** `fitAnchor`'s doc comment still asserts the pre-Round-8 coupled behaviour: "Returned only when the fit reproduces every sample to within a pixel *on both axes*. **Both**, because the renderer takes `x` and `width` from the anchor together". The code four lines below contradicts it (`:535-539` returns an anchor when *either* axis fits), as does the UAT at `tests/req88-viewport-relative-and-nowrap.test.ts:409`. The matrix is now correct and the code is correct; only this comment is stale, so it is neither a `code-issue` (the code does what the matrix describes) nor a matrix repair. Confirmed still present this cycle. Third cycle running it has been observed | None required for this gate. Worth a separate free-coded comment fix — it is very likely why the per-axis anchor rule survived eleven cycles unnoticed, and it will mislead the next reader auditing the fold from its own comments |

## Notes for the Editor

**Nothing to edit.** This level passes; no story body, AC or test needs a change as
a result of this check.

Two notes for whoever picks this capability up next:

- **The three sweep angles spent this cycle are genuinely spent.** Intents newer
  than the ledger will need re-running only when a new bundle lands (the frontier
  is currently `bundle-77b28def`, 2026-08-18, `reconciling`); the operator comment
  threads on REQ-88 / REQ-94 / REQ-96 are now fully read and carry nothing further;
  the identifier sweep is mechanical and can simply be re-run
  (`.xgd/tmp/idents13.py`). Combined with the prior cycles' spent angles — the
  archived CAP-73 body, the `*Options` input surface, the reported output fields,
  and every round-pass section of BUNDLE-10 — this capability has no large unmined
  intent region left that this assessor can identify.
- **The `fitAnchor` stale comment (finding 1) is the one loose thread**, and it sits
  in code rather than in the matrix. It does not belong to this gate, but a
  free-coded comment fix would remove the single most likely source of a future
  false finding in this region.

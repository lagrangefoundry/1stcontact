---
uid: report-88982a68
id: REPORT-2473
type: report
title: 'Fix Site Storage Port: One Async Store Behind Every Edit (ac) — attempt 5'
created_by: xgd
created_at: '2026-08-20T16:33:47.317891+00:00'
updated_at: '2026-08-20T16:33:47.317891+00:00'
completed_at: null
last_field_updated: created_at
fields:
  report_kind: fix_structural_validation
  subject_uid: capability-c4c7a854
  level: ac
  fixes_applied: 1
  progress_made: true
  needs_more_work: false
  violations_remaining: 0
  anchor_report_uid: report-2485c83c
---

# Fix Summary — Site Storage Port: One Async Store Behind Every Edit (ac)

**Attempt**: 5
**Fixes applied this call**: 1
**Violations remaining**: 0
**Needs more work**: false

REPORT-18a88eb5 carried exactly one violation and prescribed exactly one action:
**delete**, not re-scope. That is what was applied. Findings 2–7 are all `info` with
resolution "none" (or "none at this level"), and were left alone deliberately.

## The premise was re-verified in the tree before editing

The report's central claim is that the two prior fix attempts failed because the boundary
was drawn from a report's assertion about provenance rather than from the code. Rather than
trust the same class of evidence a fourth time, the disambiguating commit the report names
was read directly — `git show 2b902ead0^:tools/generate/src/cli/preview.ts`, the commit
immediately before the port landed. It confirms all three elements of the deleted claim
predate this capability:

| Element of the deleted claim | Present at `2b902ead0^` |
|---|---|
| `DraftSnapshot.stamp` + its "picked up on the next request rather than needing the server restarted" docstring | yes, line ~41 |
| `PreviewRenderer` memoised per `(slug, channel)`, "invalidated by the store's stamp … checked before the cache is read, not on a timer" | yes, lines ~113–117 |
| `cache = new Map<string, { stamp: string; rendered: RenderedSite }>()` and `if (hit && hit.stamp === snapshot.stamp) return hit.rendered` | yes, lines ~120 and ~172 |
| Per-request re-ask: `const snapshot = this.store.load(slug)` inside `file()` | yes, line ~135 |

REQ-142's contribution at this site is the interface the renderer reads through
(`DraftStore` → `SiteStore`) and the snapshot's payload shape (`loaded: LoadedSite` →
`result: LoadResult`). The cache, its invalidation rule and the per-request re-ask are
untouched. The assessor's recommendation stands; deletion was correct.

## Actions Taken — by Resolution Category

| # | Category | Element | Action |
|---|---|---|---|
| 1 | ac-edit | AC-1327 (`acceptance_criterion-16093733`) | Deleted bullet 4 ("The preview re-asks the store on each request; its memoised render is invalidated by the store's own stamp…") and the final Verification sentence ("Change the definition the store holds and assert the store answers with a different stamp…"). Rewrote the hand-off paragraph so it hands off the *mechanism* as well as the outcome, removing the misattributing sentence "This capability owns only the store-shaped half…", and cited `2b902ead0^` as the disambiguator so a future pass reading only `origin/main` cannot re-derive the same wrong conclusion. Bullets 1–3 and their three Verification sentences preserved byte-for-byte per finding 2. |

AC-1327 now claims exactly three things — renders from a store with no filesystem tree, an
asset resolves to bytes plus a content type, an absent asset resolves to nothing — and
nothing about freshness at any layer.

## Parallel edits considered and deliberately NOT made

- **STORY-118 body — no edit needed.** Grepped the full body (10,971 chars) for
  `stamp|cache|fresh|memois|restart`. It carries no freshness claim of its own; line 111 is
  the Technical Context sentence that *hands* freshness to CAP-85 ("CAP-85's builder origin
  owns request confinement and freshness, not the store's shape"), which the deletion makes
  the AC tree consistent with rather than inconsistent. The In-scope bullet at line 82 (the
  preview "driven through the store they were given") remains carried by AC-1327 bullets 1–3
  plus AC-1354. No coverage gap opened.
- **CAP-85's AC-1033 — untouched**, as instructed by both REPORT-2470 and this report.
- **AC-1321's stamp clause — untouched**, per finding 3 (in bounds: it describes the shape of
  the store's *answer*, which is the port's deliverable, not a consumer's caching policy).
- **All eleven sibling ACs scanned** for residual `memois|render cache|invalidat|restart|stale`
  claims: zero hits. The pattern the report tracks (four instances across AC-1327 and AC-1329)
  has no fifth instance latent in the tree.

## Code Edits

None this call. `preview.ts` was read at a historical commit for verification only; nothing
was modified.

## Tests

None run. Per finding 6, this worktree's HEAD (`d0d0fd1ea`) predates BUNDLE-19's merge — the
port modules are absent from `tools/generate/src/store/`, so the tests these ACs describe
cannot be hosted or run here. This is a known, recorded constraint, not drift. The mutation
this call is ticket-only and has no test surface.

## needs_review Items Forwarded

None. The single violation was actionable and is resolved.

## Carried forward to the uat level (not actionable at level=ac)

- Finding 5: AC-1353 and AC-1354 still have no `test_UAT_AC1353_*` / `test_UAT_AC1354_*` on
  `origin/main`. AC-1353's evidence exists under an intent name
  (`tests/test_UAT_FC_REQ-142_site_store_port.test.ts`) and needs re-pointing; AC-1354 needs
  authoring.
- Finding 1's exclusivity note: before authoring any UAT for AC-1327, check CAP-85's AC-1033
  first — it already carries `uat_coverage: pass` for the freshness experiment, and the
  deletion above exists precisely so a second proof of it is not authored here.

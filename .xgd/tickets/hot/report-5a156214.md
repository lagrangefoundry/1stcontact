---
uid: report-5a156214
id: REPORT-4277
type: report
title: 'Reconcile resolve conflicts: reconcile-BUNDLE-27'
created_by: xgd
created_at: '2026-09-18T04:10:23.780723+00:00'
updated_at: '2026-09-18T04:10:23.780723+00:00'
completed_at: null
last_field_updated: created_at
result: pass
fields:
  report_kind: resolve_conflicts
  subject_uid: reconcile-BUNDLE-27
---

## Summary

The picked commit `a28d2f522f` (REQ-148, "behavior modules render in workerd —
Astro leaves the render path") is a **remapped duplicate** of
`36afb0a80daff2c5a6502d779e61e96eea28d6b8`, which is already an ancestor of HEAD.

Evidence:
- Same author, same author-date (`2026-08-19 17:44:20 -0700`), same subject.
- `git diff --stat a28d2f522f 36afb0a80d -- <all six conflicted paths>` is **empty**
  — the two commits' versions of every conflicted file are byte-identical.
- `git merge-base --is-ancestor 36afb0a80d HEAD` succeeds.
- `git diff --cached --stat HEAD` listed only the six conflicted paths as
  `Unmerged` and otherwise `0 files changed`: every one of the commit's other
  52 files auto-merged to exactly HEAD's content, which is what a
  re-application of already-integrated work looks like.

So REQ-148's content is present in HEAD, and each conflict is HEAD's **later
refinement** of that content meeting the older text as "theirs". All six files
resolved to HEAD's side; the staged tree is byte-identical to HEAD, so this pick
nets to no diff (BUG-1109/BUG-1122 — staged and exiting @done as instructed,
`--skip` deliberately NOT called).

## Files resolved

All six are `UU` on code files, rule 2c, resolved under 2c's *both sides are
`free_coded`* exception (take the later working-timeline position) plus, for the
two hunks noted below, the BUG-1301 PRECEDENCE note.

- `apps/control-app/src/router.ts` — UU, 3 hunks. 2c.
  Incoming's REQ-148 change (drop the `render?` dep, drop the `RenderSiteOptions`
  import, drop the `render` argument from `previewRenderer`/`servePreview`) is
  present in HEAD. HEAD adds: `export` on `previewRenderer` (`1213d247dd`,
  REQ-154), lazy `await openStore()` at the preview route (`d01e1e1891`,
  REQ-149), and the `tickets`/`knowledge`/`index`/`describeImage`/`fetch` deps
  (REQ-158/REQ-163).
- `packages/framework/src/modules/behavior.ts` — UU, 2 hunks. 2c.
  Incoming's REQ-148 change (drop `AstroComponentFactory`, add `BehaviorProps`
  and `BehaviorComponent`, retype `BehaviorDefinition.Component`) is present in
  HEAD. HEAD adds `locale?: ResolvedLocale` (`bbce12ddd4`, REQ-151).
- `tools/generate/src/render/render.ts` — UU, 4 hunks. 2c.
  Incoming's REQ-148 change (Astro gone from the file, `getModule` imported
  statically from `@1stcontact/framework/worker`, `Component` called as a plain
  synchronous function) is present in HEAD. HEAD adds locale threading through
  `renderModuleInstances`/`renderPage` (`bbce12ddd4`, REQ-151) and re-sources
  `LoadedSite` from `../store/assemble` (`94983a2b61`, REQ-149).
- `tools/generate/src/cli/builder.ts` — UU, 1 hunk. 2c.
  Incoming's REQ-148 change (drop the `render: { createContainer, resolveModule }`
  dep and the `astroContainer`/`getModule` imports) is present in HEAD. The one
  incoming line not in the result is `importStore: store` — see below.
- `tests/reconciliation-1c-astro-free-render.test.ts` — UU, 3 hunks. 2c/2f.
  Incoming's REQ-148 prose and its section labels are present in HEAD; HEAD
  appends the REQ-150 paragraphs and adds the AC-739 static-scan clause (a)
  (`550dca2bc4`), renumbering incoming's `(a)` to `(b)`. No test function from
  either side was deleted.
- `tests/req89-astro-lazy.test.ts` — UU, 3 hunks. 2c/2f.
  Same shape: incoming's REQ-148 prose present, HEAD appends REQ-150's. No test
  function from either side was deleted.

## Incoming changes preserved

Verified in the resolved working tree, not asserted:

- `builder.ts` — `astroContainer`, `getModule`, `createContainer` and the
  `render:` dep entry are all absent (grep, 0 hits). REQ-148's deletion landed.
- `router.ts` — `RenderSiteOptions`, `render?:`, `deps.render`, `createContainer`
  all absent (0 hits); `previewRenderer(store: TenantSiteStore)` at :164 and
  `servePreview(store, slug, channel, rel)` at :1174 both carry REQ-148's
  single/render-free signatures.
- `behavior.ts` — `AstroComponentFactory` absent; `export interface BehaviorProps`
  (:164), `export type BehaviorComponent = (props: BehaviorProps) => string`
  (:196) and `Component: BehaviorComponent` (:201) all present, with REQ-148's
  own docstring ("props in, HTML out (REQ-148)", :187).
- `render.ts` — `renderSiteFilesNode` and `createContainer` absent; the
  "ASTRO IS GONE FROM THIS FILE (REQ-148)" header, the static `getModule` import
  from `@1stcontact/framework/worker` (:22) and `opts.resolveModule ?? getModule`
  (:288) all present; `Component({...})` is the plain synchronous call (:121).
- Both test files — REQ-148's superseding prose present; the module-page
  assertion still asserts *no* container for a behavior-module page, which is
  the flip that IS the ticket.
- `./node_modules/.bin/tsc --noEmit -p tools/generate` — 16 errors, **zero in
  any of the six resolved files**. All 16 are the missing `src/generated/*`
  worktree artifact (build-produced, absent in a fresh worktree) and its
  downstream `CoRankedKnowledge._declared`; this is the unmodified HEAD
  baseline, since the staged tree equals HEAD byte-for-byte.
- No conflict markers remain in any of the six (grep for `^<<<<<<<`/`^>>>>>>>`,
  0 hits; `builder.ts` scanned with `grep -a`, as it carries NUL bytes and reads
  as binary).

### Incoming text intentionally not carried (4 hunks of 16)

1. **`importStore` — 2 hunks** (`builder.ts` `importStore: store`;
   `router.ts` `RouterDeps.importStore?`). Removed from both files by
   `f84d4a46c7 fix(control-app): register the configured tenant so a fresh
   builder boots [FREE-CODED]` (2026-08-23), confirmed by
   `git log -S'importStore' 36afb0a80d..HEAD` on each path. That commit is a
   **descendant of the incoming commit's twin** (`git merge-base --is-ancestor
   36afb0a80d f84d4a46c7` succeeds) and is itself `free_coded`, so 2c's
   both-sides-`free_coded` rule takes the later working-timeline position: HEAD.
   Not a discard — restoring `importStore` would revert a later developer commit.

2. **`expect(createSpy).not.toHaveBeenCalled()` — 2 hunks**
   (`req89-astro-lazy.test.ts:146`, `reconciliation-1c-astro-free-render.test.ts:226`).
   **Dropped under the BUG-1301 PRECEDENCE exception.**
   - HEAD-side commit that already removed the target:
     `2d10299709 refactor(1c): boot a plain Vite SSR server and drop Astro
     [FREE-CODED]` (REQ-150, 2026-08-20), a descendant of `36afb0a80d`.
   - Why the removal is a legitimate refactor, not a resolution shortcut: that
     commit removed the `astro` dependency itself (verified: it is the commit
     that deletes `"astro"` from the package manifests, and the sole commit that
     adds `tests/support/astro-absent.ts`). `createSpy` spied on
     `experimental_AstroContainer.create`; with the package gone there is no
     longer any object to attach a spy to, so the hunk's entire target is gone.
   - The replacement is strictly stronger and documents itself as such —
     `expectNoAstroContainerToConstruct()` resolves `astro/container` via
     `createRequire` and asserts it throws, i.e. "no container can be
     constructed by ANY render" rather than the spy's "none was constructed
     during THIS render". Its docstring names REQ-148 and REQ-150 explicitly.
   - No test function was deleted on either side: every `it(...)` present in
     either version is present in the result. Only the assertion *mechanism*
     inside two of them differs, and the guarantee asserted is unchanged.

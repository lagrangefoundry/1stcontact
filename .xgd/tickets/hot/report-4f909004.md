---
uid: report-4f909004
id: REPORT-4304
type: report
title: 'Reconcile resolve conflicts: reconcile-BUNDLE-27'
created_by: xgd
created_at: '2026-09-18T05:24:04.409527+00:00'
updated_at: '2026-09-18T05:24:04.409527+00:00'
completed_at: null
last_field_updated: created_at
result: pass
fields:
  report_kind: resolve_conflicts
  subject_uid: reconcile-BUNDLE-27
---

## Files resolved

- `.xgd/tickets/hot/request-b88b79fe.md` (REQ-154) — class **AA** (both added; no stage-1 base). Intent/bookkeeping ticket, so **rule 2e** applies, resolved by the *strict superset* clause: `git checkout --ours` + `git add --sparse` (path is outside the sparse-checkout cone — `.xgd/tickets/**` is negated in `git sparse-checkout list`).

  Both sides carry the same `xgd(ticket): update request request-b88b79fe` subject, so the auto-enrichment marked intent unknown and prescribed "take the more recent commit by timestamp". Both the superset test and the timestamp test point the same way:

  - **Ours** — `d86cbfd7ca6766e50e7d0446ccc1015d92e887ae`, 2026-08-30 17:52:48 -0700; `status: bundled`, `updated_at: 2026-08-31T05:05:09Z`.
  - **Incoming** — `97327f55c1d75dfef7bf44d407e7b73949eef6e6`, 2026-08-23 16:26:15 -0700; `status: draft`, `updated_at: 2026-08-20T23:16:27Z` — the original draft of the request, created as a 98-line add.

  Ours is a strict superset per fact: it has every field the incoming side has (`priority`, `story_points`, `depends_on`, `auto_merge_back`, `needs_review`, `chat_comment`, all identical), plus `commits`, `version: 0.2.16` and `bundled_in: bundle-8eef3846`; and it carries the whole `# What was built` half the incoming draft predates. No field or section exists on the incoming side and not on ours, so there was no genuine per-fact conflict to put to `working-timeline`.

## Incoming changes preserved

Verified with `git diff -w --word-diff=plain <incoming blob> <ours blob>`. Every deletion in that direction is cosmetic only:

- markdown emphasis restyled, `*"…"*` → `_"…"_`, in four quotations (DOC-13 §8, DOC-13 §6 heading, the blank-screenshot parenthetical);
- paragraphs unwrapped from ~100-column hard wraps to single lines;
- list items separated by blank lines.

Every sentence, acceptance criterion (1–6), section heading and field value from the incoming commit is present verbatim in the resolved file. Nothing from the incoming side was dropped, and the BUG-1301 precedence exception was not needed — no hunk was dropped.

No code or UAT files were in this conflict.

## Net result

The staged tree is byte-identical to HEAD (`git diff --cached --stat HEAD` is empty). This is the redundant-commit case of BUG-1109/BUG-1122, not a discard: STEP 3's check distinguishes them, and the incoming commit's key content is *present* in HEAD (the full REQ-154 request body, reflowed, under a later bundling status) rather than *absent*. Per STEP 4 the resolution is staged and left for `cherry_pick_finalize_resolution`; `--skip` was not called and `CHERRY_PICK_HEAD` (`97327f55…`) is intact.

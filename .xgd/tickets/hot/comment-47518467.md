---
uid: comment-47518467
id: COMMENT-1295
type: comment
title: Claude response for capability_validation report
created_by: xgd
created_at: '2026-08-20T11:46:57.894879+00:00'
updated_at: '2026-08-20T11:46:57.894879+00:00'
completed_at: null
last_field_updated: created_at
status: null
fields:
  subject_uid: report-31eea910
  kind: note
---

Report created and schema verified: **REPORT-2425** (`report-31eea910`) — `result=fail`, `report_kind=capability_validation`, `subject_uid=capability-2049c9ec`, `level=story`, violations=3, warnings=2, needs_review=0.

## Verdict: FAIL — 3 violations, 2 warnings, 0 needs_review

Attempt 9's four repairs are **verified landed** in both story bodies (scrim + image-or-scrim condition, bar rule, `mounted` channel, ladder-only oracle). None of this cycle's findings is a repeat.

**Violations**

1. **STORY-84 / BUG-23** — Materialization reports a *second* asset channel that the story omits: mirrored `image`/`font` assets the folded document references nowhere, printed by `1c repro` as a **fold gap** (`assets.ts:35-41`, `repro.ts:53-58`, `cli/index.ts:567-572`). The story states only the converse (unmirrored handle → hard failure). Zero hits for `unreferenced` across all 31 story bodies. Distinct from STORY-86's gate-side coverage proxy, which asks the same question of the *reference manifest*.

2. **STORY-84 / REQ-93 scope item 3** — The fold/`repro` pair *derives* each seam's behavioural config from the capture (fields from the a11y accessible name, type from captured input type else height, endpoint from the captured form action) and records a **derivation gap** rather than inventing one — a channel the code deliberately keeps distinct from the fold residual (`forms.ts:97-105`, `:208-254`). REQ-93's items 2 and 4 are expressed; item 3 is not, anywhere in the matrix.

3. **STORY-86 / BUG-14** — Fold-synthesized backing surfaces are excluded from the fidelity probe's non-text pairing queue (`probes.ts:678-685`), and the story's rule says the opposite: it states non-text pairing unconditionally and enumerates the backing-surface exemption as overlap-only. This is the third exclusion mechanism and the only reproduced-side one.

**Warnings**: STORY-86's unconditional "the row's height is its tallest child" (REQ-104's wrapping row makes it the sum of lines — owned by STORY-81/CAP-70, so repair must defer rather than duplicate); and the overlap check exempting `slot` leaves as well as backing surfaces.

Two notes for the loop: all three violations are the same defect shape as last cycle's `mounted` finding — *a second reported channel on an already-expressed mechanism* — so I recorded a closed, high-yield sweep for the next cycle (enumerate every reported field on the capability's eight public return types). Each violation already has a passing `FC`-named UAT that needs re-attribution downstream rather than authoring. Ownership is settled for all five; nothing to escalate.

One environment note: the `xgd report create` git push failed (`Connection to UNKNOWN port 65535: Broken pipe`) — the report is committed locally but the remote is unreachable from this sandbox.

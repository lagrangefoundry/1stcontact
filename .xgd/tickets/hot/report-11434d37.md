---
uid: report-11434d37
id: REPORT-3682
type: report
title: 'Fix Site Storage Port: One Async Store Behind Every Edit (uat) — attempt 1'
created_by: xgd
created_at: '2026-09-10T06:35:06.115606+00:00'
updated_at: '2026-09-10T06:35:06.115606+00:00'
completed_at: null
last_field_updated: created_at
fields:
  report_kind: fix_structural_validation
  subject_uid: capability-c4c7a854
  level: uat
  fixes_applied: 6
  progress_made: true
  needs_more_work: false
  violations_remaining: 0
  anchor_report_uid: report-e37a6b4a
---

# Fix Summary — Site Storage Port: One Async Store Behind Every Edit (uat)

**Attempt**: 1
**Fixes applied this call**: 6
**Violations remaining**: 0
**Needs more work**: false

All three actionable findings (1 violation, 2 warnings) are resolved. Findings 4,
5 and 6 are `info` with resolution category `—` and were deliberately left alone —
each records *why* a test looks the way it does so a later editor does not "fix"
the claim out of it.

## Actions Taken — by Resolution Category

| # | Category | Element | Action |
|---|---|---|---|
| 1 | ac-edit | AC-1329 (acceptance_criterion-ae2c7f77) | Finding 1 (violation): rewrote bullet 1 — it no longer claims an Astro container-render path, and names REQ-148/REQ-150 as what replaced it, so the runtimes are described as separated by the filesystem rather than by a transform. Rewrote the Verification's first sentence to "Assert a behavior module renders in the filesystem runtime and that its configuration carries the same aliases and timeouts it had before the split, with no Astro transform on either project." Bullet 3 was left in place: it claims the Workers runtime carries no Astro transform, which `test_UAT_AC1329_…` still asserts (`expect(workers).not.toContain('astro')`). No test changed. |
| 2 | ac-edit | AC-1329 (acceptance_criterion-ae2c7f77) | Finding 3 (warning): demoted the failing-set bullet out of `## Criterion` into a new `## Recorded at reconciliation, not asserted` section, and dropped the matching sentence from `## Verification`. The AC's checkable surface is now exactly what `test_UAT_AC1329_…` checks; the historical comparison is retained as an observation and cross-referenced to STORY-118's Technical Context, which already records it. |
| 3 | uat-edit | tests/support/site-store-contract.ts:352 | Finding 2 (warning): renamed `UAT_FC_REQ-149 AC-1619 a site that has never published answers the revision verbs emptily` → `test_UAT_AC1619_revision_verbs_answer_emptily_before_any_publish`. No assertion changed. The `REQ-149` reference already lives in the block comment above the pair. |
| 4 | uat-edit | tests/support/site-store-contract.ts:366 | Finding 2 (warning): renamed `UAT_FC_REQ-149 AC-1619 a frozen revision lists, reads back and re-parents the draft` → `test_UAT_AC1619_a_frozen_revision_lists_reads_back_and_reparents_the_draft`. No assertion changed. |
| 5 | story-body-edit | STORY-118 (story-3f4a5f2b) | "Notes for the Editor", cross-cutting pattern: the "And the claim is checked where it will run" paragraph claimed the filesystem runtime "keeps the Astro container-render path". Rewritten to "keeps the component-render path", with a sentence naming REQ-148/REQ-150 and stating that what separates the two runtimes is now the filesystem rather than a build transform. `grep -i astro` over the story body now returns only that one deliberate historical reference. The `### In scope` list turned out to carry no Astro claim of its own (its runtime bullet names only routing, bindings and compatibility settings), so it needed no edit. |
| 6 | uat-edit (comments) | tests/support/site-store-contract.ts:48-54, tests/test_UAT_FC_REQ-142_site_store_port.test.ts:103-106 | "Notes for the Editor": both comments explained the two preview cases' node-only placement by "rendering runs through Astro's container API, which workerd has no transform for". Reworded so the *reason* is the filesystem workerd does not have, with the Astro history kept as a parenthetical and the placement explicitly stated as unchanged. Comments only — no assertion touched. |

## Verification

| Command | Result |
|---|---|
| `npm test -- tests/test_UAT_FC_REQ-142_site_store_port.test.ts` | 41 passed (1 file) — the contract body over the filesystem and memory adapters |
| `npm test -- tests/test_UAT_FC_REQ-142_site_store_port.test.ts -t AC1619 --reporter=verbose` | 4 passed, 37 skipped — both renamed cases run under the mandated name over **both** host-runtime adapters |
| `npm test -- tests/test_UAT_FC_REQ-143_d1r2_store.workers.test.ts -t AC1619 --reporter=verbose` | 2 passed, 24 skipped — the same two cases run under the mandated name over the **D1/R2** adapter in workerd, so all three adapters are now discoverable by a `test_UAT_AC1619_*` lookup |
| `npm test -- tests/reconciliation-site-storage-port.test.ts` | 9 passed — `test_UAT_AC1329_…` unchanged and green after the AC rewrite |

No test outside the rename was modified, and no assertion changed anywhere.

## Code Edits (if any)

None this call. All six mutations are matrix bodies, test *names*, and test
*comments*; no production source was touched.

## Deliberately Not Done

| Item | Why |
|---|---|
| Setting `uat_coverage` on AC-1619 | The uat-edit recipe suggests it, but `uat_coverage` is owned by check/fix_uat_coverage, and no AC on either of this capability's two stories currently carries the field at all — setting it on one AC would introduce a value nothing else in the capability uses. Finding 2 was a naming/traceability gap, not a coverage gap; the assessor can now derive coverage from the mandated name. |
| The pool-version pin comment in `vitest.workers.config.mts:28-34` | The report states explicitly that nothing at this level should touch it: STORY-118 records the divergence, and AC-1328/AC-1329 deliberately encode neither the pin nor its retracted rationale. |
| Findings 4, 5, 6 | `info`, resolution category `—`. Each documents why a test is shaped the way it is (assertion placement across the runtime split; AC-1325 vs AC-1385 asserting different surfaces; AC-1447/AC-1448 asserting object identity rather than equality). No action requested and none taken. |

## needs_review Items Forwarded

None. No finding in report-06854a25 was categorized `needs_review`.

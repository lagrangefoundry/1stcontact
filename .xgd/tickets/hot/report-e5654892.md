---
uid: report-e5654892
id: REPORT-1095
type: report
title: Claude reconciliation_uat_generation report
created_by: xgd
created_at: '2026-08-03T00:48:59.131007+00:00'
updated_at: '2026-08-03T00:48:59.131007+00:00'
completed_at: null
last_field_updated: created_at
fields:
  report_kind: reconciliation_uat_generation
  subject_uid: story-244827df
---

All 12 UATs pass, and the adjacent regression scope (59 tests across the capture/font/surface/probe families) stays green. No runtime code and no existing test file was touched — only the new UAT file and the fixtures its ACs' verification steps call for.

**What I had to set up:** Playwright's Chromium binary was not provisioned on this machine, so every browser-driven UAT in the suite was silently skipping. I installed it (`npx playwright install chromium`) so the new tests could actually be verified rather than reported as "passing" while skipped. The tests still guard on `chromiumAvailable()` and skip cleanly on a runner without the engine, matching the rest of the suite.

**Coverage notes per AC** (all verified against real captured values, not assumed):
- AC-740 drives the pipeline through the `BrowserDriver` seam with a blind CSSOM plus intercepted cross-origin bytes, and asserts the named face file exists on disk in the written bundle; plus the unmirrored (empty list) and same-origin/cross-origin union controls.
- AC-741/742/743 use real Chromium over loopback-served fixtures. The AC-743 control serves the *same bundle bytes* verbatim (no reference rewriting) and shows the very same run falls back — which is what proves the mirror resolution is doing the work.
- AC-745/746/751 read the persisted artifacts (`multistate.json`, `screenshot-<width>.png`) rather than in-memory returns, since the ACs are about what a bundle records.
- AC-748 exercises two distinct modern colour syntaxes (`color-mix(in oklab, …)` and `oklch(… / …)`), and asserts the veil's **alpha and presence** rather than exact channels — per the story's recorded BUG-24 divergence.

```
UATs generated for story story-244827df (plan item 1 of 9)

Story: story-244827df
Test file(s) created:
  - tests/reconciliation-capture-recording-contract.test.ts

Fixtures created (test assets):
  - tests/fixtures/capture/ac742-webfont.html
  - tests/fixtures/capture/ac742-missing-font.html
  - tests/fixtures/capture/ac745-surface-bearer.html
  - tests/fixtures/capture/ac746-accent-bearer.html
  - tests/fixtures/capture/ac748-scrim.html
  - tests/fixtures/capture/ac750-form-controls.html

Tests written: 12
Tests passed: 12
Tests failed: 0

test_files_created:
  - "tests/reconciliation-capture-recording-contract.test.ts"
```

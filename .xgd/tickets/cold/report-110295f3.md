---
uid: report-110295f3
id: REPORT-673
type: report
title: Claude fix_review_free_coded report
created_by: xgd
created_at: '2026-07-19T04:29:31.834807+00:00'
updated_at: '2026-07-19T04:29:31.834807+00:00'
completed_at: null
last_field_updated: created_at
fields:
  report_kind: fix_review_free_coded
  subject_uid: bundle-ab9e0cb6
---

All fixes are complete and verified. Here's the summary.

## Issues fixed

**CRITICAL-1 — dangling `--color-accent-mid` on the footer `accent-muted` surface** (blocking)
- Restored `accentMid: '#d98324'` (with its documenting comment) to `defaultTokens` in `packages/framework/src/tokens/defaults.ts:24` — so `--color-accent-mid` always resolves in `:root`, even when a site omits `accentMid`. This was deleted as accidental collateral in resync commit `6004c2df`.
- Restored the deleted guard test (`tests/req36-heading-treatment.test.ts:766`) asserting `generateThemeCss({})` declares `--color-accent-mid` — the invariant is back under test.
- Updated the token-count assertions in `tests/framework-tokens.test.ts:63,72` (88 → 89), since the restored token legitimately adds one custom property.

**Warnings (should fix)**
- Declared undeclared dials/content in module `meta.ts`:
  - `footer/meta.ts`: `textColor`, `linkColor` dials (role overlay via `TREATMENT_ROLE_DIAL`) + `copyright` content field.
  - `contact-form/meta.ts`: `submitColor` dial + `submitInline` dial.
  - `header/meta.ts`: `navCollapse` dial.
- Converted `submitInline` from the stringly-typed `=== 'true'` boolean to a proper enum dial `SUBMIT_INLINE_DIAL = ['stacked','inline']` (`dials.ts`), updating `contact-form/index.astro:49`, both tests, and the gigabytealchemy site JSON (`'true'` → `'inline'`). Also added `NAV_COLLAPSE_DIAL`.

**Nits (optional, done)**
- Removed dead `resolveStep` imports from `footer`, `header`, `services-grid`, `contact-form` `/index.astro`.
- Removed the dead `responsivePropertyRules` import from `text-block/index.astro` (`resolveStep` is used there, so it stayed).
- Deleted the superseded `padding-top/bottom: var(--space-4)` lines in `header/index.astro`, leaving only the `--fc-pt`/`--fc-pb` declarations.
- Restored the `resolveFamily` `\`/`"` escaping in `text-style.ts` (defense-in-depth against untrusted family names), also dropped accidentally in the resync.

## Verification
- Framework `tsc --noEmit`: clean (exit 0).
- Full vitest suite: **734/734 passed, 77/77 files** (confirms astro-module frontmatter compiles and no other references to the old dial value or removed imports remain).

## Confidence
High. The blocking CRITICAL is fixed with its guard test restored and independently exercised; all warnings and nits are addressed; the full suite is green and typecheck is clean. The only residual "verify" item from the report (the `resolveFamily` escaping) was restored rather than left ambiguous.

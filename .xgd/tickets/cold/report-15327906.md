---
uid: report-15327906
id: REPORT-1053
type: report
title: 'Code Review: bundle-cceaba25'
created_by: xgd
created_at: '2026-07-29T05:57:10.601134+00:00'
updated_at: '2026-07-29T05:57:10.601134+00:00'
completed_at: null
last_field_updated: created_at
result: pass
fields:
  report_kind: code_review
  subject_uid: bundle-cceaba25
  anchor_uid: bundle-cceaba25
---

# Code Review

**Result**: PASS

## Summary

Third review pass on bundle-cceaba25 (BUG-6/7/8/9/10/11 + REQ-89/90/91/92). All five findings from REPORT-1051 are genuinely fixed and I verified each against the current tree: the image-leaf `src` gate now signals a typed `FoldResidual` instead of throwing the fold (`fold.ts:601-620`), `usedFontFaces` drops an envelope-unsafe face rather than crashing (`fold.ts:111`), the overlap exclusion is narrowed to fold-synthesized `surface-*` boxes only (`probes.ts:352-363`), `rotateDeg` has its own envelope entry (`validate.ts:38,137-144`), and the duplicated `px(...)` calls are hoisted (`render.ts:387-393`).

I re-ran every gate independently: full vitest suite 98 files / 668 tests / 0 failed, `pnpm -r build` exit 0, `tsc --noEmit` clean on `packages/framework`, `packages/site-schema` and `tools/generate`, and the real `bin/1c` boots with an empty stderr on `--help` and `list` (REQ-89 acceptance) and renders the L1-only gigabytealchemy site with no Astro container. No new critical or blocking defect surfaced in my own pass over the diff; the remaining items are repo-hygiene and follow-up warnings.

## Quality Gates

| Gate | Status | Evidence |
|------|--------|----------|
| Lint | PASS | REPORT-1052 (report-2930216f): 0 errors, 0 warnings |
| Build | PASS | REPORT-1052 + independent `pnpm -r build`, exit 0 |
| Typecheck | PASS | `npx tsc --noEmit -p` on `packages/framework`, `packages/site-schema`, `tools/generate` — all exit 0 |
| Tests (scoped) | PASS | REPORT-1052: 54 passed, 0 failed, 0 skipped |
| Tests (full suite, run for this review) | PASS | `pnpm vitest run` — 98 files, 668 tests, 0 failed, 0 skipped, 39s |
| Coverage | PASS | REPORT-1052: threshold met |

## External Interface Accessibility

New entry points wired in: **yes**.

- Fold residual API exported: `foldToL1`, `classifyElement`, `isSynthesizedSurfaceId`, `SYNTHESIZED_SURFACE_ID_PREFIX`, `FoldResidual`, `FoldableElement`, `FoldLeafKind` (`tools/generate/src/l1/index.ts:14-22`).
- `foldResiduals` threaded `cmdL1Gate` → `L1GateResult` (`tools/generate/src/cli/repro.ts:116-146`) and printed on both the human and `--json` CLI paths (`tools/generate/src/cli/index.ts:375-384`, with a 10-row cap plus an explicit `… +N more`).
- REQ-90 font substance wired capture → fold (`capture.ts:15-33,82`) → `doc.resources` (`fold.ts:688-693`) → `@font-face` emission (`render.ts:128-153,557-563`).
- REQ-92 media substance threaded extract → field → element → fold (`extract.ts:143-149,843-845`; `sections.ts:157-159`; `types.ts:366-369`; `values-diff.ts:156-161,779-781`).
- REQ-91 axes reachable from the schema (`l1TextAxesSchema` / `l1BoxAxesSchema` / `l1ImageAxesSchema` + node-level `transform`/`mask`) through to renderer emitters — every family has an emitting branch in `render.ts`.
- REQ-89: Astro made lazy in `renderSite` (`tools/generate/src/render/render.ts:190-198`); launcher gates Astro's own logger via the second `getViteConfig` argument (`bin/1c.mjs:40`).

## Code Quality

| File | Finding | Severity |
|------|---------|----------|
| `tools/generate/src/l1/fold.ts:601-620`, `:111` | Prior critical (unsafe image/font `src` throwing the fold) fixed — both now signal/drop with a typed reason. Verified in source. | Resolved |
| `tools/generate/src/l1/probes.ts:352-363` | Overlap exclusion narrowed to `box && isSynthesizedSurfaceId(id)`; genuine `box-*` collisions are reported again. | Resolved |
| `packages/site-schema/src/l1/validate.ts:38,137-144` | `rotateDeg` given its own envelope range with its own message. | Resolved |
| `packages/framework/src/l1/render.ts:70-90` | `cssUrl` remains the sole CSS `url()` sink: `isSafeUrl` scheme check **plus** an independent character allowlist. `grep 'url('` in `render.ts` hits only `cssUrl`/`maskDecls` gradients (numeric/hex only). Defence in depth intact. | — |
| `packages/site-schema/src/l1/schema.ts` (REQ-91/90 forms) | All new forms are `.strict()`, hex-only colours, closed enums, finite numbers. No raw-CSS passthrough anywhere. | — |
| `storage/sites/gigabytealchemy/**` | 2.6 MB of captured third-party site data (incl. a 2.49 MB PNG and a served `.woff2`) added to the repo by the resync plumbing commit `4020a700b` — not by any `[FREE-CODED]` commit. The committed draft is a **stale** pre-BUG-11/REQ-90/REQ-91 fold (`{box:1, text:55}`, no `background`, no `resources`), so it neither exercises nor demonstrates the new folder, and no test references it. | Warning |
| `tools/generate/src/l1/fold.ts:620-630` | An image leaf folds the origin's absolute `currentSrc`/`src`, while `cmdRepro` copies the bundle's mirrored assets next to the page — so a reproduction hotlinks the origin for images, unlike the font path which binds mirrored substance. Safe (scheme-allowlisted) but an unfinished half of "handle→substance". | Warning |
| `tools/generate/src/l1/probes.ts:278,288` | `children.indexOf(child)` inside the per-child flow loop — O(n²) and would mis-path if the same node object appeared twice; a precomputed index map would be clearer. | Nit |
| `tools/generate/src/l1/probes.ts:772-774` | The `components.length === 0` return has textually identical ternary arms (a TS union-narrowing idiom); reads as a bug without a one-line comment. | Nit |
| `packages/framework/src/l1/render.ts` (enum axes) | New closed-enum axes (`textDecoration`, `fontVariantCaps`, `listMarker`, `blendMode`, `border.style`) are interpolated into CSS without a Layer-2 re-check, matching the pre-existing pattern for `textAlign`/`textTransform`/`fontStyle`. Consistent with the current code; the schema enums are the guarantee. | Informational |
| changed prod files | No TODO/FIXME/debugger/`console.log` leftovers in the diff. | — |

### Test evidence validity

Every ticket in the bundle carries independent free-coded UATs: REQ-92 (18), REQ-91 (13), REQ-90 (10), BUG-11 (9), BUG-7 (6), BUG-9 (4), BUG-6 (4), BUG-10 (4), REQ-89 (3), BUG-8 (3). They drive real `foldToL1` / `renderL1Document` / `validateL1` / `evaluateLayout` / `promoteToFlow` / the real `EXTRACT_SCRIPT` under jsdom / the real `cmdRender` and `run(argv)` CLI paths. No internal component is mocked: the only `vi.spyOn` uses are observational — `experimental_AstroContainer.create` (asserting it is *not* called on the L1 path) and `console.log`/`console.error` capture for CLI output. Valid evidence.

## Checklist Compliance

No architecture, security, or design checklist reports exist for this project — `xgd ticket list --type report --filter fields.report_kind=<kind>` returned an empty item list for all three. Sections omitted.

## Smoke Test

Entry points tested (real `bin/1c`, this worktree):

- `1c --help` — exit 0, usage on stdout, **stderr empty** (no `Missing pages directory`). REQ-89 acceptance confirmed on the real binary.
- `1c list` — exit 0, 3 sites listed, **stderr empty**.
- `1c render gigabytealchemy --out /tmp/rg8` — exit 0, `Rendered 2 file(s)`, stderr empty; L1 page emitted with no Astro container.
- `1c l1-gate gigabytealchemy` (no `--ref`) — exit 1 with the intended usage message, no stack trace. A full gate run is not reproducible here: `storage/references/` is gitignored and absent in a fresh reconcile worktree (environmental, not a code defect); the fold→gate seam is covered green by `tests/reconciliation-3probe-gate-evaluator.test.ts` and `tests/bug6-signal-not-drop.test.ts`, which drive `run(argv)` end to end.

## Issues Found

**Critical (must fix)**: none.

**Warnings (should fix — not blocking this bundle)**:

- `storage/sites/gigabytealchemy/**` (2.6 MB, incl. a 2.49 MB PNG) is a stale captured-site artifact swept in by the resync plumbing commit, permanently in git history, referenced by nothing. Consider removing it (or the binary asset) in a follow-up rather than carrying it into `main`.
- Image leaves fold the origin's absolute URL; the mirrored bundle asset is copied but never bound. Worth a follow-up increment so a reproduction is self-contained for images the way it now is for fonts.
- `probes.ts:278,288` — precompute the child index instead of `indexOf` inside the loop.
- `probes.ts:772-774` — add a one-line comment explaining the identical-looking ternary arms (union narrowing).

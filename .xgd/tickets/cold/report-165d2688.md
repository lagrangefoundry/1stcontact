---
uid: report-165d2688
id: REPORT-610
type: report
title: Claude reconciliation_uat_generation report
created_by: xgd
created_at: '2026-07-19T02:25:20.940905+00:00'
updated_at: '2026-07-19T02:25:20.940905+00:00'
completed_at: null
last_field_updated: created_at
fields:
  report_kind: reconciliation_uat_generation
  subject_uid: story-d5de22a5
---

All 5 UATs pass, including the real-Chromium capture in AC-631. Test names all satisfy the `^test_UAT_AC\d+_\w+$` convention.

## Completion Report

```
UATs generated for story story-d5de22a5 (plan item 1 of 8)

Story: story-d5de22a5
Test file(s) created:
  - tests/reconcile-values-diff-fidelity.test.ts

Tests written: 5
Tests passed: 5
Tests failed: 0

test_files_created:
  - "tests/reconcile-values-diff-fidelity.test.ts"
```

### What each UAT verifies (one per AC)

| Test | AC | Boundary & assertion |
|------|-----|----------------------|
| `test_UAT_AC629_rendered_text_extent_delta_when_font_values_match` | AC-629 | `diffManifests`: identical computed font (size/weight/family/letter-spacing), glyph extent +7.4% → `renderedTextBox` delta fires, no `fontSizePx` delta |
| `test_UAT_AC630_rendered_text_extent_suppresses_and_honours_tolerant` | AC-630 | Three cases: ~0.4% long-line → none; extent absent on one side → skipped; ~1.9% → fires by default, absorbed under `{ tolerant: true }` |
| `test_UAT_AC631_surface_fill_is_composited_alpha_colour` | AC-631 | Real `cmdCapturePage` against the committed `req58-treatments.html` fixture: `Translucent` card's captured `surfaceFill` is the blended tint (~#ece6dd), not `#ffffff`; then `diffManifests` flags an opaque-white repro and passes a blended-tint one |
| `test_UAT_AC632_box_border_delta_cases` | AC-632 | Three paired cases: same width/diff colour → `border` delta; identical borders → none; no border either side → none |
| `test_UAT_AC633_duplicate_text_paired_by_nearest_position` | AC-633 | Reversed-order duplicate CTAs at same positions → no false colour swap; a real top-instance colour change surfaces; single-occurrence text unaffected |

### Notes
- Followed the established boundary for this module — the exported `diffManifests` engine ("the same code path the CLI runs," per existing test comments) plus the real capture entry point `cmdCapturePage` for the compositing axis. No internal functions mocked.
- I had to `pnpm --filter @1stcontact/site-schema build` first — the workspace package's `dist/` was absent, which blocked the whole vitest suite from resolving. That's a build artifact, not a code change; no runtime/production or existing-test files were modified.
- No commit made — per the free-coding/reconcile process, commits and the FC→AC rename/orphan handling are the outer reconcile orchestration's responsibility, not this sub-process's.

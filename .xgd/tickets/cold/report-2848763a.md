---
uid: report-2848763a
id: REPORT-976
type: report
title: Claude reconciliation_story_generation report
created_by: xgd
created_at: '2026-07-27T20:39:06.050615+00:00'
updated_at: '2026-07-27T20:39:06.050615+00:00'
completed_at: null
last_field_updated: created_at
fields:
  report_kind: reconciliation_story_generation
  subject_uid: bug-5b7153d2
  plan_item_index: '1'
---

Mutations applied. Summary of what I did and why:

**Intent read first** (BUG-5 body + both comments): the operator's declared scope was stable-identity pairing, deterministic duplicate handling, coverage-gap vs mispairing distinction, and an idempotency suite. The ticket's own implementation note (and COMMENT-304) record a deliberate narrowing — the stable identity is the **occurrence index within a text key** that `buildResponsiveTable` already assigns, **not** a new node-id field. That supersedes the body's "index path / node id" phrasing, so the matrix now says occurrence index.

**Code verified**: `sampleFidelityProbe` (tools/generate/src/l1/probes.ts:392) builds per-key FIFO leaf queues in document order and walks the oracle table per width with a per-key cursor; `buildResponsiveTable` (tools/generate/src/cli/responsive-diff.ts:100) confirms the matching FIFO occurrence ordering, and `elementKey` keys textful elements on normalized text — the same key the probe uses. Four `test_UAT_FC_BUG-5_*` UATs in `tests/bug5-fidelity-pairing.test.ts` pin the four behaviours.

```
Upgrade mutations applied for plan item 1 of 1

Target Stories: story-24098299
Primary Story UID: story-24098299
Stories Modified: 1
ACs Modified: 1
ACs Added: 1
ACs Removed: 0

tickets_modified:
  stories:
    - "story-24098299"   # STORY-86, story_kind feature -> upgrade
  acceptance_criteria:
    modified: ["acceptance_criterion-330b48e4"]   # AC-705
    added: ["acceptance_criterion-9727d55f"]      # AC-724
    removed: []

Progress: 1 of 1 plan items complete
```

- **STORY-86**: sample-fidelity bullet now states the occurrence-identity correspondence rule and its three consequences; added the idempotence identity to the description; in-scope list extended to name the pairing contract. Technical Context gained two notes — that occurrence-index pairing is load-bearing on the fold's FIFO row ordering (a cross-story coupling, plus the explicit intent→implementation narrowing), and that this is **not** the CAP-72 `1c values-diff` duplicate-text pairing.
- **AC-705**: replaced the undefined "the corresponding oracle box" with the explicit pairing rule (keyed by normalized text, paired by occurrence index in document order, per width) plus the repeated-text/coverage-gap/per-occurrence-residual consequences; verification extended with the three repeated-text scenarios.
- **AC-724** (new): the idempotence identity — deterministic re-evaluation and per-occurrence faithfulness with repeated text.

AC-706/707/708/709/710 and all CAP-72/STORY-75 artifacts were left untouched (out of scope). No new stories created, no runtime code touched.

---
uid: report-2425206e
id: REPORT-1004
type: report
title: 'Resync resolve conflicts: 006568bd9e49cc7323728e278446fbe1f1e9b96b'
created_by: xgd
created_at: '2026-07-27T21:45:02.772181+00:00'
updated_at: '2026-07-27T21:45:02.772181+00:00'
completed_at: null
last_field_updated: created_at
result: pass
fields:
  report_kind: resolve_conflicts
  subject_uid: report-510318c4
---

## Files resolved

- `package.json` — **UU** (config file, version scalar). Both sides changed only
  the `version` field: ours `0.0.215` (main-rooted resync tip), incoming
  `0.0.210` (from `fc5d83aff`, which bumped `0.0.209` → `0.0.210` on
  xgd-working). Taking incoming verbatim would move the version *backwards* on
  the resync branch. Resolved to **`0.0.216`** — continuing the branch's
  monotonic sequence, which is exactly how the three preceding replayed picks on
  this branch resolved the same conflict (`34f0b80d6` 212→213, `5acc0d5c0`
  213→214, `2611a9b69` 214→215). This preserves incoming's intent (a free-coded
  commit carries a version bump) without regressing the sequence.
  Flagged for post-merge review per the enrichment rule.

All other files in the cherry-pick auto-merged cleanly (staged `M`/`A`); no
other conflict-class entries were present.

## Incoming changes preserved

Incoming commit `fc5d83afffc21414e29279867944b2a09307e152` — "feat(l1): share one
surface/paint axis group across every node kind [FREE-CODED]" (REQ-98). Verified
each of its 8 touched files:

- `packages/site-schema/src/l1/schema.ts` — `surfaceAxesShape` declared once
  (L434) and `l1SurfaceAxesSchema` exported (L470); spread into `text` (L543) and
  `image` (L551) axis bags and referenced by `slot` (L674), `container`/`control`
  (L740, L761). `l1BoxAxesSchema` is gone. ✅
- `packages/site-schema/src/l1/types.ts` — `L1SurfaceAxes` present (L65);
  `L1BoxAxes` removed. ✅
- `packages/framework/src/l1/render.ts` — single `surfaceDecls()` emitter (L255)
  called from all five kind branches (L695, 769, 785, 793, 811). ✅
- `packages/site-schema/src/l1/validate.ts` — group bounded once, closing the two
  gaps the per-kind checks left: `borderLeft.widthPx` now length-checked (L254)
  and `backgroundImageUrl` scheme-checked for any kind (L256-259). ✅
- `tools/generate/src/l1/assets.ts` — `localizeAssets` resolves
  `axes.backgroundImageUrl` on any kind (L85-92). ✅
- `tools/generate/src/l1/fold.ts` — all three `L1BoxAxes` sites retyped to
  `L1SurfaceAxes`. ✅
- `tests/req98-uniform-surface-axes.test.ts` — added (405 lines). ✅
- `package.json` — see above.

### Coexistence with the ours-side change

The immediately preceding pick (`2611a9b69`, REQ-97) added `sizing` to
`l1TextSchema`, touching the same schema region incoming rewrites. Both survive:
`l1TextSchema` (L562-597) carries REQ-97's `sizing: l1AxisSizingSchema.optional()`
*and* its axes bag spreads REQ-98's `surfaceAxesShape`. Nothing was discarded from
either side.

### Verification run

- `npx vitest run tests/req98-uniform-surface-axes.test.ts` — **5 passed**.
- `npx tsc --noEmit -p packages/site-schema/tsconfig.json` — **exit 0**.
- Repo-wide grep for `L1BoxAxes` / `l1BoxAxesSchema` in sources — no hits. The
  only remaining references are in `packages/site-schema/dist/**` (stale compiled
  output, regenerated by `pnpm -r build`; not part of this resolution).
- No conflict markers remain anywhere in the tree.

Staging is clean: `M package.json`, `M packages/framework/src/l1/render.ts`,
`M packages/site-schema/src/l1/{schema,types,validate}.ts`,
`A tests/req98-uniform-surface-axes.test.ts`,
`M tools/generate/src/l1/{assets,fold}.ts`. Net change from HEAD is non-empty.
`CHERRY_PICK_HEAD` left intact — no `--continue`/`--skip`/`--quit`/`--abort` was run.

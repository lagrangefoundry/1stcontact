---
uid: report-3c651178
id: REPORT-820
type: report
title: 'Capability-Intent Alignment: Framework Responsive Per-Breakpoint Dials (level=ac)'
created_by: xgd
created_at: '2026-07-23T09:29:14.235932+00:00'
updated_at: '2026-07-23T09:29:14.235932+00:00'
completed_at: null
last_field_updated: created_at
result: pass
fields:
  report_kind: capability_validation
  subject_uid: capability-bd0b722e
  level: ac
  violations: 0
  warnings: 0
  needs_review_count: 0
---

# Capability-Intent Alignment: Framework Responsive Per-Breakpoint Dials
# Level: ac

**Result**: PASS
**Violations**: 0
**Warnings**: 0
**Needs review**: 0

**Attempt**: 6 (previous_attempt_count=5). All 5 prior attempts were **story-level**
cycles (REPORT-807/809/811/815/817 validate; 808/810/812/814/816/818 fix). The
story level reached PASS on this attempt (REPORT-819: 0/0/0) once the operator
recorded the explicit RETIRE disposition (CAP-68 `active` → `superseded`, STORY-81
archived, body aligned). This is the **first** AC-level check, and it inherits an
aligned story body as its working reference (per the level cascade). State re-read
fresh against tickets AND source; deterministic — same state → same verdict.

## Cumulative Intent Considered

Both intents touching CAP-68 are `bundle` tickets, both `free_and_reconciled`.

| Intent ID | Status | When | Asked / changed | Counts? |
|---|---|---|---|---|
| BUNDLE-6 (bundle-ab9e0cb6 = REQ-58+59+62+61) | free_and_reconciled | merged 7a42e182 (2026-07-17) | Originating intent (`intent_uid` of STORY-81). Landed responsive per-breakpoint **module dials** (`{base,sm,md,lg,xl}`) on spacing-bearing layout modules + header `navCollapse`. | YES |
| BUNDLE-7 (bundle-31e474b9 = REQ-63+79+82+83+84+2) | free_and_reconciled | merged edeb1c2c (2026-07-22) | `updated_by` of STORY-81. REQ-79/REQ-84 pivot (commit 1a2faeee) **deleted** the semantic layout modules + every dial (all per-breakpoint length dials and `navCollapse`); REQ-82/83 re-homed per-viewport variation to the L1 substrate (owned by CAP-70/CAP-71). | YES (retires BUNDLE-6's mechanism) |

**Cumulative intent = fully RETIRED.** BUNDLE-7 retired every behaviour BUNDLE-6
landed under CAP-68. A retired intent's behaviour must NOT appear in the matrix.
The surviving concern (per-viewport geometry) is owned by CAP-70/CAP-71, not CAP-68.

## Alignment Ledger

At AC level the elements are the ACs under CAP-68's stories. CAP-68 has one story,
STORY-81, which is `story_kind=upgrade` but **archived**, and its (aligned) body
describes only retired behaviour.

| Element | Intents aligned to | Outcome |
|---|---|---|
| STORY-81 (story-3569e1a4) | BUNDLE-6 (landed), BUNDLE-7 (retired) | archived; body describes retired mechanism only; 0 ACs |
| ACs under STORY-81 | — | none exist (`fields.story_uid=story-3569e1a4` → 0 tickets) |

## Findings

| # | Severity | Property | Element | Resolution category | Issue | Suggested edit |
|---|---|---|---|---|---|---|
| 1 | info | coverage | STORY-81 | — | STORY-81 is an upgrade story (normally AC-bearing) with 0 ACs. Correct: its cumulative intent is fully retired by BUNDLE-7 and the story is archived — there is no active behavioural surface for an AC to cover. Zero ACs faithfully expresses zero active behaviour. Not an `ac-add` gap. | none |
| 2 | info | consistency | (ACs under STORY-81) | — | No ACs exist, so no AC can be inconsistent with the story body or claim retired behaviour. The attempt-1 false "single AC repoints the capability" claim was already removed at story level (fix 00a518c5). | none |
| 3 | info | exclusivity | (ACs under STORY-81) | — | Zero ACs → no overlapping/duplicate criteria possible. | none |
| 4 | info | — | AC-694 | — | AC-694 ("Capture emits advisory structural-hint sidecar … breakpoints") matches on the word "breakpoint" but belongs to the capture/L1 tree (CAP-71), not STORY-81/CAP-68. No cross-ownership leak. | none |

## Notes for the Editor

Nothing to repair at AC level. CAP-68 is a validly superseded/retired container:
the only story (STORY-81) is archived, its body aligns to the retired cumulative
intent, and it carries zero ACs — which is the correct expression of fully-retired
behaviour, not a coverage gap. Code corroborates the disposition: `navCollapse` /
`perBreakpoint` / `breakpointDial` = 0 hits across `packages/` and `tools/`; the L1
successor (`l1KeyframeSchema` / `l1SegmentSchema` in
`packages/site-schema/src/l1/schema.ts`, `foldToL1` in `tools/generate/src/l1/fold.ts`)
exists and is owned by active CAP-70/CAP-71. AC level passes cleanly.

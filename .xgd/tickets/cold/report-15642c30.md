---
uid: report-15642c30
id: REPORT-658
type: report
title: Claude reconciliation_uat_generation report
created_by: xgd
created_at: '2026-07-19T03:39:52.292213+00:00'
updated_at: '2026-07-19T03:39:52.292213+00:00'
completed_at: null
last_field_updated: created_at
fields:
  report_kind: reconciliation_uat_generation
  subject_uid: story-46e3b3c7
---

All 8 tests pass — one UAT per acceptance criterion, names match the `test_UAT_AC\d+_\w+` convention.

## Completion Report

```
UATs generated for story story-46e3b3c7 (plan item 8 of 8)

Story: story-46e3b3c7
Test file(s) created:
  - tests/reconciliation-repro-treatments.test.ts

Tests written: 8
Tests passed: 8
Tests failed: 0

test_files_created:
  - "tests/reconciliation-repro-treatments.test.ts"
```

**AC → test mapping (1:1):**

| AC | Test | Module behaviour verified |
|----|------|---------------------------|
| AC-674 | `test_UAT_AC674_card_veil_paints_translucent_white_none_keeps_solid_surface` | services-grid `cardVeil` steps 40–90 → `rgba(255,255,255,0.N)`; `none` keeps `var(--color-surface)` |
| AC-675 | `test_UAT_AC675_card_border_none_drops_hairline_but_accented_card_keeps_left_bar` | `cardBorder: none` zeroes hairline, accented card re-asserts left bar; default = 1px hairline |
| AC-676 | `test_UAT_AC676_field_labels_placeholder_moves_label_into_placeholder_and_hides_label` | `fieldLabels: placeholder` moves label→placeholder + visually-hides label (kept in DOM); `above` default |
| AC-677 | `test_UAT_AC677_submit_inline_lays_field_and_button_on_one_row` | `submitInline` → `submit-inline` row layout; unset stacks |
| AC-678 | `test_UAT_AC678_submit_color_paints_button_fill_with_literal_or_role` | `submitColor` `#hex` / role → button `background`; unset keeps `submit-primary` |
| AC-679 | `test_UAT_AC679_copyright_override_renders_verbatim_line_else_generated_default` | verbatim copyright vs generated `© <BUILD_YEAR> <holder>` |
| AC-680 | `test_UAT_AC680_text_color_renders_footer_body_in_literal_or_role_else_surface_default` | footer `textColor` `#hex`/role → `--fc-text`; unset omits it |
| AC-681 | `test_UAT_AC681_link_color_renders_footer_links_in_literal_or_role_else_surface_default` | footer `linkColor` `#hex`/role → `--fc-link`; unset omits it |

**Notes:**
- Tests assert **observable rendered behaviour** via Astro's container SSR path (the same path the generator uses), matching the existing `tests/req58-framework-gaps.test.ts` pattern. Markup assertions read the rendered HTML; CSS-rule assertions read aggregated module CSS via `getModuleCss()` (the container drops scoped `<style>`).
- Per the story's flagged intent/code divergence, the ACs describe rendered outcomes, so I asserted **render output only** — not the module `meta` schema — for `submitInline`/`submitColor` and the footer `copyright`/`textColor`/`linkColor` overrides (which are consumed by render code but not yet declared in `meta`). All tests pass against current code; no runtime code or existing tests were modified.

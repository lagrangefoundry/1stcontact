
# ============================================================================
# XGD Managed Project — Merge Drivers
# ============================================================================
#
# XGD_GITATTRIBUTES_MARKER
#
# The xgd-ticket-recent driver (most-recent-updated_at-wins for ticket file
# conflicts) was removed by xgd's REQ-816: it silently discarded legitimate
# concurrent edits. Ticket files now merge via plain git 3-way merge; real
# content conflicts surface as ordinary conflict markers. See xgd's
# xgd_source/system_docs/FRAGILE-INTENT-LIFECYCLE.md §5.2 / DOC-986.

# Self-rule: if .gitattributes itself conflicts, keep the rebase-target
# (main) version — it is always the superset of registered drivers.
.gitattributes merge=ours
